/* 出題慣例見專案根目錄 AUTHORING.md:
 *   - answer 一律為 0(正確答案寫在第一個選項),顯示順序由 quiz.js 依題目 id 洗牌
 *   - 詳解禁止用「選項 A/B/C」字母指涉,必須直接描述選項內容
 *   - 有程式碼的題目一律附 walkthrough(逐行說明);反面案例必須同時附上 √ 正確寫法 */
window.RUST_LESSONS = window.RUST_LESSONS || {};
window.RUST_LESSONS["lesson2-12"] = {
  id: "lesson2-12",
  title: "Cargo 生態與實戰慣用法",
  goal: "看懂 feature、profile、workspace 這些專案層級的設定各自在管什麼,知道常用 crate 與工具的分工,並能把整個進階類學過的東西組合成一個實際情境。",
  primer: {
    intro: "Cargo 同時是 Rust 的建置工具與套件管理器。你手寫維護的是 Cargo.toml——宣告這個專案叫什麼、要哪些相依套件、以及各種建置設定;cargo 依它解析出精確版本、寫進自動產生的 Cargo.lock,再從 crates.io 把原始碼抓下來一起編譯。所有相依都是原始碼編譯進你的執行檔,沒有執行期的套件載入這件事。",
    examples: [
      {
        code: "[package]\nname = \"hello\"\nversion = \"0.1.0\"\nedition = \"2021\"\n\n[dependencies]\nserde_json = \"1\"",
        note: "最小的 Cargo.toml:[package] 是這個專案自己的中繼資料,[dependencies] 一行一個相依套件,右邊是可接受的版本範圍。",
        lang: "ini",
      },
      {
        code: "cargo new hello\ncargo add serde_json\ncargo build\ncargo run\ncargo test",
        note: "日常會用到的五個指令:建立專案、把相依寫進 Cargo.toml、編譯、編譯並執行、跑測試——後四個都在專案目錄裡執行。",
        lang: "bash",
      },
    ],
    csharp: "對照 .NET:Cargo.toml 相當於 .csproj,Cargo.lock 相當於(很少人啟用的)packages.lock.json,crates.io 相當於 nuget.org,cargo 則一口氣包辦了 dotnet CLI 與 NuGet 的工作。最大的差別是 Rust 的相依是原始碼、在你的機器上編譯,不是預先編好的組件。",
  },
  questions: [
    {
      id: "2-12-01",
      question: "專案的 Cargo.toml 裡有 [features] 區段,宣告了 default = [] 與 csv = []。直接執行 cargo build(沒有加 --features),以下程式碼的結果是?",
      questionCode: "#[cfg(feature = \"csv\")]\nfn export_csv(rows: &[&str]) -> String {\n    rows.join(\",\")\n}\n\nfn main() {\n    println!(\"{}\", export_csv(&[\"a\", \"b\"]));\n}",
      options: [
        { text: "編譯失敗:error[E0425]: cannot find function `export_csv` in this scope,編譯器還會補上 note: found an item that was configured out 與 the item is gated behind the `csv` feature——feature 沒啟用時,被 #[cfg] 保護的那幾行根本沒有進入這次編譯" },
        { text: "可以編譯,但執行到 export_csv 時才 panic,訊息是 feature \"csv\" is not enabled" },
        { text: "可以編譯,未啟用 feature 的函式會被換成一個什麼都不做的版本,所以印出空字串" },
        { text: "編譯失敗,但原因是 [features] 裡的 csv = [] 沒有列出任何東西,空的 feature 宣告不合法" },
      ],
      answer: 0,
      explanation: `feature 是編譯期的開關,不是執行期的設定。cargo 看到你啟用了哪些 feature,就把對應的 --cfg feature="csv" 傳給 rustc;沒有啟用時,#[cfg(feature = "csv")] 底下那個項目在這次編譯裡等於被整段刪掉——所以錯誤不是「不能呼叫」,而是「找不到這個函式」。編譯器特別加的那句 the item is gated behind the csv feature,就是在提醒你它看到了一個被 cfg 排除掉的同名項目。
「執行到才 panic」把 feature 當成了執行期的設定檔開關,這是最常見的誤解:程式碼都還沒被編進去,哪來的執行期。「空的 feature 宣告不合法」也不對——csv = [] 完全合法,中括號裡放的是「啟用這個 feature 時要連帶啟用的其他 feature 或可選相依」,沒有就留空。
修法有三條:建置時加 cargo build --features csv、把 default 改成 ["csv"]、或是替沒啟用的情況補一個 #[cfg(not(feature = "csv"))] 的備援版本(見下方正確寫法),讓程式碼兩種設定都編得過。`,
      walkthrough: [
        {
          label: "X Cargo.toml (the csv feature is declared but off)",
          lang: "ini",
          lines: [
            { code: "[package]", note: "這個專案自己的中繼資料。" },
            { code: "name = \"report\"", note: "套件名稱,也決定預設的執行檔名。" },
            { code: "version = \"0.1.0\"", note: "本專案的版本。" },
            { code: "edition = \"2021\"", note: "語言版次。" },
            { code: "", note: "" },
            { code: "[features]", note: "feature 宣告區段,一行一個開關。" },
            { code: "default = []", note: "⛔ 問題出在這裡:default 是「什麼都不加時會啟用哪些 feature」的清單,現在是空的,所以 csv 預設關閉。" },
            { code: "csv = []", note: "宣告有一個叫 csv 的 feature;中括號裡是啟用它時要連帶啟用的其他 feature 或可選相依,這裡不需要,所以留空——這一行本身完全合法。" },
          ],
        },
        {
          label: "X src/main.rs (won't compile)",
          lines: [
            { code: "#[cfg(feature = \"csv\")]", note: "條件編譯屬性:只有在 csv feature 被啟用時,底下這個項目才存在於編譯結果裡。" },
            { code: "fn export_csv(rows: &[&str]) -> String {", note: "這次編譯沒有啟用 csv,所以整個函式連同簽名都不會被編進去。" },
            { code: "    rows.join(\",\")", note: "把每個欄位用逗號接起來——不過這次根本走不到這裡。" },
            { code: "}", note: "函式結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    println!(\"{}\", export_csv(&[\"a\", \"b\"]));", note: "⛔ 這裡編譯失敗:error[E0425]: cannot find function `export_csv` in this scope,附註 the item is gated behind the `csv` feature。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "重點在錯誤代碼:E0425 是「找不到這個名字」,跟權限或可見性無關。被 cfg 排除的項目對編譯器來說就是不存在。",
        },
        {
          label: "√ Correct version: Cargo.toml",
          lang: "ini",
          lines: [
            { code: "[features]", note: "同一個區段。" },
            { code: "default = [\"csv\"]", note: "改好了:把 csv 放進預設清單,cargo build 就會啟用它。想保持預設關閉也行,改成建置時加 --features csv。" },
            { code: "csv = []", note: "feature 本身的宣告不用動。" },
          ],
        },
        {
          label: "√ Correct version (complete, compiles with or without the feature)",
          lines: [
            { code: "#[cfg(feature = \"csv\")]", note: "啟用 csv 時採用這個版本。" },
            { code: "fn export(rows: &[&str]) -> String {", note: "真正的實作。注意兩個版本的簽名必須一致,呼叫端才不用跟著改。" },
            { code: "    rows.join(\",\")", note: "產生逗號分隔的內容。" },
            { code: "}", note: "函式結束。" },
            { code: "", note: "" },
            { code: "#[cfg(not(feature = \"csv\"))]", note: "沒啟用 csv 時採用這個版本。有了這一組互斥的 cfg,不論開關如何都剛好有一個 export 存在。" },
            { code: "fn export(_rows: &[&str]) -> String {", note: "備援版本。參數前面加底線,表示刻意不使用,避免 unused 警告。" },
            { code: "    String::from(\"csv support is off\")", note: "回傳一句說明,讓使用者知道這個建置沒有包含該功能。" },
            { code: "}", note: "函式結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    println!(\"{}\", export(&[\"a\", \"b\"]));", note: "呼叫端完全不必管 feature:cargo run 印出 csv support is off,cargo run --features csv 印出 a,b。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "這組互斥 cfg 是 library 常見的寫法:功能可以被關掉,但關掉之後 API 仍然存在,呼叫端不會因為別人的建置設定而編不過。",
        },
      ],
      csharp: `NuGet 沒有等價機制。C# 最接近的是 #if DEBUG 這類前置處理符號,但那是專案自己在 csproj 的 DefineConstants 裡設的,不會沿著套件相依傳遞——你沒辦法「引用某個套件時順便打開它的某個功能」。Rust 的 feature 是套件層級的,而且是累加的:相依圖上任何一個 crate 要求某個 feature,那個 feature 對整個建置就是開的。這也是為什麼慣例要求 feature 必須是「加東西」而不是「改行為」,否則開關會互相打架。`,
    },
    {
      id: "2-12-02",
      question: "同一支程式分別用 cargo run 與 cargo run --release 執行,輸出是?",
      questionCode: "fn main() {\n    println!(\"debug_assertions = {}\", cfg!(debug_assertions));\n    let limit = 3;\n    debug_assert!(limit > 5, \"limit too small: {}\", limit);\n    println!(\"still running\");\n}",
      options: [
        { text: "cargo run 印出 debug_assertions = true 之後 panic(limit too small: 3)、以 101 結束;cargo run --release 印出 debug_assertions = false 與 still running、正常結束——dev 與 release 是兩組不同的編譯設定,差別不只在速度" },
        { text: "兩邊都印出 debug_assertions = true 然後 panic:debug_assert! 跟 assert! 一樣,永遠都會檢查" },
        { text: "兩邊都印出 debug_assertions = false 與 still running:cfg! 是執行期才求值的函式,跟用哪個 profile 編譯無關" },
        { text: "兩邊輸出完全一樣,--release 只是編譯慢一點、跑快一點,不改變任何程式行為" },
      ],
      answer: 0,
      explanation: `cargo 內建兩組 profile:dev(cargo build / cargo run 預設)是 opt-level = 0、debug = true、debug-assertions = true、overflow-checks = true;release(加 --release)是 opt-level = 3,後三項全部關掉。兩者的產物分別放在 target/debug 與 target/release,是兩份獨立的編譯結果,互不覆蓋。
cfg!(debug_assertions) 在編譯期就被換成 true 或 false 的字面值,所以兩個 profile 印出不同的值;debug_assert! 在 release 下整段消失,連運算式都不會被求值。「只差在速度」是最需要破的誤解:整數溢位在 dev 會 panic、在 release 會照二補數環繞,也是同一組開關造成的行為差異。
需要單獨調整時可以在 Cargo.toml 覆寫,例如在 [profile.release] 寫 overflow-checks = true,就能保留 release 的最佳化又保留溢位檢查。`,
      walkthrough: [
        {
          label: "⊕ Question code — walkthrough (dev panics, release does not)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    println!(\"debug_assertions = {}\", cfg!(debug_assertions));", note: "cfg! 是編譯期求值的巨集:dev 編譯時整個運算式變成字面值 true,release 變成 false。" },
            { code: "    let limit = 3;", note: "一個故意不符合下一行條件的值。" },
            { code: "    debug_assert!(limit > 5, \"limit too small: {}\", limit);", note: "只有在 debug-assertions 開啟時才會被編進來。dev:條件不成立,panic 並印出 limit too small: 3;release:這一行整段不存在。" },
            { code: "    println!(\"still running\");", note: "dev 走不到這裡(前一行已經 panic);release 會印出來。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "⊕ The two built-in profiles (Cargo's defaults)",
          lang: "ini",
          lines: [
            { code: "[profile.dev]", note: "cargo build / cargo run 預設用的 profile,不寫也存在,寫出來是為了覆寫其中幾項。" },
            { code: "opt-level = 0", note: "完全不最佳化,換到最快的編譯速度與最好讀的除錯資訊。" },
            { code: "debug = true", note: "產生除錯符號,所以 panic 訊息帶得出檔名與行號。" },
            { code: "debug-assertions = true", note: "debug_assert! 與 cfg!(debug_assertions) 的總開關。" },
            { code: "overflow-checks = true", note: "整數溢位時 panic;預設值跟著 debug-assertions 走。" },
            { code: "", note: "" },
            { code: "[profile.release]", note: "加 --release 時用的 profile。" },
            { code: "opt-level = 3", note: "全力最佳化,編譯慢很多,執行通常快上一個數量級。" },
            { code: "debug = false", note: "不產生除錯符號,執行檔小很多。" },
            { code: "debug-assertions = false", note: "debug_assert! 整段消失。" },
            { code: "overflow-checks = false", note: "整數溢位改成二補數環繞,不再 panic。" },
          ],
          outro: "要發佈的東西一律用 --release 量效能:拿 dev 的執行結果去評估 Rust 的速度,得到的數字通常沒有參考價值。",
        },
      ],
      csharp: `對應 C# 的 Debug / Release 組態:debug_assert! 對應只在 DEBUG 常數下編譯的 Debug.Assert,cfg!(debug_assertions) 對應 #if DEBUG。差別在落差的幅度——C# 的 Debug 組態只是 IL 不最佳化,JIT 在執行期仍然會最佳化;Rust 的 dev 是真的 opt-level 0,泛型與迭代器那些「零成本抽象」在 dev 下一個都沒被消掉,所以同一支程式 dev 與 release 差上十倍很常見。`,
    },
    {
      id: "2-12-03",
      question: "一個 workspace 的根目錄 Cargo.toml 如下,底下有 core/ 與 cli/ 兩個 crate(cli 以 path 相依 core)。在根目錄執行 cargo build 之後,哪個敘述正確?",
      questionCode: "[workspace]\nresolver = \"2\"\nmembers = [\"core\", \"cli\"]",
      questionLang: "ini",
      options: [
        { text: "整個 workspace 只有根目錄那一份 Cargo.lock 與一個 target/ 目錄,兩個 member 的產物都放在裡面;cargo build 會一次建置所有 member,共用的相依套件只會被編譯一次,要單獨處理某一個 member 就加 -p(例如 cargo run -p cli)" },
        { text: "每個 member 各自產生自己的 Cargo.lock 與 target/,彼此完全獨立,放在同一個資料夾只是為了方便管理" },
        { text: "[workspace] 只是給人看的清單,cargo build 仍然只建置根目錄自己的 crate,要建 member 得 cd 進各自的目錄" },
        { text: "根目錄的 Cargo.toml 必須同時也是一個 package(要有 [package] 與 src/),否則 cargo 會拒絕這個設定檔" },
      ],
      answer: 0,
      explanation: `workspace 的定義就是「共用一份 Cargo.lock 與一個 target/ 目錄的一組 package」。共用 lock 代表整個 workspace 對同一個相依只會解析出一致的版本;共用 target 代表相同的相依只編譯一次,所有 member 一起受惠——這也是大型專案把東西切成多個 crate 卻不會讓編譯時間爆炸的原因。
只有 [workspace] 而沒有 [package] 的根 Cargo.toml 叫 virtual manifest,是完全合法而且很常見的做法(根目錄只負責組織,不產生任何程式碼)。「每個 member 各自獨立」則正好相反:member 目錄底下不會出現 target/,也不會有自己的 Cargo.lock。
常用指令:cargo build 建全部、cargo build -p core_lib 只建一個、cargo test --workspace 跑全部的測試。`,
      walkthrough: [
        {
          label: "⊕ Root Cargo.toml — walkthrough (a virtual manifest)",
          lang: "ini",
          lines: [
            { code: "[workspace]", note: "宣告這個目錄是一個 workspace 的根。整份檔案沒有 [package],所以它本身不產生任何 crate。" },
            { code: "resolver = \"2\"", note: "指定相依解析器版本。virtual manifest 沒有 edition 可以推,所以要自己寫,否則 cargo 會警告。" },
            { code: "members = [\"core\", \"cli\"]", note: "列出納入這個 workspace 的子目錄,每個子目錄各自有一份帶 [package] 的 Cargo.toml。" },
          ],
        },
        {
          label: "⊕ What you get after cargo build",
          lang: "bash",
          lines: [
            { code: "cargo build", note: "在根目錄執行:core 與 cli 一起被建置,共用的相依只編一次。" },
            { code: "ls", note: "根目錄多出兩樣東西:Cargo.lock 與 target/——整個 workspace 就這一份。" },
            { code: "ls core", note: "member 目錄裡只有 Cargo.toml 與 src/,沒有自己的 lock,也沒有自己的 target/。" },
            { code: "cargo run -p cli", note: "-p 指定要操作哪一個 member。多個 member 都有執行檔時,這是最不會搞錯的寫法。" },
            { code: "cargo test --workspace", note: "一口氣跑完所有 member 的測試。" },
          ],
        },
      ],
      csharp: `結構上對應一個 .sln 底下多個 .csproj,連「專案之間用相對路徑互相參考」都很像(Rust 這邊是 core_lib = { path = "../core" })。最大的差別在輸出位置:C# 的 bin/ 與 obj/ 長在各個專案底下,Rust 反過來全部集中在 workspace 根的 target/。這個集中不只是整齊——它是共用編譯快取的前提。`,
    },
    {
      id: "2-12-04",
      question: "同一個 workspace 裡,core 的 Cargo.toml 寫 itoa = \"1.0.5\",cli 寫 itoa = \"1.0\"。作者的想法是「這樣兩邊各自鎖住自己要的版本,互不影響」。實際上會發生什麼?",
      options: [
        { text: "兩個宣告落在同一個相容範圍(1.x),cargo 只會解析出一個同時滿足兩邊的版本(例如 1.0.18)寫進根目錄那份唯一的 Cargo.lock,兩個 member 共用同一份編譯產物;只有當要求跨越不相容界線(例如 0.4 與 1.0)時,兩個版本才會並存" },
        { text: "cargo 會替每個 member 各自鎖定它宣告的版本:core 拿到 1.0.5、cli 拿到 1.0 系列最新版,兩份各自編譯" },
        { text: "cargo build 會直接失敗,要求兩個 member 把版本字串改成完全一致" },
        { text: "後解析到的那個 member 會覆蓋前一個,所以整個 workspace 都用 cli 宣告的 \"1.0\",core 寫的 1.0.5 等於沒寫" },
      ],
      answer: 0,
      explanation: `這題沒有任何編譯錯誤——cargo build 會成功,rustc 一句話都不會說。錯的是心智模型,而這種錯誤最難發現。
一個 workspace 只有一份 Cargo.lock,所以解析器的工作是替每個相依找出「同時滿足所有 member 宣告」的版本。"1.0.5" 是下限、"1.0" 也是下限,兩者都被 1.0.18 滿足,於是統一成一個版本、編譯一次、兩邊共用。「後面覆蓋前面」的說法剛好搞反了:core 要求的 1.0.5 是硬下限,解析器不會挑一個比它低的版本。
不相容的情況才會並存:如果 core 寫 "0.4"、cli 寫 "1.0",lock 裡會同時出現 0.4.8 與 1.0.18,兩份都編進最終的執行檔——用 cargo tree -d 可以把這種重複列出來。真正麻煩的是型別:兩個版本的同名型別在編譯器眼中是不同型別,把 0.4 的值傳給要 1.0 的函式,會得到「expected struct itoa::Buffer, found struct itoa::Buffer」這種看起來很荒謬的錯誤訊息。
想讓整個 workspace 的版本宣告只有一處,慣用做法是把它提到根 manifest 的 [workspace.dependencies](見下方正確寫法)。`,
      walkthrough: [
        {
          label: "X The wrong mental model (declared twice, in two members)",
          lang: "ini",
          lines: [
            { code: "# core/Cargo.toml", note: "第一個 member 的相依宣告。" },
            { code: "[dependencies]", note: "相依區段。" },
            { code: "itoa = \"1.0.5\"", note: "⛔ 誤解的起點:這不是「core 專用的版本」,而是「core 能接受的最低版本」。它只是解析器要滿足的其中一個條件。" },
            { code: "", note: "" },
            { code: "# cli/Cargo.toml", note: "第二個 member 的相依宣告。" },
            { code: "[dependencies]", note: "相依區段。" },
            { code: "itoa = \"1.0\"", note: "另一個條件。兩個條件都指向 1.x,於是被統一成同一個版本——不會變成兩份。" },
            { code: "core_lib = { path = \"../core\" }", note: "cli 以路徑相依 core;workspace 內部的相依都這樣寫,不經過 crates.io。" },
          ],
          outro: "cargo build 成功,Cargo.lock 裡 itoa 只出現一次(1.0.18)。想確認的話跑 cargo tree -d:沒有重複時它會回你 nothing to print。",
        },
        {
          label: "√ Correct version: declare it once at the workspace root",
          lang: "ini",
          lines: [
            { code: "# Cargo.toml(workspace 根)", note: "把共用相依集中宣告的地方。" },
            { code: "[workspace]", note: "workspace 根的宣告。" },
            { code: "resolver = \"2\"", note: "virtual manifest 要自己指定解析器版本。" },
            { code: "members = [\"core\", \"cli\"]", note: "兩個 member。" },
            { code: "", note: "" },
            { code: "[workspace.dependencies]", note: "整個 workspace 的相依清單:版本只寫在這裡一次,要升級也只改這一行。" },
            { code: "itoa = \"1.0\"", note: "唯一的版本宣告。" },
            { code: "", note: "" },
            { code: "# core/Cargo.toml 與 cli/Cargo.toml 都改寫成:", note: "兩個 member 的寫法變得一模一樣。" },
            { code: "[dependencies]", note: "相依區段。" },
            { code: "itoa = { workspace = true }", note: "繼承根 manifest 的宣告。從此不可能出現「兩邊寫了不同版本」這件事,cargo tree -d 也就沒有東西可印。" },
          ],
        },
      ],
      csharp: `.NET 也有同樣的情境:一個 solution 裡兩個專案引用同一套件的不同版本。差別在解決的時機——.NET 通常在執行期靠組件繫結(以及 binding redirect)處理,常見的 MissingMethodException 就是這樣炸出來的。Rust 沒有執行期載入這一層:相容的版本在解析時就被統一,不相容的兩份都編進去且型別互不相通,所有後果都在編譯期結清。至於 [workspace.dependencies],對應的是 .NET 的 Directory.Packages.props(中央套件版本管理),用途幾乎一樣。`,
    },
    {
      id: "2-12-05",
      question: "一個 Rust 專案要做這五件事:讀寫 JSON 設定檔、解析命令列參數、產生亂數、用正規表示式檢查輸入、處理日期。標準庫裡這些功能各在哪裡?",
      options: [
        { text: "標準庫一個都沒有:JSON 用 serde 加 serde_json、命令列參數用 clap、亂數用 rand、正規表示式用 regex、日期用 chrono。std 只給了 std::env::args 的原始字串與 std::time 的時間點,沒有解析、沒有格式化、沒有年月日" },
        { text: "std 幾乎都有:std::json、std::regex、std::random 都存在,外部 crate 只是提供更快的實作" },
        { text: "std 有亂數(std::random)與日期(std::time::Date),JSON 與正規表示式才需要外部 crate" },
        { text: "這些現在是外部 crate,但都由官方維護、最終會被搬進標準庫;Rust 的方向是讓 std 逐步涵蓋這些常用功能" },
      ],
      answer: 0,
      explanation: `std 刻意保持小,原因在 1-15 講過:std 跟編譯器綁在一起出貨,一旦納入就要永遠維持相容、再也不能改。序列化格式、CLI 慣例、時間函式庫都還在演進,放進 std 等於把整個生態鎖死在某一版的設計上。
「最終會被搬進 std」是很自然但錯誤的推論。rand 確實由官方相關團隊維護,可是它至今仍在 crates.io——而且正因為留在外面,它才能發布 0.9 這種帶破壞性改動的版本,慢慢把 API 修好。這是刻意的分工,不是過渡狀態。
std 實際有的東西要分清楚:std::env::args 給得到參數字串,但不幫你解析旗標與子命令;std::time::SystemTime 與 Instant 給得到時間點與間隔,但沒有「幾年幾月幾日」與格式化,那是 chrono(或 time)的守備範圍。`,
      walkthrough: {
        label: "⊕ A typical Cargo.toml — who does what",
        lang: "ini",
        lines: [
          { code: "[dependencies]", note: "相依區段。以下每一個都是 crates.io 上事實上的標準選擇。" },
          { code: "serde = { version = \"1\", features = [\"derive\"] }", note: "序列化框架本身:定義 Serialize / Deserialize 兩個 trait,不綁定任何格式。" },
          { code: "serde_json = \"1\"", note: "serde 的 JSON 格式實作。換成 toml、serde_yaml 就能讀寫別的格式,型別那一側完全不用改。" },
          { code: "clap = { version = \"4\", features = [\"derive\"] }", note: "命令列參數解析:旗標、子命令、說明文字、錯誤訊息一次搞定。" },
          { code: "rand = \"0.9\"", note: "亂數。std 沒有亂數產生器,連洗牌與取樣都在這裡。" },
          { code: "regex = \"1\"", note: "正規表示式。刻意不支援回溯,換到「比對時間與輸入長度成線性」的保證。" },
          { code: "chrono = \"0.4\"", note: "日期時間:年月日、時區、格式化與解析。" },
          { code: "reqwest = \"0.12\"", note: "HTTP 用戶端。std 連 TCP 都只給到 std::net,HTTP 以上全部交給生態。" },
          { code: "tokio = { version = \"1\", features = [\"full\"] }", note: "async 執行期。async/await 是語言語法,但「誰來驅動那些狀態機」std 不提供,要自己挑一個。" },
        ],
        outro: "看 crate 的 Cargo.toml 幾乎就看懂了這個專案在做什麼——這是 Rust 生態少數幾個「沒有官方標準卻高度一致」的地方:多數領域都有一個大家公認的選擇。",
      },
      csharp: `.NET 走的是相反的路:BCL 隨 runtime 一起版本化出貨,所以 System.Text.Json、DateTime、Regex、Random 全都內建,升級 .NET 版本就一起升級。Rust 沒有「runtime 出貨」這一層,std 只能往前相容,於是把演進中的東西推到 crates.io。實務上的影響是:C# 開發者習慣「先找 BCL 有沒有」,在 Rust 要改成「先找這個領域的事實標準 crate 是哪一個」。順帶一提,命令列解析在 .NET 其實也走 NuGet(System.CommandLine),跟 Rust 的情況一樣。`,
    },
    {
      id: "2-12-06",
      question: "Cargo.toml 的 [dependencies] 寫的是 serde = \"1\" 與 serde_json = \"1\"。以下 src/main.rs 執行 cargo build 的結果是?",
      questionCode: "use serde::{Serialize, Deserialize};\n\n#[derive(Serialize, Deserialize, Debug)]\nstruct Task {\n    id: u32,\n    title: String,\n}\n\nfn main() {\n    let t = Task { id: 1, title: String::from(\"buy milk\") };\n    println!(\"{}\", serde_json::to_string(&t).unwrap());\n}",
      options: [
        { text: "編譯失敗:error: cannot find derive macro `Serialize` in this scope,編譯器還補上 note: `Serialize` is imported here, but it is only a trait, without a derive macro——derive 巨集要開 serde 的 derive feature 才會進來,宣告得寫成 serde = { version = \"1\", features = [\"derive\"] }" },
        { text: "編譯成功:serde = \"1\" 已經包含整個 serde,#[derive(Serialize, Deserialize)] 本來就能用" },
        { text: "編譯成功,但執行時 to_string 回傳 {},因為沒有任何欄位被標記為要序列化" },
        { text: "編譯失敗,原因是 serde_json 必須寫在 serde 前面,cargo 依宣告順序決定巨集的可見性" },
      ],
      answer: 0,
      explanation: `這是新手裝 serde 時百分之百會踩一次的坑。serde 這個 crate 預設只帶 Serialize / Deserialize 兩個 trait;產生 impl 的那兩個同名 derive 巨集藏在 derive feature 後面,沒開就不存在。
錯誤訊息把這件事講得非常清楚:第一句說找不到 derive macro Serialize,第二句說「Serialize 是被匯入了沒錯,但它只是一個 trait,沒有附帶 derive 巨集」——同一個名字,trait 在、巨集不在。
serde 為什麼要這樣切?因為 derive 會拉進 proc-macro 的編譯成本(syn、quote 那一串),只想手寫 impl 或只是傳遞型別的人不必付這筆錢。讀任何 crate 的文件時,先看它的 Feature flags 一節就是這個原因。
「執行時回傳 {}」的想像在 Rust 裡不存在:序列化能力是編譯期的 trait 實作,沒有 impl 就根本編不過,不會有「安靜地做錯」這個選項。至於在 [dependencies] 另外加一行 serde_derive,技術上確實能動(derive feature 底下啟用的就是它),但官方文件明講要用 features = ["derive"],別自己直接相依。`,
      walkthrough: [
        {
          label: "X Cargo.toml (derive feature not enabled)",
          lang: "ini",
          lines: [
            { code: "[dependencies]", note: "相依區段。" },
            { code: "serde = \"1\"", note: "⛔ 問題出在這一行:只寫版本等於「所有 feature 都不開」,derive 巨集因此沒有被編進來。" },
            { code: "serde_json = \"1\"", note: "JSON 格式實作,這一行沒問題。它自己會相依 serde,但不會替你打開 serde 的 derive feature。" },
          ],
        },
        {
          label: "X src/main.rs (won't compile)",
          lines: [
            { code: "use serde::{Serialize, Deserialize};", note: "把兩個名字引進作用域。這一行是成功的——trait 確實存在。" },
            { code: "", note: "" },
            { code: "#[derive(Serialize, Deserialize, Debug)]", note: "⛔ 這裡編譯失敗:error: cannot find derive macro `Serialize` in this scope,附註說明它只是一個 trait,沒有附帶 derive 巨集。Deserialize 也報一次。" },
            { code: "struct Task {", note: "要被序列化的型別。" },
            { code: "    id: u32,", note: "欄位 id。" },
            { code: "    title: String,", note: "欄位 title。" },
            { code: "}", note: "struct 結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let t = Task { id: 1, title: String::from(\"buy milk\") };", note: "建立一筆資料。" },
            { code: "    println!(\"{}\", serde_json::to_string(&t).unwrap());", note: "⛔ 連帶失敗:the trait bound `Task: serde::Serialize` is not satisfied——derive 沒跑,自然沒有 impl。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "兩個錯誤是同一個原因的兩層表現:上面是「找不到巨集」,下面是「因為巨集沒跑,所以缺 impl」。修 Cargo.toml 一行,兩個一起消失。",
        },
        {
          label: "√ Correct version: Cargo.toml",
          lang: "ini",
          lines: [
            { code: "[dependencies]", note: "相依區段。" },
            { code: "serde = { version = \"1\", features = [\"derive\"] }", note: "改好了:版本與 feature 一起寫成表格形式,derive 巨集這才進得來。" },
            { code: "serde_json = \"1\"", note: "維持原樣。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use serde::{Serialize, Deserialize};", note: "現在這一行同時引進 trait 與同名的 derive 巨集。" },
            { code: "", note: "" },
            { code: "#[derive(Serialize, Deserialize, Debug)]", note: "編譯期產生三組 impl:序列化、反序列化、Debug 格式化。" },
            { code: "struct Task {", note: "要被序列化的型別,程式碼完全不用改。" },
            { code: "    id: u32,", note: "欄位 id。" },
            { code: "    title: String,", note: "欄位 title。" },
            { code: "}", note: "struct 結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let t = Task { id: 1, title: String::from(\"buy milk\") };", note: "建立一筆資料。" },
            { code: "    println!(\"{}\", serde_json::to_string(&t).unwrap());", note: "印出 {\"id\":1,\"title\":\"buy milk\"}。" },
            { code: "}", note: "main 結束。" },
          ],
        },
      ],
      csharp: `System.Text.Json 沒有這個步驟:[JsonPropertyName] 這些 attribute 隨 BCL 一起來,引用命名空間就能用。Rust 的 crate 則普遍把「會拖慢編譯或拉進額外相依」的功能藏在 feature 後面,serde 的 derive 是最經典的一個,clap 的 derive 也一樣。這是讀 Rust 相依宣告時的基本功:看到 features = [...] 不是裝飾,那是這個 crate 的哪幾塊要被編進來。順帶一提,.NET 7 之後的 System.Text.Json 也有了編譯期產生序列化程式碼的做法(source generator),方向跟 serde 的 derive 其實一致。`,
    },
    {
      id: "2-12-07",
      question: "serde 的 derive feature 已經開好了。以下程式的輸出是?",
      questionCode: "use serde::{Serialize, Deserialize};\n\n#[derive(Serialize, Deserialize, Debug)]\nstruct Task {\n    id: u32,\n    title: String,\n    done: bool,\n}\n\nfn main() {\n    let t = Task { id: 7, title: String::from(\"write docs\"), done: false };\n    let json = serde_json::to_string(&t).unwrap();\n    println!(\"{}\", json);\n    let back: Task = serde_json::from_str(&json).unwrap();\n    println!(\"{:?}\", back);\n    let extra: Task = serde_json::from_str(r#\"{\"id\":1,\"title\":\"a\",\"done\":true,\"note\":\"x\"}\"#).unwrap();\n    println!(\"{:?}\", extra);\n}",
      options: [
        { text: "{\"id\":7,\"title\":\"write docs\",\"done\":false} / Task { id: 7, title: \"write docs\", done: false } / Task { id: 1, title: \"a\", done: true }——欄位依宣告順序輸出,JSON 裡多出來的 note 欄位預設會被忽略" },
        { text: "前兩行一樣,但第三行 panic:JSON 出現了 struct 沒有的欄位,serde 預設會拒絕" },
        { text: "編譯失敗:from_str 不知道要反序列化成什麼型別,一定要寫成 from_str::<Task>(&json)" },
        { text: "第一行印出 {\"done\":false,\"id\":7,\"title\":\"write docs\"}(JSON 物件的鍵會依字母排序),其餘兩行同前" },
      ],
      answer: 0,
      explanation: `三件事各自有它的道理。第一,to_string 依 struct 的宣告順序輸出,不排序——derive 產生的程式碼就是照欄位順序一個一個寫出去。第二,from_str 的目標型別由 let back: Task 這個標註反推,turbofish 只是另一種寫法而不是必要。第三,JSON 裡多出來的 note 欄位預設被忽略,這是刻意的寬鬆:對接的 API 常常會長出新欄位,沒道理因此讓舊程式全部壞掉。
「多餘欄位會 panic」是把嚴格模式當成預設。要那個行為得自己加 #[serde(deny_unknown_fields)],加了之後才會回 Err。順帶一提反過來的方向:反序列化成 serde_json::Value 時,物件的鍵是依鍵排序的(底層是 BTreeMap),要保留原始順序得開 serde_json 的 preserve_order feature——又是一個 feature 決定行為的例子。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use serde::{Serialize, Deserialize};", note: "同時引進兩個 trait 與同名的 derive 巨集(derive feature 已開)。" },
          { code: "", note: "" },
          { code: "#[derive(Serialize, Deserialize, Debug)]", note: "編譯期替 Task 產生序列化、反序列化與 Debug 的 impl,一行換三組程式碼。" },
          { code: "struct Task {", note: "要被序列化的型別。" },
          { code: "    id: u32,", note: "第一個欄位——輸出的 JSON 也會把它排在第一個。" },
          { code: "    title: String,", note: "第二個欄位。" },
          { code: "    done: bool,", note: "第三個欄位。" },
          { code: "}", note: "struct 結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let t = Task { id: 7, title: String::from(\"write docs\"), done: false };", note: "建立一筆資料。" },
          { code: "    let json = serde_json::to_string(&t).unwrap();", note: "借用 t 產生緊湊的 JSON 字串(要排版好看的版本用 to_string_pretty)。回傳 Result,這裡直接 unwrap。" },
          { code: "    println!(\"{}\", json);", note: "印出 {\"id\":7,\"title\":\"write docs\",\"done\":false}——順序與宣告一致,沒有多餘空白。" },
          { code: "    let back: Task = serde_json::from_str(&json).unwrap();", note: "反過來解析。目標型別由左邊的標註決定,from_str 才知道要產生什麼。" },
          { code: "    println!(\"{:?}\", back);", note: "印出 Task { id: 7, title: \"write docs\", done: false },證明來回一趟沒有失真。" },
          { code: "    let extra: Task = serde_json::from_str(r#\"{\"id\":1,\"title\":\"a\",\"done\":true,\"note\":\"x\"}\"#).unwrap();", note: "這串 JSON 多了一個 note 欄位。Task 沒有它,serde 預設直接略過,不算錯誤。" },
          { code: "    println!(\"{:?}\", extra);", note: "印出 Task { id: 1, title: \"a\", done: true },多出來的 note 消失無蹤。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "要更嚴格就加 #[serde(deny_unknown_fields)];要讓缺少的欄位用預設值而不是報錯,則是 #[serde(default)]。兩個方向都由屬性控制,程式碼本身不用改。",
      },
      csharp: `對應 JsonSerializer.Serialize 與 Deserialize<T>,連「預設忽略未知欄位」這點都一樣。真正的差別在機制:C# 是執行期反射(或 .NET 7 之後的 source generator)讀取型別資訊;Rust 的 impl 是編譯期由 derive 產生的具體程式碼,執行期沒有任何型別查詢。所以 from_str 必須在編譯期就知道目標型別——不是標註,就是 turbofish,沒有第三條路。代價是彈性少一點,回報是沒有反射成本,而且型別對不上時是編譯錯誤,不是執行期的 JsonException。`,
    },
    {
      id: "2-12-08",
      question: "接一個 JSON API,它的欄位名是小駝峰。以下程式 cargo build 完全沒有錯誤,執行的結果是?",
      questionCode: "use serde::Deserialize;\n\n#[derive(Deserialize, Debug)]\nstruct User {\n    user_name: String,\n    age: u32,\n}\n\nfn main() {\n    let data = r#\"{\"userName\":\"ada\",\"age\":36}\"#;\n    let u: User = serde_json::from_str(data).unwrap();\n    println!(\"{:?}\", u);\n}",
      options: [
        { text: "執行時 panic:called `Result::unwrap()` on an `Err` value: Error(\"missing field `user_name`\", line: 1, column: 27)——serde 拿 struct 的欄位名原樣去 JSON 裡找鍵,不會自己做命名風格轉換" },
        { text: "正常印出 User { user_name: \"ada\", age: 36 }:serde 會自動把 userName 與 user_name 視為同一個欄位" },
        { text: "編譯失敗:struct 的欄位名與 JSON 的鍵對不起來,derive 產生的程式碼在編譯期就會比對出來" },
        { text: "正常印出 User { user_name: \"\", age: 36 }:找不到對應鍵的欄位會被填成該型別的預設值" },
      ],
      answer: 0,
      explanation: `serde 的欄位對應規則就是字串相等比對,而且大小寫敏感:struct 寫 user_name,它就只找 user_name 這個鍵。JSON 裡只有 userName,於是這個必要欄位缺席,回傳 Err;unwrap 把 Err 變成 panic。錯誤訊息裡的 column 27 是整個物件讀完、確定補不上來的位置。
「編譯期就會比對出來」不可能:derive 在編譯期看得到的只有你的 struct,那串 JSON 是執行期才拿到的資料,甚至可能來自網路。「找不到就填預設值」也不是預設行為——那要加 #[serde(default)];預設是「缺必要欄位就是錯誤」,因為安靜地給你一個空字串比直接失敗更難查。
兩種修法:欄位層級的 #[serde(rename = "userName")] 一次改一個;或 struct 層級的 #[serde(rename_all = "camelCase")] 整包轉換,欄位多的時候慣例用後者。這兩個屬性同時影響序列化與反序列化,輸出的 JSON 也會跟著變成小駝峰。`,
      walkthrough: [
        {
          label: "X Question code (compiles, fails at runtime)",
          lines: [
            { code: "use serde::Deserialize;", note: "只需要反序列化,所以只引進這一個。" },
            { code: "", note: "" },
            { code: "#[derive(Deserialize, Debug)]", note: "產生的程式碼會照欄位名去找 JSON 的鍵——名字就寫死在那段產生出來的程式碼裡。" },
            { code: "struct User {", note: "目標型別。" },
            { code: "    user_name: String,", note: "⛔ 問題的根源:Rust 慣例用蛇形命名,於是解析時要找的鍵是 user_name,跟 API 給的 userName 對不上。" },
            { code: "    age: u32,", note: "這個欄位兩邊名字剛好一致,沒問題。" },
            { code: "}", note: "struct 結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let data = r#\"{\"userName\":\"ada\",\"age\":36}\"#;", note: "原始字串字面值,裡面的雙引號不用跳脫。API 給的鍵是小駝峰。" },
            { code: "    let u: User = serde_json::from_str(data).unwrap();", note: "⛔ 這裡 panic:called `Result::unwrap()` on an `Err` value: Error(\"missing field `user_name`\", line: 1, column: 27)。編譯期一切正常,問題到執行期才出現。" },
            { code: "    println!(\"{:?}\", u);", note: "走不到這一行。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use serde::Deserialize;", note: "同上。" },
            { code: "", note: "" },
            { code: "#[derive(Deserialize, Debug)]", note: "同上。" },
            { code: "#[serde(rename_all = \"camelCase\")]", note: "修正的一行:告訴 serde 這個 struct 的所有欄位在 JSON 那一側都是小駝峰。user_name 自動對應 userName,age 維持 age。" },
            { code: "struct User {", note: "型別本身完全不用改,Rust 這一側維持蛇形命名。" },
            { code: "    user_name: String,", note: "欄位名保持慣用寫法。" },
            { code: "    age: u32,", note: "同上。" },
            { code: "}", note: "struct 結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let data = r#\"{\"userName\":\"ada\",\"age\":36}\"#;", note: "同一串 JSON,沒有動過。" },
            { code: "    let u: User = serde_json::from_str(data).unwrap();", note: "這次對得上,回傳 Ok。" },
            { code: "    println!(\"{:?}\", u);", note: "印出 User { user_name: \"ada\", age: 36 }。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "只有一兩個欄位要改名時,改成在該欄位上方掛 #[serde(rename = \"userName\")] 效果一樣。rename_all 另外還支援 PascalCase、kebab-case、SCREAMING_SNAKE_CASE 等等。",
        },
      ],
      csharp: `System.Text.Json 的行為其實一樣嚴格:預設 PropertyNameCaseInsensitive 是 false,要嘛掛 [JsonPropertyName("userName")],要嘛在 JsonSerializerOptions 設 PropertyNamingPolicy = JsonNamingPolicy.CamelCase——跟 rename 與 rename_all 幾乎一一對應。真正不同的是「缺欄位」怎麼處理:C# 會把該屬性留成 null 或 0,給你一個看似成功的物件,錯誤往往要到用它的時候才爆;Rust 在反序列化當下就回傳 Err,問題與原因綁在同一個地方。`,
    },
    {
      id: "2-12-09",
      question: "以下程式碼 cargo build 完全沒有警告。改執行 cargo clippy,會發生什麼?",
      questionCode: "fn total(scores: &Vec<i32>) -> i32 {\n    let mut sum = 0;\n    for i in 0..scores.len() {\n        sum += scores[i];\n    }\n    sum\n}\n\nfn main() {\n    let scores = vec![1, 2, 3];\n    if scores.len() == 0 {\n        println!(\"empty\");\n    }\n    println!(\"{}\", total(&scores));\n}",
      options: [
        { text: "照樣編譯成功,但多出三個警告:writing `&Vec` instead of `&[_]` involves a new object where a slice will do(clippy::ptr_arg)、the loop variable `i` is only used to index `scores`(clippy::needless_range_loop)、length comparison to zero(clippy::len_zero)——clippy 抓的是「能編譯但不慣用」的寫法" },
        { text: "clippy 會把這三個地方自動改掉並順便重新排版,它就是 Rust 的格式化工具" },
        { text: "clippy 只是 cargo build 的別名,輸出跟 cargo build 一模一樣" },
        { text: "建置直接失敗:這三種寫法是 Rust 社群明令禁止的,clippy 的檢查屬於編譯流程的一部分" },
      ],
      answer: 0,
      explanation: `clippy 是 rustup 的一個 component,本質上是掛了幾百條額外 lint 的 rustc——所以它看得到完整的型別與借用資訊,不是在做文字比對。這三條警告分別對應三個很典型的 C 系語言習慣:參數寫 &Vec<i32>(應該寫 &[i32],呼叫端才能傳陣列或切片)、用索引跑迴圈(應該寫 for s in scores)、用 len() == 0 判斷空(應該用 is_empty(),因為不是所有集合算長度都是常數時間)。
這些預設是 warning,不會擋建置;要在 CI 擋下來得自己寫 cargo clippy -- -D warnings。想讓它直接幫你改,可以跑 cargo clippy --fix,不過它只會套用有明確修正建議的那幾條。
「clippy 會順便排版」是最常見的混淆:clippy 不會動任何一個空白,排版是 rustfmt 的工作,兩者分工不同。「禁止」也不成立——clippy 的建議可以用 #[allow(clippy::needless_range_loop)] 在單一位置關掉,專案層級的設定則放在 clippy.toml 或 crate 根的屬性裡。`,
      walkthrough: [
        {
          label: "⊕ Question code — walkthrough (compiles; clippy has opinions)",
          lines: [
            { code: "fn total(scores: &Vec<i32>) -> i32 {", note: "clippy::ptr_arg:收 &Vec<i32> 等於要求呼叫端一定得有一個 Vec。改成 &[i32] 之後,陣列、切片、Vec 都能傳進來(自動解參考會處理),功能只多不少。" },
            { code: "    let mut sum = 0;", note: "累加用的變數,型別由後面的 += 推成 i32。" },
            { code: "    for i in 0..scores.len() {", note: "clippy::needless_range_loop:i 只被拿去索引 scores,沒有別的用途。" },
            { code: "        sum += scores[i];", note: "每次索引都要做一次邊界檢查;改成 for s in scores 可以省掉,而且不可能寫出差一位的錯誤。" },
            { code: "    }", note: "迴圈結束。" },
            { code: "    sum", note: "沒有分號,這是回傳值。" },
            { code: "}", note: "函式結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let scores = vec![1, 2, 3];", note: "建立資料。" },
            { code: "    if scores.len() == 0 {", note: "clippy::len_zero:跟零比長度。慣用寫法是 scores.is_empty(),語意更清楚,而且對某些集合更有效率。" },
            { code: "        println!(\"empty\");", note: "這裡不會執行。" },
            { code: "    }", note: "if 結束。" },
            { code: "    println!(\"{}\", total(&scores));", note: "印出 6。整支程式的行為完全正確——clippy 講的從頭到尾都是「寫法」而不是「對錯」。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "⊕ Using clippy",
          lang: "bash",
          lines: [
            { code: "rustup component add clippy", note: "clippy 不是預設安裝的,第一次要自己加(rustfmt 也一樣)。" },
            { code: "cargo clippy", note: "跑一次分析。它會重新檢查整個 crate,輸出格式跟 cargo build 的警告一模一樣,帶檔名、行號與修正建議。" },
            { code: "cargo clippy --fix", note: "把有明確修正建議的那幾條直接改進原始碼(需要乾淨的 git 工作區,或加 --allow-dirty)。" },
            { code: "cargo clippy -- -D warnings", note: "把所有警告升級成錯誤,CI 常用這一行來擋 PR。" },
          ],
        },
      ],
      csharp: `對應 Roslyn Analyzer 與 StyleCop,連使用方式都很像:預設是建議(warning),可以在 CI 把嚴重度升成 error,也可以逐條關掉。C# 用 .editorconfig 逐條設嚴重度,Rust 用 #[allow(...)] / #[deny(...)] 屬性或 clippy.toml。差別在覆蓋率:clippy 是整個 Rust 生態共用的同一套 lint,幾乎所有專案都跑它,所以「慣用寫法」在 Rust 是有工具背書的共識,不像 C# 那樣各家 analyzer 各有一套。`,
    },
    {
      id: "2-12-10",
      question: "以下這個檔案排版很亂,但可以正常編譯執行。對它執行 cargo fmt 之後會怎樣?",
      questionCode: "fn  main(){let v=vec![1,2,3];\nlet mut sum=0;\nfor i in 0..v.len(){sum+=v[i];}\nprintln!(\"{}\",sum);}",
      options: [
        { text: "只有排版改變:縮排成四個空白、運算子與逗號兩側補上空白、擠在一行的敘述拆開;for i in 0..v.len() 這種索引寫法原封不動留著——接著跑 clippy,它仍然會抱怨 the loop variable `i` is only used to index `v`" },
        { text: "cargo fmt 會順手把索引迴圈改寫成 for x in &v,因為那才是慣用寫法" },
        { text: "cargo fmt 只會列出排版不合規的檔案,不會真的改動任何內容" },
        { text: "cargo fmt 會把整個檔案壓成一行一個敘述的緊湊格式,並刪掉所有空白行" },
      ],
      answer: 0,
      explanation: `rustfmt 的工作範圍只有一件事:把原始碼解析成語法樹,再依一套固定規則重新印出來。所以它只會動 token 之間的空白與換行,不會增刪任何一個 token——程式的語意在格式化前後完全相同,連 for 迴圈的形狀都不會被碰。「這個迴圈寫得好不好」是 clippy 的守備範圍,兩個工具刻意分開:格式歸 rustfmt,慣用寫法歸 clippy。
「只列出不合規的檔案」說的其實是 cargo fmt --check:它不改檔案,發現不合規就以非零狀態碼結束,是 CI 的標準用法。預設的 cargo fmt 會直接就地改寫。
rustfmt 的規則可以用 rustfmt.toml 微調,但社群幾乎人人照用預設值——這是為什麼隨便打開一個 Rust 開源專案,程式碼看起來都像同一個人寫的,也是為什麼 Rust 生態沒有「大括號要不要換行」這種爭論。`,
      walkthrough: [
        {
          label: "⊕ Before cargo fmt",
          lines: [
            { code: "fn  main(){let v=vec![1,2,3];", note: "函式名後面兩個空白、大括號沒有換行、宣告擠在同一行——全部都是排版問題,編譯器不在乎。" },
            { code: "let mut sum=0;", note: "沒有縮排,等號兩側沒有空白。" },
            { code: "for i in 0..v.len(){sum+=v[i];}", note: "整個迴圈擠成一行。索引寫法本身是合法的。" },
            { code: "println!(\"{}\",sum);}", note: "逗號後面沒有空白,結尾的大括號跟敘述黏在一起。" },
          ],
        },
        {
          label: "⊕ After cargo fmt (same program, same behaviour)",
          lines: [
            { code: "fn main() {", note: "多餘的空白被收掉,大括號依慣例留在同一行、前面一個空白。" },
            { code: "    let v = vec![1, 2, 3];", note: "縮排四個空白,等號與逗號兩側補上空白。" },
            { code: "    let mut sum = 0;", note: "同樣補齊排版。" },
            { code: "    for i in 0..v.len() {", note: "迴圈被拆開成多行——但 0..v.len() 這個索引寫法一個字都沒變,rustfmt 不改邏輯。" },
            { code: "        sum += v[i];", note: "迴圈本體往內縮一層。" },
            { code: "    }", note: "迴圈結束。" },
            { code: "    println!(\"{}\", sum);", note: "逗號後面補空白。印出 6,跟格式化前完全一樣。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "接著對這份格式化好的檔案跑 cargo clippy,它仍然會說 the loop variable `i` is only used to index `v`(clippy::needless_range_loop),另外還會提醒這裡的 vec! 其實可以用陣列。兩個工具看的是完全不同的東西。",
        },
      ],
      csharp: `對應 dotnet format 與 .editorconfig,分工也一樣:format 只動排版,程式碼建議是 analyzer 的事。差別在一致性——C# 團隊通常各自在 .editorconfig 調自己的偏好,Rust 幾乎整個生態共用 rustfmt 的預設值,所以格式在 Rust 早就不是需要討論的題目。CI 的標準組合是 cargo fmt --check 加 cargo clippy -- -D warnings,兩行擋掉所有格式與慣用寫法的爭議。`,
    },
    {
      id: "2-12-11",
      question: "C# 裡常寫 new Server { Port = 9000, Workers = 4 },其餘屬性留預設值。Rust 沒有這個語法,最接近的慣用做法是?",
      options: [
        { text: "builder pattern:一個 ServerBuilder 先帶著預設值,每個設定方法收 mut self、改完回傳 Self 讓呼叫端串下去,最後 build(self) 產生真正的 Server(也可以 derive(Default) 搭配 ..Default::default() 的 struct update 語法,但那要求呼叫端看得到所有欄位)" },
        { text: "Rust 其實有具名引數,直接寫 Server::new(port: 9000, workers: 4) 就好,沒填的參數會自動補預設值" },
        { text: "替每種組合各寫一個建構函式:Server::with_port、Server::with_port_and_workers⋯⋯讓呼叫端挑一個來用" },
        { text: "把設定包成 HashMap<String, String> 傳給 Server::new,在裡面依鍵取值,沒有的鍵就用預設值" },
      ],
      answer: 0,
      explanation: `Rust 的函式沒有具名引數、沒有預設參數值、也沒有多載,所以「一堆可選設定」這件事沒辦法靠呼叫語法解決,只能靠型別。builder 的三個要點:new() 提供一組預設值;每個設定方法收 mut self 並回傳 Self,讓呼叫端可以一路串接,同時把所有權搬下去;build(self) 消耗掉 builder、產出最終型別,需要驗證時讓它回傳 Result 就好。
「各寫一個建構函式」在兩三個選項時還行,n 個可選設定會長出 2^n 種組合。「用 HashMap<String, String>」則是放棄型別檢查:鍵打錯、值的格式不對,全部要到執行期才知道,而這正是 Rust 想消滅的那一類錯誤。至於 ..Default::default() 的 struct update 語法,確實是更輕量的替代方案,但它要求所有欄位對呼叫端可見(pub),library 通常不想做這個承諾——欄位一旦公開,以後連改名都是破壞性改動。`,
      walkthrough: [
        {
          label: "◇ C# equivalent: object initializer",
          lang: "csharp",
          lines: [
            { code: "var server = new Server", note: "先呼叫無參數建構函式,產生一個物件。" },
            { code: "{", note: "物件初始化語法開始。" },
            { code: "    Port = 9000,", note: "建構完成後再逐一指派屬性——物件在這個中間狀態是半成品。" },
            { code: "    Workers = 4,", note: "沒有列出的屬性維持欄位初始器或建構函式給的預設值。" },
            { code: "};", note: "初始化結束。C# 另外還有具名引數與預設參數值可用,所以很少需要 builder。" },
          ],
        },
        {
          label: "√ Rust: the builder pattern (complete, runnable)",
          lines: [
            { code: "#[derive(Debug)]", note: "只是為了等一下能印出來看。" },
            { code: "struct Server {", note: "最終要產出的型別。欄位保持私有,呼叫端不能直接組出一個半成品。" },
            { code: "    host: String,", note: "欄位一:主機位址。" },
            { code: "    port: u16,", note: "欄位二:通訊埠。" },
            { code: "    workers: usize,", note: "欄位三:工作執行緒數。" },
            { code: "}", note: "struct 結束。" },
            { code: "", note: "" },
            { code: "struct ServerBuilder {", note: "中間型別,負責承接一項一項的設定。" },
            { code: "    host: String,", note: "跟 Server 同樣的三個欄位,只是這裡每一個都先有預設值。" },
            { code: "    port: u16,", note: "同上。" },
            { code: "    workers: usize,", note: "同上。" },
            { code: "}", note: "struct 結束。" },
            { code: "", note: "" },
            { code: "impl ServerBuilder {", note: "builder 的方法。" },
            { code: "    fn new() -> Self {", note: "起點:提供整組預設值,呼叫端只需要覆寫在意的那幾項。" },
            { code: "        ServerBuilder { host: String::from(\"127.0.0.1\"), port: 8080, workers: 1 }", note: "預設值集中在這一行,是唯一的真相來源。" },
            { code: "    }", note: "new 結束。" },
            { code: "", note: "" },
            { code: "    fn port(mut self, port: u16) -> Self {", note: "關鍵簽名:按值收下 self(所以標 mut 就能改),改完再把自己交回去。" },
            { code: "        self.port = port;", note: "覆寫這一項。" },
            { code: "        self", note: "回傳 Self,呼叫端才能繼續 .workers(4) 串下去。" },
            { code: "    }", note: "方法結束。" },
            { code: "", note: "" },
            { code: "    fn workers(mut self, workers: usize) -> Self {", note: "同樣的形狀,再一個設定項。" },
            { code: "        self.workers = workers;", note: "覆寫。" },
            { code: "        self", note: "繼續交棒。" },
            { code: "    }", note: "方法結束。" },
            { code: "", note: "" },
            { code: "    fn build(self) -> Server {", note: "收尾:按值消耗掉 builder,產出真正的 Server。builder 到此用完,不可能被重複使用。" },
            { code: "        Server { host: self.host, port: self.port, workers: self.workers }", note: "把欄位搬過去。要做驗證(例如 port 不能是 0)就把回傳型別改成 Result<Server, ...>,在這裡檢查。" },
            { code: "    }", note: "build 結束。" },
            { code: "}", note: "impl 結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let server = ServerBuilder::new().port(9000).workers(4).build();", note: "讀起來跟 C# 的物件初始化語法一樣清楚,而且每一步都有型別檢查:port 必須是 u16,打錯名字是編譯錯誤。" },
            { code: "    println!(\"{:?}\", server);", note: "印出 Server { host: \"127.0.0.1\", port: 9000, workers: 4 }——沒設定的 host 用了預設值。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "欄位很多時,手寫這一串很煩,實務上常用 derive_builder 這類 crate 自動產生。標準庫自己也到處在用這個模式,例如 std::thread::Builder 與 std::process::Command。",
        },
      ],
      csharp: `C# 有三樣東西讓 builder 變得可有可無:物件初始化語法、具名引數、預設參數值。Rust 一樣都沒有,所以同樣的需求被推到型別層次。順帶一個容易忽略的差異:C# 的物件初始化是「先建構、再逐一指派」,物件在中途是一個屬性還沒設完的半成品,如果建構函式裡有驗證,那些驗證看不到後來指派的值;Rust 的 builder 一直到 build() 才產生 Server,驗證可以放在那一刻,而且 build 能回傳 Result,把「設定不合法」變成呼叫端必須處理的值。`,
    },
    {
      id: "2-12-12",
      question: "函式參數寫成 impl Into<String>。以下程式的結果是?",
      questionCode: "struct Label {\n    text: String,\n}\n\nfn make(text: impl Into<String>) -> Label {\n    Label { text: text.into() }\n}\n\nfn main() {\n    let owned = String::from(\"owned\");\n    let a = make(\"literal\");\n    let b = make(owned);\n    let c = make(format!(\"{}-{}\", \"x\", 1));\n    println!(\"{} {} {}\", a.text, b.text, c.text);\n}",
      options: [
        { text: "印出 literal owned x-1——三種呼叫都合法:&str 有轉成 String 的實作,String 自己也轉得成 String(標準庫有恆等轉換),呼叫端不必在 make(\"literal\") 與 make(owned) 之間切換寫法" },
        { text: "編譯失敗:make(owned) 傳進去的已經是 String,不需要也不能再轉換一次" },
        { text: "編譯失敗:impl Into<String> 只接受 &str,String 要先呼叫 .as_str() 才傳得進去" },
        { text: "印出 literal owned x-1,而且比參數寫成 &str 更有效率:不論傳什麼進去都完全不會配置記憶體" },
      ],
      answer: 0,
      explanation: `這是 2-9 那條分工(定義端實作 From,要求端寫 Into)在 API 設計上最常見的應用。標準庫有 impl From<&str> for String,所以字串字面值能轉;同時還有一條恆等實作,任何型別都能轉成自己,所以 String 傳進來也合法——這時候 .into() 是原值搬移,不複製也不配置。
「更有效率」是這題最想破的誤解。傳字串字面值時,那一次配置躲不掉:就算參數寫死 &str,函式內部還是得 .to_string() 才存得進欄位。真正省下來的是另一邊:呼叫端已經有 String 時,直接搬進去,少一次複製。所以 impl Into<String> 換到的是呼叫端的彈性,不是速度。
代價也要知道:泛型會為每個實際傳入的型別各生一份程式碼(單型化),編譯產物變大,錯誤訊息也變長。慣例因此是「公開 API 值得這樣寫,內部小函式直接寫死型別就好」。至於 as_str():那會把 String 退回借用,反而讓呼叫端必須額外配置一次才能存進欄位。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "struct Label {", note: "一個持有自有字串的型別。" },
          { code: "    text: String,", note: "欄位是 String,不是 &str——所以不管呼叫端給什麼,最後都得有一份自有的字串。" },
          { code: "}", note: "struct 結束。" },
          { code: "", note: "" },
          { code: "fn make(text: impl Into<String>) -> Label {", note: "參數的意思是「任何能轉成 String 的型別都收」。這是泛型的簡寫,等同 fn make<T: Into<String>>(text: T)。" },
          { code: "    Label { text: text.into() }", note: "在這裡做唯一一次轉換。傳進來的是 &str 就配置一份新字串;本來就是 String 就直接搬進來,什麼都不做。" },
          { code: "}", note: "函式結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let owned = String::from(\"owned\");", note: "先準備一個自有字串。" },
          { code: "    let a = make(\"literal\");", note: "傳 &'static str。這裡會配置一份新的 String。" },
          { code: "    let b = make(owned);", note: "傳 String,所有權直接搬進去(owned 從這一行之後不能再用)。沒有任何複製。" },
          { code: "    let c = make(format!(\"{}-{}\", \"x\", 1));", note: "format! 產生的也是 String,同樣直接搬進去——三種常見的字串來源都不用改寫法。" },
          { code: "    println!(\"{} {} {}\", a.text, b.text, c.text);", note: "印出 literal owned x-1。" },
          { code: "}", note: "main 結束,三個 Label 依序釋放。" },
        ],
        outro: "什麼時候不該這樣寫:函式只是讀一讀字串、不需要持有它的時候,參數寫 &str 才是慣用做法。impl Into<String> 的前提是「我最後真的需要一個自有的 String」。",
      },
      csharp: `C# 沒有對應的寫法,因為它只有一種 string,不存在 &str 與 String 的二分,呼叫端也就沒有「我該給哪一種」的問題。硬要對照的話,像是多載一個吃 string、一個吃 ReadOnlySpan<char>。要帶走的觀念是:Rust 用泛型把「呼叫端可以給哪些型別」直接寫進函式簽名,而不是靠列舉多載;簽名本身就是文件,編譯器也照著它檢查。`,
    },
    {
      id: "2-12-13",
      question: "Cargo.toml 寫的是 itoa = \"1\",Cargo.lock 裡解析出來的是 1.0.18。有人想換個版本,直接打開 Cargo.lock 把版本號改成 0.4.8,然後執行 cargo build。結果是?",
      options: [
        { text: "cargo build 成功,而且不算錯誤:它發現 lock 裡的 0.4.8 不在 Cargo.toml 宣告的範圍內,就重新解析、印出 Updating itoa v0.4.8 -> v1.0.18,順手把 Cargo.lock 改回去——手改的內容等於沒發生" },
        { text: "cargo build 失敗並要求你修好 Cargo.lock:鎖定的版本與 Cargo.toml 的宣告衝突時,cargo 一律拒絕建置" },
        { text: "cargo build 成功,而且真的用 0.4.8 編譯:Cargo.lock 的優先權高於 Cargo.toml,這正是「鎖定」的意思" },
        { text: "cargo build 成功並忽略 Cargo.lock:每次建置都會重新抓符合範圍的最新版,lock 檔只是給人看的紀錄" },
      ],
      answer: 0,
      explanation: `這題沒有任何編譯錯誤,連 cargo 都不會抱怨——錯的是對兩個檔案分工的理解,而這種錯誤最容易一路帶到正式環境。
Cargo.toml 是「你要求的範圍」,Cargo.lock 是「上次解析出來的精確結果」。lock 只在仍然滿足 Cargo.toml 的前提下才被沿用;一旦不滿足(手改成 0.4.8 就不滿足 "1" 了),cargo 會重新解析並把 lock 改回去。所以「手改 lock 來換版本」永遠不可靠。
「每次都抓最新版」剛好相反:只要 lock 裡的版本仍在範圍內,cargo 就會一直用它,重現性正是 lock 存在的理由,要拿新版得自己跑 cargo update。「lock 的優先權高於 toml」也不對:優先權關係反過來,toml 是規格、lock 是結果。
真的想固定某個版本有兩條路:改 Cargo.toml 的範圍(例如寫成 =1.0.5),或用 cargo update -p itoa --precise 1.0.5——後者會印出 Downgrading itoa v1.0.18 -> v1.0.5,而且因為 1.0.5 仍然滿足 "1",這次的結果會被保留下來。至於「拒絕建置」真正會出現的場合是 CI:cargo build --locked 在需要改動 lock 時會直接失敗,訊息是 error: cannot update the lock file ... because --locked was passed to prevent this。`,
      walkthrough: [
        {
          label: "X Hand-edited Cargo.lock (this change will not survive)",
          lang: "ini",
          lines: [
            { code: "# Cargo.toml 這一側完全沒動:", note: "先看規格。" },
            { code: "[dependencies]", note: "相依區段。" },
            { code: "itoa = \"1\"", note: "可接受的範圍是 1.x。這才是真正決定版本的地方。" },
            { code: "", note: "" },
            { code: "# Cargo.lock(自動產生,被手動改過):", note: "再看被動的那一側。" },
            { code: "[[package]]", note: "lock 檔裡每個套件一個區塊,記的是「精確版本」而不是範圍。" },
            { code: "name = \"itoa\"", note: "套件名。" },
            { code: "version = \"0.4.8\"", note: "⛔ 手改的這一行撐不過下一次建置:0.4.8 不滿足 Cargo.toml 要求的 1.x,cargo 會重新解析,印出 Updating itoa v0.4.8 -> v1.0.18 並把這一行改回去。" },
            { code: "source = \"registry+https://github.com/rust-lang/crates.io-index\"", note: "來源註冊表,這一行不用動。" },
          ],
          outro: "記住這個分工:Cargo.toml 由人維護、Cargo.lock 由 cargo 維護。lock 要提交進版控(讓別人與 CI 重現同一組版本),但不要用編輯器去改它的內容。",
        },
        {
          label: "√ Correct version: pin it the way cargo intends",
          lang: "bash",
          lines: [
            { code: "cargo update -p itoa --precise 1.0.5", note: "指定某個套件要退回(或前進)到哪一個精確版本。印出 Downgrading itoa v1.0.18 -> v1.0.5,並把 lock 改成這個結果。" },
            { code: "cargo build", note: "1.0.5 仍然滿足 Cargo.toml 的 \"1\",所以這次的鎖定會被保留,不會又被改回去。" },
            { code: "git add Cargo.lock", note: "把結果提交進版控,別人 clone 下來拿到的就是同一組版本。" },
            { code: "cargo build --locked", note: "CI 用這個:一旦需要改動 lock 就直接失敗(error: cannot update the lock file ... because --locked was passed to prevent this),確保建置真的可重現。" },
          ],
        },
        {
          label: "√ Correct version: or state it in Cargo.toml",
          lang: "ini",
          lines: [
            { code: "[dependencies]", note: "相依區段。" },
            { code: "itoa = \"=1.0.5\"", note: "另一條路:把「就是要這一版」寫進規格。這樣連 cargo update 都動不了它,而且任何人一看 Cargo.toml 就知道為什麼。" },
          ],
        },
      ],
      csharp: `.NET 的對應物是 packages.lock.json,但要自己在專案裡開啟 RestorePackagesWithLockFile,而且多數專案沒開——所以「同一份原始碼在兩台機器還原出不同版本」在 .NET 是家常便飯,在 Rust 反而是意外。Rust 的慣例已經收斂成:Cargo.lock 一律提交進版控(近年官方文件連 library 也建議提交),CI 一律加 --locked。你要的是「今天編出來的東西跟三個月前一模一樣」,那份 lock 就是唯一的保證。`,
    },
    {
      id: "2-12-14",
      question: "itoa 目前發布到 1.0.18,更早的 0.4 系列最後停在 0.4.8。以下四種寫法(分別試,不是同時存在)各自會解析出哪個版本?",
      questionCode: "itoa = \"1\"\nitoa = \"~1.0.5\"\nitoa = \"=1.0.5\"\nitoa = \"0.4\"",
      questionLang: "ini",
      options: [
        { text: "1.0.18 / 1.0.18 / 1.0.5 / 0.4.8——不加符號等於 ^,允許升到「下一個不相容版本」之前;~1.0.5 只允許 1.0.x;=1.0.5 完全鎖死;而 0.x 的不相容界線在第二個數字,所以 \"0.4\" 不會跳到 0.5" },
        { text: "四種都解析出 1.0.18:版本字串只是最低需求,cargo 永遠裝得到的最新版" },
        { text: "可能是 2.x(^ 只保證不低於 1,沒有上限)/ 1.0.18 / 1.0.5 / 0.4.8" },
        { text: "1.0.18 / 1.0.5(~ 表示連修訂號都要一致)/ 1.0.5 / 1.0.18(0.x 會自動升級到第一個穩定版)" },
      ],
      answer: 0,
      explanation: `Cargo 預設的 ^ 規則可以用一句話記:允許升級,但不准更動版本號裡最左邊那個非零的數字。所以 "1" 涵蓋整個 1.x(到 2.0 之前為止),"0.4" 因為最左非零的是 4,只涵蓋 0.4.x——0.5 在 semver 裡已經算不相容了,這個特例最常被忘記。
"~1.0.5" 的意思是「指定到修訂號時,只允許修訂號往上」,也就是 1.0.x;要注意 ~1.0 則等於 1.0.x、~1 等於 1.x,寫法不同語意不同。"=1.0.5" 完全鎖死,連修訂號都不准動。
「^1 可能跳到 2.x」正好把 ^ 的重點搞反了:^ 的價值就在於有上限,沒有上限的寫法是 ">=1"。「四種都是最新版」則忽略了範圍宣告存在的理由。最後別忘了:這些都只是「可接受的範圍」,真正用哪一版由 Cargo.lock 決定——範圍變寬不代表明天就會自己升上去。`,
      walkthrough: {
        label: "⊕ Version requirements — walkthrough",
        lang: "ini",
        lines: [
          { code: "itoa = \"1\"", note: "等同 ^1,涵蓋 >=1.0.0 且 <2.0.0。解析出 1.0.18。這是最常見的寫法,也是 cargo add 預設幫你寫的形式。" },
          { code: "itoa = \"~1.0.5\"", note: "涵蓋 >=1.0.5 且 <1.1.0。因為 1.x 目前只出到 1.0.18,結果同樣是 1.0.18;但如果上游發布 1.1.0,這一行不會跟上去,\"1\" 那一行會。" },
          { code: "itoa = \"=1.0.5\"", note: "完全鎖死,只接受 1.0.5。適合「已知 1.0.6 有回歸問題」這種暫時性的閃避,平常不要這樣寫,否則跟別人的要求很容易解析不出交集。" },
          { code: "itoa = \"0.4\"", note: "涵蓋 >=0.4.0 且 <0.5.0,解析出 0.4.8。0.x 的每一次第二位數更動都視為不相容,所以它絕對不會自己跳到 0.5 或 1.0。" },
        ],
        outro: "還有一組偶爾會看到的寫法:\">=1.2, <1.5\" 這種顯式區間,以及 \"*\"(任何版本,crates.io 禁止發布這種宣告)。日常九成九的情況寫 \"1\" 或 \"0.4\" 就夠了。",
      },
      csharp: `NuGet 的 Version=\"1.0.5\" 其實是「最低版本」,還原時會挑符合條件的最低可用版本;要表達範圍得寫 [1.0.5,2.0) 這種區間語法,而且實務上很少人寫。Rust 反過來:預設就是 ^,範圍是常態。更關鍵的是文化——Rust 生態高度遵守 semver(破壞性改動一律升 major,crates.io 甚至有工具檢查),所以「升 patch 不會壞」在 Rust 是可以信任的假設,這也是預設允許自動升級 patch 的前提。`,
    },
    {
      id: "2-12-15",
      question: "專案的 Cargo.toml 寫 thiserror = \"1\",鎖定在 1.0.69,上游早就發布了 2.0.20。團隊每週都跑 cargo update,想說這樣總會自動升上去。實際上呢?",
      options: [
        { text: "永遠升不上去:\"1\" 的上限就在 2.0 之前,cargo update 只會在這個範圍內找最新版。它甚至不會明講,只印 Locking 0 packages to latest Rust 1.97.1 compatible versions 加上一行很容易被忽略的 note: pass `--verbose` to see 1 unchanged dependencies behind latest。要升級必須自己把 Cargo.toml 改成 \"2\"" },
        { text: "cargo update 會升到 2.0.20:update 的語意就是「無視 Cargo.toml,抓目前最新版」" },
        { text: "cargo update 會失敗並要求先處理版本衝突,因為鎖定的版本已經不是最新的" },
        { text: "cargo build 會印出警告,提醒有新的主要版本可用" },
      ],
      answer: 0,
      explanation: `這一題跟上一題是同一枚硬幣的兩面:上一題講範圍怎麼寫,這一題講寫窄了會怎樣。而這裡沒有任何錯誤訊息——程式照編、測試照過、cargo update 每週照跑,專案就這樣安靜地停在一個舊的 major 上好幾年。等到哪天需要某個新功能,才發現要一次跨過兩年份的破壞性改動。
cargo update 的工作是「在 Cargo.toml 允許的範圍內把 lock 更新到最新」,範圍是你自己寫的。如果它真的會無視 Cargo.toml,^ 的保護就完全沒有意義了。「build 會警告」也不會發生:cargo 不知道你想不想升 major,那是人的決定。
要察覺這件事,可以看 cargo update 那行 note(加 --verbose 會列出是哪幾個套件落後)、裝 cargo outdated 這個第三方 subcommand,或讓 Dependabot 之類的機器人定期開 PR。升級 major 的流程則是:讀上游的 CHANGELOG、把 Cargo.toml 的 \"1\" 改成 \"2\"(或直接 cargo add thiserror@2)、修掉編譯錯誤。好消息是 Rust 這一步通常很踏實——破壞性改動會以編譯錯誤的形式全部攤在你面前,不會留到執行期。`,
      walkthrough: [
        {
          label: "X Cargo.toml (silently stuck on the 1.x line)",
          lang: "ini",
          lines: [
            { code: "[dependencies]", note: "相依區段。" },
            { code: "thiserror = \"1\"", note: "⛔ 問題就在這一行:範圍上限是 2.0 之前,所以不管 cargo update 跑幾次,都只會在 1.x 裡面找最新版,永遠看不到 2.0.20。" },
          ],
          outro: "實際跑 cargo update 看到的是:Locking 0 packages to latest Rust 1.97.1 compatible versions,再加一行 note: pass `--verbose` to see 1 unchanged dependencies behind latest。那個 note 就是唯一的線索,而它非常容易被當成雜訊略過。",
        },
        {
          label: "√ Correct version: bump the requirement on purpose",
          lang: "ini",
          lines: [
            { code: "[dependencies]", note: "相依區段。" },
            { code: "thiserror = \"2\"", note: "把範圍往前搬。升 major 是一個需要人來做的決定,要先讀過上游的 CHANGELOG,知道有哪些破壞性改動。" },
          ],
        },
        {
          label: "√ Correct version: the same thing from the command line",
          lang: "bash",
          lines: [
            { code: "cargo update --verbose", note: "先看清楚有哪些相依落後在舊的 major,這是最容易被忽略的資訊。" },
            { code: "cargo add thiserror@2", note: "直接把 Cargo.toml 的宣告改成 \"2\" 並重新解析,比手改檔案少一個出錯機會。" },
            { code: "cargo build", note: "破壞性改動會在這裡以編譯錯誤的形式一次攤開來,逐個修完就升級完成。" },
          ],
        },
      ],
      csharp: `NuGet 也有一模一樣的問題,只是形狀不同:PackageReference 的 Version 實際是最低版本,還原時被固定住,不主動跨 major。差別在可見度——.NET 的 major 升級往往伴隨 target framework 變動或 IDE 的提示,比較顯眼;Rust 這邊升不升 major 只是 Cargo.toml 裡一個字元的事,沒有任何人提醒你,所以更需要把「定期檢查相依」變成流程的一部分。`,
    },
    {
      id: "2-12-16",
      question: "你想查 regex 這個 crate 的 Regex::captures 怎麼用。關於 crates.io 與 docs.rs 的分工,哪個描述正確?",
      options: [
        { text: "crates.io 是套件註冊表(誰發布了什麼版本、README、相依清單、feature 清單、下載量),API 文件則由 docs.rs 在每次發布時自動跑一次 cargo doc 產生,每個版本都有一份永久網址;crates.io 頁面上的 Documentation 連結通常就指過去" },
        { text: "crates.io 本身就是完整的 API 文件站,docs.rs 只是它的鏡像,內容一樣" },
        { text: "API 文件要看作者自己架的網站,crates.io 與 docs.rs 都只提供下載與版本資訊" },
        { text: "docs.rs 只收錄官方維護的 crate,第三方 crate 的文件得自己 clone 原始碼跑 cargo doc" },
      ],
      answer: 0,
      explanation: `分工很乾淨:crates.io 管「發布與中繼資料」,docs.rs 管「文件的建置與託管」。只要成功發布到 crates.io,docs.rs 就會自動替那個版本跑一次 cargo doc 並永久保存,網址形如 docs.rs/regex/1.11.1/regex/。這代表你永遠查得到「你專案實際用的那一版」的文件,而不是只有最新版——這在對照舊專案時非常重要。
文件是從原始碼裡的三斜線註解產生的,所以它跟程式碼一定同步,連範例程式碼都會被 cargo test 當成 doc test 跑過。「要看作者自己架的站」在 Rust 幾乎不存在:文件基礎建設是生態內建的,連一行說明都沒寫的 crate,至少也有完整的型別簽名可以看。
另外兩個實用細節:docs.rs 的頁面上有 Feature flags 一節,列出這個 crate 有哪些 feature(第 6 題那種坑就是查這裡);本機跑 cargo doc --open 則會產生「你這個專案實際用到的那幾版」的文件,而且連你自己的 crate 也包含在內。`,
      walkthrough: {
        label: "⊕ Looking things up",
        lang: "bash",
        lines: [
          { code: "cargo add regex", note: "把相依寫進 Cargo.toml。指令會印出實際選用的版本,接著就能去 docs.rs 查那一版的文件。" },
          { code: "cargo doc --open", note: "在本機產生文件並用瀏覽器打開:內容是你這個專案實際用到的那幾版,還包含你自己寫的 crate。" },
          { code: "cargo doc --no-deps", note: "只產生自己這個 crate 的文件,不含相依,發布到自家網站時常用。" },
          { code: "cargo test", note: "文件裡的範例會被當成 doc test 一起執行——這是 Rust 文件很少過時的真正原因。" },
        ],
        outro: "查東西的順序建議是:docs.rs 看 API 與 Feature flags、crates.io 看版本與下載量(判斷這個 crate 還活著沒)、原始碼倉庫看 CHANGELOG 與 issue。三個地方各有各的用途。",
      },
      csharp: `nuget.org 相當於 crates.io,有 README 與中繼資料;但 API 文件那一塊 .NET 是分裂的:BCL 在 learn.microsoft.com,第三方套件則各自為政,有些有站、有些只有 GitHub 的 README、有些什麼都沒有。Rust 把這件事變成強制且統一的基礎建設——發布就一定有 docs.rs 文件,連版本都對得上。這對評估一個陌生 crate 幫助很大:打開文件就看得到完整的 API 形狀,不必先把原始碼 clone 下來。`,
    },
    {
      id: "2-12-17",
      question: "綜合題:把閉包、迭代器與 Result 串成一條「任一筆失敗就整體失敗」的鏈。以下程式的輸出是?",
      questionCode: "fn main() {\n    let max = 100;\n    let raw = vec![\"cpu=40\", \"mem=80\", \"disk=250\", \"net=10\"];\n\n    let parsed: Result<Vec<(&str, u32)>, String> = raw\n        .iter()\n        .map(|line| {\n            println!(\"checking {}\", line);\n            let (key, value) = line.split_once('=').ok_or_else(|| format!(\"bad line: {}\", line))?;\n            let n: u32 = value.parse().map_err(|_| format!(\"bad number: {}\", value))?;\n            if n > max {\n                return Err(format!(\"{} over limit: {}\", key, n));\n            }\n            Ok((key, n))\n        })\n        .collect();\n\n    println!(\"{:?}\", parsed);\n}",
      options: [
        { text: "checking cpu=40 / checking mem=80 / checking disk=250 / Err(\"disk over limit: 250\")——閉包捕獲了外部的 max,collect 逐一拉取,碰到第一個 Err 就停手,net=10 那一筆根本沒被檢查過" },
        { text: "四行 checking 都會印出來,最後印 Err(\"disk over limit: 250\")——collect 必須把整個迭代器走完,才知道有沒有錯誤" },
        { text: "checking 四行都印,最後印 Ok([(\"cpu\", 40), (\"mem\", 80), (\"net\", 10)])——失敗的那一筆被跳過,其餘照樣收集" },
        { text: "編譯失敗:? 運算子只能用在函式本體,閉包裡不能用" },
      ],
      answer: 0,
      explanation: `這一題把三課的東西疊在一起,每一層都有一個容易踩空的地方。
閉包那一層:max 是閉包外面的變數,閉包直接捕獲它來做上限檢查,不必多傳一個參數——這是閉包相對於具名函式最實際的好處。
? 那一層:? 的作用對象是「當前這個回傳 Result 的函式體」,而閉包本身就是一個函式體,所以這裡的 ? 是提早從閉包返回,不是從 main 返回。這也是為什麼閉包的回傳型別能被推成 Result<(&str, u32), String>。
collect 那一層:目標型別標成 Result<Vec<_>, _>,語意就是「全部成功才給 Ok(Vec),碰到第一個 Err 就停下來、整體回傳那個 Err」。停是真的停——collect 不再呼叫 next(),map 的閉包不會對 net=10 執行,所以輸出裡沒有第四行 checking。
「要走完才知道」低估了這個寫法的價值:當每一筆都要讀檔或發網路請求時,短路省下的是真實成本。「失敗的被跳過」則是另一個需求,要用 filter_map(|s| ...ok()) 才會得到那種結果——兩者是不同的意圖,由你選的目標型別決定。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let max = 100;", note: "上限值。它待會會被閉包捕獲——這是閉包與具名函式最大的差別。" },
          { code: "    let raw = vec![\"cpu=40\", \"mem=80\", \"disk=250\", \"net=10\"];", note: "四筆待解析的設定。第三筆會超過上限,第四筆永遠不會被看到。" },
          { code: "", note: "" },
          { code: "    let parsed: Result<Vec<(&str, u32)>, String> = raw", note: "這個標註決定了整條鏈的收尾語意:要嘛全部成功給一個 Vec,要嘛給第一個遇到的錯誤。" },
          { code: "        .iter()", note: "借用走訪,交出 &&str;下一步的閉包參數 line 就是它。" },
          { code: "        .map(|line| {", note: "閉包本體開始。它同時捕獲了 max,而且回傳 Result,所以裡面可以用 ?。" },
          { code: "            println!(\"checking {}\", line);", note: "每次真的處理一筆才會印,拿來觀察短路發生在哪裡。" },
          { code: "            let (key, value) = line.split_once('=').ok_or_else(|| format!(\"bad line: {}\", line))?;", note: "split_once 回傳 Option,用 ok_or_else 換成 Result(閉包只在真的失敗時才被呼叫,省下不必要的字串配置),再用 ? 提早離開閉包。" },
          { code: "            let n: u32 = value.parse().map_err(|_| format!(\"bad number: {}\", value))?;", note: "parse 的錯誤型別是 ParseIntError,和整條鏈約定的 String 對不上,所以用 map_err 換掉再 ?。" },
          { code: "            if n > max {", note: "這裡用到捕獲進來的 max。" },
          { code: "                return Err(format!(\"{} over limit: {}\", key, n));", note: "第三筆 disk=250 走到這裡,回傳 Err(\"disk over limit: 250\")。注意這個 return 返回的是閉包,不是 main。" },
          { code: "            }", note: "if 結束。" },
          { code: "            Ok((key, n))", note: "一切正常時交出一個 tuple。" },
          { code: "        })", note: "map 結束。到這裡什麼都還沒發生——迭代器是惰性的。" },
          { code: "        .collect();", note: "真正開始拉取。cpu、mem 成功後暫存;disk 回傳 Err,collect 立刻停止,不再向迭代器要第四筆。" },
          { code: "", note: "" },
          { code: "    println!(\"{:?}\", parsed);", note: "印出 Err(\"disk over limit: 250\")。已經成功解析的 cpu 與 mem 全部被丟掉——「整批成功或整批失敗」就是這個型別的承諾。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "同一條鏈只要改目標型別就能換語意:標成 Vec<Result<..>> 會把每一筆的結果都收起來(適合「回報所有錯誤」),標成 Result<Vec<..>, _> 則是這裡的短路。要收集全部錯誤而不是只要第一個,則是用 partition 把 Ok 與 Err 分成兩堆。",
      },
      csharp: `LINQ 沒有這個組合。C# 想在 Select 裡短路,實務上只能丟例外再到外面 try/catch,而「哪一筆失敗」要自己塞進例外訊息;想收集全部錯誤則得拆成命令式迴圈。Rust 這裡把兩件事分開交給兩個機制:短路交給型別(Result 的 FromIterator 實作),提早返回交給 ?,組合起來就是一條沒有例外也能短路的鏈。更重要的是,失敗的可能性寫在型別裡——呼叫端拿到的是 Result,不處理就編不過,不會像 C# 那樣忘了包 try/catch 也照樣通過編譯。`,
    },
    {
      id: "2-12-18",
      question: "綜合題:多條執行緒各自反序列化一份 JSON,再把結果收進同一個 Vec。以下程式的輸出是?",
      questionCode: "use serde::Deserialize;\nuse std::sync::{Arc, Mutex};\nuse std::thread;\n\n#[derive(Deserialize, Debug)]\nstruct Reading {\n    sensor: String,\n    value: i32,\n}\n\nfn main() {\n    let payloads = vec![\n        r#\"{\"sensor\":\"a\",\"value\":10}\"#,\n        r#\"{\"sensor\":\"b\",\"value\":20}\"#,\n        r#\"{\"sensor\":\"c\",\"value\":30}\"#,\n    ];\n\n    let collected = Arc::new(Mutex::new(Vec::new()));\n    let mut handles = Vec::new();\n\n    for payload in payloads {\n        let collected = Arc::clone(&collected);\n        handles.push(thread::spawn(move || {\n            let reading: Reading = serde_json::from_str(payload).unwrap();\n            collected.lock().unwrap().push(reading);\n        }));\n    }\n\n    for h in handles {\n        h.join().unwrap();\n    }\n\n    let done = collected.lock().unwrap();\n    let total: i32 = done.iter().map(|r| r.value).sum();\n    println!(\"{} readings, total {}\", done.len(), total);\n}",
      options: [
        { text: "3 readings, total 60——三筆各由一條執行緒反序列化後 push 進同一個 Vec;Arc 讓三條執行緒共享同一個 Mutex,Mutex 保證同一時間只有一個能改 Vec。Vec 裡的順序不固定,但筆數與總和是確定的" },
        { text: "編譯失敗:Vec 不能被多條執行緒修改,必須改用專門的並行集合型別" },
        { text: "3 readings, total 60,而且 Vec 裡的順序一定是 a、b、c——執行緒是依序建立的,鎖也會照建立順序給出去" },
        { text: "編譯失敗:collected 在第一圈就被 move 進執行緒了,後面兩圈沒有東西可以 clone" },
      ],
      answer: 0,
      explanation: `Arc<Mutex<T>> 的分工在 2-7 學過:Arc 負責「多個擁有者共享同一份資料」,Mutex 負責「同一時間只有一個能動它」。這題把它接到實際的工作上——每條執行緒各自做自己的 from_str(那一段完全不需要鎖),只有最後 push 的那一瞬間才進臨界區,鎖的粒度很小。
迴圈裡那行 let collected = Arc::clone(&collected); 是整段程式的關鍵:它遮蔽出一個新的 Arc handle 專門給這一圈的閉包 move 走,外層那個 collected 完好如初,所以下一圈還能再 clone 一份——這正好破掉「第一圈就被 move 光」的誤解。Arc::clone 只是把參考計數加一,沒有複製底層的 Vec。
另外兩個細節:collected.lock().unwrap().push(reading); 產生的 MutexGuard 是暫時值,在這個敘述結束的分號處就被丟棄,鎖也就跟著釋放——所以不會有人一直霸著鎖。lock() 回傳 Result 是因為鎖可能被「毒化」(持有鎖的執行緒 panic 過),這裡直接 unwrap。
順序的部分:三條執行緒搶同一把鎖,誰先搶到由作業系統排程決定,每次執行都可能不同。這正是題目最後印筆數與總和、而不是直接印 Vec 的原因——寫並行程式時,要讓斷言只依賴真正有保證的東西。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use serde::Deserialize;", note: "反序列化用的 trait 與 derive 巨集。" },
          { code: "use std::sync::{Arc, Mutex};", note: "跨執行緒共享(Arc)與互斥(Mutex),兩個要一起用。" },
          { code: "use std::thread;", note: "建立 OS 執行緒。" },
          { code: "", note: "" },
          { code: "#[derive(Deserialize, Debug)]", note: "編譯期產生解析 JSON 的程式碼。" },
          { code: "struct Reading {", note: "每條執行緒各自解析出來的資料型別。" },
          { code: "    sensor: String,", note: "感測器名稱。" },
          { code: "    value: i32,", note: "讀數,最後要加總。" },
          { code: "}", note: "struct 結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let payloads = vec![", note: "三份待處理的 JSON,模擬三個來源。" },
          { code: "        r#\"{\"sensor\":\"a\",\"value\":10}\"#,", note: "原始字串字面值,裡面的雙引號不必跳脫。型別是 &'static str,所以搬進執行緒沒有生命週期問題。" },
          { code: "        r#\"{\"sensor\":\"b\",\"value\":20}\"#,", note: "第二份。" },
          { code: "        r#\"{\"sensor\":\"c\",\"value\":30}\"#,", note: "第三份。" },
          { code: "    ];", note: "vec! 結束。" },
          { code: "", note: "" },
          { code: "    let collected = Arc::new(Mutex::new(Vec::new()));", note: "共享的收集點:由內往外讀是「一個 Vec,包在 Mutex 裡,再包進 Arc」。元素型別由後面的 push 推成 Reading。" },
          { code: "    let mut handles = Vec::new();", note: "收好每條執行緒的 handle,等一下要逐一 join。" },
          { code: "", note: "" },
          { code: "    for payload in payloads {", note: "按值走訪,每圈把一份 payload 的所有權交出去。" },
          { code: "        let collected = Arc::clone(&collected);", note: "替這一圈複製一個 Arc handle(只是計數加一)。遮蔽的新變數才是被 move 走的那個,外層的 collected 毫髮無傷,下一圈還能再 clone。" },
          { code: "        handles.push(thread::spawn(move || {", note: "move 把這一圈的 payload 與 Arc handle 的所有權搬進閉包——thread::spawn 要求閉包是 'static,所以幾乎一定要 move。" },
          { code: "            let reading: Reading = serde_json::from_str(payload).unwrap();", note: "解析這一段完全在執行緒自己的堆疊上進行,不碰共享資料,所以不需要持有鎖。" },
          { code: "            collected.lock().unwrap().push(reading);", note: "到這一刻才進臨界區:lock() 取得鎖(回傳 Result 是因為鎖可能被毒化),push 完之後暫時的 MutexGuard 在分號處被丟棄,鎖立刻釋放。" },
          { code: "        }));", note: "閉包與 spawn 結束,handle 被收進 handles。" },
          { code: "    }", note: "迴圈結束,三條執行緒都已經在跑了。" },
          { code: "", note: "" },
          { code: "    for h in handles {", note: "按值走訪 handles,才能把每個 handle 交給 join 消耗掉。" },
          { code: "        h.join().unwrap();", note: "等這條執行緒結束。三個 join 都回來之後,才保證所有 push 都完成了。" },
          { code: "    }", note: "迴圈結束。" },
          { code: "", note: "" },
          { code: "    let done = collected.lock().unwrap();", note: "主執行緒自己上鎖來讀結果。這個 guard 綁在具名變數上,會活到 main 結束。" },
          { code: "    let total: i32 = done.iter().map(|r| r.value).sum();", note: "透過 guard 直接走訪底下的 Vec(自動解參考),把三筆讀數加起來。" },
          { code: "    println!(\"{} readings, total {}\", done.len(), total);", note: "印出 3 readings, total 60。刻意不印 Vec 本身——裡面的順序每次執行都可能不同。" },
          { code: "}", note: "main 結束,guard 先釋放鎖,最後一個 Arc 歸零時整個 Mutex 與 Vec 才被釋放。" },
        ],
        outro: "值得注意的是這段程式裡沒有任何一行在「檢查」有沒有正確上鎖——因為根本不需要檢查:Vec 被關在 Mutex 裡面,不先 lock() 就拿不到它。忘記上鎖這種錯誤在這個結構下寫不出來。",
      },
      csharp: `C# 會直接用 ConcurrentBag<T>,或是自己 lock (gate) { list.Add(...) }。差別在於鎖與資料的關係:C# 的 lock 鎖的是另一個物件,跟 List 之間只有「大家說好」的約定,漏掉一處就是執行期的資料競爭,而且可能好幾個月才重現一次。Rust 把鎖與資料綁成同一個型別,要碰資料就得先拿鎖,編譯器替你守住這個約定。這也是整個進階類反覆出現的主題:把「必須遵守的規則」寫成型別,讓它變成編譯器看得懂、也擋得住的東西。`,
    },
    {
      id: "2-12-19",
      question: "一個小工具把第一個命令列參數當成 port。以下寫法執行 cargo run -- abc 的結果是?",
      questionCode: "use std::env;\n\nfn main() {\n    let arg = env::args().nth(1).unwrap();\n    let port: u16 = arg.parse().unwrap();\n    println!(\"port = {}\", port);\n}",
      options: [
        { text: "panic:thread 'main' panicked at src/main.rs:5:33: called `Result::unwrap()` on an `Err` value: ParseIntError { kind: InvalidDigit },後面還跟著 note: run with `RUST_BACKTRACE=1` ...,行程以 101 結束——使用者拿到的是一份程式當掉的報告,而不是一句「參數要填數字」" },
        { text: "印出 error: invalid digit found in string 並以 1 結束:unwrap 在 main 裡會被特別處理成友善的錯誤訊息" },
        { text: "程式安靜結束、什麼都不印、狀態碼 0:unwrap 失敗時會回傳該型別的預設值" },
        { text: "編譯失敗:main 沒有回傳 Result,函式裡就不能出現可能失敗的呼叫" },
      ],
      answer: 0,
      explanation: `unwrap 的語意是「我保證這不會失敗,失敗就當場停掉」。寫一次性的實驗程式無所謂,一旦有第二個人用就差很多:panic 訊息帶著檔名、行號與 RUST_BACKTRACE 提示,那是給開發者看的東西;退出碼是 panic 固定的 101,而不是慣例上表示「執行失敗」的 1;而且使用者完全不知道自己做錯了什麼。
「unwrap 會回傳預設值」說的其實是 unwrap_or(0),unwrap 沒有安靜失敗這個選項。「main 裡的 unwrap 會變友善」也不存在——panic 就是 panic,不會因為在哪個函式裡就換一種樣子。
慣用的收尾是讓 main 回傳 Result<(), Box<dyn Error>>(2-6 學過的寫法),把 unwrap 換成 ?:失敗時執行環境會印出 Error: 加上錯誤值,並以 1 結束。要注意印的是 Debug 而不是 Display,所以 ok_or("usage: ...")? 這種字串錯誤會印成帶引號的 Error: "usage: ..."。真的要給使用者看的訊息,慣例是自己 match 之後 eprintln! 再 std::process::exit,或者用 anyhow 這類 crate 把報告格式弄好看;不過光是換成回傳 Result,就已經從「程式當掉」變成「程式回報失敗」了。`,
      walkthrough: [
        {
          label: "X Question code (compiles, but panics on bad input)",
          lines: [
            { code: "use std::env;", note: "取得命令列參數用的模組。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "回傳型別是 ()——也就是「這支程式不會回報失敗」。" },
            { code: "    let arg = env::args().nth(1).unwrap();", note: "⛔ 第一個地雷:沒給參數時 nth(1) 是 None,這裡 panic,訊息是 called `Option::unwrap()` on a `None` value——使用者只知道程式壞了。" },
            { code: "    let port: u16 = arg.parse().unwrap();", note: "⛔ 第二個地雷:參數是 abc 時 parse 回傳 Err,這裡 panic:called `Result::unwrap()` on an `Err` value: ParseIntError { kind: InvalidDigit },退出碼 101。" },
            { code: "    println!(\"port = {}\", port);", note: "輸入正確時才走得到這裡。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "兩個 unwrap 各自代表一個沒被處理的失敗情境。程式能跑不代表它處理過這些情境——unwrap 是把問題推遲到執行期,而且推給使用者。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::env;", note: "同上。" },
            { code: "use std::error::Error;", note: "為了在回傳型別裡寫 Box<dyn Error>。" },
            { code: "", note: "" },
            { code: "fn main() -> Result<(), Box<dyn Error>> {", note: "關鍵的一行:main 現在會回報失敗。標準庫允許 main 回傳 Result,條件是錯誤型別實作了 Debug。" },
            { code: "    let arg = env::args().nth(1).ok_or(\"usage: portcheck <port>\")?;", note: "ok_or 把 None 換成一則說明文字,? 在失敗時提早結束 main。字串能直接當錯誤用,是因為標準庫有現成的轉換。" },
            { code: "    let port: u16 = arg.parse()?;", note: "parse 的 ParseIntError 也會被 ? 自動裝箱成 Box<dyn Error>,不必手動轉換。" },
            { code: "    println!(\"port = {}\", port);", note: "成功路徑,跟原本一模一樣。" },
            { code: "    Ok(())", note: "明確表示成功。少了這一行會編譯失敗——回傳型別是 Result,就得交出一個 Result。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "三種輸入的實際結果:cargo run -- 8080 印出 port = 8080、退出碼 0;cargo run -- abc 印出 Error: ParseIntError { kind: InvalidDigit }、退出碼 1;完全不給參數則印出 Error: \"usage: portcheck <port>\"(引號來自 Debug 格式)、退出碼 1。沒有堆疊、沒有 RUST_BACKTRACE 提示,退出碼也符合慣例。",
        },
      ],
      csharp: `等同於一個 C# Console App 完全不接例外,讓 Unhandled exception 加一整串堆疊噴到主控台:能跑,但不專業。兩邊的專業做法也很像——C# 在 Main 外圍 try/catch 再回傳退出碼,Rust 讓 main 回傳 Result 或自己 match。真正的差別在編譯器幫不幫你記得:C# 的例外是隱形的,哪一行可能丟什麼完全看文件;Rust 的失敗寫在回傳型別裡,你必須主動寫下 unwrap 才能忽略它——而那個字在 code review 時看得見,這是刻意的設計。`,
    },
    {
      id: "2-12-20",
      question: "本課與整個進階類的總結:一個 C# 開發者把常用工具搬到 Rust 生態,哪一組對應與結論最準確?",
      options: [
        { text: "System.Text.Json 對應 serde 加 serde_json(功能相近,但 Rust 的實作是編譯期由 derive 產生的程式碼,沒有執行期反射)、System.CommandLine 對應 clap、TPL 與執行緒池對應 tokio(async)或 std::thread(OS 執行緒)、NuGet 對應 cargo 加 crates.io。整體來說,Rust 把 C# 留到執行期才會發現的一整類錯誤——空參考、資料競爭、忘了處理的例外、對不上的序列化欄位——用型別與編譯期檢查擋在前面,代價是意圖必須顯式寫出來" },
        { text: "兩邊其實是同一套東西換個名字:serde 就是反射式序列化、tokio 就是執行緒池,語法換一下就好,思考方式不用改" },
        { text: "Rust 沒有對應的生態,這些功能都得自己實作,這正是 Rust 專案開發速度慢的原因" },
        { text: "Rust 的安全保證來自執行期的額外檢查,所以雖然比 C# 安全,執行效能明顯較差" },
      ],
      answer: 0,
      explanation: `對應表只是表面;真正的差別是「檢查發生在什麼時候」,而這也是整個進階類十二課共同的主題。序列化由 derive 在編譯期產生程式碼(第 6、7 題),型別對不上是編譯錯誤而不是執行期的 JsonException;並行由型別系統在編譯期擋下資料競爭(2-7、第 18 題),不是靠測試碰運氣;錯誤是一個必須被處理的值而不是可以忽略的例外(2-6、第 19 題);借用檢查器讓「別人還在用的東西被改掉」這件事根本寫不出來。
代價也是真的,而且這一課處處都是:沒有具名引數,所以要寫 builder(第 11 題);沒有隱式轉換,所以要在簽名寫出 impl Into<String>(第 12 題);沒有 GC 與執行期同步,所以共享狀態要自己組出 Arc<Mutex<T>>。這些顯式的東西換到的是「編譯過了就幾乎不會在半夜出事」。
「安全來自執行期檢查」正好相反:Rust 的保證幾乎全部在編譯期完成,執行期沒有 GC、沒有額外的型別查詢,這才是它能跟 C 與 C++ 比效能的原因。「思考方式不用改」則是這十二課從頭到尾都在反駁的一句話——從閉包怎麼捕獲環境開始,每一課都有一個需要改變的地方。`,
      walkthrough: [
        {
          label: "◇ C# equivalent: reflection-based JSON, exceptions on failure",
          lang: "csharp",
          lines: [
            { code: "using System.Text.Json;", note: "序列化功能隨 BCL 一起來,不需要任何套件或開關。" },
            { code: "", note: "" },
            { code: "public record Config(string Host, int Port);", note: "型別宣告。序列化器在執行期用反射(或產生器)認得這些屬性。" },
            { code: "", note: "" },
            { code: "public static class Program", note: "進入點所在的類別。" },
            { code: "{", note: "類別本體開始。" },
            { code: "    public static void Main()", note: "回傳 void:這支程式沒有在簽名上表達「可能失敗」。" },
            { code: "    {", note: "方法本體開始。" },
            { code: "        var text = \"{\\\"host\\\":\\\"localhost\\\",\\\"port\\\":8080}\";", note: "待解析的 JSON。鍵是小駝峰,跟屬性名大小寫不同。" },
            { code: "        var options = new JsonSerializerOptions { PropertyNameCaseInsensitive = true };", note: "要自己打開忽略大小寫,否則對不上——跟第 8 題是同一個問題的兩種解法。" },
            { code: "        var cfg = JsonSerializer.Deserialize<Config>(text, options);", note: "失敗時丟 JsonException。編譯器不會提醒你這件事,也不會要求你處理。" },
            { code: "        Console.WriteLine($\"{cfg!.Host}:{cfg.Port}\");", note: "回傳值可能是 null,所以要用 ! 告訴編譯器「相信我」——又一個把責任推給人的地方。" },
            { code: "    }", note: "方法結束。" },
            { code: "}", note: "類別結束。" },
          ],
        },
        {
          label: "√ Rust: the same job, checked at compile time",
          lines: [
            { code: "use serde::{Deserialize, Serialize};", note: "trait 與 derive 巨集(Cargo.toml 要寫 serde = { version = \"1\", features = [\"derive\"] } 與 serde_json = \"1\")。" },
            { code: "", note: "" },
            { code: "#[derive(Serialize, Deserialize, Debug)]", note: "編譯期產生序列化與反序列化的程式碼。執行期沒有反射,也沒有型別查詢。" },
            { code: "struct Config {", note: "對應的型別。" },
            { code: "    host: String,", note: "欄位名與 JSON 的鍵一致,所以這次不用 rename。" },
            { code: "    port: u16,", note: "型別比 C# 的 int 更窄:通訊埠本來就只有 0 到 65535,範圍寫進型別裡,超出範圍在解析當下就是錯誤。" },
            { code: "}", note: "struct 結束。" },
            { code: "", note: "" },
            { code: "fn main() -> Result<(), Box<dyn std::error::Error>> {", note: "簽名就說明了這支程式可能失敗,呼叫端(這裡是作業系統)拿得到非零退出碼。" },
            { code: "    let text = r#\"{\"host\":\"localhost\",\"port\":8080}\"#;", note: "原始字串字面值,JSON 裡的雙引號不必跳脫。" },
            { code: "    let cfg: Config = serde_json::from_str(text)?;", note: "失敗就提早返回。沒有 null:拿到的 cfg 一定是一個完整的 Config,不需要任何「相信我」的標記。" },
            { code: "    println!(\"{}:{}\", cfg.host, cfg.port);", note: "印出 localhost:8080。" },
            { code: "    println!(\"{}\", serde_json::to_string(&cfg)?);", note: "再序列化回去,印出 {\"host\":\"localhost\",\"port\":8080}——來回一趟資料沒有失真。" },
            { code: "    Ok(())", note: "成功結束,退出碼 0。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "兩段程式做的事一樣,分量也差不多。差別在於:C# 那邊有兩個地方靠「人會記得」(可能丟例外、可能是 null),Rust 這邊都變成了簽名的一部分,漏掉就編不過。整個進階類談的東西,收束起來就是這一句話。",
        },
      ],
      csharp: `最後三個帶著走的結論。一、找套件的方式要換:不要先問「標準庫有沒有」,而是問「這個領域的事實標準 crate 是哪一個」,然後去 docs.rs 看它的 API 與 Feature flags。二、Cargo.toml 不只是相依清單,feature、profile、workspace 都是專案層級的設計決定,值得花時間讀懂。三、Rust 要你顯式寫出來的每一件事——所有權、生命週期、錯誤、共享、轉換——都是 C# 裡本來就存在、只是沒人逼你講清楚的東西。寫起來比較慢,換到的是一整類 bug 在編譯期就被擋下,以及一份看簽名就知道會發生什麼事的程式碼。`,
    },
  ],
};
