/* 出題慣例見專案根目錄 AUTHORING.md:
 *   - answer 一律為 0(正確答案寫在第一個選項),顯示順序由 quiz.js 依題目 id 洗牌
 *   - 詳解禁止用「選項 A/B/C」字母指涉,必須直接描述選項內容
 *   - 有程式碼的題目一律附 walkthrough(逐行說明);反面案例必須同時附上 √ 正確寫法 */
window.RUST_LESSONS = window.RUST_LESSONS || {};
window.RUST_LESSONS["lesson2-8"] = {
  id: "lesson2-8",
  title: "非同步 async/await",
  goal: "看懂 Future 是惰性的、為什麼一定要有 runtime 來驅動它,會用 join! / select! / tokio::spawn 安排並發,也看得懂 async 撞上所有權與生命週期時編譯器到底在抱怨什麼。",
  primer: {
    intro: "非同步(async)讓一條執行緒在等 I/O 的時候轉頭去做別的事,不必像 2-7 那樣替每個工作開一條作業系統執行緒。Rust 的做法是把 async 函式的本體編譯成一台狀態機,每個 .await 都是一個可以暫停、之後再從原地接續的點。標準庫只定義了這台狀態機的介面,真正輪流驅動它們的執行器(runtime)不在標準庫裡,要自己挑一個 crate——本課一律用 tokio(1.x),每支程式的起手式都是在 main 上掛 #[tokio::main]。",
    examples: [
      {
        code: "use tokio::time::{sleep, Duration};\n\nasync fn greet(name: &str) {\n    sleep(Duration::from_millis(10)).await;\n    println!(\"hello {}\", name);\n}\n\n#[tokio::main]\nasync fn main() {\n    greet(\"rust\").await;\n}",
        note: "greet 等 10 毫秒再印出 hello rust;#[tokio::main] 讓 main 可以寫成 async fn,並在背後備好一個 runtime。",
      },
    ],
    csharp: "語法幾乎和 C# 一模一樣:async 標在函式上、await 接在呼叫後面。真正的差別在「這段工作是誰、在什麼時候推動的」,後面的題目會一題一題把它拆開。",
  },
  questions: [
    {
      id: "2-8-01",
      question: "呼叫一個 async fn 之後,函式本體是從哪一刻開始執行的?以下程式印出什麼?",
      questionCode: "async fn step(name: &str) -> u32 {\n    println!(\"running {}\", name);\n    1\n}\n\n#[tokio::main]\nasync fn main() {\n    let a = step(\"a\");\n    println!(\"created\");\n    let n = a.await;\n    println!(\"n = {}\", n);\n}",
      options: [
        { text: "created、running a、n = 1——呼叫 step(\"a\") 只是把一個「還沒開始的工作」建出來,本體一行都還沒跑;直到 a.await 把它交給 runtime 驅動,running a 才被印出來" },
        { text: "running a、created、n = 1——呼叫 step(\"a\") 的當下本體就執行到底了,.await 只是把已經算好的結果取出來" },
        { text: "created、n = 1——本體從頭到尾沒被執行,.await 只會拿到回傳型別的預設值 1" },
        { text: "created、running a、n = 1,但順序沒有保證——呼叫的那一刻 runtime 就可能在背景開始推動 step,running a 有機會插到 created 前面" },
      ],
      answer: 0,
      explanation: `這是本課最重要的一件事,也是 C# 開發者第一個會摔跤的地方:async fn 是惰性的。step("a") 這個呼叫只做一件事——把本體編譯成的狀態機建出來、把參數存進去,然後回傳。println! 屬於本體,所以一行都不會跑。
真正推動它的是 .await:它把狀態機交給 runtime,runtime 從頭開始推,印出 running a、算出 1、狀態機完成,.await 才拿到那個 1。所以順序是 created(呼叫後的下一行)、running a(被 await 才跑)、n = 1。
「呼叫的當下本體就跑到底」是把 C# 的心智模型直接搬過來——C# 的 async 方法確實會同步執行到第一個 await。「順序沒有保證,runtime 可能在背景先跑起來」則是另一種常見誤解:沒有人 await 它、也沒有人 spawn 它之前,根本沒有任何 runtime 知道這台狀態機存在,談不上背景執行。這一題的輸出是完全確定的。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "async fn step(name: &str) -> u32 {", note: "async fn。簽名寫 -> u32,意思是「最後會產出一個 u32」,不是「呼叫後馬上得到 u32」。" },
          { code: "    println!(\"running {}\", name);", note: "本體第一行。它要等這台狀態機被驅動才會執行。" },
          { code: "    1", note: "本體的結果值。" },
          { code: "}", note: "step 定義結束。" },
          { code: "", note: "" },
          { code: "#[tokio::main]", note: "把 main 包成「建立 runtime 並驅動這段 async 本體」,詳情見第 4 題。" },
          { code: "async fn main() {", note: "main 因此可以寫成 async,裡面才能用 .await。" },
          { code: "    let a = step(\"a\");", note: "只是建立狀態機並把 name 存進去,本體完全沒執行。a 現在是一個「待驅動的工作」。" },
          { code: "    println!(\"created\");", note: "所以 created 是第一個被印出來的。" },
          { code: "    let n = a.await;", note: "把 a 交給 runtime 推到完成:此時才印出 running a,然後把本體的結果 1 交回來給 n。" },
          { code: "    println!(\"n = {}\", n);", note: "印出 n = 1,最後一行。" },
          { code: "}", note: "main 結束,runtime 關閉。" },
        ],
        outro: "記住這條分界線:呼叫 async fn = 建立工作;.await = 驅動工作。本課後面所有陷阱都是這條線的延伸。",
      },
      csharp: `C# 裡 var t = StepAsync("a"); 會立刻同步執行 StepAsync 的本體,一路跑到第一個 await 為止——所以 Console.WriteLine("running a") 在建立 Task 的那一行就印出來了,而且就算你從此不理會 t,它也會繼續跑完。Rust 完全相反:沒有人 .await 或 spawn,那台狀態機就只是躺在變數裡的一塊資料。C# 的 Task 是「已經上路的工作」,Rust 的 Future 是「還沒發動的計畫書」。`,
    },
    {
      id: "2-8-02",
      question: "下面這段程式想在結束前先把資料存起來。它實際上印出什麼?",
      questionCode: "async fn save(id: u32) {\n    println!(\"saved {}\", id);\n}\n\n#[tokio::main]\nasync fn main() {\n    save(1);\n    println!(\"done\");\n}",
      options: [
        { text: "只印出 done——save(1) 建出來的工作沒有被 .await、也沒有交給 runtime,當場就被丟棄,saved 1 永遠不會出現;編譯器只會給一個 unused_must_use 警告,不是錯誤" },
        { text: "印出 saved 1、done——呼叫 save(1) 就等於開始執行,.await 只是「等它結束」,不寫也照樣會跑完" },
        { text: "印出 done,然後 runtime 在 main 結束前會把沒被 await 的工作補跑完,所以 saved 1 出現在 done 後面" },
        { text: "編譯錯誤:async fn 的回傳值一定要被 .await,少寫編譯器就拒絕通過" },
      ],
      answer: 0,
      explanation: `承上題:save(1) 只是建立一台狀態機。這一行沒有把它綁給任何變數,語句結束時它就被 drop 掉了——一台從來沒被推動過的狀態機被丟棄,什麼副作用都不會發生,saved 1 印不出來。
這種「忘記 await」在 Rust 裡很難默默放過,因為 Future 標了 #[must_use],編譯器會給 warning: unused implementer of \`Future\` that must be used,並附上 note: futures do nothing unless you \`.await\` or poll them。但它是警告,cargo build 照樣成功、程式照樣執行,所以還是有機會上線才發現。
「runtime 會在結束前補跑」是把它想成 C# 的執行緒池餘留工作——Rust 沒有這種機制,沒人持有的狀態機就只是一塊被釋放的記憶體。「不寫 .await 也會跑完」則是第 1 題那個 C# 心智模型的直接後果。`,
      walkthrough: [
        {
          label: "X Question code (compiles, but the save never happens)",
          intro: "這段程式編譯得過、跑得完,只是完全沒做到它想做的事:",
          lines: [
            { code: "async fn save(id: u32) {", note: "一個 async fn,本體有副作用(印東西)。" },
            { code: "    println!(\"saved {}\", id);", note: "這行要等狀態機被驅動才會執行。" },
            { code: "}", note: "save 定義結束。" },
            { code: "", note: "" },
            { code: "#[tokio::main]", note: "備好 runtime。" },
            { code: "async fn main() {", note: "main 本身是 async,裡面可以 .await。" },
            { code: "    save(1);", note: "⛔ 這一行什麼都沒做:呼叫只是把工作打包成一個值,沒有綁給任何變數,語句一結束就被丟棄,本體一行都沒跑。編譯器給的是 unused_must_use 警告,但警告不是錯誤,程式照樣編譯執行。" },
            { code: "    println!(\"done\");", note: "唯一會被印出來的一行。" },
            { code: "}", note: "main 結束。輸出只有 done。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "async fn save(id: u32) {", note: "save 本身不用改。" },
            { code: "    println!(\"saved {}\", id);", note: "本體不變。" },
            { code: "}", note: "定義結束。" },
            { code: "", note: "" },
            { code: "#[tokio::main]", note: "備好 runtime。" },
            { code: "async fn main() {", note: "進入點。" },
            { code: "    save(1).await;", note: "改動處:補上 .await,把狀態機交給 runtime 推到完成,saved 1 才真的被印出來。" },
            { code: "    println!(\"done\");", note: "接著印 done。" },
            { code: "}", note: "輸出變成 saved 1 與 done 兩行。" },
          ],
          outro: "看到 unused_must_use 這個警告就要立刻停下來檢查:在 async 程式裡,它幾乎總是代表「你漏了一個 .await」。",
        },
      ],
      csharp: `C# 的同一個寫法(呼叫 SaveAsync(1); 卻不 await)是另一種 bug:工作真的會開始跑,但你沒有等它,例外會被吞掉、main 也可能在它寫完之前就結束——這就是所謂的 fire-and-forget。編譯器最多給你一個 CS4014 警告。Rust 的版本更極端:它連跑都不會跑。兩邊的警告看起來像,含意完全不同——C# 是「你沒等」,Rust 是「你根本沒發動」。`,
    },
    {
      id: "2-8-03",
      question: "兩個工作被放進同一個 tokio::join! 裡並發執行。.await 在中間做了什麼?以下程式印出什麼?",
      questionCode: "use tokio::time::{sleep, Duration};\n\nasync fn work(name: &str, ms: u64) {\n    println!(\"{} start\", name);\n    sleep(Duration::from_millis(ms)).await;\n    println!(\"{} end\", name);\n}\n\n#[tokio::main]\nasync fn main() {\n    tokio::join!(work(\"alpha\", 60), work(\"beta\", 20));\n    println!(\"all done\");\n}",
      options: [
        { text: "alpha start、beta start、beta end、alpha end、all done——alpha 推進到 sleep 的 .await 時還沒就緒,就把控制權交還 runtime,runtime 轉去推動 beta;beta 只睡 20 毫秒所以先醒來" },
        { text: "alpha start、alpha end、beta start、beta end、all done——.await 是原地等待,會把整條執行緒停住,所以 alpha 一定整個跑完才輪得到 beta" },
        { text: "alpha start、beta start、alpha end、beta end、all done——兩個工作幾乎同時開始,結束順序自然跟著開始順序走" },
        { text: "beta start、beta end、alpha start、alpha end、all done——runtime 會先比較兩個 sleep 的長度,優先安排比較快做完的工作" },
      ],
      answer: 0,
      explanation: `.await 不是「原地站著等」,而是一個交接點:如果被 await 的工作還沒就緒,目前這台狀態機就把控制權交還給 runtime,並記住自己停在哪一行;等就緒了再被叫醒,從那一行繼續。
所以流程是:runtime 先推 alpha,印出 alpha start,碰到 sleep(60) 還沒到期,交還控制權;runtime 轉去推 beta,印出 beta start,碰到 sleep(20) 也還沒到期,再次交還;兩台狀態機都暫停了,runtime 就去等計時器。20 毫秒後 beta 被叫醒,印 beta end 並完成;60 毫秒後 alpha 被叫醒,印 alpha end。兩個都完成,join! 才回傳,最後印 all done。
「.await 會把整條執行緒停住」是最要命的誤解——那樣的話整個 async 就沒有意義了,一條執行緒永遠只能做一件事。真正會把執行緒停住的是 std::thread::sleep 這種同步阻塞呼叫(第 17 題的主題)。「結束順序跟著開始順序」則是忽略了每個工作等待的時間長短不同。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use tokio::time::{sleep, Duration};", note: "tokio 自己的 sleep:它是 async 的,等待期間會讓出控制權,跟 std::thread::sleep 完全不同。" },
          { code: "", note: "" },
          { code: "async fn work(name: &str, ms: u64) {", note: "一個會先報到、睡一下、再報離開的工作。" },
          { code: "    println!(\"{} start\", name);", note: "這台狀態機被推動時印出的第一件事。" },
          { code: "    sleep(Duration::from_millis(ms)).await;", note: "關鍵這一行:計時器還沒到期,狀態機就在這裡暫停並交還控制權,runtime 可以去做別的事。" },
          { code: "    println!(\"{} end\", name);", note: "計時器到期、狀態機被叫醒後,從上一行的下一步接著跑。" },
          { code: "}", note: "work 定義結束。" },
          { code: "", note: "" },
          { code: "#[tokio::main]", note: "備好 runtime。" },
          { code: "async fn main() {", note: "進入點。" },
          { code: "    tokio::join!(work(\"alpha\", 60), work(\"beta\", 20));", note: "把兩台狀態機交給同一個 join!,由它輪流推動:alpha 先被推(印 alpha start),暫停後換 beta(印 beta start)。" },
          { code: "    println!(\"all done\");", note: "join! 要兩個都完成才回傳,所以這行一定在 beta end 與 alpha end 之後。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "整段程式總共只花約 60 毫秒,而不是 60 + 20 = 80 毫秒——兩段等待是重疊的。同一條執行緒在這 60 毫秒裡其實閒著,隨時可以再接更多工作。",
      },
      csharp: `C# 的 await 語意在這裡是一樣的:await Task.Delay(60) 也是把方法「掛起」、把執行緒還給執行緒池,不是把執行緒卡住。C# 開發者對這題通常有正確直覺。要注意的差別在於誰來叫醒你:C# 由 SynchronizationContext / TaskScheduler 決定續跑在哪條執行緒,Rust 則是你選的那個 runtime(這裡是 tokio)說了算,而且預設不保證回到原來那條執行緒。`,
    },
    {
      id: "2-8-04",
      question: "這支程式沒有 #[tokio::main],main 也不是 async fn。它能跑嗎?",
      questionCode: "use tokio::runtime::Runtime;\n\nasync fn hello() -> &'static str {\n    \"hi\"\n}\n\nfn main() {\n    let rt = Runtime::new().unwrap();\n    let msg = rt.block_on(hello());\n    println!(\"{}\", msg);\n}",
      options: [
        { text: "能跑,印出 hi——#[tokio::main] 本來就只是語法糖,展開後就是「建立一個 runtime,再用 block_on 驅動那段 async 本體」;自己手寫一樣有效" },
        { text: "編譯錯誤:async fn 只能出現在標了 #[tokio::main] 的程式裡,少了那個屬性連 hello 都定義不起來" },
        { text: "編譯錯誤:block_on 也是一種等待,必須寫在 async 函式裡才合法" },
        { text: "能編譯,但執行時 panic——沒有 #[tokio::main] 就沒有 runtime,block_on 找不到執行器可用" },
      ],
      answer: 0,
      explanation: `Rust 標準庫只定義了 Future 這個介面(以及 async/.await 這層語法),沒有附任何執行器。誰來一次又一次推動狀態機、誰來註冊計時器與 I/O 事件,全部是 runtime crate 的事——這就是為什麼寫 async 一定要多拉一個 tokio(或 async-std、smol⋯)。
#[tokio::main] 做的事非常單純:把你寫的 async fn main 本體搬進一個 async 區塊,再產生大致等同於 Runtime::new().unwrap().block_on(...) 的程式碼。這一題就是把那層糖手動攤開,所以當然能跑,印出 hi。
block_on 是 async 世界與同步世界的橋:它會阻塞目前這條(同步的)執行緒,直到那台狀態機跑完。因此它必須寫在普通 fn 裡,寫在 async 函式裡反而是錯的(會把自己的 runtime 執行緒卡住)。「沒有 #[tokio::main] 就沒有 runtime」這個選項只對了一半——runtime 不是屬性變出來的,是 Runtime::new() 建出來的。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use tokio::runtime::Runtime;", note: "直接引入 runtime 型別,這是 #[tokio::main] 背後真正在用的東西。" },
          { code: "", note: "" },
          { code: "async fn hello() -> &'static str {", note: "一個普通的 async fn。它不需要任何屬性標記就能定義。" },
          { code: "    \"hi\"", note: "本體:直接產出一個字串常值。" },
          { code: "}", note: "hello 定義結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "注意這是普通 fn,不是 async fn——程式的進入點本來就不能是 async,一定要有人先把 runtime 建起來。" },
          { code: "    let rt = Runtime::new().unwrap();", note: "建立一個多執行緒 runtime(預設 flavor),它內部有一組工作執行緒與一個事件迴圈。" },
          { code: "    let msg = rt.block_on(hello());", note: "hello() 先建出狀態機,block_on 把它交給 rt 一路推到完成,並阻塞目前這條執行緒等結果。msg 拿到 \"hi\"。" },
          { code: "    println!(\"{}\", msg);", note: "印出 hi。" },
          { code: "}", note: "rt 在這裡被 drop,runtime 關閉。" },
        ],
        outro: "實務上還是寫 #[tokio::main] 比較省事;手動建 runtime 的價值在於你可以自訂執行緒數量、或在一支原本同步的程式裡只讓某一小段走 async。",
      },
      csharp: `C# 沒有這一步,因為 .NET 執行時本身就內建了執行緒池與 SynchronizationContext——你寫 await 就有人排程,不必挑也不必安裝。Rust 選擇把執行器留給生態系,好處是嵌入式、瀏覽器 WASM、高吞吐伺服器可以各自用最合適的排程策略,代價就是每支 async 程式都要自己決定用哪一個 runtime,而且不同 runtime 的 API(spawn、sleep、channel)彼此不通用。`,
    },
    {
      id: "2-8-05",
      question: "下面這支程式想在普通的 main 裡直接 .await 一個 async fn,結果是?",
      questionCode: "async fn load() -> u32 {\n    42\n}\n\nfn main() {\n    let n = load().await;\n    println!(\"{}\", n);\n}",
      options: [
        { text: "編譯錯誤:`await` is only allowed inside `async` functions and blocks——main 是普通函式,裡面不能有 .await" },
        { text: "能跑,印出 42——只要專案引入了 tokio,.await 在任何函式裡都合法" },
        { text: "能編譯,但執行時 panic——.await 語法本身沒問題,是因為沒有 runtime 在跑才炸掉" },
        { text: "編譯錯誤:普通函式裡根本不能呼叫 async fn,load() 這一行就已經過不了" },
      ],
      answer: 0,
      explanation: `.await 是一個暫停點,而「暫停」這件事只有狀態機做得到:編譯器必須把你寫的函式本體整個改寫成可以中途停下、之後接續的結構,而它只對標了 async 的函式與 async 區塊做這件改寫。所以普通的 fn main 裡出現 .await,編譯器連改寫的機會都沒有,直接報錯:\`await\` is only allowed inside \`async\` functions and blocks。
要注意錯的只有 .await 那一半。「普通函式裡不能呼叫 async fn」是錯的——load() 這個呼叫完全合法,它只是產生一個值而已(第 1 題);真正不合法的是接在後面的 .await。這個區別很重要:它正是第 4 題 block_on 能存在的原因——在普通 fn 裡拿到那個值之後,交給 runtime 去驅動,而不是用 .await。
「引入 tokio 就到處能 await」則是把 crate 當成語言特性:tokio 提供的是執行器,不是語法。修法有兩種:把 main 標成 #[tokio::main] async fn(下面的正確寫法),或照第 4 題手動 block_on。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "async fn load() -> u32 {", note: "定義一個 async fn,這部分沒有問題。" },
            { code: "    42", note: "本體:產出 42。" },
            { code: "}", note: "load 定義結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "普通函式。編譯器不會把它改寫成狀態機,所以它沒有「暫停再接續」的能力。" },
            { code: "    let n = load().await;", note: "⛔ 編譯失敗:`await` is only allowed inside `async` functions and blocks(E0728)。注意 load() 這個呼叫本身是合法的,錯的是後面的 .await。" },
            { code: "    println!(\"{}\", n);", note: "這行本身沒問題,但整支程式已經編譯不過。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "編譯器的附註會指著 main 說 this is not `async`——它已經替你指出該改哪裡了。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "async fn load() -> u32 {", note: "load 不用改。" },
            { code: "    42", note: "本體不變。" },
            { code: "}", note: "定義結束。" },
            { code: "", note: "" },
            { code: "#[tokio::main]", note: "改動處:這個屬性會把下面的 async fn main 包成「建立 runtime 並 block_on 本體」。" },
            { code: "async fn main() {", note: "改動處:main 現在是 async fn,編譯器會把它改寫成狀態機,裡面才能出現 .await。" },
            { code: "    let n = load().await;", note: "這一行原封不動,但現在合法了:它所在的函式是 async 的。n 拿到 42。" },
            { code: "    println!(\"{}\", n);", note: "印出 42。" },
            { code: "}", note: "本體跑完,runtime 關閉,程式結束。" },
          ],
        },
      ],
      csharp: `C# 的限制長得很像:await 只能寫在標了 async 的方法(或 lambda)裡,否則是 CS4033 / CS1061。差別在 C# 的 Main 可以直接寫成 static async Task Main(),編譯器會替你接上執行緒池,不必挑 runtime;Rust 的 fn main 永遠是同步的,要嘛掛 #[tokio::main] 讓 crate 替你把糖攤開,要嘛自己 block_on。另外 C# 開發者常誤以為「同步方法裡不能碰 async 方法」,其實兩邊都可以拿到那個值(Task / Future),只是不能用 await 語法等它。`,
    },
    {
      id: "2-8-06",
      question: "兩個各要等 100 毫秒的工作被放進 join!,總共花多久?以下程式印出什麼?",
      questionCode: "use tokio::time::{sleep, Duration, Instant};\n\nasync fn fetch(id: u32, ms: u64) -> u32 {\n    sleep(Duration::from_millis(ms)).await;\n    id * 10\n}\n\n#[tokio::main]\nasync fn main() {\n    let start = Instant::now();\n    let (a, b) = tokio::join!(fetch(1, 100), fetch(2, 100));\n    println!(\"a = {}, b = {}\", a, b);\n    println!(\"under 150ms: {}\", start.elapsed() < Duration::from_millis(150));\n}",
      options: [
        { text: "a = 10, b = 20 與 under 150ms: true——join! 交錯驅動兩台狀態機,兩段 100 毫秒的等待是重疊的,總時間接近 100 毫秒;兩個都完成之後,結果一次湊成 tuple 交回來" },
        { text: "a = 10, b = 20 與 under 150ms: false——join! 是「依序等」,跑完第一個才輪到第二個,總時間大約 200 毫秒" },
        { text: "a = 10, b = 20 與 under 150ms: true,而且這兩個工作是被丟到兩條不同的執行緒上平行跑的——join! 會自動把每個分支送進執行緒池" },
        { text: "編譯錯誤:join! 只吃已經被 tokio::spawn 出去的 JoinHandle,不能直接塞 async fn 的呼叫結果" },
      ],
      answer: 0,
      explanation: `join! 把每個分支當成一台狀態機,輪流去推:推到有人暫停就換下一個,全部暫停就去等事件。所以兩段 sleep(100) 是同時在倒數的,整段大約 100 毫秒多一點就結束,under 150ms 印出 true。回傳值是一個 tuple,順序跟你寫的分支順序一致,所以 a = 10、b = 20。
「依序等」的直覺來自寫慣同步程式:如果你寫成 let a = fetch(1, 100).await; let b = fetch(2, 100).await;,那才真的是 200 毫秒——兩個 .await 一前一後,第二台狀態機在第一台完成前根本沒被建立、沒被推動。join! 存在的意義就是避免這件事。
「join! 會把分支送進執行緒池平行跑」則是另一個方向的誤解:join! 完全不 spawn,它產生的是「單一一台更大的狀態機」,在呼叫它的那個 task 裡並發,從頭到尾只用一條執行緒也能完成。並發(concurrency)不等於平行(parallelism),要平行得用 tokio::spawn 搭配多執行緒 runtime(第 8、18 題)。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use tokio::time::{sleep, Duration, Instant};", note: "Instant 用來量實際經過的時間,好驗證兩段等待有沒有重疊。" },
          { code: "", note: "" },
          { code: "async fn fetch(id: u32, ms: u64) -> u32 {", note: "模擬一次要等 ms 毫秒的遠端查詢。" },
          { code: "    sleep(Duration::from_millis(ms)).await;", note: "暫停點:計時器到期前把控制權交還 runtime。" },
          { code: "    id * 10", note: "本體的結果,對應簽名的 -> u32。" },
          { code: "}", note: "fetch 定義結束。" },
          { code: "", note: "" },
          { code: "#[tokio::main]", note: "備好 runtime。" },
          { code: "async fn main() {", note: "進入點。" },
          { code: "    let start = Instant::now();", note: "記下起跑時間。" },
          { code: "    let (a, b) = tokio::join!(fetch(1, 100), fetch(2, 100));", note: "兩個呼叫先各自建出狀態機(都還沒跑),join! 再輪流推動它們,並等到兩個都完成才回傳 (10, 20)。" },
          { code: "    println!(\"a = {}, b = {}\", a, b);", note: "tuple 的順序跟分支順序一致,印出 a = 10, b = 20。" },
          { code: "    println!(\"under 150ms: {}\", start.elapsed() < Duration::from_millis(150));", note: "實測約 100 毫秒多,所以印出 true;若是依序執行會逼近 200 毫秒,這裡就會印 false。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "把 join! 那一行改寫成兩個獨立的 .await,輸出的數字完全一樣,只有這個 true 會變成 false——時間是唯一看得出「有沒有並發」的證據。",
      },
      csharp: `對應的是 var (a, b) = await Task.WhenAll(...) 或 C# 的 await t1; await t2; 之後取 Result。有一個關鍵差異:在 C# 裡,就算你寫 await t1; await t2;,兩個工作也早就同時在跑了(Task 建立即執行),總時間一樣是 100 毫秒;Rust 沒有 join!/spawn 就真的是 200 毫秒。換句話說,C# 用 WhenAll 是為了語法方便,Rust 用 join! 是為了真的讓它們並發,這是第 1 題的惰性在實務上最直接的後果。`,
    },
    {
      id: "2-8-07",
      question: "select! 讓多個工作賽跑。以下程式印出什麼?",
      questionCode: "use tokio::time::{sleep, Duration};\n\nasync fn slow() -> &'static str {\n    sleep(Duration::from_millis(100)).await;\n    println!(\"slow finished\");\n    \"slow\"\n}\n\nasync fn fast() -> &'static str {\n    sleep(Duration::from_millis(10)).await;\n    println!(\"fast finished\");\n    \"fast\"\n}\n\n#[tokio::main]\nasync fn main() {\n    let winner = tokio::select! {\n        r = slow() => r,\n        r = fast() => r,\n    };\n    println!(\"winner = {}\", winner);\n    sleep(Duration::from_millis(200)).await;\n    println!(\"end\");\n}",
      options: [
        { text: "fast finished、winner = fast、end——select! 只等第一個完成的分支,勝負一揭曉,其餘分支的狀態機就地被丟棄,slow 永遠停在 sleep 那裡不會再被推動,所以 slow finished 不會出現" },
        { text: "fast finished、winner = fast、slow finished、end——select! 取最先完成的當結果,落敗的分支仍會在背景被推動到完成,只是結果沒人收" },
        { text: "slow finished、fast finished、winner = slow、end——select! 會等所有分支都完成,再把寫在最前面那個分支的結果當回傳值" },
        { text: "winner = fast、fast finished、end——select! 會先決定勝負,再去執行勝出分支的本體,所以 winner 先被印出來" },
      ],
      answer: 0,
      explanation: `select! 同時推動所有分支,誰先完成就用誰的結果,然後——這是關鍵——整個 select! 運算式結束,其他分支的狀態機當場被 drop。被 drop 的狀態機停在哪就停在哪,再也不會被推動,所以 slow 卡在 sleep 的暫停點上,println!("slow finished") 那行永遠輪不到。程式最後那個 sleep(200) 就是故意留的時間窗:即使多等 200 毫秒,slow finished 也不會冒出來。
「落敗分支會在背景跑完」是從 C# 搬過來的直覺,而且在 C# 是對的:Task.WhenAny 回傳之後,沒被選中的 Task 依然自己在執行緒池上跑,你甚至要另外處理它拋出的例外。Rust 這裡是「取消」的預設語意——丟掉 Future 就等於取消這個工作,不需要 CancellationToken。
「等所有分支完成」講的是 join! 的行為(第 6 題),不是 select!。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use tokio::time::{sleep, Duration};", note: "兩個工作靠不同長度的 sleep 分出勝負。" },
          { code: "", note: "" },
          { code: "async fn slow() -> &'static str {", note: "慢的那一個。" },
          { code: "    sleep(Duration::from_millis(100)).await;", note: "暫停點。slow 的狀態機會一直停在這裡,直到被推動或被丟棄。" },
          { code: "    println!(\"slow finished\");", note: "這一行是觀測點:只要它被印出來,就代表 slow 有被推到完成。" },
          { code: "    \"slow\"", note: "本體結果。" },
          { code: "}", note: "slow 定義結束。" },
          { code: "", note: "" },
          { code: "async fn fast() -> &'static str {", note: "快的那一個,只睡 10 毫秒。" },
          { code: "    sleep(Duration::from_millis(10)).await;", note: "暫停點。" },
          { code: "    println!(\"fast finished\");", note: "10 毫秒後被叫醒,印出這一行。" },
          { code: "    \"fast\"", note: "本體結果。" },
          { code: "}", note: "fast 定義結束。" },
          { code: "", note: "" },
          { code: "#[tokio::main]", note: "備好 runtime。" },
          { code: "async fn main() {", note: "進入點。" },
          { code: "    let winner = tokio::select! {", note: "建立兩個分支的狀態機並輪流推動,誰先完成就採用誰。" },
          { code: "        r = slow() => r,", note: "第一個分支:完成時把結果綁給 r,再把 r 當成整個 select! 的值。" },
          { code: "        r = fast() => r,", note: "第二個分支同理。10 毫秒後它先完成,勝出。" },
          { code: "    };", note: "select! 在這裡結束——落敗的 slow 狀態機就地被 drop,等於被取消。" },
          { code: "    println!(\"winner = {}\", winner);", note: "印出 winner = fast。順序上它一定在 fast finished 之後,因為那一行是 fast 本體跑完前印的。" },
          { code: "    sleep(Duration::from_millis(200)).await;", note: "刻意多等 200 毫秒:足夠 slow 的計時器到期,但 slow 已經被丟掉了,不會有人推動它。" },
          { code: "    println!(\"end\");", note: "印出 end。整段輸出共三行,沒有 slow finished。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "「丟掉 Future = 取消工作」是 Rust async 很省事的一面,但也意味著工作可能停在任何一個 .await 上——寫到會改動共享狀態的程式時,要考慮「停在半途」是不是安全的。",
      },
      csharp: `語意上對應 Task.WhenAny,但兩邊對「落選者」的處理完全相反。C# 的 Task 已經在跑了,WhenAny 只是先把最快的那個交給你,其餘的繼續跑到完;要真的停掉得靠 CancellationToken,而且被取消的工作要自己檢查 token。Rust 是把落選的 Future 直接 drop,狀態機連同它捕獲的資源一起釋放,取消是免費且立即的。代價是你必須接受「工作可能停在任何暫停點」,C# 開發者習慣的「這段程式一定會跑完」在這裡不成立。`,
    },
    {
      id: "2-8-08",
      question: "這支程式的 runtime 只有一條執行緒(flavor = \"current_thread\"),tokio::spawn 還能用嗎?印出什麼?",
      questionCode: "use tokio::time::{sleep, Duration};\n\n#[tokio::main(flavor = \"current_thread\")]\nasync fn main() {\n    let handle = tokio::spawn(async {\n        sleep(Duration::from_millis(20)).await;\n        println!(\"task done\");\n        7\n    });\n    println!(\"spawned\");\n    let n = handle.await.unwrap();\n    println!(\"n = {}\", n);\n}",
      options: [
        { text: "spawned、task done、n = 7——tokio::spawn 建立的是 task(一台交給 runtime 排程的狀態機),不是作業系統執行緒,所以單執行緒 runtime 一樣排得動;回傳的 JoinHandle 本身可以被 .await,拿到 Result<i32, JoinError>" },
        { text: "spawned、task done、n = 7,而且這個 task 是在另一條作業系統執行緒上跑的——tokio::spawn 跟 thread::spawn 一樣,每呼叫一次就開一條新執行緒" },
        { text: "編譯錯誤:flavor = \"current_thread\" 的 runtime 只有一條執行緒,tokio::spawn 必須搭配多執行緒 runtime 才能用" },
        { text: "task done、spawned、n = 7——spawn 會立刻切過去把 task 跑完,回到 main 才印 spawned" },
      ],
      answer: 0,
      explanation: `tokio::spawn 的單位是 task,不是執行緒。一個 task 就是一台被登記到 runtime 排程佇列裡的狀態機,runtime 用它手上的執行緒(這裡只有一條)輪流推動所有 task。所以單執行緒 runtime 完全可以同時跑成千上萬個 task,只要它們大部分時間都停在 .await 上等 I/O——這正是 async 相對 thread 最大的優勢:一個 task 的成本是一小塊狀態機記憶體,一條執行緒的成本是作業系統的堆疊(通常以 MB 計)。
輸出順序是確定的:spawn 只是把 task 排進佇列就回傳,所以 spawned 先印;直到 main 執行到 handle.await 交出控制權,runtime 才有機會推動那個 task,20 毫秒後印出 task done;task 完成後 handle.await 拿到 Ok(7),印出 n = 7。
「每次 spawn 開一條執行緒」是把 2-7 的 thread::spawn 直接套過來——那是這一課最需要拆掉的類比。JoinHandle 這個名字雖然一樣,用法也不同:2-7 的是呼叫 .join() 阻塞等待,這裡的是 .await(它本身就是一台狀態機),而且同樣把 panic 包成 Err 交回來。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use tokio::time::{sleep, Duration};", note: "async 版的 sleep。" },
          { code: "", note: "" },
          { code: "#[tokio::main(flavor = \"current_thread\")]", note: "指定單執行緒 runtime:整個程式從頭到尾只有 main 這一條執行緒在推動所有 task,方便看出排程順序。" },
          { code: "async fn main() {", note: "進入點。" },
          { code: "    let handle = tokio::spawn(async {", note: "把這個 async 區塊登記成一個獨立 task,立刻回傳 JoinHandle。這一行不會執行區塊本體。" },
          { code: "        sleep(Duration::from_millis(20)).await;", note: "task 被推動後停在這個暫停點,把控制權交還 runtime。" },
          { code: "        println!(\"task done\");", note: "20 毫秒後被叫醒,印出這一行。" },
          { code: "        7", note: "task 的結果值,型別是 i32。" },
          { code: "    });", note: "async 區塊結束。handle 的型別是 JoinHandle<i32>。" },
          { code: "    println!(\"spawned\");", note: "main 沒有交出控制權,所以這一行先被印出來——單執行緒 runtime 此時還沒機會碰那個 task。" },
          { code: "    let n = handle.await.unwrap();", note: "JoinHandle 本身可以被 .await。main 在這裡交出控制權,runtime 才開始推那個 task;完成後拿到 Ok(7),unwrap 取出 7(task 若 panic 這裡會是 Err)。" },
          { code: "    println!(\"n = {}\", n);", note: "印出 n = 7。" },
          { code: "}", note: "main 結束,runtime 關閉。" },
        ],
        outro: "把 flavor 拿掉改用預設的多執行緒 runtime,輸出的三行順序不變,但 task 會在另一條工作執行緒上跑,spawned 與 task done 的時間間隔可能變得更短。",
      },
      csharp: `最接近的是 Task.Run:兩者都是「把一段工作交給排程器,拿一個把手回來等結果」。差別有兩個。第一,C# 的 Task.Run 一定是丟到執行緒池,Rust 的 tokio::spawn 可能連一條新執行緒都沒用到。第二,C# 的 Task 就算你不 await 也會被跑完,Rust 的 task 一旦 spawn 出去確實也會被排程(這是少數不 await 也會動的情況),但 main 一結束 runtime 就關閉,還沒跑完的 task 會直接被丟掉——跟 2-7 的 thread 一樣不會有人等它。`,
    },
    {
      id: "2-8-09",
      question: "已經寫了 async move,為什麼還是過不了?以下程式的結果是?",
      questionCode: "#[tokio::main]\nasync fn main() {\n    let data = String::from(\"config\");\n    let r = &data;\n    let handle = tokio::spawn(async move {\n        println!(\"{}\", r);\n    });\n    handle.await.unwrap();\n}",
      options: [
        { text: "編譯錯誤:`data` does not live long enough——async move 移進去的是參考 r 這個值本身,r 指向的 data 仍然只活到 main 結束;而 tokio::spawn 要求 task 必須是 'static,不能含任何短命的借用" },
        { text: "能跑,印出 config——已經寫了 async move,所有權都移進 async 區塊了,不會有生命週期問題" },
        { text: "能跑,印出 config——下一行就 handle.await 了,task 一定在 data 被釋放前結束,借用是安全的" },
        { text: "編譯錯誤:String 不能被送進 tokio::spawn 的 task,要先包成 Arc<Mutex<String>> 才行" },
      ],
      answer: 0,
      explanation: `move 只決定「捕獲時是搬走還是借走」,不會改變被搬走的東西是什麼。這裡 r 的型別是 &String,async move 忠實地把這個參考值搬進狀態機——搬進去的還是一個參考,它指向的 data 依然是 main 的區域變數,main 結束就被釋放。
tokio::spawn 的簽名要求 task 是 'static:spawn 出去的 task 交給 runtime 之後,runtime 可以在任何時刻、甚至在呼叫端的函式早就回傳之後才推動它,所以它裡面不能存在任何「比程式其餘生命週期短」的借用。編譯器因此指著 data 說 does not live long enough,並附註 argument requires that \`data\` is borrowed for \`'static\`。
「下一行就 await 了所以安全」是 2-7 第 3 題那個坑的 async 版本,而且這裡更不成立:handle 可以被丟掉、被傳到別的地方,task 也可能在 main 之後才輪到。spawn 只看簽名,不做「你實際上何時等它」的推理。「寫了 move 就沒事」則是這一題真正的教學點——2-7 加上 move 就解決了,這裡不行,因為被 move 的本來就是個參考。真正的修法是讓 task 擁有資料本身:直接把 String move 進去,或(main 之後還要用的話)改用 Arc 共享所有權。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "#[tokio::main]", note: "備好 runtime。" },
            { code: "async fn main() {", note: "進入點。data 與 r 都是 main 的區域變數。" },
            { code: "    let data = String::from(\"config\");", note: "data 的所有權在 main 手上,main 結束時釋放。" },
            { code: "    let r = &data;", note: "r 是一個 &String,它的存活期被綁在 data 上。" },
            { code: "    let handle = tokio::spawn(async move {", note: "⛔ 編譯失敗:`data` does not live long enough(E0597),附註 argument requires that `data` is borrowed for `'static`。spawn 要求 task 不含任何短命的借用,而這個 async move 區塊搬進去的 r 正是一個借用。" },
            { code: "        println!(\"{}\", r);", note: "本體用到 r,所以 r 被捕獲——move 搬走的是 r 這個參考值,不是 data 的所有權。" },
            { code: "    });", note: "async 區塊結束。" },
            { code: "    handle.await.unwrap();", note: "即使這一行保證了 task 會在 main 結束前跑完,編譯器也不採信:spawn 的簽名沒有辦法表達這個保證。" },
            { code: "}", note: "data 在這裡才被釋放。" },
          ],
          outro: "把 let r = &data; 刪掉、本體直接用 data,這段就會通過——因為那時 async move 搬走的是 String 本身的所有權。下面的正確寫法示範的是「main 之後還要繼續用 data」的情況。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::sync::Arc;", note: "改動處:引入 Arc,讓資料的所有權可以被多方共享(2-7 學過的執行緒安全版參考計數)。" },
            { code: "", note: "" },
            { code: "#[tokio::main]", note: "備好 runtime。" },
            { code: "async fn main() {", note: "進入點。" },
            { code: "    let data = Arc::new(String::from(\"config\"));", note: "改動處:把 String 放進 Arc。現在 data 的型別是 Arc<String>,底下的字串由計數決定何時釋放。" },
            { code: "    let r = Arc::clone(&data);", note: "改動處:不再是借用,而是把計數加一、拿到一份獨立持有的 Arc<String>。r 本身沒有綁定任何區域變數的存活期。" },
            { code: "    let handle = tokio::spawn(async move {", note: "async move 把 r 的所有權搬進狀態機。r 是 Arc<String>,自己就是 'static,通過 spawn 的檢查。" },
            { code: "        println!(\"{}\", r);", note: "task 被推動時印出 config;結束後 r 被 drop,計數減一。" },
            { code: "    });", note: "async 區塊結束。" },
            { code: "    handle.await.unwrap();", note: "等 task 完成。" },
            { code: "    println!(\"main still has {}\", data);", note: "main 手上那份 Arc 還在,字串也還在,印出 main still has config。" },
            { code: "}", note: "最後一份 Arc 被 drop,String 才真的被釋放。" },
          ],
        },
      ],
      csharp: `C# 的 Task.Run(() => Console.WriteLine(r)) 永遠不會有這個問題:lambda 捕獲的變數會被提升到堆積上的物件,只要 lambda 還活著,GC 就保證那個字串不消失,你完全不必想「data 什麼時候被釋放」。Rust 沒有 GC,所以這個保證必須由型別系統事先證明,而 spawn 能接受的證明只有一種——'static,也就是「裡面完全沒有借用」。實務上的對應寫法就是 Arc:把「誰負責釋放」這件事交給計數,而不是交給作用域。`,
    },
    {
      id: "2-8-10",
      question: "async move 是在哪一刻把 name 的所有權拿走的?以下程式印出什麼?",
      questionCode: "#[tokio::main]\nasync fn main() {\n    let name = String::from(\"alice\");\n    let fut = async move {\n        println!(\"hello {}\", name);\n    };\n    println!(\"future created\");\n    fut.await;\n    println!(\"done\");\n}",
      options: [
        { text: "future created、hello alice、done——name 的所有權在「建立 async 區塊」那一行就被移進狀態機了(從那行之後 main 再用 name 就編譯失敗),但本體要等 fut.await 才被驅動" },
        { text: "future created、hello alice、done——name 是在 fut.await 執行的那一刻才被移走的;所有權跟著「實際執行」走,await 之前 main 還可以照常使用 name" },
        { text: "hello alice、future created、done——async move 的 move 就是為了讓區塊可以立刻執行,所以本體會先跑" },
        { text: "編譯錯誤:async move 區塊必須交給 tokio::spawn 或 join!,不能先存成區域變數再 .await" },
      ],
      answer: 0,
      explanation: `這一題要把兩件事分開:所有權什麼時候搬走,以及本體什麼時候執行。
所有權在「建立」那一刻就搬走了。async move { ... } 這個運算式會生成一台狀態機,被捕獲的變數就是它的欄位;既然要當欄位存起來,值當然得在建立時被 move 進去。所以從 let fut = async move {...}; 這一行之後,main 就不能再用 name 了(試著在下一行 println!("{}", name) 會得到 borrow of moved value)。
本體則要等 fut.await 才跑,所以 future created 先印、hello alice 後印。這正是 async move 相對於一般 move 閉包的新意:move 閉包捕獲之後,你至少還能決定「什麼時候呼叫它」;async 狀態機更進一步,它可以被存起來、被傳給 spawn、被塞進集合,很久以後才被某個 runtime 在某條執行緒上推動——所有權必須先被搬進去,才可能撐到那個時候。第 9 題的 'static 要求就是這件事的必然結果。
「所有權跟著實際執行走」是把 move 想成執行期行為;其實它是編譯期的捕獲決策,跟這台狀態機之後有沒有被推動完全無關——就算你從頭到尾不 await fut,name 也已經被搬走了。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "#[tokio::main]", note: "備好 runtime。" },
          { code: "async fn main() {", note: "進入點。" },
          { code: "    let name = String::from(\"alice\");", note: "name 目前由 main 持有。" },
          { code: "    let fut = async move {", note: "建立一台狀態機。move 讓它把 name 的所有權搬進自己的欄位——這件事在這一行就完成了,與之後有沒有 await 無關。" },
          { code: "        println!(\"hello {}\", name);", note: "本體。它讀的是狀態機自己欄位裡的那個 String,不是 main 的變數。" },
          { code: "    };", note: "async 區塊結束。fut 現在是一台「已經含有 alice、但一行都還沒跑」的狀態機。從這裡開始 main 不能再使用 name。" },
          { code: "    println!(\"future created\");", note: "第一個被印出來的,因為本體還沒被驅動。" },
          { code: "    fut.await;", note: "把狀態機交給 runtime 推到完成,此時才印出 hello alice。" },
          { code: "    println!(\"done\");", note: "最後印出 done。" },
          { code: "}", note: "fut 已被消耗,它欄位裡的 String 在本體結束時就被釋放了。" },
        ],
        outro: "想驗證所有權真的在建立時就搬走了,把 println!(\"future created\"); 改成 println!(\"{}\", name); 再編譯一次——錯誤訊息會指著 async move 那一行說 value moved here。",
      },
      csharp: `C# 沒有對應語法,因為它不需要:async 方法捕獲的區域變數會被編譯器搬進一個狀態機類別(跟 Rust 其實很像),但那個類別在堆積上,由 GC 保證存活,所以「誰擁有這份資料」從來不是問題,也就沒有 move 這個關鍵字存在的理由。Rust 把這個決定攤在你眼前:不寫 move 就是借用(可能活不夠久,第 9 題),寫了 move 就是搬走(原變數從此不能用)。C# 開發者最容易漏掉的是後半句——搬走是立刻生效的,不是等到工作真的跑起來才生效。`,
    },
    {
      id: "2-8-11",
      question: "async fn load 的簽名寫著 -> i32,為什麼下面這行過不了?",
      questionCode: "async fn load() -> i32 {\n    7\n}\n\nfn show(v: i32) {\n    println!(\"v = {}\", v);\n}\n\n#[tokio::main]\nasync fn main() {\n    show(load());\n}",
      options: [
        { text: "編譯錯誤:mismatched types — expected `i32`, found future——async fn 簽名上的 -> i32 是「最後會產出 i32」,函式真正回傳的是一台待驅動的狀態機" },
        { text: "能跑,印出 v = 7——簽名寫 -> i32,回傳的當然就是 i32,show 直接收得下" },
        { text: "能跑,印出 v = 7——show 的參數型別是 i32,編譯器會自動替 load() 補上 .await 做轉換" },
        { text: "編譯錯誤:show 必須改成 async fn,普通函式不能接收 async fn 的回傳值" },
      ],
      answer: 0,
      explanation: `async fn 的函式簽名本身就是一層語法糖。你寫 async fn load() -> i32,編譯器實際生成的是 fn load() -> impl Future<Output = i32>——箭頭後面那個型別被搬進了 Output 這個關聯型別裡,而函式真正回傳的是一台狀態機。所以把 load() 直接塞給吃 i32 的 show,型別當然對不上:expected \`i32\`, found future,附註 calling an async function returns a future,並建議 consider \`await\`ing on the \`Future\`——即使你的程式碼裡從來沒寫過 Future 這個字。
修法是 show(load().await):先把狀態機推到完成、取出 Output,再交給 show。
「編譯器會自動補 .await」是最需要打掉的期待:Rust 不做隱式的等待,這是刻意的——每個暫停點都必須在原始碼裡看得見,否則你沒辦法判斷一段程式會在哪裡讓出控制權。「show 要改成 async」也搞錯了方向:show 完全不需要動,它拿到的 i32 是不是從 async 來的根本無所謂。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "async fn load() -> i32 {", note: "簽名看起來回傳 i32,但這是糖衣;真正的回傳型別見下面的去糖區塊。" },
            { code: "    7", note: "本體的結果值。" },
            { code: "}", note: "load 定義結束。" },
            { code: "", note: "" },
            { code: "fn show(v: i32) {", note: "一個普通函式,只收 i32。它本身沒有任何問題。" },
            { code: "    println!(\"v = {}\", v);", note: "印出參數。" },
            { code: "}", note: "show 定義結束。" },
            { code: "", note: "" },
            { code: "#[tokio::main]", note: "備好 runtime。" },
            { code: "async fn main() {", note: "進入點。" },
            { code: "    show(load());", note: "⛔ 編譯失敗:mismatched types — expected `i32`, found future(E0308)。附註是 calling an async function returns a future,並建議 consider `await`ing on the `Future`。load() 給的是狀態機,不是數字。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "async fn load() -> i32 {", note: "load 不用改。" },
            { code: "    7", note: "本體不變。" },
            { code: "}", note: "定義結束。" },
            { code: "", note: "" },
            { code: "fn show(v: i32) {", note: "show 也不用改。" },
            { code: "    println!(\"v = {}\", v);", note: "印出參數。" },
            { code: "}", note: "定義結束。" },
            { code: "", note: "" },
            { code: "#[tokio::main]", note: "備好 runtime。" },
            { code: "async fn main() {", note: "進入點。" },
            { code: "    show(load().await);", note: "改動處:補上 .await,把狀態機推到完成、取出 Output(也就是 7),再把這個 i32 交給 show。印出 v = 7。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "△ Desugared (what the compiler sees)",
          intro: "錯誤訊息叫你 consider `await`ing on the `Future`,但你的程式碼裡根本沒有 Future 這個字。它藏在 async fn 這幾個字母底下——函式簽名本身就是一層糖:",
          lines: [
            { code: "async fn load() -> i32 {", note: "你寫的簽名。" },
            { code: "    7", note: "你寫的本體。" },
            { code: "}", note: "定義結束。" },
            { code: "", note: "" },
            { code: "// 編譯器把它展開成:", note: "以下才是編譯器實際檢查的東西。" },
            { code: "fn load() -> impl Future<Output = i32> {", note: "注意 async 不見了,回傳型別也不是 i32:它是「某個會產出 i32 的狀態機型別」。錯誤訊息裡的 impl Future<Output = i32> 就是從這裡來的。" },
            { code: "    async move { 7 }", note: "原本的本體被包進一個 async 區塊,編譯成一台匿名狀態機;呼叫 load() 只是把這台狀態機建出來回傳而已。" },
            { code: "}", note: "展開結束。" },
            { code: "", note: "" },
            { code: "// Output 是 Future 這個 trait 的關聯型別:", note: "它描述的是「跑完之後會拿到什麼」。" },
            { code: "// 只有把狀態機推到完成,才取得出那個 i32", note: ".await 做的正是這件事——所以 -> i32 從來沒有騙你,只是它講的是終點,不是回傳的那一刻。" },
          ],
        },
      ],
      csharp: `C# 的對應簽名是 async Task<int> LoadAsync(),那個 Task<int> 是寫在原始碼上、看得見的——所以 C# 開發者很清楚「方法回傳的是 Task,不是 int」,把它直接傳給吃 int 的方法時,編譯器的抱怨也一看就懂。Rust 把 Task<int> 這一層藏進了 async 關鍵字,簽名上只剩 -> i32,讀起來比較順,代價就是這個錯誤訊息會冒出一個你沒寫過的 impl Future<Output = i32>。把它讀成「C# 的 Task<int>」,整句話就通了。`,
    },
    {
      id: "2-8-12",
      question: "下面這行想把非同步查來的價格加上 10,結果是?",
      questionCode: "async fn price() -> i32 {\n    100\n}\n\n#[tokio::main]\nasync fn main() {\n    let total = price() + 10;\n    println!(\"total = {}\", total);\n}",
      options: [
        { text: "編譯錯誤:cannot add `{integer}` to `impl Future<Output = i32>`——price() 交回來的是一台待驅動的狀態機,不是數字,要先 .await 才拿得到那個 i32" },
        { text: "能跑,印出 total = 110——簽名是 -> i32,所以 price() 的結果就是 100,直接加就好" },
        { text: "能跑,印出 total = 110——編譯器看到算術運算,會自動把那台狀態機先推到完成再相加" },
        { text: "編譯錯誤:async fn 不能回傳 i32 這種 Copy 型別,要改成 Result<i32, ...> 之類的包裝型別才合法" },
      ],
      answer: 0,
      explanation: `承上題:price() 的靜態型別是 impl Future<Output = i32>,而加法要求兩邊是數字,所以編譯器直接說 cannot add \`{integer}\` to \`impl Future<Output = i32>\`(E0369),並在底下標出左邊那個運算元的型別。修法只是補一個 .await:let total = price().await + 10;。
這題看起來跟上一題很像,但誤解的來源不同。上一題是「傳參數時型別對不上」,這一題是運算子——很多人會下意識覺得「運算子應該會幫我處理」,就像 C# 裡 someInt + 10 不需要多想。Rust 不做任何隱式的等待或轉換,運算子看到的就是那個實際型別。
「編譯器會自動推到完成再相加」這個選項值得單獨點名:它其實是在期待一個隱式的暫停點。Rust 刻意不提供,因為暫停點決定了程式在哪裡可能讓出控制權、在哪裡可能被取消(第 7 題),這種事不能藏起來。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "async fn price() -> i32 {", note: "簽名看起來回傳 i32,實際回傳的是狀態機(見去糖區塊)。" },
            { code: "    100", note: "本體的結果值。" },
            { code: "}", note: "price 定義結束。" },
            { code: "", note: "" },
            { code: "#[tokio::main]", note: "備好 runtime。" },
            { code: "async fn main() {", note: "進入點。" },
            { code: "    let total = price() + 10;", note: "⛔ 編譯失敗:cannot add `{integer}` to `impl Future<Output = i32>`(E0369)。編譯器會在這一行底下標出左邊運算元的型別就是 impl Future<Output = i32>——一台還沒被推動的狀態機,不是數字。" },
            { code: "    println!(\"total = {}\", total);", note: "這一行本身沒問題,但整支程式已經編譯不過。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "async fn price() -> i32 {", note: "price 不用改。" },
            { code: "    100", note: "本體不變。" },
            { code: "}", note: "定義結束。" },
            { code: "", note: "" },
            { code: "#[tokio::main]", note: "備好 runtime。" },
            { code: "async fn main() {", note: "進入點。" },
            { code: "    let total = price().await + 10;", note: "改動處:先 .await 把狀態機推到完成、取出 100,再做加法。.await 的優先序高於 +,所以不需要額外括號。total 是 110。" },
            { code: "    println!(\"total = {}\", total);", note: "印出 total = 110。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "△ Desugared (what the compiler sees)",
          intro: "錯誤訊息裡的 impl Future 是 price() 的真實型別(見第 11 題的展開);而 .await 這個看起來只是個後綴的東西,底下其實是一個輪詢迴圈:",
          lines: [
            { code: "let total = price().await + 10;", note: "你寫的那一行(修正後)。" },
            { code: "", note: "" },
            { code: "// .await 大致展開成這樣(示意,省略 Pin 與 Context 的細節):", note: "以下是編譯器替你生成的東西。" },
            { code: "let mut fut = price();", note: "呼叫只是把狀態機建出來,本體一行都還沒跑——沒有 .await 的話,故事就停在這裡。" },
            { code: "let value = loop {", note: "接下來是反覆「推它一把」的迴圈。" },
            { code: "    match Future::poll(&mut fut, cx) {", note: "poll 就是推一把:問這台狀態機「好了沒」。cx 是 runtime 傳進來的 Context,裡面帶著喚醒用的 Waker。" },
            { code: "        Poll::Ready(v) => break v,", note: "已完成:把 Output 交出來,v 才是那個 i32。" },
            { code: "        Poll::Pending => yield,", note: "還沒好:把控制權交還給 runtime(第 3 題看到的就是這一步),被叫醒後從這裡再來一次。" },
            { code: "    }", note: "match 結束。" },
            { code: "};", note: "迴圈結束,value 的型別是 i32。" },
            { code: "let total = value + 10;", note: "到這一步加法才成立。少寫 .await 就等於整個迴圈沒發生,手上拿的仍然是那台狀態機——這就是錯誤訊息的由來。" },
          ],
        },
      ],
      csharp: `C# 裡對應的寫法 var total = PriceAsync() + 10; 一樣會編譯失敗,訊息是「運算子 + 無法套用至 Task<int> 和 int」。兩邊的錯誤本質相同,差別只在可讀性:C# 訊息裡的 Task<int> 是你自己寫在簽名上的,Rust 訊息裡的 impl Future<Output = i32> 從頭到尾沒在你的程式碼裡出現過。另外 C# 開發者要注意優先序:C# 常見的寫法是 (await PriceAsync()) + 10 或 await 在最前面,Rust 的 .await 是後綴,price().await + 10 讀起來更接近方法鏈。`,
    },
    {
      id: "2-8-13",
      question: "同一個 Future 變數被 .await 兩次,會發生什麼?",
      questionCode: "async fn ping() -> u32 {\n    1\n}\n\n#[tokio::main]\nasync fn main() {\n    let fut = ping();\n    let a = fut.await;\n    let b = fut.await;\n    println!(\"{} {}\", a, b);\n}",
      options: [
        { text: "編譯錯誤:use of moved value: `fut`——.await 會按值取用左邊那個值,第一次 await 就把 fut 消耗掉了,第二次沒有東西可以等" },
        { text: "印出 1 1——.await 只是「讀取結果」,同一個值當然可以重複讀" },
        { text: "印出 1 1——第二次 .await 發現工作已經完成,直接把快取起來的結果再回傳一次" },
        { text: "能編譯,但執行時 panic:cannot await a completed future" },
      ],
      answer: 0,
      explanation: `.await 不是讀取,是消耗。它要把那台狀態機一路推到完成,過程中會修改狀態機內部的狀態(停在第幾個暫停點、局部變數目前的值),所以它必須取得那個值的所有權。fut 的型別沒有實作 Copy,第一次 .await 就把它 move 走了;第二行再寫 fut.await,編譯器報 use of moved value: \`fut\`(E0382),並在第一次那行標註 \`fut\` moved due to this await。
要拿兩次結果就呼叫兩次:let a = ping().await; let b = ping().await;——每次呼叫都建立一台全新的狀態機。若結果本身很貴、想重複使用,就把結果存起來重複用那個值,而不是重複 await 同一台狀態機。
「第二次會回傳快取的結果」正是 C# 的行為,也是這題最容易選錯的地方:C# 的 Task 完成後會把結果(或例外)留在物件裡,await 幾次都拿得到同一份。Rust 的 Future 沒有這個角色——它是「一次性的工作」,不是「結果的容器」。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "async fn ping() -> u32 {", note: "一個最簡單的 async fn。" },
            { code: "    1", note: "本體的結果值。" },
            { code: "}", note: "ping 定義結束。" },
            { code: "", note: "" },
            { code: "#[tokio::main]", note: "備好 runtime。" },
            { code: "async fn main() {", note: "進入點。" },
            { code: "    let fut = ping();", note: "建立一台狀態機並綁給 fut。fut 的型別沒有實作 Copy,所以它遵守一般的 move 規則。" },
            { code: "    let a = fut.await;", note: "把 fut 推到完成,a 拿到 1。注意這一行同時把 fut 的所有權消耗掉了——.await 是按值取用。" },
            { code: "    let b = fut.await;", note: "⛔ 編譯失敗:use of moved value: `fut`(E0382)。上一行已經把 fut 移走,這裡手上沒有值可以等。" },
            { code: "    println!(\"{} {}\", a, b);", note: "這行沒問題,但整支程式編譯不過。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "async fn ping() -> u32 {", note: "ping 不用改。" },
            { code: "    1", note: "本體不變。" },
            { code: "}", note: "定義結束。" },
            { code: "", note: "" },
            { code: "#[tokio::main]", note: "備好 runtime。" },
            { code: "async fn main() {", note: "進入點。" },
            { code: "    let a = ping().await;", note: "改動處:每次要一個結果就呼叫一次,這裡建立並消耗第一台狀態機,a 拿到 1。" },
            { code: "    let b = ping().await;", note: "改動處:再呼叫一次,建立第二台全新的狀態機,b 也拿到 1。" },
            { code: "    println!(\"{} {}\", a, b);", note: "印出 1 1。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "如果那個工作很貴、只想做一次但結果要用很多遍,做法是把 .await 的結果存進變數重複使用——重複使用結果,而不是重複使用工作本身。",
        },
      ],
      csharp: `這是兩邊行為差最多的一題。C# 的 Task 完成之後就變成一個裝著結果的物件,var a = await t; var b = await t; 完全合法,第二次立刻回傳同一份結果(例外也是同一個例外再拋一次),所以 C# 開發者會習慣把 Task 當成「可以重複讀的值」傳來傳去。Rust 的 Future 是一次性的工作計畫:推完就沒了,語言用所有權把這件事強制表達出來。換句話說,C# 的 Task 同時扮演「工作」和「結果容器」兩個角色,Rust 只讓 Future 扮演前者。`,
    },
    {
      id: "2-8-14",
      question: "async fn 的參數是一個參考時,產生出來的 Future 跟那個參考是什麼關係?以下程式印出什麼?",
      questionCode: "async fn first_word(s: &str) -> &str {\n    s.split(' ').next().unwrap()\n}\n\n#[tokio::main]\nasync fn main() {\n    let text = String::from(\"hello async world\");\n    let fut = first_word(&text);\n    println!(\"future created\");\n    println!(\"{}\", fut.await);\n}",
      options: [
        { text: "future created、hello——first_word(&text) 產生的狀態機把 &text 存進自己的欄位,所以它的生命週期被綁在 text 上:在 fut.await 之前若把 text 釋放掉,編譯就會失敗" },
        { text: "future created、hello——狀態機在建立時就把 &text 指到的字串內容複製了一份進去,之後 text 被釋放也不影響" },
        { text: "編譯錯誤:async fn 的回傳型別不能跟參數共用生命週期,必須自己標上 <'a> 才合法" },
        { text: "future created、hello——而且 fut 可以先被搬到別處(例如交給 tokio::spawn)之後再 await,反正 text 還在 main 的作用域裡活著" },
      ],
      answer: 0,
      explanation: `async fn 的參數不會在呼叫時被用掉,而是被存進狀態機的欄位裡等著本體之後去讀。參數是 &str 的話,存進去的就是那個參考——狀態機因此天生帶著那個參考的生命週期,去糖後的簽名其實是 fn first_word<'a>(s: &'a str) -> impl Future<Output = &'a str> + 'a。
實際輸出是確定的:建立狀態機不執行本體,所以 future created 先印;fut.await 才推動本體,取出第一個字 hello。
「內容會被複製一份進去」是把 async 想成「先把資料抓進來再慢慢跑」——並沒有,狀態機存的就是你傳進去的那個參考本身,這也是為什麼 text 必須活到 await 結束(在 await 之前插一行 drop(text),會得到 cannot move out of \`text\` because it is borrowed,並指著 fut.await 說 borrow later used here)。
「可以先搬給 tokio::spawn 再 await」則是第 9 題那個陷阱的變形:spawn 要求 'static,而這台狀態機帶著 &text 的生命週期,過不了那一關。在 join!、select! 或直接 .await 這些「留在同一個 task 裡」的用法下它完全沒問題——這正是第 18 題要談的差別。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "async fn first_word(s: &str) -> &str {", note: "參數與回傳都是參考。依 1-13 的省略規則,回傳的 &str 借用的就是 s;這條規則在 async fn 上一樣成立,而且會一路傳到狀態機身上。" },
          { code: "    s.split(' ').next().unwrap()", note: "本體:切出第一個字。回傳的 &str 指向 s 所借用的那塊字串資料,沒有任何複製。" },
          { code: "}", note: "first_word 定義結束。" },
          { code: "", note: "" },
          { code: "#[tokio::main]", note: "備好 runtime。" },
          { code: "async fn main() {", note: "進入點。" },
          { code: "    let text = String::from(\"hello async world\");", note: "資料的所有權在 main 手上。" },
          { code: "    let fut = first_word(&text);", note: "建立狀態機,並把 &text 這個參考存進它的欄位。從這一刻起 text 處於被借用狀態,借用要持續到 fut 被用完為止。" },
          { code: "    println!(\"future created\");", note: "本體還沒被推動,所以這行先印出來。" },
          { code: "    println!(\"{}\", fut.await);", note: "推動狀態機,本體執行並回傳 \"hello\";印出 hello。await 結束後借用才解除。" },
          { code: "}", note: "text 在這裡被釋放。" },
        ],
        outro: "試著在 println!(\"future created\"); 之後加一行 drop(text); 再編譯:錯誤是 cannot move out of `text` because it is borrowed(E0505),並指著最後那行 fut.await 說 borrow later used here。這就是「狀態機綁住了參數的生命週期」最直接的證據。",
      },
      csharp: `C# 的 Task<string> 不會有這種綁定:async 方法捕獲的字串是堆積上的物件參考,只要 Task 還活著,GC 就不讓它被回收,所以你可以安心地把 Task 存進欄位、丟進集合、跨方法傳遞,完全不必想來源物件還在不在。Rust 沒有 GC,這個「還在不在」必須由生命週期在編譯期證明,於是 Future 就繼承了參數的生命週期。實務上的結論很簡單:帶參考的 Future 只適合在同一段 .await 鏈裡用完(join!、select!);要存起來或 spawn 出去,就得讓它擁有資料(第 9 題的 Arc 或直接 move)。`,
    },
    {
      id: "2-8-15",
      question: "一個 &self 的 async 方法,產生的 Future 被存到外層變數。以下程式的結果是?",
      questionCode: "struct Cache {\n    items: Vec<String>,\n}\n\nimpl Cache {\n    async fn first(&self) -> &str {\n        &self.items[0]\n    }\n}\n\n#[tokio::main]\nasync fn main() {\n    let fut;\n    {\n        let cache = Cache { items: vec![String::from(\"alpha\")] };\n        fut = cache.first();\n    }\n    println!(\"{}\", fut.await);\n}",
      options: [
        { text: "編譯錯誤:`cache` does not live long enough——cache.first() 產生的狀態機借用了 cache,而 fut 宣告在外層、活得比 cache 久,借用會在內層區塊結束時懸空" },
        { text: "能跑,印出 alpha——fut 在建立時就已經把要回傳的字串算好收進去了,cache 之後被釋放也沒關係" },
        { text: "能跑,印出 alpha——非同步方法回傳的東西跟一般方法回傳值一樣是獨立的,不會綁住 self" },
        { text: "編譯錯誤:async fn 不能定義在 impl 區塊裡,方法要自己手寫成 fn first(&self) -> impl Future<Output = &str> 才合法" },
      ],
      answer: 0,
      explanation: `承上題,只是把參數換成 &self:cache.first() 產生的狀態機把 &cache 存進欄位,所以它的生命週期綁在 cache 上。這段程式故意讓 fut 宣告在外層、cache 活在內層區塊裡——內層區塊結束時 cache 被釋放,而 fut 還要活到下一行才被 await,借用就懸空了。編譯器報 \`cache\` does not live long enough(E0597),並指出 borrow later used here。
修法是讓 cache 至少活到 fut 被用完:把它移出內層區塊,和 fut 放在同一層(下面的正確寫法),或者反過來把 .await 移進內層區塊裡。
「非同步方法回傳的東西是獨立的值」是這題的核心誤解。一般方法 fn first(&self) -> &str 回傳的 &str 也綁在 self 上,這點 1-13 已經教過;async 版本只是把這個綁定往前挪了一步——連「還沒開始跑的那台狀態機」都已經帶著借用了。「已經把結果算好收進去」同樣站不住腳:本體在 await 之前一行都沒跑過(第 1 題)。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "struct Cache {", note: "一個持有資料的型別。" },
            { code: "    items: Vec<String>,", note: "資料本身由 Cache 擁有。" },
            { code: "}", note: "struct 定義結束。" },
            { code: "", note: "" },
            { code: "impl Cache {", note: "方法區塊。" },
            { code: "    async fn first(&self) -> &str {", note: "非同步方法,借用 self、回傳借自 self 的 &str。呼叫它會產生一台帶著 &self 的狀態機。" },
            { code: "        &self.items[0]", note: "本體:回傳第一個元素的參考。它指向 self 裡面的資料。" },
            { code: "    }", note: "方法結束。" },
            { code: "}", note: "impl 區塊結束。" },
            { code: "", note: "" },
            { code: "#[tokio::main]", note: "備好 runtime。" },
            { code: "async fn main() {", note: "進入點。" },
            { code: "    let fut;", note: "先在外層宣告,打算讓它活過下面那個內層區塊。" },
            { code: "    {", note: "內層區塊開始。" },
            { code: "        let cache = Cache { items: vec![String::from(\"alpha\")] };", note: "⛔ 編譯失敗:`cache` does not live long enough(E0597)。cache 只活到這個內層區塊結束。" },
            { code: "        fut = cache.first();", note: "建立狀態機,把 &cache 存進它的欄位;借用必須活到 fut 被用完為止。" },
            { code: "    }", note: "內層區塊結束,cache 在這裡被釋放——但 fut 還握著指向它的參考,編譯器在這裡擋下來。" },
            { code: "    println!(\"{}\", fut.await);", note: "編譯器指這一行說 borrow later used here:借用真正被用到的地方在這裡,已經太晚了。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "struct Cache {", note: "型別定義不用改。" },
            { code: "    items: Vec<String>,", note: "資料不變。" },
            { code: "}", note: "struct 定義結束。" },
            { code: "", note: "" },
            { code: "impl Cache {", note: "方法區塊。" },
            { code: "    async fn first(&self) -> &str {", note: "方法本身也不用改——問題從來不在方法,而在呼叫端讓誰活得比誰久。" },
            { code: "        &self.items[0]", note: "本體不變。" },
            { code: "    }", note: "方法結束。" },
            { code: "}", note: "impl 區塊結束。" },
            { code: "", note: "" },
            { code: "#[tokio::main]", note: "備好 runtime。" },
            { code: "async fn main() {", note: "進入點。" },
            { code: "    let cache = Cache { items: vec![String::from(\"alpha\")] };", note: "改動處:把 cache 移到跟 fut 同一層,它會活到 main 結束。" },
            { code: "    let fut = cache.first();", note: "改動處:狀態機借用 cache,而 cache 的存活期涵蓋了下一行,借用成立。" },
            { code: "    println!(\"{}\", fut.await);", note: "推動狀態機,印出 alpha。借用在這一行之後才解除。" },
            { code: "}", note: "cache 在這裡被釋放,順序正確。" },
          ],
          outro: "要讓 Future 真的脫離 self 獨立存在(存起來、spawn 出去),就得讓它擁有資料——例如方法改成 async fn first_owned(&self) -> String 回傳 clone 過的值,或把 Cache 放進 Arc。",
        },
      ],
      csharp: `C# 完全沒有這個限制:cache.FirstAsync() 回傳的 Task<string> 是獨立物件,它內部持有的 Cache 參考會讓 GC 把 cache 留著,你想把 Task 存多久都行。Rust 用生命週期把同一件事變成編譯期的義務,而且挑的是最嚴格的時機——不是「await 的時候 cache 還在嗎」,而是「這個借用的作用域有沒有完整涵蓋 fut 的存活期」。好處是你永遠不會拿到指向已釋放物件的結果,代價是 async 方法的回傳值不能像 C# 的 Task 那樣隨手到處傳。`,
    },
    {
      id: "2-8-16",
      question: "一個服務要同時處理上萬條網路連線,另外還有一份大圖檔要做壓縮運算。以下哪一種安排是慣用做法?",
      options: [
        { text: "上萬條連線用 async task(tokio::spawn),圖檔壓縮丟給執行緒(thread 或 tokio::task::spawn_blocking)——async task 很輕、等 I/O 時不佔執行緒,但 CPU 密集運算中間沒有 .await 可以讓出控制權,留在 runtime 裡會把執行緒卡死" },
        { text: "兩邊都用 async task——async 是比較新的模型,各方面都比 thread 快,thread 只是還沒被淘汰而已" },
        { text: "兩邊都用執行緒——只有執行緒才是真正的平行,async 不過是在單一執行緒上假裝並行,撐不起上萬條連線" },
        { text: "上萬條連線用執行緒、圖檔壓縮用 async task——連線是長期存在的工作,本來就該一條一條配執行緒;壓縮是一次性的,交給 runtime 排程比較省事" },
      ],
      answer: 0,
      explanation: `兩個模型解的是不同的問題。async task 解的是「等待」:一個 task 停在 .await 上時幾乎不佔資源(成本是一小塊狀態機記憶體),所以一條執行緒可以照顧上萬條大部分時間都在等封包的連線。執行緒解的是「同時算」:每條執行緒有自己的堆疊、由作業系統排程,能真正同時使用多顆核心,而且會被作業系統強制輪替。
問題在於 tokio 的排程是協作式的:runtime 只能在 task 主動 .await 時換人。一段純 CPU 運算中間沒有任何 .await,它一旦開始就會霸佔那條執行緒直到算完,期間所有排在同一條執行緒上的 task 全部停擺(第 17 題會實測這件事)。所以 CPU 密集的工作要丟到 runtime 之外:std 的 thread,或 tokio 專門為此提供的 spawn_blocking(它有獨立的執行緒池)。
「兩邊都用執行緒」低估了 async:上萬條執行緒光是堆疊就要好幾 GB,作業系統的 context switch 也會吃掉大量時間。「連線配執行緒、壓縮走 async」則是把兩者的適用面完全顛倒——它剛好是 async 出現之前那套做法會踩到的天花板。`,
      csharp: `C# 的取捨原則一模一樣,只是預設值比較寬容:I/O 用 await,CPU 密集用 Task.Run 丟到執行緒池,長期阻塞的工作用 TaskCreationOptions.LongRunning 或自己開 Thread。差別在後果的嚴重程度——C# 的執行緒池會偵測飢餓並慢慢增開執行緒,所以在 async 方法裡做同步阻塞運算通常只是「變慢、延遲抖動」;tokio 的工作執行緒數量預設固定(等於 CPU 核心數),被卡住就是真的卡住。Rust 這邊 spawn_blocking 對應的就是 C# 那個「另外開一池」的動作,只是必須由你明講。`,
    },
    {
      id: "2-8-17",
      question: "下面這支程式跑在單執行緒 runtime 上,一個 task 做了 500 毫秒的同步阻塞工作。印出什麼?",
      questionCode: "use tokio::time::{sleep, Duration, Instant};\n\n#[tokio::main(flavor = \"current_thread\")]\nasync fn main() {\n    let start = Instant::now();\n    let a = tokio::spawn(async {\n        std::thread::sleep(std::time::Duration::from_millis(500));\n        println!(\"report done\");\n    });\n    let b = tokio::spawn(async move {\n        sleep(Duration::from_millis(10)).await;\n        println!(\"timer late: {}\", start.elapsed() >= Duration::from_millis(500));\n    });\n    let _ = tokio::join!(a, b);\n}",
      options: [
        { text: "report done、timer late: true——std::thread::sleep 把整條執行緒停住,單執行緒 runtime 在這 500 毫秒裡沒辦法推動任何其他 task,連只等 10 毫秒的計時器 task 都被拖到 500 毫秒之後才跑" },
        { text: "timer late: false、report done——runtime 會像作業系統排程器那樣定時打斷長時間執行的 task,計時器照樣準時被叫醒" },
        { text: "timer late: false、report done——tokio::spawn 會把每個 task 放到各自的執行緒上,彼此不互相影響" },
        { text: "編譯錯誤:async 區塊裡不能呼叫 std::thread::sleep 這類同步阻塞函式,編譯器會擋下來" },
      ],
      answer: 0,
      explanation: `tokio 的排程是協作式的:runtime 只有在 task 主動停在 .await 上時才有機會換人。std::thread::sleep 不是 .await,它是貨真價實地把作業系統執行緒停住 500 毫秒——單執行緒 runtime 就只有那一條執行緒,於是整個 runtime 在這段時間內完全停擺,連計時器到期都沒人處理。等第一個 task 結束,第二個 task 才被推動,它一看時間已經過了 500 毫秒,印出 timer late: true。
這是完全編得過、跑得完的程式,沒有任何警告——所以它比編譯錯誤危險得多:在測試環境只跑一兩個請求時看不出來,上線後才發現延遲全面惡化。用多執行緒 runtime 也只是把問題稀釋:被卡住的是其中一條工作執行緒,核心數用完照樣癱瘓。
「runtime 會定時打斷長時間運算」把協作式排程誤認成作業系統的搶佔式排程,這是最需要拆掉的假設。「每個 task 各自一條執行緒」則是第 8 題已經澄清過的誤解。正確做法是把阻塞工作交給 tokio::task::spawn_blocking(它有一池專門吸收阻塞呼叫的執行緒),或改用 async 版本的 API(這裡就是 tokio::time::sleep)。`,
      walkthrough: [
        {
          label: "X Question code (compiles, but starves the runtime)",
          intro: "這段程式編得過、也不會 panic,錯在行為:一個 task 吃掉了所有人的執行時間。",
          lines: [
            { code: "use tokio::time::{sleep, Duration, Instant};", note: "注意這裡引入的是 tokio 的 sleep,下面卻刻意用了 std 的版本。" },
            { code: "", note: "" },
            { code: "#[tokio::main(flavor = \"current_thread\")]", note: "單執行緒 runtime:所有 task 共用同一條執行緒,問題會最清楚地暴露出來。" },
            { code: "async fn main() {", note: "進入點。" },
            { code: "    let start = Instant::now();", note: "記下起跑時間,用來證明計時器被拖延了。" },
            { code: "    let a = tokio::spawn(async {", note: "第一個 task。" },
            { code: "        std::thread::sleep(std::time::Duration::from_millis(500));", note: "⛔ 問題在這一行:std::thread::sleep 把整條作業系統執行緒停住 500 毫秒。它不是暫停點,runtime 沒有機會換人,同一條執行緒上的其他 task 全部停擺。" },
            { code: "        println!(\"report done\");", note: "500 毫秒後印出,這是第一行輸出。" },
            { code: "    });", note: "第一個 task 結束。" },
            { code: "    let b = tokio::spawn(async move {", note: "第二個 task,move 把 start 搬進去。" },
            { code: "        sleep(Duration::from_millis(10)).await;", note: "只想等 10 毫秒。但計時器到期時沒有人能推動它——執行緒還被上一個 task 佔著。" },
            { code: "        println!(\"timer late: {}\", start.elapsed() >= Duration::from_millis(500));", note: "真正被叫醒時已經是 500 毫秒之後,所以印出 timer late: true。" },
            { code: "    });", note: "第二個 task 結束。" },
            { code: "    let _ = tokio::join!(a, b);", note: "等兩個 JoinHandle 都完成。輸出固定是 report done 後面接 timer late: true。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          intro: "把阻塞的工作移出 runtime 的工作執行緒,其他 task 就不再被卡住:",
          lines: [
            { code: "use tokio::time::{sleep, Duration, Instant};", note: "引入不變。" },
            { code: "", note: "" },
            { code: "#[tokio::main(flavor = \"current_thread\")]", note: "runtime 設定不變,一樣只有一條工作執行緒。" },
            { code: "async fn main() {", note: "進入點。" },
            { code: "    let start = Instant::now();", note: "一樣記下起跑時間。" },
            { code: "    let a = tokio::task::spawn_blocking(|| {", note: "改動處:改用 spawn_blocking,並且傳的是普通閉包而不是 async 區塊。tokio 會把它丟到一池專門承接阻塞工作的執行緒,不佔用 runtime 的工作執行緒。" },
            { code: "        std::thread::sleep(std::time::Duration::from_millis(500));", note: "同樣阻塞 500 毫秒,但現在被阻塞的是那池裡的執行緒,與 runtime 無關。" },
            { code: "        println!(\"report done\");", note: "500 毫秒後印出,現在變成第二行輸出。" },
            { code: "    });", note: "回傳的一樣是 JoinHandle,用法不變。" },
            { code: "    let b = tokio::spawn(async move {", note: "第二個 task 完全沒改。" },
            { code: "        sleep(Duration::from_millis(10)).await;", note: "計時器到期時工作執行緒是空的,馬上被叫醒。" },
            { code: "        println!(\"timer late: {}\", start.elapsed() >= Duration::from_millis(500));", note: "這次只過了約 10 毫秒,印出 timer late: false。" },
            { code: "    });", note: "第二個 task 結束。" },
            { code: "    let _ = tokio::join!(a, b);", note: "等兩邊完成。輸出變成 timer late: false 在前、report done 在後。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "判斷準則很簡單:一段程式如果會呼叫同步 I/O、或算好幾毫秒以上而中間沒有 .await,它就不該直接寫在 async task 裡。",
        },
      ],
      csharp: `同樣的錯誤在 C# 叫做「在 async 方法裡同步阻塞」——Thread.Sleep、task.Result、task.Wait()、同步的檔案讀寫都算,後果是執行緒池飢餓(thread pool starvation),請求延遲暴增。差別在兩點。第一,C# 的執行緒池會偵測飢餓並慢慢增開執行緒(大約每秒一條),所以症狀是「先卡一下再恢復」,不像 tokio 那樣直接停擺。第二,C# 的對策通常是 Task.Run 或改用真正的 async API,Rust 這邊對應的就是 spawn_blocking 與 tokio 自己的 async 版函式。原則兩邊一致:async 的程式碼路徑上不可以有會阻塞的呼叫。`,
    },
    {
      id: "2-8-18",
      question: "tokio::join!(a(), b()) 和「先各自 tokio::spawn 再分別 .await 兩個 JoinHandle」,差在哪裡?",
      options: [
        { text: "join! 把兩台狀態機合成更大的一台,在目前這個 task 裡交錯驅動,離不開這條 .await 鏈;spawn 則是把工作交給 runtime 變成獨立 task,呼叫端就算不 await 那個 handle 它也會被排程,多執行緒 runtime 下還可能跑在別條執行緒上,代價是要求 'static 與 Send" },
        { text: "完全等價,只是語法不同——join! 內部就是對每個分支呼叫 tokio::spawn" },
        { text: "join! 是並發的,spawn 是依序的——spawn 出去的 task 要等前一個結束才會被排程" },
        { text: "join! 只能用在多執行緒 runtime,spawn 只能用在單執行緒 runtime,兩者的使用場合互斥" },
      ],
      answer: 0,
      explanation: `兩者都能讓多個工作並發,但「工作屬於誰」不一樣。
join! 不 spawn 任何東西:它把幾台狀態機組合成一台更大的狀態機,留在目前這個 task 裡輪流推動。好處是沒有額外要求——分支裡可以帶著借用(第 14 題那種綁著 &text 的 Future 用 join! 完全沒問題),也不需要 Send。壞處是它離不開這條 .await 鏈:呼叫端一旦不再推動這台狀態機(例如所在的 select! 分支落敗被丟掉),裡面的工作就全部停住。
spawn 把工作交給 runtime 登記成獨立 task:它有自己的排程資格,呼叫端不 await 那個 JoinHandle 它照樣會跑(只要 runtime 還活著),多執行緒 runtime 下還可能被別條執行緒推動,因此能拿到真正的平行。代價就是第 9 題那道關卡——'static 與 Send。
「join! 內部就是 spawn」正好解釋了為什麼這兩個選項容易混:如果真是那樣,第 14 題那個帶借用的 Future 就不可能被 join!。「spawn 是依序的」則與第 8 題的觀察矛盾——spawn 一回傳,那個 task 就已經在排程佇列裡了。`,
      csharp: `C# 這邊的對照是 await Task.WhenAll(A(), B()) 與 Task.Run(A) / Task.Run(B) 之後再 WhenAll。差異的方向類似:WhenAll 只是等待,不改變工作在哪裡跑;Task.Run 則明確把工作丟進執行緒池。但因為 C# 的 Task 建立即執行,「不 spawn 就不會並發」這件事在 C# 不成立——A() 和 B() 光是被呼叫就已經同時在跑了,WhenAll 純粹是等待語法。Rust 這邊 join! 是讓它們並發的必要條件,不是可有可無的糖,這是第 6 題就出現過的差別。`,
    },
    {
      id: "2-8-19",
      question: "async 版的生產者/消費者。以下程式印出什麼?",
      questionCode: "use tokio::sync::mpsc;\n\n#[tokio::main]\nasync fn main() {\n    let (tx, mut rx) = mpsc::channel::<u32>(8);\n\n    tokio::spawn(async move {\n        for i in 1..=3 {\n            tx.send(i).await.unwrap();\n        }\n    });\n\n    while let Some(v) = rx.recv().await {\n        println!(\"got {}\", v);\n    }\n    println!(\"channel closed\");\n}",
      options: [
        { text: "got 1、got 2、got 3、channel closed——tokio 版的 send 是 async 的(緩衝滿了就等),recv 回傳 Option;tx 被 move 進 task,task 結束時 tx 被釋放,channel 關閉,recv 回傳 None,while let 就結束了" },
        { text: "got 1、got 2、got 3,然後程式永遠停在 rx.recv().await——沒有人呼叫 close(),recv 會一直等下去" },
        { text: "編譯錯誤:tokio::sync::mpsc 的 API 跟 std::sync::mpsc 一樣,recv() 沒有 .await,不能寫在 while let ... .await 裡" },
        { text: "got 1、got 2、got 3、channel closed,不過 send 其實不用寫 .await——mpsc::channel(8) 有緩衝,只有緩衝塞滿的那一次才需要等" },
      ],
      answer: 0,
      explanation: `結構跟 2-7 的 mpsc 完全一樣:多個送出端、單一接收端,所有 Sender 都被釋放後 channel 關閉。換成 async 版本之後有三個具體差別要記。
第一,send 是 async 的:tokio::sync::mpsc::channel(8) 是有容量上限的,緩衝滿了 send 就停在暫停點等消費端收走(這就是背壓 backpressure),所以一定要寫 .await。就算緩衝沒滿,send 回傳的仍然是一台狀態機——不 await 的話一個訊息都送不出去(第 2 題)。
第二,recv 也是 async 的,等訊息時讓出控制權而不是阻塞執行緒。
第三,回傳型別是 Option 而不是 std 版的 Result:有訊息是 Some(v),channel 關閉就是 None,所以慣用寫法直接是 while let Some(v) = rx.recv().await。
「會永遠停在 recv」忽略了關閉條件:tx 被 async move 搬進 task,task 跑完就被 drop,唯一的 Sender 消失,channel 隨即關閉。「send 不用 await」這個選項專門針對「有緩衝所以不會等」的直覺——緩衝影響的是會不會停下來等,不影響它是不是一台需要被驅動的狀態機。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use tokio::sync::mpsc;", note: "注意是 tokio::sync::mpsc,不是 std::sync::mpsc。兩者概念相同但 API 不同,而且 std 版的阻塞式 recv 會把 runtime 的工作執行緒卡住(第 17 題)。" },
          { code: "", note: "" },
          { code: "#[tokio::main]", note: "備好 runtime。" },
          { code: "async fn main() {", note: "進入點。" },
          { code: "    let (tx, mut rx) = mpsc::channel::<u32>(8);", note: "建立容量 8 的有界 channel。rx 要宣告成 mut,因為 recv 需要可變借用。" },
          { code: "", note: "" },
          { code: "    tokio::spawn(async move {", note: "生產者 task。async move 把 tx 的所有權搬進狀態機——tx 是 'static 的值,通過 spawn 的檢查(第 9 題)。" },
          { code: "        for i in 1..=3 {", note: "送出 1、2、3 三個訊息。" },
          { code: "            tx.send(i).await.unwrap();", note: "send 是 async 的:緩衝滿了就停在這裡等消費端收走。回傳 Result,接收端已經消失時會是 Err,這裡用 unwrap 代表「送不出去就 panic」。" },
          { code: "        }", note: "迴圈結束。" },
          { code: "    });", note: "task 結束時 tx 被 drop——這是唯一的 Sender,它一消失 channel 就關閉。" },
          { code: "", note: "" },
          { code: "    while let Some(v) = rx.recv().await {", note: "消費端。recv 回傳 Option:有訊息是 Some(v),channel 關閉且緩衝清空後是 None,while let 自然結束。等待期間讓出控制權,生產者 task 才有機會被推動。" },
          { code: "        println!(\"got {}\", v);", note: "依序印出 got 1、got 2、got 3。" },
          { code: "    }", note: "收到 None,迴圈結束。" },
          { code: "    println!(\"channel closed\");", note: "最後印出 channel closed。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "要多個生產者就 clone tx(和 2-7 一樣,Sender 可以 clone、Receiver 不行);要多個消費者則改用 tokio::sync::broadcast 之類的其他 channel,而不是去包 Arc<Mutex<Receiver>>。",
      },
      csharp: `C# 的 System.Threading.Channels 幾乎是同一套設計:Channel.CreateBounded<int>(8) 對應這裡的容量 8,writer.WriteAsync / reader.ReadAsync 對應 send / recv,而 writer.Complete() 對應「最後一個 Sender 被 drop」。差別有兩個。第一,C# 要明確呼叫 Complete() 才算結束,Rust 靠所有權自動判定——Sender 全部離開作用域就關閉,忘不了也漏不掉。第二,C# 的同一個 Channel 同時提供同步與非同步的讀寫方法,可以混用;Rust 把它拆成兩套獨立的型別(std::sync::mpsc 與 tokio::sync::mpsc),在 async 程式裡誤用同步版本不會編譯失敗,但會卡住 runtime。`,
    },
    {
      id: "2-8-20",
      question: "回頭總結:C# 的 async/await 和 Rust 的 async/await 語法幾乎一模一樣,最關鍵的模型差異是什麼?",
      options: [
        { text: "C# 的 Task 建立即執行(呼叫 async 方法會同步跑到第一個 await),排程由內建的執行緒池負責;Rust 的 Future 是惰性的,沒有人 .await 或 spawn 就一行都不會跑,而且執行器不在標準庫裡,要自己選一個 runtime——所以 join!/select!/spawn 在 Rust 是讓工作並發的必要條件,不只是等待語法" },
        { text: "兩邊其實完全一樣,Rust 只是把 Task 改名叫 Future、把執行緒池改名叫 runtime,換個名字而已" },
        { text: "最大的差異是 Rust 的 async 一定跑在單一執行緒上,C# 的 async 一定跑在多執行緒上" },
        { text: "最大的差異是 Rust 的 .await 會阻塞整條執行緒,C# 的 await 不會,所以 Rust 的 async 只適合少量工作" },
      ],
      answer: 0,
      explanation: `把這一課的線索串起來:第 1、2 題確立了惰性——呼叫 async fn 只是建立一台狀態機,沒有人驅動就什麼都不會發生;第 4 題說明了為什麼要自己挑 runtime——標準庫只定義介面,不附執行器;第 6 題顯示 join! 是讓兩個工作真的重疊的必要條件,不像 C# 的 WhenAll 只是等待語法;第 7 題的 select! 用「丟掉就等於取消」取代了 CancellationToken;第 9、14、15 題則是所有權與生命週期在 async 世界的延伸,對應 C# 完全交給 GC 的那一塊。
選項裡「Rust 的 async 一定單執行緒」搞混了兩件事:單執行緒 runtime 只是一種 flavor(第 8 題),預設的多執行緒 runtime 可以真正平行。「.await 會阻塞整條執行緒」正好相反(第 3 題),真正會阻塞的是 std::thread::sleep 那類同步呼叫(第 17 題)。「只是改個名字」則低估了惰性帶來的所有連鎖後果——忘記 .await 在 C# 是「沒等到」,在 Rust 是「根本沒發生」。`,
      csharp: `給要在兩邊切換的開發者三條速查規則。第一,看到 async fn 就在心裡把簽名讀成 C# 的 Task<T>:Rust 把那層包裝藏在關鍵字底下,錯誤訊息會把它原形畢露成 impl Future<Output = T>。第二,凡是 C# 靠 GC 撐住的地方(把 Task 存進欄位、到處傳、跨作用域用),Rust 都會要你先證明生命週期,不然就得讓工作擁有資料(move、Arc)。第三,C# 的「在 async 裡同步阻塞」是效能問題,Rust 的同一件事在單執行緒 runtime 上是功能性停擺——這條的嚴重程度不同,要比在 C# 更小心。`,
    },
  ],
};
