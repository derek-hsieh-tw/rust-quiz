/* 出題慣例見專案根目錄 AUTHORING.md:
 *   - answer 一律為 0(正確答案寫在第一個選項),顯示順序由 quiz.js 依題目 id 洗牌
 *   - 詳解禁止用「選項 A/B/C」字母指涉,必須直接描述選項內容
 *   - 有程式碼的題目一律附 walkthrough(逐行說明);反面案例必須同時附上 √ 正確寫法 */
window.RUST_LESSONS = window.RUST_LESSONS || {};
window.RUST_LESSONS["lesson2-2"] = {
  id: "lesson2-2",
  title: "迭代器(Iterators)",
  goal: "用轉接器/消耗器串成的鏈取代手寫迴圈,看懂「只實作 next() 就能免費獲得整套方法」的 trait 設計,以及迭代器怎麼跟所有權、借用互動。",
  primer: {
    intro: "迭代器代表「一串可以逐一取出的值」。Rust 的集合用 iter() 這類方法交出一個迭代器,再用 map、filter 這類方法把處理步驟串起來,最後用 sum、collect 之類的方法拿到結果。這套寫法取代了大部分手寫的索引迴圈:不用自己管索引,也不會越界。",
    examples: [
      {
        code: "fn main() {\n    let prices = vec![30, 120, 75];\n    for p in prices.iter() {\n        println!(\"{}\", p);\n    }\n}",
        note: "iter() 交出一個迭代器,for 迴圈逐一取出元素並印出。",
      },
      {
        code: "fn main() {\n    let prices = vec![30, 120, 75];\n    let total: i32 = prices.iter().map(|p| p * 2).sum();\n    println!(\"{}\", total);\n}",
        note: "把每個價格乘以 2 再加總,印出 450;map 的參數就是上一課學過的閉包。",
      },
    ],
    csharp: "整體手感和 LINQ 很像:map 對應 Select、filter 對應 Where、sum 對應 Sum。差別在於 Rust 的每一步都帶著所有權與借用的規則,這一課大部分的題目都在講這件事。",
  },
  questions: [
    {
      id: "2-2-01",
      question: "Iterator trait 的核心是 next()。以下程式碼的輸出是?",
      questionCode: "struct Countdown {\n    n: u32,\n}\n\nimpl Iterator for Countdown {\n    type Item = u32;\n    fn next(&mut self) -> Option<u32> {\n        if self.n == 0 {\n            return None;\n        }\n        self.n -= 1;\n        Some(self.n + 1)\n    }\n}\n\nfn main() {\n    let mut c = Countdown { n: 2 };\n    println!(\"{:?}\", c.next());\n    println!(\"{:?}\", c.next());\n    println!(\"{:?}\", c.next());\n    println!(\"{:?}\", c.next());\n}",
      options: [
        { text: "依序印出 Some(2)、Some(1)、None、None——next() 每呼叫一次就往前推進一格,沒有東西了就回傳 None,之後再呼叫仍然依實作回傳 None" },
        { text: "編譯錯誤:實作 Iterator 必須同時提供 map、filter、collect 等方法,只寫 next() 不完整" },
        { text: "依序印出 2、1、None、None——next() 回傳的是元素本身,結束時才回傳 None" },
        { text: "前三行正常,第四行 panic:序列已經結束後再呼叫 next() 是執行期錯誤" },
      ],
      answer: 0,
      explanation: `Iterator trait 只有一個必須實作的方法:fn next(&mut self) -> Option<Self::Item>。每呼叫一次,迭代器就推進一格並交出 Some(元素);沒有東西可交時回傳 None。type Item = u32 則宣告「這個迭代器吐出的元素是什麼型別」。
「必須同時實作 map、filter、collect」是最常見的誤解——那些方法全部是 Iterator trait 的預設方法,寫好 next() 就自動擁有(第 16 題會實際用到)。「回傳元素本身」也不對:回傳型別明明寫著 Option<u32>,{:?} 印出的是 Some(2) 而不是 2。至於結束後再呼叫:trait 本身沒有規定一定要 panic,行為完全由你的 next() 決定——這裡 n 已經是 0,所以再呼叫一次仍然走 return None 那條路。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "struct Countdown {", note: "迭代器本身就是一個普通的 struct,用來記住「目前走到哪裡」。" },
          { code: "    n: u32,", note: "唯一的狀態:還剩幾個數字要交出去。" },
          { code: "}", note: "定義結束。" },
          { code: "", note: "" },
          { code: "impl Iterator for Countdown {", note: "為它實作標準函式庫的 Iterator trait。" },
          { code: "    type Item = u32;", note: "關聯型別:宣告每次交出的元素型別是 u32。" },
          { code: "    fn next(&mut self) -> Option<u32> {", note: "唯一必須實作的方法。接收 &mut self,因為每呼叫一次都要修改內部狀態。" },
          { code: "        if self.n == 0 {", note: "先檢查是否已經沒東西可交。" },
          { code: "            return None;", note: "用 None 表示「序列結束」,這是迭代器唯一的結束訊號。" },
          { code: "        }", note: "if 區塊結束。" },
          { code: "        self.n -= 1;", note: "推進狀態。" },
          { code: "        Some(self.n + 1)", note: "交出推進前的那個數字,包在 Some 裡。" },
          { code: "    }", note: "方法結束。" },
          { code: "}", note: "impl 結束。map、filter、sum、collect 等幾十個方法此時已經自動可用。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let mut c = Countdown { n: 2 };", note: "必須是 mut:呼叫 next() 需要 &mut self。" },
          { code: "    println!(\"{:?}\", c.next());", note: "n 從 2 變 1,印出 Some(2)。" },
          { code: "    println!(\"{:?}\", c.next());", note: "n 從 1 變 0,印出 Some(1)。" },
          { code: "    println!(\"{:?}\", c.next());", note: "n 已經是 0,印出 None——序列結束。" },
          { code: "    println!(\"{:?}\", c.next());", note: "再呼叫一次,依這份實作仍然印出 None,不會 panic。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "for 迴圈、sum、collect 做的事,底層都是「一直呼叫 next(),直到拿到 None 為止」。記住這個模型,後面每一題都能從它推出來。",
      },
      csharp: `對應 C# 的 IEnumerator<T>:MoveNext() 推進並回傳 bool,Current 讀取目前元素。Rust 把這兩步合成一個 next(),用 Option 同時表達「還有沒有」與「是什麼」——不會出現 C# 那種「還沒呼叫 MoveNext 就讀 Current」或「MoveNext 回傳 false 之後還讀 Current」的未定義狀態。另外 C# 要分 IEnumerable(可列舉的東西)與 IEnumerator(游標)兩個介面,Rust 的 Iterator 本身就是游標,「可以被走訪的集合」則由 IntoIterator 描述(第 4 題)。`,
    },
    {
      id: "2-2-02",
      question: "惰性求值。以下程式碼的輸出順序是?",
      questionCode: "fn main() {\n    let v = vec![1, 2, 3];\n    let it = v.iter().map(|x| {\n        println!(\"map {}\", x);\n        x * 10\n    });\n    println!(\"built\");\n    let out: Vec<i32> = it.collect();\n    println!(\"{:?}\", out);\n}",
      options: [
        { text: "先印 built,再印 map 1、map 2、map 3,最後印 [10, 20, 30]——map 只是把步驟記下來,要等 collect 開始要元素時閉包才會執行" },
        { text: "先印 map 1、map 2、map 3,再印 built,最後印 [10, 20, 30]——呼叫 map 的當下就把整個集合算完了" },
        { text: "先印 map 1、map 2、map 3,再印 built,最後印 [10, 20, 30]——map 回傳的是一個已經算好的 Vec,collect 只是把它複製一份" },
        { text: "只印 built 與 [10, 20, 30]——map 裡的 println! 在迭代器鏈中會被最佳化掉" },
      ],
      answer: 0,
      explanation: `迭代器轉接器(adapter)是惰性的:v.iter().map(...) 只是建立一個「包著原迭代器與閉包」的新結構,什麼都還沒算。真正驅動的是消耗器(consumer)——collect 開始反覆呼叫 next(),每要一個元素,map 才對那個元素執行一次閉包。所以 built 一定最先出現。
「呼叫 map 當下就把整個集合算完」是從 eager 集合 API(例如 JavaScript 的 Array.prototype.map)帶過來的直覺;「map 回傳 Vec」也是同一個誤解的變形——map 的回傳型別是 Map<Iter<i32>, 閉包>,根本不是 Vec。至於「println! 被最佳化掉」:最佳化絕不會改變程式可觀察的行為,輸出一定照實出現。
如果你只寫 v.iter().map(...); 而不接任何消耗器,編譯器會給出 unused Map that must be used 的警告,附註 iterators are lazy and do nothing unless consumed——連編譯器都知道這是常見陷阱。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let v = vec![1, 2, 3];", note: "來源資料。" },
          { code: "    let it = v.iter().map(|x| {", note: "建立一條鏈:iter() 產生借用走訪的迭代器,map 把它包起來。此時閉包一次都還沒執行,it 只是一個「待辦步驟」的結構。" },
          { code: "        println!(\"map {}\", x);", note: "閉包本體:用來觀察它實際在什麼時間點執行。" },
          { code: "        x * 10", note: "x 是 &i32,乘以 10 得到 i32。" },
          { code: "    });", note: "鏈建好了,但什麼都沒印。" },
          { code: "    println!(\"built\");", note: "第一行輸出就是 built,證明上面的 map 還沒動。" },
          { code: "    let out: Vec<i32> = it.collect();", note: "消耗器登場:collect 反覆呼叫 next(),每次呼叫都讓 map 對一個元素執行閉包,依序印出 map 1、map 2、map 3。" },
          { code: "    println!(\"{:?}\", out);", note: "印出 [10, 20, 30]。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "分清兩類方法:回傳另一個迭代器的是轉接器(map、filter、zip、enumerate、take…),惰性;回傳「不是迭代器的東西」的是消耗器(collect、sum、count、fold、any…),它們才會真的把鏈跑完。",
      },
      csharp: `這一題和 LINQ 的延遲執行(deferred execution)完全對應:var q = list.Select(x => { Console.WriteLine(x); return x * 10; }); 在 ToList() 或 foreach 之前一行都不會印。C# 開發者對這件事通常已經有直覺,需要調整的只有名詞:Select/Where 是 Rust 的轉接器,ToList/Sum/Count 是消耗器。差別在 Rust 的編譯器會對「建了鏈卻沒消耗」直接發警告,C# 則要靠分析器或 code review 才抓得到。`,
    },
    {
      id: "2-2-03",
      question: "iter() / iter_mut() / into_iter() 三兄弟。以下程式碼的輸出是?",
      questionCode: "fn main() {\n    let mut names = vec![String::from(\"a\"), String::from(\"b\")];\n    for n in names.iter_mut() {\n        n.push('!');\n    }\n    for n in names.iter() {\n        print!(\"{} \", n);\n    }\n    let owned: Vec<String> = names.into_iter().collect();\n    println!(\"{:?}\", owned);\n}",
      options: [
        { text: "印出 a! b! [\"a!\", \"b!\"]——iter_mut 交出 &mut String 可以就地修改,iter 交出 &String 只能讀,into_iter 交出 String 本身並拿走 names 的所有權" },
        { text: "印出 a b [\"a\", \"b\"]——for 迴圈拿到的是元素的複本,push 改的是複本" },
        { text: "編譯錯誤:同一個 Vec 先被 iter_mut 可變借用,之後就不能再呼叫 iter" },
        { text: "編譯錯誤:into_iter 產生的元素型別是 &String,collect 不能收進 Vec<String>" },
      ],
      answer: 0,
      explanation: `三個方法的差別只在「元素以什麼形式交出來」:iter() 交出 &T(唯讀借用)、iter_mut() 交出 &mut T(可變借用)、into_iter() 交出 T 本身(按值取用,呼叫之後原集合就被吃掉了)。第一個迴圈透過 &mut String 直接改到 Vec 裡的字串;第二個迴圈唯讀;最後 into_iter 把兩個 String 的所有權一個個搬出來,收進新的 Vec<String>。
「拿到的是複本」不成立:iter_mut 交出的是指向原元素的可變參考,不是複製品。「先 iter_mut 就不能再 iter」也不對——第一個迴圈結束時可變借用就結束了,第二個迴圈是全新的借用。最後一個干擾選項把 into_iter 跟 iter 搞混:對 Vec<String> 本身呼叫 into_iter,元素型別是 String;只有對 &Vec<String> 呼叫 into_iter 才會得到 &String。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let mut names = vec![String::from(\"a\"), String::from(\"b\")];", note: "names 擁有兩個 String。宣告成 mut,因為待會要可變借用它。" },
          { code: "    for n in names.iter_mut() {", note: "iter_mut 對 names 建立可變借用,每一輪 n 的型別是 &mut String,指向 Vec 裡真正的那個元素。" },
          { code: "        n.push('!');", note: "透過可變參考就地修改,Vec 裡的字串變成 a! 與 b!。" },
          { code: "    }", note: "迴圈結束,可變借用也結束。" },
          { code: "    for n in names.iter() {", note: "iter 建立新的唯讀借用,n 的型別是 &String。上一段的可變借用已結束,兩者不衝突。" },
          { code: "        print!(\"{} \", n);", note: "印出 a! b!(各自接一個空白)。" },
          { code: "    }", note: "唯讀借用結束。" },
          { code: "    let owned: Vec<String> = names.into_iter().collect();", note: "into_iter 按值取用 names,逐一交出 String 本身;collect 把它們收進新的 Vec。從這一行之後 names 已經被移走,不能再用。" },
          { code: "    println!(\"{:?}\", owned);", note: "印出 [\"a!\", \"b!\"]。" },
          { code: "}", note: "owned 離開作用域,兩個 String 被釋放。" },
        ],
        outro: "口訣:要讀用 iter(),要改用 iter_mut(),要拿走用 into_iter()。for x in &v、for x in &mut v、for x in v 分別就是這三者的簡寫。",
      },
      csharp: `C# 的 foreach 對集合永遠只有一種模式:拿到元素的「值」——參考型別就是參考的複本,可以改物件內部但不能換掉集合裡那一格;值型別則是複本,改了也不會寫回集合(對 List<struct> 做 foreach 修改欄位甚至直接編譯錯誤)。Rust 把這三種意圖拆成三個方法,並由型別系統保證:唯讀走訪時沒人能改、可變走訪時沒人能讀、按值走訪之後原集合就不存在了。`,
    },
    {
      id: "2-2-04",
      question: "for 迴圈走訪 Vec 之後,還能用原來的變數嗎?以下程式碼的結果是?",
      questionCode: "fn main() {\n    let v = vec![String::from(\"a\"), String::from(\"b\")];\n    for s in v {\n        println!(\"{}\", s);\n    }\n    println!(\"{}\", v.len());\n}",
      options: [
        { text: "編譯錯誤:borrow of moved value: v——for s in v 會對 v 呼叫 into_iter(),按值取用,整個 Vec 在迴圈開始時就被吃掉了" },
        { text: "印出 a、b、2——for 迴圈只是借用集合,走訪完就歸還" },
        { text: "印出 a、b、0——迴圈把元素一個個移出去了,但 Vec 本身還在,只是變空" },
        { text: "編譯錯誤:for 迴圈不能直接走訪 Vec,必須寫 v.iter()" },
      ],
      answer: 0,
      explanation: `for s in v 不是魔法語法,它會被展開成 IntoIterator::into_iter(v),而 into_iter 按值接收 self——v 的所有權在迴圈開始時就交出去了,每一輪 s 拿到的是 String 本身。迴圈結束後,迭代器與剩下的東西一起被釋放,v 已經不存在,所以 v.len() 報 borrow of moved value。
「for 迴圈只是借用」是 C# foreach 帶來的最強烈直覺,這題就是要打破它。「Vec 還在但變空」也不對:被移走的是整個 Vec,不是元素一個個被抽走後留下空殼,編譯器根本不允許你再碰 v。「必須寫 v.iter()」則是把合法寫法當成唯一寫法——for 可以直接吃任何實作 IntoIterator 的東西,Vec、&Vec、&mut Vec 都是。修法就是依意圖選一個:只要讀就寫 for s in &v。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let v = vec![String::from(\"a\"), String::from(\"b\")];", note: "v 擁有這個 Vec 與裡面的兩個 String。" },
            { code: "    for s in v {", note: "⛔ 所有權在這裡交出去:編譯器的註記是 v moved due to this implicit call to .into_iter()。into_iter 按值接收 self,v 從這一行之後就被移走了。" },
            { code: "        println!(\"{}\", s);", note: "s 的型別是 String(不是 &String),每一輪擁有一個元素。這一行本身沒有問題,會印出 a、b。" },
            { code: "    }", note: "迴圈結束,迭代器被釋放。" },
            { code: "    println!(\"{}\", v.len());", note: "⛔ 編譯失敗:borrow of moved value: v。要呼叫 len 得先借用 v,但它的所有權已經在迴圈開頭交出去了。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "編譯器的 help 會直接建議:consider iterating over a slice of the Vec<String>'s content to avoid moving into the for loop,也就是在 v 前面加一個 &。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let v = vec![String::from(\"a\"), String::from(\"b\")];", note: "完全不用改。" },
            { code: "    for s in &v {", note: "改動處:走訪 &v 而不是 v。對 &Vec 呼叫 into_iter 得到的是借用版的迭代器,v 的所有權留在原地。" },
            { code: "        println!(\"{}\", s);", note: "s 的型別變成 &String,一樣印出 a、b。" },
            { code: "    }", note: "迴圈結束,借用也結束。" },
            { code: "    println!(\"{}\", v.len());", note: "v 仍然有效,印出 2。" },
            { code: "}", note: "v 在這裡才被釋放。" },
          ],
        },
        {
          label: "△ Desugared (what the compiler sees)",
          intro: "錯誤訊息提到了 into_iter,但你的程式碼裡沒有這個字。因為 for 迴圈本身就是一層糖:",
          lines: [
            { code: "for s in v { println!(\"{}\", s); }", note: "你寫的這一行。" },
            { code: "", note: "" },
            { code: "// 編譯器把它展開成:", note: "以下才是編譯器實際檢查的東西。" },
            { code: "let mut it = IntoIterator::into_iter(v);", note: "第一步就把 v 按值交給 into_iter——錯誤訊息裡的 into_iter 就是從這裡露出來的,v 的所有權在此移走。" },
            { code: "while let Some(s) = it.next() {", note: "之後就是第 1 題的模型:一直呼叫 next(),直到拿到 None。" },
            { code: "    println!(\"{}\", s);", note: "迴圈本體原樣照抄。" },
            { code: "}", note: "拿到 None 就結束。" },
            { code: "", note: "" },
            { code: "// for s in &v 則展開成 IntoIterator::into_iter(&v)", note: "交出去的是一份借用,等同於 v.iter(),v 本身不受影響。" },
          ],
          outro: "同一個 into_iter,接收的東西不同,效果就不同:給它 Vec 會吃掉 Vec,給它 &Vec 只是借用,給它 &mut Vec 則是可變借用。",
        },
      ],
      csharp: `C# 的 foreach (var s in list) 永遠不會讓 list 失效,因為它只是呼叫 list.GetEnumerator(),集合的生死由 GC 決定。Rust 的 for 則讓你選擇「走訪完之後這個集合還要不要」:for s in v 說的是「不要了,元素直接歸我」,好處是可以把 String 直接搬走而不用 clone;for s in &v 才是 C# foreach 的語意。兩者寫法只差一個 &,編譯器會在你選錯的時候告訴你。`,
    },
    {
      id: "2-2-05",
      question: "map / filter 的執行順序。以下程式碼的輸出是?",
      questionCode: "fn main() {\n    let v = vec![1, 2, 3];\n    let r: Vec<i32> = v\n        .iter()\n        .filter(|&&x| {\n            print!(\"f{} \", x);\n            x != 2\n        })\n        .map(|&x| {\n            print!(\"m{} \", x);\n            x * 10\n        })\n        .collect();\n    println!(\"{:?}\", r);\n}",
      options: [
        { text: "f1 m1 f2 f3 m3 [10, 30]——元素是一個一個流過整條管線的,1 先通過 filter 再進 map,2 被 filter 擋下,3 再走完全程" },
        { text: "f1 f2 f3 m1 m3 [10, 30]——filter 先把整個集合過濾完,再把結果整批交給 map" },
        { text: "編譯錯誤:filter 的閉包必須回傳 Option,用 Some(x) 表示保留、None 表示丟掉" },
        { text: "f1 m1 f2 m2 f3 m3 [10, 30]——每個元素都會經過 map,filter 只是在最後 collect 時才生效" },
      ],
      answer: 0,
      explanation: `惰性求值的另一面是「逐元素拉取」:collect 向 map 要一個元素,map 向 filter 要一個,filter 向 iter 要一個並檢查條件,不通過就再要下一個。所以輸出是每個元素各自走完它能走的路:1 通過 filter、進 map;2 在 filter 被擋下,map 根本看不到它;3 再走完全程。
「filter 先整批過濾完再交給 map」是把每個轉接器想成會產生中間集合的步驟——Rust 的鏈裡沒有任何中間 Vec,這也是它能跟手寫迴圈一樣快的原因之一。「filter 閉包要回傳 Option」是把 filter 跟 filter_map 搞混:filter 的閉包回傳 bool,filter_map 才是回傳 Option、同時過濾又轉換。
附帶一提,filter 閉包的參數寫成 |&&x| 是因為 iter() 交出 &i32,而 filter 為了不拿走元素,再借一層交給閉包,所以參數型別是 &&i32;模式 &&x 把兩層參考拆掉,讓 x 直接是 i32。map 則直接收到元素本身(&i32),所以只要一個 &。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let v = vec![1, 2, 3];", note: "來源資料。" },
          { code: "    let r: Vec<i32> = v", note: "整條鏈的結果收進 Vec<i32>。" },
          { code: "        .iter()", note: "借用走訪,交出 &i32。" },
          { code: "        .filter(|&&x| {", note: "filter 把元素再借一層給閉包,參數型別是 &&i32;模式 &&x 拆掉兩層,x 是 i32。" },
          { code: "            print!(\"f{} \", x);", note: "記錄 filter 看到了哪個元素。" },
          { code: "            x != 2", note: "回傳 bool:true 保留、false 丟掉。2 會在這裡被擋下。" },
          { code: "        })", note: "filter 結束,它仍然只是一個惰性的轉接器。" },
          { code: "        .map(|&x| {", note: "map 收到的是通過 filter 的元素本身(&i32),模式 &x 拆掉一層。" },
          { code: "            print!(\"m{} \", x);", note: "記錄 map 看到了哪個元素——只會看到 1 和 3。" },
          { code: "            x * 10", note: "轉換成新的值。" },
          { code: "        })", note: "map 結束。" },
          { code: "        .collect();", note: "消耗器開始逐一拉取:依序印出 f1 m1 f2 f3 m3。" },
          { code: "    println!(\"{:?}\", r);", note: "接在同一行後面印出 [10, 30]。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "想像成一條輸送帶:元素一次一個被往前推,半路被丟掉的就不會出現在下游。這個「一個接一個」的模型,也是第 10、12 題短路行為的基礎。",
      },
      csharp: `LINQ to Objects 其實也是逐元素拉取:list.Where(...).Select(...) 對每個元素依序跑 Where 再跑 Select,印出來的順序和這題一樣。容易混淆的是寫過 List<T>.FindAll(...).ConvertAll(...) 這種 eager API 的人,它們每一步都產生新 List,順序就會變成「先整批過濾再整批轉換」。Rust 的轉接器對應的是 LINQ 的行為,而不是 FindAll 的行為。`,
    },
    {
      id: "2-2-06",
      question: "enumerate() 與 zip()。以下程式碼的輸出是?",
      questionCode: "fn main() {\n    let names = vec![\"a\", \"b\", \"c\"];\n    let scores = vec![90, 80];\n    for (i, (n, s)) in names.iter().zip(scores.iter()).enumerate() {\n        println!(\"{} {} {}\", i, n, s);\n    }\n}",
      options: [
        { text: "印出兩行:0 a 90 與 1 b 80——zip 在較短的那一邊結束時就停,enumerate 把索引放在 tuple 的第一個位置、從 0 開始" },
        { text: "印出兩行後 panic:zip 的兩邊長度不同,走到第三個元素時找不到對應的分數" },
        { text: "編譯錯誤:enumerate 產生的 tuple 是 (元素, 索引),解構模式應該寫成 ((n, s), i)" },
        { text: "印出三行,第三行是 2 c 0——長度不足的那一邊會補上型別的預設值" },
      ],
      answer: 0,
      explanation: `zip 把兩個迭代器「拉鍊式」配對,每次各向兩邊要一個元素,任何一邊回傳 None 就整體結束——所以長度不同完全合法,結果長度取短的那一邊,多出來的 c 被默默忽略。enumerate 則把每個元素包成 (索引, 元素),索引型別是 usize、從 0 開始。
「長度不同會 panic」是最常見的誤解,事實正好相反:zip 從不 panic,這個「默默截短」有時反而會藏住資料對不齊的 bug,需要時要自己先比對長度。「tuple 是 (元素, 索引)」是記反了順序——Rust 固定是索引在前,模式寫反的話型別對不上,會得到 mismatched types。「補預設值」的行為在標準函式庫的 zip 裡不存在,要那種效果得自己另外組合。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let names = vec![\"a\", \"b\", \"c\"];", note: "三個名字。" },
          { code: "    let scores = vec![90, 80];", note: "只有兩個分數——刻意讓長度不一致。" },
          { code: "    for (i, (n, s)) in names.iter().zip(scores.iter()).enumerate() {", note: "zip 產生 (&&str, &i32) 的配對,enumerate 再把它包成 (usize, (&&str, &i32))。模式 (i, (n, s)) 一次拆開兩層 tuple。第三輪時 scores 那邊回傳 None,整條鏈就結束了。" },
          { code: "        println!(\"{} {} {}\", i, n, s);", note: "依序印出 0 a 90、1 b 80。c 永遠不會出現。" },
          { code: "    }", note: "迴圈結束。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "zip 與 enumerate 都是轉接器,一樣是惰性、逐元素拉取;它們取代的是手動維護索引變數、用同一個索引同時存取兩個陣列的寫法,而且不會越界。",
      },
      csharp: `C# 裡最常見的寫法是 for (int i = 0; i < names.Count; i++) 再用 i 同時索引兩個 List——長度不一時就是 ArgumentOutOfRangeException。LINQ 其實有 Zip,行為和 Rust 一樣是「取短的一邊」,也有 Select((x, i) => ...) 的帶索引多載,只是很多 C# 開發者平常用得少。Rust 把這兩個動作做成一等公民的轉接器,取代索引迴圈是預設寫法而不是進階技巧。`,
    },
    {
      id: "2-2-07",
      question: "對自訂迭代器呼叫 rev()。以下程式碼的結果是?",
      questionCode: "struct Span {\n    lo: u32,\n    hi: u32,\n}\n\nimpl Iterator for Span {\n    type Item = u32;\n    fn next(&mut self) -> Option<u32> {\n        if self.lo >= self.hi {\n            return None;\n        }\n        self.lo += 1;\n        Some(self.lo - 1)\n    }\n}\n\nfn main() {\n    for x in (Span { lo: 1, hi: 4 }).rev() {\n        println!(\"{}\", x);\n    }\n}",
      options: [
        { text: "編譯錯誤:the trait bound Span: DoubleEndedIterator is not satisfied——rev() 需要能「從尾端取元素」,只實作 next() 的迭代器不具備這個能力" },
        { text: "依序印出 3、2、1——rev() 是 Iterator 的預設方法,只要有 next() 就能用" },
        { text: "依序印出 1、2、3——rev() 只對標準函式庫的集合有效,對自訂型別會被忽略" },
        { text: "依序印出 3、2、1,但會先把所有元素收進一個暫存 Vec 再倒過來走,所以比正向走訪慢" },
      ],
      answer: 0,
      explanation: `rev() 確實定義在 Iterator 上,但它帶了一個額外的 bound:Self: DoubleEndedIterator。意思是「要倒著走,你得證明自己能從尾端交出元素」——也就是實作 next_back()。只實作 next() 的 Span 只知道怎麼往前,編譯器不會替你偷偷把元素收起來再倒過來,而是直接拒絕。
「rev 是預設方法所以一定能用」只對一半:預設方法不代表沒有條件,有些方法對 Self 或 Item 還有額外要求。「對自訂型別會被忽略」不存在——Rust 不會默默忽略一個呼叫。「先收進暫存 Vec 再倒過來」則是某些其他語言的做法,Rust 的 rev() 從不配置記憶體,它就是把 next() 轉呼叫成 next_back(),這正是它必須要求 DoubleEndedIterator 的原因。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "struct Span {", note: "一個代表半開區間 [lo, hi) 的迭代器。" },
            { code: "    lo: u32,", note: "目前的前端位置。" },
            { code: "    hi: u32,", note: "結束位置(不含)。" },
            { code: "}", note: "定義結束。" },
            { code: "", note: "" },
            { code: "impl Iterator for Span {", note: "只實作了 Iterator。" },
            { code: "    type Item = u32;", note: "元素型別。" },
            { code: "    fn next(&mut self) -> Option<u32> {", note: "只知道怎麼從前端交出元素。" },
            { code: "        if self.lo >= self.hi {", note: "前端追上尾端就代表空了。" },
            { code: "            return None;", note: "序列結束。" },
            { code: "        }", note: "if 區塊結束。" },
            { code: "        self.lo += 1;", note: "前端往後推一格。" },
            { code: "        Some(self.lo - 1)", note: "交出推進前的位置。" },
            { code: "    }", note: "方法結束。" },
            { code: "}", note: "impl 結束。注意這裡沒有任何「從尾端取」的方法。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    for x in (Span { lo: 1, hi: 4 }).rev() {", note: "⛔ 編譯失敗:the trait bound Span: DoubleEndedIterator is not satisfied(required by this bound in Iterator::rev)。rev 要求 Self 能從尾端取元素,Span 沒有證明這件事。" },
            { code: "        println!(\"{}\", x);", note: "永遠到不了這裡。" },
            { code: "    }", note: "方法結束。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "struct Span {", note: "結構不變。" },
            { code: "    lo: u32,", note: "前端位置。" },
            { code: "    hi: u32,", note: "尾端位置(不含)。" },
            { code: "}", note: "定義結束。" },
            { code: "", note: "" },
            { code: "impl Iterator for Span {", note: "原本的 Iterator 實作完全不用改。" },
            { code: "    type Item = u32;", note: "元素型別。" },
            { code: "    fn next(&mut self) -> Option<u32> {", note: "從前端取。" },
            { code: "        if self.lo >= self.hi {", note: "兩端相遇就是空了。" },
            { code: "            return None;", note: "序列結束。" },
            { code: "        }", note: "if 區塊結束。" },
            { code: "        self.lo += 1;", note: "前端推進。" },
            { code: "        Some(self.lo - 1)", note: "交出推進前的位置。" },
            { code: "    }", note: "方法結束。" },
            { code: "}", note: "定義結束。" },
            { code: "", note: "" },
            { code: "impl DoubleEndedIterator for Span {", note: "改動處:補上 DoubleEndedIterator,證明這個迭代器也能從尾端取元素。" },
            { code: "    fn next_back(&mut self) -> Option<u32> {", note: "唯一要實作的方法,跟 next() 對稱。" },
            { code: "        if self.lo >= self.hi {", note: "結束條件和 next() 共用,前後兩端不會互相越過。" },
            { code: "            return None;", note: "序列結束。" },
            { code: "        }", note: "if 區塊結束。" },
            { code: "        self.hi -= 1;", note: "尾端往前縮一格。" },
            { code: "        Some(self.hi)", note: "交出新的尾端位置。" },
            { code: "    }", note: "方法結束。" },
            { code: "}", note: "impl 結束,rev() 的 bound 從此成立。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    for x in (Span { lo: 1, hi: 4 }).rev() {", note: "rev() 把每次 next() 轉呼叫成 next_back(),沒有任何暫存配置。" },
            { code: "        println!(\"{}\", x);", note: "依序印出 3、2、1。" },
            { code: "    }", note: "迴圈結束。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "如果不方便實作 next_back(例如資料是一邊產生一邊丟掉的串流),替代做法是先 collect 成 Vec 再對 Vec 的迭代器呼叫 rev()——這時配置記憶體的成本是你自己明確選擇付的。",
        },
      ],
      csharp: `LINQ 的 Reverse() 對任何 IEnumerable<T> 都能呼叫,代價藏在實作裡:它會先把整個序列緩衝進一個陣列,再倒過來吐,對無窮序列甚至會卡死。Rust 反過來把「能不能倒著走」寫進型別:有 DoubleEndedIterator 的迭代器(Vec、slice、range 的迭代器都有)可以零成本倒走,沒有的就在編譯期被擋下,讓你自己決定要不要付緩衝的成本。`,
    },
    {
      id: "2-2-08",
      question: "設計選擇:下面三種寫法都想把結果收成 Vec<i32>。關於它們,正確的說法是?",
      questionCode: "let v = vec![1, 2, 3];\nlet a: Vec<i32> = v.iter().map(|x| x + 1).collect();\nlet b = v.iter().map(|x| x + 1).collect::<Vec<i32>>();\nlet c: Vec<_> = v.iter().map(|x| x + 1).collect();",
      options: [
        { text: "三種都合法而且結果相同。collect 需要知道目標型別,可以從變數標註得知,也可以用 ::<> 直接寫在呼叫上;Vec<_> 只指定容器、元素型別交給編譯器推斷" },
        { text: "只有 b 合法:collect 是泛型方法,泛型參數一定要用 ::<> 明確寫出來,變數的型別標註不會影響方法的泛型推斷" },
        { text: "a 與 b 合法,c 不合法:型別標註裡不能用底線 _ 當佔位符" },
        { text: "三種都合法,但 b 效能最好:turbofish 讓編譯器不用推斷,少做一次型別轉換" },
      ],
      answer: 0,
      explanation: `collect 的簽名是 fn collect<B: FromIterator<Self::Item>>(self) -> B——它能收成 Vec、HashMap、String、Result 等任何實作了 FromIterator 的型別,所以必須有人告訴它 B 是什麼。資訊可以從兩個方向來:從左邊的變數標註反推(a),或用 turbofish ::<> 直接寫在呼叫上(b)。c 的 Vec<_> 是「我只在乎容器,元素型別你照鏈推」,在元素型別很長的時候特別好用。
「變數標註不會影響方法的泛型推斷」是誤解——Rust 的型別推斷是雙向的,回傳值要放進什麼型別,本身就是推斷 B 的線索。「_ 不能當佔位符」也不對,_ 在型別位置就是「這一格請推斷」。至於效能:型別推斷是編譯期的事,三種寫法產生的機器碼完全相同,沒有「少做一次轉換」這回事。
怎麼選?結果要存進變數時,標註在變數上最自然(a 或 c);鏈的結果要直接傳給下一個呼叫、沒有變數可以標註時,就用 turbofish(b)。`,
      walkthrough: {
        label: "⊕ All three forms in one program — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let v = vec![1, 2, 3];", note: "來源資料。" },
          { code: "    let a: Vec<i32> = v.iter().map(|x| x + 1).collect();", note: "寫法一:從變數標註反推 collect 的目標型別是 Vec<i32>。" },
          { code: "    let b = v.iter().map(|x| x + 1).collect::<Vec<i32>>();", note: "寫法二:turbofish 直接把泛型參數寫在呼叫上,b 的型別跟著確定。" },
          { code: "    let c: Vec<_> = v.iter().map(|x| x + 1).collect();", note: "寫法三:只指定容器是 Vec,元素型別 i32 由 map 的閉包推斷出來。" },
          { code: "    println!(\"{:?} {:?} {:?}\", a, b, c);", note: "印出 [2, 3, 4] [2, 3, 4] [2, 3, 4]。三者產生的程式碼完全一樣。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "turbofish 這個名字來自 ::<> 的長相(像一條魚)。它不是 collect 專屬語法,任何泛型函式或方法都能這樣明確指定泛型參數,例如第 10 題的 parse::<i32>()。",
      },
      csharp: `C# 的 ToList()、ToArray()、ToDictionary() 是不同名字的方法,目標型別寫在方法名裡,所以不需要額外標註。Rust 只有一個 collect,目標型別由泛型參數 B 決定——好處是任何人都能為自己的型別實作 FromIterator,讓 collect 直接收成它,不用等標準函式庫加一個 ToMyType()。代價是你偶爾得告訴編譯器 B 是誰,下一題就是忘了告訴它的樣子。`,
    },
    {
      id: "2-2-09",
      question: "collect() 不知道要收成什麼。以下程式碼的結果是?",
      questionCode: "fn main() {\n    let v = vec![1, 2, 3];\n    let doubled = v.iter().map(|x| x * 2).collect();\n    println!(\"{:?}\", doubled);\n}",
      options: [
        { text: "編譯錯誤:type annotations needed——collect 可以收成許多種型別,這裡沒有任何線索說明要哪一種" },
        { text: "印出 [2, 4, 6]——collect 沒有指定時預設收成 Vec" },
        { text: "印出 [2, 4, 6]——println! 的 {:?} 需要 Debug,編譯器從這裡推斷出 Vec<i32>" },
        { text: "編譯錯誤:map 的閉包沒有標註參數型別,編譯器無法推斷 x 的型別" },
      ],
      answer: 0,
      explanation: `collect 的目標型別 B 只有一個要求:實作 FromIterator<i32>。Vec<i32>、VecDeque<i32>、HashSet<i32> 都符合,而 let doubled 沒標型別、之後也只被拿去 {:?} 印出——這些型別全部實作了 Debug,線索不足以選出唯一答案,編譯器只好報 type annotations needed,並附註 the type must implement FromIterator<i32>。
「預設收成 Vec」是這題要打破的誤解:collect 的泛型參數沒有「預設值」可以退回,推不出來就是錯誤。「從 {:?} 推斷」的想法方向對了,但 Debug 這個條件太寬,無法縮小到單一型別。至於閉包參數:map 的閉包參數型別由 v.iter() 交出的元素型別(&i32)決定,推得很清楚,問題不在這裡。修法就是第 8 題的任一種寫法。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let v = vec![1, 2, 3];", note: "來源資料。" },
            { code: "    let doubled = v.iter().map(|x| x * 2).collect();", note: "⛔ 編譯失敗:type annotations needed(E0283),附註 the type must implement FromIterator<i32>。collect 能收成的型別不只一種,而 doubled 沒有標型別。" },
            { code: "    println!(\"{:?}\", doubled);", note: "這裡只要求 Debug,幾乎每個集合型別都符合,幫不上忙。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "錯誤訊息裡出現的 FromIterator 就是 collect 的 bound 本身——collect 內部會呼叫 B::from_iter(self),所以 B 必須先確定下來。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let v = vec![1, 2, 3];", note: "完全不用改。" },
            { code: "    let doubled: Vec<i32> = v.iter().map(|x| x * 2).collect();", note: "改動處:在變數上標註 Vec<i32>,collect 的目標型別因此確定。寫 Vec<_> 或改用 collect::<Vec<i32>>() 也可以。" },
            { code: "    println!(\"{:?}\", doubled);", note: "印出 [2, 4, 6]。" },
            { code: "}", note: "main 結束。" },
          ],
        },
      ],
      csharp: `C# 的 ToList() 不會有這個問題,因為方法名本身就決定了結果是 List<T>;var 推斷永遠從右往左。Rust 的推斷兩個方向都走:可以從左邊的標註往右推,但左邊沒寫、右邊又有多種可能時,編譯器寧可報錯也不猜。這個設計讓「同一條鏈收成 HashSet 就自動去重」這種寫法成為可能,代價是這個錯誤訊息你會很常看到。`,
    },
    {
      id: "2-2-10",
      question: "collect 成 Result<Vec<T>, E>。以下程式碼的輸出是?",
      questionCode: "fn main() {\n    let inputs = vec![\"1\", \"2\", \"x\", \"4\"];\n    let parsed: Result<Vec<i32>, _> = inputs\n        .iter()\n        .map(|s| {\n            print!(\"p{} \", s);\n            s.parse::<i32>()\n        })\n        .collect();\n    println!(\"{:?}\", parsed);\n}",
      options: [
        { text: "p1 p2 px Err(ParseIntError { kind: InvalidDigit })——遇到第一個 Err 就停止拉取,整體結果就是那個 Err,4 根本不會被解析" },
        { text: "p1 p2 px p4 Ok([1, 2, 4])——解析失敗的元素被略過,其餘成功的照樣收集" },
        { text: "p1 p2 px p4 Err(ParseIntError { kind: InvalidDigit })——所有元素都會先解析完,最後再判斷有沒有錯誤" },
        { text: "編譯錯誤:map 產生的元素是 Result<i32, _>,collect 只能收成 Vec<Result<i32, _>>,不能把 Result 翻到外層" },
      ],
      answer: 0,
      explanation: `Result<Vec<T>, E> 本身實作了 FromIterator<Result<T, E>>,語意是「全部成功才給 Ok(Vec),碰到第一個 Err 就停下來、整體回傳那個 Err」。停下來是真的停:collect 不再呼叫 next(),所以 map 的閉包不會對 4 執行,輸出裡沒有 p4。
「略過失敗、收集其餘」是另一種需求,要用 filter_map(|s| s.parse().ok()) 才會得到 [1, 2, 4];collect 成 Result 的語意是「要嘛全對、要嘛告訴我哪裡錯」。「先全部解析完再判斷」忽略了第 5 題的逐元素拉取模型——提前停止正是這種寫法的價值。「只能收成 Vec<Result>」也不對:兩種都能收,差別完全由你標註的目標型別決定,這正是 collect 用泛型 B 的威力。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let inputs = vec![\"1\", \"2\", \"x\", \"4\"];", note: "第三個元素不是合法數字。" },
          { code: "    let parsed: Result<Vec<i32>, _> = inputs", note: "目標型別是 Result<Vec<i32>, _>,錯誤型別交給推斷(會是 ParseIntError)。這個標註決定了 collect 的收集語意。" },
          { code: "        .iter()", note: "借用走訪,交出 &&str。" },
          { code: "        .map(|s| {", note: "每個元素轉換成一個 Result<i32, ParseIntError>。" },
          { code: "            print!(\"p{} \", s);", note: "記錄哪些元素真的被解析了。" },
          { code: "            s.parse::<i32>()", note: "turbofish 指定要解析成 i32,回傳 Result。" },
          { code: "        })", note: "map 結束。" },
          { code: "        .collect();", note: "逐一拉取:1、2 解析成功先暫存;x 回傳 Err,collect 立刻停止,不再要 4。依序印出 p1 p2 px。" },
          { code: "    println!(\"{:?}\", parsed);", note: "印出 Err(ParseIntError { kind: InvalidDigit })。已經解析好的 1、2 被丟棄。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "同樣的技巧也適用於 Option:collect 成 Option<Vec<T>> 時,碰到第一個 None 就整體回傳 None。要收成 HashMap 則是讓元素是 (key, value) 的 tuple,再標註 HashMap<K, V>。",
      },
      csharp: `LINQ 沒有原生的「短路收集 Result」:C# 通常是在 Select 裡用 int.Parse 丟例外,靠例外中斷整條查詢;或用 TryParse 配 out 變數,手動處理失敗。Rust 沒有例外,錯誤就是一個普通的值,所以「一串 Result 能不能翻成一個 Result」變成型別層級的問題——標準函式庫為 Result 實作好了這個翻轉,你只要改目標型別就能在「全部收集」與「遇錯就停」之間切換。`,
    },
    {
      id: "2-2-11",
      question: "用 fold 把字串接起來。以下程式碼的結果是?",
      questionCode: "fn main() {\n    let words = vec![\"ab\", \"cd\", \"ef\"];\n    let joined = words.iter().fold(String::new(), |acc, w| acc.push_str(w));\n    println!(\"{}\", joined);\n}",
      options: [
        { text: "編譯錯誤:mismatched types, expected String, found ()——fold 的累加閉包必須回傳「下一輪的累加值」,push_str 是就地修改、回傳 ()" },
        { text: "印出 abcdef——push_str 修改了 acc,fold 會自動把修改後的 acc 帶到下一輪" },
        { text: "編譯錯誤:fold 的初始值型別必須跟元素型別相同,元素是 &&str,初始值卻是 String" },
        { text: "編譯錯誤:acc 沒有宣告成 mut,所以不能呼叫 push_str;加上 mut 就能印出 abcdef" },
      ],
      answer: 0,
      explanation: `fold(init, f) 的規則是:累加值從 init 開始,每個元素呼叫一次 f(acc, 元素),f 的「回傳值」成為下一輪的 acc。所以 f 的回傳型別必須跟 init 相同——這裡 init 是 String,閉包本體卻是 acc.push_str(w),而 push_str 就地修改、回傳 (),編譯器報 expected String, found (),還貼心附註 this call modifies acc in-place。
「fold 會自動把修改後的 acc 帶到下一輪」是最容易掉進去的誤解:fold 只認回傳值,不看你對參數做了什麼。「初始值要跟元素同型別」也不對,fold 的累加型別完全獨立於元素型別,這正是它比 sum 更通用的地方。至於 mut:確實也少了 mut,但就算補上 |mut acc, w| acc.push_str(w),回傳值仍然是 (),錯誤不會消失——必須在本體最後把 acc 交回去。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let words = vec![\"ab\", \"cd\", \"ef\"];", note: "三個字串切片。" },
            { code: "    let joined = words.iter().fold(String::new(), |acc, w| acc.push_str(w));", note: "⛔ 編譯失敗:mismatched types, expected String, found ()。初始值決定了累加型別是 String,但閉包的回傳值是 push_str 的回傳值 ()。編譯器的附註是 this call modifies acc in-place。" },
            { code: "    println!(\"{}\", joined);", note: "永遠到不了這裡。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let words = vec![\"ab\", \"cd\", \"ef\"];", note: "完全不用改。" },
            { code: "    let joined = words.iter().fold(String::new(), |mut acc, w| {", note: "改動處一:參數寫成 mut acc。acc 是按值傳進來的 String,要就地修改就得把這個綁定宣告成可變。" },
            { code: "        acc.push_str(w);", note: "就地把字串接上去,回傳的 () 直接丟掉。" },
            { code: "        acc", note: "改動處二:把 acc 本身當作回傳值交出去,它就是下一輪的累加值。型別是 String,和初始值一致。" },
            { code: "    });", note: "fold 結束,回傳最後一輪的 acc。" },
            { code: "    println!(\"{}\", joined);", note: "印出 abcdef。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "累加值是按值在每一輪之間傳遞的:上一輪把 String 交出去,下一輪接手,沒有任何複製。如果只是要把字串接起來,words.concat() 或 words.join(\"\") 更直接;fold 的價值在於累加型別可以是任何東西。",
        },
      ],
      csharp: `C# 的 Aggregate 是同一個概念:words.Aggregate(new StringBuilder(), (sb, w) => sb.Append(w))。這行在 C# 能跑,是因為 StringBuilder.Append 剛好回傳 this——換成回傳 void 的方法(例如 List<T>.Add)就一樣是編譯錯誤。Rust 的 push_str 設計成回傳 (),所以這個誤會在第一次寫的時候就會被抓出來,而不是依賴「那個方法剛好有回傳自己」。`,
    },
    {
      id: "2-2-12",
      question: "any() 與 all() 的短路。以下程式碼的輸出是?",
      questionCode: "fn main() {\n    let v = vec![1, 3, 4, 5, 7];\n    let found = v.iter().any(|&x| {\n        print!(\"{} \", x);\n        x % 2 == 0\n    });\n    println!(\"-> {}\", found);\n    let ok = v.iter().all(|&x| {\n        print!(\"{} \", x);\n        x < 4\n    });\n    println!(\"-> {}\", ok);\n}",
      options: [
        { text: "兩行都是 1 3 4,結果分別是 true 與 false——any 碰到第一個 true 就停,all 碰到第一個 false 就停" },
        { text: "兩行都是 1 3 4 5 7,結果分別是 true 與 false——any 與 all 一定要看過每個元素才能下結論" },
        { text: "第一行 1 3 4 -> true,第二行 1 3 4 5 7 -> false——只有 any 會短路,all 必須檢查全部" },
        { text: "第一行 1 3 4 -> true,第二行什麼都沒印就是 -> false——any 已經把迭代器走到 4 了,all 接著從 5 開始" },
      ],
      answer: 0,
      explanation: `any 在找「有沒有任何一個成立」,一旦碰到成立的元素,答案就確定是 true,不必再往下看;all 在確認「是否全部成立」,一旦碰到不成立的元素,答案就確定是 false。兩者都只呼叫到答案確定為止,所以兩行都只印到 4。
「一定要看過每個元素」和「只有 any 會短路」都是低估了這兩個方法:短路是它們的定義,而且因為迭代器是逐元素拉取的,短路代表後面的元素連產生都不必。最後一個干擾選項誤以為兩次呼叫共用同一個迭代器——其實每次 v.iter() 都建立一個全新的迭代器,從頭開始。如果真的把同一個迭代器先給 any 再給 all(用 by_ref() 之類的方式),all 才會從 5 接著走。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let v = vec![1, 3, 4, 5, 7];", note: "只有 4 是偶數,也只有 1、3 小於 4。" },
          { code: "    let found = v.iter().any(|&x| {", note: "any 的閉包收到 &i32(不像 filter 多借一層),模式 &x 讓 x 是 i32。" },
          { code: "        print!(\"{} \", x);", note: "記錄閉包實際看了哪些元素。" },
          { code: "        x % 2 == 0", note: "1、3 回傳 false,4 回傳 true——any 在這裡停止。" },
          { code: "    });", note: "any 回傳 true,5 與 7 從未被取出。" },
          { code: "    println!(\"-> {}\", found);", note: "第一行完整輸出:1 3 4 -> true。" },
          { code: "    let ok = v.iter().all(|&x| {", note: "重新呼叫 v.iter(),建立一個從頭開始的新迭代器。" },
          { code: "        print!(\"{} \", x);", note: "同樣記錄看過的元素。" },
          { code: "        x < 4", note: "1、3 成立,4 不成立——all 在這裡停止。" },
          { code: "    });", note: "all 回傳 false。" },
          { code: "    println!(\"-> {}\", ok);", note: "第二行完整輸出:1 3 4 -> false。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "同一家族的短路消耗器還有 find、position、try_fold 等。對一個空的迭代器,any 回傳 false、all 回傳 true——這是邏輯上的「空真」,不是特例。",
      },
      csharp: `LINQ 的 Any(predicate) 與 All(predicate) 行為完全相同,也是短路、空集合分別回傳 false 與 true。C# 開發者在這題應該有正確直覺;值得帶走的是 Rust 的 iter() 每次呼叫都是新的游標,這點對應的是 C# 每次 foreach 都重新呼叫 GetEnumerator()——下一個反面案例(第 14 題)就是忘了這件事的樣子。`,
    },
    {
      id: "2-2-13",
      question: "find() 的回傳值拿來當索引。以下程式碼的結果是?",
      questionCode: "fn main() {\n    let v = vec![10, 20, 30, 40];\n    let i = v.iter().find(|&&x| x > 15);\n    println!(\"{}\", v[i]);\n}",
      options: [
        { text: "編譯錯誤:the type [{integer}] cannot be indexed by Option<&{integer}>——find 回傳的是「找到的元素」包在 Option 裡,不是索引;要索引應該用 position" },
        { text: "印出 20——find 回傳第一個符合條件的索引 1,v[1] 是 20" },
        { text: "印出 20——Option 會自動解開,v[Some(1)] 等同 v[1]" },
        { text: "執行期 panic:find 回傳的是元素值 20,拿 20 當索引超出範圍" },
      ],
      answer: 0,
      explanation: `find 回傳 Option<&T>——「找到的那個元素本身的參考」,沒找到則是 None。這裡 i 的型別是 Option<&i32>,拿它去索引 Vec,編譯器找不到對應的索引規則,報 the type [{integer}] cannot be indexed by Option<&{integer}>,並提示 slice indices are of type usize or ranges of usize。要「第一個符合條件的位置」應該用 position,它回傳 Option<usize>。
「find 回傳索引」就是把 find 與 position 搞混,這題正是要拆開兩者。「Option 會自動解開」不存在:Rust 從不隱式解開 Option,這是它與 null 最大的不同。「拿 20 當索引所以 panic」的想像方向對了一半(find 確實回傳元素),但連型別都不對,根本走不到執行期——型別系統在這裡直接擋住了一個原本會是執行期越界的 bug。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let v = vec![10, 20, 30, 40];", note: "來源資料。" },
            { code: "    let i = v.iter().find(|&&x| x > 15);", note: "find 的閉包跟 filter 一樣多借一層,所以寫 &&x。回傳 Some(&20)——型別是 Option<&i32>,裡面裝的是元素的參考,不是位置。" },
            { code: "    println!(\"{}\", v[i]);", note: "⛔ 編譯失敗:the type [{integer}] cannot be indexed by Option<&{integer}>。附註指出 slice indices are of type usize,並說明 Vec 並沒有實作以 Option<&{integer}> 為索引的 Index。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let v = vec![10, 20, 30, 40];", note: "完全不用改。" },
            { code: "    let first = v.iter().find(|&&x| x > 15);", note: "要的是元素本身時用 find,結果是 Some(&20)。" },
            { code: "    println!(\"{:?}\", first);", note: "印出 Some(20)。{:?} 會把參考透明地印成它指向的值。" },
            { code: "    if let Some(i) = v.iter().position(|&x| x > 15) {", note: "改動處:要的是位置時用 position,回傳 Option<usize>;用 if let 解開,處理「找不到」的情況。注意 position 的閉包只收一層參考,寫 &x。" },
            { code: "        println!(\"{} {}\", i, v[i]);", note: "i 是 usize,可以合法索引,印出 1 20。" },
            { code: "    }", note: "找不到時整個區塊略過,不會 panic。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "實務上,拿到位置再去索引往往是多繞一圈:大多數情況直接用 find 拿元素就好,只有真的需要位置(例如之後要 remove(i) 或 swap)時才用 position。",
        },
        {
          label: "△ Desugared (what the compiler sees)",
          intro: "錯誤訊息的附註提到了 Index,但你的程式碼裡只寫了中括號。因為 v[i] 是一層糖:",
          lines: [
            { code: "println!(\"{}\", v[i]);", note: "你寫的這一行。" },
            { code: "", note: "" },
            { code: "// 編譯器把 v[i] 展開成:", note: "以下才是編譯器實際檢查的東西。" },
            { code: "*Index::index(&v, i)", note: "中括號是 Index trait 的 index 方法,再解參考取出值。錯誤訊息裡的 Index 就是從這裡露出來的。" },
            { code: "", note: "" },
            { code: "// Vec<T> 只為這類 I 實作了 Index<I>:", note: "Vec 的 Index 實作把工作轉交給 slice,條件是 I 必須實作 SliceIndex。" },
            { code: "// usize、Range<usize>、RangeFrom<usize> ……", note: "也就是單一位置或一段範圍。Option<&i32> 不在清單上,所以 Index::index(&v, i) 找不到可用的實作。" },
          ],
          outro: "看到 cannot be indexed by 某型別時,問題永遠是「中括號裡的型別不在 Index 的實作清單上」。對 Vec 與 slice 而言,清單裡只有 usize 與各種 usize 範圍。",
        },
      ],
      csharp: `C# 同樣有這組容易混淆的方法:List<T>.Find 回傳元素、FindIndex 回傳索引;LINQ 的 FirstOrDefault 回傳元素。差別在失敗模式:C# 若誤用,常見的結果是拿到預設值 0 或 null 後在別處出錯,而 int 當索引也能編譯,錯誤延後到執行期;Rust 的 find 回傳 Option<&T>,型別上就不可能被當成索引,也不可能忘了處理「找不到」。`,
    },
    {
      id: "2-2-14",
      question: "同一個迭代器用兩次。以下程式碼的結果是?",
      questionCode: "fn main() {\n    let v = vec![1, 2, 3, 4, 5];\n    let odds = v.iter().filter(|&&x| x % 2 == 1);\n    let total: i32 = odds.sum();\n    let n = odds.count();\n    println!(\"{} {}\", total, n);\n}",
      options: [
        { text: "編譯錯誤:use of moved value: odds——sum 按值接收迭代器並把它走完,odds 在那一行之後就不存在了" },
        { text: "印出 9 3——odds 像集合一樣,可以反覆走訪" },
        { text: "印出 9 0——sum 已經把迭代器走到底,count 從尾端開始數,所以得到 0" },
        { text: "印出 9 3,但 filter 的閉包會被執行 10 次,因為每個消耗器都會重新計算一次" },
      ],
      answer: 0,
      explanation: `迭代器不是集合,而是一個「走到哪裡」的一次性狀態機。消耗器的簽名是 fn sum(self)、fn count(self)——按值接收迭代器、把它走完、最後丟掉。所以 odds.sum() 之後 odds 已經被移走,編譯器報 use of moved value: odds,附註 sum takes ownership of the receiver self, which moves odds。
「可以反覆走訪」是把迭代器當成集合;要重複走訪,應該保留集合(v)並在需要時各自呼叫一次 v.iter()。「count 從尾端開始數得到 0」描述的是 C# 共用同一個 IEnumerator 的行為,但在 Rust 這種狀態根本無法寫出來,因為所有權已經轉移。至於「重新計算」:如果真的想各跑一次,就得建兩條鏈,那時閉包的確會執行 10 次——但這是你明確寫出來的成本,不是隱藏的行為。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let v = vec![1, 2, 3, 4, 5];", note: "集合本身。" },
            { code: "    let odds = v.iter().filter(|&&x| x % 2 == 1);", note: "odds 是一個惰性的 Filter 迭代器,它不是集合,不能「重新開始」。" },
            { code: "    let total: i32 = odds.sum();", note: "sum 按值接收 odds,把它走完得到 9。odds 的所有權在這裡被移走。" },
            { code: "    let n = odds.count();", note: "⛔ 編譯失敗:use of moved value: odds。附註:sum takes ownership of the receiver self, which moves odds——上一行已經把它用掉了。" },
            { code: "    println!(\"{} {}\", total, n);", note: "永遠到不了這裡。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let v = vec![1, 2, 3, 4, 5];", note: "集合才是可以反覆走訪的東西,把它留著。" },
            { code: "    let total: i32 = v.iter().filter(|&&x| x % 2 == 1).sum();", note: "改動處:第一條鏈從 v.iter() 建立,由 sum 消耗掉,得到 9。" },
            { code: "    let n = v.iter().filter(|&&x| x % 2 == 1).count();", note: "改動處:需要第二次走訪時,從集合重新建立一條新的鏈,由 count 消耗掉,得到 3。" },
            { code: "    println!(\"{} {}\", total, n);", note: "印出 9 3。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "其他做法:如果這個迭代器實作了 Clone(這裡的 Filter 就有),可以先 odds.clone().sum() 再 odds.count();或者只走一次,用 fold 同時累加總和與個數。選哪個取決於「重新走一次」和「多存一份狀態」哪個比較便宜。",
        },
      ],
      csharp: `C# 的 IEnumerable 大多可以重複列舉:同一個 LINQ 查詢變數先 Sum() 再 Count() 是合法的,只是會重新執行整條查詢(分析器會警告 possible multiple enumeration),而如果來源是只能讀一次的串流,第二次就會拿到空結果或例外。Rust 把這兩種情況在型別上分開:集合可以反覆 iter(),迭代器則是一次性的,用掉就被移走,「重複列舉」在編譯期就會被看見。`,
    },
    {
      id: "2-2-15",
      question: "一邊走訪一邊修改原集合。以下程式碼的結果是?",
      questionCode: "fn main() {\n    let mut v = vec![1, 2, 3];\n    for x in v.iter().filter(|&&x| x > 1) {\n        v.push(x * 10);\n    }\n    println!(\"{:?}\", v);\n}",
      options: [
        { text: "編譯錯誤:cannot borrow v as mutable because it is also borrowed as immutable——filter 包起來的迭代器仍然借用著 v,整個迴圈期間都不能修改它" },
        { text: "印出 [1, 2, 3, 20, 30]——filter 產生的是新的迭代器,已經跟 v 無關了" },
        { text: "無窮迴圈:push 進去的 20、30 也大於 1,會再被走訪到" },
        { text: "執行期 panic:在走訪期間修改集合,就像 C# 的 Collection was modified" },
      ],
      answer: 0,
      explanation: `v.iter() 建立的迭代器持有 v 的不可變借用,filter 把它包起來,借用也跟著被包進去——轉接器不會「切斷」和來源的關係,它只是在原迭代器外面加一層。這個借用要活到 for 迴圈結束,而迴圈本體的 v.push 需要可變借用,兩者重疊,編譯器報 cannot borrow v as mutable because it is also borrowed as immutable。
「filter 產生的新迭代器跟 v 無關」是這題的核心誤解。另外兩個干擾選項描述的都是其他語言在執行期才會發生的後果:無窮迴圈或 Collection was modified 例外。在 Rust 這兩種事情都不會發生,因為程式根本編譯不過——更根本的原因是 push 可能讓 Vec 重新配置記憶體,迭代器手上的指標會變成懸空指標,借用檢查器擋的就是這個。修法是把「讀」與「寫」分成兩個階段:先把要新增的東西收集起來,借用結束後再一次寫回去。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let mut v = vec![1, 2, 3];", note: "可變的 Vec。" },
            { code: "    for x in v.iter().filter(|&&x| x > 1) {", note: "v.iter() 對 v 建立不可變借用,filter 把它包起來,這份借用要一直活到迴圈結束。每一輪 x 是 &i32。" },
            { code: "        v.push(x * 10);", note: "⛔ 編譯失敗:cannot borrow v as mutable because it is also borrowed as immutable。push 需要 &mut v,但外層迭代器的不可變借用還活著;而且 push 可能搬移 Vec 的緩衝區,讓迭代器指向已釋放的記憶體。" },
            { code: "    }", note: "借用本來要到這裡才結束。" },
            { code: "    println!(\"{:?}\", v);", note: "永遠到不了這裡。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let mut v = vec![1, 2, 3];", note: "完全不用改。" },
            { code: "    let extra: Vec<i32> = v.iter().filter(|&&x| x > 1).map(|&x| x * 10).collect();", note: "改動處一:讀取階段。把要新增的值收進一個獨立的 Vec,collect 結束時對 v 的借用也跟著結束,extra 是 [20, 30]。" },
            { code: "    v.extend(extra);", note: "改動處二:寫入階段。此時沒有任何借用在活著,可以合法地可變借用 v,把 extra 的元素全部接到尾端。" },
            { code: "    println!(\"{:?}\", v);", note: "印出 [1, 2, 3, 20, 30]。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "「先收集、後寫回」是這類問題的標準解法。如果只是要依條件就地修改元素的值(不改變長度),用 iter_mut() 就可以在一次走訪中完成;要依條件刪除元素則用 retain。",
        },
      ],
      csharp: `同樣的邏輯在 C# 能編譯:foreach (var x in list.Where(x => x > 1)) list.Add(x * 10); 執行到第一次 Add 之後,下一次 MoveNext 丟出 InvalidOperationException: Collection was modified。這個檢查靠的是 List<T> 內部的版本號,只有在執行到那一行才會發現。Rust 的借用檢查器把同一條規則搬到編譯期,而且不需要任何執行期的版本號欄位。`,
    },
    {
      id: "2-2-16",
      question: "自訂迭代器能不能直接串轉接器?以下程式碼的輸出是?",
      questionCode: "struct Fib {\n    a: u64,\n    b: u64,\n}\n\nimpl Iterator for Fib {\n    type Item = u64;\n    fn next(&mut self) -> Option<u64> {\n        let r = self.a;\n        self.a = self.b;\n        self.b += r;\n        Some(r)\n    }\n}\n\nfn main() {\n    let v: Vec<u64> = Fib { a: 0, b: 1 }.filter(|x| x % 2 == 0).take(3).collect();\n    println!(\"{:?}\", v);\n}",
      options: [
        { text: "印出 [0, 2, 8]——只實作 next() 的 Fib 自動擁有 filter、take、collect;即使 Fib 永遠不回傳 None,take(3) 拿夠三個就停止拉取" },
        { text: "編譯錯誤:Fib 沒有實作 filter 與 take,自訂迭代器要自己寫這些方法才能呼叫" },
        { text: "程式卡住不會結束:Fib 永遠回傳 Some,filter 會嘗試先把整個無窮序列過濾完" },
        { text: "印出 [0, 2]——take(3) 先從 Fib 取前三個 0、1、1,再交給 filter 過濾" },
      ],
      answer: 0,
      explanation: `這題驗證本課的核心設計:Iterator trait 有幾十個預設方法,全部建立在 next() 之上。Fib 只寫了 next(),filter、take、collect 就自動可用,而且和標準函式庫的迭代器一樣是惰性、逐元素拉取。
Fib 是一個無窮迭代器——永遠回傳 Some。它之所以能安全地被 collect,是因為 take(3) 在交出第三個元素後就回傳 None,collect 也就停止向上游要東西。filter 逐一看到 0(偶數,保留)、1、1、2(保留)、3、5、8(保留),take 拿滿三個就結束,後面的數字從未被計算。
「要自己寫 filter 與 take」是從「介面要把所有成員實作出來」的直覺帶過來的誤解。「filter 會先過濾整個無窮序列」忽略了逐元素拉取。「take 先取前三個再過濾」則是把鏈的順序看反了:寫在前面的轉接器離來源比較近,filter 在 take 的上游,所以 take 計算的是「通過 filter 的元素」。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "struct Fib {", note: "費氏數列的狀態:目前這一項與下一項。" },
          { code: "    a: u64,", note: "即將交出的那一項。" },
          { code: "    b: u64,", note: "再下一項。" },
          { code: "}", note: "定義結束。" },
          { code: "", note: "" },
          { code: "impl Iterator for Fib {", note: "只實作 Iterator 的必要部分。" },
          { code: "    type Item = u64;", note: "每次交出一個 u64。" },
          { code: "    fn next(&mut self) -> Option<u64> {", note: "唯一必須實作的方法。" },
          { code: "        let r = self.a;", note: "記下要交出的這一項。" },
          { code: "        self.a = self.b;", note: "往前推一項。" },
          { code: "        self.b += r;", note: "算出新的下一項(舊的 a 加舊的 b)。" },
          { code: "        Some(r)", note: "永遠回傳 Some——這是一個刻意設計的無窮迭代器。" },
          { code: "    }", note: "方法結束。" },
          { code: "}", note: "impl 結束。沒有寫 filter、take、collect,但它們已經全部可用。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let v: Vec<u64> = Fib { a: 0, b: 1 }.filter(|x| x % 2 == 0).take(3).collect();", note: "filter 的閉包收到 &u64,x % 2 對參考也能運算。filter 在 take 的上游:依序看到 0、1、1、2、3、5、8,保留 0、2、8;take 拿滿三個就回傳 None,collect 隨之停止,13 之後的數字從未被算出來。" },
          { code: "    println!(\"{:?}\", v);", note: "印出 [0, 2, 8]。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "「無窮迭代器 + take」是常見而且安全的組合,前提是下游有東西負責喊停。下一題看的是另一面:你以為序列會結束,但 next() 沒有回傳 None。",
      },
      csharp: `C# 的對應做法是寫一個 yield return 的方法,或實作 IEnumerable<T>/IEnumerator<T>,之後所有 LINQ 方法都能用——這點兩邊一樣,因為 LINQ 是掛在 IEnumerable<T> 上的擴充方法。差別在機制:LINQ 靠擴充方法加上介面呼叫(虛擬分派),Rust 靠 trait 預設方法加上單態化,Fib.filter(...).take(3) 在編譯後會被展開成專門為 Fib 產生的具體程式碼。`,
    },
    {
      id: "2-2-17",
      question: "自訂迭代器的結束條件。以下程式碼執行時會發生什麼事?",
      questionCode: "struct Countdown {\n    n: u32,\n}\n\nimpl Iterator for Countdown {\n    type Item = u32;\n    fn next(&mut self) -> Option<u32> {\n        if self.n > 0 {\n            self.n -= 1;\n        }\n        Some(self.n)\n    }\n}\n\nfn main() {\n    let v: Vec<u32> = Countdown { n: 3 }.collect();\n    println!(\"{:?}\", v);\n}",
      options: [
        { text: "程式永遠不會結束:next() 從來不回傳 None,collect 會不停地把 0 收進 Vec,直到記憶體耗盡被終止" },
        { text: "印出 [2, 1, 0]——collect 偵測到 next() 連續回傳相同的值,就判定序列結束" },
        { text: "編譯錯誤:next() 的所有路徑都回傳 Some,編譯器會警告這個迭代器永遠不會結束並拒絕編譯" },
        { text: "印出 [2, 1, 0, 0]——n 變成 0 之後再呼叫一次 next() 就會自動回傳 None" },
      ],
      answer: 0,
      explanation: `迭代器唯一的結束訊號是 None。這份 next() 在 n 降到 0 之後,每次都回傳 Some(0)——對 collect 來說,這就是「還有元素」,於是它會一直呼叫、一直 push,Vec 越長越大,直到記憶體耗盡、程式被作業系統終止。這段程式碼能順利編譯,錯的是行為。
「collect 偵測重複值就停止」不存在,重複值完全是合法的序列內容(想想一個全是 0 的資料流)。「編譯器會拒絕永遠回傳 Some 的迭代器」也不對——無窮迭代器是合法而且有用的設計(上一題的 Fib 就是),編譯器無從分辨你是故意的還是忘了。「n 變成 0 之後自動回傳 None」則是把「數值歸零」跟「序列結束」混為一談:Option 不會因為值是 0 就變成 None。`,
      walkthrough: [
        {
          label: "X Question code (never terminates)",
          lines: [
            { code: "struct Countdown {", note: "倒數計時器。" },
            { code: "    n: u32,", note: "剩餘的數字。" },
            { code: "}", note: "定義結束。" },
            { code: "", note: "" },
            { code: "impl Iterator for Countdown {", note: "實作 Iterator。" },
            { code: "    type Item = u32;", note: "元素型別。" },
            { code: "    fn next(&mut self) -> Option<u32> {", note: "每次呼叫交出一個數字。" },
            { code: "        if self.n > 0 {", note: "只負責「不要讓 n 減到負數」,卻沒有處理「序列該結束了」。" },
            { code: "            self.n -= 1;", note: "n 依序變成 2、1、0,之後停在 0。" },
            { code: "        }", note: "if 區塊結束。" },
            { code: "        Some(self.n)", note: "⛔ 行為錯誤的起因:不管 n 是多少都回傳 Some。n 停在 0 之後,這一行會永遠回傳 Some(0),序列沒有終點。" },
            { code: "    }", note: "方法結束。" },
            { code: "}", note: "定義結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let v: Vec<u32> = Countdown { n: 3 }.collect();", note: "⛔ collect 會一直呼叫 next() 直到拿到 None——而 None 永遠不會來。Vec 無限增長,最終記憶體耗盡。如果改成 .take(6).collect() 來觀察,會得到 [2, 1, 0, 0, 0, 0]。" },
            { code: "    println!(\"{:?}\", v);", note: "永遠到不了這裡。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "struct Countdown {", note: "結構不變。" },
            { code: "    n: u32,", note: "剩餘的數字。" },
            { code: "}", note: "定義結束。" },
            { code: "", note: "" },
            { code: "impl Iterator for Countdown {", note: "實作 Iterator。" },
            { code: "    type Item = u32;", note: "元素型別。" },
            { code: "    fn next(&mut self) -> Option<u32> {", note: "每次呼叫交出一個數字,或宣告結束。" },
            { code: "        if self.n == 0 {", note: "改動處:先檢查結束條件。" },
            { code: "            return None;", note: "改動處:沒有數字可交時回傳 None——這是 collect、for 迴圈等消耗器唯一認得的停止訊號。" },
            { code: "        }", note: "if 區塊結束。" },
            { code: "        self.n -= 1;", note: "推進狀態。" },
            { code: "        Some(self.n)", note: "交出 2、1、0。" },
            { code: "    }", note: "方法結束。" },
            { code: "}", note: "定義結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let v: Vec<u32> = Countdown { n: 3 }.collect();", note: "第四次呼叫 next() 時拿到 None,collect 正常結束。" },
            { code: "    println!(\"{:?}\", v);", note: "印出 [2, 1, 0]。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "寫 next() 時的習慣:先寫「什麼時候回傳 None」,再寫推進邏輯。如果迭代器是刻意設計成無窮的,就讓呼叫端用 take、take_while 或 find 這類會喊停的方法消耗它。",
        },
      ],
      csharp: `C# 手寫 MoveNext() 忘了回傳 false,或者 yield return 的方法裡少了 yield break 的條件,後果一模一樣:ToList() 卡住並吃光記憶體。這是兩邊風險相同的少數情況——型別系統只能保證 next() 回傳的是 Option,無法保證它「最終」會回傳 None。Rust 的 Option 讓結束訊號至少是明確的,不會因為 Current 的值剛好是 0 或 null 而被誤讀。`,
    },
    {
      id: "2-2-18",
      question: "設計選擇:下面兩個函式計算「偶數的平方和」,在開啟最佳化(--release)編譯時,關於效能正確的說法是?",
      questionCode: "fn sum_loop(v: &[i32]) -> i32 {\n    let mut total = 0;\n    for &x in v {\n        if x % 2 == 0 {\n            total += x * x;\n        }\n    }\n    total\n}\n\nfn sum_chain(v: &[i32]) -> i32 {\n    v.iter().filter(|&&x| x % 2 == 0).map(|&x| x * x).sum()\n}",
      options: [
        { text: "兩者通常會被編譯成相同等級的機器碼:每個轉接器都是具體型別、每個閉包都是可內聯的具體函式,鏈式呼叫最佳化後就是一個迴圈,沒有中間集合也沒有 heap 配置" },
        { text: "手寫迴圈明顯較快:鏈式寫法的 filter 與 map 各自產生一個中間 Vec,要多配置兩次記憶體" },
        { text: "手寫迴圈明顯較快:每個閉包都是 heap 上的物件,每處理一個元素都要經過一次虛擬呼叫" },
        { text: "鏈式寫法明顯較快:iter() 會自動把工作分散到多個執行緒平行處理" },
      ],
      answer: 0,
      explanation: `這就是 Rust 所謂的零成本抽象(zero-cost abstraction):你不用的東西不必付錢,你用的東西,手寫也不會寫得更好。v.iter().filter(...).map(...) 的型別是 Map<Filter<Iter<i32>, 閉包1>, 閉包2>——每一層都是編譯期已知的具體型別,閉包是各自獨立的匿名型別(上一課的內容),呼叫都是靜態分派。最佳化器把這些 next() 層層內聯之後,剩下的就是一個迴圈加一個 if,和 sum_loop 幾乎一樣,有時甚至更好(slice 迭代器能省掉索引的邊界檢查)。
「每個轉接器產生中間 Vec」是把惰性鏈想成 eager 的集合操作,第 2、5 題已經證明鏈中間沒有任何集合。「閉包是 heap 物件加虛擬呼叫」是 C# 委派的模型,Rust 只有在你明確寫出 Box<dyn Fn> 時才會那樣。「自動平行化」不存在:標準函式庫的 iter() 永遠是單執行緒循序執行,要平行得借助第三方函式庫(例如 rayon 的 par_iter)。
所以選擇的依據應該是可讀性而不是效能迷思:條件與轉換單純時用鏈,需要複雜的提前跳出、多個可變狀態互相牽動時,手寫迴圈往往更清楚。`,
      walkthrough: {
        label: "⊕ Both versions in one program — walkthrough (runs successfully)",
        lines: [
          { code: "fn sum_loop(v: &[i32]) -> i32 {", note: "手寫迴圈版本,接收一個 slice 的借用。" },
          { code: "    let mut total = 0;", note: "累加器。" },
          { code: "    for &x in v {", note: "借用走訪 slice,模式 &x 直接取出 i32。" },
          { code: "        if x % 2 == 0 {", note: "只處理偶數。" },
          { code: "            total += x * x;", note: "累加平方。" },
          { code: "        }", note: "if 區塊結束。" },
          { code: "    }", note: "迴圈結束。" },
          { code: "    total", note: "回傳總和。" },
          { code: "}", note: "定義結束。" },
          { code: "", note: "" },
          { code: "fn sum_chain(v: &[i32]) -> i32 {", note: "鏈式版本,簽名完全相同。" },
          { code: "    v.iter().filter(|&&x| x % 2 == 0).map(|&x| x * x).sum()", note: "型別是 Map<Filter<Iter<i32>, 閉包>, 閉包>,全是編譯期已知的具體型別;最佳化後內聯成一個迴圈,結構與上面的手寫版本相同。" },
          { code: "}", note: "定義結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let v = vec![1, 2, 3, 4, 5, 6];", note: "偶數是 2、4、6,平方和是 4 + 16 + 36。" },
          { code: "    assert_eq!(sum_loop(&v), sum_chain(&v));", note: "兩種寫法的結果必然相同;&v 讓 &Vec<i32> 自動轉成 &[i32] 傳進去。" },
          { code: "    println!(\"{}\", sum_chain(&v));", note: "印出 56。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "想親眼確認的話,可以把兩個函式貼到 Compiler Explorer(godbolt)並加上 -O,比對產生的組合語言。這種「抽象不帶執行期成本」的保證,正是 Rust 敢把迭代器鏈當成預設寫法的原因。",
      },
      csharp: `C# 的直覺在這題會帶錯方向:LINQ 在熱點路徑上確實比手寫 for 迴圈慢,原因是每個 Where/Select 都是透過 IEnumerable<T> 介面的虛擬呼叫、每個 lambda 是一個委派物件(捕獲變數時還要配置閉包物件),JIT 很難把它們內聯攤平。所以 C# 社群有「熱點路徑避開 LINQ」的經驗法則。這條法則在 Rust 不成立,因為那些成本的來源——介面分派與委派物件——在 Rust 的迭代器鏈裡根本不存在。`,
    },
    {
      id: "2-2-19",
      question: "設計選擇:要把一批名字轉成大寫。下面兩個函式都能編譯。關於該選哪一個,正確的說法是?",
      questionCode: "fn shout_borrowed(names: &[String]) -> Vec<String> {\n    names.iter().map(|s| s.to_uppercase()).collect()\n}\n\nfn shout_owned(names: Vec<String>) -> Vec<String> {\n    names\n        .into_iter()\n        .map(|mut s| {\n            s.make_ascii_uppercase();\n            s\n        })\n        .collect()\n}",
      options: [
        { text: "看呼叫端之後還需不需要原資料:還要用就選 iter() 版,代價是為每個名字配置新字串;原資料之後用不到就選 into_iter() 版,直接接管每個 String 就地修改,不必額外配置" },
        { text: "永遠選 iter() 版:借用比較安全,into_iter() 會把資料吃掉,容易造成 use of moved value" },
        { text: "永遠選 into_iter() 版:它一定比較快,iter() 版只是給初學者用的寫法" },
        { text: "兩者完全等價:iter() 也會把 String 交出來,into_iter() 只是比較長的寫法" },
      ],
      answer: 0,
      explanation: `iter() 與 into_iter() 不是安全與危險的差別,而是「這份資料之後歸誰」的差別。shout_borrowed 只借用,呼叫端保留原本的 Vec;代價是閉包拿到的是 &String,無法修改原字串,只能用 to_uppercase() 各自配置一個新 String。shout_owned 接管整個 Vec,into_iter() 交出每個 String 的所有權,閉包可以就地改寫,最後 collect 甚至能重用原本的緩衝區——沒有任何新字串被配置。
「永遠選 iter() 因為比較安全」是這題最常見的誤解:借用版在原資料用不到時是白白多付配置成本,而 use of moved value 是編譯期錯誤,不會變成執行期 bug,沒什麼好怕的。「永遠選 into_iter()」則忽略了呼叫端可能還要用原資料——那時硬選它,呼叫端就得先 clone 一份整個 Vec,反而更貴。「兩者等價」混淆了元素型別:iter() 交出的是 &String,不是 String。
判斷口訣:函式簽名收 &[T] 表示「我只看」,收 Vec<T> 表示「給我,我會處理掉」。選哪一個由呼叫端的需求決定,不是由安全感決定。`,
      walkthrough: {
        label: "⊕ Both versions in one program — walkthrough (runs successfully)",
        lines: [
          { code: "fn shout_borrowed(names: &[String]) -> Vec<String> {", note: "借用版:只要一份唯讀借用,呼叫端保有原資料。" },
          { code: "    names.iter().map(|s| s.to_uppercase()).collect()", note: "s 是 &String,不能改原字串;to_uppercase() 為每個名字配置一個新的 String。" },
          { code: "}", note: "定義結束。" },
          { code: "", note: "" },
          { code: "fn shout_owned(names: Vec<String>) -> Vec<String> {", note: "接管版:呼叫端把整個 Vec 交出來。" },
          { code: "    names", note: "names 的所有權已經在這個函式裡。" },
          { code: "        .into_iter()", note: "按值走訪,每個元素以 String 本身交出。" },
          { code: "        .map(|mut s| {", note: "閉包擁有 s,宣告成 mut 就能就地修改。" },
          { code: "            s.make_ascii_uppercase();", note: "直接改寫原本的緩衝區,沒有新的配置(只處理 ASCII 字母,對這個範例足夠)。" },
          { code: "            s", note: "把改好的 String 交給下游。" },
          { code: "        })", note: "map 結束。" },
          { code: "        .collect()", note: "收成新的 Vec<String>;裡面的字串就是原本那幾個,只是被改寫過。" },
          { code: "}", note: "定義結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let names = vec![String::from(\"ann\"), String::from(\"bob\")];", note: "原資料。" },
          { code: "    let loud = shout_borrowed(&names);", note: "之後還要印 names,所以選借用版。" },
          { code: "    println!(\"{:?} {:?}\", names, loud);", note: "印出 [\"ann\", \"bob\"] [\"ANN\", \"BOB\"]——原資料完好。" },
          { code: "    let loud2 = shout_owned(names);", note: "names 之後不再需要,交給接管版,省下所有配置。從這一行起 names 已被移走。" },
          { code: "    println!(\"{:?}\", loud2);", note: "印出 [\"ANN\", \"BOB\"]。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "兩個版本各有正確的使用時機,同一支程式裡並存也很正常。反過來看,如果一個函式收 Vec<T> 卻只是讀取,那是在逼呼叫端交出(或 clone)不必要的所有權——這種簽名才是真正該避免的。",
      },
      csharp: `C# 沒有這個選擇:foreach 一律「借用式」走訪,而 string 是不可變的,ToUpper() 永遠配置新字串,原本的字串要等 GC 回收。Rust 讓你在簽名上表達「我只看」或「給我」,呼叫端與被呼叫端在編譯期就對所有權達成共識——對 C# 開發者來說,這是一個全新的設計維度,也是 Rust 在不犧牲安全的前提下省掉大量配置的來源。`,
    },
    {
      id: "2-2-20",
      question: "總結:LINQ 與 Rust 迭代器都是延遲執行的鏈式 API。以下關於兩者差異的說法,哪一個正確?",
      questionCode: "// C#\nvar total = orders.Where(x => x >= 100).Select(x => x * 2).Sum();\n\n// Rust\nlet total: i32 = orders.iter().filter(|&&x| x >= 100).map(|&x| x * 2).sum();",
      options: [
        { text: "兩者都是延遲執行、逐元素拉取;差別在於 LINQ 的每一步都是透過介面與委派的動態呼叫,Rust 的每一步都是編譯期已知的具體型別,單態化並內聯後等同手寫迴圈,而且借用檢查器會在編譯期擋下走訪期間修改集合之類的錯誤" },
        { text: "LINQ 是延遲執行,Rust 的迭代器則是每個轉接器立即執行並回傳新的集合,所以 Rust 必須在最後呼叫 collect" },
        { text: "兩者在效能與安全保證上完全相同,只是語法不同:filter 對應 Where、map 對應 Select" },
        { text: "Rust 的迭代器比 LINQ 慢,因為每個元素都要經過所有權檢查,這些檢查是在執行期進行的" },
      ],
      answer: 0,
      explanation: `兩者的相同點是真的:都是延遲執行,都是元素一個一個流過整條管線,都靠一個「要下一個」的操作(MoveNext 與 next)驅動,Where/Select/Sum 與 filter/map/sum 幾乎一一對應。差別在底層機制與保證:LINQ 的鏈是 IEnumerable<T> 介面的串接,每一步經過虛擬呼叫、每個 lambda 是委派物件;Rust 的鏈是一層包一層的具體型別,編譯器能完整內聯,結果就是一個迴圈。安全面上,C# 在執行期才會發現的「走訪時修改集合」與「重複列舉只能讀一次的來源」,Rust 在編譯期就擋下了。
「Rust 的轉接器立即執行並回傳集合」直接違反第 2 題看到的行為;collect 只是其中一種消耗器,sum、count、any 都能結束一條鏈。「完全相同只是語法不同」抹掉了本課大半的內容——iter/iter_mut/into_iter 的三分法、消耗器按值取用、轉接器保留借用,這些 LINQ 都沒有。「所有權檢查在執行期進行」則是對 Rust 最常見的誤解之一:借用檢查完全發生在編譯期,產生的機器碼裡沒有任何對應的檢查。`,
      walkthrough: [
        {
          label: "◇ C# equivalent: LINQ with deferred execution",
          lang: "csharp",
          lines: [
            { code: "var orders = new List<int> { 120, 45, 300, 80 };", note: "來源資料。" },
            { code: "var big = orders.Where(x => x >= 100).Select(x => x * 2);", note: "延遲執行:這一行只建立查詢物件。Where 與 Select 各自是一個實作 IEnumerable<int> 的物件,兩個 lambda 各是一個委派。" },
            { code: "var total = big.Sum();", note: "Sum 驅動列舉:每個元素經過兩次介面呼叫(MoveNext / Current)與兩次委派呼叫。結果是 840。" },
            { code: "Console.WriteLine(total);", note: "印出 840。如果在 Sum 之前對 orders 呼叫 Add,這行程式照樣能編譯,結果會把新元素算進去。" },
          ],
          outro: "LINQ 的彈性來自執行期:任何 IEnumerable<T> 都能串,代價是介面分派與委派呼叫,以及「查詢建立之後來源還能被改」這種要靠紀律避免的狀況。",
        },
        {
          label: "√ Rust: the same pipeline, resolved at compile time",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let orders = vec![120, 45, 300, 80];", note: "來源資料。" },
            { code: "    let big = orders.iter().filter(|&&x| x >= 100).map(|&x| x * 2);", note: "一樣是延遲執行,但 big 的型別是 Map<Filter<Iter<i32>, 閉包>, 閉包>——具體、在 stack 上、沒有任何 heap 配置。而且它持有 orders 的借用,在它被消耗之前,任何修改 orders 的程式碼都無法通過編譯。" },
            { code: "    let total: i32 = big.sum();", note: "sum 按值取用 big 並驅動整條鏈;最佳化後等同一個 for 迴圈加一個 if。big 從此被移走,不能再用。" },
            { code: "    println!(\"{}\", total);", note: "印出 840(120 與 300 各乘以 2 再相加)。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "帶著走的對照表:LINQ = 介面 + 委派 + 可重複列舉 + 執行期才發現的修改衝突;Rust 迭代器 = 具體型別 + 靜態分派 + 一次性消耗 + 編譯期擋下的借用衝突。想要 C# 那種型別擦除的彈性,Rust 也買得到(Box<dyn Iterator<Item = T>>),但那是明確選擇付出的成本,不是預設值。",
        },
      ],
      csharp: `給 C# 開發者的三個帶著走的結論。一、LINQ 的心智模型幾乎可以直接搬過來:延遲執行、逐元素拉取、短路的 Any/All/First,在 Rust 都成立。二、要多想一件 LINQ 從來不讓你想的事:元素是借來的(iter)、可改的(iter_mut)、還是拿走的(into_iter)——這個選擇決定了原集合之後還在不在,也決定了能不能省掉複製。三、「熱點路徑避開 LINQ」的經驗法則在 Rust 要丟掉:迭代器鏈就是預設寫法,它的效能和手寫迴圈同級,可讀性通常更好。`,
    },
  ],
};
