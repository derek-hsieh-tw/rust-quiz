/* 出題慣例見專案根目錄 AUTHORING.md:
 *   - answer 一律為 0(正確答案寫在第一個選項),顯示順序由 quiz.js 依題目 id 洗牌
 *   - 詳解禁止用「選項 A/B/C」字母指涉,必須直接描述選項內容
 *   - 有程式碼的題目一律附 walkthrough(逐行說明);反面案例必須同時附上 √ 正確寫法 */
window.RUST_LESSONS = window.RUST_LESSONS || {};
window.RUST_LESSONS["lesson2-3"] = {
  id: "lesson2-3",
  title: "智慧指標與內部可變性",
  goal: "分清楚 Box(獨佔 + heap)、Rc(多重擁有)、RefCell(執行期借用檢查)各自解決什麼問題,看懂 Rc<RefCell<T>> 這個組合的動機,以及 Weak 如何打破循環參考。",
  primer: {
    intro: "智慧指標是「擁有一份資料、並且在自己被釋放時負責收尾」的結構,最常見的三個是 Box、Rc 和 RefCell。基礎課學到的所有權規則是「一份資料只有一個擁有者、可變存取同一時間只有一份」,大多數程式這樣就夠用;但遇到遞迴型別、多個地方要共同持有同一份資料、或是在只有共享參考時仍然需要修改,就要請智慧指標出場。它們不是繞過規則,而是把規則換一種方式執行——有的換成參考計數,有的把借用檢查從編譯期挪到執行期。",
    examples: [
      {
        code: "fn main() {\n    let boxed: Box<String> = Box::new(String::from(\"on the heap\"));\n    let owner = boxed;\n    println!(\"{}\", owner);\n}",
        note: "Box::new 把值放到 heap 上,Box 本身跟一般的值一樣可以移動所有權;印出 on the heap。",
      },
      {
        code: "use std::rc::Rc;\n\nfn main() {\n    let first = Rc::new(String::from(\"config\"));\n    let second = Rc::clone(&first);\n    println!(\"{} {}\", first, second);\n}",
        note: "Rc::new 建立一個可被多方持有的值,Rc::clone 讓 second 也成為擁有者;印出 config config。",
      },
    ],
    csharp: "C# 的每個 class 物件天生就在 heap 上、可以被任意多個變數參考、也可以隨時透過任何一個參考修改,GC 替你追蹤它何時能回收。Rust 把這三種能力拆成三個型別,讓你只為真正需要的那一種付出成本。",
  },
  questions: [
    {
      id: "2-3-01",
      question: "Box<T> 的基本行為。以下程式碼的結果是?",
      questionCode: "fn main() {\n    let b = Box::new(5);\n    let sum = *b + 1;\n    println!(\"{}\", sum);\n}",
      options: [
        { text: "印出 6——Box::new 把 5 放到 heap,*b 透過解參考取出裡面的值;b 在 main 結束時自動釋放那塊 heap 記憶體,不需要任何手動動作" },
        { text: "編譯錯誤:Box 配置的 heap 記憶體必須在離開作用域前呼叫 drop(b) 或類似的 free 手動釋放" },
        { text: "編譯錯誤:Box<i32> 不能用 * 解參考,要改用 b.get() 之類的方法才能取出裡面的值" },
        { text: "印出 6,但程式結束時會記憶體洩漏,因為沒有人釋放 Box 指向的 heap 空間" },
      ],
      answer: 0,
      explanation: `Box<T> 是最單純的智慧指標:它在 heap 上配置一塊剛好放得下 T 的空間,把值搬進去,自己則是 stack 上的一根指標。它仍然遵守所有權規則——b 是那塊 heap 空間唯一的擁有者,b 離開作用域時,編譯器自動插入釋放的程式碼(先 drop 裡面的值,再釋放 heap 空間)。
「必須手動 drop 或 free」是 C/C++ 的 malloc/free 直覺:Rust 裡 drop(b) 可以寫,但只是「提早」釋放,不寫也不會洩漏。至於 *b,Box 實作了解參考,* 就能取出裡面的 i32;這跟基礎課對 &i32 用 * 是同一個運算子。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let b = Box::new(5);", note: "在 heap 上配置一個 i32 大小的空間並把 5 搬進去;b 本身是 stack 上的一根指標,而且是那塊 heap 空間唯一的擁有者。" },
          { code: "    let sum = *b + 1;", note: "*b 解參考,取出 heap 上的 5(i32 是 Copy,所以是複製出來,b 仍然有效)。sum 是 6。" },
          { code: "    println!(\"{}\", sum);", note: "印出 6。" },
          { code: "}", note: "sum 與 b 離開作用域。b 被 drop 時自動釋放它指向的 heap 空間——這就是「智慧」指標的意思:收尾工作綁在所有權上。" },
        ],
        outro: "把 5 放進 Box 在實務上沒有意義(i32 本來就很小,放 stack 更快),這題只是看清楚 Box 的生命週期。Box 真正派得上用場的地方是下一題的遞迴型別。",
      },
      csharp: `C# 裡所有 class 物件本來就在 heap 上,你不需要也沒辦法「把東西包一層放上 heap」;int 這種值型別被強迫放上 heap 叫做 boxing,而且釋放時機交給 GC。Rust 的 Box 名字相近但意義不同:它是明確的、獨佔的 heap 配置,釋放時機是確定的——擁有者離開作用域的那一刻,不是 GC 哪天有空。`,
    },
    {
      id: "2-3-02",
      question: "遞迴型別。以下程式碼的結果是?",
      questionCode: "enum List {\n    Cons(i32, List),\n    Nil,\n}\n\nfn main() {\n    let _list = List::Nil;\n}",
      options: [
        { text: "編譯錯誤:recursive type List has infinite size——enum 的大小要在編譯期算出,而 Cons 直接內嵌一個 List,算式會無限展開" },
        { text: "可以編譯:因為有 Nil 這個不含資料的變體,遞迴一定會在 Nil 結束,大小是有限的" },
        { text: "可以編譯:編譯器會自動把遞迴的欄位改存成指標,就像 C# 的 class 一樣" },
        { text: "編譯錯誤:enum 的變體不能包含跟自己同型別的欄位,Rust 完全不支援遞迴資料結構" },
      ],
      answer: 0,
      explanation: `enum 的大小 = 最大那個變體的大小(再加上標記)。Cons(i32, List) 的大小是 i32 加上一個 List,而那個 List 又可能是 Cons,裡面又有一個 List……編譯器算不出終點,所以報 recursive type has infinite size。
「有 Nil 就會結束」是最常見的誤解:Nil 決定的是「某一個值」執行期會在哪裡結束,但型別大小是編譯期要替「所有可能的值」決定的——一個 List 變數必須預留足以放下任何長度串列的空間,那是無限大。另一個誤解是「Rust 不支援遞迴結構」:支援,只是你要自己加一層間接,讓欄位存的是固定大小的指標而不是整個值,錯誤訊息的 help 也直接提示了 Box、Rc 或 &。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "enum List {", note: "⛔ 編譯失敗:recursive type List has infinite size(E0072)。錯誤標在型別定義上,不是在使用處。" },
            { code: "    Cons(i32, List),", note: "問題出在這裡:List 直接內嵌一個 List。算大小時 List = i32 + List = i32 + i32 + List = ……永遠算不完。" },
            { code: "    Nil,", note: "Nil 不帶資料,但它只影響「某個值」在哪裡結束,救不了「型別」的大小計算。" },
            { code: "}", note: "List 的定義結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let _list = List::Nil;", note: "就算只用 Nil,型別本身已經不合法,整個程式都不會編譯。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "錯誤訊息的 help 會寫 insert some indirection (e.g., a Box, Rc, or &) to break the cycle——「間接」就是改存指標:不管指向的東西多大,指標本身的大小是固定的。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "#[derive(Debug)]", note: "讓 List 可以用 {:?} 印出來,方便觀察結構。" },
            { code: "enum List {", note: "定義遞迴串列。" },
            { code: "    Cons(i32, Box<List>),", note: "改動處:下一個節點改成 Box<List>。Box 在 stack 上只是一根指標(固定大小),實際的 List 放在 heap,大小計算在這裡終止。" },
            { code: "    Nil,", note: "串列的結尾。" },
            { code: "}", note: "現在 List 的大小 = max(i32 + 一根指標, 0) + 標記,是個有限的常數。" },
            { code: "", note: "" },
            { code: "use List::{Cons, Nil};", note: "把兩個變體名稱引入作用域,少寫 List:: 前綴。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let list = Cons(1, Box::new(Cons(2, Box::new(Nil))));", note: "每個節點的尾巴用 Box::new 包起來放到 heap 上。list 擁有第一個節點,第一個節點的 Box 擁有第二個,一路串下去——整條鏈的所有權是單一路徑。" },
            { code: "    println!(\"{:?}\", list);", note: "印出 Cons(1, Cons(2, Nil))——Box 的 Debug 輸出直接印內容,不會印出指標。" },
            { code: "}", note: "list 被 drop 時,Box 一層一層往下釋放整條串列。" },
          ],
        },
      ],
      csharp: `C# 寫 class Node { int Value; Node Next; } 天經地義,因為 class 是參考型別,Next 欄位存的本來就是一根參考,大小固定。但如果你把它改成 struct Node { int Value; Node Next; },C# 一樣會報錯(struct member causes a cycle in the struct layout)——這正是 Rust 的情況:Rust 的 enum/struct 預設是「值直接內嵌」,就像 C# 的 struct。Box 就是讓你在需要時顯式選擇「這個欄位改存參考」。`,
    },
    {
      id: "2-3-03",
      question: "透過 Box 呼叫方法。以下程式碼的結果是?",
      questionCode: "enum List {\n    Cons(i32, Box<List>),\n    Nil,\n}\n\nuse List::{Cons, Nil};\n\nimpl List {\n    fn sum(&self) -> i32 {\n        match self {\n            Cons(value, rest) => value + rest.sum(),\n            Nil => 0,\n        }\n    }\n}\n\nfn main() {\n    let list = Cons(1, Box::new(Cons(2, Box::new(Cons(3, Box::new(Nil))))));\n    println!(\"{}\", list.sum());\n}",
      options: [
        { text: "印出 6——rest 的型別是 &Box<List>,呼叫 rest.sum() 時編譯器自動解參考穿過 Box,找到 List 的 sum 方法" },
        { text: "編譯錯誤:rest 是 Box<List> 不是 List,必須寫成 (*rest).sum() 或 (**rest).sum() 才能呼叫 List 的方法" },
        { text: "編譯錯誤:Box<List> 沒有 sum 方法;要先用 rest.as_ref() 把 Box 轉成 &List 才能呼叫" },
        { text: "印出 1——match 只比對了最外層的節點,rest.sum() 呼叫的是 Box 自己的方法,回傳 0" },
      ],
      answer: 0,
      explanation: `方法呼叫的點號會自動解參考:編譯器拿 rest(型別 &Box<List>)去找 sum,找不到就解一層變成 Box<List>,還是找不到就再解一層變成 List,找到了 fn sum(&self),再自動補上 & 呼叫。所以透過 Box 呼叫內部型別的方法,寫起來跟直接呼叫一模一樣。遞迴 1 + (2 + (3 + 0)) = 6。
「必須手寫 (*rest).sum()」不算錯到無法編譯——那樣寫也能跑,但完全不必要,這是從 C/C++ 的 ptr->method() 與 (*ptr).method() 帶過來的習慣。「要先 as_ref()」也一樣能跑但多餘。Box 本身沒有叫 sum 的方法,所以也不會發生「呼叫到 Box 自己的方法」這種事。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "enum List {", note: "上一題修好的遞迴串列。" },
          { code: "    Cons(i32, Box<List>),", note: "尾巴存成 Box<List>,大小固定。" },
          { code: "    Nil,", note: "結尾。" },
          { code: "}", note: "List 的定義結束。" },
          { code: "", note: "" },
          { code: "use List::{Cons, Nil};", note: "引入兩個變體名稱。" },
          { code: "", note: "" },
          { code: "impl List {", note: "替 List 本身實作方法——注意是 List,不是 Box<List>。" },
          { code: "    fn sum(&self) -> i32 {", note: "接收 &List。" },
          { code: "        match self {", note: "self 是 &List,match 參考時綁定出來的欄位也都是參考。" },
          { code: "            Cons(value, rest) => value + rest.sum(),", note: "value 是 &i32、rest 是 &Box<List>。rest.sum():編譯器依序嘗試 &Box<List>、Box<List>、List,在 List 上找到 sum,自動插入解參考後呼叫。&i32 + i32 也有實作,結果是 i32。" },
          { code: "            Nil => 0,", note: "遞迴的終點。" },
          { code: "        }", note: "match 結束。" },
          { code: "    }", note: "sum 結束。" },
          { code: "}", note: "List 的方法區塊結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let list = Cons(1, Box::new(Cons(2, Box::new(Cons(3, Box::new(Nil))))));", note: "建立 1 → 2 → 3 → Nil 的串列,每個尾巴都在 heap 上。" },
          { code: "    println!(\"{}\", list.sum());", note: "1 + (2 + (3 + 0)),印出 6。" },
          { code: "}", note: "list 被 drop,整條串列由外往內逐層釋放。" },
        ],
        outro: "這個「點號會自動穿過指標」的能力不是 Box 專屬的魔法,而是 Box 實作了 Deref trait 帶來的;第 16 題會看到自訂型別沒實作它時會發生什麼事。",
      },
      csharp: `C# 開發者對這題不會有感覺:node.Next.Sum() 本來就不需要任何解參考語法,因為 C# 的參考型別在語法上就是「直接用」。Rust 讓指標在呼叫方法時也能「直接用」,效果一樣;差別是 Rust 在需要的時候(例如取出 Copy 的值、或對整個值賦值)仍然看得到 * 這個動作,指標與值的區別沒有被語言藏起來。`,
    },
    {
      id: "2-3-04",
      question: "Drop 的呼叫時機與順序。以下程式碼印出什麼?",
      questionCode: "struct Noisy(&'static str);\n\nimpl Drop for Noisy {\n    fn drop(&mut self) {\n        println!(\"drop {}\", self.0);\n    }\n}\n\nfn main() {\n    let _a = Noisy(\"a\");\n    let _b = Box::new(Noisy(\"b\"));\n    {\n        let _c = Noisy(\"c\");\n    }\n    println!(\"end\");\n}",
      options: [
        { text: "drop c、end、drop b、drop a——內層區塊結束時 c 先被釋放;main 結束時同一作用域的變數以「宣告的反向順序」釋放,裝在 Box 裡的值也會跟著 Box 一起被 drop" },
        { text: "drop c、end、drop a、drop b——同一作用域的變數依宣告順序釋放,先宣告的先走" },
        { text: "drop c、end、drop a——Box 裡的 Noisy 在 heap 上,Box 被釋放時只收回記憶體,不會呼叫裡面值的 drop" },
        { text: "end、drop c、drop b、drop a——所有 drop 都延後到 main 結束時才統一執行" },
      ],
      answer: 0,
      explanation: `Drop 的時機完全由作用域決定,而且是確定的:值離開作用域的那一刻就呼叫 drop。內層區塊結束時 _c 離開作用域,所以 drop c 最先出現,在 end 之前。main 結束時 _a 與 _b 同時離開作用域,順序是宣告的反向:後宣告的 _b 先走,再來是 _a——這樣後建立的值(可能依賴先建立的值)總是先被清掉。
「Box 只收回記憶體不呼叫 drop」是錯的:Box 被 drop 時會先 drop 它擁有的值(這裡印出 drop b),再釋放 heap 空間。Box 是擁有者,擁有者的責任就是把收尾做完整。「依宣告順序釋放」則是最常見的誤判——想想堆疊:先放進去的最後拿出來。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "struct Noisy(&'static str);", note: "一個只帶名字的 tuple struct,用來觀察釋放時機。" },
          { code: "", note: "" },
          { code: "impl Drop for Noisy {", note: "實作 Drop:值被釋放前,編譯器會自動呼叫這個方法。你不能自己呼叫 x.drop(),要提早釋放得用 drop(x)。" },
          { code: "    fn drop(&mut self) {", note: "drop 拿到 &mut self,可以在這裡做收尾(關檔、釋放鎖、印 log)。" },
          { code: "        println!(\"drop {}\", self.0);", note: "印出被釋放的是誰。" },
          { code: "    }", note: "drop 結束。" },
          { code: "}", note: "Drop for Noisy 的實作結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let _a = Noisy(\"a\");", note: "第一個宣告。名字前加底線只是告訴編譯器「我知道沒用到」,它仍然活到作用域結束(跟單獨一個 _ 不同)。" },
          { code: "    let _b = Box::new(Noisy(\"b\"));", note: "第二個宣告,Noisy 放在 heap 上,由 Box 擁有。" },
          { code: "    {", note: "開一個內層作用域。" },
          { code: "        let _c = Noisy(\"c\");", note: "_c 只活在這對大括號裡。" },
          { code: "    }", note: "內層作用域結束,_c 被 drop,印出 drop c。" },
          { code: "    println!(\"end\");", note: "印出 end。" },
          { code: "}", note: "main 結束,依宣告反向釋放:先 _b(Box 先 drop 裡面的 Noisy,印出 drop b,再釋放 heap 空間),再 _a(印出 drop a)。" },
        ],
        outro: "完整輸出依序是 drop c、end、drop b、drop a,每個一行。補充一點:struct 的「欄位」則是依宣告順序 drop(先寫的欄位先走),跟區域變數的反向順序剛好相反,這兩條規則是分開的。",
      },
      csharp: `C# 的 IDisposable 需要你寫 using 或手動呼叫 Dispose(),忘了就只能等 finalizer——而 finalizer 何時執行、甚至會不會執行,GC 都不保證。Rust 的 Drop 是 using 的強化版:每個值都自動套上,時機是確定的(離開作用域的那一行),順序也是確定的(反向)。檔案、鎖、網路連線在 Rust 裡都靠這個機制釋放,不存在「忘了 Dispose」這類 bug。`,
    },
    {
      id: "2-3-05",
      question: "Rc<T> 的 clone 與參考計數。以下程式碼印出什麼?",
      questionCode: "use std::rc::Rc;\n\nfn main() {\n    let a = Rc::new(String::from(\"shared\"));\n    let b = Rc::clone(&a);\n    {\n        let c = a.clone();\n        println!(\"{}\", Rc::strong_count(&c));\n    }\n    println!(\"{} {}\", Rc::strong_count(&a), Rc::ptr_eq(&a, &b));\n}",
      options: [
        { text: "先印 3,再印 2 true——每次 clone 只把計數加一,三個 Rc 指向同一份 String;c 離開內層作用域時計數減回 2" },
        { text: "先印 1,再印 1 false——clone 複製出一份新的 String,每個 Rc 各自的計數都是 1" },
        { text: "先印 3,再印 3 true——計數只增不減,要等到所有 Rc 都離開作用域才一次歸零" },
        { text: "先印 3,再印 2 false——a.clone() 是深拷貝,只有 Rc::clone(&a) 才是共享" },
      ],
      answer: 0,
      explanation: `Rc(reference counted)讓一份資料有多個擁有者。Rc::new 在 heap 上放資料和一個計數器;每次 clone 不複製 String,只把 strong_count 加一,拿到一根指向同一塊資料的新指標;每個 Rc 被 drop 時計數減一,減到 0 才真的釋放 String。所以內層印 3,c 離開後剩 2;ptr_eq 比較的是兩個 Rc 是否指向同一塊記憶體,答案是 true。
「a.clone() 是深拷貝」是很合理的擔心,因為一般型別的 .clone() 通常就是深拷貝。但對 Rc 呼叫 .clone() 走的是 Rc 自己的 Clone 實作,跟 Rc::clone(&a) 完全相同。社群慣例寫 Rc::clone(&a),正是為了讓讀程式碼的人一眼看出「這裡只是加計數,很便宜」,不必去猜這個 clone 會不會很貴。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::rc::Rc;", note: "Rc 不在 prelude 裡,要自己引入。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let a = Rc::new(String::from(\"shared\"));", note: "在 heap 上配置「計數器 + String」,strong_count = 1。a 是第一個擁有者。" },
          { code: "    let b = Rc::clone(&a);", note: "不複製 String,只把計數加到 2,b 拿到指向同一塊資料的指標。這是慣用寫法,一看就知道是便宜的操作。" },
          { code: "    {", note: "內層作用域。" },
          { code: "        let c = a.clone();", note: "方法呼叫寫法,呼叫的是 Rc 的 Clone 實作,效果跟 Rc::clone(&a) 一模一樣:計數變 3,不複製 String。" },
          { code: "        println!(\"{}\", Rc::strong_count(&c));", note: "印出 3。計數是三個 Rc 共用的,從哪一個查都一樣。" },
          { code: "    }", note: "c 離開作用域被 drop,計數減為 2。String 還有人擁有,所以不釋放。" },
          { code: "    println!(\"{} {}\", Rc::strong_count(&a), Rc::ptr_eq(&a, &b));", note: "印出 2 true。ptr_eq 比的是指標位址,a 和 b 指向同一份 String。" },
          { code: "}", note: "b、a 依序被 drop,計數 2 → 1 → 0,歸零時才釋放 String 和計數器。" },
        ],
        outro: "Rc::clone 的成本是「加一個整數」,跟 String 多長無關。這也是 Rc 的定位:讓多個地方共同擁有一份昂貴的資料,而不必各自複製一份。",
      },
      csharp: `C# 的每個 class 物件本來就像是「隱含的 Rc」:var b = a; 只複製參考,兩個變數指向同一個物件。差別在回收的方式:GC 不數參考,而是定期從根物件做可達性分析;Rc 則是即時計數,最後一個擁有者離開的那一刻就釋放,時機確定,但代價是每次 clone/drop 都要改計數,而且(第 14 題會看到)它處理不了循環參考。`,
    },
    {
      id: "2-3-06",
      question: "想透過 Rc 修改裡面的資料。以下程式碼的結果是?",
      questionCode: "use std::rc::Rc;\n\nfn main() {\n    let a = Rc::new(String::from(\"hi\"));\n    let b = Rc::clone(&a);\n    a.push_str(\"!\");\n    println!(\"{} {}\", a, b);\n}",
      options: [
        { text: "編譯錯誤:cannot borrow data in an Rc as mutable——Rc 只提供共享的唯讀存取,因為同一份資料可能同時有好幾個擁有者" },
        { text: "印出 hi! hi——b 是 clone 出來的獨立複本,修改 a 不影響 b" },
        { text: "印出 hi! hi!——兩個 Rc 共享同一份 String,透過 a 修改後 b 也看得到" },
        { text: "編譯錯誤:a 沒有宣告成 mut;改成 let mut a 就能正常印出 hi! hi!" },
      ],
      answer: 0,
      explanation: `Rc 的承諾是「很多人可以同時擁有同一份資料」。如果其中任何一個擁有者能拿到 &mut,其他擁有者手上的共享存取就會跟它同時存在——這正是借用規則禁止的「可變存取與其他存取並存」。所以 Rc 只給你 &T,不給 &mut T:push_str 需要 &mut String,編譯器找不到合法的方式取得,就報 cannot borrow data in an Rc as mutable。
「改成 let mut a 就好」是最直覺的修法,但沒用:mut 讓你能改 a 這個 Rc 變數本身(例如讓它指向別的 Rc),不代表能改它指向的共享資料。「b 是獨立複本」則是上一題的誤解——Rc::clone 不複製資料。真的需要「多個擁有者都能改」要搭配 RefCell(第 11 題);如果其實只是想要一份自己能改的副本,就明確複製資料本身。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "use std::rc::Rc;", note: "引入 Rc。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let a = Rc::new(String::from(\"hi\"));", note: "a 是第一個擁有者,計數 1。" },
            { code: "    let b = Rc::clone(&a);", note: "b 成為第二個擁有者,計數 2。a 和 b 指向同一個 String。" },
            { code: "    a.push_str(\"!\");", note: "⛔ 編譯失敗:cannot borrow data in an Rc as mutable(E0596)。help 附註:trait DerefMut is required to modify through a dereference, but it is not implemented for Rc<String>。push_str 需要 &mut String,而 Rc 只提供共享的唯讀存取。" },
            { code: "    println!(\"{} {}\", a, b);", note: "如果上一行能通過,b 手上的共享存取就會跟 a 的修改同時存在,借用規則不允許這種情況。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "錯誤訊息的 help 提到 DerefMut,但你的程式碼裡從來沒寫過這個字——見下方去糖區塊。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::rc::Rc;", note: "引入 Rc。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let a = Rc::new(String::from(\"hi\"));", note: "共享的原始資料維持不變。" },
            { code: "    let mut b = (*a).clone();", note: "改動處:*a 解參考到 String,再呼叫 String 的 clone,得到一份獨立的 String(不是 Rc)。b 是它唯一的擁有者,可以宣告成 mut。" },
            { code: "    b.push_str(\"!\");", note: "改動處:修改的是自己的副本,不牽涉任何共享資料,合法。" },
            { code: "    println!(\"{} {}\", a, b);", note: "印出 hi hi!——原始的共享資料沒被動到。" },
            { code: "}", note: "b 釋放自己的 String;a 的計數歸零後釋放共享的 String。" },
          ],
          outro: "這個修法對應「每個人各改各的」這種意圖。如果意圖是「一個人改了,其他擁有者都要看到」,那需要的是內部可變性(RefCell),第 8 題與第 11 題會看到。",
        },
        {
          label: "△ Desugared (what the compiler sees)",
          intro: "錯誤訊息提到 DerefMut,但你只寫了一個方法呼叫。因為點號穿過指標呼叫方法時,編譯器要先把 a 解參考成 String:",
          lines: [
            { code: "a.push_str(\"!\");", note: "你寫的這一行。" },
            { code: "", note: "" },
            { code: "// 編譯器把它展開成:", note: "以下才是編譯器實際檢查的東西。" },
            { code: "String::push_str(&mut *a, \"!\");", note: "push_str 的接收者是 &mut self,所以需要一個 &mut String,也就是 &mut *a。" },
            { code: "String::push_str(DerefMut::deref_mut(&mut a), \"!\");", note: "而「對指標取 &mut *」就是呼叫 DerefMut::deref_mut。Rc 只實作了 Deref(給 &T),刻意沒有實作 DerefMut——錯誤訊息裡的 DerefMut 就是從這裡來的。" },
          ],
        },
      ],
      csharp: `C# 的兩個參考指向同一個物件時,任何一邊都能修改,另一邊立刻看到——這很方便,也是大量 bug 的來源(一個方法「順手」改了傳進來的清單,呼叫端的資料就變了)。Rust 的 Rc 預設就是唯讀共享,想要「共享又可改」必須顯式換成 Rc<RefCell<T>>,讓每個會修改共享資料的地方都在型別上看得出來。`,
    },
    {
      id: "2-3-07",
      question: "Rc::get_mut 可以在計數為 1 時取得 &mut T。以下程式碼的結果是?",
      questionCode: "use std::rc::Rc;\n\nfn main() {\n    let mut a = Rc::new(vec![1, 2]);\n    let b = Rc::clone(&a);\n    Rc::get_mut(&mut a).unwrap().push(3);\n    println!(\"{:?} {:?}\", a, b);\n}",
      options: [
        { text: "可以編譯,但執行時 panic:called Option::unwrap() on a None value——b 還活著,計數是 2,get_mut 無法保證獨佔,所以回傳 None" },
        { text: "印出 [1, 2, 3] [1, 2, 3]——a 宣告成 mut,get_mut 就能拿到可變參考,修改後 b 也看得到" },
        { text: "印出 [1, 2, 3] [1, 2]——get_mut 會自動替 a 複製一份資料再修改" },
        { text: "編譯錯誤:b 還持有共享存取,借用檢查器在編譯期就擋下 get_mut" },
      ],
      answer: 0,
      explanation: `Rc::get_mut 是「計數剛好為 1 時才給 &mut」的安全出口:它在執行期檢查 strong_count(以及 weak_count),只有確定沒有其他擁有者時才回傳 Some(&mut T),否則回傳 None。這裡 b 還活著,計數是 2,拿到 None,unwrap 就 panic。
「編譯期會擋下」是把 Rc 的計數當成借用規則的一部分——但計數是執行期的數字,編譯器不知道你在呼叫 get_mut 時到底有幾個擁有者,所以它只能把答案包成 Option 交給你處理。「會自動複製一份再修改」描述的其實是另一個函式 Rc::make_mut(clone-on-write),這正是修正版要用的。`,
      walkthrough: [
        {
          label: "X Question code (panics at runtime)",
          lines: [
            { code: "use std::rc::Rc;", note: "引入 Rc。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let mut a = Rc::new(vec![1, 2]);", note: "a 宣告成 mut,因為 get_mut 需要 &mut Rc。計數 1。" },
            { code: "    let b = Rc::clone(&a);", note: "計數變 2。從這一刻起,a 不再是唯一的擁有者。" },
            { code: "    Rc::get_mut(&mut a).unwrap().push(3);", note: "⛔ 執行期 panic:called Option::unwrap() on a None value。get_mut 看到計數是 2,回傳 None;unwrap 對 None 直接 panic。編譯器沒有任何警告,因為計數是執行期才知道的。" },
            { code: "    println!(\"{:?} {:?}\", a, b);", note: "永遠執行不到。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "get_mut 的設計本意就是要你處理 None 的情況,用 unwrap 等於假設「一定只有我一個擁有者」,而這個假設在 clone 之後就不成立了。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::rc::Rc;", note: "引入 Rc。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let mut a = Rc::new(vec![1, 2]);", note: "同上。" },
            { code: "    let b = Rc::clone(&a);", note: "同上,計數 2。" },
            { code: "    Rc::make_mut(&mut a).push(3);", note: "改動處:make_mut 在計數為 1 時直接給 &mut;計數大於 1 時先把資料 clone 一份,讓 a 指向新的那份(計數 1),再給 &mut。所以它一定成功,不需要 unwrap。" },
            { code: "    println!(\"{:?} {:?}\", a, b);", note: "印出 [1, 2, 3] [1, 2]。a 已經換成自己的副本,b 仍然指向原本那份。" },
            { code: "}", note: "兩份 Vec 各自在擁有者離開時釋放。" },
          ],
          outro: "另外兩種同樣合法的修法:先 drop(b) 讓計數回到 1 再 get_mut;或是用 if let Some(v) = Rc::get_mut(&mut a) 明確處理拿不到的情況。選哪個取決於你要的語意——make_mut 是「寫入時複製」,drop 是「確定沒人需要了」。",
        },
      ],
      csharp: `C# 沒有任何 API 可以問「這個物件現在有幾個參考指著它」——GC 不維護這個數字。所以 C# 也不存在「只有我一個人持有時才准改」這種機制;只要拿到參考就能改。Rust 的 get_mut 把「獨佔」這個條件變成可以在執行期查詢的事實,make_mut 則是 C# 世界裡要自己手寫的 copy-on-write(例如 ImmutableArray 的 builder 模式)的現成版本。`,
    },
    {
      id: "2-3-08",
      question: "RefCell<T>:內部可變性。以下程式碼的結果是?",
      questionCode: "use std::cell::RefCell;\n\nfn main() {\n    let cell = RefCell::new(vec![1]);\n    {\n        let r1 = cell.borrow();\n        let r2 = cell.borrow();\n        println!(\"{} {}\", r1.len(), r2.len());\n    }\n    cell.borrow_mut().push(2);\n    println!(\"{:?}\", cell.borrow());\n}",
      options: [
        { text: "印出 1 1 與 [1, 2]——borrow() 可以同時有很多個;cell 沒有宣告 mut 也能透過 borrow_mut() 修改,這就是內部可變性" },
        { text: "編譯錯誤:cell 沒有宣告成 mut,不能呼叫會修改內容的 borrow_mut()" },
        { text: "執行期 panic:RefCell 同一時間只允許一個借用,r2 那一行就會失敗" },
        { text: "印出 1 1 與 [1, 2],而且 RefCell 也允許同時持有兩個 borrow_mut(),它的用途就是關掉借用規則" },
      ],
      answer: 0,
      explanation: `RefCell 把借用規則從編譯期搬到執行期,但規則本身一條都沒少:同一時間可以有很多個唯讀借用(borrow() 回傳 Ref<T>),或者只有一個可變借用(borrow_mut() 回傳 RefMut<T>),兩者不能並存。它在內部記一個借用計數,每次 borrow 時檢查、Ref/RefMut 被 drop 時歸還。
關鍵在 borrow_mut 的簽名是 fn borrow_mut(&self)——只需要共享參考就能取得可變存取,所以 cell 不用宣告 mut。這就是「內部可變性」:外表看起來不可變,裡面卻能改。「RefCell 用來關掉借用規則」是最危險的誤解:規則還在,只是違反時不是編譯錯誤,而是執行期 panic(下一題)。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::cell::RefCell;", note: "RefCell 在 std::cell 模組。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let cell = RefCell::new(vec![1]);", note: "沒有 mut。cell 擁有一個 Vec,外加一個執行期的借用計數(目前沒人借)。" },
          { code: "    {", note: "內層作用域,用來控制借用的存活範圍。" },
          { code: "        let r1 = cell.borrow();", note: "取得唯讀借用,型別是 Ref<Vec<i32>>。計數記下「有 1 個唯讀借用」。" },
          { code: "        let r2 = cell.borrow();", note: "再取一個唯讀借用,合法——唯讀借用可以同時存在很多份,跟編譯期規則一樣。" },
          { code: "        println!(\"{} {}\", r1.len(), r2.len());", note: "Ref 實作了 Deref,可以直接呼叫 Vec 的方法。印出 1 1。" },
          { code: "    }", note: "r2、r1 被 drop,Ref 的 Drop 實作把唯讀借用計數歸還,現在又沒人借了。" },
          { code: "    cell.borrow_mut().push(2);", note: "取得可變借用(RefMut),呼叫 push。這個 RefMut 是暫時值,這一行結束時就被 drop,可變借用立刻歸還。整個過程 cell 都不需要是 mut。" },
          { code: "    println!(\"{:?}\", cell.borrow());", note: "再取一次唯讀借用來印,印出 [1, 2]。" },
          { code: "}", note: "cell 離開作用域,Vec 被釋放。" },
        ],
        outro: "為什麼需要這種東西?因為有些情況你手上只有共享參考(例如資料被 Rc 共享,或方法簽名是 &self),但仍然有合理的理由要改內容。RefCell 讓你在這些地方改,代價是檢查從編譯期延後到執行期。",
      },
      csharp: `C# 從來沒有「借用規則」:只要有參考就能讀寫欄位,readonly 欄位擋得住重新賦值,卻擋不住你呼叫 list.Add()。從這個角度看,C# 的每個物件都像是一個「沒有檢查的 RefCell」。Rust 的 RefCell 保留了那份彈性,但每一次借用仍然會被記帳,違反規則時立刻停下來,而不是讓兩段程式碼默默踩到彼此的資料。`,
    },
    {
      id: "2-3-09",
      question: "RefCell 的借用衝突。以下程式碼的結果是?",
      questionCode: "use std::cell::RefCell;\n\nfn main() {\n    let cell = RefCell::new(vec![1, 2]);\n    let first = cell.borrow();\n    cell.borrow_mut().push(3);\n    println!(\"{}\", first[0]);\n}",
      options: [
        { text: "可以編譯,但執行到 borrow_mut() 那一行時 panic(目前版本的訊息是 RefCell already borrowed)——first 這個唯讀借用還活著" },
        { text: "編譯錯誤:cannot borrow as mutable because it is also borrowed as immutable,跟一般的借用衝突一樣在編譯期被擋下" },
        { text: "印出 1——RefCell 的 borrow_mut 會等 first 用完才生效" },
        { text: "印出 1——first 是 push 之前取得的快照,之後的修改不影響它" },
      ],
      answer: 0,
      explanation: `這是本課第一次出現的新錯誤形態:程式編譯得過,執行時才炸。RefCell 在執行期記帳:first 持有一個唯讀借用還沒歸還,borrow_mut 檢查時發現「已經有人在讀」,不能再發出可變借用,於是 panic。訊息在目前的 Rust 版本是 RefCell already borrowed(較舊的版本寫成 already borrowed: BorrowMutError)。
「編譯期就會擋下」是最關鍵的誤解:編譯器看到的只是對 cell 呼叫了兩次 &self 方法,兩個共享參考當然合法,它不知道 borrow 與 borrow_mut 的語意衝突——那個檢查被刻意延後到執行期了。至於「borrow_mut 會等待」,那是鎖的語意;RefCell 不會等,單執行緒裡等待只會變成死結,所以它選擇直接 panic。如果不想 panic,可以改用 try_borrow_mut() 拿到一個 Result 自己處理。`,
      walkthrough: [
        {
          label: "X Question code (panics at runtime)",
          lines: [
            { code: "use std::cell::RefCell;", note: "引入 RefCell。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let cell = RefCell::new(vec![1, 2]);", note: "cell 擁有 Vec,借用計數為 0。" },
            { code: "    let first = cell.borrow();", note: "first 是一個 Ref<Vec<i32>>,它活多久,唯讀借用就記多久。它綁在變數上,會一直活到 main 結束。" },
            { code: "    cell.borrow_mut().push(3);", note: "⛔ 執行期 panic:RefCell already borrowed。要取得可變借用,但 first 的唯讀借用還沒歸還。編譯器不會報錯,因為兩個呼叫在型別上都只需要 &self。" },
            { code: "    println!(\"{}\", first[0]);", note: "正是這一行讓 first 必須活過上一行,把衝突坐實。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "如果 push 真的成功了,Vec 可能重新配置記憶體,first 指向的舊位址就變成懸空指標——這正是借用規則要防止的事。RefCell 用 panic 阻止它發生。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::cell::RefCell;", note: "引入 RefCell。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let cell = RefCell::new(vec![1, 2]);", note: "同上。" },
            { code: "    let first = cell.borrow()[0];", note: "改動處:只把需要的 i32 複製出來(i32 是 Copy)。borrow() 回傳的 Ref 是暫時值,這一行結束就被 drop,唯讀借用立刻歸還。first 現在是普通的 i32,跟 cell 沒有任何借用關係。" },
            { code: "    cell.borrow_mut().push(3);", note: "此時沒有任何人持有借用,可變借用成功,Vec 變成 [1, 2, 3]。" },
            { code: "    println!(\"{} {:?}\", first, cell.borrow());", note: "印出 1 [1, 2, 3]。" },
            { code: "}", note: "cell 被釋放。" },
          ],
          outro: "修法的重點不是換 API,而是縮短借用的存活時間:別把 Ref/RefMut 存在長壽的變數裡,取完需要的東西就讓它消失。下一題專門討論這個習慣。",
        },
      ],
      csharp: `C# 沒有等價的東西:同一個執行緒裡一邊讀一邊改同一個 List 完全合法,最接近的是 foreach 走訪途中 Add 會丟 InvalidOperationException: Collection was modified——那是集合自己實作的版本檢查,只保護走訪這一種情境。RefCell 的檢查是通用的:任何「有人在讀的時候有人要寫」都會被攔下,而且攔在寫入之前,而不是資料已經被改壞之後。`,
    },
    {
      id: "2-3-10",
      question: "設計選擇:下面兩種寫法都想「先算總和,再把總和加到清單尾端」。關於它們的差異,正確的說法是?",
      questionCode: "use std::cell::RefCell;\n\n// 寫法一\nfn append_total_1(log: &RefCell<Vec<i32>>) {\n    let items = log.borrow();\n    let sum: i32 = items.iter().sum();\n    log.borrow_mut().push(sum);\n}\n\n// 寫法二\nfn append_total_2(log: &RefCell<Vec<i32>>) {\n    let sum: i32 = log.borrow().iter().sum();\n    log.borrow_mut().push(sum);\n}",
      options: [
        { text: "寫法一會在執行期 panic,寫法二正常:寫法一的 Ref 存在變數 items 裡,活到函式結束;寫法二的 Ref 是暫時值,算完 sum 那一行就被 drop,借用自動歸還" },
        { text: "兩種都正常:同一個執行緒先讀後寫不算衝突,RefCell 跟 lock 一樣允許同一個執行緒重複進入" },
        { text: "兩種都會 panic:RefCell 的借用要等函式回傳才會歸還,除非先呼叫一個類似 release 的方法手動結束借用" },
        { text: "寫法一會在編譯期被擋下,寫法二可以編譯:借用檢查器看得出 items 的存活範圍跟 borrow_mut 重疊" },
      ],
      answer: 0,
      explanation: `RefCell 的借用什麼時候歸還?答案是 Ref/RefMut 被 drop 的時候——它們的 Drop 實作會把計數還回去,沒有任何 unlock 或 release 方法要呼叫。所以問題就變成「這個 Ref 活多久」:寫法一把它綁在 items 變數上,活到函式結尾,borrow_mut 那一行它還活著,panic;寫法二的 Ref 是運算式中的暫時值,在 let 陳述式結束時就被 drop,下一行的 borrow_mut 暢通無阻。
「跟 lock 一樣可以重入」是 C# 開發者特有的誤解:C# 的 lock(Monitor)允許同一個執行緒重複取得。RefCell 不看是誰在借,只看「目前有沒有未歸還的借用」,同一個執行緒自己跟自己衝突一樣 panic。「編譯期會擋下」也不對——這正是上一題的重點,RefCell 的衝突只在執行期偵測。`,
      walkthrough: [
        {
          label: "X Wrong version (panics at runtime)",
          lines: [
            { code: "use std::cell::RefCell;", note: "引入 RefCell。" },
            { code: "", note: "" },
            { code: "fn append_total_1(log: &RefCell<Vec<i32>>) {", note: "寫法一。參數只要 &RefCell,就能在裡面修改內容。" },
            { code: "    let items = log.borrow();", note: "Ref 綁在變數 items 上,唯讀借用從這裡開始,一直持續到 items 被 drop(函式結尾)。" },
            { code: "    let sum: i32 = items.iter().sum();", note: "透過 items 讀取並加總。" },
            { code: "    log.borrow_mut().push(sum);", note: "⛔ 執行期 panic:RefCell already borrowed。items 還活著,可變借用發不出來。" },
            { code: "}", note: "items 要到這裡才被 drop,太晚了。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let log = RefCell::new(vec![1, 2, 3]);", note: "準備資料。" },
            { code: "    append_total_1(&log);", note: "呼叫寫法一,在函式內部 panic。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "在 push 之前加一行 drop(items); 也能修好——drop 就是「提早讓 Ref 消失」的標準做法,它本身沒有任何特殊的 unlock 語意。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::cell::RefCell;", note: "引入 RefCell。" },
            { code: "", note: "" },
            { code: "fn append_total_2(log: &RefCell<Vec<i32>>) {", note: "寫法二。" },
            { code: "    let sum: i32 = log.borrow().iter().sum();", note: "改動處:Ref 不綁變數,只是運算式中的暫時值。sum 算出來是 i32(跟 Ref 沒有借用關係),這個 let 陳述式結束時暫時的 Ref 就被 drop,唯讀借用歸還。" },
            { code: "    log.borrow_mut().push(sum);", note: "沒有未歸還的借用,可變借用成功。這個 RefMut 同樣是暫時值,這一行結束就歸還。" },
            { code: "}", note: "append_total_2 結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let log = RefCell::new(vec![1, 2, 3]);", note: "準備資料。" },
            { code: "    append_total_2(&log);", note: "總和 6 被加到尾端。" },
            { code: "    println!(\"{:?}\", log.borrow());", note: "印出 [1, 2, 3, 6]。" },
            { code: "}", note: "log 被釋放。" },
          ],
          outro: "習慣:RefCell 的借用盡量用成暫時值或包在小區塊裡,讓它「用完就消失」;需要跨好幾行的時候,用一對大括號畫出借用的範圍,比依賴自己記得在哪裡 drop 可靠。",
        },
      ],
      csharp: `C# 的 lock(obj) { } 與 using (...) { } 都是「區塊結束自動釋放」,這一點跟 Rust 很像;但 C# 的 lock 允許同一執行緒重入,而且你也可以手動呼叫 Monitor.Exit。RefCell 沒有重入、也沒有手動釋放的 API:借用的結束完全由 Ref/RefMut 的 Drop 決定,所以控制借用範圍的唯一工具就是控制值的存活範圍。`,
    },
    {
      id: "2-3-11",
      question: "Rc<RefCell<T>>:多個擁有者都能修改。以下程式碼印出什麼?",
      questionCode: "use std::cell::RefCell;\nuse std::rc::Rc;\n\nfn main() {\n    let shared = Rc::new(RefCell::new(0));\n    let a = Rc::clone(&shared);\n    let b = Rc::clone(&shared);\n    *a.borrow_mut() += 10;\n    *b.borrow_mut() += 5;\n    println!(\"{} {}\", shared.borrow(), Rc::strong_count(&shared));\n}",
      options: [
        { text: "15 3——三個 Rc 指向同一個 RefCell,透過任何一個取得 borrow_mut 修改的都是同一個整數" },
        { text: "0 3——Rc::clone 連同 RefCell 一起複製,a 和 b 改的是各自的副本" },
        { text: "5 3——每次 borrow_mut 都會覆蓋上一個可變借用的結果" },
        { text: "執行期 panic:a 與 b 同時對同一個 RefCell 取得可變借用" },
      ],
      answer: 0,
      explanation: `這是本課的核心組合:Rc 負責「多個擁有者」,RefCell 負責「在共享的情況下仍能修改」。Rc::clone 只加計數,所以 shared、a、b 三個 Rc 指向同一個 RefCell,裡面同一個 i32。*a.borrow_mut() += 10 把它改成 10,*b.borrow_mut() += 5 再改成 15。
「同時取得兩個可變借用會 panic」看起來很合理,但仔細看時間:a.borrow_mut() 回傳的 RefMut 是暫時值,那一行結束就歸還了,輪到 b 時已經沒有人在借,兩次借用是先後而不是同時。「Rc::clone 會連 RefCell 一起複製」是第 5 題的誤解換了個包裝——Rc 裡裝什麼都一樣,clone 永遠只加計數。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::cell::RefCell;", note: "內部可變性。" },
          { code: "use std::rc::Rc;", note: "多重擁有。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let shared = Rc::new(RefCell::new(0));", note: "由內往外讀:一個整數,包進 RefCell 讓它可以在共享狀態下修改,再包進 Rc 讓它可以有多個擁有者。計數 1。" },
          { code: "    let a = Rc::clone(&shared);", note: "計數 2,a 指向同一個 RefCell。" },
          { code: "    let b = Rc::clone(&shared);", note: "計數 3,b 也指向同一個 RefCell。" },
          { code: "    *a.borrow_mut() += 10;", note: "a.borrow_mut():點號先穿過 Rc 找到 RefCell 的 borrow_mut,取得 RefMut<i32>,再用 * 解參考修改。值變成 10。RefMut 是暫時值,這一行結束就歸還。" },
          { code: "    *b.borrow_mut() += 5;", note: "此時沒有未歸還的借用,b 取得可變借用成功。改的是同一個整數,值變成 15。" },
          { code: "    println!(\"{} {}\", shared.borrow(), Rc::strong_count(&shared));", note: "印出 15 3。Ref<i32> 實作了 Display,可以直接用 {} 印。" },
          { code: "}", note: "b、a、shared 依序被 drop,計數歸零,RefCell 與整數一起釋放。" },
        ],
        outro: "兩層包裝各司其職:拿掉 Rc,就只能有一個擁有者;拿掉 RefCell,就只能讀不能改(第 6 題)。兩個都要,才得到「很多人持有、誰都能改」。",
      },
      csharp: `這段程式碼描述的行為,就是 C# 每一個 class 物件的日常:var a = shared; var b = shared; a.Value += 10; b.Value += 5; 之後 shared.Value 就是 15,完全不需要任何包裝。Rust 要組合兩個型別才做得到,是因為它把「多重擁有」和「共享可變」當成兩個需要分別付費、分別檢查的能力;C# 預設兩者全開,檢查全免。`,
    },
    {
      id: "2-3-12",
      question: "Rc 能不能交給另一個執行緒?下面用一個只接受 Send 型別的泛型函式做實驗(不需要真的開執行緒)。結果是?",
      questionCode: "use std::rc::Rc;\n\nfn require_send<T: Send>(_value: T) {}\n\nfn main() {\n    let boxed = Box::new(5);\n    require_send(boxed);\n    let counted = Rc::new(5);\n    require_send(counted);\n}",
      options: [
        { text: "編譯錯誤,而且只錯在 Rc 那一行:Rc<i32> cannot be sent between threads safely——Rc 的計數器用的是非原子的加減,兩個執行緒同時 clone/drop 會把計數算錯" },
        { text: "可以編譯:Rc 跟 Box 一樣只是一根指向 heap 的指標,裡面的 i32 可以跨執行緒,指標當然也可以" },
        { text: "兩行都編譯錯誤:所有放在 heap 上的智慧指標都不能跨執行緒,Box 也不例外" },
        { text: "可以編譯,但把 Rc 真的送進執行緒時會在執行期 panic" },
      ],
      answer: 0,
      explanation: `Send 是一個標記 trait,意思是「這個型別的值可以安全地移交給另一個執行緒」。編譯器會自動推導:Box<i32> 獨佔它的資料,移交之後原執行緒就碰不到了,所以是 Send。Rc 不同:移交一個 Rc 給別的執行緒時,原執行緒可能還握著它的另一個 clone,兩邊會同時修改同一個計數器,而 Rc 的計數是普通的加減(沒有原子指令),可能漏算,導致資料被提早釋放或永遠不釋放。所以標準庫刻意讓 Rc 不實作 Send。
「Rc 跟 Box 一樣只是指標」是這題要打破的直覺:決定能不能跨執行緒的不是「指標長什麼樣」,而是「共享的狀態有沒有被同步保護」。「執行期才 panic」也不對——這是 trait bound,在編譯期就檢查。補充一點:RefCell<i32> 其實是 Send(可以整個移交),但它不是 Sync(不能讓兩個執行緒同時透過參考存取),因為它的借用計數同樣沒有同步保護。完整的多執行緒版本(Arc、Mutex)留到 2-7。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "use std::rc::Rc;", note: "引入 Rc。" },
            { code: "", note: "" },
            { code: "fn require_send<T: Send>(_value: T) {}", note: "一個什麼都不做的函式,只用 trait bound 問編譯器:T 能不能安全地移交給另一個執行緒?標準庫的 thread::spawn 對閉包的要求之一就是這個 bound。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let boxed = Box::new(5);", note: "Box<i32>:獨佔擁有,i32 是 Send,所以 Box<i32> 也是 Send。" },
            { code: "    require_send(boxed);", note: "通過。所有權整個移交,原處再也碰不到這份資料。" },
            { code: "    let counted = Rc::new(5);", note: "Rc<i32>:計數器沒有同步保護。" },
            { code: "    require_send(counted);", note: "⛔ 編譯失敗:Rc<{integer}> cannot be sent between threads safely(E0277),help:the trait Send is not implemented for Rc<{integer}>。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "錯誤發生在編譯期,程式根本產生不出來——這比「執行期偶爾算錯計數」好上太多,那種 bug 幾乎無法重現。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn require_send<T: Send>(_value: T) {}", note: "同樣的檢查函式。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let boxed = Box::new(5);", note: "Box<i32> 是 Send。" },
            { code: "    require_send(boxed);", note: "通過。" },
            { code: "    let another = Box::new(5);", note: "改動處:資料不需要被多方共享,就用獨佔的 Box,而不是 Rc。" },
            { code: "    require_send(another);", note: "通過。" },
            { code: "}", note: "兩個 Box 已經在 require_send 裡被 drop。" },
          ],
          outro: "如果真的需要「多個執行緒共同擁有」,答案是 Arc(atomically reference counted):介面跟 Rc 幾乎一樣,計數改用原子指令,所以是 Send。那是 2-7 的主題,這裡只要記住:Rc 是單執行緒專用的。",
        },
      ],
      csharp: `C# 的物件天生可以被任何執行緒拿去讀寫,編譯器不會問,安全與否全看你有沒有記得加 lock——漏掉了就是偶發的資料競爭。Rust 用 Send/Sync 這兩個自動推導的標記,讓「這個型別能不能跨執行緒」變成型別系統的一部分:Rc 這種為單執行緒最佳化的型別,一被帶進多執行緒情境就是編譯錯誤,而不是線上環境半夜才出現的計數錯亂。`,
    },
    {
      id: "2-3-13",
      question: "Weak<T>:不擁有資料的弱參考。以下程式碼印出什麼?",
      questionCode: "use std::rc::{Rc, Weak};\n\nfn main() {\n    let strong = Rc::new(String::from(\"data\"));\n    let weak: Weak<String> = Rc::downgrade(&strong);\n    println!(\"{} {}\", Rc::strong_count(&strong), Rc::weak_count(&strong));\n    println!(\"{:?}\", weak.upgrade());\n    drop(strong);\n    println!(\"{:?}\", weak.upgrade());\n}",
      options: [
        { text: "1 1、Some(\"data\")、None——Weak 不算擁有者,strong_count 維持 1;最後一個 Rc 被 drop 後資料就釋放了,upgrade 回傳 None" },
        { text: "2 0、Some(\"data\")、Some(\"data\")——downgrade 也是一種 clone,Weak 會讓資料繼續活著" },
        { text: "1 1、Some(\"data\")、Some(\"data\")——Weak 只是不算進計數,但資料要等 Weak 也消失才釋放" },
        { text: "編譯錯誤:Weak 必須像 Rc 一樣直接解參考使用,沒有 upgrade 這種方法" },
      ],
      answer: 0,
      explanation: `Rc 有兩種計數:strong_count 決定資料活多久,weak_count 只是記錄「有幾個人在觀望」。Rc::downgrade 產生的 Weak 只增加 weak_count,所以印出 1 1。Weak 不保證資料還在,所以不能直接解參考;要用 upgrade() 嘗試換回一個 Rc,回傳 Option<Rc<T>>——資料還在就是 Some(並且在你持有期間暫時加一個強計數),已經被釋放就是 None。drop(strong) 讓強計數歸零,String 被釋放,第二次 upgrade 就是 None。
「Weak 會讓資料繼續活著」正好說反了,Weak 存在的意義就是「不延長生命」。「要等 Weak 也消失才釋放」只對了一小半:資料(String)在強計數歸零時就釋放;真正等 Weak 全部消失才釋放的,只是 Rc 內部那塊存放計數器的小記憶體。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::rc::{Rc, Weak};", note: "Weak 跟 Rc 在同一個模組。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let strong = Rc::new(String::from(\"data\"));", note: "強計數 1,弱計數 0。" },
          { code: "    let weak: Weak<String> = Rc::downgrade(&strong);", note: "從 Rc 產生一個 Weak。強計數不變,弱計數變 1。weak 知道資料在哪,但不擁有它。" },
          { code: "    println!(\"{} {}\", Rc::strong_count(&strong), Rc::weak_count(&strong));", note: "印出 1 1。" },
          { code: "    println!(\"{:?}\", weak.upgrade());", note: "upgrade 檢查強計數大於 0,回傳 Some(Rc)。印出 Some(\"data\")。這個暫時的 Rc 在這一行結束時就被 drop,強計數回到 1。" },
          { code: "    drop(strong);", note: "最後一個強擁有者消失,強計數歸零,String 立刻被釋放。weak 還在,但它指向的資料已經沒了。" },
          { code: "    println!(\"{:?}\", weak.upgrade());", note: "upgrade 發現強計數是 0,回傳 None。印出 None。" },
          { code: "}", note: "weak 被 drop,弱計數歸零,Rc 內部的計數器區塊才最後釋放。" },
        ],
        outro: "upgrade 回傳 Option 是整個設計的重點:型別系統強迫你處理「對方可能已經不在了」的情況,不可能不小心用到已釋放的資料。",
      },
      csharp: `C# 有 WeakReference<T>:var weak = new WeakReference<Foo>(obj); if (weak.TryGetTarget(out var target)) { ... }——語意幾乎一樣,TryGetTarget 就是 upgrade。差別在失效的時機:C# 的弱參考要等 GC 真的執行回收才會失效,時間點不可預測;Rust 的 Weak 在最後一個 Rc 被 drop 的那一行就失效,完全確定。`,
    },
    {
      id: "2-3-14",
      question: "父子節點用 Rc 互相持有。以下程式碼印出什麼?",
      questionCode: "use std::cell::RefCell;\nuse std::rc::Rc;\n\nstruct Node {\n    name: &'static str,\n    parent: RefCell<Option<Rc<Node>>>,\n    child: RefCell<Option<Rc<Node>>>,\n}\n\nimpl Drop for Node {\n    fn drop(&mut self) {\n        println!(\"drop {}\", self.name);\n    }\n}\n\nfn main() {\n    let parent = Rc::new(Node { name: \"parent\", parent: RefCell::new(None), child: RefCell::new(None) });\n    let child = Rc::new(Node { name: \"child\", parent: RefCell::new(None), child: RefCell::new(None) });\n    *parent.child.borrow_mut() = Some(Rc::clone(&child));\n    *child.parent.borrow_mut() = Some(Rc::clone(&parent));\n    println!(\"{} {}\", Rc::strong_count(&parent), Rc::strong_count(&child));\n}",
      options: [
        { text: "只印出 2 2,之後沒有任何 drop 訊息——兩個節點互相持有強參考,main 結束時計數各自只降到 1,永遠不會歸零,記憶體洩漏" },
        { text: "印出 2 2,接著 drop child、drop parent——main 結束時所有區域變數都會被釋放" },
        { text: "編譯錯誤:Rust 的所有權系統會偵測到兩個 Rc 形成循環並拒絕編譯" },
        { text: "印出 1 1,接著 drop child、drop parent——同一組節點之間的互相參考不計入 strong_count" },
      ],
      answer: 0,
      explanation: `數一數擁有者:parent 節點被「變數 parent」和「child 節點的 parent 欄位」擁有,計數 2;child 節點被「變數 child」和「parent 節點的 child 欄位」擁有,也是 2。main 結束時兩個變數被 drop,各減一,剩下 1——而剩下的那個擁有者,正好在對方身上。parent 要等 child 釋放才會歸零,child 要等 parent 釋放才會歸零,誰都等不到。Drop 永遠不會被呼叫,這塊記憶體洩漏到程式結束。
「所有權系統會偵測循環」是這題要誠實面對的事實:不會。Rust 的記憶體安全保證的是「不會用到已釋放的資料」,並不保證「不會洩漏」——洩漏在 Rust 裡被視為安全的(標準庫甚至有 std::mem::forget 和 Box::leak)。參考計數天生看不見循環,這是 Rc 相對於追蹤式 GC 較弱的一角。解法是讓其中一個方向不擁有對方,也就是 Weak。`,
      walkthrough: [
        {
          label: "X Question code (leaks memory)",
          lines: [
            { code: "use std::cell::RefCell;", note: "欄位需要在建立後才填入,所以要 RefCell。" },
            { code: "use std::rc::Rc;", note: "節點要被多方持有。" },
            { code: "", note: "" },
            { code: "struct Node {", note: "一個同時知道父節點和子節點的樹節點。" },
            { code: "    name: &'static str,", note: "用來辨識是哪個節點。" },
            { code: "    parent: RefCell<Option<Rc<Node>>>,", note: "⛔ 洩漏的起因之一:子節點用強參考(Rc)擁有父節點。" },
            { code: "    child: RefCell<Option<Rc<Node>>>,", note: "父節點用強參考擁有子節點——這個方向本身沒問題。" },
            { code: "}", note: "Node 的定義結束。" },
            { code: "", note: "" },
            { code: "impl Drop for Node {", note: "用 Drop 觀察節點有沒有被釋放。" },
            { code: "    fn drop(&mut self) {", note: "被釋放時呼叫。" },
            { code: "        println!(\"drop {}\", self.name);", note: "如果這一行從沒印出來,就代表節點沒有被釋放。" },
            { code: "    }", note: "drop 結束。" },
            { code: "}", note: "Drop for Node 的實作結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let parent = Rc::new(Node { name: \"parent\", parent: RefCell::new(None), child: RefCell::new(None) });", note: "parent 節點,強計數 1。" },
            { code: "    let child = Rc::new(Node { name: \"child\", parent: RefCell::new(None), child: RefCell::new(None) });", note: "child 節點,強計數 1。" },
            { code: "    *parent.child.borrow_mut() = Some(Rc::clone(&child));", note: "父擁有子,child 的強計數變 2。" },
            { code: "    *child.parent.borrow_mut() = Some(Rc::clone(&parent));", note: "⛔ 循環在這一行成形:子也擁有父,parent 的強計數變 2。從此兩個節點互相讓對方活著。" },
            { code: "    println!(\"{} {}\", Rc::strong_count(&parent), Rc::strong_count(&child));", note: "印出 2 2。" },
            { code: "}", note: "變數 child、parent 被 drop,兩個計數都只降到 1。沒有任何 drop 訊息——兩個節點洩漏了。編譯器與執行期都不會報錯。" },
          ],
          outro: "這個 bug 完全安靜:沒有編譯錯誤、沒有 panic、輸出看起來也正常,只有記憶體用量會持續上升。Drop 裡印 log 是檢查這類問題最簡單的方法。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::cell::RefCell;", note: "同上。" },
            { code: "use std::rc::{Rc, Weak};", note: "改動處:多引入 Weak。" },
            { code: "", note: "" },
            { code: "struct Node {", note: "同一個樹節點。" },
            { code: "    name: &'static str,", note: "用來辨識節點。" },
            { code: "    parent: RefCell<Weak<Node>>,", note: "改動處:子節點對父節點只持有 Weak——知道父親是誰,但不讓父親活著。Weak::new() 本身就能表示「還沒有父節點」,不必再包 Option。" },
            { code: "    child: RefCell<Option<Rc<Node>>>,", note: "父擁有子,維持強參考。" },
            { code: "}", note: "Node 的定義結束。" },
            { code: "", note: "" },
            { code: "impl Drop for Node {", note: "同上。" },
            { code: "    fn drop(&mut self) {", note: "同上。" },
            { code: "        println!(\"drop {}\", self.name);", note: "這次會印出來。" },
            { code: "    }", note: "drop 結束。" },
            { code: "}", note: "Drop for Node 的實作結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let parent = Rc::new(Node { name: \"parent\", parent: RefCell::new(Weak::new()), child: RefCell::new(None) });", note: "改動處:初始的 parent 欄位用 Weak::new(),一個不指向任何東西的弱參考。強計數 1。" },
            { code: "    let child = Rc::new(Node { name: \"child\", parent: RefCell::new(Weak::new()), child: RefCell::new(None) });", note: "child 節點,強計數 1。" },
            { code: "    *parent.child.borrow_mut() = Some(Rc::clone(&child));", note: "父擁有子,child 強計數 2。" },
            { code: "    *child.parent.borrow_mut() = Rc::downgrade(&parent);", note: "改動處:子對父用 downgrade,parent 的強計數維持 1,只有弱計數加一。" },
            { code: "    println!(\"{} {}\", Rc::strong_count(&parent), Rc::strong_count(&child));", note: "印出 1 2。" },
            { code: "}", note: "變數 child 先 drop(計數 2 → 1,還在);變數 parent 再 drop(計數 1 → 0),印出 drop parent;parent 的 child 欄位跟著被釋放,child 計數歸零,印出 drop child。" },
          ],
          outro: "完整輸出是 1 2、drop parent、drop child。循環沒有消失(子仍然找得到父),只是其中一段不再是「擁有」,計數就能正常歸零。",
        },
      ],
      csharp: `這是 Rust 相對於 C# 較弱的一角,值得誠實說清楚:.NET 的 GC 是追蹤式的,從根物件出發標記可達的物件,兩個互相參考但從根走不到的物件會被一起回收,C# 開發者從來不用想循環參考。Rc 是參考計數,只看「有幾個人指著我」,看不出「指著我的人是不是也只被我指著」。換來的是確定的釋放時機、沒有 GC 暫停——代價就是這種資料結構要你自己決定哪一個方向用 Weak。`,
    },
    {
      id: "2-3-15",
      question: "設計選擇:要做一個雙向鏈結串列,每個節點同時有 next(指向下一個)與 prev(指向上一個),程式只持有指向第一個節點的 Rc。哪一種設計才能既不洩漏、又不會讓節點被提早釋放?",
      questionCode: "struct Node {\n    value: i32,\n    next: /* ? */,\n    prev: /* ? */,\n}\n\n// 外部只持有 head: Rc<RefCell<Node>>",
      options: [
        { text: "next 用 Rc(強),prev 用 Weak(弱):擁有關係順著 head 往後一路延伸,每個節點都被前一個節點擁有;往回走的那條邊只是觀察,不形成循環" },
        { text: "next 和 prev 都用 Weak,這樣絕對不會有循環,最安全" },
        { text: "next 用 Weak,prev 用 Rc:往回走的操作比較常用,應該讓常被存取的那一端用強參考" },
        { text: "兩邊都用 Rc,等串列不用時再手動把每個節點的 prev 設成 None 就好,這樣兩個方向的存取都比較方便" },
      ],
      answer: 0,
      explanation: `選擇哪一邊用 Weak,要看「擁有權從哪裡流進來」。外部只握著 head,所以擁有權必須從 head 順著 next 一路傳下去:head 擁有第二個節點、第二個擁有第三個……每個節點都有人讓它活著。prev 則是反方向的「回頭看」,用 Weak 就不會形成循環。
「兩邊都用 Weak」聽起來最安全,實際上最糟:除了 head 之外沒有任何節點被強參考持有,第二個節點一建立完、區域變數一離開作用域就被釋放了,串列只剩一個節點。「讓常被存取的那一端用強參考」把存取頻率跟擁有權混在一起——Weak 的存取成本只是多一次 upgrade,真正的判準是誰擁有誰;反過來設計的話,從 head 走 next 拿到的都是已經被釋放的節點。「兩邊都 Rc 再手動斷開」技術上可行,但把正確性押在「每條程式路徑都記得清理」上,只要有一個提早 return 或 panic 就洩漏。`,
      walkthrough: {
        label: "⊕ Correct design — walkthrough (runs successfully)",
        lines: [
          { code: "use std::cell::RefCell;", note: "節點建立後還要改 next/prev,所以要 RefCell。" },
          { code: "use std::rc::{Rc, Weak};", note: "強、弱兩種參考都會用到。" },
          { code: "", note: "" },
          { code: "struct Node {", note: "雙向鏈結串列的節點。" },
          { code: "    value: i32,", note: "節點的資料。" },
          { code: "    next: Option<Rc<RefCell<Node>>>,", note: "往後:強參考。擁有權沿著這條邊流動。" },
          { code: "    prev: Option<Weak<RefCell<Node>>>,", note: "往前:弱參考。只是「知道前一個是誰」,不讓它活著。" },
          { code: "}", note: "Node 的定義結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let first = Rc::new(RefCell::new(Node { value: 1, next: None, prev: None }));", note: "第一個節點,也就是 head。強計數 1。" },
          { code: "    let second = Rc::new(RefCell::new(Node { value: 2, next: None, prev: None }));", note: "第二個節點,強計數 1(暫時由區域變數持有)。" },
          { code: "    first.borrow_mut().next = Some(Rc::clone(&second));", note: "first 擁有 second,second 強計數 2。之後就算區域變數 second 消失,它仍然活著。" },
          { code: "    second.borrow_mut().prev = Some(Rc::downgrade(&first));", note: "second 往回指向 first,只增加弱計數,first 的強計數維持 1。" },
          { code: "    let back = second.borrow().prev.as_ref().unwrap().upgrade().unwrap();", note: "往回走:取出 Weak,upgrade 成 Rc。first 還活著,所以是 Some。" },
          { code: "    println!(\"{} {}\", back.borrow().value, first.borrow().next.as_ref().unwrap().borrow().value);", note: "印出 1 2:從 second 往回找到 1,從 first 往後找到 2,兩個方向都走得通。" },
          { code: "    println!(\"{} {}\", Rc::strong_count(&first), Rc::strong_count(&second));", note: "印出 2 2。first 的 2 是「變數 first + 暫時借出的 back」,不是 prev——prev 只算在弱計數裡。" },
          { code: "}", note: "back、second、first 依序被 drop,強計數全部歸零,兩個節點都被正確釋放。" },
        ],
        outro: "通用的判斷法:畫出擁有關係,確保從你實際持有的那個根出發,沿著強參考能走到每一個節點;其他所有「回頭」或「橫向」的邊,一律用 Weak。樹是父強子弱,串列是 next 強 prev 弱,觀察者模式是主體弱持有觀察者。",
      },
      csharp: `C# 寫雙向串列(包括 LinkedList<T> 的實作)時,Next 和 Previous 都是普通參考,從來不需要決定哪一個是「擁有者」,GC 從根出發自然會找到整條鏈。Rust 要你把這個隱含的所有權結構畫出來——一開始是額外的設計負擔,但它也逼你回答一個 C# 裡常被忽略的問題:這個物件到底歸誰管?`,
    },
    {
      id: "2-3-16",
      question: "自訂的包裝型別。以下程式碼的結果是?",
      questionCode: "struct MyBox<T>(T);\n\nfn main() {\n    let b = MyBox(String::from(\"hello\"));\n    println!(\"{}\", b.len());\n}",
      options: [
        { text: "編譯錯誤:no method named len found for struct MyBox——方法呼叫只會穿過實作了 Deref 的型別,MyBox 沒有實作,編譯器不知道要往哪裡找" },
        { text: "印出 5——MyBox 只有一個欄位,方法呼叫會自動轉發給那個欄位" },
        { text: "編譯錯誤,但只要改寫成 (*b).len() 就能通過" },
        { text: "編譯錯誤,要在 struct 上加 #[derive(Deref)] 讓編譯器自動產生轉發" },
      ],
      answer: 0,
      explanation: `第 3 題看到 Box 的方法呼叫會自動穿透,那不是因為「Box 只包一個值」,而是因為 Box 實作了 std::ops::Deref。方法呼叫的自動解參考只會沿著 Deref 走:MyBox 沒有實作它,編譯器在 MyBox 上找不到 len,也沒有下一層可以找,就報 no method named len。
「只有一個欄位就自動轉發」是很自然的猜測,但 Rust 不做這種推論——結構長什麼樣子不構成行為,實作了哪些 trait 才算。「改寫成 (*b).len()」也一樣失敗:* 運算子對自訂型別同樣要靠 Deref 才能用,錯誤只會換成 type MyBox<String> cannot be dereferenced。標準庫也沒有 #[derive(Deref)](有第三方 crate 提供),要自己寫 impl。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "struct MyBox<T>(T);", note: "一個只包一個值的 tuple struct,外觀跟 Box 很像,但沒有任何 trait 實作。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let b = MyBox(String::from(\"hello\"));", note: "b 的型別是 MyBox<String>。" },
            { code: "    println!(\"{}\", b.len());", note: "⛔ 編譯失敗:no method named len found for struct MyBox<T> in the current scope(E0599)。MyBox 自己沒有 len,又沒有實作任何解參考 trait 讓編譯器往內層找。help 會提示 one of the expressions' fields has a method of the same name,也就是建議你寫 b.0.len()。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "b.0.len() 確實能通過,但那等於讓每個呼叫端都知道 MyBox 的內部結構;要讓 MyBox 像真正的智慧指標一樣「直接用」,就要實作 Deref。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::ops::Deref;", note: "改動處:引入 Deref trait。" },
            { code: "", note: "" },
            { code: "struct MyBox<T>(T);", note: "結構本身不用改。" },
            { code: "", note: "" },
            { code: "impl<T> Deref for MyBox<T> {", note: "改動處:對所有 T 實作 Deref,告訴編譯器「解參考 MyBox<T> 會得到一個 T」。" },
            { code: "    type Target = T;", note: "關聯型別 Target:解參考之後是什麼型別。方法查找會沿著 Target 往下一層找。" },
            { code: "    fn deref(&self) -> &T {", note: "唯一要實作的方法:給出指向內部值的共享參考。" },
            { code: "        &self.0", note: "回傳第一個欄位的參考。" },
            { code: "    }", note: "deref 結束。" },
            { code: "}", note: "Deref for MyBox 的實作結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let b = MyBox(String::from(\"hello\"));", note: "同上。" },
            { code: "    println!(\"{}\", b.len());", note: "編譯器在 MyBox<String> 上找不到 len,透過 Deref 往下一層到 String,找到 len。印出 5。" },
            { code: "}", note: "b 被釋放,連同裡面的 String。" },
          ],
          outro: "注意 Deref 只給了 &T:它讓「讀取類」的方法穿透得過去。需要 &mut self 的方法(例如 push_str)能不能穿透,是另一個 trait 的事,第 19 題會看到。",
        },
      ],
      csharp: `C# 想做一個「包一層、但用起來像裡面的物件」的型別,只能手寫每一個轉發方法(或用 DynamicObject/Castle 動態代理),沒有「忘了實作就編譯失敗」的問題,因為本來就沒有自動轉發這回事。Rust 的 Deref 是語言層級的轉發機制,一個 impl 就讓所有方法穿透——但它刻意設計成「只給指標類型用」,不鼓勵拿來模擬繼承。`,
    },
    {
      id: "2-3-17",
      question: "設計選擇:下面三個情境各自最適合哪一種智慧指標?(1)一棵每個節點只有一個父節點的遞迴表達式樹;(2)一份設定資料被好幾個服務物件共同持有、唯讀使用,不確定哪個服務最後才被釋放;(3)一個方法簽名必須是 &self,卻要在裡面累加呼叫次數。",
      options: [
        { text: "(1) Box、(2) Rc、(3) RefCell——分別對應「獨佔 + 放上 heap」、「多重擁有」、「只有共享參考時仍要修改」,每一種只付自己需要的成本" },
        { text: "三個都用 Rc<RefCell<T>>,功能最完整,以後需求變了也不用改型別" },
        { text: "(1) Rc、(2) Rc、(3) Box——Rc 比 Box 多一份計數,在樹裡也比較安全;Box 裡的值可以透過 &self 修改" },
        { text: "(1) RefCell、(2) Box 並在每個服務各 clone 一份、(3) RefCell——RefCell 也能打斷遞迴型別的無限大小" },
      ],
      answer: 0,
      explanation: `三個型別各自回答一個不同的問題。Box:這個值要放在 heap(通常因為大小不定或遞迴),而且只有一個擁有者——表達式樹的子節點只屬於一個父節點,Box 最便宜,沒有計數也沒有執行期檢查。Rc:同一份資料有多個擁有者,而且沒辦法在編譯期決定誰最後離開——設定資料正是這種情況,唯讀就不需要 RefCell。RefCell:手上只有共享參考,但有合理的理由要修改——&self 方法裡累加計數,就是把 RefCell(或更輕量的 Cell)放在欄位裡。
「全部用 Rc<RefCell<T>>」是從 GC 語言帶來的習慣:它確實「什麼都能做」,但每次存取都付計數與借用檢查的成本,還把原本編譯期就能抓到的錯誤變成執行期 panic,而且型別再也不能說明資料的擁有關係。「RefCell 能打斷遞迴大小」是誤解:RefCell<T> 把 T 直接內嵌,大小就是 T 再加一個計數,遞迴一樣無限大——只有指標類型(Box、Rc、&)才能打斷。`,
      walkthrough: {
        label: "⊕ All three in one program — walkthrough (runs successfully)",
        lines: [
          { code: "use std::cell::RefCell;", note: "情境三要用。" },
          { code: "use std::rc::Rc;", note: "情境二要用。" },
          { code: "", note: "" },
          { code: "enum Expr {", note: "情境一:遞迴的表達式樹。" },
          { code: "    Num(i32),", note: "葉節點。" },
          { code: "    Add(Box<Expr>, Box<Expr>),", note: "每個子節點只有一個擁有者(這個 Add 節點),Box 就夠了。" },
          { code: "}", note: "Expr 的定義結束。" },
          { code: "", note: "" },
          { code: "impl Expr {", note: "替樹實作求值。" },
          { code: "    fn eval(&self) -> i32 {", note: "遞迴求值。" },
          { code: "        match self {", note: "依節點種類分派。" },
          { code: "            Expr::Num(n) => *n,", note: "葉節點直接回傳數值。" },
          { code: "            Expr::Add(left, right) => left.eval() + right.eval(),", note: "left、right 是 &Box<Expr>,方法呼叫自動穿過 Box。" },
          { code: "        }", note: "match 結束。" },
          { code: "    }", note: "eval 結束。" },
          { code: "}", note: "Expr 的方法區塊結束。" },
          { code: "", note: "" },
          { code: "struct Service {", note: "情境二與三:一個服務物件。" },
          { code: "    config: Rc<String>,", note: "共同持有、唯讀的設定。只要 Rc,不需要 RefCell。" },
          { code: "    calls: RefCell<u32>,", note: "只有這個欄位需要在 &self 方法裡修改,所以只有它包 RefCell。" },
          { code: "}", note: "Service 的定義結束。" },
          { code: "", note: "" },
          { code: "impl Service {", note: "服務的方法。" },
          { code: "    fn handle(&self) {", note: "簽名是 &self,呼叫端只需要共享參考。" },
          { code: "        *self.calls.borrow_mut() += 1;", note: "透過 RefCell 在共享參考下修改計數;RefMut 是暫時值,這一行結束就歸還。" },
          { code: "    }", note: "handle 結束。" },
          { code: "}", note: "Service 的方法區塊結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let expr = Expr::Add(Box::new(Expr::Num(1)), Box::new(Expr::Num(2)));", note: "1 + 2 的樹。" },
          { code: "    let config = Rc::new(String::from(\"prod\"));", note: "設定資料,強計數 1。" },
          { code: "    let api = Service { config: Rc::clone(&config), calls: RefCell::new(0) };", note: "服務持有設定的一份 Rc,強計數 2。" },
          { code: "    api.handle();", note: "計數變 1。" },
          { code: "    api.handle();", note: "計數變 2。api 從頭到尾都沒有宣告 mut。" },
          { code: "    println!(\"{} {} {} {}\", expr.eval(), api.config, Rc::strong_count(&config), api.calls.borrow());", note: "印出 3 prod 2 2。" },
          { code: "}", note: "所有值離開作用域,依各自的規則釋放。" },
        ],
        outro: "讀型別就能讀出設計:Box 說「這是我的」,Rc 說「這是大家的」,RefCell 說「這裡會在共享狀態下被改」。全部都寫成 Rc<RefCell<T>>,這些資訊就消失了。",
      },
      csharp: `C# 開發者的預設世界觀,等於「每個物件都是 Rc<RefCell<T>>,而且不檢查借用」:誰都能持有、誰都能改、GC 替你收尾。Rust 不給這個預設,而是要你為每一份資料選一個剛好夠用的型別。一開始像是囉嗦,但選完之後,型別本身就是一份不會過時的所有權文件。`,
    },
    {
      id: "2-3-18",
      question: "綜合:用 Rc<RefCell<Node>> 組成的小樹。以下程式碼印出什麼?",
      questionCode: "use std::cell::RefCell;\nuse std::rc::Rc;\n\nstruct Node {\n    value: i32,\n    children: Vec<Rc<RefCell<Node>>>,\n}\n\nfn main() {\n    let leaf = Rc::new(RefCell::new(Node { value: 3, children: vec![] }));\n    let root = Rc::new(RefCell::new(Node { value: 1, children: vec![Rc::clone(&leaf)] }));\n    leaf.borrow_mut().value = 30;\n    let total = root.borrow().value + root.borrow().children[0].borrow().value;\n    println!(\"{} {}\", total, Rc::strong_count(&leaf));\n}",
      options: [
        { text: "31 2——root 的 children 裡存的是指向同一個葉節點的 Rc,透過 leaf 修改後,從 root 走下去看到的也是 30;葉節點被變數 leaf 與 root 的 children 共同持有" },
        { text: "4 2——建立 root 時 Rc::clone 把葉節點複製了一份放進 children,之後修改 leaf 不影響那份複本" },
        { text: "31 1——放進 Vec 之後,擁有權就轉移給 root 了,變數 leaf 不再算擁有者" },
        { text: "執行期 panic:同一個運算式裡對 root 呼叫了兩次 borrow(),RefCell 不允許同時有兩個借用" },
      ],
      answer: 0,
      explanation: `Rc::clone(&leaf) 放進 children 的是「指向同一個節點的另一根指標」,不是節點的副本。所以不管從變數 leaf 還是從 root.children[0] 走過去,都會到同一個 RefCell<Node>。透過 leaf 把 value 改成 30,root 走下去讀到的就是 30,1 + 30 = 31。葉節點有兩個擁有者(變數 leaf 與 root 的 children),強計數是 2。
「同一個運算式兩次 borrow() 會 panic」是把第 9 題的規則記得太嚴:RefCell 允許同時有很多個唯讀借用,只有「唯讀 + 可變」或「可變 + 可變」才衝突。那一行的兩個 root.borrow() 同時存在也沒問題;而上一行的 borrow_mut 是暫時值,早就歸還了。「clone 複製了節點」則又是第 5 題的誤解——在樹狀結構裡它的後果最明顯:如果真的是複製,修改就會「不見」。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::cell::RefCell;", note: "節點要能在共享狀態下修改。" },
          { code: "use std::rc::Rc;", note: "節點要能被多方持有。" },
          { code: "", note: "" },
          { code: "struct Node {", note: "樹節點。" },
          { code: "    value: i32,", note: "節點的資料。" },
          { code: "    children: Vec<Rc<RefCell<Node>>>,", note: "子節點清單,每個元素都是共享、可修改的節點。" },
          { code: "}", note: "Node 的定義結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let leaf = Rc::new(RefCell::new(Node { value: 3, children: vec![] }));", note: "葉節點,強計數 1。" },
          { code: "    let root = Rc::new(RefCell::new(Node { value: 1, children: vec![Rc::clone(&leaf)] }));", note: "root 的 children 放進葉節點的另一根 Rc,葉節點強計數 2。沒有任何資料被複製。" },
          { code: "    leaf.borrow_mut().value = 30;", note: "透過變數 leaf 取得可變借用,把葉節點的 value 改成 30。RefMut 在這一行結束時歸還。" },
          { code: "    let total = root.borrow().value + root.borrow().children[0].borrow().value;", note: "兩個 root.borrow() 都是唯讀借用,可以並存。從 root 走到 children[0],再借一次葉節點,讀到 30。1 + 30 = 31。整行結束後所有暫時的 Ref 都歸還。" },
          { code: "    println!(\"{} {}\", total, Rc::strong_count(&leaf));", note: "印出 31 2。" },
          { code: "}", note: "root 先被 drop(宣告較晚),它的 children 跟著釋放,葉節點計數 2 → 1;接著 leaf 被 drop,計數歸零,葉節點釋放。" },
        ],
        outro: "這就是 Rc<RefCell<T>> 在實務上的樣子:同一個節點可以從好幾個入口走到,從任何一個入口修改,其他入口都看得到。如果還需要子節點往回找父節點,父指標就要用 Weak(第 14 題)。",
      },
      csharp: `C# 的樹完全就是這種語意:var leaf = new Node(3); var root = new Node(1) { Children = { leaf } }; leaf.Value = 30; 之後 root.Children[0].Value 就是 30,不需要任何設計。Rust 的版本多了兩層型別,但每一層都在型別上說明了一件 C# 程式碼沒寫出來的事:這個節點會被共享,而且會在共享的狀態下被修改。`,
    },
    {
      id: "2-3-19",
      question: "自訂智慧指標只實作了 Deref。以下程式碼的結果是?",
      questionCode: "use std::ops::Deref;\n\nstruct Stack<T> {\n    items: Vec<T>,\n}\n\nimpl<T> Deref for Stack<T> {\n    type Target = Vec<T>;\n    fn deref(&self) -> &Vec<T> {\n        &self.items\n    }\n}\n\nfn main() {\n    let mut s = Stack { items: vec![1, 2] };\n    println!(\"{}\", s.len());\n    s.push(3);\n    println!(\"{:?}\", *s);\n}",
      options: [
        { text: "編譯錯誤,錯在 s.push(3):cannot borrow data in dereference of Stack<i32> as mutable——Deref 只提供 &Vec,push 需要 &mut Vec,要另外實作 DerefMut 才能穿透" },
        { text: "印出 2 與 [1, 2, 3]——實作了 Deref 之後,Vec 的所有方法都能直接透過 Stack 呼叫" },
        { text: "編譯錯誤,錯在 s.len():Deref 只能讓 * 運算子生效,方法呼叫不會自動穿透自訂型別" },
        { text: "編譯錯誤,但只要把 push 那一行改成 (*s).push(3) 就能通過" },
      ],
      answer: 0,
      explanation: `方法呼叫的自動解參考分兩條路:接收者是 &self 的方法(len)走 Deref::deref,拿到 &Vec<T>;接收者是 &mut self 的方法(push)走 DerefMut::deref_mut,需要 &mut Vec<T>。Stack 只實作了前者,所以 s.len() 順利穿透,s.push(3) 找不到取得 &mut Vec 的合法途徑,報 cannot borrow data in dereference of Stack<i32> as mutable,並提示 trait DerefMut is required。
「實作 Deref 就能呼叫所有方法」是這題的核心誤解——Deref 只打通唯讀那一半。s 已經宣告成 mut 也沒用,mut 只允許修改 s 本身,穿透到 Vec 還是要靠 DerefMut(編譯器甚至會警告這個 mut 用不到)。「改寫成 (*s).push(3)」只是把自動解參考換成手寫,底下呼叫的仍然是 deref_mut,錯誤完全一樣。這跟第 6 題 Rc 的錯誤訊息是同一件事:Rc 正是刻意只實作 Deref、不實作 DerefMut 的智慧指標。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "use std::ops::Deref;", note: "只引入了 Deref。" },
            { code: "", note: "" },
            { code: "struct Stack<T> {", note: "自訂的包裝型別,想讓它用起來像 Vec。" },
            { code: "    items: Vec<T>,", note: "實際的資料。" },
            { code: "}", note: "Stack 的定義結束。" },
            { code: "", note: "" },
            { code: "impl<T> Deref for Stack<T> {", note: "實作 Deref,讓 Stack 可以被解參考成 Vec<T>。" },
            { code: "    type Target = Vec<T>;", note: "解參考之後是 Vec<T>。" },
            { code: "    fn deref(&self) -> &Vec<T> {", note: "只給共享參考 &Vec<T>。" },
            { code: "        &self.items", note: "回傳內部 Vec 的參考。" },
            { code: "    }", note: "deref 結束。" },
            { code: "}", note: "Deref for Stack 的實作結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let mut s = Stack { items: vec![1, 2] };", note: "宣告成 mut,但編譯器會警告 variable does not need to be mutable——因為沒有任何合法的修改路徑用得到它。" },
            { code: "    println!(\"{}\", s.len());", note: "len 的接收者是 &self,透過 Deref 拿到 &Vec,穿透成功。" },
            { code: "    s.push(3);", note: "⛔ 編譯失敗:cannot borrow data in dereference of Stack<i32> as mutable(E0596),help:trait DerefMut is required to modify through a dereference, but it is not implemented for Stack<i32>。push 的接收者是 &mut self,Deref 給不出 &mut Vec。" },
            { code: "    println!(\"{:?}\", *s);", note: "*s 是唯讀使用,透過 Deref 沒問題。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "錯誤訊息提到 DerefMut,你的程式碼裡只寫了 Deref——見下方去糖區塊。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::ops::{Deref, DerefMut};", note: "改動處:多引入 DerefMut。" },
            { code: "", note: "" },
            { code: "struct Stack<T> {", note: "同上。" },
            { code: "    items: Vec<T>,", note: "實際的資料。" },
            { code: "}", note: "Stack 的定義結束。" },
            { code: "", note: "" },
            { code: "impl<T> Deref for Stack<T> {", note: "唯讀的那一半,不用改。" },
            { code: "    type Target = Vec<T>;", note: "解參考之後是 Vec<T>。" },
            { code: "    fn deref(&self) -> &Vec<T> {", note: "給共享參考。" },
            { code: "        &self.items", note: "回傳內部 Vec 的參考。" },
            { code: "    }", note: "deref 結束。" },
            { code: "}", note: "Deref for Stack 的實作結束。" },
            { code: "", note: "" },
            { code: "impl<T> DerefMut for Stack<T> {", note: "改動處:實作可變的那一半。DerefMut 以 Deref 為前提,沿用它的 Target,不用再寫一次。" },
            { code: "    fn deref_mut(&mut self) -> &mut Vec<T> {", note: "給出內部 Vec 的可變參考。" },
            { code: "        &mut self.items", note: "回傳可變參考。" },
            { code: "    }", note: "deref_mut 結束。" },
            { code: "}", note: "DerefMut for Stack 的實作結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let mut s = Stack { items: vec![1, 2] };", note: "現在 mut 真的派上用場:deref_mut 需要 &mut s。" },
            { code: "    println!(\"{}\", s.len());", note: "走 Deref,印出 2。" },
            { code: "    s.push(3);", note: "走 DerefMut,穿透到 Vec 呼叫 push。" },
            { code: "    println!(\"{:?}\", *s);", note: "印出 [1, 2, 3]。" },
            { code: "}", note: "s 被釋放,連同內部的 Vec。" },
          ],
          outro: "要不要實作 DerefMut 是設計決定:Box 兩個都實作(獨佔,改了也不影響別人);Rc 只實作 Deref(共享,不能讓任何一個擁有者單方面修改)。自訂指標也應該依同樣的道理選擇。",
        },
        {
          label: "△ Desugared (what the compiler sees)",
          intro: "兩個方法呼叫寫法一模一樣,卻一個成功一個失敗,因為點號穿過指標時,會依接收者的需求選不同的 trait:",
          lines: [
            { code: "s.len();", note: "你寫的第一行:len 需要 &Vec<T>。" },
            { code: "Vec::len(Deref::deref(&s));", note: "編譯器展開成呼叫 Deref::deref,這個你有實作,通過。" },
            { code: "", note: "" },
            { code: "s.push(3);", note: "你寫的第二行:push 需要 &mut Vec<T>。" },
            { code: "Vec::push(DerefMut::deref_mut(&mut s), 3);", note: "編譯器展開成呼叫 DerefMut::deref_mut——你沒有實作這個 trait,錯誤訊息裡的 DerefMut 就是從這裡來的。" },
          ],
        },
      ],
      csharp: `C# 沒有「解參考」這一層,所以也沒有「唯讀穿透」和「可寫穿透」的區分;要做唯讀包裝得自己挑選要轉發哪些方法(例如 ReadOnlyCollection<T> 只轉發讀取的成員)。Rust 用 Deref 與 DerefMut 兩個 trait 把這件事分成兩個開關,一個 impl 就決定了整批方法「能讀」或「能讀也能寫」。`,
    },
    {
      id: "2-3-20",
      question: "總結:從 C# 的角度看 Box、Rc、RefCell,以下哪一個說法最準確?",
      options: [
        { text: "C# 的每個 class 物件都同時具備「放在 heap」、「可被多方持有」、「任何持有者都能修改」三種能力,由 GC 統一處理;Rust 把三者拆成 Box、Rc、RefCell,只為真正需要的能力付成本,代價是循環參考要自己用 Weak 處理,RefCell 的借用衝突延後到執行期才發現" },
        { text: "Rc<RefCell<T>> 就是 Rust 版的 GC:只要全部改用它,就能得到跟 C# 完全相同的語意,包括自動回收循環參考" },
        { text: "Box 對應 C# 的 boxing,用途是把值型別轉成參考型別以便放進泛型集合,所以在 Rust 裡只有處理 i32 這類基本型別時才需要" },
        { text: "RefCell 等同 C# 的 lock:它讓多個執行緒可以安全地修改同一份資料,同一執行緒重複借用也不會出錯" },
      ],
      answer: 0,
      explanation: `這一課的所有題目都可以濃縮成一件事:C# 把「heap 配置」、「共享擁有」、「共享可變」綁在一起,一律送給每個 class 物件;Rust 把它們拆開,Box 只給 heap 與獨佔,Rc 加上多重擁有,RefCell 加上共享可變,你可以只拿需要的那一個。拆開的好處是成本可見、意圖寫在型別上;代價有兩個,都在這一課看過:Rc 看不見循環參考(第 14 題),RefCell 的違規變成執行期 panic(第 9 題)。
把 Rc<RefCell<T>> 當成 GC 是最常見的過度推論——語意接近,但它不會回收循環,也會在借用衝突時 panic,C# 兩件事都不會發生。Box 與 C# boxing 只是名字相近:Box 用在遞迴型別、大型資料或需要固定大小的地方,跟泛型集合無關(Rust 的泛型是單態化的,Vec<i32> 本來就不需要 boxing)。RefCell 則明確是單執行緒專用,不是 Sync,也沒有重入——跨執行緒的對應物是 2-7 的 Mutex。`,
      csharp: `給 C# 開發者的一句話對照:Box<T> 像是「只能有一個變數指著的 class 物件」,Rc<T> 像是「唯讀的 class 物件,數得出有幾個人指著」,Rc<RefCell<T>> 最接近你熟悉的普通 class 物件——只是多了借用檢查,少了循環回收。平常寫 Rust 時,越往前面選越好;需要一路走到 Rc<RefCell<T>> 時,通常值得停下來想想資料結構能不能換一種擁有方式。`,
    },
  ],
};
