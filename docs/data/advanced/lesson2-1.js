/* 出題慣例見專案根目錄 AUTHORING.md:
 *   - answer 一律為 0(正確答案寫在第一個選項),顯示順序由 quiz.js 依題目 id 洗牌
 *   - 詳解禁止用「選項 A/B/C」字母指涉,必須直接描述選項內容
 *   - 有程式碼的題目一律附 walkthrough(逐行說明);反面案例必須同時附上 √ 正確寫法 */
window.RUST_LESSONS = window.RUST_LESSONS || {};
window.RUST_LESSONS["lesson2-1"] = {
  id: "lesson2-1",
  title: "閉包(Closures)",
  goal: "看懂閉包怎麼捕獲環境,以及 Fn / FnMut / FnOnce 的呼叫語意如何決定一個閉包能不能重複呼叫、能不能當參數或回傳值傳遞。",
  primer: {
    intro: "閉包是可以就地定義、存進變數再呼叫的匿名函式,而且它能直接使用定義處周遭的變數,這叫做「捕獲」環境。Rust 沒有垃圾回收,所以編譯器必須在編譯期弄清楚閉包對每個捕獲的變數是借用還是取得所有權。這個決定會影響原變數之後還能不能用,也會影響閉包本身能被呼叫幾次、能被傳到多遠——本課的題目就圍繞這些規則展開。",
    examples: [
      {
        code: "fn main() {\n    let square = |x: i32| x * x;\n    println!(\"{}\", square(4));\n}",
        note: "直立線 |x: i32| 包住參數,後面接本體;square 像一般函式一樣呼叫,印出 16。",
      },
      {
        code: "fn main() {\n    let greeting = String::from(\"Hello\");\n    let greet = |name: &str| println!(\"{}, {}!\", greeting, name);\n    greet(\"Rust\");\n}",
        note: "greet 的本體直接用到外層的 greeting,不必當參數傳進去,這就是捕獲;印出 Hello, Rust!。",
      },
    ],
    csharp: "寫法和 C# 的 lambda(x => x * x)幾乎一樣,也同樣能直接使用外層變數;不同的是 C# 不過問你怎麼捕獲,Rust 則把捕獲方式納入編譯期的所有權與借用檢查。",
  },
  questions: [
    {
      id: "2-1-01",
      question: "閉包的語法與型別推斷。以下程式碼的結果是?",
      questionCode: "fn main() {\n    let add_one = |x| x + 1;\n    let a = add_one(5);\n    let double = |x: i32| -> i32 { x * 2 };\n    let b = double(a);\n    println!(\"{} {}\", a, b);\n}",
      options: [
        { text: "印出 6 12——閉包的參數型別與回傳型別都可以省略,編譯器從「它實際被怎麼使用」推斷出來;想寫也可以寫,但要配一組大括號本體" },
        { text: "編譯錯誤:閉包的參數必須像具名函式一樣標註型別,只寫 |x| 不合法" },
        { text: "印出 6 12,而且 add_one 之後還能傳 f64 進去——沒標型別的閉包是泛型的,每次呼叫各自推斷" },
        { text: "編譯錯誤:閉包的本體是單一運算式時不能省略大括號,要寫成 |x| { x + 1 }" },
      ],
      answer: 0,
      explanation: `閉包是「寫在別的程式碼裡面」的東西,編譯器看得到它的使用現場,所以型別標註可以省。add_one 沒標型別,編譯器從第一次呼叫 add_one(5) 推斷出參數與回傳值都是 i32;double 手寫了型別,那就必須配一組大括號本體(這是語法規定,|x: i32| -> i32 x * 2 不合法)。
「沒標型別的閉包是泛型的」是很誘人的誤解:型別一旦由第一次使用推斷出來就定死了,之後再傳 f64 進去會直接編譯錯誤——閉包不是泛型函式,它是一個具體型別的值。這也是閉包和 fn 最大的語法差異:具名函式的簽名是公開契約,必須寫死;閉包是區域的實作細節,交給推斷就好。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let add_one = |x| x + 1;", note: "閉包的骨架是「一對直立線包住參數,後面接本體」。這裡完全沒有型別標註,編譯器先把參數型別掛起來,等看到實際用法再回填。" },
          { code: "    let a = add_one(5);", note: "第一次呼叫傳進 i32,型別推斷在這裡定案:參數是 i32、回傳也是 i32。a 的值是 6。從此 add_one 的型別就固定了,不會再對別的型別開放。" },
          { code: "    let double = |x: i32| -> i32 { x * 2 };", note: "同一件事的完整寫法。注意:一旦寫了回傳型別的箭頭,本體就必須用大括號包起來,這是語法規定不是風格偏好。" },
          { code: "    let b = double(a);", note: "把 6 乘以 2,b 的值是 12。" },
          { code: "    println!(\"{} {}\", a, b);", note: "印出 6 12。" },
          { code: "}", note: "main 結束,兩個閉包和兩個整數一起離開作用域。" },
        ],
        outro: "把「閉包」想成一個值:它有自己的型別(編譯器生成的匿名型別),可以放進變數、當參數傳、當回傳值。這一課後面十幾題都在回答同一個問題——這個值的型別到底承諾了什麼?",
      },
      csharp: `C# 的對應寫法幾乎一模一樣:Func<int,int> addOne = x => x + 1; 同樣不用標參數型別,因為委派型別已經把簽名說死了。差別在推斷的方向——C# 是「從左邊的 Func<int,int> 推出右邊的 x 是 int」,Rust 是「從後面的呼叫現場推出前面的 x 是 i32」。另一個差別更重要:C# 的 addOne 型別是 Func<int,int>,是個公開的具名型別;Rust 的 add_one 型別是編譯器生成、連名字都沒有的匿名型別,你只能用 Fn 系 trait 去描述它。`,
    },
    {
      id: "2-1-02",
      question: "閉包預設怎麼捕獲環境?以下程式碼的結果是?",
      questionCode: "fn main() {\n    let list = vec![1, 2, 3];\n    let print_list = || println!(\"{:?}\", list);\n    print_list();\n    print_list();\n    println!(\"{:?}\", list);\n}",
      options: [
        { text: "印出三行 [1, 2, 3]——閉包本體只是讀取 list,所以編譯器選擇「不可變借用」這種最輕的捕獲方式,原變數從頭到尾都還在" },
        { text: "編譯錯誤:建立閉包時 list 就被搬進閉包裡了,最後一行的 println! 用不到它" },
        { text: "印出兩行 [1, 2, 3],第三行印出 []——閉包被呼叫過之後 list 已經被消耗掉" },
        { text: "編譯錯誤:同一個閉包不能連續呼叫兩次" },
      ],
      answer: 0,
      explanation: `閉包捕獲環境有三種方式:不可變借用、可變借用、取得所有權。編譯器不會問你要哪一種,它看「本體實際上對捕獲的東西做了什麼」,然後挑「能編譯的最輕那一種」。這裡本體只是把 list 拿去 {:?} 印出來,只需要讀,於是選了不可變借用——list 的所有權沒有動,而且不可變借用可以同時存在很多份,最後一行照樣能讀。
「建立閉包時就被搬進去」是把閉包想成資料容器才會有的誤解:閉包捕獲的是「存取權」,不是資料的搬家。要讓它真的搬,必須顯式寫 move(第 5 題)。至於「呼叫過之後就被消耗」——那是 FnOnce 的行為,而 FnOnce 的成因是「本體把捕獲的東西移了出去」,單純的印出來不會觸發。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let list = vec![1, 2, 3];", note: "list 擁有這個 Vec,資料在 heap 上。" },
          { code: "    let print_list = || println!(\"{:?}\", list);", note: "閉包沒有參數,本體用到了外部的 list——這就叫「捕獲環境」。因為本體只需要讀,編譯器選擇最輕的捕獲方式:對 list 建立一個不可變借用存進閉包裡。list 的所有權完全沒有動。" },
          { code: "    print_list();", note: "第一次呼叫,印出 [1, 2, 3]。呼叫只是讀了那份借用,不會消耗任何東西。" },
          { code: "    print_list();", note: "第二次呼叫,一樣印出 [1, 2, 3]——不可變借用可以被重複讀取,呼叫幾次都行。" },
          { code: "    println!(\"{:?}\", list);", note: "原變數仍然完全有效,印出第三行 [1, 2, 3]。不可變借用可以同時存在多份,閉包持有一份、這一行再借一份,兩者並存沒有衝突。" },
          { code: "}", note: "閉包先離開作用域(借用結束),list 後釋放。" },
        ],
        outro: "三種捕獲方式由編譯器按「本體怎麼用」自動選擇,順序是:能唯讀就唯讀(不可變借用)、需要改就可變借用、非得拿走不可才取得所有權。第 3 題看第二種,第 5 題看第三種。",
      },
      csharp: `C# 的 lambda 捕獲外部區域變數,行為在感覺上很接近這裡的「借用」:捕獲的是變數本身而不是一份快照,所以 lambda 裡看到的永遠是最新的值。實作上 C# 是把變數提升(hoist)到一個編譯器生成的顯示類別欄位裡,lambda 與外層方法共用那個物件,由 GC 保證它活著。Rust 沒有這個 heap 物件——閉包裡存的就是一根指向 stack 上 list 的指標,而借用檢查器負責證明這根指標永遠不會比 list 活得久。`,
    },
    {
      id: "2-1-03",
      question: "閉包修改捕獲的變數。以下程式碼的結果是?",
      questionCode: "fn main() {\n    let mut count = 0;\n    let mut inc = || count += 1;\n    inc();\n    inc();\n    println!(\"{}\", count);\n}",
      options: [
        { text: "印出 2——編譯器看到本體要改 count,自動升級成「可變借用」捕獲;兩次呼叫都作用在同一個 count 上" },
        { text: "編譯錯誤:要在閉包裡修改捕獲的變數,必須自己寫出借用,例如 || { let r = &mut count; *r += 1; }" },
        { text: "印出 0——閉包捕獲的是 count 的一份複本,改的是複本,外面的 count 不受影響" },
        { text: "編譯錯誤:count 已經被閉包可變借用,最後一行的 println! 不能再讀它" },
      ],
      answer: 0,
      explanation: `編譯器依本體的需求選捕獲方式:這裡本體寫了 count += 1,唯讀不夠用,於是自動升級成可變借用。你不需要(也不能)自己寫 &mut count,那是編譯器的工作。兩次呼叫改的都是同一個 count,所以印出 2。
「捕獲的是複本」是從 C# 值型別直覺帶過來的誤解——Rust 的閉包只有在你寫 move、而且捕獲的型別是 Copy 時才會發生複製,預設是借用。最後一行能讀 count 是因為 NLL(非詞法生命週期):inc 的最後一次使用是上一行,可變借用到那裡就結束了,println! 再借用完全合法。這也是為什麼把 println! 移到兩次 inc() 之間反而會編譯失敗。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let mut count = 0;", note: "count 必須宣告成 mut——被可變借用的變數本身要是可變的。" },
          { code: "    let mut inc = || count += 1;", note: "本體要改 count,唯讀捕獲不夠用,編譯器自動改用可變借用。你不用寫 &mut,那是編譯器推出來的。閉包變數自己也標了 mut,原因見第 8 題。" },
          { code: "    inc();", note: "第一次呼叫,透過閉包裡那份可變借用把 count 改成 1。" },
          { code: "    inc();", note: "第二次呼叫,count 變成 2。兩次改的是同一個 count,不是複本。" },
          { code: "    println!(\"{}\", count);", note: "這是 inc 最後一次使用之後,可變借用已經結束(NLL),所以這裡重新借用 count 來讀完全合法,印出 2。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "把這一行的位置換一下就能驗證借用規則還在:把 println! 搬到兩次 inc() 中間,立刻報 cannot borrow count as immutable because it is also borrowed as mutable——閉包持有的可變借用和讀取衝突,這正是第 4 題的主題。",
      },
      csharp: `C# 的 int count = 0; Action inc = () => count++; 寫起來一樣自然,而且執行期也真的改到同一個變數(count 被提升到顯示類別的欄位)。差別在檢查:C# 完全不管你在 lambda 還活著的時候對 count 做什麼,兩邊同時讀寫也沒人攔;Rust 把「閉包持有可變借用」視為一次真正的獨佔借用,期間任何其他存取都會被編譯器擋下——同樣的程式碼,多了一層靜態保證。`,
    },
    {
      id: "2-1-04",
      question: "閉包持有的借用從什麼時候開始算?以下程式碼的結果是?",
      questionCode: "fn main() {\n    let mut list = vec![1, 2, 3];\n    let mut push_it = || list.push(4);\n    println!(\"{:?}\", list);\n    push_it();\n}",
      options: [
        { text: "編譯錯誤:cannot borrow list as immutable because it is also borrowed as mutable——可變借用在「建立閉包」那一刻就開始,一直持續到閉包最後一次被使用為止" },
        { text: "印出 [1, 2, 3] 之後正常結束:閉包還沒被呼叫,借用還沒真正發生" },
        { text: "印出 [1, 2, 3, 4]:push 的效果會先套用" },
        { text: "編譯錯誤:閉包本體裡不能呼叫會修改捕獲變數的方法,要改成回傳新的 Vec" },
      ],
      answer: 0,
      explanation: `這題只考一個時間點:借用什麼時候開始。答案是「建立閉包那一行」,不是「呼叫閉包那一行」。閉包是一個值,它的欄位裡存著那份 &mut list;值一建立,借用就成立了,而且要活到閉包最後一次被使用(這裡是最後一行的 push_it())。夾在中間的 println! 需要一份不可變借用,和獨佔的可變借用直接衝突。
「還沒呼叫所以還不算借用」是最常見的誤解,而且很難自己發現,因為錯誤訊息指的是 println! 那一行,人會直覺去懷疑 println!。修法是調整順序:把讀取移到閉包建立之前,或移到閉包用完之後。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let mut list = vec![1, 2, 3];", note: "list 擁有這個 Vec,宣告成 mut 才能被可變借用。" },
            { code: "    let mut push_it = || list.push(4);", note: "⛔ 衝突的起點:本體要呼叫 push(需要 &mut self),所以閉包以「可變借用」捕獲 list。這份獨佔借用從這一行就開始生效,不是等到呼叫才開始。" },
            { code: "    println!(\"{:?}\", list);", note: "⛔ 編譯失敗:cannot borrow list as immutable because it is also borrowed as mutable。這一行要對 list 建立不可變借用,但上一行的獨佔可變借用還活著。" },
            { code: "    push_it();", note: "正是這一行讓可變借用必須活過 println!,把衝突坐實。如果把這一行刪掉,可變借用在建立後就沒人用了,println! 反而合法。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "記住借用的兩個端點:起點是「閉包被建立」的那一行,終點是「閉包最後一次被使用」的那一行(NLL)。兩點之間,捕獲的變數處於被獨佔狀態。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let mut list = vec![1, 2, 3];", note: "完全不用改。" },
            { code: "    let mut push_it = || list.push(4);", note: "閉包的寫法也完全不用改,可變借用一樣從這裡開始。" },
            { code: "    push_it();", note: "改動處:把呼叫提前。這是閉包的最後一次使用,可變借用在這一行結束。" },
            { code: "    println!(\"{:?}\", list);", note: "改動處:讀取移到借用結束之後,這裡重新對 list 建立不可變借用,完全合法,印出 [1, 2, 3, 4]。" },
            { code: "}", note: "list 離開作用域並釋放。" },
          ],
          outro: "另一種同樣有效的修法是把 println! 移到「建立閉包之前」——借用還沒開始,自然不衝突。兩種修法的共通點都是「錯開借用的存活區間」,而不是改閉包本身。",
        },
      ],
      csharp: `同樣一段邏輯在 C# 完全不會被攔:var list = new List<int>{1,2,3}; Action pushIt = () => list.Add(4); Console.WriteLine(string.Join(",", list)); pushIt(); 順序怎麼排都能跑。代價是另一類 bug 要留到執行期才炸——最經典的就是在 foreach 走訪集合的過程中有個 lambda 去 Add,拋 InvalidOperationException: Collection was modified。Rust 把「誰在什麼區間獨佔這個集合」變成編譯期就能回答的問題,這題的錯誤訊息就是那道防線。`,
    },
    {
      id: "2-1-05",
      question: "move 關鍵字。以下程式碼的結果是?",
      questionCode: "fn main() {\n    let list = vec![1, 2, 3];\n    let print_it = move || println!(\"{:?}\", list);\n    print_it();\n    println!(\"{:?}\", list);\n}",
      options: [
        { text: "編譯錯誤:borrow of moved value: list——move 在「建立閉包」時就把所有權交給閉包了,原變數從那一行之後就不能再用" },
        { text: "印出兩行 [1, 2, 3]:move 只是提示,本體只讀不寫,編譯器仍然會選擇借用" },
        { text: "印出一行 [1, 2, 3] 後編譯錯誤:所有權是在「呼叫閉包」時才轉移的" },
        { text: "編譯錯誤:move 只能用在要送進另一個執行緒的閉包上,單執行緒程式不能寫 move" },
      ],
      answer: 0,
      explanation: `move 的意思很單純:不要推斷了,把本體用到的每一個外部變數,一律以「取得所有權」的方式捕獲。Vec 不是 Copy,所以 list 的所有權在建立閉包那一行就搬進閉包裡,原變數立刻失效,最後一行的讀取報 borrow of moved value。
「所有權在呼叫時才轉移」是這題的核心陷阱——閉包是一個值,捕獲發生在「這個值被建立」的時候,和它被呼叫幾次、有沒有被呼叫都無關。另外 move 也不是執行緒專用語法:它只是所有權的指示,和跨執行緒沒有語法上的關係(2-7 會看到為什麼在那個情境幾乎一定要寫它)。要同時保留兩邊,就把閉包本體改成用複本(捕獲 list.clone()),或者根本不寫 move。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let list = vec![1, 2, 3];", note: "list 擁有這個 Vec。" },
            { code: "    let print_it = move || println!(\"{:?}\", list);", note: "⛔ 所有權在這一行就轉移了:move 要求「取得所有權」式的捕獲,Vec 不是 Copy,所以整個 Vec 被搬進閉包的欄位裡。從這一行之後,原本的 list 就是一個已經被移走的變數。" },
            { code: "    print_it();", note: "閉包自己現在擁有那個 Vec,印出 [1, 2, 3] 完全沒問題。呼叫本身沒有任何錯。" },
            { code: "    println!(\"{:?}\", list);", note: "⛔ 編譯失敗:borrow of moved value: list。要印出來得先借用它,但它的所有權已經不在這裡了。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "注意錯誤指的是「借用一個已經被移走的值」,不是「閉包有問題」。閉包本身從頭到尾都是對的——問題出在有人以為自己還持有那份資料。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let list = vec![1, 2, 3];", note: "完全不用改。" },
            { code: "    let print_it = || println!(\"{:?}\", list);", note: "改動處:拿掉 move。本體只需要讀,編譯器就選不可變借用,list 的所有權留在原地。" },
            { code: "    print_it();", note: "透過借用讀取,印出 [1, 2, 3]。" },
            { code: "    println!(\"{:?}\", list);", note: "改動處的效果:原變數仍然有效,再印一次 [1, 2, 3]。整體輸出兩行。" },
            { code: "}", note: "閉包先離開作用域(借用結束),list 後釋放。" },
          ],
          outro: "如果情境要求非 move 不可(例如閉包要活得比 list 的作用域久,見第 19 題),但你又想留一份給原來的地方用,做法是讓閉包捕獲複本:先 let copy = list.clone();,再寫 move || println!(\"{:?}\", copy);——付一次配置成本,換兩邊各自獨立。",
        },
      ],
      csharp: `C# 沒有對應語法,因為它沒有這個選擇權:lambda 捕獲參考型別變數時永遠是共用同一個物件,捕獲值型別時共用的是提升後的那個欄位。換句話說 C# 永遠是「Rust 不加 move」那一側,而且 GC 讓「誰擁有這份資料」這個問題不存在。給 C# 開發者的心智模型:move 不是效能開關,而是回答「這份資料之後歸誰」的宣告;寫了 move 就等於說「原來的地方不要了」。`,
    },
    {
      id: "2-1-06",
      question: "設計選擇:下面兩種寫法,原變數在閉包建立之後都不再被使用。關於這兩者的差異,正確的說法是?",
      questionCode: "// 不加 move\nlet a = vec![1, 2, 3];\nlet show_a = || println!(\"{:?}\", a);\nshow_a();\n\n// 加了 move\nlet b = vec![1, 2, 3];\nlet show_b = move || println!(\"{:?}\", b);\nshow_b();",
      options: [
        { text: "兩者都能編譯、行為也相同。差別只在捕獲方式:不加 move 時閉包裡存的是一根指向 Vec 的指標,加了 move 則是把 Vec 的三個欄位(指標、長度、容量)搬進閉包。既然原變數之後沒人用,對呼叫端沒有任何可觀察的差異,也沒有額外的執行期成本" },
        { text: "加了 move 的版本會把 Vec 的元素複製一份到閉包裡,所以比不加 move 慢,除非跨執行緒否則不該寫" },
        { text: "不加 move 的版本編譯不過:閉包要能被呼叫,就必須真正擁有它用到的資料" },
        { text: "加了 move 之後 show_b 只能呼叫一次,因為所有權已經轉移進去了" },
      ],
      answer: 0,
      explanation: `move 決定的是「捕獲什麼」,不是「執行期做什麼額外工作」。不加 move,閉包的匿名 struct 裡有一個 &Vec<i32> 欄位;加了 move,欄位變成 Vec<i32> 本身(那三個 usize 大小的欄位被搬進去,heap 上的元素一個都沒動)。兩者都是一次 stack 上的位移,沒有配置、沒有 GC、沒有虛擬呼叫——「加 move 會複製元素所以比較慢」是把 move 誤讀成 clone 才會有的結論。
另一個要澄清的是「加了 move 就只能呼叫一次」:能不能重複呼叫取決於「本體怎麼用捕獲的東西」,不是取決於 move。這裡兩個本體都只是把 Vec 拿去印,所以都是 Fn,呼叫幾次都行(對照第 10 題:本體把捕獲的值移出去,才會變成只能呼叫一次)。
實務上的取捨:原變數之後還要用就別寫 move;閉包要活得比原變數的作用域久(存進 struct、當回傳值、送進另一個執行緒)就必須寫 move。兩種需求都不成立時,兩種寫法等價,選讀起來意圖最清楚的那個。`,
      walkthrough: {
        label: "⊕ Both versions in one program — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。兩種寫法放在同一支程式裡直接對照。" },
          { code: "    let a = vec![1, 2, 3];", note: "第一份資料。" },
          { code: "    let show_a = || println!(\"借用版:{:?}\", a);", note: "不加 move:本體只需要讀,編譯器選不可變借用,閉包裡存的是一根指向 a 的指標。a 的所有權留在原地。" },
          { code: "    show_a();", note: "印出「借用版:[1, 2, 3]」。" },
          { code: "    show_a();", note: "再印一次——唯讀捕獲可以重複呼叫,和有沒有 move 無關。" },
          { code: "", note: "" },
          { code: "    let b = vec![1, 2, 3];", note: "第二份資料,內容刻意相同以便對照。" },
          { code: "    let show_b = move || println!(\"move 版:{:?}\", b);", note: "加了 move:Vec 的所有權搬進閉包的欄位。搬的是「指標、長度、容量」這三個值,heap 上的 1、2、3 一個都沒有被複製——move 不是 clone。" },
          { code: "    show_b();", note: "印出「move 版:[1, 2, 3]」。閉包用自己欄位裡的那個 Vec。" },
          { code: "    show_b();", note: "同樣可以再呼叫一次:本體只是把 b 拿去印(需要 &self.b),沒有把它移出去,所以這個閉包是 Fn 不是 FnOnce。" },
          { code: "    println!(\"a 還在:{:?}\", a);", note: "不加 move 的那一邊,原變數仍然有效——這就是兩者唯一真正的差異,而題目的前提是「原變數之後不再使用」,所以這個差異在原情境裡觀察不到。" },
          { code: "}", note: "show_b 離開作用域時,它擁有的那個 Vec 跟著被釋放;a 則由自己釋放。" },
        ],
        outro: "判斷準則兩句話:原變數之後還要用 → 不能寫 move;閉包要活得比原變數的作用域久 → 必須寫 move。兩者都不成立時,兩種寫法在機器碼層級幾乎沒有差別,挑意圖最清楚的寫法就好。",
      },
      csharp: `C# 沒有這個選擇權,語言替你決定了:捕獲一律是「共用同一個提升後的變數」,而且捕獲的東西被 GC 保證活著。好處是永遠不用想;壞處是你也永遠不能表達「這份資料從現在起歸這個 lambda 所有,別人別再碰」這個意圖——想模擬只能靠紀律(把變數設成 null、或刻意複製一份)而不是語言保證。Rust 的 move 就是把這個意圖變成語法,並且讓編譯器替你執行它。`,
    },
    {
      id: "2-1-07",
      question: "Fn 這個 bound 到底要求什麼?以下程式碼的結果是?",
      questionCode: "fn call_twice<F: Fn()>(f: F) {\n    f();\n    f();\n}\n\nfn main() {\n    let name = String::from(\"Rust\");\n    let greet = || println!(\"Hello, {}!\", name);\n    call_twice(greet);\n    println!(\"{}\", name);\n}",
      options: [
        { text: "印出兩行 Hello, Rust! 再印出 Rust——Fn 要求的是「呼叫時只需要 &self」,也就是不修改也不消耗捕獲的東西;唯讀捕獲完全符合" },
        { text: "編譯錯誤:greet 捕獲了外部的 name,只有完全不捕獲任何東西的閉包才滿足 Fn" },
        { text: "編譯錯誤:greet 被按值傳進 call_twice,所有權已經轉移,最後一行不能再用 name" },
        { text: "印出一行 Hello, Rust! ——滿足 Fn 的閉包在第一次呼叫後就被消耗掉了" },
      ],
      answer: 0,
      explanation: `Fn 這個 bound 常被讀成「不能有捕獲」,其實它要求的是呼叫方式:滿足 Fn 的閉包,呼叫時只需要對自己建立一個不可變借用(&self),因此可以重複呼叫,而且過程中不會改動或消耗捕獲的東西。這裡 greet 以不可變借用捕獲 name,本體只把它拿去印,完全符合。
「所有權轉移所以最後一行不能用 name」這個選項值得拆開看:greet 這個閉包值確實被按值搬進 call_twice 了,但搬走的是閉包,不是 name。閉包裡存的只是一份指向 name 的不可變借用;call_twice 結束時閉包被釋放,借用跟著結束,於是最後一行讀 name 完全合法。分清「閉包這個值的所有權」和「被捕獲變數的所有權」是這一課的關鍵。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn call_twice<F: Fn()>(f: F) {", note: "泛型參數 F 加上 Fn() 這個 bound:要求傳進來的東西「不帶參數、不回傳值,而且可以用不可變借用重複呼叫」。f 按值收下。" },
          { code: "    f();", note: "第一次呼叫。因為 bound 是 Fn,這裡實際發生的是用 &f 去呼叫,f 本身不會被消耗。" },
          { code: "    f();", note: "第二次呼叫——正是 Fn 這個 bound 讓這一行合法。如果 bound 只有 FnOnce,這裡就會報錯(第 13 題)。" },
          { code: "}", note: "函式結束,f 這個閉包值在這裡被釋放,它持有的借用也跟著結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let name = String::from(\"Rust\");", note: "name 擁有這個 String。" },
          { code: "    let greet = || println!(\"Hello, {}!\", name);", note: "本體只需要讀 name,所以捕獲方式是不可變借用。這是一個「有捕獲」但仍然滿足 Fn 的閉包——Fn 管的是呼叫方式,不是有沒有捕獲。" },
          { code: "    call_twice(greet);", note: "把閉包按值傳進去。搬走的是閉包這個值,不是 name;閉包裡那份對 name 的不可變借用仍然指向 main 裡的 name。印出兩行 Hello, Rust!。" },
          { code: "    println!(\"{}\", name);", note: "call_twice 已經結束、閉包已經被釋放,借用結束,name 完全有效,印出 Rust。" },
          { code: "}", note: "name 離開作用域並釋放。" },
        ],
        outro: "把 Fn 讀成一句話:「呼叫我不需要獨佔我,也不會用掉我」。完全不捕獲的閉包當然滿足它,但唯讀捕獲的閉包同樣滿足——這才是它真正的門檻。",
      },
      csharp: `最接近的對照是 Func<T,R> / Action:拿到一個委派實例後想呼叫幾次就幾次,沒有人會問你「這次呼叫會不會把它用掉」。Rust 的 Fn 對應的就是這個直覺,差別在 C# 把它當成唯一可能,Rust 則把它變成三種可能裡「要求最嚴、能力最強」的那一個——要求最嚴是指閉包本體不能改也不能吃掉捕獲的東西,能力最強是指呼叫端拿到之後幾乎什麼都能做。`,
    },
    {
      id: "2-1-08",
      question: "以下程式碼的結果是?",
      questionCode: "fn main() {\n    let mut log = Vec::new();\n    let record = |msg: &str| log.push(msg.to_string());\n    record(\"start\");\n    record(\"done\");\n    println!(\"{:?}\", log);\n}",
      options: [
        { text: "編譯錯誤:cannot borrow record as mutable, as it is not declared as mutable——閉包要改捕獲的 log,呼叫它就得對「閉包自己」建立可變借用,所以閉包變數也要宣告成 let mut record" },
        { text: "正常印出 [\"start\", \"done\"]:log 已經宣告成 mut 了,閉包變數不需要再標一次" },
        { text: "正常印出 [\"start\", \"done\"]:只有 move 閉包才需要把閉包變數宣告成 mut" },
        { text: "編譯錯誤:log 的型別推不出來,必須寫成 let mut log: Vec<String> = Vec::new();" },
      ],
      answer: 0,
      explanation: `這是初學者最常卡住的一個位置:mut 要標兩次,而且兩次的理由不同。log 標 mut,是因為它會被可變借用;record 也要標 mut,是因為「會修改捕獲內容的閉包」在被呼叫時,需要對閉包本身建立可變借用(呼叫它走的是 call_mut(&mut self, ...) 這條路),而一個沒有宣告成 mut 的變數不能被可變借用。
「log 已經是 mut 了就夠」正是這題要破的迷思:那兩個 mut 修飾的是兩個不同的變數,各自回答各自的問題。「只有 move 閉包才要標」也不對,和 move 沒有關係。至於型別推斷那一項:Vec::new() 的元素型別可以從 log.push(msg.to_string()) 反推成 String,這裡完全不需要手寫型別,那不是錯誤的原因。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let mut log = Vec::new();", note: "log 標 mut 沒有問題——它待會要被閉包可變借用。元素型別由後面的 push 反推成 String,不用手寫。" },
            { code: "    let record = |msg: &str| log.push(msg.to_string());", note: "⛔ 問題的起點:本體呼叫 log.push,需要對 log 的可變借用,所以這是一個「會修改捕獲內容」的閉包。但 record 自己沒有宣告成 mut。" },
            { code: "    record(\"start\");", note: "⛔ 編譯失敗:cannot borrow record as mutable, as it is not declared as mutable。呼叫這種閉包需要對 record 建立可變借用,而不可變的綁定給不出來。" },
            { code: "    record(\"done\");", note: "同樣的錯誤。修掉上面的宣告之後這一行就沒事了。" },
            { code: "    println!(\"{:?}\", log);", note: "因為前面編譯失敗而沒有機會執行。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "錯誤訊息指的是 record 不是 log,這是很有用的線索:編譯器抱怨的對象是「閉包這個變數」,代表問題出在閉包的可變性,不是被捕獲資料的可變性。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let mut log = Vec::new();", note: "第一個 mut:log 會被閉包可變借用,所以它本身要可變。" },
            { code: "    let mut record = |msg: &str| log.push(msg.to_string());", note: "改動處:第二個 mut。閉包會修改捕獲的內容,呼叫它需要對閉包自己建立可變借用,所以這個綁定也必須是可變的。兩個 mut 回答的是兩個不同的問題。" },
            { code: "    record(\"start\");", note: "呼叫成功,log 變成 [\"start\"]。" },
            { code: "    record(\"done\");", note: "再呼叫一次,log 變成 [\"start\", \"done\"]——狀態在多次呼叫之間累積(第 9 題的主題)。" },
            { code: "    println!(\"{:?}\", log);", note: "record 最後一次使用在上一行,可變借用到那裡結束,這裡重新借用 log 來讀,印出 [\"start\", \"done\"]。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "記憶法:閉包本體會「改」外面的東西 → 閉包變數要 mut;閉包本體會「吃掉」外面的東西 → 閉包只能呼叫一次(第 10 題)。兩個現象都是從「本體做了什麼」推出來的結論。",
        },
      ],
      csharp: `C# 這邊完全沒有對應的負擔:var log = new List<string>(); Action<string> record = m => log.Add(m); 之後直接 record(\"start\") 就好,委派變數本身從來不需要任何修飾詞。原因是 C# 的委派呼叫永遠是「透過一個參考去呼叫」,不區分呼叫過程會不會改動捕獲狀態。Rust 把這個區別做成三個不同的 trait(呼叫時分別需要 &self、&mut self、self),所以呼叫端必須先滿足對應的可變性——mut 要標兩次就是這個設計的直接後果。`,
    },
    {
      id: "2-1-09",
      question: "閉包的捕獲狀態會跨呼叫累積嗎?以下程式碼的結果是?",
      questionCode: "fn main() {\n    let mut total = 0;\n    let mut accumulate = |x: i32| {\n        total += x;\n        total\n    };\n    println!(\"{}\", accumulate(10));\n    println!(\"{}\", accumulate(5));\n    println!(\"{}\", accumulate(1));\n}",
      options: [
        { text: "依序印出 10、15、16——閉包持有對 total 的可變借用,三次呼叫改的都是同一個 total,狀態因此累積下來" },
        { text: "依序印出 10、5、1——每次呼叫都是一次獨立的求值,total 每次都從 0 開始" },
        { text: "依序印出 0、10、15——回傳的是這次相加「之前」的值" },
        { text: "編譯錯誤:total 已經被閉包可變借用,不能在閉包本體裡把它當回傳值讀出來" },
      ],
      answer: 0,
      explanation: `閉包不是純函式:它的匿名 struct 裡帶著捕獲的環境,而這個環境在多次呼叫之間是同一份。這裡捕獲的是 &mut total,三次呼叫依序把 total 改成 10、15、16,每次回傳當下的值。
「每次都從 0 開始」是把閉包當成數學函式才會有的直覺——那是不捕獲任何東西的閉包才成立。最後一個選項也值得澄清:閉包本體裡對 total 的讀寫,走的都是那一份 &mut 借用,借用規則只禁止「同一時間存在其他借用」,不禁止你透過可變借用去讀值。真正會被擋下的是「閉包還活著的時候,外面另外去讀 total」,那是第 4 題。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let mut total = 0;", note: "累加器的真身在這裡,型別由後面的 += x 推斷為 i32。" },
          { code: "    let mut accumulate = |x: i32| {", note: "閉包有一個參數 x,本體用大括號包住多行。因為本體要改 total,捕獲方式是可變借用;因為要呼叫這種閉包,綁定也標了 mut。" },
          { code: "        total += x;", note: "透過捕獲的可變借用修改外部的 total——這一行讓狀態跨呼叫延續。" },
          { code: "        total", note: "本體最後一個運算式沒有分號,成為這次呼叫的回傳值(型別 i32,由推斷得出)。" },
          { code: "    };", note: "閉包定義結束。從這一行開始,total 處於被閉包獨佔借用的狀態。" },
          { code: "    println!(\"{}\", accumulate(10));", note: "第一次呼叫:total 從 0 變成 10,回傳 10,印出 10。" },
          { code: "    println!(\"{}\", accumulate(5));", note: "第二次呼叫:同一個 total 從 10 變成 15,印出 15。這就是「狀態累積」。" },
          { code: "    println!(\"{}\", accumulate(1));", note: "第三次呼叫:total 變成 16,印出 16。三行輸出依序是 10、15、16。" },
          { code: "}", note: "accumulate 先離開作用域(借用結束),total 後釋放。" },
        ],
        outro: "想在三次呼叫之後看看 total 的最終值,把 println!(\"{}\", total); 加在最後一行就可以——那時 accumulate 已經沒人用了,可變借用結束(NLL),讀取合法。但把它插在兩次呼叫「之間」就會編譯失敗。",
      },
      csharp: `C# 的 lambda 也能做到同樣的累積效果——int total = 0; Func<int,int> acc = x => { total += x; return total; }; 三次呼叫同樣印出 10、15、16,因為 total 被提升到顯示類別的欄位,lambda 和外層方法共用它。真正的差異在函式參數:C# 想把「一個可以持續累加的東西」交給別的方法,得傳委派或包成物件,不能用 ref int(ref 參數不能被 lambda 捕獲);Rust 則是把整個閉包(含它捕獲的 &mut)當成一個值傳出去,接收端宣告 FnMut bound 就能繼續累加。`,
    },
    {
      id: "2-1-10",
      question: "以下程式碼的結果是?",
      questionCode: "fn main() {\n    let name = String::from(\"Rust\");\n    let consume = move || {\n        let owned = name;\n        println!(\"{}\", owned);\n    };\n    consume();\n    consume();\n}",
      options: [
        { text: "編譯錯誤:use of moved value: consume——本體把捕獲的 name 移了出去,這種閉包呼叫一次就會把自己耗掉,不能呼叫第二次" },
        { text: "正常印出兩行 Rust:move 只是把 name 搬進閉包,閉包本身仍然可以重複呼叫" },
        { text: "編譯錯誤;而且就算把 name 換成 i32 這種 Copy 型別,一樣只能呼叫一次" },
        { text: "第一次呼叫正常,第二次呼叫時執行期 panic:存取已經被釋放的記憶體" },
      ],
      answer: 0,
      explanation: `決定「能呼叫幾次」的不是 move,而是本體怎麼處理捕獲的東西。這裡 let owned = name; 把捕獲的 String 整個移出閉包,閉包自己被掏空了——這種閉包只能提供「按值接收自己」的呼叫方式,呼叫一次就等於把整個閉包消耗掉,所以第二次呼叫報 use of moved value: consume。
「move 只是搬進來,閉包還是能重複呼叫」正好把因果搞反了:第 6 題那個 move 閉包就能呼叫很多次,因為它的本體只是借來印。把 name 換成 i32 也不會有同樣的限制:i32 是 Copy,let owned = n; 只會複製一份,閉包的欄位原封不動,所以仍然可以重複呼叫。至於「執行期 panic」——Rust 的立場永遠是編譯不過,不會讓程式帶著這種錯誤跑起來。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let name = String::from(\"Rust\");", note: "name 擁有這個 String(不是 Copy 型別)。" },
            { code: "    let consume = move || {", note: "move 讓閉包取得 name 的所有權,String 被搬進閉包的欄位裡。單看這一行還沒有任何問題。" },
            { code: "        let owned = name;", note: "⛔ 真正的成因在這裡:本體把捕獲的那個 String 整個移出去給 owned。編譯器的說法是 closure cannot be invoked more than once because it moves the variable name out of its environment。" },
            { code: "        println!(\"{}\", owned);", note: "印出來。這一行沒有問題。" },
            { code: "    };", note: "閉包定義結束。因為本體會把自己的欄位掏空,它只能提供「按值接收自己」的呼叫方式。" },
            { code: "    consume();", note: "第一次呼叫。這一次是合法的,但呼叫的同時整個閉包被消耗掉了——編譯器的註記是 this value implements FnOnce, which causes it to be moved when called。" },
            { code: "    consume();", note: "⛔ 編譯失敗:use of moved value: consume。上一行已經把 consume 用掉了,這裡再用就是使用已被移走的值。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let name = String::from(\"Rust\");", note: "完全不用改。" },
            { code: "    let show = move || println!(\"{}\", name);", note: "改動處:本體不再把捕獲的 String 移出去,只是把它借來印。move 照樣保留(String 的所有權仍然搬進閉包),但閉包從此不會掏空自己,呼叫時只需要 &self。" },
            { code: "    show();", note: "第一次呼叫,印出 Rust,閉包毫髮無傷。" },
            { code: "    show();", note: "第二次呼叫,再印出 Rust。整體輸出兩行。" },
            { code: "}", note: "show 離開作用域,它擁有的那個 String 跟著被釋放。" },
          ],
          outro: "如果本體真的非得取得那份資料的所有權不可(例如要把它傳給某個按值收參數的 API),而你又需要呼叫多次,做法是在本體裡複製一份:move || { let owned = name.clone(); ... }——閉包的欄位保留,每次呼叫各拿一份複本。",
        },
        {
          label: "△ Desugared (what the compiler sees)",
          intro: "錯誤訊息提到了 FnOnce,但你的程式碼裡沒有這個字。原因是閉包本身就是一層糖——編譯器會替它生成一個匿名 struct,再依「本體怎麼用那些欄位」決定實作哪一個 trait:",
          lines: [
            { code: "let consume = move || { let owned = name; println!(\"{}\", owned); };", note: "你寫的這一行。" },
            { code: "", note: "" },
            { code: "// 編譯器大致生成:", note: "以下才是它實際檢查的東西。" },
            { code: "struct AnonClosure { name: String }", note: "捕獲的東西變成匿名 struct 的欄位。因為寫了 move,欄位型別是 String 本身而不是 &String。" },
            { code: "", note: "" },
            { code: "impl FnOnce<()> for AnonClosure {", note: "本體會把 self.name 移出去,所以只能實作到這一層——錯誤訊息裡的 FnOnce 就是從這裡露出來的。" },
            { code: "    type Output = ();", note: "呼叫的回傳型別,對應本體最後沒有回傳任何值。" },
            { code: "    fn call_once(self, _args: ()) -> () {", note: "關鍵在接收者:是 self(按值)而不是 &self。呼叫一次就把整個閉包吃掉,這就是「use of moved value: consume」的來源。" },
            { code: "        let owned = self.name;", note: "你寫的 let owned = name; 展開後是把 struct 的欄位移出來——欄位被掏空,這個 struct 就不完整了,所以不可能再呼叫第二次。" },
            { code: "        println!(\"{}\", owned);", note: "本體的其餘部分原樣照抄。" },
            { code: "    }", note: "call_once 結束。" },
            { code: "}", note: "impl 結束。如果本體只是借來印(√ 的版本),編譯器就能多實作 FnMut 與 Fn,接收者變成 &mut self 與 &self,閉包才有辦法被重複呼叫。" },
          ],
          outro: "一句話總結這個機制:閉包實作哪幾個 trait,完全由本體對捕獲欄位的用法決定——只讀 → Fn、要改 → FnMut、要移走 → 只有 FnOnce。你從來不用自己寫 impl,但錯誤訊息會用這些名字跟你講話。",
        },
      ],
      csharp: `C# 沒有「呼叫一次就耗盡」這個概念:委派實例拿到手就是可以重複叫的,捕獲的物件由 GC 保證活著,也沒有任何語法能表達「這個 lambda 只能被執行一次」。要模擬只能靠執行期紀律(呼叫完設成 null、或用一個 bool 旗標擋第二次),而且忘了做也沒人提醒。Rust 把「這個閉包會吃掉自己的捕獲環境」變成型別層級的事實,誤用在編譯期就結束了——代價是你得學會讀 FnOnce 這個名字。`,
    },
    {
      id: "2-1-11",
      question: "Fn、FnMut、FnOnce 三者是什麼關係?以下程式碼的結果是?",
      questionCode: "fn needs_fn<F: Fn() -> String>(f: F) -> String { f() }\nfn needs_fn_mut<F: FnMut() -> String>(mut f: F) -> String { f() }\nfn needs_fn_once<F: FnOnce() -> String>(f: F) -> String { f() }\n\nfn main() {\n    let word = String::from(\"hi\");\n    let make = || word.clone();\n    println!(\"{}\", needs_fn(make));\n    println!(\"{}\", needs_fn_mut(make));\n    println!(\"{}\", needs_fn_once(make));\n    println!(\"{}\", word);\n}",
      options: [
        { text: "印出四行 hi——三者是階層關係,FnMut 的 supertrait 是 FnOnce、Fn 的 supertrait 是 FnMut,所以滿足 Fn 的閉包自動也滿足另外兩個 bound" },
        { text: "編譯錯誤:一個閉包只會實作三者其中之一,不可能同時滿足三種 bound" },
        { text: "編譯錯誤:make 在第一次按值傳進 needs_fn 時就被 move 走了,後面兩次不能再用" },
        { text: "編譯錯誤:word 被閉包借用之後,最後一行的 println! 不能再讀它" },
      ],
      answer: 0,
      explanation: `三個 trait 不是三選一,而是一條由嚴到寬的階層:Fn 繼承 FnMut、FnMut 繼承 FnOnce。實作了最嚴的 Fn,另外兩個自動成立,所以 make 這種唯讀捕獲的閉包三個 bound 都能過。反過來就不行——只滿足 FnOnce 的閉包塞不進要求 Fn 的位置(第 13 題)。
「第一次就被 move 走」這個選項其實抓到了一個真實的規則(閉包按值傳遞確實會被移動),但這裡有個例外救了它:閉包的 Copy / Clone 是由捕獲內容決定的,make 捕獲的只是一個 &String(參考是 Copy),所以整個閉包也是 Copy,三次傳遞各複製一份。若把 make 改成 move 閉包(捕獲 String 本身),這個選項就會變成正確答案。最後一行能讀 word,是因為閉包的最後一次使用在上一行,不可變借用到那裡就結束了。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn needs_fn<F: Fn() -> String>(f: F) -> String { f() }", note: "要求最嚴的那一層:呼叫時只需要 &f,因此可以重複呼叫、不會改動也不會消耗捕獲的東西。" },
          { code: "fn needs_fn_mut<F: FnMut() -> String>(mut f: F) -> String { f() }", note: "中間那一層:呼叫時需要 &mut f(所以參數要寫 mut f),允許閉包改動捕獲的東西,仍然可以重複呼叫。" },
          { code: "fn needs_fn_once<F: FnOnce() -> String>(f: F) -> String { f() }", note: "最寬鬆的那一層:呼叫時按值接收 f,只保證能呼叫一次。所有閉包都滿足這一層。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let word = String::from(\"hi\");", note: "要被捕獲的資料。" },
          { code: "    let make = || word.clone();", note: "本體只是讀 word 再複製一份出來,所以捕獲方式是不可變借用,呼叫只需要 &self——這是一個 Fn 閉包。" },
          { code: "    println!(\"{}\", needs_fn(make));", note: "傳進要求最嚴的位置,通過,印出 hi。順帶一提:捕獲內容只有一個 &String(參考是 Copy),所以這個閉包本身也是 Copy,傳進去的是一份複本,make 沒有被移走。" },
          { code: "    println!(\"{}\", needs_fn_mut(make));", note: "同一個閉包傳進要求 FnMut 的位置也通過——因為 Fn 的 supertrait 是 FnMut,實作 Fn 就自動有 FnMut。再印一次 hi。" },
          { code: "    println!(\"{}\", needs_fn_once(make));", note: "傳進最寬鬆的位置當然也通過(FnMut 的 supertrait 是 FnOnce)。第三行 hi。" },
          { code: "    println!(\"{}\", word);", note: "make 的最後一次使用在上一行,它持有的不可變借用到那裡結束,這裡讀 word 完全合法,印出第四行 hi。" },
          { code: "}", note: "word 離開作用域並釋放。" },
        ],
        outro: "方向要記牢:Fn ⊂ FnMut ⊂ FnOnce(能力由強到弱、集合由小到大)。所以「宣告參數 bound」時要挑你真正需要的最寬鬆那一個——只呼叫一次就寫 FnOnce,呼叫端的選擇最多;非得重複呼叫且不能改狀態才寫 Fn。",
      },
      csharp: `C# 的 Func 與 Action 之間沒有這種階層,它們只是簽名不同的兩個委派型別(有沒有回傳值),而且彼此不能互相指派。C# 真正對應「呼叫語意分層」的東西一個都沒有——所有委派都是「可重複呼叫、不消耗自己」,等於全部停在 Rust 的 Fn 那一層。Rust 多出來的那兩層,換來的是「只能跑一次的回呼」這種需求可以被型別直接表達,不必靠註解或執行期檢查。`,
    },
    {
      id: "2-1-12",
      question: "具名函式可以傳進要求閉包的地方嗎?以下程式碼的結果是?",
      questionCode: "fn shout(s: &str) -> String {\n    s.to_uppercase()\n}\n\nfn apply<F: Fn(&str) -> String>(f: F, input: &str) -> String {\n    f(input)\n}\n\nfn main() {\n    println!(\"{}\", apply(shout, \"rust\"));\n    println!(\"{}\", apply(|s| format!(\"<{}>\", s), \"rust\"));\n}",
      options: [
        { text: "印出 RUST 與 <rust>——具名函式也實作了 Fn / FnMut / FnOnce,所以它和閉包可以傳進同一個參數位置" },
        { text: "編譯錯誤:apply 的參數只收閉包,具名函式要先包一層 |s| shout(s) 才能傳進去" },
        { text: "編譯錯誤:函式名稱本身不是值,要寫成 apply(&shout, \"rust\") 取位址才行" },
        { text: "編譯錯誤:Fn(&str) -> String 這種圓括號寫法只能用在 dyn 上,泛型 bound 必須寫成 Fn<(&str,), Output = String>" },
      ],
      answer: 0,
      explanation: `每一個具名函式都有自己的「函式項型別」(一個零大小的匿名型別),而標準函式庫替這種型別實作了三個 Fn 系 trait。所以 shout 可以直接當值傳進要求 Fn(&str) -> String 的位置,和閉包完全平等——差別只在具名函式不捕獲任何環境。
「要先包一層 |s| shout(s)」是很多人習慣性的多此一舉,能編譯但完全沒必要,還多產生一個閉包型別。「要加 & 取位址」也不對:函式名稱在運算式位置本來就是一個值;真的需要函式指標時型別是 fn(&str) -> String(小寫 fn),同樣不需要 &。最後那個選項則是把語法糖當成限制:Fn(&str) -> String 就是 Fn<(&str,), Output = String> 的糖,兩者都能寫在泛型 bound 上,只是後者要開 unstable 功能才寫得出來,平常沒人這樣寫。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn shout(s: &str) -> String {", note: "一個普通的具名函式。它有一個「函式項型別」——零大小、連名字都印不出來,但標準函式庫替它實作了 Fn、FnMut、FnOnce 三個 trait。" },
          { code: "    s.to_uppercase()", note: "回傳全大寫的新 String。" },
          { code: "}", note: "函式結束。" },
          { code: "", note: "" },
          { code: "fn apply<F: Fn(&str) -> String>(f: F, input: &str) -> String {", note: "bound 寫的是 Fn(...) 的圓括號糖,展開後是 Fn<(&str,), Output = String>。注意它只說「能用不可變借用重複呼叫,收一個 &str 回傳 String」,完全沒有規定 F 必須是閉包。" },
          { code: "    f(input)", note: "呼叫傳進來的東西。不論它是閉包還是具名函式,這裡的寫法都一樣。" },
          { code: "}", note: "函式結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    println!(\"{}\", apply(shout, \"rust\"));", note: "直接把函式名稱當值傳進去,不用 &、不用包一層閉包。印出 RUST。" },
          { code: "    println!(\"{}\", apply(|s| format!(\"<{}>\", s), \"rust\"));", note: "換成閉包,同一個參數位置照收。印出 <rust>。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "反過來的方向要注意:不捕獲任何環境的閉包可以被強制轉型成函式指標 fn(&str) -> String,但只要捕獲了東西就不行(它得帶著那些欄位,不可能塞進一根指標裡)。所以「閉包 → 函式指標」有條件,「函式 → Fn bound」永遠成立。",
      },
      csharp: `完全對應 C# 的方法群組轉換:寫 Func<string,string> f = Shout; 就能把方法名直接指派給委派,不用手動包成 s => Shout(s)。差別在實作代價——C# 每次方法群組轉換都會建立一個委派物件(有 Target 與 MethodPtr 兩個欄位,而且早期版本每次都重新配置);Rust 的函式項型別是零大小的,傳進泛型參數之後會被單態化成一次直接呼叫,連間接跳躍都不會有。`,
    },
    {
      id: "2-1-13",
      question: "以下程式碼的結果是?",
      questionCode: "fn run_twice(f: impl Fn()) {\n    f();\n    f();\n}\n\nfn main() {\n    let data = String::from(\"payload\");\n    let send = move || {\n        let owned = data;\n        println!(\"送出 {}\", owned);\n    };\n    run_twice(send);\n}",
      options: [
        { text: "編譯錯誤:expected a closure that implements the Fn trait, but this closure only implements FnOnce——本體把捕獲的 data 移了出去,這個閉包只能呼叫一次,滿足不了 impl Fn() 這個要求" },
        { text: "正常印出兩行:impl Fn() 只要求「能被當成函式呼叫」,至於呼叫幾次是 run_twice 自己的事" },
        { text: "編譯錯誤,但把 run_twice 的參數改成 impl FnMut() 就能解決" },
        { text: "正常編譯,第一次印出「送出 payload」,第二次印出「送出 」(空字串)" },
      ],
      answer: 0,
      explanation: `impl Fn() 是 <F: Fn()> 的語法糖,它的要求很具體:這個閉包必須能用 &self 反覆呼叫。而 send 的本體寫了 let owned = data;,把捕獲的 String 移了出去,只提供「按值接收自己」的呼叫方式——階層方向不對,編譯器在呼叫端就擋下,錯誤碼是 E0525。
「改成 impl FnMut() 就好」是最值得拆解的干擾項:那是把方向記反了。階層是 Fn ⊂ FnMut ⊂ FnOnce,放寬到 FnMut 仍然比 FnOnce 嚴,一樣過不了;真的要收這種閉包,參數得寫成 impl FnOnce(),而且 run_twice 就只能呼叫它一次——這正是 bound 在替你把設計講清楚。至於「第二次印出空字串」:Rust 不會讓被移走的值留下一個空殼給你讀,這種情況一律是編譯期擋下。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn run_twice(f: impl Fn()) {", note: "impl Fn() 是 <F: Fn()> 的語法糖。它要求傳進來的東西能用不可變借用反覆呼叫。" },
            { code: "    f();", note: "第一次呼叫。" },
            { code: "    f();", note: "第二次呼叫——正是這一行讓 Fn 這個 bound 有存在的必要。" },
            { code: "}", note: "函式結束。這個函式本身完全正確,問題出在呼叫端傳了不符合 bound 的東西。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let data = String::from(\"payload\");", note: "要被捕獲的資料。" },
            { code: "    let send = move || {", note: "⛔ 這個閉包的問題起點。move 把 String 搬進閉包欄位——這一步本身沒錯。" },
            { code: "        let owned = data;", note: "⛔ 真正的成因:本體把捕獲的欄位整個移出去,所以這個閉包只能被消耗式地呼叫一次,編譯器的判定是 this closure implements FnOnce, not Fn。" },
            { code: "        println!(\"送出 {}\", owned);", note: "本體的其餘部分沒有問題。" },
            { code: "    };", note: "閉包定義結束。" },
            { code: "    run_twice(send);", note: "⛔ 編譯失敗:expected a closure that implements the Fn trait, but this closure only implements FnOnce(E0525)。錯誤指向的是閉包定義,因為要改的是那裡。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "注意這是「呼叫端違約」而不是「函式寫錯」:run_twice 誠實地宣告了它要呼叫兩次,bound 就是那個承諾的書面形式。改 bound 之前先問自己——函式真的只需要呼叫一次嗎?",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn run_twice(f: impl Fn()) {", note: "函式簽名完全不用改,它的需求是合理的。" },
            { code: "    f();", note: "第一次呼叫。" },
            { code: "    f();", note: "第二次呼叫。" },
            { code: "}", note: "函式結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let data = String::from(\"payload\");", note: "完全不用改。" },
            { code: "    let send = move || println!(\"送出 {}\", data);", note: "改動處:本體不再把捕獲的欄位移出去,只是借來印。move 保留(閉包仍然擁有那個 String),但呼叫只需要 &self,於是這個閉包同時滿足 Fn、FnMut、FnOnce。" },
            { code: "    run_twice(send);", note: "bound 通過,印出兩行「送出 payload」。" },
            { code: "}", note: "main 結束,send 已被移進 run_twice 並在那裡釋放。" },
          ],
          outro: "另一條路是承認需求變了:把 run_twice 的參數改成 impl FnOnce() 並且只呼叫一次。選哪一條取決於語意——「這個回呼會被觸發很多次」和「這個回呼只會被觸發一次」是兩種不同的 API,bound 讓你在簽名上就說清楚。",
        },
        {
          label: "△ Desugared (what the compiler sees)",
          intro: "錯誤訊息拿 Fn 和 FnOnce 兩個名字在比較,但你的程式碼裡只寫過 Fn。這兩個名字的差別其實只有一個地方——呼叫時「怎麼接收自己」:",
          lines: [
            { code: "fn run_twice(f: impl Fn()) { ... }", note: "你寫的簽名。impl Fn() 展開後是 fn run_twice<F: Fn()>(f: F)。" },
            { code: "", note: "" },
            { code: "// 標準函式庫裡這三個 trait 的核心(簡化過):", note: "三者的方法名不同、接收者也不同,這就是全部的差異。" },
            { code: "trait FnOnce<Args> { type Output; fn call_once(self, args: Args) -> Self::Output; }", note: "按值接收 self:呼叫一次就把閉包吃掉。門檻最低,所有閉包都實作它。" },
            { code: "trait FnMut<Args>: FnOnce<Args> { fn call_mut(&mut self, args: Args) -> Self::Output; }", note: "按可變借用接收:可以重複呼叫,而且允許改動捕獲的欄位。冒號後面的 FnOnce 是 supertrait——實作 FnMut 就必須也實作 FnOnce。" },
            { code: "trait Fn<Args>: FnMut<Args> { fn call(&self, args: Args) -> Self::Output; }", note: "按不可變借用接收:可以重複呼叫且不改動任何欄位。階層最頂端,自動滿足另外兩個。" },
            { code: "", note: "" },
            { code: "// run_twice 本體裡的 f() 實際上是:", note: "把糖拿掉之後就看得出衝突在哪。" },
            { code: "Fn::call(&f, ());", note: "要用 &f 呼叫,f 就必須實作 Fn。而 send 的本體把欄位移了出去,它只能提供 call_once(self, ...),提供不出 call(&self, ...)——這就是 expected Fn, but only implements FnOnce 的完整意思。" },
          ],
          outro: "順帶一提圓括號那層糖:Fn() 是 Fn<(), Output = ()> 的簡寫,Fn(i32) -> bool 是 Fn<(i32,), Output = bool> 的簡寫。參數被包成一個 tuple,回傳值變成關聯型別——平常完全不用寫這種形式,但看到錯誤訊息印出 Fn<(i32,), Output = bool> 時要知道那是同一件事。",
        },
      ],
      csharp: `C# 只有一種呼叫語意:拿到 Action 就是可以叫很多次,沒有辦法在型別上表達「這個回呼只能跑一次」。想表達就只能寫在文件註解裡,或在執行期用旗標擋——典型的例子是 IProgress 的回呼、或者 TaskCompletionSource.SetResult 第二次呼叫直接拋 InvalidOperationException。Rust 把「這個回呼會被觸發幾次」提升成簽名的一部分:寫 impl FnOnce() 的 API 等於在型別上宣告「我只會觸發一次」,呼叫端因此可以安心交出所有權。`,
    },
    {
      id: "2-1-14",
      question: "把閉包當回傳值。以下程式碼的結果是?",
      questionCode: "fn adder(n: i32) -> impl Fn(i32) -> i32 {\n    move |x| x + n\n}\n\nfn main() {\n    let add5 = adder(5);\n    let add10 = adder(10);\n    println!(\"{} {}\", add5(1), add10(1));\n}",
      options: [
        { text: "印出 6 11——impl Fn(i32) -> i32 的意思是「回傳某一個實作了這個 trait 的具體型別,型別是什麼由我決定、你不用知道」;兩次呼叫回傳的是同一個閉包型別,只是各自捕獲了不同的 n" },
        { text: "編譯錯誤:n 是 adder 的參數,函式結束就消失了,閉包不可能捕獲它" },
        { text: "印出 6 11,但 adder 每呼叫一次就會在 heap 上配置一個閉包物件" },
        { text: "編譯錯誤:回傳閉包一定要寫成 Box<dyn Fn(i32) -> i32>,impl Fn 不能出現在回傳位置" },
      ],
      answer: 0,
      explanation: `impl Trait 出現在回傳位置時,意思是「這裡有一個確定的具體型別,我只告訴你它實作了哪些 trait」。adder 每次回傳的都是「move |x| x + n 這個閉包」對應的那一個匿名型別,兩次呼叫只是各自捕獲了不同的 n 值,型別完全相同——這也是下一題的伏筆:如果兩個分支回傳的是兩個「不同」的閉包,impl Fn 就承受不住了。
「n 會消失所以不能捕獲」正是 move 存在的理由:move 把 n 的值複製進閉包的欄位(i32 是 Copy),閉包自己擁有它,函式結束也不影響。「每次都在 heap 上配置」則是把它和 Box<dyn Fn> 搞混了:impl Fn 回傳的是那個匿名 struct 本身,直接放在呼叫端的 stack 上,沒有配置、沒有 vtable、呼叫會被靜態分派甚至內聯。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn adder(n: i32) -> impl Fn(i32) -> i32 {", note: "回傳位置的 impl Trait:「我回傳的是某一個確定的具體型別,它實作了 Fn(i32) -> i32,型別叫什麼你不用知道」。閉包的匿名型別沒有名字可寫,這個語法就是為它而生的。" },
          { code: "    move |x| x + n", note: "本體要用到 n,而 n 是這個函式的參數,函式一結束就沒了。move 把 n 的值複製進閉包的欄位(i32 是 Copy),閉包從此自己擁有它,不再借用任何外部的東西,所以可以安全地被回傳出去。" },
          { code: "}", note: "函式結束。回傳出去的是一個 4 bytes 的匿名 struct(只有 n 這一個欄位),不是 heap 上的物件。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let add5 = adder(5);", note: "拿到一個捕獲了 5 的閉包。它的型別是編譯器生成的匿名型別,寫不出來,但可以放進變數。" },
          { code: "    let add10 = adder(10);", note: "拿到另一個捕獲了 10 的閉包——注意型別和 add5 完全相同(都是同一行原始碼生成的那一個型別),只是欄位值不同。" },
          { code: "    println!(\"{} {}\", add5(1), add10(1));", note: "分別算出 1+5 與 1+10,印出 6 11。因為型別在編譯期就確定,兩次呼叫都是靜態分派,通常會被直接內聯成一次加法。" },
          { code: "}", note: "兩個閉包離開作用域,它們只持有 i32,沒有任何 heap 資源要釋放。" },
        ],
        outro: "把 impl Fn 讀成「一個具體型別的匿名版本」就不會用錯:它可以回傳、可以當參數,但它從頭到尾只能代表一個型別。要在同一個函式裡回傳兩種不同的閉包,就得換成 Box<dyn Fn>(第 15、16 題)。",
      },
      csharp: `C# 的對應寫法是 Func<int,int> Adder(int n) => x => x + n;,而且沒有任何限制:Func<int,int> 是一個具名的參考型別,不同的 lambda 全都是它的實例,想在 if/else 分支回傳不同的 lambda 也沒人攔。代價是每次呼叫 Adder 都會 new 一個顯示類別實例加一個委派物件(兩次 heap 配置),而且之後每次 f(1) 都是一次虛擬呼叫。Rust 的 impl Fn 把同樣的東西壓成一個 stack 上的 4 bytes 值加一次可內聯的呼叫——換來的限制就是「只能是一個型別」。`,
    },
    {
      id: "2-1-15",
      question: "以下程式碼的結果是?",
      questionCode: "fn pick(double: bool, n: i32) -> impl Fn(i32) -> i32 {\n    if double {\n        move |x| x * n\n    } else {\n        move |x| x + n\n    }\n}\n\nfn main() {\n    let f = pick(true, 3);\n    println!(\"{}\", f(5));\n}",
      options: [
        { text: "編譯錯誤:if and else have incompatible types——兩個閉包各自是一個獨立的匿名型別,而 impl Fn 只能代表其中一個具體型別" },
        { text: "印出 15:兩個閉包的簽名都是 Fn(i32) -> i32,簽名相同型別當然也相同" },
        { text: "編譯錯誤:閉包不能寫在 if 運算式的分支裡當作分支的值" },
        { text: "印出 15,但 pick(false, 3) 會在執行期 panic,因為 impl Fn 只記得第一個分支的閉包" },
      ],
      answer: 0,
      explanation: `編譯器對這件事的說法非常直白:no two closures, even if identical, have the same type。每一個閉包字面值都會生成一個獨屬於它的匿名型別,就算兩個閉包的參數、回傳值、捕獲內容一模一樣,型別也不同。impl Fn(i32) -> i32 的承諾是「我回傳某一個具體型別」,而這個函式想回傳兩個,於是 if 與 else 的型別對不起來(E0308)。
「簽名相同型別就相同」正是這裡要破的直覺——那是 C# 的世界觀(都是 Func<int,int>),在 Rust 不成立。修法是把具體型別擦掉:包成 Box<dyn Fn(i32) -> i32>,兩個分支各自 Box::new 之後,型別就統一成同一個 trait object 了。注意這題有個容易被忽略的細節:如果兩個閉包都不捕獲任何東西,它們可以被自動轉型成同一個函式指標型別 fn(i32) -> i32,反而編得過——是這裡的 move |x| x * n 捕獲了 n,才讓「兩個不同型別」這件事浮上檯面。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn pick(double: bool, n: i32) -> impl Fn(i32) -> i32 {", note: "⛔ 承諾的起點:impl Fn 代表「一個」具體型別。函式體只要回傳超過一種型別,這個承諾就兌現不了。" },
            { code: "    if double {", note: "依參數決定走哪一條分支。" },
            { code: "        move |x| x * n", note: "第一個閉包字面值——編譯器替它生成一個匿名型別,姑且叫它「型別甲」。" },
            { code: "    } else {", note: "另一條分支。" },
            { code: "        move |x| x + n", note: "⛔ 編譯失敗:expected closure, found a different closure,整體錯誤是 if and else have incompatible types(E0308)。這是「型別乙」,和型別甲毫無關係——編譯器的原話是 no two closures, even if identical, have the same type。" },
            { code: "    }", note: "if 運算式要求兩條分支型別相同,這個要求在這裡破功。" },
            { code: "}", note: "函式結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let f = pick(true, 3);", note: "呼叫端完全沒問題,是函式的回傳型別自己撐不住。" },
            { code: "    println!(\"{}\", f(5));", note: "因為 pick 編譯失敗而沒有機會執行。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "一個容易忽略的對照:把兩個分支的 move 拿掉、改成不捕獲任何東西的 |x| x * 2 與 |x| x + 100,這段反而編得過——不捕獲的閉包可以自動轉型成函式指標 fn(i32) -> i32,兩條分支就統一到同一個型別了。捕獲了東西之後,這條退路才消失。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn pick(double: bool, n: i32) -> Box<dyn Fn(i32) -> i32> {", note: "改動處:回傳型別從 impl Fn 換成 Box<dyn Fn(i32) -> i32>。dyn 代表「執行期才決定是哪個具體型別」,兩個分支因此可以統一成同一個型別;Box 負責把大小不一的閉包放到 heap 上,讓回傳值本身固定是一根胖指標。" },
            { code: "    if double {", note: "分支邏輯完全不變。" },
            { code: "        Box::new(move |x| x * n)", note: "改動處:把閉包包進 Box。型別甲在這裡被擦成 Box<dyn Fn(i32) -> i32>。" },
            { code: "    } else {", note: "另一條分支。" },
            { code: "        Box::new(move |x| x + n)", note: "改動處:型別乙同樣被擦成 Box<dyn Fn(i32) -> i32>——兩條分支現在型別一致,if 運算式成立。" },
            { code: "    }", note: "if 運算式結束,值成為函式回傳值。" },
            { code: "}", note: "函式結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let f = pick(true, 3);", note: "拿到走乘法那條分支的閉包。" },
            { code: "    let g = pick(false, 3);", note: "拿到走加法那條分支的閉包——兩個變數的靜態型別現在完全相同。" },
            { code: "    println!(\"{} {}\", f(5), g(5));", note: "分別算出 5*3 與 5+3,印出 15 8。呼叫語法和一般閉包一樣,Box 會自動被穿透。" },
            { code: "}", note: "兩個 Box 離開作用域,heap 上的閉包跟著釋放。" },
          ],
          outro: "代價要記得:Box<dyn Fn> 多了一次 heap 配置與一次透過 vtable 的間接呼叫,不能內聯。判斷準則很簡單——回傳的閉包只有一種寫法就用 impl Fn(零成本),需要依執行期條件回傳不同閉包(或要把多個閉包裝進同一個集合)才用 Box<dyn Fn>。",
        },
      ],
      csharp: `同樣的程式在 C# 天經地義:Func<int,int> Pick(bool d, int n) => d ? x => x * n : x => x + n; 兩個 lambda 都是 Func<int,int> 的實例,三元運算子毫無壓力。原因是 C# 的委派從一開始就是參考型別,所有 lambda 都被統一裝箱成同一個具名型別——等於「永遠強制走 Box<dyn Fn> 那條路」。Rust 預設給你零成本的 impl Fn,需要 C# 那種彈性時才付出裝箱的代價,選擇權與帳單都在你手上。`,
    },
    {
      id: "2-1-16",
      question: "把不同的閉包裝進同一個集合。以下程式碼的結果是?",
      questionCode: "fn main() {\n    let offset = 100;\n    let ops: Vec<Box<dyn Fn(i32) -> i32>> = vec![\n        Box::new(|x| x + 1),\n        Box::new(|x| x * 2),\n        Box::new(move |x| x + offset),\n    ];\n    for op in &ops {\n        print!(\"{} \", op(10));\n    }\n    println!();\n}",
      options: [
        { text: "印出 11 20 110 ——Box<dyn Fn(i32) -> i32> 把三個各不相同的匿名型別擦成同一個型別,所以能放進同一個 Vec;呼叫時直接寫 op(10),Box 與參考都會自動被穿透" },
        { text: "編譯錯誤:op 的型別是 &Box<dyn Fn(i32) -> i32>,要呼叫必須先手動解到底,寫成 (***op)(10)" },
        { text: "編譯錯誤:三個閉包是三個不同的型別,就算各自包成 Box 也不能放進同一個 Vec" },
        { text: "編譯錯誤:第三個閉包寫了 move,和前兩個不屬於同一種 trait,整個 Vec 必須改宣告成 Vec<Box<dyn FnMut(i32) -> i32>>" },
      ],
      answer: 0,
      explanation: `dyn Trait 是「型別擦除」:三個閉包各自的匿名型別被抹掉,只留下「它實作了 Fn(i32) -> i32」這個資訊,於是三者在型別系統眼中變成同一種東西。Box 則負責解決大小問題——三個閉包捕獲的內容不同、大小也不同,裝進 Box 之後在 Vec 裡佔的都是一根固定大小的胖指標(資料指標 + vtable 指標)。
呼叫語法完全不需要特別處理:Box<F> 與 &F 都替實作了 Fn 的型別轉發了 Fn,所以 op(10) 直接成立,不用寫一串星號。另外「寫了 move 就變成另一種 trait」也是誤解:move 決定的是捕獲方式(這裡捕獲的 offset 是 i32,被複製一份進閉包),本體只是讀它來做加法,所以第三個閉包和前兩個一樣都是 Fn。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let offset = 100;", note: "待會要被第三個閉包捕獲的值,型別是 i32(Copy)。" },
          { code: "    let ops: Vec<Box<dyn Fn(i32) -> i32>> = vec![", note: "元素型別讀成三層:Vec 裝的是 Box,Box 裡是 dyn Fn(i32) -> i32。dyn 抹掉各自的匿名型別只留 trait,Box 讓大小不一的閉包在 Vec 裡佔一樣寬的位置。" },
          { code: "        Box::new(|x| x + 1),", note: "第一個閉包:不捕獲任何東西,零大小。裝進 Box 後被擦成 dyn Fn(i32) -> i32。" },
          { code: "        Box::new(|x| x * 2),", note: "第二個閉包:同樣不捕獲。注意它和第一個是兩個不同的匿名型別,能放在一起純粹是因為都被擦成了同一個 trait object。" },
          { code: "        Box::new(move |x| x + offset),", note: "第三個閉包:move 把 offset 複製一份進閉包欄位,所以它的大小是 4 bytes、和前兩個不同——這正是需要 Box 的原因。本體只是讀欄位來加,所以它一樣是 Fn。" },
          { code: "    ];", note: "Vec 建立完成,三個元素的靜態型別完全一致。" },
          { code: "    for op in &ops {", note: "借用走訪,每個 op 的型別是 &Box<dyn Fn(i32) -> i32>。" },
          { code: "        print!(\"{} \", op(10));", note: "直接呼叫就好。標準函式庫替 Box<F> 與 &F 都轉發了 Fn,所以這一行不需要任何解參考語法。依序印出 11、20、110。" },
          { code: "    }", note: "迴圈結束。因為是借用走訪,ops 仍然有效,可以再跑一次。" },
          { code: "    println!();", note: "補一個換行,讓輸出停在 11 20 110 這一行的結尾。" },
          { code: "}", note: "ops 離開作用域,三個 Box 依序釋放各自 heap 上的閉包。" },
        ],
        outro: "什麼時候該用 Box<dyn Fn>:要把多個不同的閉包放進同一個集合、存進 struct 欄位、或依執行期條件回傳不同閉包時。代價是一次 heap 配置與一次 vtable 間接呼叫;只有一種閉包時用 impl Fn 就好,完全零成本。",
      },
      csharp: `List<Func<int,int>> 混裝不同的 lambda 在 C# 是原生寫法,連想都不用想——因為 C# 的委派本來就是參考型別,所有 lambda 早就被統一成同一個具名型別了。換句話說 C# 的每一個 Func 欄位都相當於 Rust 的 Box<dyn Fn>:一次 heap 配置、一次間接呼叫。Rust 的差別只在這個成本是「你自己選的」而不是預設值——所以當你在 Rust 裡寫下 Box<dyn Fn> 時,其實就是主動選擇了 C# 一直以來的模型。`,
    },
    {
      id: "2-1-17",
      question: "以下程式碼的結果是?",
      questionCode: "fn main() {\n    let mut items: Vec<(&str, f64)> = vec![(\"apple\", 1.5), (\"banana\", 0.5), (\"cherry\", 2.0)];\n    items.sort_by_key(|item| item.1);\n    println!(\"{:?}\", items);\n}",
      options: [
        { text: "編譯錯誤:the trait bound f64: Ord is not satisfied——sort_by_key 要求鍵值型別實作 Ord(全序),而浮點數只實作了 PartialOrd" },
        { text: "正常印出依價格排好的結果:f64 是數字,當然可以拿來排序" },
        { text: "編譯錯誤:sort_by_key 的閉包必須回傳參考,要寫成 |item| &item.1" },
        { text: "正常編譯,但資料裡一旦出現 NaN 就會在執行期 panic" },
      ],
      answer: 0,
      explanation: `sort_by_key 的鍵值型別有一條 K: Ord 的 bound,而 f64 沒有實作 Ord——因為 NaN 和任何值(包括自己)都比不出大小,浮點數只滿足「部分順序」PartialOrd。閉包本身完全沒問題,問題出在它回傳的型別。
這題最值得注意的是錯誤訊息的形狀:你的程式碼裡一個 Ord 都沒寫,編譯器卻拿它來罵你,因為那個字來自 sort_by_key 的簽名(見去糖區塊)。「閉包要回傳參考」這個選項則是另一個常見誤打誤撞:sort_by_key 的鍵值必須是能被拿走的值,回傳參考反而會引出借用相關的錯誤,不是解法。真正的修法是改用 sort_by 自己提供比較邏輯,明確處理「兩個浮點數比不出大小時怎麼辦」。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let mut items: Vec<(&str, f64)> = vec![(\"apple\", 1.5), (\"banana\", 0.5), (\"cherry\", 2.0)];", note: "三筆「名稱 + 價格」的資料,價格型別明確標成 f64。排序會改動 items,所以宣告成 mut。" },
            { code: "    items.sort_by_key(|item| item.1);", note: "⛔ 編譯失敗:the trait bound f64: Ord is not satisfied(E0277)。閉包本身寫得完全正確,問題在它回傳的 f64:sort_by_key 要求鍵值型別實作 Ord(全序),而 f64 只實作 PartialOrd。" },
            { code: "    println!(\"{:?}\", items);", note: "因為上一行編譯失敗而沒有機會執行。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "這個錯誤有個很有代表性的特徵:錯誤指向的是 sort_by_key 這個方法名,而不是閉包——代表違約的是「閉包回傳的型別」,不是閉包的寫法。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let mut items: Vec<(&str, f64)> = vec![(\"apple\", 1.5), (\"banana\", 0.5), (\"cherry\", 2.0)];", note: "資料完全不用改。" },
            { code: "    items.sort_by(|a, b| a.1.partial_cmp(&b.1).unwrap());", note: "改動處:換成 sort_by,自己提供比較邏輯。閉包收兩個元素的參考、回傳 Ordering;partial_cmp 回傳 Option<Ordering>(比不出來時是 None),這裡用 unwrap 表態「我確定資料裡沒有 NaN」。sort_by 對鍵值沒有 Ord 的要求,所以通過。" },
            { code: "    println!(\"{:?}\", items);", note: "印出 [(\"banana\", 0.5), (\"apple\", 1.5), (\"cherry\", 2.0)]。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "不想寫 unwrap 的話有更好的選擇:items.sort_by(|a, b| a.1.total_cmp(&b.1)); ——total_cmp 替浮點數定義了一套包含 NaN 的全序,不會 panic 也不用解包。另一條路是換一個真的有 Ord 的鍵值型別,例如把價格用整數的「分」來存。",
        },
        {
          label: "△ Desugared (what the compiler sees)",
          intro: "錯誤訊息提到了 Ord,但你的程式碼裡沒有這個字。它來自 sort_by_key 的簽名——閉包只是把鍵值交出去,真正對鍵值提出要求的是方法本身:",
          lines: [
            { code: "items.sort_by_key(|item| item.1);", note: "你寫的這一行。看得到的只有閉包,看不到任何 trait。" },
            { code: "", note: "" },
            { code: "// 標準函式庫裡 sort_by_key 的簽名:", note: "把它攤開,Ord 的出處就清楚了。" },
            { code: "pub fn sort_by_key<K, F>(&mut self, f: F)", note: "K 是「鍵值」的型別,也就是閉包回傳的東西——在這題是 f64。" },
            { code: "where", note: "接下來兩條 bound 才是重點。" },
            { code: "    F: FnMut(&T) -> K,", note: "對閉包的要求只有 FnMut,你的閉包輕鬆滿足,所以問題不在這裡。" },
            { code: "    K: Ord;", note: "錯誤訊息裡的 Ord 從這一行來:排序需要「任兩個鍵值都一定能比出大小」,也就是全序。f64 給不出這個保證。" },
            { code: "", note: "" },
            { code: "// 為什麼 f64 不實作 Ord:", note: "問題出在 IEEE 754 的一個特例。" },
            { code: "// f64::NAN < 1.0 是 false,f64::NAN > 1.0 是 false,f64::NAN == f64::NAN 也是 false", note: "NaN 和任何值都比不出大小,連「等於自己」都不成立。所以浮點數只滿足部分順序(PartialOrd),而排序演算法需要全序(Ord),否則結果會沒有意義。" },
          ],
          outro: "這條線索之後會反覆出現:Rust 把「可以比較」拆成 PartialOrd 與 Ord 兩層,f64 只有前者,整數與 &str 兩者都有。看到 the trait bound X: Ord is not satisfied,第一個反應就該是「我的鍵值是不是浮點數」。",
        },
      ],
      csharp: `C# 這裡完全不會攔你:items.Sort((a, b) => a.Price.CompareTo(b.Price)); 對 double 排序天經地義,因為 double 實作了 IComparable<double>,CompareTo 對 NaN 也有定義(把 NaN 排在所有值之前,而且 NaN.CompareTo(NaN) 回傳 0)。C# 是「挑一套規則寫死在型別裡」,Rust 是「在型別系統上區分全序與部分順序,強迫你自己決定 NaN 怎麼辦」。兩種做法都合理,但只有 Rust 會在你沒想過這件事的時候提醒你——代價就是這個乍看莫名其妙的編譯錯誤。`,
    },
    {
      id: "2-1-18",
      question: "以下程式碼的結果是?",
      questionCode: "struct Counter {\n    n: i32,\n}\n\nimpl Counter {\n    fn show(&self) {\n        println!(\"count = {}\", self.n);\n    }\n}\n\nfn main() {\n    let c = Counter { n: 7 };\n    let render = move || c.show();\n    let backup = render.clone();\n    render();\n    backup();\n}",
      options: [
        { text: "編譯錯誤:the trait bound Counter: Clone is not satisfied——閉包只有在「捕獲的每一樣東西都實作 Clone」時才跟著實作 Clone,而 Counter 沒有 derive Clone" },
        { text: "正常印出兩行 count = 7:閉包是編譯器生成的型別,永遠支援 clone()" },
        { text: "編譯錯誤:寫了 move 的閉包一律不能 clone(),要把 move 拿掉才行" },
        { text: "正常編譯,但 backup 和 render 共用同一份捕獲資料,先呼叫的那個會把它消耗掉" },
      ],
      answer: 0,
      explanation: `閉包的匿名型別會不會實作 Clone,是由它捕獲的欄位決定的:全部欄位都 Clone,閉包就 Clone;有一個不是,整個閉包就不是。這裡 move 讓閉包持有一個 Counter 欄位,而 Counter 沒有 derive Clone,所以 render.clone() 失敗——編譯器甚至會直接建議你在 Counter 上加 #[derive(Clone)]。
「閉包這個型別本身永遠可以複製」是從委派帶過來的直覺,在 Rust 不成立。「move 閉包一律不能 clone」也不對:第 6 題那個捕獲 Vec<i32> 的 move 閉包就可以,因為 Vec<i32> 是 Clone。順帶一提捕獲方式也會影響結論——以不可變借用捕獲的閉包永遠是 Clone 甚至 Copy(參考本身是 Copy),但以可變借用捕獲的閉包兩者都不是(可變借用不能被複製)。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "struct Counter {", note: "⛔ 問題的根源在這裡:這個 struct 沒有任何 derive。" },
            { code: "    n: i32,", note: "欄位本身是 Copy,但這不代表 struct 就自動是 Clone——derive 要自己寫。" },
            { code: "}", note: "struct 定義結束。" },
            { code: "", note: "" },
            { code: "impl Counter {", note: "行為區塊。" },
            { code: "    fn show(&self) {", note: "接收者是 &self,只讀不改。" },
            { code: "        println!(\"count = {}\", self.n);", note: "印出欄位值。" },
            { code: "    }", note: "方法結束。" },
            { code: "}", note: "impl 結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let c = Counter { n: 7 };", note: "建立實例。" },
            { code: "    let render = move || c.show();", note: "move 讓閉包取得 c 的所有權,於是閉包的匿名 struct 裡有一個 Counter 型別的欄位。" },
            { code: "    let backup = render.clone();", note: "⛔ 編譯失敗:the trait bound Counter: Clone is not satisfied in this closure。閉包要實作 Clone,前提是它捕獲的每一樣東西都實作 Clone;這裡卡在 Counter 上。編譯器的建議就是 consider annotating Counter with #[derive(Clone)]。" },
            { code: "    render();", note: "呼叫本身完全沒問題(本體只借用欄位來呼叫 show),因為上一行失敗而沒有機會執行。" },
            { code: "    backup();", note: "同上。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "錯誤訊息點名的是 Counter 而不是閉包,這是很重要的線索:閉包的 Clone 是「跟著捕獲內容走」的推導結果,要修就要修捕獲的那個型別。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "#[derive(Clone)]", note: "改動處:替 Counter 補上 Clone。一旦捕獲的每一樣東西都是 Clone,閉包的匿名型別就自動跟著實作 Clone——你不用(也不能)手寫閉包的 impl Clone。" },
            { code: "struct Counter {", note: "struct 本體完全不用改。" },
            { code: "    n: i32,", note: "欄位不變。" },
            { code: "}", note: "struct 定義結束。" },
            { code: "", note: "" },
            { code: "impl Counter {", note: "行為區塊不變。" },
            { code: "    fn show(&self) {", note: "方法不變。" },
            { code: "        println!(\"count = {}\", self.n);", note: "印出欄位值。" },
            { code: "    }", note: "方法結束。" },
            { code: "}", note: "impl 結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let c = Counter { n: 7 };", note: "建立實例。" },
            { code: "    let render = move || c.show();", note: "閉包捕獲 c 的所有權,欄位型別是 Counter——現在它是 Clone 了。" },
            { code: "    let backup = render.clone();", note: "複製整個閉包,連同它捕獲的那份 Counter 一起複製一份。backup 與 render 從此各自持有獨立的資料。" },
            { code: "    render();", note: "印出 count = 7。" },
            { code: "    backup();", note: "再印出 count = 7。整體輸出兩行,兩者互不影響。" },
            { code: "}", note: "兩個閉包各自釋放自己那份 Counter。" },
          ],
          outro: "決定閉包能力的規則一路都一樣:能不能重複呼叫看「本體怎麼用捕獲的欄位」,能不能 Clone 看「捕獲的欄位是不是 Clone」,能不能跨執行緒送(2-7 的主題)看「捕獲的欄位是不是 Send」。閉包從來沒有自己的個性,它的能力全部是捕獲內容的加總。",
        },
      ],
      csharp: `C# 這邊根本不會遇到這個問題:委派是參考型別,把 Action a 指派給 Action b 只是複製一根參考,兩者指向同一個委派實例、共用同一份捕獲狀態,而且永遠成功。要「深複製」一個 lambda 連同它捕獲的物件,C# 反而做不到。Rust 的 clone() 語意是真正的複製(連捕獲的 Counter 一起複製,兩份完全獨立),代價就是它得先確定每一樣被捕獲的東西都複製得出來——這個編譯錯誤就是那道檢查。`,
    },
    {
      id: "2-1-19",
      question: "設計選擇:下面這段程式把閉包存進 struct 欄位,並在建立它的函式結束之後才呼叫。這裡的 move 為什麼是必要的?",
      questionCode: "struct Button {\n    on_click: Box<dyn Fn()>,\n}\n\nfn make_button(label: String) -> Button {\n    Button {\n        on_click: Box::new(move || println!(\"clicked: {}\", label)),\n    }\n}\n\nfn main() {\n    let b = make_button(String::from(\"OK\"));\n    (b.on_click)();\n    (b.on_click)();\n}",
      options: [
        { text: "因為 label 是 make_button 的區域變數,函式一回傳就消失。閉包要活得比這個作用域久,就不能持有任何指向作用域內部的借用——move 把 label 的所有權搬進閉包,讓閉包自己擁有這份資料,從此不依賴任何外部生命週期" },
        { text: "因為 Box::new 規定放進去的值必須用 move 閉包建立,這是語法要求" },
        { text: "因為 Box<dyn Fn()> 隱含要求閉包活到程式結束,而 move 的作用就是把 label 的壽命延長到程式結束" },
        { text: "因為不寫 move 的閉包無法實作 Fn,只能實作 FnOnce,而欄位型別要求的是 Fn" },
      ],
      answer: 0,
      explanation: `這是 move 最常見、也最值得建立直覺的使用情境:閉包要脫離建立它的作用域活下去。不寫 move 時,閉包捕獲的是 &label——一根指向 make_button 堆疊框的指標;函式一回傳那個框就沒了,編譯器會報 label does not live long enough 或 closure may outlive the current function。寫了 move,String 的所有權被搬進閉包欄位,閉包自給自足,回傳出去毫無問題。
第三個選項藏著本課最值得破的迷思:Box<dyn Fn()> 確實隱含一條 'static 的要求,但 'static 的意思是「這個值不借用任何比它短命的東西」,不是「這個值會活到程式結束」——這裡的 Button 在 main 結束時就被釋放了,完全不影響它滿足 'static。而且 move 從來不會「延長」任何東西的壽命,它只是換一個擁有者。另外兩個選項也各錯一處:Box::new 收任何值,沒有規定要 move;不寫 move 的閉包照樣可以是 Fn(第 2、7 題都是),Fn 與 move 是兩件不相干的事。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "struct Button {", note: "一個持有回呼的型別——GUI、事件系統、重試邏輯都是這個形狀。" },
          { code: "    on_click: Box<dyn Fn()>,", note: "欄位型別讀成兩層:dyn Fn() 抹掉閉包的具體型別(不同按鈕可以裝不同的回呼),Box 讓大小不一的閉包在欄位裡佔固定寬度。這個型別隱含一條「不借用任何短命東西」的要求,所以能不能存進來,取決於閉包自己有沒有帶著外部借用。" },
          { code: "}", note: "struct 定義結束。" },
          { code: "", note: "" },
          { code: "fn make_button(label: String) -> Button {", note: "label 按值收下,是這個函式的區域變數——函式一回傳,它的堆疊框就沒了。" },
          { code: "    Button {", note: "建立實例。" },
          { code: "        on_click: Box::new(move || println!(\"clicked: {}\", label)),", note: "關鍵這一行。不寫 move 的話,閉包捕獲的是 &label(指向這個堆疊框),回傳之後就是懸空參考,編譯器會擋。寫了 move,String 的所有權整個搬進閉包欄位,閉包從此不依賴任何外部的東西,可以被安全地帶出這個函式。" },
          { code: "    }", note: "struct 實字結束,成為回傳值。" },
          { code: "}", note: "函式結束。label 已經被移進閉包,所以這裡不會釋放任何東西。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let b = make_button(String::from(\"OK\"));", note: "拿到一個自給自足的 Button。它裡面那個 String 的擁有者現在是閉包。" },
          { code: "    (b.on_click)();", note: "呼叫欄位裡的閉包。外面那層括號是必要的——少了它,b.on_click() 會被解析成「呼叫 b 身上一個叫 on_click 的方法」。印出 clicked: OK。" },
          { code: "    (b.on_click)();", note: "再呼叫一次。本體只是把欄位借來印,所以這是一個 Fn,呼叫幾次都行。印出第二行 clicked: OK。" },
          { code: "}", note: "b 離開作用域,Box 釋放 heap 上的閉包,閉包再釋放它擁有的那個 String。" },
        ],
        outro: "把判斷準則記成一個問題:「這個閉包會不會活得比它捕獲的變數所在的作用域久?」會 → 必須 move(存進 struct、當回傳值、放進集合、送去另一個執行緒);不會 → 不寫 move,借用更輕也更彈性。2-7 講並行時會看到同一條推理的另一個版本,只是那裡多了一個「另一個執行緒不知道會跑多久」的理由。",
      },
      csharp: `C# 完全不用想這件事:Button MakeButton(string label) => new Button { OnClick = () => Console.WriteLine($\"clicked: {label}\") }; 直接就能編譯,因為 label 被提升到一個 heap 上的顯示類別,委派持有它的參考,GC 保證只要委派還在、那個物件就不會消失。等於「捕獲的東西自動延命」是語言內建行為。Rust 沒有 GC,無法自動延長任何東西的壽命,只能要你明說「這份資料從現在起歸閉包所有」——那個明說就是 move。所以 C# 開發者看到 move 時,不要把它讀成效能或執行緒的關鍵字,要讀成「這個 lambda 從此自帶資料,不再依賴任何人」。`,
    },
    {
      id: "2-1-20",
      question: "本課 C# 對照總結:關於 C# 的 lambda / 委派與 Rust 閉包,哪一組描述最準確?",
      options: [
        { text: "C# 的 lambda 一律被編譯成 heap 上的委派物件(捕獲的變數被提升到顯示類別),呼叫是虛擬分派,而且永遠可以重複呼叫;Rust 的閉包是編譯期生成的匿名 struct,捕獲的東西直接變成它的欄位,再依「本體怎麼用這些欄位」自動實作 Fn / FnMut / FnOnce——能呼叫幾次、能不能複製、所有權歸誰,全部在編譯期就決定好了" },
        { text: "兩者一一對應:Func<T,R> 對應 Fn、Action 對應 FnMut、delegate 對應 FnOnce,只是關鍵字不同" },
        { text: "Rust 的閉包和 C# 的 lambda 一樣都是 heap 上的物件,差別只在 Rust 要你自己寫 Box 而 C# 幫你寫了" },
        { text: "C# 沒有閉包捕獲相關的陷阱,Rust 的 Fn 分層純粹是為了效能最佳化,對正確性沒有影響" },
      ],
      answer: 0,
      explanation: `一句話收束本課:C# 的 lambda 是「一個裝著方法指標與捕獲物件的 heap 物件」,Rust 的閉包是「一個裝著捕獲欄位的 stack 上匿名 struct」。從這個差別可以推出本課的每一條規則——捕獲方式有三種(借用、可變借用、取得所有權),因為欄位型別可以是 &T、&mut T 或 T;呼叫語意有三層(Fn / FnMut / FnOnce),因為方法的接收者可以是 &self、&mut self 或 self;閉包能不能 Clone、能不能跨執行緒,全部是捕獲欄位的能力加總。
第二個選項是最容易被記混的版本:Func 與 Action 的差別只是「有沒有回傳值」,和呼叫次數毫無關係,兩者都停在 Rust 的 Fn 那一層。第三個選項則是把 Box<dyn Fn> 當成 Rust 閉包的常態——實際上那只是你需要型別擦除時才付的帳單,預設的 impl Fn 連一次配置都沒有。
最後一個選項值得單獨破:C# 有非常有名的捕獲陷阱。for 迴圈的迴圈變數只有一個,迴圈裡建立的每個 lambda 捕獲的都是「同一個變數」,跑完之後全部看到終值(foreach 在 C# 5 之後改成每次迭代一個新變數,for 迴圈至今仍是舊行為)。同樣的寫法在 Rust 不寫 move 會直接編譯失敗,寫了 move 則每個閉包各自複製一份自己的 i——語言先替你排除了這個歧義。`,
      walkthrough: [
        {
          label: "◇ C# equivalent: the classic loop-variable capture trap",
          lang: "csharp",
          lines: [
            { code: "var actions = new List<Action>();", note: "準備裝三個回呼。" },
            { code: "for (int i = 0; i < 3; i++)", note: "整個迴圈只有「一個」i,它被提升到顯示類別的欄位裡。" },
            { code: "    actions.Add(() => Console.WriteLine(i));", note: "三個 lambda 捕獲的都是同一個欄位,不是三份各自的複本。這一行完全合法,沒有任何警告。" },
            { code: "foreach (var a in actions) a();", note: "迴圈早就結束、i 已經是 3,所以這裡印出三行 3 而不是 0、1、2。foreach 在 C# 5 之後改成每次迭代給一個新變數,但 for 迴圈到今天仍是這個行為。" },
          ],
          outro: "這個 bug 的特徵是「執行期才發現、而且要看輸出才會注意到」。C# 沒有任何機制能在編譯期告訴你「這三個 lambda 其實共用同一個變數」,因為在 GC 的世界裡這是完全合法的操作。",
        },
        {
          label: "√ Rust: the same shape, with the ambiguity removed at compile time",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let mut actions: Vec<Box<dyn Fn()>> = Vec::new();", note: "對應 C# 的 List<Action>。Box<dyn Fn()> 就是 Rust 版的「委派物件」——要付一次 heap 配置與一次間接呼叫,而在 C# 這個成本是預設值。" },
            { code: "    for i in 0..3 {", note: "Rust 的 for 迴圈每一輪都是一個「全新的」i,而且它的作用域只到這一輪結束。" },
            { code: "        actions.push(Box::new(move || println!(\"{}\", i)));", note: "關鍵在 move:把這一輪的 i(i32,Copy)複製一份進閉包欄位,三個閉包各自持有自己的 0、1、2。如果拿掉 move,閉包會想借用這一輪的 i,而它要活到迴圈外面——編譯器直接報錯,根本沒有機會變成執行期的驚喜。" },
            { code: "    }", note: "迴圈結束。" },
            { code: "    for a in &actions {", note: "借用走訪三個閉包。" },
            { code: "        a();", note: "依序印出 0、1、2——和直覺一致,而且這個結果是編譯期就保證的。" },
            { code: "    }", note: "迴圈結束。" },
            { code: "}", note: "actions 離開作用域,三個 Box 依序釋放。" },
          ],
          outro: "帶著走的對照表:C# 的 lambda 永遠是 heap 物件 + 虛擬呼叫 + 可重複呼叫 + 捕獲靠 GC 延命;Rust 的閉包預設是 stack 上的匿名 struct + 靜態分派 + 呼叫次數由 Fn 系 trait 決定 + 捕獲方式由本體用法決定。想要 C# 那種彈性隨時可以用 Box<dyn Fn> 買回來,反過來則不行——這就是「零成本抽象」在閉包這一題上的具體形狀。",
        },
      ],
      csharp: `給 C# 開發者的三個帶著走的結論。一、看到 Fn / FnMut / FnOnce 不要背定義,問「這個閉包的本體對捕獲的東西做了什麼」:只讀 → Fn,要改 → FnMut,要移走 → FnOnce;宣告參數 bound 時則反過來,挑你真正需要的最寬鬆那一個。二、看到 move 不要讀成 thread 或效能,讀成「這份資料從現在起歸閉包所有」——需要它的時機只有一個:閉包要活得比它捕獲的變數所在的作用域久。三、看到 Box<dyn Fn> 就是「我現在選擇 C# 的模型」:一次配置、一次間接呼叫,換來可以把不同閉包裝進同一個集合或回傳型別。下一課(2-2 迭代器)會把閉包大量當成 map / filter 的參數用,那時這三條結論會變成你讀每一行鏈式呼叫的預設背景知識。`,
    },
  ],
};
