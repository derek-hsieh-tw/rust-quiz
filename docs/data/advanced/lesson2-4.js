/* 出題慣例見專案根目錄 AUTHORING.md:
 *   - answer 一律為 0(正確答案寫在第一個選項),顯示順序由 quiz.js 依題目 id 洗牌
 *   - 詳解禁止用「選項 A/B/C」字母指涉,必須直接描述選項內容
 *   - 有程式碼的題目一律附 walkthrough(逐行說明);反面案例必須同時附上 √ 正確寫法 */
window.RUST_LESSONS = window.RUST_LESSONS || {};
window.RUST_LESSONS["lesson2-4"] = {
  id: "lesson2-4",
  title: "生命週期進階",
  goal: "把函式層級的 'a 延伸到 struct、方法、多個生命週期之間的關係與泛型:看懂 'a: 'b、T: 'a、T: 'static 各自承諾了什麼,並能讀懂編譯器用 '1、'2 描述的錯誤訊息。",
  primer: {
    intro: "入門課學的生命週期標註都在函式簽名上:描述回傳的參考跟哪個參數綁在一起。進階之後,參考會被存進 struct、從方法裡交出去、和泛型型別參數放在一起,生命週期參數也就跟著出現在 struct 定義、impl 區塊與 trait bound 裡。它們仍然只做一件事——描述「誰不能比誰活得久」,讓編譯器在編譯期證明沒有懸空參考。",
    examples: [
      {
        code: "struct Highlight<'a> {\n    part: &'a str,\n}\n\nfn main() {\n    let text = String::from(\"Rust is fast\");\n    let h = Highlight { part: &text[0..4] };\n    println!(\"{}\", h.part);\n}",
        note: "struct 持有參考時要在名字後面宣告生命週期參數,欄位再用它標註;h 只能活在 text 還有效的範圍內,印出 Rust。",
      },
      {
        code: "struct Highlight<'a> {\n    part: &'a str,\n}\n\nimpl<'a> Highlight<'a> {\n    fn size(&self) -> usize {\n        self.part.len()\n    }\n}\n\nfn main() {\n    let h = Highlight { part: \"fast\" };\n    println!(\"{}\", h.size());\n}",
        note: "為帶生命週期的 struct 寫方法時,impl 後面也要宣告同一組參數;size 回傳的是數字不是參考,所以不牽涉任何綁定,印出 4。",
      },
    ],
    csharp: "C# 的類別欄位可以隨意持有別的物件,GC 會保證被引用的物件活著;Rust 沒有 GC,所以「這個 struct 裡的參考指向的資料活多久」必須寫進型別,由編譯器檢查。",
  },
  questions: [
    {
      id: "2-4-01",
      question: "struct 裡有兩個參考欄位,共用同一個生命週期參數。以下程式碼的結果是?",
      questionCode: "struct Record<'a> {\n    key: &'a str,\n    value: &'a str,\n}\n\nfn main() {\n    let key = String::from(\"lang\");\n    let value = String::from(\"rust\");\n    let r = Record { key: &key, value: &value };\n    println!(\"{}={}\", r.key, r.value);\n}",
      options: [
        { text: "印出 lang=rust——'a 不是「某一個變數的壽命」,而是一段兩個參考都必須涵蓋的區間;key 與 value 都活到 main 結束,足以涵蓋 r 的整個使用期間" },
        { text: "編譯錯誤:struct 裡每個參考欄位都必須各自宣告不同名字的生命週期,例如 Record<'a, 'b>" },
        { text: "編譯錯誤:key 和 value 是兩個不同的變數,來源不同的參考不能標同一個 'a" },
        { text: "印出 lang=rust,而且 Record 在建立時把兩段字串各複製了一份,所以 key、value 之後被釋放也不影響 r" },
      ],
      answer: 0,
      explanation: `共用同一個 'a 完全合法。struct 上的 'a 是一個「待填入的區間」:建立 Record 時,編譯器替 'a 選一段兩個參考都能涵蓋的區間(實務上就是兩者重疊的那段),並要求 r 不能活出這段區間。key 與 value 都活到 main 結束,r 的使用也在那之前,一切成立。
「每個欄位要各自一個名字」是把生命週期參數想成「每個參考專屬的標籤」,其實它是「關係」的描述:同一個名字代表「這幾個參考受同一個上限約束」。「來源不同不能共用」也是同一個誤解的變形——入門課的 longest(x, y) 就是兩個不同來源共用一個 'a。至於「複製了一份」:欄位型別是 &str,存的只是指標加長度,Record 不擁有任何字串資料,這也正是它需要生命週期參數的原因。什麼時候共用一個名字「不夠用」?第 4 題會看到。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "struct Record<'a> {", note: "宣告一個生命週期參數 'a。它不是具體的時間長度,而是「建立實例時才決定」的區間。" },
          { code: "    key: &'a str,", note: "key 指向的資料至少要涵蓋 'a。" },
          { code: "    value: &'a str,", note: "value 受同一個上限約束。同名代表「同一條約束」,不代表兩者來自同一個變數。" },
          { code: "}", note: "Record 只存兩組「指標 + 長度」,不擁有任何字串。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let key = String::from(\"lang\");", note: "key 擁有 lang 這段 heap 資料,活到 main 結束。" },
          { code: "    let value = String::from(\"rust\");", note: "value 是另一個擁有者,同樣活到 main 結束。" },
          { code: "    let r = Record { key: &key, value: &value };", note: "兩個借用放進 r。編譯器替 'a 找一段兩個借用都涵蓋、又涵蓋 r 所有使用點的區間——找得到,建立成功。" },
          { code: "    println!(\"{}={}\", r.key, r.value);", note: "r 最後一次使用,印出 lang=rust。兩個借用在此之後結束。" },
          { code: "}", note: "r、value、key 依宣告的相反順序離開作用域。" },
        ],
        outro: "同一個 'a 的實際意義是「取交集」:如果其中一個 String 活得比較短,'a 就被壓成較短的那段,r 也只能活在那段裡面。",
      },
      csharp: `C# 的 class Record { public string Key; public string Value; } 同樣只存兩個參考,但沒有人需要說明「被引用的字串活多久」——只要 Record 還可達,GC 就不會回收那兩個字串。Rust 的 Record<'a> 等於把這個「可達就不回收」的保證改寫成一條靜態規則:r 不准活出 'a,而 'a 不准超出 key、value 任何一個的壽命。`,
    },
    {
      id: "2-4-02",
      question: "impl<'a> 裡的方法回傳 &'a str。以下程式碼的結果是?",
      questionCode: "struct Parser<'a> {\n    input: &'a str,\n}\n\nimpl<'a> Parser<'a> {\n    fn first_word(&self) -> &'a str {\n        self.input.split(' ').next().unwrap()\n    }\n}\n\nfn main() {\n    let text = String::from(\"hello rust world\");\n    let word;\n    {\n        let p = Parser { input: &text };\n        word = p.first_word();\n    }\n    println!(\"{}\", word);\n}",
      options: [
        { text: "印出 hello——回傳型別寫的是 &'a str,綁的是 Parser 裡那份 input 的來源 text,不是 &self;Parser 在內層區塊結束時消失,word 照樣有效" },
        { text: "編譯錯誤:`p` does not live long enough——方法有 &self,回傳的參考一定綁在 p 身上" },
        { text: "編譯錯誤:impl<'a> 宣告的 'a 和 struct Parser<'a> 的 'a 是兩個不同的生命週期,應該寫成 impl<'b> Parser<'b> 以免衝突" },
        { text: "印出 hello rust world——split 回傳的片段在 Parser 消失後會退回整段字串" },
      ],
      answer: 0,
      explanation: `關鍵在回傳型別寫了 'a。入門課學過第三條省略規則:方法有 &self 時,沒標註的回傳參考綁 &self。但這裡回傳型別明確寫 &'a str,省略規則就不介入了——'a 是 Parser<'a> 的那個參數,代表 input 指向的資料(也就是 text)的區間。所以 word 只受 text 限制,跟 p 活多久無關。
選「p does not live long enough」的人其實描述的是另一個版本:把回傳型別寫成 &str(省略),它就會綁到 &self 上,p 在區塊結束時消失,word 就不能再用——那個版本真的會報這個錯。impl<'a> Parser<'a> 的兩個 'a 則是同一個:impl<'a> 是「宣告」這個名字,Parser<'a> 是「使用」它,就像泛型的 impl<T> Wrapper<T>;改成 impl<'b> Parser<'b> 也合法,但只是換名字,不是在避免衝突。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "struct Parser<'a> {", note: "Parser 持有一個參考,所以帶生命週期參數 'a。" },
          { code: "    input: &'a str,", note: "input 指向的資料至少活 'a 這麼久。" },
          { code: "}", note: "struct 定義結束。" },
          { code: "", note: "" },
          { code: "impl<'a> Parser<'a> {", note: "impl<'a> 宣告名字,Parser<'a> 使用它——兩處是同一個 'a,跟 impl<T> Wrapper<T> 的寫法完全對稱。" },
          { code: "    fn first_word(&self) -> &'a str {", note: "回傳型別明確寫 'a,省略規則不介入:回傳值綁的是 input 的來源,而不是 &self 這次借用。" },
          { code: "        self.input.split(' ').next().unwrap()", note: "切出第一個單字。回傳的片段直接指進 text 的資料,本來就跟 Parser 這個外殼無關。" },
          { code: "    }", note: "方法本體結束。" },
          { code: "}", note: "這個定義到此結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let text = String::from(\"hello rust world\");", note: "text 擁有資料,活到 main 結束。" },
          { code: "    let word;", note: "先宣告、稍後賦值,讓 word 活得比內層區塊久。" },
          { code: "    {", note: "內層區塊開始。" },
          { code: "        let p = Parser { input: &text };", note: "p 借用 text;這裡的 'a 只需涵蓋 text 還有效的範圍。" },
          { code: "        word = p.first_word();", note: "word 拿到的是 &'a str,也就是指進 text 的參考;&self 這次借用在這行結束就結束了。" },
          { code: "    }", note: "p 在這裡被丟棄。word 沒有借用 p,所以不受影響。" },
          { code: "    println!(\"{}\", word);", note: "text 仍然有效,印出 hello。" },
          { code: "}", note: "text 離開作用域並釋放。" },
        ],
        outro: "把回傳型別改成 &str(交給省略規則),同一段 main 就會在 word = p.first_word() 報 `p` does not live long enough——因為回傳值被綁到 &self 上了。對「只是轉手 struct 裡的參考」的方法,回傳 &'a 讓結果活得跟原始資料一樣久,而不是跟這個 struct 一樣久。",
      },
      csharp: `C# 的方法永遠不需要宣告「回傳的東西跟 this 活多久」:回傳一個子字串或子物件,只要還有人引用它,它就活著,this 被丟掉也無所謂。Rust 要你在簽名上做選擇:&str(預設綁 &self,結果不能活過這個 struct)或 &'a str(綁原始資料,struct 消失也沒關係)。這個選擇寫進型別之後,呼叫端就能靠簽名知道結果能保留多久,不必讀實作。`,
    },
    {
      id: "2-4-03",
      question: "方法想回傳自己新建的字串的參考。以下程式碼的結果是?",
      questionCode: "struct Parser<'a> {\n    input: &'a str,\n}\n\nimpl<'a> Parser<'a> {\n    fn shout(&self) -> &str {\n        let upper = self.input.to_uppercase();\n        &upper\n    }\n}\n\nfn main() {\n    let p = Parser { input: \"hello\" };\n    println!(\"{}\", p.shout());\n}",
      options: [
        { text: "編譯錯誤:cannot return reference to local variable `upper`——upper 是方法內新建的 String,方法結束就被釋放;它既不屬於 self 也不屬於任何參數,沒有任何生命週期標註能讓它活下來,應該直接回傳 String" },
        { text: "印出 HELLO——方法有 &self,回傳值被綁到 self 的生命週期上,upper 會跟著 p 一起活下去" },
        { text: "印出 HELLO——&upper 是最後一個運算式,等於把 upper 回傳出去,所有權自然轉交給呼叫端" },
        { text: "編譯錯誤:missing lifetime specifier,把回傳型別改成 &'a str 就能編譯" },
      ],
      answer: 0,
      explanation: `生命週期標註只能「描述」既有資料的存活關係,不能「創造」存活時間。upper 是方法內部新建、由方法自己擁有的 String,方法一結束它就被 drop,任何指向它的參考都會懸空,所以編譯器直接拒絕。
「綁到 self 就會跟著活下去」是這題最誘人的誤解:省略規則把回傳型別解讀成「跟 &self 活一樣久」,這是一個承諾,編譯器接著檢查函式本體有沒有兌現——&upper 指向的資料根本不在 self 裡,承諾做不到。改標 &'a str 也一樣,upper 不屬於 input 的來源。至於「最後一個運算式等於回傳 upper」:回傳的是 &upper(借用),不是 upper 本身,所有權沒有移出去。修法是承認這是新資料:回傳擁有的 String。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "struct Parser<'a> {", note: "Parser 帶生命週期參數 'a。" },
            { code: "    input: &'a str,", note: "input 借用外部資料。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "impl<'a> Parser<'a> {", note: "為 Parser<'a> 實作方法。" },
            { code: "    fn shout(&self) -> &str {", note: "回傳型別省略,依省略規則等於承諾「回傳值跟 &self 活一樣久」。" },
            { code: "        let upper = self.input.to_uppercase();", note: "to_uppercase 配置一個全新的 String,擁有者是這個區域變數 upper,跟 self 或 input 都沒有關係。" },
            { code: "        &upper", note: "⛔ 編譯失敗:cannot return reference to local variable `upper`(returns a reference to data owned by the current function)。upper 在方法結束時就被釋放,這個參考註定懸空。" },
            { code: "    }", note: "upper 在這裡被 drop——這就是參考不能交出去的原因。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let p = Parser { input: \"hello\" };", note: "p 借用一個字串字面值。" },
            { code: "    println!(\"{}\", p.shout());", note: "如果能編譯,這裡會讀到一塊已釋放的記憶體。" },
            { code: "}", note: "這個定義到此結束。" },
          ],
          outro: "判斷法:回傳的參考一定要指向「呼叫前就已經存在」的資料(self 裡的、參數指向的、或 'static 的)。方法裡新建的東西只能以擁有的形式交出去。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "struct Parser<'a> {", note: "struct 定義不用改。" },
            { code: "    input: &'a str,", note: "input 借用外部資料。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "impl<'a> Parser<'a> {", note: "為 Parser<'a> 實作方法。" },
            { code: "    fn shout(&self) -> String {", note: "改動處:回傳擁有的 String。新資料就該連同所有權一起交出去,簽名也不再牽涉任何生命週期。" },
            { code: "        let upper = self.input.to_uppercase();", note: "一樣新建一個 String。" },
            { code: "        upper", note: "改動處:把 upper 本身移出方法,所有權轉給呼叫端,不會被 drop。" },
            { code: "    }", note: "方法本體結束。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let p = Parser { input: \"hello\" };", note: "p 借用字串字面值。" },
            { code: "    println!(\"{}\", p.shout());", note: "印出 HELLO;回傳的 String 在這行用完後釋放。" },
            { code: "}", note: "這個定義到此結束。" },
          ],
        },
      ],
      csharp: `在 C# 裡 string Shout() => input.ToUpper(); 天經地義——新字串配置在 managed heap 上,回傳的是參考,只要有人拿著它 GC 就不會回收。Rust 的 String 同樣在 heap 上,但它的釋放時機是「擁有者離開作用域」,不是「沒人引用」,所以「回傳參考」與「回傳擁有權」是兩種不同的簽名,必須選對。`,
    },
    {
      id: "2-4-04",
      question: "struct 持有兩個來源不同的參考,各自標 'a 與 'b。以下程式碼的結果是?",
      questionCode: "struct Lookup<'a, 'b> {\n    text: &'a str,\n    key: &'b str,\n}\n\nimpl<'a, 'b> Lookup<'a, 'b> {\n    fn found(&self) -> Option<&'a str> {\n        self.text.split(' ').find(|w| w.starts_with(self.key))\n    }\n}\n\nfn main() {\n    let text = String::from(\"apple banana cherry\");\n    let hit;\n    {\n        let key = String::from(\"ba\");\n        let lk = Lookup { text: &text, key: &key };\n        hit = lk.found();\n    }\n    println!(\"{:?}\", hit);\n}",
      options: [
        { text: "印出 Some(\"banana\")——found 的結果只綁 'a(text 的來源),跟活得較短的 key 無關;key 在內層區塊結束時釋放,不影響 hit" },
        { text: "編譯錯誤:同一個 struct 裡的參考欄位一定要標同一個生命週期,不能拆成 'a 和 'b" },
        { text: "編譯錯誤:`key` does not live long enough——hit 是從 Lookup 算出來的,而 Lookup 裡有 key,所以 hit 不能活過 key" },
        { text: "印出 None——key 在 println! 之前就被釋放了,find 比對時讀不到它" },
      ],
      answer: 0,
      explanation: `分成兩個生命週期,就是在告訴編譯器「這兩個參考彼此無關」。found 的回傳型別只提到 'a,所以 hit 只受 text 約束;key 只在 find 執行的那一刻被讀,比對完就沒它的事了。
選「key does not live long enough」的人描述的,正好是把 struct 寫成單一 'a 的版本:那時 text 與 key 共用同一個 'a,'a 被壓成兩者的交集(也就是 key 的內層區塊),回傳的 &'a str 也就不能活出那個區塊——那個版本真的會報這個錯。第 1 題說同一個名字代表「同一條約束」,這題就是「不想被同一條約束綁住」時的寫法。至於「印出 None」:find 在 lk.found() 那一行就執行完了,之後 key 釋放不會回頭影響已經算出來的結果。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "struct Lookup<'a, 'b> {", note: "兩個生命週期參數:宣告這兩個參考可以來自存活期不同的資料。" },
          { code: "    text: &'a str,", note: "被搜尋的文字,受 'a 約束。" },
          { code: "    key: &'b str,", note: "搜尋關鍵字,受 'b 約束,和 'a 無關。" },
          { code: "}", note: "這個定義到此結束。" },
          { code: "", note: "" },
          { code: "impl<'a, 'b> Lookup<'a, 'b> {", note: "兩個參數都要在 impl 上宣告。" },
          { code: "    fn found(&self) -> Option<&'a str> {", note: "回傳型別只提到 'a:結果是 text 的片段,只需要 text 活著。" },
          { code: "        self.text.split(' ').find(|w| w.starts_with(self.key))", note: "key 只在比對時讀取;找到的 w 是 text 的片段,型別是 &'a str。" },
          { code: "    }", note: "方法本體結束。" },
          { code: "}", note: "這個定義到此結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let text = String::from(\"apple banana cherry\");", note: "text 活到 main 結束。" },
          { code: "    let hit;", note: "hit 要活得比內層區塊久。" },
          { code: "    {", note: "內層區塊開始。" },
          { code: "        let key = String::from(\"ba\");", note: "key 只活在這個區塊裡。" },
          { code: "        let lk = Lookup { text: &text, key: &key };", note: "'a 取 text 的範圍,'b 取 key 的範圍,兩者各自獨立。" },
          { code: "        hit = lk.found();", note: "hit 的型別是 Option<&'a str>,只借用 text。" },
          { code: "    }", note: "key 與 lk 在這裡釋放;hit 沒有借用它們,不受影響。" },
          { code: "    println!(\"{:?}\", hit);", note: "印出 Some(\"banana\")。" },
          { code: "}", note: "text 最後釋放。" },
        ],
        outro: "把 struct 改成 Lookup<'a> 並讓兩個欄位共用 'a,同一段 main 會在 &key 那裡報 `key` does not live long enough。多一個生命週期參數的代價是簽名變長,換到的是呼叫端的彈性——只有在「結果只該綁其中一個來源」時才值得這樣拆。",
      },
      csharp: `C# 的 class Lookup { string Text; string Key; } 不需要區分兩個欄位的存活期,GC 讓兩者一律「還有人引用就活著」。Rust 的 'a、'b 是在型別層面寫下「這兩份資料的壽命互不相干」,讓編譯器可以放行「只依賴 text」的結果活得比 key 更久——C# 靠執行期可達性自動做到的事,Rust 要你在簽名上說清楚依賴關係。`,
    },
    {
      id: "2-4-05",
      question: "兩個生命週期參數,回傳型別只標其中一個。以下程式碼的結果是?",
      questionCode: "fn pick<'a, 'b>(x: &'a str, y: &'b str, first: bool) -> &'a str {\n    if first { x } else { y }\n}\n\nfn main() {\n    let a = String::from(\"left\");\n    let b = String::from(\"right\");\n    println!(\"{}\", pick(&a, &b, false));\n}",
      options: [
        { text: "編譯錯誤:lifetime may not live long enough——函式承諾回傳 'a 的資料,但 else 分支交出的是 'b 的 y,而簽名沒有說 'b 至少跟 'a 一樣長" },
        { text: "印出 right——a 和 b 都活到 main 結束,實際上不可能懸空,編譯器看得出來" },
        { text: "編譯錯誤發生在 main:a 和 b 是不同變數,傳進 pick 時必須是同一個生命週期" },
        { text: "印出 right——回傳型別標哪一個名字都可以,編譯器會自動把它調整成兩者中較短的那個" },
      ],
      answer: 0,
      explanation: `編譯器檢查函式本體時看不到呼叫端,只看簽名。在 pick 裡面,'a 與 'b 是兩個毫無關係的區間,'b 可能比 'a 短;回傳型別卻承諾「結果至少活 'a」。else 分支把 y(只保證活 'b)交出去,就是在兌現一個做不到的承諾,錯誤訊息直接點出來:function was supposed to return data with lifetime 'a but it is returning data with lifetime 'b。
「a、b 都活到 main 結束所以沒問題」是把生命週期當成執行期檢查——Rust 對每個泛型函式只檢查一次,要對「所有可能的呼叫」都成立,而不是只對這一次呼叫。「標哪個名字都行,編譯器會自動取短的」也不對:取短的是呼叫端選定 'a 時會做的事,但函式本體必須先證明兩個分支都符合同一個回傳型別。修法是把兩者綁在同一個 'a 上;第 6 題會看到另一種修法。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn pick<'a, 'b>(x: &'a str, y: &'b str, first: bool) -> &'a str {", note: "兩個獨立的生命週期,回傳型別只承諾 'a。簽名裡沒有任何關於 'a 與 'b 誰長誰短的資訊。" },
            { code: "    if first { x } else { y }", note: "⛔ 編譯失敗:lifetime may not live long enough——function was supposed to return data with lifetime 'a but it is returning data with lifetime 'b。else 分支的 y 只保證活 'b。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let a = String::from(\"left\");", note: "a 活到 main 結束。" },
            { code: "    let b = String::from(\"right\");", note: "b 也活到 main 結束——但 pick 的本體根本看不到這件事。" },
            { code: "    println!(\"{}\", pick(&a, &b, false));", note: "呼叫本身沒有錯;錯誤在 pick 的定義就被擋下了。" },
            { code: "}", note: "這個定義到此結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn pick<'a>(x: &'a str, y: &'a str, first: bool) -> &'a str {", note: "改動處:兩個參數都綁同一個 'a。回傳值可能來自任一邊,所以兩邊都要滿足同一個承諾。" },
            { code: "    if first { x } else { y }", note: "兩個分支的型別都是 &'a str,本體檢查通過。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let a = String::from(\"left\");", note: "a 活到 main 結束。" },
            { code: "    let b = String::from(\"right\");", note: "b 活到 main 結束。" },
            { code: "    println!(\"{}\", pick(&a, &b, false));", note: "呼叫端的 'a 取兩者重疊的範圍;first 是 false,印出 right。" },
            { code: "}", note: "這個定義到此結束。" },
          ],
          outro: "判斷原則:回傳值「可能」來自哪幾個參數,那幾個參數就必須跟回傳型別綁在同一條約束下。只可能來自其中一個時,才適合把另一個拆成獨立的生命週期(像第 4 題的 key)。",
        },
      ],
      csharp: `C# 的 string Pick(string x, string y, bool first) 回傳誰都無所謂,兩個字串都在 managed heap 上,由 GC 負責讓它們活到沒人用為止。Rust 的函式簽名則是一份給呼叫端的契約:回傳值能活多久,只能從簽名推出來,所以本體交出去的每一個參考都必須符合簽名寫下的那個生命週期。`,
    },
    {
      id: "2-4-06",
      question: "生命週期之間的 outlives 關係:'a: 'b。以下程式碼的結果是?",
      questionCode: "fn shorter_text<'a: 'b, 'b>(outer: &'a str, inner: &'b str) -> &'b str {\n    if outer.len() < inner.len() { outer } else { inner }\n}\n\nfn main() {\n    let config = String::from(\"ok\");\n    {\n        let input = String::from(\"hello\");\n        println!(\"{}\", shorter_text(&config, &input));\n    }\n}",
      options: [
        { text: "印出 ok——'a: 'b 讀作「'a 至少活得跟 'b 一樣久」,所以 &'a str 可以當成 &'b str 交出去,兩個分支都符合回傳型別" },
        { text: "編譯錯誤:冒號後面只能接 trait,'a: 'b 把生命週期當成 trait 用,語法不合法" },
        { text: "編譯錯誤:'a: 'b 的意思是 'a 比 'b 短,所以把 outer 當成 'b 回傳反而不合法" },
        { text: "編譯錯誤:config 和 input 的作用域不同,呼叫時必須先把兩者放進同一個區塊" },
      ],
      answer: 0,
      explanation: `'a: 'b 是生命週期之間的 bound,讀作「'a outlives 'b」:'a 至少涵蓋 'b 的整段區間(可以一樣長)。有了這條已知關係,本體裡把 outer(&'a str)當成 &'b str 回傳就合法——活得久的參考,本來就能用在只要求活得短的地方。config 長度 2、input 長度 5,回傳 outer,印出 ok。
冒號在 Rust 的泛型位置統一表示「受…約束」:T: Display 是型別受 trait 約束,T: 'a 是型別受生命週期約束(下一題),'a: 'b 是生命週期受生命週期約束——不是把生命週期當 trait。方向也容易讀反:冒號左邊是「活得比較久」的那個,跟 T: Display 的「左邊滿足右邊的要求」是同一種讀法。沒有這條 bound 的話,本體會報 function was supposed to return data with lifetime 'b but it is returning data with lifetime 'a——正是第 5 題那個錯誤,只是方向反過來。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn shorter_text<'a: 'b, 'b>(outer: &'a str, inner: &'b str) -> &'b str {", note: "宣告 'a: 'b:outer 的資料至少活得跟 inner 一樣久。回傳型別只承諾較短的 'b。" },
          { code: "    if outer.len() < inner.len() { outer } else { inner }", note: "outer 是 &'a str,因為 'a 涵蓋 'b,它可以直接當成 &'b str 交出去;inner 本來就是 'b。兩個分支型別一致。" },
          { code: "}", note: "這個定義到此結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let config = String::from(\"ok\");", note: "config 活到 main 結束,對應活得較久的 'a。" },
          { code: "    {", note: "內層區塊開始。" },
          { code: "        let input = String::from(\"hello\");", note: "input 只活在內層區塊,對應較短的 'b。" },
          { code: "        println!(\"{}\", shorter_text(&config, &input));", note: "呼叫端選定 'a、'b,並檢查 'a: 'b 成立(config 確實比 input 活得久)。長度 2 小於 5,回傳 outer,印出 ok。" },
          { code: "    }", note: "input 釋放;回傳值已經用完。" },
          { code: "}", note: "config 釋放。" },
        ],
        outro: "第 5 題的 pick 也可以用這招修:寫成 fn pick<'a, 'b: 'a>(x: &'a str, y: &'b str, first: bool) -> &'a str,宣告 y 至少活得跟 'a 一樣久。實務上多半直接合併成一個 'a 比較好讀;'a: 'b 在 struct 持有「兩層參考」、兩層壽命確實不同時最有用。",
      },
      csharp: `C# 沒有能寫出「A 至少活得跟 B 一樣久」的語法——在 GC 世界裡這件事是隱含的:只要 B 還引用著 A,A 就會一直活著。'a: 'b 相當於把「外層資料活得比內層長」這個事實明文寫進簽名,讓編譯器在沒有 GC 的情況下據以驗證每一次轉手。`,
    },
    {
      id: "2-4-07",
      question: "型別參數的生命週期 bound:T: 'a。以下程式碼的結果是?",
      questionCode: "use std::fmt::Debug;\n\nstruct Slot<'a, T: 'a> {\n    item: &'a T,\n}\n\nfn show<'a, T: Debug + 'a>(s: Slot<'a, T>) {\n    println!(\"{:?}\", s.item);\n}\n\nfn main() {\n    let n = 42;\n    let name = String::from(\"ferris\");\n    let word: &str = \"crab\";\n    show(Slot { item: &n });\n    show(Slot { item: &name });\n    show(Slot { item: &word });\n}",
      options: [
        { text: "印出三行 42、\"ferris\"、\"crab\"——T: 'a 的意思是「T 裡面如果含有參考,那些參考都要活過 'a」;i32、String 不含參考,自動滿足,&'static str 也滿足" },
        { text: "編譯錯誤:T: 'a 要求 T 本身是參考型別,i32 和 String 都不是參考,不能當 T" },
        { text: "編譯錯誤:T 是 &str 時 item 變成 &&str,兩層參考必須各自宣告一個生命週期參數" },
        { text: "編譯錯誤:&'a T 已經隱含 T: 'a,在 struct 上重複寫 T: 'a 屬於衝突的 bound" },
      ],
      answer: 0,
      explanation: `T: 'a 讀作「T outlives 'a」:型別 T 的值在 'a 這段期間內都是有效的。一個型別「會失效」的唯一原因是它裡面藏著參考,所以這條 bound 實際上檢查的是「T 內部的每一個參考都活過 'a」。i32、String 這種完全擁有自己資料的型別,沒有任何會提早失效的部分,對任何 'a 都自動成立;T = &'static str 裡的參考永遠有效,也成立。三次呼叫都通過,印出 42、"ferris"、"crab"(Debug 格式會替字串加引號)。
「T: 'a 要求 T 是參考」是最常見的誤讀——bound 限制的是 T 裡面的參考,不要求 T 本身是參考。「兩層參考要各自一個生命週期」也不對:&'a &'static str 裡的內層 'static 已經在型別裡寫好了,外層只需要 'a。至於重複:自 2018 版起,struct 欄位 &'a T 會讓編譯器自動推出 T: 'a,手寫出來是多餘但完全合法的,常見於想把約束寫明白的程式碼。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::fmt::Debug;", note: "引入 Debug,show 要用 {:?} 印出 item。" },
          { code: "", note: "" },
          { code: "struct Slot<'a, T: 'a> {", note: "T: 'a:T 內部的任何參考都要活過 'a。這裡是明寫,編譯器其實能從下一行自動推出來。" },
          { code: "    item: &'a T,", note: "持有一個指向 T 的參考;要讓 &'a T 有意義,被指向的 T 本身在 'a 期間必須有效——這正是 T: 'a 描述的事。" },
          { code: "}", note: "這個定義到此結束。" },
          { code: "", note: "" },
          { code: "fn show<'a, T: Debug + 'a>(s: Slot<'a, T>) {", note: "型別參數可以同時帶 trait bound 與生命週期 bound,用 + 串起來。" },
          { code: "    println!(\"{:?}\", s.item);", note: "用 Debug 格式印出。" },
          { code: "}", note: "這個定義到此結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let n = 42;", note: "i32,不含任何參考。" },
          { code: "    let name = String::from(\"ferris\");", note: "String 擁有自己的 heap 資料,也不含參考。" },
          { code: "    let word: &str = \"crab\";", note: "word 本身就是參考,指向靜態資料,型別是 &'static str。" },
          { code: "    show(Slot { item: &n });", note: "T = i32,T: 'a 對任何 'a 都成立。印出 42。" },
          { code: "    show(Slot { item: &name });", note: "T = String,同樣自動成立。印出 \"ferris\"(Debug 會加引號)。" },
          { code: "    show(Slot { item: &word });", note: "T = &'static str,item 的型別是 &'a &'static str。內層參考永遠有效,成立。印出 \"crab\"。" },
          { code: "}", note: "name 釋放。" },
        ],
        outro: "T: 'a 只有在 T 裡面真的藏著短命參考時才會擋人。例如 T = &'x str 而 'x 比 'a 短,就不成立——下一題就是這種情況。",
      },
      csharp: `C# 的泛型 where 子句只有型別層面的約束(where T : class、where T : IComparable),沒有「T 裡面的參考要活多久」這種約束——因為 C# 的物件參考不會懸空,GC 讓這件事不需要證明。Rust 的 T: 'a 是泛型碰上「借用」時必要的第三維:除了 T 能做什麼(trait)之外,還要說明 T 能活多久。`,
    },
    {
      id: "2-4-08",
      question: "泛型值被存進一個承諾活 'a 的回傳值裡,卻沒寫 T: 'a。以下程式碼的結果是?",
      questionCode: "fn counter<'a, T: AsRef<str>>(item: T) -> impl Fn() -> usize + 'a {\n    move || item.as_ref().len()\n}\n\nfn main() {\n    let f = counter(String::from(\"hello\"));\n    println!(\"{}\", f());\n}",
      options: [
        { text: "編譯錯誤:the parameter type `T` may not live long enough——回傳的閉包擁有 item 並承諾活 'a,但 T 可能含有比 'a 短的參考,簽名必須加上 T: 'a" },
        { text: "印出 5——簽名宣告了 <'a, T> 兩個參數,T 就自動被當成活得過 'a" },
        { text: "印出 5——main 傳進去的是擁有資料的 String,不含參考,所以這次呼叫不會有問題" },
        { text: "編譯錯誤:閉包不能捕獲泛型型別的值,要先把 item 裝進 Box 才能回傳" },
      ],
      answer: 0,
      explanation: `回傳型別 impl Fn() -> usize + 'a 承諾:這個閉包在 'a 期間內都有效。閉包用 move 把 item 整個搬了進去,所以閉包有效的前提是 item(型別 T)有效。問題在於 T 是任意型別——呼叫端可以傳一個 &'x str 進來,'x 比 'a 短,那閉包就會在承諾期間內帶著一個懸空參考。簽名沒有排除這種可能,所以編譯器拒絕:the parameter type T may not live long enough,並建議加上 T: 'a。
「<'a, T> 寫在一起,T 就自動被視為活過 'a」是本題的主要誤解——兩個參數是各自獨立的,彼此的關係必須明寫。「main 傳的是 String 所以沒事」則是把泛型當成巨集:Rust 對泛型函式本體只檢查一次,要對所有可能的 T 成立,跟這一次呼叫傳了什麼無關。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn counter<'a, T: AsRef<str>>(item: T) -> impl Fn() -> usize + 'a {", note: "⛔ 編譯失敗:the parameter type `T` may not live long enough。回傳型別承諾活 'a,但 T 只有 AsRef<str> 一條約束,沒有說明它能活多久。" },
            { code: "    move || item.as_ref().len()", note: "閉包以 move 取得 item 的所有權,item 從此是閉包的一部分:閉包能活多久,取決於 T 能活多久。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let f = counter(String::from(\"hello\"));", note: "這次傳的 String 其實沒問題,但編譯器在 counter 的定義就擋下了,不會等到呼叫。" },
            { code: "    println!(\"{}\", f());", note: "如果能編譯,會印出 5。" },
            { code: "}", note: "這個定義到此結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn counter<'a, T: AsRef<str> + 'a>(item: T) -> impl Fn() -> usize + 'a {", note: "改動處:加上 T: 'a。現在簽名保證 T 內部的參考都活過 'a,閉包的承諾才兌現得了。" },
            { code: "    move || item.as_ref().len()", note: "本體不用改,閉包一樣擁有 item。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let f = counter(String::from(\"hello\"));", note: "T = String,不含參考,T: 'a 自動成立。" },
            { code: "    println!(\"{}\", f());", note: "印出 5。" },
            { code: "}", note: "這個定義到此結束。" },
          ],
          outro: "代價在呼叫端:現在若傳入 &'x str,呼叫端選的 'a 就不能超過 'x——約束沒有消失,只是從「定義處無法證明」變成「每次呼叫各自檢查」。",
        },
      ],
      csharp: `C# 的 Func<int> Counter<T>(T item) => () => item.ToString().Length; 不會有這個問題,也沒有這個問題的位置:lambda 捕獲 item 之後,GC 讓 item 參考到的物件一直活著。Rust 的閉包沒有 GC 可以依靠,它能活多久由它捕獲的東西決定,而泛型 T 捕獲了什麼,只能靠簽名上的 T: 'a 事先說清楚。`,
    },
    {
      id: "2-4-09",
      question: "本課最大的迷思:T: 'static 到底要求什麼?以下程式碼的結果是?",
      questionCode: "use std::fmt::Debug;\n\nfn register<T: Debug + 'static>(item: T) {\n    println!(\"registered {:?}\", item);\n}\n\nfn main() {\n    let name = String::from(\"temp\");\n    register(&name);\n}",
      options: [
        { text: "編譯錯誤:`name` does not live long enough(argument requires that `name` is borrowed for `'static`)——T 被推成指向區域變數的參考,不滿足 'static;直接傳 name 本身就能編譯,即使 name 是 main 裡剛建立、用完就釋放的值" },
        { text: "印出 registered \"temp\"——register 只在呼叫期間用到 item,呼叫結束前 name 都還活著" },
        { text: "編譯錯誤,而且改成傳 name 本身也沒用:T: 'static 要求值必須活到程式結束,只能傳字串字面值或刻意洩漏的記憶體" },
        { text: "印出 registered \"temp\"——編譯器看到 'static bound,會把 name 提升到靜態記憶體區" },
      ],
      answer: 0,
      explanation: `T: 'static 是第 7 題 T: 'a 在 'a = 'static 時的特例,意思一樣:「T 裡面如果有參考,那些參考都必須是 'static」。它限制的是型別,不是值:一個不含任何參考的型別(String、Vec<i32>、自訂的擁有型 struct)永遠滿足 T: 'static,不管那個值是什麼時候建立、什麼時候被 drop。
這題傳的是 &name,T 被推成 &'x String,'x 是 name 的區域範圍,不是 'static,所以報錯,訊息也寫得很明白:argument requires that name is borrowed for 'static。改成 register(name),T = String,立刻通過——而那個 String 在 register 結束時就被釋放,根本沒有「活到程式結束」。「必須活到程式結束」正是這題要破除的迷思:'static bound 的真正含義是「這個值可以被你留多久都安全」,擁有資料的值天生就是這樣。「只在呼叫期間用到」則是把 bound 當成使用期間的描述;bound 是型別層面的條件,不看本體實際怎麼用。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "use std::fmt::Debug;", note: "引入 Debug。" },
            { code: "", note: "" },
            { code: "fn register<T: Debug + 'static>(item: T) {", note: "T: 'static:T 裡面若有參考,必須是 'static 的參考。" },
            { code: "    println!(\"registered {:?}\", item);", note: "本體只是印出來——但 bound 是型別條件,不管本體怎麼用。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let name = String::from(\"temp\");", note: "name 是 main 的區域變數。" },
            { code: "    register(&name);", note: "⛔ 編譯失敗:`name` does not live long enough——argument requires that `name` is borrowed for `'static`。T 被推成 &String,而這個參考只活在 main 的範圍內。" },
            { code: "}", note: "name 在這裡釋放,所以對它的借用不可能是 'static。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::fmt::Debug;", note: "引入 Debug。" },
            { code: "", note: "" },
            { code: "fn register<T: Debug + 'static>(item: T) {", note: "簽名完全不用改。" },
            { code: "    println!(\"registered {:?}\", item);", note: "印出 registered \"temp\"。" },
            { code: "}", note: "item 在這裡被 drop——它根本沒有活到程式結束,卻完全滿足 T: 'static。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let name = String::from(\"temp\");", note: "一樣是剛建立的區域值。" },
            { code: "    register(name);", note: "改動處:傳值而不是傳參考。T = String,不含任何參考,T: 'static 成立;name 的所有權移進 register。" },
            { code: "    println!(\"main continues\");", note: "程式照常往下跑,印出 main continues。" },
            { code: "}", note: "這個定義到此結束。" },
          ],
          outro: "帶著走的一句話:T: 'static 不是「這個值活到程式結束」,而是「這個值不借用任何會先死的東西」。擁有型別永遠通過;只有「含非 'static 參考」的型別會被擋。",
        },
      ],
      csharp: `最容易被 C# 的 static 誤導:C# 的 static 欄位確實活到 AppDomain 結束,但 Rust 的 'static bound 跟「存放在哪裡、活多久」無關,它只是說「不含短命的借用」。在 C# 裡,把任何物件交給一個會長期保存它的 API(例如塞進背景工作佇列)都沒問題,GC 會保它;Rust 用 T: 'static 表達同一個需求——「我可能會留著它很久」——並在編譯期拒絕「借來的、會先失效」的東西。`,
    },
    {
      id: "2-4-10",
      question: "延續上題:含短命參考的型別,能靠 'static bound「延長」壽命嗎?以下程式碼的結果是?",
      questionCode: "struct Record<'a> {\n    name: &'a str,\n}\n\nfn archive<T: 'static>(store: &mut Vec<T>, item: T) {\n    store.push(item);\n}\n\nfn main() {\n    let mut store = Vec::new();\n    let line = String::from(\"alice\");\n    archive(&mut store, Record { name: &line });\n    println!(\"{}\", store.len());\n}",
      options: [
        { text: "編譯錯誤:`line` does not live long enough——Record<'x> 只有在 'x 是 'static 時才滿足 T: 'static,bound 只負責「拒絕」,不會延長 line 的壽命;要存進去就讓 Record 擁有自己的 String" },
        { text: "印出 1——加了 'static bound,編譯器就會把 line 的壽命延長到符合要求" },
        { text: "印出 1——line 活到 main 結束,而 main 結束就等於程式結束,所以 main 的區域變數都算 'static" },
        { text: "編譯錯誤:'static 只能寫在參考型別上(&'static str),不能當作泛型參數的 bound" },
      ],
      answer: 0,
      explanation: `生命週期 bound 是檢查,不是指令。T: 'static 要求 Record<'x> 裡的 'x 必須是 'static,而 &line 借的是一個區域變數,編譯器找不到符合的 'x,只能報錯:line does not live long enough,argument requires that line is borrowed for 'static。沒有任何標註能讓 line 活得更久。
「main 的區域變數活到程式結束,所以算 'static」是很常見也很合理的推論,但不成立:main 的區域變數在 main 回傳時就被 drop,之後還有其他清理動作;更重要的是編譯器不會為 main 開特例,它看到的就是一個區域借用。「'static 只能寫在參考上」則是把入門課學到的 &'static str 當成唯一用法;第 9 題已經看到 T: 'static 是合法而且常見的 bound。真正的修法是改設計:要被長期保存的資料就讓它擁有自己的內容,Record 改存 String,T: 'static 自然成立。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "struct Record<'a> {", note: "Record 借用外部字串,所以帶 'a。" },
            { code: "    name: &'a str,", note: "name 不擁有資料。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "fn archive<T: 'static>(store: &mut Vec<T>, item: T) {", note: "T: 'static:要存進 store 的東西不能借用任何會先死的資料。" },
            { code: "    store.push(item);", note: "把 item 放進集合。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let mut store = Vec::new();", note: "元素型別由下面的呼叫推斷。" },
            { code: "    let line = String::from(\"alice\");", note: "line 是 main 的區域變數,main 回傳時就會被 drop。" },
            { code: "    archive(&mut store, Record { name: &line });", note: "⛔ 編譯失敗:`line` does not live long enough——argument requires that `line` is borrowed for `'static`。Record<'x> 的 'x 只能是 line 的區域範圍,不是 'static。" },
            { code: "    println!(\"{}\", store.len());", note: "如果能編譯,這裡會印出 1。" },
            { code: "}", note: "line 在這裡被釋放。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "struct Record {", note: "改動處:拿掉生命週期參數。" },
            { code: "    name: String,", note: "改動處:Record 擁有自己的字串。不含任何參考,T: 'static 自動成立。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "fn archive<T: 'static>(store: &mut Vec<T>, item: T) {", note: "簽名不用改。" },
            { code: "    store.push(item);", note: "把 item 放進集合。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let mut store = Vec::new();", note: "元素型別推斷為 Record。" },
            { code: "    let line = String::from(\"alice\");", note: "一樣是區域變數。" },
            { code: "    archive(&mut store, Record { name: line });", note: "改動處:把 line 的所有權移進 Record,不再借用。" },
            { code: "    println!(\"{} {}\", store.len(), store[0].name);", note: "印出 1 alice。" },
            { code: "}", note: "store 連同裡面的 Record 一起釋放。" },
          ],
          outro: "遇到 'static bound 擋路時,方向是「讓資料被擁有」(String、Vec、clone 一份),而不是找辦法讓借用變長。",
        },
      ],
      csharp: `C# 開發者最常這樣想:「我把東西交給一個長期保存它的集合,集合還在,東西就還在」——在 GC 下這是真的。Rust 的 T: 'static 是把同樣的需求寫成型別條件,但它只能判斷「你交出來的東西能不能被長期保存」,不能替你延長什麼。能被保存的,是擁有自己資料的值;借來的東西,在借出者離開時就注定失效。`,
    },
    {
      id: "2-4-11",
      question: "struct 裡巢狀持有另一個帶生命週期的 struct。以下程式碼的結果是?",
      questionCode: "struct Line<'a> {\n    text: &'a str,\n}\n\nstruct Page<'a> {\n    header: Line<'a>,\n    body: Vec<Line<'a>>,\n}\n\nfn main() {\n    let doc = String::from(\"Title\\nfirst\\nsecond\");\n    let mut lines = doc.lines();\n    let header = Line { text: lines.next().unwrap() };\n    let body: Vec<Line> = lines.map(|t| Line { text: t }).collect();\n    let page = Page { header, body };\n    println!(\"{} ({} lines)\", page.header.text, page.body.len());\n}",
      options: [
        { text: "印出 Title (2 lines)——Page 持有 Line<'a>,等於間接持有參考,所以 Page 也必須宣告 <'a> 並往下傳;main 裡的型別標註則可以省略生命週期,交給推斷" },
        { text: "編譯錯誤:Line 已經宣告過 'a,Page 再宣告一次 <'a> 屬於重複宣告,應該寫成 struct Page { header: Line, ... }" },
        { text: "編譯錯誤:Vec 裡的每個 Line 來自不同的行,需要各自不同的生命週期參數" },
        { text: "印出 Title (3 lines)——lines() 回傳的迭代器被 header 用過之後會從頭再來一次" },
      ],
      answer: 0,
      explanation: `生命週期參數會「往外傳染」:只要一個型別裡面(直接或間接)含有借用,它就必須帶生命週期參數,好讓使用者知道它不能活過被借用的資料。Page 裡的 Line<'a> 藏著 &'a str,所以 Page 也要宣告 <'a>,再把它交給 Line。三行文字切出 Title 當 header,剩下兩行進 body,印出 Title (2 lines)。
「Line 自己宣告過了,Page 不用再寫」是這題的核心誤解——struct 定義是公開的型別,每一層都要把參數寫完整,寫成 header: Line 會得到 missing lifetime specifier。容易混淆的是 main 裡的 let body: Vec<Line> 可以不寫生命週期:那是函式本體裡的區域型別標註,編譯器能自己推斷;只有 struct、函式簽名這類「對外的定義」需要明寫。至於 Vec 裡的元素:它們全都切自同一個 doc,共用一個 'a 剛好描述「全部都不能活過 doc」。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "struct Line<'a> {", note: "Line 借用一行文字。" },
          { code: "    text: &'a str,", note: "指向原始文件裡的某一段。" },
          { code: "}", note: "這個定義到此結束。" },
          { code: "", note: "" },
          { code: "struct Page<'a> {", note: "Page 本身沒有直接寫 &,但它持有 Line<'a>,等於間接借用,所以也要宣告 'a。" },
          { code: "    header: Line<'a>,", note: "把自己的 'a 傳給 Line。" },
          { code: "    body: Vec<Line<'a>>,", note: "Vec 裡每個 Line 都受同一個 'a 約束——它們都切自同一份文件。" },
          { code: "}", note: "這個定義到此結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let doc = String::from(\"Title\\nfirst\\nsecond\");", note: "doc 擁有整份文件,所有 Line 都借用它。" },
          { code: "    let mut lines = doc.lines();", note: "逐行切割的迭代器,產出的每一行都是指進 doc 的 &str。" },
          { code: "    let header = Line { text: lines.next().unwrap() };", note: "取出第一行 Title。迭代器前進了一格。" },
          { code: "    let body: Vec<Line> = lines.map(|t| Line { text: t }).collect();", note: "剩下的 first、second 包成 Line 收集起來。區域標註 Vec<Line> 省略了生命週期,由編譯器推斷。" },
          { code: "    let page = Page { header, body };", note: "header 與 body 移進 page(欄位簡寫)。page 的 'a 就是 doc 的有效範圍。" },
          { code: "    println!(\"{} ({} lines)\", page.header.text, page.body.len());", note: "印出 Title (2 lines)。" },
          { code: "}", note: "page 先釋放,doc 最後釋放——順序正好符合「借用者不能活過資料」。" },
        ],
        outro: "把 Page 寫成 struct Page { header: Line } 會得到 error[E0106]: missing lifetime specifier。規則很機械:型別定義裡出現任何帶生命週期的型別,就要在外層宣告參數並傳下去。",
      },
      csharp: `C# 的 class Page { Line Header; List<Line> Body; } 一層層持有物件,不需要任何額外宣告,存活期完全交給 GC。Rust 的 Page<'a> 把「我裡面某處借用了外部資料」這個事實一路標到最外層,好處是任何人拿到 Page<'a> 這個型別,不用看實作就知道它不能活過原始文件——在 C# 裡這種「這個物件其實只是某份緩衝區的檢視」的關係只能寫在文件註解裡。`,
    },
    {
      id: "2-4-12",
      question: "方法的回傳值不綁 self,而是綁另一個參數。以下程式碼的結果是?",
      questionCode: "struct Trimmer<'a> {\n    prefix: &'a str,\n}\n\nimpl<'a> Trimmer<'a> {\n    fn strip<'b>(&self, s: &'b str) -> &'b str {\n        s.strip_prefix(self.prefix).unwrap_or(s)\n    }\n}\n\nfn main() {\n    let path = String::from(\"/usr/local/bin\");\n    let rest;\n    {\n        let prefix = String::from(\"/usr\");\n        let t = Trimmer { prefix: &prefix };\n        rest = t.strip(&path);\n    }\n    println!(\"{}\", rest);\n}",
      options: [
        { text: "印出 /local/bin——strip 用 'b 明確把回傳值綁到參數 s 上,推翻了「有 &self 就綁 self」的預設;Trimmer 與 prefix 在內層區塊結束時釋放,不影響 rest" },
        { text: "編譯錯誤:只要方法有 &self,回傳的參考就一定綁在 self 上,不可能綁到其他參數" },
        { text: "編譯錯誤:impl<'a> 已經宣告過生命週期,方法上不能再引入新的 'b" },
        { text: "編譯錯誤:`prefix` does not live long enough——strip_prefix 比對時用到了 self.prefix,所以結果也借用了 prefix" },
      ],
      answer: 0,
      explanation: `第三條省略規則只是「沒寫的時候」的預設。strip 自己宣告了方法層級的 'b,並把參數 s 與回傳型別都標成 'b,等於明說「結果切自 s,只依賴 s」。strip_prefix 回傳的確實是 s 的片段,本體檢查通過;呼叫端的 rest 只借用 path,prefix 與 t 在內層區塊結束時釋放完全不影響它,印出 /local/bin。
「有 &self 就一定綁 self」是把省略規則誤當成硬規定;「impl 宣告過就不能再加」也不對——方法可以有自己的泛型與生命週期參數,和 impl 上的並存。「比對時用到 prefix,所以結果也借用 prefix」混淆了「計算時讀過」和「結果指向」:prefix 只是被拿來比較,回傳的片段指向的是 s 的資料。如果把簽名寫成省略形式 fn strip(&self, s: &str) -> &str,反而會在定義處就編譯失敗——那是第 14 題的主題。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "struct Trimmer<'a> {", note: "Trimmer 借用一個前綴字串。" },
          { code: "    prefix: &'a str,", note: "要去掉的前綴。" },
          { code: "}", note: "這個定義到此結束。" },
          { code: "", note: "" },
          { code: "impl<'a> Trimmer<'a> {", note: "impl 層級的 'a 屬於 Trimmer。" },
          { code: "    fn strip<'b>(&self, s: &'b str) -> &'b str {", note: "方法自己宣告 'b,把回傳值綁到 s。有明確標註,省略規則就不套用,回傳值與 &self 無關。" },
          { code: "        s.strip_prefix(self.prefix).unwrap_or(s)", note: "self.prefix 只用來比對;成功時回傳 s 去掉前綴後的片段,失敗時回傳 s 本身——兩者都指向 s 的資料,都是 &'b str。" },
          { code: "    }", note: "方法本體結束。" },
          { code: "}", note: "這個定義到此結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let path = String::from(\"/usr/local/bin\");", note: "path 活到 main 結束。" },
          { code: "    let rest;", note: "rest 要活得比內層區塊久。" },
          { code: "    {", note: "內層區塊開始。" },
          { code: "        let prefix = String::from(\"/usr\");", note: "prefix 只活在內層區塊。" },
          { code: "        let t = Trimmer { prefix: &prefix };", note: "t 借用 prefix。" },
          { code: "        rest = t.strip(&path);", note: "'b 取 path 的範圍;rest 只借用 path。" },
          { code: "    }", note: "t、prefix 釋放,rest 不受影響。" },
          { code: "    println!(\"{}\", rest);", note: "印出 /local/bin。" },
          { code: "}", note: "path 釋放。" },
        ],
        outro: "三種常見的方法回傳綁定:省略(綁 &self,結果不能活過這次借用)、&'a(綁 struct 持有的資料,第 2 題)、方法自己的 'b(綁某個參數,本題)。選哪一種,取決於回傳的參考實際指向誰。",
      },
      csharp: `C# 的方法回傳參數的一部分(例如 string Strip(string s) => s.Substring(...))是再普通不過的事,也不需要說明結果跟 this 或 s 誰有關。Rust 裡這個「結果依賴誰」會直接影響呼叫端能把結果留多久,所以預設值(綁 self)不對的時候,就要用顯式標註推翻它。`,
    },
    {
      id: "2-4-13",
      question: "錯誤訊息解讀:returns a value referencing data owned by the current function。以下程式碼的結果與正確解讀是?",
      questionCode: "fn first_word(text: &str) -> &str {\n    let words: Vec<&str> = text.split_whitespace().collect();\n    let first = words[0].to_string();\n    first.as_str()\n}\n\nfn main() {\n    let s = String::from(\"hello world\");\n    println!(\"{}\", first_word(&s));\n}",
      options: [
        { text: "編譯錯誤:first 是函式自己新建並擁有的 String,回傳指向它的參考會懸空;而 words[0] 本身就指向 text 的資料,直接回傳它就能編譯" },
        { text: "編譯錯誤:這個訊息代表 Rust 的函式不能回傳參考,只要改成回傳 String 就是唯一的解法" },
        { text: "印出 hello——to_string 複製出一份獨立的資料,指向副本的參考是安全的" },
        { text: "編譯錯誤,而且回傳 words[0] 也一樣不行:words 是函式裡的區域 Vec,從裡面取出的東西同樣屬於這個函式" },
      ],
      answer: 0,
      explanation: `錯誤訊息的全文是 cannot return value referencing local variable first,標註寫著 returns a value referencing data owned by the current function。重點在「owned by the current function」:不是回傳參考本身有錯,而是這個參考指向的資料由函式自己擁有,函式結束就被 drop。first 是 to_string 新配置的 String,正是這種情況。
最值得分辨的是最後一個干擾項。words 確實是區域 Vec,但它的元素型別是 &str,每個元素都是指向 text 的參考;words[0] 取出的是那根參考的複本(&str 是 Copy),它指向的是呼叫端的資料,跟 words 這個 Vec 的死活無關,所以可以回傳。「Rust 函式不能回傳參考」則是被這個訊息嚇到後的過度推論——函式簽名 fn first_word(text: &str) -> &str 本身完全合法,只要回傳值指向 text 就行。改成回傳 String 也能編譯,但那是多做了一次配置,不是唯一解。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn first_word(text: &str) -> &str {", note: "省略規則:唯一的參考參數是 text,回傳值綁 text。簽名本身沒問題。" },
            { code: "    let words: Vec<&str> = text.split_whitespace().collect();", note: "words 是區域 Vec,但它的每個元素都是指進 text 的 &str。" },
            { code: "    let first = words[0].to_string();", note: "to_string 配置一個全新的 String,擁有者是函式裡的 first——這份資料已經跟 text 無關。" },
            { code: "    first.as_str()", note: "⛔ 編譯失敗:cannot return value referencing local variable `first`——returns a value referencing data owned by the current function。回傳的 &str 指向 first,而 first 在函式結束時就被釋放。" },
            { code: "}", note: "first 與 words 在這裡被 drop。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let s = String::from(\"hello world\");", note: "s 擁有原始資料。" },
            { code: "    println!(\"{}\", first_word(&s));", note: "如果能編譯,這裡會讀到已釋放的記憶體。" },
            { code: "}", note: "這個定義到此結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn first_word(text: &str) -> &str {", note: "簽名不用改。" },
            { code: "    let words: Vec<&str> = text.split_whitespace().collect();", note: "一樣收集成 Vec<&str>。" },
            { code: "    words[0]", note: "改動處:直接回傳元素。words[0] 是一根指向 text 的 &str(Copy 出來的),Vec 被 drop 不影響它指向的資料。" },
            { code: "}", note: "words 在這裡被 drop,只釋放了 Vec 自己的緩衝區,text 的資料原封不動。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let s = String::from(\"hello world\");", note: "s 擁有原始資料。" },
            { code: "    println!(\"{}\", first_word(&s));", note: "回傳值指向 s,印出 hello。" },
            { code: "}", note: "這個定義到此結束。" },
          ],
          outro: "實務上連中間的 Vec 都不需要:text.split_whitespace().next().unwrap() 一步到位。讀這個錯誤訊息時要問的是「我回傳的參考指向誰擁有的資料」,而不是「能不能回傳參考」。",
        },
      ],
      csharp: `C# 沒有對應的錯誤類別:回傳一個在方法裡新建的物件參考是日常操作,GC 會讓它活著。唯一接近的是 C# 的 ref 回傳與 Span<T>——return ref localVar; 會被編譯器擋下(不能回傳區域變數的 ref),原因和 Rust 這題完全一樣。差別是 C# 只在少數 ref/Span 場景做這種檢查,Rust 對每一個參考都做。`,
    },
    {
      id: "2-4-14",
      question: "錯誤訊息解讀:錯誤訊息裡出現了你沒寫過的 '1、'2。以下程式碼的結果與正確解讀是?",
      questionCode: "struct Settings<'a> {\n    theme: Option<&'a str>,\n}\n\nimpl<'a> Settings<'a> {\n    fn theme_or(&self, fallback: &str) -> &str {\n        match self.theme {\n            Some(t) => t,\n            None => fallback,\n        }\n    }\n}\n\nfn main() {\n    let s = Settings { theme: None };\n    println!(\"{}\", s.theme_or(\"light\"));\n}",
      options: [
        { text: "編譯錯誤:lifetime may not live long enough——'1、'2 是編譯器替省略的生命週期取的臨時名字;省略規則讓回傳值綁 &self('2),但 None 分支交出的是 fallback('1),兩者沒有關係。修法是讓 fallback 與回傳值都綁 struct 的 'a" },
        { text: "編譯錯誤:錯誤訊息裡的 '1、'2 是不合法的生命週期名稱(生命週期名不能以數字開頭),要找出程式碼裡寫錯的標註並改名" },
        { text: "印出 light——兩個分支來源不同時,編譯器會自動取兩者中較短的生命週期當作回傳值的生命週期" },
        { text: "編譯錯誤,照編譯器的 help 把 fallback 與回傳值標成新的 'b 就能修好,self.theme 那一支不受影響" },
      ],
      explanation: `lifetime may not live long enough 搭配 '1、'2 是現代 rustc 最常見、也最讓人困惑的生命週期錯誤。'1、'2 不在你的程式碼裡,它們是編譯器替「省略掉的生命週期」取的臨時名字,訊息會在簽名上標出哪個參考是 '1、哪個是 '2。這題裡 &self 是 '2,fallback 是 '1;依省略規則,回傳值綁 &self,也就是 '2。match 的 None 分支交出 fallback('1),而簽名沒有任何「'1 活得比 '2 久」的保證,於是報錯。這不是語法錯誤,而是簽名描述的關係跟本體實際做的事對不上——編譯器沒辦法替你決定回傳值「應該」綁哪一個,只能照預設綁 self,再告訴你本體不符合。
最陷阱的是 help 建議:它提示加一個 'b 標在 fallback 與回傳值上。照做之後 fallback 那一支通過了,但 Some(t) 那一支交出的是 &'a str,換成「'a 與 'b 沒有關係」的同一種錯誤。真正的關係是:兩個分支都該受 struct 的 'a 約束,所以讓 fallback: &'a str、回傳 &'a str。`,
      answer: 0,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "struct Settings<'a> {", note: "Settings 可能借用一個主題名稱。" },
            { code: "    theme: Option<&'a str>,", note: "有值時是 &'a str。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "impl<'a> Settings<'a> {", note: "為 Settings<'a> 實作方法。" },
            { code: "    fn theme_or(&self, fallback: &str) -> &str {", note: "三個參考都沒標註。編譯器稱 &self 為 '2、fallback 為 '1;省略規則讓回傳值綁 '2。" },
            { code: "        match self.theme {", note: "取出 Option<&'a str>(Copy)。" },
            { code: "            Some(t) => t,", note: "t 是 &'a str;'a 活得比這次 &self 借用久,當成 '2 回傳沒問題。" },
            { code: "            None => fallback,", note: "⛔ 編譯失敗:lifetime may not live long enough——method was supposed to return data with lifetime '2 but it is returning data with lifetime '1。fallback 跟 &self 毫無關係。" },
            { code: "        }", note: "match 結束。" },
            { code: "    }", note: "方法本體結束。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let s = Settings { theme: None };", note: "沒有設定主題。" },
            { code: "    println!(\"{}\", s.theme_or(\"light\"));", note: "如果能編譯,會走 None 分支印出 light。" },
            { code: "}", note: "這個定義到此結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "struct Settings<'a> {", note: "struct 定義不用改。" },
            { code: "    theme: Option<&'a str>,", note: "有值時是 &'a str。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "impl<'a> Settings<'a> {", note: "為 Settings<'a> 實作方法。" },
            { code: "    fn theme_or(&self, fallback: &'a str) -> &'a str {", note: "改動處:fallback 與回傳值都標 'a。兩個分支的來源(self.theme 與 fallback)現在受同一條約束,&self 則回到「只是暫時借用」的角色。" },
            { code: "        match self.theme {", note: "取出 Option<&'a str>。" },
            { code: "            Some(t) => t,", note: "t 是 &'a str,符合回傳型別。" },
            { code: "            None => fallback,", note: "fallback 也是 &'a str,符合回傳型別。" },
            { code: "        }", note: "match 結束。" },
            { code: "    }", note: "方法本體結束。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let s = Settings { theme: None };", note: "沒有設定主題。" },
            { code: "    println!(\"{}\", s.theme_or(\"light\"));", note: "走 None 分支,印出 light。" },
            { code: "    let dark = Settings { theme: Some(\"dark\") };", note: "設定了主題。" },
            { code: "    println!(\"{}\", dark.theme_or(\"light\"));", note: "走 Some 分支,印出 dark。" },
            { code: "}", note: "這個定義到此結束。" },
          ],
        },
        {
          label: "△ Desugared (what the compiler sees)",
          intro: "錯誤訊息提到了 '1 和 '2,但你的程式碼裡沒有這兩個名字。因為省略的生命週期在編譯器眼中都是具名的:",
          lines: [
            { code: "fn theme_or(&self, fallback: &str) -> &str", note: "你寫的簽名。" },
            { code: "", note: "" },
            { code: "// 編譯器把它展開成(名字只是示意,'1、'2 在程式碼裡不能直接寫):", note: "以下才是編譯器實際檢查的簽名。" },
            { code: "fn theme_or<'s, 'f>(self: &'s Settings<'a>, fallback: &'f str) -> &'s str", note: "省略規則一:每個參考參數各得一個生命週期('s 就是訊息裡的 '2,'f 就是 '1)。省略規則三:有 &self 時回傳值綁 self 的那一個,所以回傳型別是 &'s str。" },
            { code: "// 本體的 None => fallback 交出 &'f str,但簽名要求 &'s str,而 'f 與 's 之間沒有任何已知關係", note: "這就是 method was supposed to return data with lifetime '2 but it is returning data with lifetime '1 的完整意思。" },
          ],
        },
      ],
      csharp: `類比 C# 的型別推斷失敗:var x = cond ? 1 : null; 會得到「無法判斷條件運算式的型別」之類的錯誤,因為兩個分支沒有共同型別,編譯器不替你猜。Rust 這題是生命週期版本:兩個分支的來源各有各的生命週期,簽名沒說它們的關係,編譯器只能照省略規則選一個,然後指出另一個分支不符合。不同的是 C# 的錯誤訊息用的是你看得到的型別名,Rust 則會替你沒寫的生命週期臨時取名 '1、'2。`,
    },
    {
      id: "2-4-15",
      question: "struct 同時帶生命週期與型別參數時,以下哪一個 impl 區塊可以編譯?(struct 定義為 struct Tagged<'a, T> { tag: &'a str, value: T })",
      questionCode: "use std::fmt::Display;\n\nstruct Tagged<'a, T> {\n    tag: &'a str,\n    value: T,\n}\n\n// 需求:為所有 T: Display 的 Tagged 實作 show 方法",
      options: [
        { code: "impl<'a, T: Display> Tagged<'a, T> {\n    fn show(&self) -> String {\n        format!(\"[{}] {}\", self.tag, self.value)\n    }\n}" },
        { code: "impl<T: Display, 'a> Tagged<'a, T> {\n    fn show(&self) -> String {\n        format!(\"[{}] {}\", self.tag, self.value)\n    }\n}" },
        { code: "impl<'a, T: Display> Tagged<T, 'a> {\n    fn show(&self) -> String {\n        format!(\"[{}] {}\", self.tag, self.value)\n    }\n}" },
        { code: "impl<T: Display> Tagged<'a, T> {\n    fn show(&self) -> String {\n        format!(\"[{}] {}\", self.tag, self.value)\n    }\n}" },
      ],
      answer: 0,
      explanation: `規則有兩條,struct 與 impl 都適用:一、參數列表裡生命週期一律排在型別參數前面;二、impl 後面的 <...> 是「宣告」,型別名稱後面的 <...> 是「使用」,使用時的順序必須跟 struct 定義一致,而且用到的名字都要先宣告。
把 impl<T: Display, 'a> 的順序對調,錯在宣告處:lifetime parameters must be declared prior to type and const parameters。寫成 Tagged<T, 'a>,宣告沒問題,但使用時位置跟 struct 定義對不上,編譯器在第一個位置預期生命週期卻拿到型別:type provided when a lifetime was expected。只寫 impl<T: Display> 而直接在 Tagged<'a, T> 裡用 'a,則是 use of undeclared lifetime name 'a——方法層級可以省略生命週期,但 impl 標頭用到的每個生命週期都必須宣告(或改寫成匿名的 Tagged<'_, T>)。入門課練過函式簽名上的排序,這題是同一條規則在 struct + impl 組合上的樣子。`,
      walkthrough: {
        label: "⊕ The compiling impl — walkthrough (runs successfully)",
        lines: [
          { code: "use std::fmt::Display;", note: "引入 Display,show 要用 {} 格式化 value。" },
          { code: "", note: "" },
          { code: "struct Tagged<'a, T> {", note: "定義順序:生命週期在前,型別在後。" },
          { code: "    tag: &'a str,", note: "借用一個標籤字串。" },
          { code: "    value: T,", note: "擁有一個任意型別的值。" },
          { code: "}", note: "這個定義到此結束。" },
          { code: "", note: "" },
          { code: "impl<'a, T: Display> Tagged<'a, T> {", note: "impl<'a, T: Display> 宣告兩個參數(順序同樣生命週期在前),Tagged<'a, T> 依 struct 定義的位置使用它們。" },
          { code: "    fn show(&self) -> String {", note: "回傳擁有的 String,不牽涉任何生命週期綁定。" },
          { code: "        format!(\"[{}] {}\", self.tag, self.value)", note: "T: Display 保證 value 能用 {} 格式化。" },
          { code: "    }", note: "方法本體結束。" },
          { code: "}", note: "這個定義到此結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let tag = String::from(\"temp\");", note: "標籤字串的擁有者。" },
          { code: "    let t = Tagged { tag: &tag, value: 36.5 };", note: "'a 取 tag 的範圍,T 推斷為 f64。" },
          { code: "    println!(\"{}\", t.show());", note: "印出 [temp] 36.5。" },
          { code: "}", note: "這個定義到此結束。" },
        ],
        outro: "不需要在方法裡用到 'a 時,也可以寫成 impl<T: Display> Tagged<'_, T>:'_ 是匿名生命週期,表示「這裡有一個,但我不需要叫它的名字」。",
      },
      csharp: `C# 的泛型類別 class Tagged<T> 只有型別參數一種,不存在排序問題;擴充方法或部分特化也不需要重新宣告參數。Rust 的 impl 區塊本身就是泛型的(impl<...> 相當於「對所有這些參數成立」),所以每次都要把參數重新宣告一遍,順序與使用位置也都有規矩。`,
    },
    {
      id: "2-4-16",
      question: "生命週期的子類型化:活得久的參考能不能放進要求較短的地方?以下程式碼的結果是?",
      questionCode: "fn main() {\n    let owned = String::from(\"local\");\n    let mut names: Vec<&str> = vec![owned.as_str()];\n    let lit: &'static str = \"forever\";\n    names.push(lit);\n    println!(\"{:?}\", names);\n}",
      options: [
        { text: "印出 [\"local\", \"forever\"]——&'static str 可以當成任何較短的 &'x str 使用;names 的元素型別受 owned 限制,放一個活得更久的參考進去完全合法" },
        { text: "編譯錯誤:mismatched types——&'static str 和 names 的元素型別 &'x str 生命週期不同,就是兩個不同的型別" },
        { text: "印出 [\"local\", \"forever\"],而且 names 的元素型別因此被提升成 &'static str" },
        { text: "印出 [\"local\", \"forever\"],但 push 時會把 \"forever\" 複製成一份新的 String 放進去" },
      ],
      answer: 0,
      explanation: `生命週期之間有「子類型」關係:如果 'long 涵蓋 'short,那 &'long T 就可以用在任何需要 &'short T 的地方。直覺很單純——要求「至少活到這裡」的位置,給它一個「活得更久」的參考,當然安全。names 的元素型別由 owned.as_str() 定為 &'x str('x 是 owned 的範圍);lit 是 &'static str,'static 涵蓋一切,push 進去合法,印出 ["local", "forever"]。
「生命週期不同就是不同型別,所以 mismatched types」只對了一半:它們確實是不同的型別,但子類型關係讓較長的可以自動轉成較短的。反方向就不行,下一題會看到。「元素型別被提升成 'static」則是把方向弄反了:集合的要求由最短命的那個決定,放一個長命的進去不會讓要求變高;如果真的提升了,owned.as_str() 反而會放不進去。至於複製:&'static str 就是一根指向唯讀資料區的指標,push 只是複製這根指標。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let owned = String::from(\"local\");", note: "owned 活到 main 結束。" },
          { code: "    let mut names: Vec<&str> = vec![owned.as_str()];", note: "第一個元素借用 owned,元素型別定為 &'x str,'x 不能超出 owned 的範圍。" },
          { code: "    let lit: &'static str = \"forever\";", note: "字串字面值存在唯讀資料區,整個程式期間都有效。" },
          { code: "    names.push(lit);", note: "&'static str 被當成 &'x str 放進去——'static 涵蓋 'x,這是自動、零成本的子類型轉換。" },
          { code: "    println!(\"{:?}\", names);", note: "印出 [\"local\", \"forever\"]。" },
          { code: "}", note: "names 先釋放(只釋放 Vec 的緩衝區),owned 後釋放。" },
        ],
        outro: "記法:長可以當短用,短不能當長用。'static 是最長的,所以 &'static str 可以放進任何需要 &str 的地方——這也是為什麼字串字面值幾乎到處都能用。",
      },
      csharp: `可以類比 C# 的協變(covariance):IEnumerable<string> 可以當成 IEnumerable<object> 用,因為「更具體的」可以放進「要求較寬鬆的」位置。機制完全不同——C# 的協變發生在型別繼承上,Rust 的子類型化發生在生命週期上——但直覺一樣:能滿足較強要求的東西,一定也滿足較弱的要求。`,
    },
    {
      id: "2-4-17",
      question: "方向顛倒:把活得短的參考塞進要求 'static 的位置。以下程式碼的結果是?",
      questionCode: "fn main() {\n    let mut names: Vec<&'static str> = vec![\"root\"];\n    {\n        let user = String::from(\"guest\");\n        names.push(&user);\n    }\n    println!(\"{:?}\", names);\n}",
      options: [
        { text: "編譯錯誤:`user` does not live long enough(type annotation requires that `user` is borrowed for `'static`)——names 的型別要求每個元素都是 'static,短命的 &user 不能當長命的用" },
        { text: "印出 [\"root\", \"guest\"]——生命週期標註只是名字,&String 能轉成 &str,型別對得上就好" },
        { text: "印出 [\"root\", \"guest\"]——push 會把 user 的內容複製進 names,user 釋放後 names 裡還有一份" },
        { text: "編譯錯誤:mismatched types——vec![\"root\"] 已經把元素定為字串字面值,不能再放入 &String" },
      ],
      answer: 0,
      explanation: `這是第 16 題的反方向。names 的型別是 Vec<&'static str>,等於承諾「裡面每個參考都永遠有效」。&user 只活在內層區塊,把它當成 &'static str 放進去,就是用短的冒充長的——子類型關係不允許這個方向。錯誤訊息點得很準:type annotation requires that user is borrowed for 'static,錯誤的根源是你親手寫的那個 'static 標註。
「生命週期只是名字,型別對得上就好」是這題要破除的誤解:生命週期是型別的一部分,&'static str 與 &'x str 是不同的型別,只有長→短的方向能自動轉換。「push 會複製內容」不對,Vec<&str> 裡存的只是指標。「&String 不能放進去」也不對:&String 會自動轉成 &str(deref coercion),型別的「形狀」沒問題,問題只在生命週期。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let mut names: Vec<&'static str> = vec![\"root\"];", note: "明確要求元素都是 &'static str。字面值 \"root\" 符合。" },
            { code: "    {", note: "內層區塊開始。" },
            { code: "        let user = String::from(\"guest\");", note: "user 只活在內層區塊。" },
            { code: "        names.push(&user);", note: "⛔ 編譯失敗:`user` does not live long enough——type annotation requires that `user` is borrowed for `'static`。短命的借用不能當成 'static 用。" },
            { code: "    }", note: "user 在這裡釋放;如果能編譯,names 裡就會留下一根懸空指標。" },
            { code: "    println!(\"{:?}\", names);", note: "會讀到已釋放的記憶體——這正是編譯器要擋的事。" },
            { code: "}", note: "這個定義到此結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let user = String::from(\"guest\");", note: "改動處:user 移到 names 之前宣告,活得比 names 久。" },
            { code: "    let mut names: Vec<&str> = vec![\"root\"];", note: "改動處:拿掉 'static,元素型別由編譯器依實際放入的東西推斷。字面值依子類型規則可以放進來。" },
            { code: "    names.push(&user);", note: "&user 轉成 &str 放入,它只需要活得比 names 的使用期間久——成立。" },
            { code: "    println!(\"{:?}\", names);", note: "印出 [\"root\", \"guest\"]。" },
            { code: "}", note: "names 先釋放,user 後釋放。" },
          ],
          outro: "如果 names 真的需要活得比 user 久(例如要存起來長期使用),另一條路是 Vec<String>,push(user) 把所有權移進去——參見第 10 題「讓資料被擁有」的原則。",
        },
      ],
      csharp: `延續上一題的協變類比:C# 允許 IEnumerable<string> → IEnumerable<object>,卻不允許反方向,因為 object 不一定是 string。Rust 的生命週期也只允許一個方向:長 → 短可以,短 → 長不行。差別在 C# 會在編譯期擋型別方向,卻不會擋「這個物件是不是已經被釋放」——因為在 GC 下那件事不會發生;Rust 則正是靠這條方向規則在編譯期排除懸空指標。`,
    },
    {
      id: "2-4-18",
      question: "高階生命週期 bound(HRTB)。以下程式碼的結果是?",
      questionCode: "fn first_word(s: &str) -> &str {\n    s.split(' ').next().unwrap()\n}\n\nfn apply<F>(f: F) -> usize\nwhere\n    F: Fn(&str) -> &str,\n{\n    let owned = String::from(\"hello world\");\n    let piece = f(&owned);\n    piece.len()\n}\n\nfn main() {\n    println!(\"{}\", apply(first_word));\n}",
      options: [
        { text: "印出 5——Fn(&str) -> &str 是 for<'a> Fn(&'a str) -> &'a str 的簡寫,要求 f 對「任何」生命週期都成立,包括 apply 內部那個區域變數的生命週期" },
        { text: "編譯錯誤:bound 裡出現接收參考的函式型別時,必須手寫 for<'a>,編譯器不會自動補上" },
        { text: "編譯錯誤:`owned` does not live long enough——f 的生命週期是呼叫端決定的,不可能短到只涵蓋 apply 內部的區域變數" },
        { text: "印出 5,但只因為傳進去的是具名函式;如果傳閉包,就一定得改寫成 for<'a> 的形式才能編譯" },
      ],
      answer: 0,
      explanation: `apply 內部建立 owned,再把 &owned 交給 f——這個參考的生命週期只存在於 apply 內部,呼叫端根本無從指定。所以 bound 需要的不是「某一個 'a」,而是「對所有 'a 都成立」,寫成 for<'a> Fn(&'a str) -> &'a str,這就是高階生命週期 bound(higher-ranked trait bound)。好消息是 Fn(&str) -> &str 這種省略寫法,編譯器會自動展開成 for<'a> 的形式,所以程式照常編譯,first_word 回傳 hello,印出 5。
「owned does not live long enough」描述的是另一種寫法:把 'a 宣告在 apply<'a, F> 上、bound 寫 F: Fn(&'a str) -> &'a str。那時 'a 由呼叫端選定,一定比 apply 內部的區域變數長,就會真的報這個錯——這正是 for<'a> 存在的理由。「必須手寫」「傳閉包就得手寫」都不對:大多數情況省略寫法就已經是 HRTB,直接把閉包傳進 apply 也一樣能用;手寫 for<'a> 主要出現在 trait 自訂的 bound 或文件、錯誤訊息裡。`,
      walkthrough: [
        {
          label: "⊕ Question code — walkthrough (runs successfully)",
          lines: [
            { code: "fn first_word(s: &str) -> &str {", note: "省略規則:回傳值綁 s。對任何生命週期都成立。" },
            { code: "    s.split(' ').next().unwrap()", note: "切出第一個單字。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "fn apply<F>(f: F) -> usize", note: "注意 apply 本身沒有宣告任何生命週期參數。" },
            { code: "where", note: "約束寫在 where 子句。" },
            { code: "    F: Fn(&str) -> &str,", note: "省略寫法,編譯器展開成 for<'a> Fn(&'a str) -> &'a str:f 必須對任何 'a 都能用。" },
            { code: "{", note: "函式本體開始。" },
            { code: "    let owned = String::from(\"hello world\");", note: "apply 內部的區域資料,生命週期只在這個函式裡。" },
            { code: "    let piece = f(&owned);", note: "用一個「只有 apply 內部才知道」的生命週期呼叫 f;因為 bound 是 for<'a>,這個 'a 也包含在內。" },
            { code: "    piece.len()", note: "piece 是 hello,長度 5。回傳 usize,不帶出任何參考。" },
            { code: "}", note: "owned 釋放。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    println!(\"{}\", apply(first_word));", note: "印出 5。" },
            { code: "}", note: "這個定義到此結束。" },
          ],
        },
        {
          label: "X Variant: 'a declared on apply (won't compile)",
          lines: [
            { code: "fn first_word(s: &str) -> &str {", note: "同上。" },
            { code: "    s.split(' ').next().unwrap()", note: "同上。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "fn apply<'a, F>(f: F) -> usize", note: "改把 'a 宣告在函式上:'a 由呼叫端決定。" },
            { code: "where", note: "約束寫在 where 子句。" },
            { code: "    F: Fn(&'a str) -> &'a str,", note: "f 只保證對「呼叫端選的那一個 'a」成立。" },
            { code: "{", note: "函式本體開始。" },
            { code: "    let owned = String::from(\"hello world\");", note: "區域資料,一定比呼叫端選的 'a 短命。" },
            { code: "    let piece = f(&owned);", note: "⛔ 編譯失敗:`owned` does not live long enough——這裡需要 &'a str,但 &owned 撐不到呼叫端選定的 'a。" },
            { code: "    piece.len()", note: "取長度。" },
            { code: "}", note: "owned 在這裡釋放,仍被視為借用中。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    println!(\"{}\", apply(first_word));", note: "錯誤在 apply 的定義,不在這裡。" },
            { code: "}", note: "這個定義到此結束。" },
          ],
        },
        {
          label: "√ Correct version: for<'a> written out (complete, runnable)",
          lines: [
            { code: "fn first_word(s: &str) -> &str {", note: "同上。" },
            { code: "    s.split(' ').next().unwrap()", note: "同上。" },
            { code: "}", note: "這個定義到此結束。" },
            { code: "", note: "" },
            { code: "fn apply<F>(f: F) -> usize", note: "改動處:函式上不宣告 'a。" },
            { code: "where", note: "約束寫在 where 子句。" },
            { code: "    F: for<'a> Fn(&'a str) -> &'a str,", note: "改動處:把 HRTB 手寫出來,和題目的省略寫法完全等價。'a 在 bound 內部宣告,意思是「對每一個 'a」。" },
            { code: "{", note: "函式本體開始。" },
            { code: "    let owned = String::from(\"hello world\");", note: "區域資料。" },
            { code: "    f(&owned).len()", note: "任何 'a 都被涵蓋,包括 owned 的區域範圍。" },
            { code: "}", note: "owned 釋放。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    println!(\"{}\", apply(first_word));", note: "傳具名函式,印出 5。" },
            { code: "    println!(\"{}\", apply(|s| s.trim()));", note: "直接傳閉包也行,參數型別由 bound 推出,印出 11。" },
            { code: "}", note: "這個定義到此結束。" },
          ],
          outro: "判斷要不要 for<'a> 的問題是:「這個參考的生命週期是誰決定的?」如果是函式內部才產生的(像這裡的 &owned),呼叫端無法指定,就需要「對所有 'a」;省略寫法已經幫你做了這件事。",
        },
      ],
      csharp: `C# 沒有等價概念:Func<string, string> 接受任何字串、回傳任何字串,不需要描述「輸入與輸出之間的存活關係」,更不需要說「對所有可能的存活期都成立」。Rust 的 for<'a> 可以理解成「泛型化到生命週期上的委派簽名」——它通常藏在 Fn(&str) -> &str 這種省略寫法背後,你多半只會在錯誤訊息或文件裡看到它。`,
    },
    {
      id: "2-4-19",
      question: "綜合:回傳一個持有輸入參考的迭代器。以下程式碼的結果是?",
      questionCode: "fn long_words<'a>(text: &'a str, min: usize) -> impl Iterator<Item = &'a str> {\n    text.split_whitespace().filter(move |w| w.len() >= min)\n}\n\nfn main() {\n    let text = String::from(\"the quick brown fox jumps\");\n    let words: Vec<&str> = long_words(&text, 5).collect();\n    println!(\"{:?}\", words);\n}",
      options: [
        { text: "印出 [\"quick\", \"brown\", \"jumps\"]——Item = &'a str 讓回傳的迭代器與 text 綁在一起,move 讓閉包自己擁有 min 的複本,整條鏈只依賴 text" },
        { text: "編譯錯誤:回傳 impl Iterator 時只要內部持有參考,就一定要在後面再加上 + 'a" },
        { text: "編譯錯誤:迭代器不能持有輸入的參考,必須在函式內 collect 成 Vec<String> 再回傳" },
        { text: "編譯錯誤:filter 的閉包借用了參數 min,函式回傳後 min 就消失了,迭代器裡留下懸空參考" },
      ],
      answer: 0,
      explanation: `這題把本課的幾件事串在一起。split_whitespace 產出的每個片段都指向 text,所以回傳的迭代器實際上借用了 text;簽名用 Item = &'a str 把這件事寫明,呼叫端就知道 words 不能活過 text。閉包用到 min,而 min 是 long_words 的區域參數,函式一回傳就消失——move 讓閉包把 min(usize,Copy)複製一份存進自己裡面,迭代器就不再依賴 long_words 的任何區域資料。字母數大於等於 5 的單字是 quick、brown、jumps。
「閉包借用了 min」描述的是拿掉 move 的版本:那時會報 closure may outlive the current function, but it borrows min——題目寫了 move,所以不成立。「一定要加 + 'a」不對:回傳型別的 Item 已經提到 'a,編譯器因此知道隱藏的迭代器型別會用到 'a;+ 'a 只在「生命週期沒出現在 bound 裡、卻被隱藏型別用到」時才需要。「迭代器不能持有參考」則是把 C# 的直覺搬過來,Rust 標準庫的迭代器幾乎都是借用型的。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn long_words<'a>(text: &'a str, min: usize) -> impl Iterator<Item = &'a str> {", note: "回傳某個實作 Iterator 的具體型別(呼叫端看不到名字);Item = &'a str 說明產出的每一項都是指向 text 的片段。" },
          { code: "    text.split_whitespace().filter(move |w| w.len() >= min)", note: "split_whitespace 借用 text;move 讓閉包擁有 min 的複本。整條鏈只依賴 'a,不依賴 long_words 的任何區域變數。" },
          { code: "}", note: "long_words 回傳時,min 參數消失,但閉包裡已經有自己的一份。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let text = String::from(\"the quick brown fox jumps\");", note: "text 擁有資料,'a 就是它的有效範圍。" },
          { code: "    let words: Vec<&str> = long_words(&text, 5).collect();", note: "惰性的迭代器在 collect 時才真正執行,收集出指向 text 的片段。" },
          { code: "    println!(\"{:?}\", words);", note: "印出 [\"quick\", \"brown\", \"jumps\"]。" },
          { code: "}", note: "words 先釋放,text 後釋放。" },
        ],
        outro: "拿掉 move,會得到 error[E0373]: closure may outlive the current function, but it borrows `min`, which is owned by the current function。回傳迭代器時要檢查兩件事:它借用的外部資料有沒有寫進簽名('a),它用到的區域資料有沒有被 move 進去。",
      },
      csharp: `C# 的 IEnumerable<string> LongWords(string text, int min) { foreach (...) if (w.Length >= min) yield return w; } 寫起來更短,而且完全不需要說明結果跟 text 的關係——GC 讓 text 一直活到迭代器不再被引用。但 C# 也有它的執行期陷阱:如果迭代器底下是一個會被修改的集合,邊走訪邊修改會在執行期拋 InvalidOperationException。Rust 版本把「迭代器借用了 text」寫進型別,走訪期間任何修改 text 的企圖都會在編譯期被擋下。`,
    },
    {
      id: "2-4-20",
      question: "C# 對照總結:以下哪一個描述最準確地說明了 Rust 生命週期與 C# 記憶體模型的差異?",
      options: [
        { text: "C# 靠 GC 在執行期保證「還被引用的物件不會被回收」,所以物件圖從不需要證明誰依賴誰;Rust 沒有 GC,把「這個參考不能活過那份資料」做成型別的一部分——struct 帶著 'a、'a: 'b 描述層級、T: 'a 延伸到泛型、'static bound 表示不借用短命資料——可以組合、可以傳遞,全部在編譯期檢查完畢" },
        { text: "Rust 的生命週期其實就是參考計數:'a 記錄還有幾個參考指向資料,歸零時釋放,跟 C# GC 的差別只是更即時" },
        { text: "Rust 的 'static 對應 C# 的 static:標了 'static 的值會被放進靜態記憶體區,活到程式結束" },
        { text: "生命週期標註決定資料活多久:寫長一點的生命週期,資料就會被保留久一點,相當於手動控制 GC 的回收時機" },
      ],
      answer: 0,
      explanation: `整課的主線:生命週期是描述,不是控制。它不改變任何值的存活時間(第 3、10 題),只讓編譯器能證明「沒有參考活過它指向的資料」。這個描述可以組合:struct 把 'a 帶在型別上(第 1、11 題),方法選擇回傳綁誰(第 2、12 題),'a: 'b 與子類型化描述長短關係(第 6、16、17 題),T: 'a、T: 'static 把約束延伸到泛型(第 7~10 題),for<'a> 表達「對所有生命週期」(第 18 題)。
「生命週期就是參考計數」混淆了編譯期與執行期:Rust 的參考在執行期就是一根普通指標,沒有任何計數,需要計數時用的是 Rc/Arc 這類型別。「'static 對應 C# 的 static」是第 9 題已經破除的迷思:T: 'static 只要求型別不借用短命資料,擁有型別的值隨時可以被 drop。「寫長一點資料就活久一點」則是第 10 題的誤解——標註只能被檢查,不能延長任何東西。`,
      csharp: `給 C# 開發者的帶著走結論。一、C# 最接近生命週期的東西是 ref struct 與 Span<T>:它們不能被裝箱、不能存進一般類別的欄位,因為編譯器要保證它們不會活過指向的記憶體——Rust 把這套規則推廣到每一個參考。二、在 C# 裡「這個物件其實只是另一份緩衝區的檢視」只能寫在文件註解裡;在 Rust 裡它就是型別上的 'a。三、遇到生命週期錯誤時,先問「這個參考指向誰擁有的資料」,再決定是改標註描述正確的關係,還是改成擁有資料——而不是想辦法讓資料活久一點。`,
    },
  ],
};
