/* 出題慣例見專案根目錄 AUTHORING.md:
 *   - answer 一律為 0(正確答案寫在第一個選項),顯示順序由 quiz.js 依題目 id 洗牌
 *   - 詳解禁止用「選項 A/B/C」字母指涉,必須直接描述選項內容
 *   - 有程式碼的題目一律附 walkthrough(逐行說明);反面案例必須同時附上 √ 正確寫法 */
window.RUST_LESSONS = window.RUST_LESSONS || {};
window.RUST_LESSONS["lesson2-7"] = {
  id: "lesson2-7",
  title: "並行程式設計",
  goal: "會用 thread::spawn / join、mpsc::channel、Arc<Mutex<T>> 寫多執行緒程式,看懂 Send / Sync 如何在編譯期擋下資料競爭,也清楚它們擋不住的是什麼(死鎖)。",
  primer: {
    intro: "並行(concurrency)是讓多個執行緒同時工作。標準庫提供三件基本工具:thread::spawn 開新執行緒、mpsc::channel 在執行緒之間傳遞訊息、Mutex 保護需要被共同修改的資料。C# 也有對應的工具,但 Rust 多做了一件事:型別系統會在編譯期檢查每個值能不能被送到別的執行緒、能不能被多個執行緒同時存取,所以資料競爭(data race)在編譯期就被擋下,而不是等上線後偶發。",
    examples: [
      {
        code: "use std::thread;\n\nfn main() {\n    let handle = thread::spawn(|| {\n        println!(\"hello from worker\");\n    });\n    handle.join().unwrap();\n}",
        note: "spawn 把閉包交給一條新的執行緒去跑,並回傳一個 handle;join 等那條執行緒結束,這裡印出 hello from worker。",
      },
    ],
    csharp: "thread::spawn 大致對應 new Thread(...).Start() 或 Task.Run(...),join 對應 Thread.Join() 或 Task.Wait();差別在後面的題目裡會一一看到,多半出現在「編譯器願不願意放行」這一關。",
  },
  questions: [
    {
      id: "2-7-01",
      question: "thread::spawn 會不會等子執行緒跑完才回傳?關於下面程式的輸出,哪一個敘述是「有保證」的?",
      questionCode: "use std::thread;\n\nfn main() {\n    let handle = thread::spawn(|| {\n        for i in 1..=3 {\n            println!(\"worker {}\", i);\n        }\n    });\n    println!(\"main: spawned\");\n    handle.join().unwrap();\n    println!(\"main: joined\");\n}",
      options: [
        { text: "worker 1、worker 2、worker 3 一定依序出現,main: joined 一定是最後一行;但 main: spawned 可能出現在 worker 各行之前、之間或之後——spawn 建好執行緒就立刻回傳 JoinHandle,兩邊從此各跑各的" },
        { text: "一定是 worker 1、2、3 先印完,才輪到 main: spawned——spawn 跟呼叫一般函式一樣,要等閉包執行完才回傳" },
        { text: "main: spawned 一定是第一行——開新執行緒需要時間,主執行緒一定搶先印出" },
        { text: "連 worker 那三行彼此的順序都不固定——同一個迴圈的 println! 一旦在別的執行緒執行,也可能被打亂" },
      ],
      answer: 0,
      explanation: `thread::spawn 不是同步呼叫:它向作業系統要一條新執行緒、把閉包交給它,然後馬上回傳一個 JoinHandle,主執行緒繼續往下走。從這一刻起兩條執行緒是並行的,誰先印什麼由排程器決定。所以「main: spawned」相對於 worker 那三行的位置沒有保證。
真正有保證的只有兩件事:同一條執行緒內部的順序(worker 1 → 2 → 3 是同一個迴圈,永遠照順序),以及 join 建立的先後關係(join 回傳時子執行緒一定已經跑完,所以 main: joined 一定最後)。
「main: spawned 一定是第一行」是最陷人的干擾選項,因為實際執行時它幾乎每次都成立——開執行緒確實要花時間。但「幾乎每次」不是保證,機器忙碌時就可能翻盤,寫程式不能依賴它。「spawn 會等閉包跑完」則是把 spawn 當成一般函式呼叫,那樣就沒有並行可言了。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::thread;", note: "引入 thread 模組,1-15 只提過它的名字,本課第一次真正使用。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點,這段程式碼本身跑在「主執行緒」上。" },
          { code: "    let handle = thread::spawn(|| {", note: "開一條新執行緒去執行這個閉包,並立刻回傳 JoinHandle<()>。這一行不會等閉包跑完。" },
          { code: "        for i in 1..=3 {", note: "以下在子執行緒裡執行。" },
          { code: "            println!(\"worker {}\", i);", note: "同一條執行緒內的三次輸出,彼此順序固定是 1、2、3。" },
          { code: "        }", note: "迴圈結束。" },
          { code: "    });", note: "閉包結束,子執行緒跑完這裡就終止。" },
          { code: "    println!(\"main: spawned\");", note: "主執行緒在 spawn 回傳後馬上執行這行。它和子執行緒的三行誰先誰後由排程決定,沒有保證(實務上常常是它先)。" },
          { code: "    handle.join().unwrap();", note: "阻塞主執行緒,直到子執行緒結束。這是本程式唯一的同步點。" },
          { code: "    println!(\"main: joined\");", note: "join 已經回傳,子執行緒一定印完了,所以這行保證是最後一行。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "並行程式的輸出要分兩類看:「同一條執行緒內的順序」與「透過同步工具(join、channel、Mutex)建立的先後」是保證;其他所有跨執行緒的相對順序都只是「這次剛好如此」。",
      },
      csharp: `C# 的 new Thread(...).Start() 和 Task.Run(...) 也是立刻回傳,語意和這裡一樣:回傳值(Thread 物件或 Task)就是之後用來等待的把手,相當於 JoinHandle。C# 開發者對這題的直覺通常是對的;要小心的是反過來的誤會——看到 Rust 的 spawn 需要一個閉包,就以為它像 Parallel.Invoke 那樣會等全部做完才回傳。它不會,等待一律要自己呼叫 join。`,
    },
    {
      id: "2-7-02",
      question: "join() 能拿回什麼?沒被 join 的執行緒又會怎樣?以下程式的輸出是?",
      questionCode: "use std::thread;\nuse std::time::Duration;\n\nfn main() {\n    let handle = thread::spawn(|| (1..=10).sum::<i32>());\n    let total = handle.join().unwrap();\n    println!(\"total = {}\", total);\n    thread::spawn(|| {\n        thread::sleep(Duration::from_millis(100));\n        println!(\"orphan\");\n    });\n}",
      options: [
        { text: "只印出 total = 55,orphan 幾乎不會出現——main 回傳時整個行程就結束,不會等還沒被 join 的執行緒;而 join() 除了等待,還會把閉包的回傳值包在 Result 裡交回來" },
        { text: "印出 total = 55 和 orphan——Rust 的行程會等所有執行緒都跑完才結束" },
        { text: "編譯錯誤:join() 回傳 (),閉包的回傳值拿不回來,要改用共享變數把結果帶出來" },
        { text: "編譯錯誤:第二個 spawn 回傳的 JoinHandle 沒有被保存,每條執行緒都必須被 join,否則編譯器拒絕" },
      ],
      answer: 0,
      explanation: `JoinHandle<T> 的 T 就是閉包的回傳型別。這裡閉包回傳 i32,所以 handle 是 JoinHandle<i32>,join() 回傳 Result<i32, _>:子執行緒正常結束就是 Ok(55),子執行緒 panic 則是 Err——panic 不會直接炸掉主執行緒,而是在 join 這裡被交回來,由你決定怎麼處理(這裡用 unwrap 代表「子執行緒 panic 就跟著 panic」)。
第二條執行緒沒有被 join。Rust 的規則是:main 函式回傳,整個行程就結束,其他還在跑的執行緒直接被終止,不會等它們。它睡了 100 毫秒才印,main 早就結束了,所以 orphan 實務上不會出現。「行程會等所有執行緒」是 C# 前景執行緒的行為,Rust 沒有這個機制。
另外,不保存 JoinHandle 是合法的(連警告都沒有),handle 被丟掉等於「放生」這條執行緒(detach),它照樣會跑,只是再也沒有人能等它或拿它的結果。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::thread;", note: "引入 thread 模組。" },
          { code: "use std::time::Duration;", note: "sleep 需要一段 Duration。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "主執行緒從這裡開始。" },
          { code: "    let handle = thread::spawn(|| (1..=10).sum::<i32>());", note: "閉包回傳 i32,所以 handle 的型別是 JoinHandle<i32>——回傳值會被存起來,等人來拿。" },
          { code: "    let total = handle.join().unwrap();", note: "等子執行緒結束,拿到 Result<i32, _>;子執行緒正常結束所以是 Ok(55),unwrap 取出 55。join 會消耗 handle,同一條執行緒只能 join 一次。" },
          { code: "    println!(\"total = {}\", total);", note: "印出 total = 55,這一行是有保證的。" },
          { code: "    thread::spawn(|| {", note: "開第二條執行緒,但回傳的 JoinHandle 直接被丟掉:這條執行緒從此沒人等。" },
          { code: "        thread::sleep(Duration::from_millis(100));", note: "子執行緒先睡 100 毫秒。" },
          { code: "        println!(\"orphan\");", note: "理論上睡醒會印出來,但在那之前 main 早已回傳。" },
          { code: "    });", note: "閉包結束。" },
          { code: "}", note: "main 回傳 = 整個行程結束,還在睡的那條執行緒被直接終止,orphan 不會印出。" },
        ],
        outro: "就算把 sleep 拿掉,orphan 也只是「有時候來得及印」,不是保證。要確定背景工作做完,就保留 JoinHandle 並在 main 結束前 join 它。",
      },
      csharp: `C# 在這裡有兩種行為:new Thread(...) 預設是前景執行緒(IsBackground = false),行程會等所有前景執行緒結束才退出,所以 orphan 會印出來;Task.Run 用的是執行緒池的背景執行緒,Main 一回傳就被收掉,跟 Rust 一樣。Rust 的 spawn 一律是後者——沒有「前景執行緒」這個選項,要等就明確 join。拿結果的方式則對應 Task<int>.Result:C# 把例外包成 AggregateException 拋出,Rust 把 panic 包成 Err 回傳。`,
    },
    {
      id: "2-7-03",
      question: "子執行緒的閉包借用外部變數,而且後面緊接著就 join。以下程式的結果是?",
      questionCode: "use std::thread;\n\nfn main() {\n    let v = vec![1, 2, 3];\n    let handle = thread::spawn(|| {\n        println!(\"{:?}\", v);\n    });\n    handle.join().unwrap();\n}",
      options: [
        { text: "編譯錯誤:closure may outlive the current function, but it borrows v——spawn 要求閉包是 'static,編譯器不會去分析你後面有沒有 join" },
        { text: "印出 [1, 2, 3]——下一行就 join 了,子執行緒一定在 v 被釋放之前結束,所以借用是安全的" },
        { text: "印出 [1, 2, 3]——閉包本體只讀 v,傳給 spawn 時編譯器會自動改成 move 捕獲" },
        { text: "編譯錯誤:Vec<i32> 不能在執行緒之間共用,必須先包進 Mutex 才能給子執行緒讀" },
      ],
      answer: 0,
      explanation: `閉包本體只讀 v,依 2-1 學過的規則,編譯器選擇「不可變借用」捕獲。問題在 thread::spawn 的簽名要求閉包是 'static——閉包裡不能含有任何「可能比執行緒活得短」的借用。原因很直接:spawn 回傳後,子執行緒可以活得比 main 這個函式還久(上一題的 orphan 就是例子),到那時 v 已經被釋放,子執行緒手上的參考就懸空了。
「後面緊接著就 join,所以安全」是本課入門最大的坑。你知道會 join,但 spawn 的簽名不知道——handle 可以被傳走、被丟掉、或 join 之前主執行緒先 panic 了。編譯器只看簽名,不做「這條執行緒實際上何時結束」的推理。「自動改成 move」也不對:捕獲方式永遠依本體用法推斷,spawn 不會改變這個規則,要 move 就得自己寫。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "use std::thread;", note: "引入 thread 模組。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let v = vec![1, 2, 3];", note: "v 屬於 main 的區域變數,main 結束時被釋放。" },
            { code: "    let handle = thread::spawn(|| {", note: "⛔ 編譯失敗:closure may outlive the current function, but it borrows v, which is owned by the current function(E0373)。附註寫著 function requires argument type to outlive 'static——spawn 要求閉包不含任何短命的借用。" },
            { code: "        println!(\"{:?}\", v);", note: "本體只讀 v,所以閉包捕獲的是 &v,這就是那個活不夠久的借用。" },
            { code: "    });", note: "閉包結束。" },
            { code: "    handle.join().unwrap();", note: "這個 join 確實會讓子執行緒在 v 釋放前結束,但編譯器不採信——spawn 的簽名無法表達「保證會被 join」。" },
            { code: "}", note: "v 在這裡被釋放。" },
          ],
          outro: "編譯器的錯誤訊息自己就給了解法:help: to force the closure to take ownership of v, use the move keyword。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::thread;", note: "引入 thread 模組。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let v = vec![1, 2, 3];", note: "完全不用改。" },
            { code: "    let handle = thread::spawn(move || {", note: "改動處:加上 move,閉包直接取得 v 的所有權。閉包裡不再有任何借用,自然滿足 'static,v 的壽命從此由子執行緒負責。" },
            { code: "        println!(\"{:?}\", v);", note: "讀的是閉包自己擁有的 v,印出 [1, 2, 3]。" },
            { code: "    });", note: "子執行緒結束時 v 跟著被釋放。" },
            { code: "    handle.join().unwrap();", note: "等子執行緒結束。" },
            { code: "}", note: "main 結束,這時 v 早已不歸 main 管。" },
          ],
          outro: "另一個選項是 thread::scope(Rust 1.63 起):在 scope 區塊裡開的執行緒保證會在區塊結束前被 join,因此可以直接借用外部變數而不必 move。一般的 thread::spawn 沒有這個保證,所以幾乎每次都要寫 move。",
        },
      ],
      csharp: `同樣的程式在 C# 完全合法:var v = new List<int>{1,2,3}; var t = Task.Run(() => Console.WriteLine(v.Count)); t.Wait(); lambda 捕獲 v 之後,GC 會讓那個 List 一直活到沒有任何人引用它為止,不存在「懸空」的可能。Rust 沒有 GC 延命,只能二選一:把所有權交給子執行緒(move),或是用 thread::scope 證明借用一定會在變數釋放前結束。C# 省掉的這一步,代價是另一種 bug——子執行緒和主執行緒同時存取同一個物件,編譯器完全不過問。`,
    },
    {
      id: "2-7-04",
      question: "加了 move 之後,資料還回得來嗎?以下程式的結果是?",
      questionCode: "use std::thread;\n\nfn main() {\n    let mut v = vec![1, 2, 3];\n    let handle = thread::spawn(move || {\n        v.push(4);\n        v\n    });\n    let v = handle.join().unwrap();\n    println!(\"{:?}\", v);\n}",
      options: [
        { text: "印出 [1, 2, 3, 4]——move 把 Vec 的所有權交給子執行緒,子執行緒改完再把它當回傳值交出來,join() 把所有權帶回主執行緒;最後那個 v 是一個新的綁定" },
        { text: "編譯錯誤:v 已經被 move 進閉包,從此這個名字不能再出現,let v = ... 也不行" },
        { text: "編譯錯誤:被 move 進閉包的值是唯讀的,要在閉包裡先寫 let mut v = v; 才能 push" },
        { text: "編譯錯誤:v 原本屬於主執行緒,子執行緒只能使用它,不能把它當回傳值交出去" },
      ],
      answer: 0,
      explanation: `move 之後,Vec 的所有權屬於子執行緒——這不是「借給它」,而是「給它」。擁有者可以做任何事,包括修改(外層宣告了 let mut,被 move 進去的變數保有這個可變性)和把它當回傳值交出去。join() 回傳 Ok(那個 Vec),所有權又回到主執行緒手上。
「名字不能再出現」混淆了變數與綁定:被移走的是舊的 v,let v = ... 是用同一個名字建立新綁定(shadowing),完全合法。真正不合法的是在 spawn 與 join 之間碰舊的 v——那時它的所有權在子執行緒那邊,主執行緒寫 println!(\"{:?}\", v) 會得到 borrow of moved value。「被 move 的值是唯讀的」也不對,可變與否跟著原本的宣告走,不需要在閉包裡重新綁定。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::thread;", note: "引入 thread 模組。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let mut v = vec![1, 2, 3];", note: "宣告成 mut:閉包之後要 push,被 move 進去的變數必須原本就是可變的。" },
          { code: "    let handle = thread::spawn(move || {", note: "move 讓閉包取得 v 的所有權。從這一行起,主執行緒的舊 v 已經失效,在這裡讀它會是 borrow of moved value。" },
          { code: "        v.push(4);", note: "子執行緒是擁有者,可以自由修改,v 變成 [1, 2, 3, 4]。" },
          { code: "        v", note: "把 v 當回傳值交出去,所有權離開閉包。閉包回傳 Vec<i32>,所以 handle 是 JoinHandle<Vec<i32>>。" },
          { code: "    });", note: "閉包結束。" },
          { code: "    let v = handle.join().unwrap();", note: "join 等子執行緒結束,並把回傳的 Vec 交還給主執行緒。這是一個新的綁定,跟被移走的舊 v 同名但是不同的變數。" },
          { code: "    println!(\"{:?}\", v);", note: "印出 [1, 2, 3, 4]。" },
          { code: "}", note: "新的 v 在這裡被釋放。" },
        ],
        outro: "「移進去、改完、從 join 拿回來」是不需要任何鎖的最簡單資料交接:任何時刻只有一條執行緒擁有這份資料,所以不可能發生資料競爭。",
      },
      csharp: `C# 沒有「原變數失效」這回事:把 List 丟進 Task.Run 的 lambda 之後,主執行緒手上的參考照樣能讀能寫,而且兩邊可能同時在改——這正是資料競爭的溫床。Rust 的 move 把交接變成單向的:資料在子執行緒手上時,主執行緒在型別層面就碰不到它;要拿回來,唯一的管道是 join 的回傳值(對應 C# 的 Task<List<int>>.Result),而那一刻子執行緒保證已經放手。`,
    },
    {
      id: "2-7-05",
      question: "channel 的接收端能不能複製給兩個消費者?以下程式的結果是?",
      questionCode: "use std::sync::mpsc;\nuse std::thread;\n\nfn main() {\n    let (tx, rx) = mpsc::channel();\n    let rx2 = rx.clone();\n    let a = thread::spawn(move || rx.recv().unwrap());\n    let b = thread::spawn(move || rx2.recv().unwrap());\n    tx.send(1).unwrap();\n    tx.send(2).unwrap();\n    println!(\"{} {}\", a.join().unwrap(), b.join().unwrap());\n}",
      options: [
        { text: "編譯錯誤:no method named clone found for struct Receiver——mpsc 是 multiple producer, single consumer,Receiver 刻意不能複製,能 clone 的只有 Sender" },
        { text: "能編譯;兩個 Receiver 搶同一個佇列,各拿到一則訊息,印出 1 2 或 2 1" },
        { text: "能編譯;複製出來的 Receiver 各自收到每一則訊息(像廣播),兩個執行緒都拿到第一則,印出 1 1" },
        { text: "編譯錯誤:send 會消耗 Sender,同一個 tx 不能連續 send 兩次" },
      ],
      answer: 0,
      explanation: `mpsc 這個模組名稱本身就是規格:multiple producer, single consumer。Sender 實作了 Clone,可以複製給很多個生產者;Receiver 沒有實作 Clone,一個 channel 永遠只有一個接收端。所以 rx.clone() 直接編譯失敗,錯誤訊息是找不到 clone 這個方法。
「兩個 Receiver 搶同一個佇列」是其他語言(包括 C# 的 Channel<T>)的行為,是很自然的預期,但 std 的 mpsc 不提供;真的需要多個消費者時,得自己把 Receiver 包起來共用(本課後面會看到做法),或改用第三方的多消費者 channel。「像廣播一樣每個都收到」則是另一種完全不同的設計(broadcast channel),不是 mpsc 的語意。send 只需要 &self,同一個 Sender 送幾次都行。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "use std::sync::mpsc;", note: "mpsc = multiple producer, single consumer。" },
            { code: "use std::thread;", note: "引入 thread 模組。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let (tx, rx) = mpsc::channel();", note: "建立一條 channel,回傳 (Sender, Receiver) 一對。" },
            { code: "    let rx2 = rx.clone();", note: "⛔ 編譯失敗:no method named clone found for struct Receiver<T> in the current scope(E0599)。Receiver 沒有實作 Clone,接收端只能有一個。" },
            { code: "    let a = thread::spawn(move || rx.recv().unwrap());", note: "打算讓第一個消費者拿 rx。" },
            { code: "    let b = thread::spawn(move || rx2.recv().unwrap());", note: "打算讓第二個消費者拿複製品——但複製品根本建不出來。" },
            { code: "    tx.send(1).unwrap();", note: "send 只借用 &self,可以重複呼叫。" },
            { code: "    tx.send(2).unwrap();", note: "第二則訊息,同樣合法。" },
            { code: "    println!(\"{} {}\", a.join().unwrap(), b.join().unwrap());", note: "印出兩個消費者各自收到的值。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::sync::mpsc;", note: "引入 mpsc 模組。" },
            { code: "use std::thread;", note: "引入 thread 模組。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let (tx, rx) = mpsc::channel();", note: "一條 channel。" },
            { code: "    let tx2 = tx.clone();", note: "改動處:複製的是 Sender,不是 Receiver。這就是 mpsc 的 multiple producer。" },
            { code: "    let a = thread::spawn(move || tx.send(1).unwrap());", note: "第一個生產者拿走 tx。" },
            { code: "    let b = thread::spawn(move || tx2.send(2).unwrap());", note: "第二個生產者拿走 tx2。兩個 Sender 在各自的執行緒結束時被 drop。" },
            { code: "    a.join().unwrap();", note: "等第一個生產者送完。" },
            { code: "    b.join().unwrap();", note: "等第二個生產者送完。此時所有 Sender 都已經被 drop。" },
            { code: "    let mut got: Vec<i32> = rx.iter().collect();", note: "唯一的消費者把訊息全部收下;所有 Sender 都消失了,iter() 收完佇列裡的東西就結束。" },
            { code: "    got.sort();", note: "兩個生產者誰先送沒有保證,排序後輸出才固定。" },
            { code: "    println!(\"{:?}\", got);", note: "印出 [1, 2]。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "把方向反過來記:多個執行緒往同一個地方「送」,用 channel 天生就支援;多個執行緒從同一個地方「搶工作」,std 的 mpsc 不直接支援,需要額外包裝。",
        },
      ],
      csharp: `C# 的 System.Threading.Channels 預設就支援多個讀取端同時從 channel.Reader 搶訊息(SingleReader 選項預設是 false),每則訊息只會被其中一個讀取端拿到。從 C# 過來的人因此會直覺地想「把 Receiver 複製一份給另一個 worker」。std 的 mpsc 把「單一消費者」寫進型別:Receiver 沒有 Clone,想複製的那一刻就編譯失敗,而不是跑起來才發現語意不對。`,
    },
    {
      id: "2-7-06",
      question: "recv() 沒有訊息時會怎樣?所有 Sender 都消失之後呢?以下程式的輸出是?",
      questionCode: "use std::sync::mpsc;\nuse std::thread;\n\nfn main() {\n    let (tx, rx) = mpsc::channel();\n    thread::spawn(move || {\n        for i in 1..=3 {\n            tx.send(i * 10).unwrap();\n        }\n    });\n    println!(\"{:?}\", rx.recv());\n    println!(\"{:?}\", rx.recv());\n    println!(\"{:?}\", rx.recv());\n    println!(\"{:?}\", rx.recv());\n}",
      options: [
        { text: "Ok(10)、Ok(20)、Ok(30)、Err(RecvError)——recv() 沒訊息時會阻塞等待;子執行緒結束時 tx 被 drop,所有 Sender 都消失,第四次 recv() 就不再等,改回傳 Err" },
        { text: "Ok(10)、Ok(20)、Ok(30)、None——佇列空了 recv() 就回傳 None" },
        { text: "印出 Ok(10)、Ok(20)、Ok(30) 之後永遠卡住——第四次 recv() 會一直等下一則訊息" },
        { text: "順序不固定,例如先印 Ok(20)——子執行緒和主執行緒同時在跑,訊息抵達的順序沒有保證" },
      ],
      answer: 0,
      explanation: `recv() 回傳 Result<T, RecvError>,它的行為有三種:佇列裡有訊息就立刻拿走一則;沒有訊息但還有 Sender 活著,就阻塞等待(主執行緒很可能比子執行緒先走到第一個 recv,那它就在那裡等);沒有訊息而且所有 Sender 都被 drop 了,代表「不可能再有訊息」,回傳 Err(RecvError)。
這裡唯一的 tx 被 move 進子執行緒,子執行緒送完三則就結束,tx 隨之被 drop,所以第四次 recv() 拿到 Err 而不是卡死。「永遠卡住」的干擾選項漏看了 drop 的效果;反過來說,如果主執行緒手上還留著一個 Sender,那才真的會卡住。「回傳 None」則是把它當成 Option,它是 Result——Err 帶的意思是「channel 已經斷線」。同一個 Sender 送出的訊息保證依序抵達(FIFO),順序不會亂。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::sync::mpsc;", note: "引入 mpsc 模組。" },
          { code: "use std::thread;", note: "引入 thread 模組。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let (tx, rx) = mpsc::channel();", note: "一條 channel,目前只有一個 Sender(tx)。" },
          { code: "    thread::spawn(move || {", note: "tx 被 move 進子執行緒,主執行緒手上不再有任何 Sender。" },
          { code: "        for i in 1..=3 {", note: "送三則訊息。" },
          { code: "            tx.send(i * 10).unwrap();", note: "依序送出 10、20、30,同一個 Sender 的訊息保證先進先出。" },
          { code: "        }", note: "迴圈結束。" },
          { code: "    });", note: "閉包結束時 tx 被 drop——channel 從此沒有任何 Sender。" },
          { code: "    println!(\"{:?}\", rx.recv());", note: "若訊息還沒到就阻塞等待;拿到 Ok(10)。" },
          { code: "    println!(\"{:?}\", rx.recv());", note: "Ok(20)。" },
          { code: "    println!(\"{:?}\", rx.recv());", note: "Ok(30)。" },
          { code: "    println!(\"{:?}\", rx.recv());", note: "佇列空了。如果子執行緒還沒結束就先等;等到 tx 被 drop,確定不會再有訊息,回傳 Err(RecvError)。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "日常寫法通常是 for msg in rx { ... } 或 rx.iter():它內部就是一直 recv(),遇到 Err 就結束迴圈。這也是為什麼「所有 Sender 都要被 drop」是收尾的關鍵——下一題會看到它的另一面。不想阻塞的話用 try_recv(),它在佇列空的時候立刻回傳 Err(TryRecvError::Empty)。",
      },
      csharp: `最接近的是 BlockingCollection<T>:Take() 在沒有項目時阻塞,生產者呼叫 CompleteAdding() 之後,消費者用 GetConsumingEnumerable() 走訪就會自然結束。Rust 把 CompleteAdding() 這一步換成「所有 Sender 都被 drop」——不用記得手動呼叫,所有權一結束就自動發生;反過來,忘了 drop 某個 Sender,就和 C# 忘了 CompleteAdding() 一樣,消費端會永遠等下去。`,
    },
    {
      id: "2-7-07",
      question: "把 Sender move 進子執行緒之後,主執行緒還想自己送一則。以下程式的結果是?",
      questionCode: "use std::sync::mpsc;\nuse std::thread;\n\nfn main() {\n    let (tx, rx) = mpsc::channel();\n    thread::spawn(move || {\n        tx.send(1).unwrap();\n    });\n    tx.send(2).unwrap();\n    let mut got: Vec<i32> = rx.iter().collect();\n    got.sort();\n    println!(\"{:?}\", got);\n}",
      options: [
        { text: "編譯錯誤:borrow of moved value: tx——move 閉包把 tx 本身搬進了子執行緒,主執行緒要送訊息,得先 tx.clone() 出另一個 Sender" },
        { text: "印出 [1, 2]——Sender 內部是共用的,move 進閉包的只是一個指向同一條 channel 的參考" },
        { text: "印出 [1, 2]——thread::spawn 會自動替閉包複製一份它用到的 Sender,原本的 tx 還留在主執行緒" },
        { text: "編譯錯誤:一條 channel 只能有一個 Sender,子執行緒與主執行緒不能同時負責送訊息" },
      ],
      answer: 0,
      explanation: `Sender 是一個有所有權的值,move 閉包捕獲它就是把它整個搬走,和搬一個 Vec 沒有兩樣。主執行緒之後再用 tx,就是使用一個已經被移走的值。
「Sender 內部是共用的,move 的只是參考」有一半是對的:Sender 內部確實指向一條共享的 channel,但這個共享要透過 clone() 明確地建立——每次 clone 都產生一個新的、獨立擁有的 Sender。沒有 clone,就只有一個 Sender,而它已經給出去了。spawn 也不會替你自動複製任何東西,捕獲規則完全由 move 決定。「只能有一個 Sender」則剛好相反,mpsc 的 m 就是 multiple producer。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "use std::sync::mpsc;", note: "引入 mpsc 模組。" },
            { code: "use std::thread;", note: "引入 thread 模組。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let (tx, rx) = mpsc::channel();", note: "一條 channel,一個 Sender。" },
            { code: "    thread::spawn(move || {", note: "move 閉包捕獲 tx:唯一的 Sender 被整個搬進子執行緒。" },
            { code: "        tx.send(1).unwrap();", note: "子執行緒用它自己擁有的 tx 送出 1。" },
            { code: "    });", note: "閉包結束。" },
            { code: "    tx.send(2).unwrap();", note: "⛔ 編譯失敗:borrow of moved value: tx(E0382)。tx 的所有權已經在上面的 move 閉包裡,主執行緒手上什麼都沒有。" },
            { code: "    let mut got: Vec<i32> = rx.iter().collect();", note: "收集所有訊息。" },
            { code: "    got.sort();", note: "排序讓輸出固定。" },
            { code: "    println!(\"{:?}\", got);", note: "印出結果。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::sync::mpsc;", note: "引入 mpsc 模組。" },
            { code: "use std::thread;", note: "引入 thread 模組。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let (tx, rx) = mpsc::channel();", note: "一條 channel。" },
            { code: "    let tx_worker = tx.clone();", note: "改動處:先複製出第二個 Sender。兩個 Sender 指向同一條 channel,但各自獨立擁有。" },
            { code: "    thread::spawn(move || {", note: "閉包現在捕獲的是 tx_worker,原本的 tx 留在主執行緒。" },
            { code: "        tx_worker.send(1).unwrap();", note: "子執行緒送出 1,結束時 tx_worker 被 drop。" },
            { code: "    });", note: "閉包結束。" },
            { code: "    tx.send(2).unwrap();", note: "主執行緒用自己的 tx 送出 2,合法。" },
            { code: "    drop(tx);", note: "關鍵的一行:主執行緒手上的 Sender 也要放掉。少了這行,下一行的 rx.iter() 會永遠等下去——因為還有一個 Sender(就是 tx)活著,channel 認為之後可能還有訊息。" },
            { code: "    let mut got: Vec<i32> = rx.iter().collect();", note: "所有 Sender 都消失後,iter() 收完佇列就結束。" },
            { code: "    got.sort();", note: "1 和 2 哪個先到沒有保證,排序後固定。" },
            { code: "    println!(\"{:?}\", got);", note: "印出 [1, 2]。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "多生產者的標準寫法:每個生產者各拿一個 clone,主執行緒自己不送的話就在迴圈後 drop 掉原本的 tx。「clone 給別人、自己手上那份記得放掉」這兩件事要一起做。",
        },
      ],
      csharp: `C# 的 ChannelWriter<T> 是參考型別,傳進多少個 Task 都是同一個物件,不需要也沒有「複製」的概念;關閉 channel 則是某個人明確呼叫 writer.Complete()。Rust 反過來:每個生產者擁有自己的 Sender,channel 在「最後一個 Sender 消失」時自動關閉。好處是不會有人在別人還在寫的時候提早 Complete();代價就是這題——你必須清楚每個 Sender 在誰手上。`,
    },
    {
      id: "2-7-08",
      question: "Mutex 什麼時候解鎖?以下程式的結果是?",
      questionCode: "use std::sync::Mutex;\n\nfn main() {\n    let m = Mutex::new(vec![1]);\n    {\n        let mut guard = m.lock().unwrap();\n        guard.push(2);\n    }\n    m.lock().unwrap().push(3);\n    println!(\"{:?}\", *m.lock().unwrap());\n}",
      options: [
        { text: "印出 [1, 2, 3]——lock() 回傳的 MutexGuard 一離開作用域就自動解鎖;第二次 lock() 的 guard 是暫時值,那一行結束就釋放" },
        { text: "永遠卡住——第一次拿到鎖之後沒有呼叫 unlock(),第二次 lock() 等不到鎖" },
        { text: "印出 [1]——guard 是資料的一份複本,對它 push 不會改到 Mutex 裡面的 Vec" },
        { text: "編譯錯誤:Mutex 是給多執行緒用的,在單一執行緒裡呼叫 lock() 不合法" },
      ],
      answer: 0,
      explanation: `std::sync::Mutex 根本沒有 unlock() 方法。lock() 回傳一個 MutexGuard,它有兩個身分:透過它可以存取裡面的資料(可以直接對它呼叫 push),而它被 drop 的那一刻就解鎖。第一段用大括號包起來,guard 在右大括號被 drop;第二次的 m.lock().unwrap() 沒有綁定到變數,是暫時值,在那一行的分號處就被 drop。所以三次 lock 彼此不衝突。
「沒呼叫 unlock 會卡住」是從手動加解鎖的 API 帶來的直覺——Rust 把解鎖綁在 Drop 上,就是為了讓「忘記解鎖」這種 bug 不可能發生。guard 也不是複本,它是指向 Mutex 內部資料的通道,改它就是改裡面那份。Mutex 在單一執行緒裡照樣能用(這題就是),只是沒有意義,它真正的用途要等下一題和 Arc 搭配。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::sync::Mutex;", note: "引入 Mutex。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let m = Mutex::new(vec![1]);", note: "Mutex 把資料「包在裡面」:想碰這個 Vec,唯一的路是先 lock()。" },
          { code: "    {", note: "開一個區塊,用來界定 guard 的壽命。" },
          { code: "        let mut guard = m.lock().unwrap();", note: "取得鎖,拿到 MutexGuard<Vec<i32>>。unwrap 處理的是「鎖被毒化」的情況:如果之前有執行緒握著鎖時 panic,lock() 會回傳 Err。" },
          { code: "        guard.push(2);", note: "透過 guard 直接修改 Mutex 裡的 Vec(guard 會自動解參考到裡面的資料),Vec 變成 [1, 2]。" },
          { code: "    }", note: "guard 在這裡被 drop,鎖同時被釋放。沒有 unlock() 這個呼叫,也不需要。" },
          { code: "    m.lock().unwrap().push(3);", note: "這次的 guard 是暫時值,push 完、這個敘述結束時就被 drop 並解鎖。Vec 變成 [1, 2, 3]。" },
          { code: "    println!(\"{:?}\", *m.lock().unwrap());", note: "第三次上鎖,解參考 guard 取得 Vec 印出 [1, 2, 3];敘述結束時解鎖。" },
          { code: "}", note: "m 被釋放。" },
        ],
        outro: "記住 guard 的壽命就是鎖的壽命。想提早解鎖就讓 guard 提早結束:用大括號縮小作用域,或明確呼叫 drop(guard)。",
      },
      csharp: `C# 的 lock (obj) { ... } 也會在區塊結束時自動釋放(編譯成 Monitor.Enter/Exit 加 try/finally),這點很像。關鍵差別在「鎖保護的是什麼」:C# 的 lock 保護的是一段程式碼,被保護的資料是另一個欄位,你在 lock 外面照樣能讀寫它,編譯器不會說話。Rust 的 Mutex<T> 擁有資料本身,不透過 guard 就碰不到 T——「忘了加鎖就存取」在型別層面不可能寫出來。`,
    },
    {
      id: "2-7-09",
      question: "Rc 換成 Arc 就可以跨執行緒了,那可以直接修改嗎?以下程式的結果是?",
      questionCode: "use std::sync::Arc;\nuse std::thread;\n\nfn main() {\n    let scores = Arc::new(vec![1, 2, 3]);\n    let s = Arc::clone(&scores);\n    let handle = thread::spawn(move || {\n        s.push(4);\n    });\n    handle.join().unwrap();\n    println!(\"{:?}\", scores);\n}",
      options: [
        { text: "編譯錯誤:cannot borrow data in an Arc as mutable——Arc 只提供共享的唯讀存取,要修改,得在裡面再包一層 Mutex" },
        { text: "印出 [1, 2, 3, 4]——Arc 是執行緒安全版的 Rc,換成它之後,多個執行緒就可以放心地修改同一份資料" },
        { text: "印出 [1, 2, 3]——Arc::clone 複製了一份 Vec,子執行緒改的是自己那份,主執行緒看不到" },
        { text: "編譯錯誤:s 用 let 宣告,不能被修改;把它改成 let mut s 就能 push" },
      ],
      answer: 0,
      explanation: `Arc(atomically reference counted)和 2-3 的 Rc 用法幾乎一樣,差別只在計數用原子指令增減,所以多條執行緒同時 clone、drop 也不會把計數算錯。它提供的「執行緒安全」僅止於計數本身:Arc 讓很多人共同擁有同一份資料,而共同擁有的東西只能透過共享參考(&T)讀,不能拿到 &mut T——否則兩條執行緒同時拿到 &mut 就是資料競爭。
「換成 Arc 就能改」是這題的核心誤解,它把「可以安全地共享」和「可以安全地修改」混為一談。「改成 let mut s」是很真實的嘗試,但 mut 只讓 s 這個 Arc 本身可以被重新賦值,不會讓它指向的共享資料變得可以獨佔修改,錯誤一模一樣。Arc::clone 也不複製 Vec,只把計數加一(和 Rc::clone 相同)。要修改,就要找一個能在執行期保證「同時只有一個人在改」的東西放在 Arc 裡面——那就是 Mutex。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "use std::sync::Arc;", note: "引入 Arc。" },
            { code: "use std::thread;", note: "引入 thread 模組。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let scores = Arc::new(vec![1, 2, 3]);", note: "Vec 放到 heap 上,由 Arc 管理共同擁有權,計數為 1。" },
            { code: "    let s = Arc::clone(&scores);", note: "計數加到 2,s 和 scores 指向同一個 Vec,沒有複製資料。" },
            { code: "    let handle = thread::spawn(move || {", note: "s 被 move 進子執行緒。Arc 可以跨執行緒,這一步本身沒問題。" },
            { code: "        s.push(4);", note: "⛔ 編譯失敗:cannot borrow data in an Arc as mutable(E0596)。push 需要 &mut Vec,但 Arc 只給得出共享參考——別的執行緒可能同時在讀同一個 Vec。" },
            { code: "    });", note: "閉包結束。" },
            { code: "    handle.join().unwrap();", note: "等子執行緒。" },
            { code: "    println!(\"{:?}\", scores);", note: "想印出修改後的結果。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::sync::{Arc, Mutex};", note: "改動處:多引入 Mutex。" },
            { code: "use std::thread;", note: "引入 thread 模組。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let scores = Arc::new(Mutex::new(vec![1, 2, 3]));", note: "改動處:Arc 負責「共同擁有」,Mutex 負責「同時只有一個人能改」,兩層各司其職。" },
            { code: "    let s = Arc::clone(&scores);", note: "複製的是 Arc(計數加一),Mutex 與 Vec 仍然只有一份。" },
            { code: "    let handle = thread::spawn(move || {", note: "s 被 move 進子執行緒。" },
            { code: "        s.lock().unwrap().push(4);", note: "改動處:先 lock() 取得獨佔的 guard,再透過 guard 修改。lock 只需要 &Mutex,共享參考就夠了,這正是 Arc 給得出來的東西。" },
            { code: "    });", note: "暫時的 guard 在上一行結束時已經解鎖。" },
            { code: "    handle.join().unwrap();", note: "等子執行緒改完。" },
            { code: "    println!(\"{:?}\", *scores.lock().unwrap());", note: "主執行緒也要透過 lock 才能讀,印出 [1, 2, 3, 4]。" },
            { code: "}", note: "最後一個 Arc 被 drop,Mutex 與 Vec 一起釋放。" },
          ],
          outro: "Mutex 的 lock(&self) 只要共享參考就能換到獨佔存取——它在執行期用鎖保證獨佔,所以型別上可以從 & 拿到可修改的 guard。這和 2-3 的 RefCell 是同一個「內部可變性」的思路,只是檢查方式從「借用計數」換成了「作業系統的鎖」。",
        },
      ],
      csharp: `C# 裡「共享」和「可修改」從來不分開:只要拿得到參考,任何執行緒都能呼叫 list.Add(4),執行緒安全是另外一件你要自己記得處理的事。Rust 把兩件事拆成兩個型別:Arc 只解決「多人共同擁有」,Mutex 只解決「同一時間只有一人修改」。少了任何一層,編譯器都會用這題的錯誤訊息提醒你,不會讓 List<T> 被兩條執行緒同時 Add 這種 C# 常見的損毀情境編譯成功。`,
    },
    {
      id: "2-7-10",
      question: "把 Rc 真的交給 thread::spawn。資料只讀不寫,以下程式的結果是?",
      questionCode: "use std::rc::Rc;\nuse std::thread;\n\nfn main() {\n    let config = Rc::new(String::from(\"prod\"));\n    let c = Rc::clone(&config);\n    let handle = thread::spawn(move || {\n        println!(\"{}\", c);\n    });\n    handle.join().unwrap();\n    println!(\"{}\", config);\n}",
      options: [
        { text: "編譯錯誤:Rc<String> cannot be sent between threads safely——閉包捕獲了 Rc,整個閉包因此不能送到別的執行緒,過不了 thread::spawn 的要求;改用 Arc 就能編譯" },
        { text: "印出兩行 prod——String 只被讀取,沒有任何人修改,跨執行緒共用是安全的" },
        { text: "印出兩行 prod——c 已經 move 進子執行緒,主執行緒手上的 config 是另一個獨立的 Rc,兩邊互不相干" },
        { text: "能編譯,但執行時 panic:Rc 在執行期偵測到自己被第二條執行緒存取,主動中止程式" },
      ],
      answer: 0,
      explanation: `2-3 用一個只接受 Send 型別的泛型函式證明過 Rc 不是 Send;這題是它在真實情境的樣子。錯誤訊息的形狀值得認得:它不是在 Rc::new 那行報錯,而是在 thread::spawn 報「within this closure, the trait Send is not implemented for Rc<String>」——編譯器替 move 閉包生成的匿名 struct 裡有一個 Rc 欄位,Send 是自動推導的,一個欄位不是 Send,整個閉包就不是 Send,而 spawn 要求閉包必須是 Send。
「只讀不寫就安全」是這題要打破的直覺:資料本身確實沒人改,但 c 和 config 共用同一個參考計數。c 在子執行緒結束時被 drop(計數減一),同一時間主執行緒可能正在 clone 或 drop config(計數加減),Rc 的計數不是原子操作,兩邊一起動就可能算錯。所以「兩個獨立的 Rc」也不成立——它們是同一個計數器的兩個把手。這一切是編譯期的 trait 檢查,不是執行期偵測。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "use std::rc::Rc;", note: "引入 Rc。" },
            { code: "use std::thread;", note: "引入 thread 模組。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let config = Rc::new(String::from(\"prod\"));", note: "Rc 管理的 String,強計數為 1。" },
            { code: "    let c = Rc::clone(&config);", note: "計數變成 2。c 和 config 共用同一個非原子的計數器。" },
            { code: "    let handle = thread::spawn(move || {", note: "⛔ 編譯失敗:Rc<String> cannot be sent between threads safely(E0277)。help:within the closure, the trait Send is not implemented for Rc<String>;note:required by a bound in spawn。閉包捕獲了 c,所以閉包也不是 Send。" },
            { code: "        println!(\"{}\", c);", note: "本體只讀,但問題不在讀,而在 c 被 drop 時會去改那個共用的計數。" },
            { code: "    });", note: "閉包結束,c 會在子執行緒被 drop。" },
            { code: "    handle.join().unwrap();", note: "等子執行緒。" },
            { code: "    println!(\"{}\", config);", note: "主執行緒繼續使用 config。" },
            { code: "}", note: "config 被 drop,計數歸零後釋放 String。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::sync::Arc;", note: "改動處:換成 Arc。" },
            { code: "use std::thread;", note: "引入 thread 模組。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let config = Arc::new(String::from(\"prod\"));", note: "改動處:Arc 的計數用原子指令增減,多條執行緒同時 clone、drop 也不會算錯。" },
            { code: "    let c = Arc::clone(&config);", note: "計數加一,仍然不複製 String。" },
            { code: "    let handle = thread::spawn(move || {", note: "c 是 Arc<String>,它是 Send,所以閉包也是 Send,通過 spawn 的檢查。" },
            { code: "        println!(\"{}\", c);", note: "印出 prod。" },
            { code: "    });", note: "子執行緒結束時 c 被 drop,計數用原子操作減一。" },
            { code: "    handle.join().unwrap();", note: "等子執行緒。" },
            { code: "    println!(\"{}\", config);", note: "再印一次 prod。" },
            { code: "}", note: "最後一個 Arc 被 drop,String 被釋放。" },
          ],
        },
        {
          label: "△ Desugared (what the compiler sees)",
          intro: "錯誤訊息提到 Send,但你的程式碼裡沒有這個字。它藏在 thread::spawn 的簽名與閉包的自動推導裡:",
          lines: [
            { code: "thread::spawn(move || { println!(\"{}\", c); });", note: "你寫的這一行。" },
            { code: "", note: "" },
            { code: "// 標準庫中 spawn 的簽名:", note: "以下才是編譯器實際檢查的東西。" },
            { code: "pub fn spawn<F, T>(f: F) -> JoinHandle<T>", note: "F 是閉包的型別,T 是閉包的回傳型別。" },
            { code: "where", note: "要求寫在 where 子句裡。" },
            { code: "    F: FnOnce() -> T + Send + 'static,", note: "閉包只會被呼叫一次、必須能送到別的執行緒(Send)、不能含短命的借用('static,第 3 題)。錯誤訊息裡的 Send 就是從這裡來的。" },
            { code: "    T: Send + 'static,", note: "回傳值也要能送回來(第 4 題的 Vec 就是這樣經過 join 回到主執行緒)。" },
            { code: "", note: "" },
            { code: "// 編譯器替 move 閉包生成的匿名 struct:", note: "閉包本質上是一個 struct,捕獲的變數是它的欄位。" },
            { code: "struct Closure { c: Rc<String> }", note: "move 捕獲了 c,所以它有一個 Rc<String> 欄位。" },
            { code: "", note: "" },
            { code: "// Send 由編譯器自動推導:所有欄位都是 Send,struct 才是 Send", note: "你不用寫 impl Send,編譯器逐一檢查欄位。" },
            { code: "// Rc<String>: 不是 Send  =>  Closure: 不是 Send  =>  不符合 F: Send", note: "推導鏈斷在 Rc,錯誤訊息的 within this closure 指的就是這條鏈。" },
          ],
        },
      ],
      csharp: `在 C# 裡,任何物件都能被丟進 Task.Run 的 lambda,編譯器不會問這個物件內部有沒有非執行緒安全的狀態——就像 List<T>、Dictionary<K,V> 都不是執行緒安全的,卻可以被多條執行緒同時碰,出事時才是難以重現的損毀。Rust 把「這個型別能不能跨執行緒」做成編譯期推導的 Send,而且會自動穿透 struct 與閉包:只要裡面藏了一個 Rc,整個東西就過不了 spawn。`,
    },
    {
      id: "2-7-11",
      question: "自訂 struct 要送到另一條執行緒,需要額外宣告什麼嗎?以下程式的結果是?",
      questionCode: "use std::thread;\n\nstruct Job {\n    id: u32,\n    name: String,\n    tags: Vec<String>,\n}\n\nfn main() {\n    let job = Job { id: 7, name: String::from(\"backup\"), tags: vec![String::from(\"nightly\")] };\n    let handle = thread::spawn(move || format!(\"{}:{}:{}\", job.id, job.name, job.tags.len()));\n    println!(\"{}\", handle.join().unwrap());\n}",
      options: [
        { text: "印出 7:backup:1——Send 是 auto trait:struct 的每個欄位都能送到別的執行緒(u32、String、Vec<String> 都可以),編譯器就自動認定整個 struct 也可以,不用寫任何東西" },
        { text: "編譯錯誤:自訂 struct 要跨執行緒,必須先在 struct 上寫 #[derive(Send)]" },
        { text: "編譯錯誤:必須手寫 unsafe impl Send for Job {},編譯器不會替自訂型別推導 Send" },
        { text: "編譯錯誤:含有 heap 資料(String、Vec)的值不能 move 到別的執行緒,要先包進 Arc" },
      ],
      answer: 0,
      explanation: `Send 的意思是「這個型別的值,所有權可以安全地轉移給另一條執行緒」。它是 auto trait:編譯器對每個型別自動推導,規則是「所有欄位都是 Send,這個型別就是 Send」。u32、String、Vec<String> 都是 Send,所以 Job 自動是 Send,move 閉包也就是 Send,spawn 直接接受。
「要寫 #[derive(Send)]」混淆了 auto trait 和 derive:derive 是給 Debug、Clone 這類需要生成程式碼的 trait 用的,Send 沒有任何方法,只是一個標記,而且根本不能 derive。「要手寫 unsafe impl Send」則是把例外當成常態——只有編譯器推導不出來、而你能自己擔保安全的型別(典型是包著裸指標的底層結構)才需要,而且要負起 unsafe 的責任。heap 資料也不是問題:move 之後整個 String 的所有權都在新執行緒,舊執行緒碰不到,不需要共享。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::thread;", note: "引入 thread 模組。" },
          { code: "", note: "" },
          { code: "struct Job {", note: "一個普通的 struct,沒有任何 derive 或 impl。" },
          { code: "    id: u32,", note: "u32 是 Send。" },
          { code: "    name: String,", note: "String 是 Send:它獨佔自己的 heap 緩衝區,整個交給別的執行緒後,原執行緒就碰不到了。" },
          { code: "    tags: Vec<String>,", note: "Vec<String> 是 Send,因為 String 是 Send。" },
          { code: "}", note: "三個欄位都是 Send,編譯器自動推導出 Job: Send。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let job = Job { id: 7, name: String::from(\"backup\"), tags: vec![String::from(\"nightly\")] };", note: "建立一個 Job,擁有權在主執行緒。" },
          { code: "    let handle = thread::spawn(move || format!(\"{}:{}:{}\", job.id, job.name, job.tags.len()));", note: "move 閉包取得整個 job,閉包的欄位是 Job,Job 是 Send,所以閉包是 Send;回傳的 String 也是 Send,spawn 的兩個條件都滿足。" },
          { code: "    println!(\"{}\", handle.join().unwrap());", note: "join 取回子執行緒組好的字串,印出 7:backup:1。" },
          { code: "}", note: "main 結束。job 早已在子執行緒結束時被釋放。" },
        ],
        outro: "反方向也是自動的:只要替 Job 加一個 Rc<String> 欄位,Job 就自動失去 Send,這段程式立刻得到上一題的錯誤。你不用記得去「標註」任何東西,型別的組成就決定了一切。",
      },
      csharp: `C# 沒有等價的靜態標記:一個類別能不能被多條執行緒使用,只寫在文件裡(「此型別的執行個體成員不保證執行緒安全」),編譯器不知道也不檢查。Rust 的 Send 則是編譯器自己推導、自己強制的事實:你寫的每一個 struct 都自動被分類成「可以送」或「不能送」,不需要任何人記得標註,也不可能標錯——除非你用 unsafe impl 明確地說「相信我」。`,
    },
    {
      id: "2-7-12",
      question: "兩條執行緒同時讀同一個 String。以下程式能編譯並執行嗎?原因是什麼?",
      questionCode: "use std::sync::Arc;\nuse std::thread;\n\nfn main() {\n    let motto = Arc::new(String::from(\"fearless\"));\n    let m2 = Arc::clone(&motto);\n    let handle = thread::spawn(move || m2.len());\n    println!(\"{} {}\", motto, handle.join().unwrap());\n}",
      options: [
        { text: "印出 fearless 8。能編譯是因為 String 同時是 Send 與 Sync:Arc 讓兩條執行緒同時透過參考讀同一個 String,這要求 String: Sync;最後一個放手的執行緒負責釋放它,這要求 String: Send" },
        { text: "編譯錯誤:兩條執行緒同時讀同一個 String,就算只是讀,也必須先包進 Mutex,因為 String 不是 Sync" },
        { text: "印出 fearless 8。能編譯是因為 Arc 對任何 T 都能送到別的執行緒——原子計數讓裡面的資料自動變得執行緒安全" },
        { text: "印出 fearless 8。Send 和 Sync 其實是同一件事的兩個名字,String 能送到別的執行緒就夠了" },
      ],
      answer: 0,
      explanation: `Send 和 Sync 回答兩個不同的問題。Send:這個值的所有權能不能交給另一條執行緒?Sync:這個值能不能同時被多條執行緒透過共享參考存取?精確定義是「T 是 Sync,若且唯若 &T 是 Send」。Arc<T> 同時牽涉兩件事——它讓多條執行緒同時持有指向同一個 T 的參考(需要 T: Sync),而且最後一個 drop 的執行緒會負責釋放 T,等於把 T 的所有權交給了那條執行緒(需要 T: Send)。所以標準庫規定:T 同時是 Send 與 Sync,Arc<T> 才是 Send。
String 的所有讀取方法都只取 &self,裡面沒有任何「讀的時候偷偷改東西」的狀態,所以它是 Sync——多條執行緒同時讀完全安全,不需要 Mutex。「Arc 對任何 T 都是 Send」正是下一題要打破的誤解:Arc 只保護計數,不保護 T。「Send 和 Sync 是同一件事」也不對:2-3 提過 RefCell 可以整個送出去(Send),卻不能讓多條執行緒同時借用(不是 Sync),兩者可以各自成立。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::sync::Arc;", note: "引入 Arc。" },
          { code: "use std::thread;", note: "引入 thread 模組。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let motto = Arc::new(String::from(\"fearless\"));", note: "String 交給 Arc 管理。" },
          { code: "    let m2 = Arc::clone(&motto);", note: "第二個把手。從現在起,兩條執行緒會同時「看得到」同一個 String。" },
          { code: "    let handle = thread::spawn(move || m2.len());", note: "m2 被 move 進子執行緒。編譯器檢查 Arc<String> 是不是 Send,而這又要求 String 同時是 Send 與 Sync——兩者都成立,通過。" },
          { code: "    println!(\"{} {}\", motto, handle.join().unwrap());", note: "格式化參數先被求值:join 等子執行緒回傳 8;期間主執行緒與子執行緒可能同時讀同一個 String,這正是 Sync 保證安全的情境。印出 fearless 8。" },
          { code: "}", note: "最後一個 Arc 被 drop,String 被釋放。" },
        ],
        outro: "常見型別的分類表:i32、String、Vec<T>(T 符合時)兩者都是;Rc 兩者都不是;RefCell 與 Cell 是 Send 但不是 Sync;Mutex<T> 只要 T 是 Send,它就是 Send 也是 Sync——Mutex 正是把「不能同時存取」的東西變成「可以共享」的轉換器。",
      },
      csharp: `C# 沒有這層區分,甚至沒有這兩個概念的名字:文件上只有籠統的「執行緒安全」一詞,而它常常指的是「可以同時讀寫」,有時又只指「可以同時讀」,全靠讀文件判斷。Rust 把它拆成兩個獨立、各自由編譯器推導的 trait:Send 管「交出去」,Sync 管「一起看」。這個拆分讓 Rc(兩者皆否)、RefCell(只有 Send)、Mutex(兩者皆是)各自落在精確的位置上,API 可以只要求它真正需要的那一個。`,
    },
    {
      id: "2-7-13",
      question: "把內部用 RefCell 的 struct 包進 Arc,送給子執行緒去改。以下程式的結果是?",
      questionCode: "use std::cell::RefCell;\nuse std::sync::Arc;\nuse std::thread;\n\nstruct Stats {\n    hits: RefCell<u32>,\n}\n\nfn main() {\n    let stats = Arc::new(Stats { hits: RefCell::new(0) });\n    let s = Arc::clone(&stats);\n    let handle = thread::spawn(move || {\n        *s.hits.borrow_mut() += 1;\n    });\n    handle.join().unwrap();\n    println!(\"{}\", stats.hits.borrow());\n}",
      options: [
        { text: "編譯錯誤:RefCell<u32> cannot be shared between threads safely——RefCell 不能被多條執行緒同時存取,於是 Stats 也不能,於是 Arc<Stats> 不能送到別的執行緒;把欄位換成 Mutex<u32> 就能編譯" },
        { text: "印出 1——資料已經包在 Arc 裡,Arc 會負責讓裡面的東西執行緒安全" },
        { text: "印出 1——只有一條子執行緒在改,主執行緒是 join 之後才讀,實際上沒有同時存取,編譯器看得出來所以放行" },
        { text: "能編譯,但若兩條執行緒同時 borrow_mut 會在執行期 panic(already borrowed)——跟單執行緒的 RefCell 一樣是執行期檢查" },
      ],
      answer: 0,
      explanation: `這是本課的亮點:一段在 C# 會安靜地編譯成功、偶爾在正式環境損毀資料的程式,Rust 在編譯期就拒絕了,而且錯誤訊息一路指出推導鏈。RefCell 的借用計數是普通整數,兩條執行緒同時 borrow_mut 會讓計數競爭,連 RefCell 自己的執行期檢查都可能失效——所以 RefCell 不是 Sync。Sync 和 Send 一樣是自動推導的:Stats 含有 RefCell 欄位,Stats 就不是 Sync;上一題說過 Arc<T> 要是 Send 必須 T: Sync,所以 Arc<Stats> 不是 Send;閉包捕獲了它,閉包也不是 Send,spawn 拒絕。
「Arc 會負責讓裡面的東西執行緒安全」是這題要破的誤解,Arc 只保證計數。「實際上沒有同時存取所以放行」也不成立——編譯器不分析執行時序,只看型別能不能保證安全(和第 3 題不看 join 是同一個道理)。「執行期 panic」則把 RefCell 在單執行緒的保護想像成多執行緒也有效,事實上在多執行緒下它根本無法保證檢查正確,這正是它必須在編譯期被擋下的原因。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "use std::cell::RefCell;", note: "引入 RefCell(2-3 的單執行緒內部可變性工具)。" },
            { code: "use std::sync::Arc;", note: "引入 Arc。" },
            { code: "use std::thread;", note: "引入 thread 模組。" },
            { code: "", note: "" },
            { code: "struct Stats {", note: "自訂 struct,沒有任何手寫的 impl。" },
            { code: "    hits: RefCell<u32>,", note: "這個欄位決定了 Stats 的命運:RefCell 的借用計數沒有同步保護,不能被多條執行緒同時存取。" },
            { code: "}", note: "編譯器自動推導:Stats 不能被多條執行緒共享。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let stats = Arc::new(Stats { hits: RefCell::new(0) });", note: "包進 Arc,打算讓兩條執行緒共同擁有。" },
            { code: "    let s = Arc::clone(&stats);", note: "第二個把手。" },
            { code: "    let handle = thread::spawn(move || {", note: "⛔ 編譯失敗:RefCell<u32> cannot be shared between threads safely(E0277)。help:within Stats, the trait Sync is not implemented for RefCell<u32>;note:required for Arc<Stats> to implement Send;note:required by a bound in spawn。" },
            { code: "        *s.hits.borrow_mut() += 1;", note: "想透過 RefCell 在子執行緒修改計數。單執行緒這樣寫沒問題,跨執行緒就是資料競爭。" },
            { code: "    });", note: "閉包結束。" },
            { code: "    handle.join().unwrap();", note: "就算這裡有 join,編譯器也不採信——它只看型別。" },
            { code: "    println!(\"{}\", stats.hits.borrow());", note: "主執行緒讀取計數。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::sync::{Arc, Mutex};", note: "改動處:不再需要 RefCell,改引入 Mutex。" },
            { code: "use std::thread;", note: "引入 thread 模組。" },
            { code: "", note: "" },
            { code: "struct Stats {", note: "同一個 struct。" },
            { code: "    hits: Mutex<u32>,", note: "改動處:RefCell 換成 Mutex。Mutex<u32> 可以被多條執行緒共享,所以 Stats 自動變成可共享,Arc<Stats> 也就能送到別的執行緒。" },
            { code: "}", note: "不用寫任何 impl,推導自動翻盤。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let stats = Arc::new(Stats { hits: Mutex::new(0) });", note: "改動處:初始化改成 Mutex::new。" },
            { code: "    let s = Arc::clone(&stats);", note: "第二個把手。" },
            { code: "    let handle = thread::spawn(move || {", note: "通過 spawn 的檢查。" },
            { code: "        *s.hits.lock().unwrap() += 1;", note: "改動處:borrow_mut() 換成 lock()。兩者的角色完全對應——都是「從共享參考換到可修改的存取」,只是 lock 用的是執行緒安全的鎖。" },
            { code: "    });", note: "暫時的 guard 在上一行結束時已經解鎖。" },
            { code: "    handle.join().unwrap();", note: "等子執行緒改完。" },
            { code: "    println!(\"{}\", stats.hits.lock().unwrap());", note: "改動處:讀取也透過 lock,印出 1。" },
            { code: "}", note: "main 結束,Stats 被釋放。" },
          ],
          outro: "如果讀遠多於寫,可以把 Mutex 換成 RwLock(允許多個讀者同時持有);如果只是一個整數計數器,std::sync::atomic 的 AtomicU32 連鎖都不用。三者都是 Sync,都能替代 RefCell 的位置。",
        },
        {
          label: "△ Desugared (what the compiler sees)",
          intro: "錯誤訊息提到 Sync 和 Send,但你的程式碼裡一個都沒寫。它們是四層自動推導串起來的:",
          lines: [
            { code: "thread::spawn(move || { *s.hits.borrow_mut() += 1; });", note: "你寫的這一行。" },
            { code: "", note: "" },
            { code: "// 1. spawn 的簽名要求:", note: "起點是 spawn 的 bound(見第 10 題的展開)。" },
            { code: "//    F: FnOnce() -> T + Send + 'static", note: "閉包必須是 Send。" },
            { code: "// 2. 閉包的匿名 struct:", note: "閉包是 Send,若且唯若它的每個欄位是 Send。" },
            { code: "struct Closure { s: Arc<Stats> }", note: "唯一的欄位是 Arc<Stats>。" },
            { code: "// 3. 標準庫對 Arc 的規定:", note: "Arc 能不能送出去,取決於裡面的 T。" },
            { code: "unsafe impl<T: ?Sized + Sync + Send> Send for Arc<T> {}", note: "T 必須同時是 Sync 與 Send,Arc<T> 才是 Send。錯誤訊息的 required for Arc<Stats> to implement Send 就是這一行。" },
            { code: "// 4. Stats 的 Sync 由欄位自動推導:", note: "所有欄位都是 Sync,struct 才是 Sync。" },
            { code: "impl<T: ?Sized> !Sync for RefCell<T> {}", note: "標準庫內部的負向實作(一般程式碼不能這樣寫):明確宣告 RefCell 永遠不是 Sync。錯誤訊息的 the trait Sync is not implemented for RefCell<u32> 就是這一行。" },
            { code: "", note: "" },
            { code: "// RefCell<u32>: 非 Sync => Stats: 非 Sync => Arc<Stats>: 非 Send => Closure: 非 Send", note: "推導鏈從最內層一路傳到 spawn,任何一環斷掉都會在這裡被擋下。" },
          ],
        },
      ],
      csharp: `把這段翻成 C#:一個 class Stats { public int Hits; },丟進兩個 Task 各自 Hits++——編譯成功、測試通過、上線後偶爾少算幾次。C# 沒有任何機制能在編譯期知道 Stats 內部的欄位「不能被兩條執行緒同時修改」。Rust 的 Send/Sync 是透過型別組成自動推導的,只要 struct 深處藏了一個非執行緒安全的元件,整個型別就被標記,錯誤會一路傳到 thread::spawn 這個關口——不需要任何人想起來去檢查。`,
    },
    {
      id: "2-7-14",
      question: "Arc<Mutex<T>>:8 條執行緒各把同一個計數器加 1000 次。以下程式的輸出是?",
      questionCode: "use std::sync::{Arc, Mutex};\nuse std::thread;\n\nfn main() {\n    let counter = Arc::new(Mutex::new(0));\n    let mut handles = Vec::new();\n    for _ in 0..8 {\n        let c = Arc::clone(&counter);\n        handles.push(thread::spawn(move || {\n            for _ in 0..1000 {\n                *c.lock().unwrap() += 1;\n            }\n        }));\n    }\n    for h in handles {\n        h.join().unwrap();\n    }\n    println!(\"{} {}\", *counter.lock().unwrap(), Arc::strong_count(&counter));\n}",
      options: [
        { text: "印出 8000 1——每次 += 1 都在鎖裡完成,不會漏算;每條子執行緒的 c 在閉包跑完時就被 drop,全部 join 之後只剩主執行緒那一份 Arc" },
        { text: "印出一個小於 8000 的數字,而且每次執行都不一樣——8 條執行緒同時 += 1 會互相覆蓋" },
        { text: "印出 8000 9——clone 出來的 8 個 Arc 還記在計數裡,要等 main 結束才會減回去" },
        { text: "印出 8000 1;而且把 Arc<Mutex<i32>> 換成 2-3 的 Rc<RefCell<i32>> 也能跑出一樣的結果,只是省掉鎖的成本" },
      ],
      answer: 0,
      explanation: `Arc<Mutex<T>> 是 2-3 那個 Rc<RefCell<T>> 的多執行緒版本:外層負責共同擁有,內層負責「從共享參考換到獨佔修改」。每次 *c.lock().unwrap() += 1 都是「上鎖、讀、加、寫、解鎖」一氣呵成,其他執行緒只能等,所以 8000 次一次都不會漏,結果每次都是 8000。
第二個數字考的是所有權:每個 move 閉包擁有自己的 c,閉包執行完畢時 c 被 drop,計數減一;join 回傳時那條執行緒已經結束,它的 c 一定已經被 drop。8 條都 join 完,只剩 counter 自己,所以是 1,不是 9。「小於 8000」是 C# 裡不加鎖寫 count++ 的經驗,那在 Rust 根本寫不出來(第 9 題)。至於換成 Rc<RefCell<i32>>——它們既不是 Send 也不是 Sync,連編譯都過不了(第 10、13 題),兩種組合不能互換。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::sync::{Arc, Mutex};", note: "兩個一起引入:這個組合幾乎總是成對出現。" },
          { code: "use std::thread;", note: "引入 thread 模組。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let counter = Arc::new(Mutex::new(0));", note: "Arc<Mutex<i32>>:Arc 讓多條執行緒共同擁有,Mutex 讓它們輪流修改。強計數為 1。" },
          { code: "    let mut handles = Vec::new();", note: "收集 JoinHandle,之後逐一 join。" },
          { code: "    for _ in 0..8 {", note: "開 8 條執行緒。" },
          { code: "        let c = Arc::clone(&counter);", note: "每一輪 clone 一個新的把手,強計數依序變成 2、3⋯⋯9。" },
          { code: "        handles.push(thread::spawn(move || {", note: "這一輪的 c 被 move 進這條執行緒,之後主執行緒碰不到它。" },
          { code: "            for _ in 0..1000 {", note: "每條執行緒加 1000 次。" },
          { code: "                *c.lock().unwrap() += 1;", note: "上鎖、加一、在敘述結束時解鎖。任一時刻只有一條執行緒在做這件事,不會互相覆蓋。" },
          { code: "            }", note: "迴圈結束。" },
          { code: "        }));", note: "閉包結束時 c 被 drop,強計數減一。" },
          { code: "    }", note: "8 條執行緒都已經開出去了。" },
          { code: "    for h in handles {", note: "逐一等待。" },
          { code: "        h.join().unwrap();", note: "join 回傳時,那條執行緒的閉包(連同它的 c)已經被釋放。" },
          { code: "    }", note: "全部結束:計數器是 8000,強計數回到 1。" },
          { code: "    println!(\"{} {}\", *counter.lock().unwrap(), Arc::strong_count(&counter));", note: "印出 8000 1。" },
          { code: "}", note: "最後一個 Arc 被 drop,Mutex 與整數被釋放。" },
        ],
        outro: "樣板要記熟:在迴圈裡先 let c = Arc::clone(&x); 再 move 進閉包。直接 move 外層的 x 只能 move 一次,第二輪就會得到 use of moved value。",
      },
      csharp: `同樣的需求在 C# 多半寫成 Interlocked.Increment(ref count) 或在 lock 區塊裡 count++,也有人直接用 ConcurrentDictionary 之類把鎖藏在內部的集合。差別在於:C# 裡不加鎖的 count++ 照樣能編譯,結果偶爾少算;Rust 裡「共享又可修改」只能透過 Mutex(或原子型別)這類同步元件取得,不加鎖的版本根本寫不出來。如果只是計數,Rust 也有對應 Interlocked 的 AtomicUsize,不需要 Mutex。`,
    },
    {
      id: "2-7-15",
      question: "兩條執行緒以相反的順序鎖兩個 Mutex(Barrier 確保雙方都先拿到第一把鎖)。以下程式的結果是?",
      questionCode: "use std::sync::{Arc, Barrier, Mutex};\nuse std::thread;\n\nfn main() {\n    let a = Arc::new(Mutex::new(0));\n    let b = Arc::new(Mutex::new(0));\n    let gate = Arc::new(Barrier::new(2));\n    let (a2, b2, gate2) = (Arc::clone(&a), Arc::clone(&b), Arc::clone(&gate));\n    let t = thread::spawn(move || {\n        let _gb = b2.lock().unwrap();\n        gate2.wait();\n        let _ga = a2.lock().unwrap();\n        println!(\"thread got both\");\n    });\n    let _ga = a.lock().unwrap();\n    gate.wait();\n    let _gb = b.lock().unwrap();\n    println!(\"main got both\");\n    t.join().unwrap();\n}",
      options: [
        { text: "編譯通過,執行後什麼都不印、永遠卡住——兩條執行緒各握一把鎖、等對方手上那一把,形成死鎖;Rust 的編譯期檢查不涵蓋死鎖" },
        { text: "編譯錯誤:借用檢查器看得出兩條執行緒上鎖的順序相反,直接拒絕" },
        { text: "印出 main got both 和 thread got both(順序不定)——Mutex 偵測到互相等待時,會讓其中一方暫時放手重試" },
        { text: "執行期 panic:std 的 Mutex 偵測到死鎖時,lock() 會回傳 Err,unwrap() 因此 panic" },
      ],
      answer: 0,
      explanation: `這題要誠實地劃出 Rust 保證的邊界。Send/Sync 與借用檢查擋下的是「資料競爭」——兩條執行緒在沒有同步的情況下同時存取同一塊記憶體。死鎖不是資料競爭:每一次存取都乖乖地上了鎖,只是上鎖的順序讓雙方互相等待。這在型別上完全合法,編譯器不會也無法察覺。
std 的 Mutex 沒有死鎖偵測,也不會自動放手重試:lock() 就是一直等。lock() 回傳的 Err 只有一種意思——「鎖被毒化」(之前持有它的執行緒 panic 了),和死鎖無關。這裡的 Barrier 讓兩條執行緒都拿到第一把鎖之後才繼續,所以每次都必定卡死;真實程式裡沒有 Barrier,死鎖只會偶爾發生,更難除錯。標準解法是讓所有執行緒以同一個固定順序上鎖。`,
      walkthrough: [
        {
          label: "X Question code (compiles, but deadlocks at runtime)",
          lines: [
            { code: "use std::sync::{Arc, Barrier, Mutex};", note: "Barrier 是一個集合點:指定數量的執行緒都呼叫 wait() 之後,大家才一起往下走。" },
            { code: "use std::thread;", note: "引入 thread 模組。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let a = Arc::new(Mutex::new(0));", note: "第一把鎖 a。" },
            { code: "    let b = Arc::new(Mutex::new(0));", note: "第二把鎖 b。" },
            { code: "    let gate = Arc::new(Barrier::new(2));", note: "要等 2 條執行緒都到齊的集合點,用來讓死鎖必定發生。" },
            { code: "    let (a2, b2, gate2) = (Arc::clone(&a), Arc::clone(&b), Arc::clone(&gate));", note: "給子執行緒的三個把手。" },
            { code: "    let t = thread::spawn(move || {", note: "子執行緒開始。" },
            { code: "        let _gb = b2.lock().unwrap();", note: "⛔ 子執行緒先鎖 b——和主執行緒的順序相反,這是死鎖的起因。" },
            { code: "        gate2.wait();", note: "等主執行緒也拿到它的第一把鎖。" },
            { code: "        let _ga = a2.lock().unwrap();", note: "想鎖 a,但 a 在主執行緒手上,而主執行緒在等 b——永遠等下去。" },
            { code: "        println!(\"thread got both\");", note: "永遠執行不到。" },
            { code: "    });", note: "閉包結束。" },
            { code: "    let _ga = a.lock().unwrap();", note: "⛔ 主執行緒先鎖 a。" },
            { code: "    gate.wait();", note: "兩邊都到齊:主執行緒握 a,子執行緒握 b。" },
            { code: "    let _gb = b.lock().unwrap();", note: "想鎖 b,但 b 在子執行緒手上——雙方互等,死鎖。程式什麼都不印,也不會結束。" },
            { code: "    println!(\"main got both\");", note: "永遠執行不到。" },
            { code: "    t.join().unwrap();", note: "永遠執行不到。" },
            { code: "}", note: "main 結束不了。" },
          ],
          outro: "注意這個區塊能順利編譯:X 在這裡代表「行為錯誤」,不是編譯錯誤。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::sync::{Arc, Mutex};", note: "不再需要 Barrier。" },
            { code: "use std::thread;", note: "引入 thread 模組。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let a = Arc::new(Mutex::new(0));", note: "第一把鎖 a。" },
            { code: "    let b = Arc::new(Mutex::new(0));", note: "第二把鎖 b。" },
            { code: "    let (a2, b2) = (Arc::clone(&a), Arc::clone(&b));", note: "給子執行緒的把手。" },
            { code: "    let t = thread::spawn(move || {", note: "子執行緒開始。" },
            { code: "        let mut ga = a2.lock().unwrap();", note: "改動處:子執行緒也先鎖 a。全程式統一「先 a 後 b」。" },
            { code: "        let mut gb = b2.lock().unwrap();", note: "再鎖 b。拿得到 a 的人才有資格等 b,所以不會出現互等。" },
            { code: "        *ga += 1;", note: "修改 a 裡的值。" },
            { code: "        *gb += 1;", note: "修改 b 裡的值。" },
            { code: "    });", note: "兩個 guard 被 drop,兩把鎖都釋放。" },
            { code: "    {", note: "主執行緒用一個區塊界定兩個 guard 的壽命。" },
            { code: "        let mut ga = a.lock().unwrap();", note: "同樣先鎖 a。" },
            { code: "        let mut gb = b.lock().unwrap();", note: "再鎖 b。" },
            { code: "        *ga += 1;", note: "修改 a。" },
            { code: "        *gb += 1;", note: "修改 b。" },
            { code: "    }", note: "兩把鎖在這裡釋放。" },
            { code: "    t.join().unwrap();", note: "等子執行緒結束。" },
            { code: "    println!(\"{} {}\", *a.lock().unwrap(), *b.lock().unwrap());", note: "兩邊各加了一次,印出 2 2。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "固定上鎖順序是最常用的防線;另一個做法是把需要一起保護的資料放進同一個 Mutex,從根本上只剩一把鎖。",
        },
      ],
      csharp: `C# 的 lock (a) { lock (b) { ... } } 配上另一條執行緒的 lock (b) { lock (a) { ... } },是一模一樣的死鎖,C# 編譯器同樣不會攔。這一點 Rust 和 C# 站在同一條線上:兩者都要靠「固定上鎖順序」這類紀律,或靠 Monitor.TryEnter 加逾時(Rust 對應 try_lock)來防範。學 Rust 並行時要記得它的承諾是 fearless concurrency 裡的「沒有資料競爭」,不是「沒有任何並行 bug」。`,
    },
    {
      id: "2-7-16",
      question: "同一條執行緒握著鎖時,又呼叫了一個會鎖同一個 Mutex 的函式。以下程式的結果是?",
      questionCode: "use std::sync::Mutex;\n\nfn total(m: &Mutex<Vec<i32>>) -> i32 {\n    m.lock().unwrap().iter().sum()\n}\n\nfn main() {\n    let m = Mutex::new(vec![1, 2]);\n    let mut guard = m.lock().unwrap();\n    guard.push(3);\n    println!(\"{}\", total(&m));\n}",
      options: [
        { text: "編譯通過,但執行時不會正常印出結果(標準庫只保證這種情況下第二次 lock() 不會成功回傳,實際上通常是永遠卡住)——main 的 guard 還沒釋放,total 又去鎖同一個 Mutex,而 std::sync::Mutex 不可重入" },
        { text: "印出 6——同一條執行緒再次 lock() 自己已經握有的鎖會直接通過,就像 C# 的 lock 可以重入" },
        { text: "印出 6——guard 在 guard.push(3) 之後就沒再被用到,非詞法生命週期(NLL)會讓鎖在那一行之後提早釋放" },
        { text: "編譯錯誤:m 已經被 guard 借走,借用檢查器不允許再把 &m 傳給 total" },
      ],
      answer: 0,
      explanation: `std::sync::Mutex 是不可重入的:它只知道「鎖被拿走了」,不管拿走的是誰。main 手上的 guard 活到 main 結束才會被 drop,total 裡的 lock() 就在等一把永遠不會被釋放的鎖——而且等的正是自己。標準庫文件對這種情況的描述是「行為未指定,但這次呼叫不會回傳(可能死鎖,也可能 panic)」,實際在常見平台上就是卡住。
「NLL 會提早釋放」是很有說服力的誤解:NLL 讓「借用」在最後一次使用後結束,但 guard 有 Drop 實作,Drop 永遠在作用域結束時才執行,不會因為後面沒用到就提早——鎖的壽命跟著 guard 的作用域,不跟著最後一次使用。「借用檢查器擋下 &m」也不會發生:lock() 只需要 &self,guard 對 m 的借用是共享的,再借一次 &m 完全合法。這也是為什麼這個 bug 能通過編譯——在型別上它沒有任何問題。`,
      walkthrough: [
        {
          label: "X Question code (compiles, but hangs at runtime)",
          lines: [
            { code: "use std::sync::Mutex;", note: "引入 Mutex。" },
            { code: "", note: "" },
            { code: "fn total(m: &Mutex<Vec<i32>>) -> i32 {", note: "一個看起來無害的輔助函式:自己上鎖、加總、解鎖。" },
            { code: "    m.lock().unwrap().iter().sum()", note: "單獨呼叫時完全正常。問題在呼叫它的人是不是已經握著這把鎖。" },
            { code: "}", note: "函式結束,暫時的 guard 解鎖。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let m = Mutex::new(vec![1, 2]);", note: "一個 Mutex 包著 Vec。" },
            { code: "    let mut guard = m.lock().unwrap();", note: "⛔ 取得鎖並綁定到變數 guard——它會活到 main 的右大括號,鎖也一樣。這是問題的起因。" },
            { code: "    guard.push(3);", note: "透過 guard 修改,Vec 變成 [1, 2, 3]。這之後 guard 雖然不再被使用,但它的 Drop 要到作用域結束才執行,鎖沒有被釋放。" },
            { code: "    println!(\"{}\", total(&m));", note: "⛔ total 裡的 lock() 要等 guard 釋放,而 guard 要等 main 結束才釋放——同一條執行緒自己等自己。程式卡住,什麼都不印。" },
            { code: "}", note: "永遠走不到這裡,guard 也永遠不會被 drop。" },
          ],
          outro: "注意這個區塊能順利編譯:X 在這裡代表「執行期行為錯誤」。現實中的版本通常更隱蔽——輔助函式在另一個模組,呼叫端根本不知道它內部會上鎖。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::sync::Mutex;", note: "引入 Mutex。" },
            { code: "", note: "" },
            { code: "fn total(m: &Mutex<Vec<i32>>) -> i32 {", note: "輔助函式完全不用改。" },
            { code: "    m.lock().unwrap().iter().sum()", note: "上鎖、加總、解鎖。" },
            { code: "}", note: "函式結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let m = Mutex::new(vec![1, 2]);", note: "同一個 Mutex。" },
            { code: "    {", note: "改動處:用一個區塊把 guard 的壽命圈起來。" },
            { code: "        let mut guard = m.lock().unwrap();", note: "在區塊裡取得鎖。" },
            { code: "        guard.push(3);", note: "修改,Vec 變成 [1, 2, 3]。" },
            { code: "    }", note: "改動處的效果:guard 在這裡被 drop,鎖被釋放。" },
            { code: "    println!(\"{}\", total(&m));", note: "total 可以順利上鎖,印出 6。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "等效的寫法是在呼叫 total 之前寫 drop(guard);。另一種常見的重構是讓輔助函式直接接收已經鎖住的資料(參數型別改成 &[i32] 或 &Vec<i32>),呼叫端傳 &guard 進去,整個流程只上一次鎖。",
        },
      ],
      csharp: `這是 C# 開發者最需要改掉的一個直覺:C# 的 lock(底層是 Monitor)是可重入的,同一條執行緒在 lock (obj) 裡再進一次 lock (obj) 會直接通過,只是計數加一。所以「外層上鎖、再呼叫一個內部也上鎖的方法」在 C# 是日常寫法。std::sync::Mutex 不可重入,同樣的結構在 Rust 就是自我死鎖。遇到需要重入的設計,通常代表鎖的範圍劃錯了,應該照正確寫法縮小 guard 的作用域,或讓內部函式接收已經鎖住的資料。`,
    },
    {
      id: "2-7-17",
      question: "設計選擇:要在多條執行緒之間共用一個「單字 → 次數」的 HashMap(C# 會直接用 ConcurrentDictionary)。下面是一種寫法,關於 Rust 在這個情境的慣例,哪個說法正確?",
      questionCode: "use std::collections::HashMap;\nuse std::sync::{Arc, Mutex};\nuse std::thread;\n\nfn main() {\n    let counts: Arc<Mutex<HashMap<String, u32>>> = Arc::new(Mutex::new(HashMap::new()));\n    let mut handles = Vec::new();\n    for word in [\"apple\", \"banana\", \"apple\"] {\n        let counts = Arc::clone(&counts);\n        handles.push(thread::spawn(move || {\n            *counts.lock().unwrap().entry(word.to_string()).or_insert(0) += 1;\n        }));\n    }\n    for h in handles {\n        h.join().unwrap();\n    }\n    let map = counts.lock().unwrap();\n    println!(\"apple={} banana={}\", map[\"apple\"], map[\"banana\"]);\n}",
      options: [
        { text: "標準庫沒有內建的執行緒安全 HashMap;慣例就是這樣用 Arc<Mutex<HashMap<K, V>>> 自己組合(讀多寫少可換成 RwLock),真的需要細粒度鎖的高並行場景,再引入第三方 crate(例如 dashmap)" },
        { text: "應該改用標準庫的 std::sync::ConcurrentHashMap,它就是 ConcurrentDictionary 的對應,Arc<Mutex<HashMap>> 是舊版 Rust 的過時寫法" },
        { text: "Mutex 是多餘的:HashMap 本身就能被多條執行緒共享,用 Arc<HashMap<K, V>> 就可以讓各執行緒同時呼叫 insert" },
        { text: "最省事的做法是宣告一個 static mut 的全域 HashMap,所有執行緒直接存取,省掉 Arc 和 Mutex 的成本" },
      ],
      answer: 0,
      explanation: `Rust 標準庫刻意保持精簡:它提供可以自由組合的基礎元件(Arc 管共同擁有、Mutex/RwLock 管互斥),而不是為每一種集合各做一個執行緒安全版本。Arc<Mutex<HashMap<K, V>>> 就是這種組合的標準產物,讀寫多的時候整張表只有一把鎖,這在大多數程式裡已經夠用。確實遇到鎖競爭成為瓶頸,社群的 dashmap 之類 crate 提供分片鎖的並行 HashMap,才更接近 ConcurrentDictionary 的內部設計。
std 裡沒有 ConcurrentHashMap,這是最常見的「名字不同而已」誤解。「Arc<HashMap> 就能同時 insert」踩的是第 9 題的坑:Arc 只給共享參考,insert 需要 &mut self,編譯不過。static mut 則更糟:每次存取都要寫 unsafe,而且多執行緒同時存取就是未定義行為——這等於把編譯器替你做的所有檢查關掉,Rust 2024 版更把對 static mut 取參考設為預設報錯。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::collections::HashMap;", note: "標準的 HashMap,本身不是給多執行緒同時修改的。" },
          { code: "use std::sync::{Arc, Mutex};", note: "兩個組合元件。" },
          { code: "use std::thread;", note: "引入 thread 模組。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let counts: Arc<Mutex<HashMap<String, u32>>> = Arc::new(Mutex::new(HashMap::new()));", note: "由外而內讀:Arc 讓大家共同擁有,Mutex 讓大家輪流修改,HashMap 是被保護的資料。型別標註通常可以省略,這裡寫出來方便閱讀。" },
          { code: "    let mut handles = Vec::new();", note: "收集 JoinHandle。" },
          { code: "    for word in [\"apple\", \"banana\", \"apple\"] {", note: "三個單字,apple 出現兩次。" },
          { code: "        let counts = Arc::clone(&counts);", note: "每一輪複製一個把手,並用同名變數遮蔽外層的 counts——這是社群常見的寫法,閉包裡照樣叫 counts。" },
          { code: "        handles.push(thread::spawn(move || {", note: "word(&'static str)和這一輪的 counts 一起被 move 進去。" },
          { code: "            *counts.lock().unwrap().entry(word.to_string()).or_insert(0) += 1;", note: "上鎖後用 entry API「沒有就插入 0,然後加一」,整個動作在鎖裡完成,不會有兩條執行緒同時插入同一個鍵。敘述結束時解鎖。" },
          { code: "        }));", note: "閉包結束,這一輪的 Arc 被 drop。" },
          { code: "    }", note: "三條執行緒都已開出。" },
          { code: "    for h in handles {", note: "逐一等待。" },
          { code: "        h.join().unwrap();", note: "等每條執行緒完成。" },
          { code: "    }", note: "全部完成。" },
          { code: "    let map = counts.lock().unwrap();", note: "主執行緒上鎖取得 guard,之後透過它讀取整張表。" },
          { code: "    println!(\"apple={} banana={}\", map[\"apple\"], map[\"banana\"]);", note: "印出 apple=2 banana=1。三條執行緒的完成順序不固定,但結果固定。" },
          { code: "}", note: "guard 被 drop 解鎖,接著 Arc、Mutex、HashMap 依序釋放。" },
        ],
        outro: "一個實務提醒:鎖的範圍就是 guard 的壽命。如果在持有 guard 的同時做耗時的工作(I/O、長計算),其他執行緒就全部在等——先把需要的資料複製出來、盡快釋放鎖,是這個組合最重要的使用習慣。",
      },
      csharp: `C# 的 ConcurrentDictionary<TKey, TValue> 開箱即用,內部用分段鎖與無鎖讀取,還提供 AddOrUpdate 這種原子操作。Rust 選擇了另一條路:標準庫只給 Arc、Mutex、RwLock 這些可組合的元件,由你組合出 Arc<Mutex<HashMap>>,需要更高並行度時再從生態系挑一個專用實作。好處是任何集合(Vec、BTreeMap、自訂 struct)都能用同一套方法變得可共享;代價是整張表一把鎖,效能特性和 ConcurrentDictionary 不同,不能直接類比。`,
    },
    {
      id: "2-7-18",
      question: "worker pool 雛形:3 個 worker 共用同一個 Receiver 搶工作。以下程式的輸出是?",
      questionCode: "use std::sync::{mpsc, Arc, Mutex};\nuse std::thread;\n\nfn main() {\n    let (tx, rx) = mpsc::channel::<u32>();\n    let rx = Arc::new(Mutex::new(rx));\n    let mut workers = Vec::new();\n    for _ in 0..3 {\n        let rx = Arc::clone(&rx);\n        workers.push(thread::spawn(move || {\n            let (mut sum, mut jobs) = (0, 0);\n            loop {\n                let msg = rx.lock().unwrap().recv();\n                match msg {\n                    Ok(n) => {\n                        sum += n;\n                        jobs += 1;\n                    }\n                    Err(_) => break,\n                }\n            }\n            (sum, jobs)\n        }));\n    }\n    for n in 1..=10 {\n        tx.send(n).unwrap();\n    }\n    drop(tx);\n    let (mut sum, mut jobs) = (0, 0);\n    for w in workers {\n        let (s, j) = w.join().unwrap();\n        sum += s;\n        jobs += j;\n    }\n    println!(\"sum={} jobs={}\", sum, jobs);\n}",
      options: [
        { text: "印出 sum=55 jobs=10——三個 worker 共用同一個 Receiver,每則訊息只會被其中一個 worker 拿走;tx 被 drop、佇列清空之後,每個 worker 的 recv() 都回傳 Err 而跳出迴圈" },
        { text: "印出 sum=165 jobs=30——每個 worker 都會收到全部 10 則訊息" },
        { text: "永遠卡住——worker 的 loop 沒有結束條件,佇列空了之後 recv() 會一直等下去" },
        { text: "編譯錯誤:Receiver 不能被多條執行緒共享,包進 Arc<Mutex<...>> 也一樣過不了 spawn" },
      ],
      answer: 0,
      explanation: `第 5 題看過 Receiver 不能 clone;這題是標準的繞法:把唯一的 Receiver 放進 Mutex,再用 Arc 分給每個 worker。任一時刻只有一個 worker 能鎖住它並呼叫 recv(),拿到的訊息就是它的,其他 worker 拿不到同一則。所以 10 則訊息被三個 worker 瓜分——誰拿幾則每次不一樣,但總數一定是 10、總和一定是 55。
「會編譯錯誤」的直覺來自第 13 題:Receiver 確實不是 Sync(不能被多條執行緒同時碰),但它是 Send,而 Mutex<T> 只要 T 是 Send 就同時是 Send 與 Sync——Mutex 正是把「不能共享」變成「可以共享」的那一層。「永遠卡住」則漏看了 drop(tx):那是唯一的 Sender,它一消失,所有 worker 的 recv() 在清空佇列後都會拿到 Err,break 跳出。「每個 worker 收到全部」是廣播語意,channel 從來不是這樣運作。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::sync::{mpsc, Arc, Mutex};", note: "channel、共同擁有、互斥,三件工具一次到齊。" },
          { code: "use std::thread;", note: "引入 thread 模組。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let (tx, rx) = mpsc::channel::<u32>();", note: "工作佇列,訊息型別是 u32。" },
          { code: "    let rx = Arc::new(Mutex::new(rx));", note: "唯一的 Receiver 放進 Mutex(同一時間只有一個 worker 能 recv),再放進 Arc(讓每個 worker 都有把手)。" },
          { code: "    let mut workers = Vec::new();", note: "收集 worker 的 JoinHandle。" },
          { code: "    for _ in 0..3 {", note: "開 3 個 worker。" },
          { code: "        let rx = Arc::clone(&rx);", note: "每個 worker 一個把手,Receiver 本身仍然只有一個。" },
          { code: "        workers.push(thread::spawn(move || {", note: "把手被 move 進 worker。" },
          { code: "            let (mut sum, mut jobs) = (0, 0);", note: "每個 worker 自己的統計,不共享,不需要鎖。" },
          { code: "            loop {", note: "一直拿工作,直到 channel 斷線。" },
          { code: "                let msg = rx.lock().unwrap().recv();", note: "上鎖、取一則(沒有就等)。guard 是暫時值,這個敘述結束就解鎖,所以處理工作時不佔著鎖。若寫成 while let Ok(n) = rx.lock().unwrap().recv(),guard 會活到整個迴圈本體結束,worker 們就變成排隊輪流處理。" },
          { code: "                match msg {", note: "看看拿到了什麼。" },
          { code: "                    Ok(n) => {", note: "拿到一個工作。" },
          { code: "                        sum += n;", note: "累加數值。" },
          { code: "                        jobs += 1;", note: "記錄處理了幾則。" },
          { code: "                    }", note: "處理完,回頭搶下一則。" },
          { code: "                    Err(_) => break,", note: "所有 Sender 都消失而且佇列空了:不會再有工作,離開迴圈。" },
          { code: "                }", note: "match 結束。" },
          { code: "            }", note: "loop 結束。" },
          { code: "            (sum, jobs)", note: "把自己的統計當回傳值,透過 join 交回主執行緒。" },
          { code: "        }));", note: "worker 閉包結束。" },
          { code: "    }", note: "3 個 worker 都在等工作了。" },
          { code: "    for n in 1..=10 {", note: "送出 10 個工作。" },
          { code: "        tx.send(n).unwrap();", note: "1 到 10,由先搶到鎖的 worker 拿走。" },
          { code: "    }", note: "工作全部送出。" },
          { code: "    drop(tx);", note: "放掉唯一的 Sender,等於宣告「沒有新工作了」,worker 才會在佇列清空後結束。" },
          { code: "    let (mut sum, mut jobs) = (0, 0);", note: "準備彙總。" },
          { code: "    for w in workers {", note: "逐一等待 worker。" },
          { code: "        let (s, j) = w.join().unwrap();", note: "取回每個 worker 的統計;各自的數字每次執行都可能不同。" },
          { code: "        sum += s;", note: "加總數值。" },
          { code: "        jobs += j;", note: "加總則數。" },
          { code: "    }", note: "彙總完成。" },
          { code: "    println!(\"sum={} jobs={}\", sum, jobs);", note: "分配方式不固定,但總和固定:印出 sum=55 jobs=10。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "這就是 thread pool 的骨架:一條 channel 當工作佇列,Arc<Mutex<Receiver>> 讓多個 worker 搶工作,drop 掉 Sender 當作關機訊號。實務上的 thread pool 還會處理 panic 與優雅關閉,但結構就是這樣。",
      },
      csharp: `C# 的 Channel<T> 原生支援多個消費者:幾個 Task 同時 await reader.ReadAsync(),每則訊息只會交給其中一個,不需要任何額外包裝;BlockingCollection<T> 的 GetConsumingEnumerable() 也一樣。Rust std 的 mpsc 只支援單一消費者,多消費者要自己用 Arc<Mutex<Receiver>> 包,或改用第三方的多消費者 channel(例如 crossbeam-channel)。好處是 Mutex 讓「同一時間誰在取」變得明確可見;代價是要多寫一層,而且要留意 guard 的壽命,別讓 worker 在處理工作時還佔著鎖。`,
    },
    {
      id: "2-7-19",
      question: "設計選擇:下面用「訊息傳遞」與「共享狀態」兩種方式算出同一個結果(1² + 2² + 3²)。關於兩者的取捨,哪個說法最符合 Rust 的慣例?",
      questionCode: "use std::sync::{mpsc, Arc, Mutex};\nuse std::thread;\n\nfn main() {\n    let (tx, rx) = mpsc::channel();\n    for id in 1..=3 {\n        let tx = tx.clone();\n        thread::spawn(move || tx.send(id * id).unwrap());\n    }\n    drop(tx);\n    let by_message: i32 = rx.iter().sum();\n\n    let total = Arc::new(Mutex::new(0));\n    let mut handles = Vec::new();\n    for id in 1..=3 {\n        let total = Arc::clone(&total);\n        handles.push(thread::spawn(move || *total.lock().unwrap() += id * id));\n    }\n    for h in handles {\n        h.join().unwrap();\n    }\n    println!(\"{} {}\", by_message, *total.lock().unwrap());\n}",
      options: [
        { text: "兩者都是一等公民,依資料的流向挑選:資料是「一方產生、交給另一方處理」的流水線,就用 channel,所有權隨訊息移動、不必上鎖;多條執行緒要反覆讀寫同一份狀態(計數器、快取、設定),就用 Arc<Mutex<T>>。兩者由同一套 Send/Sync 規則把關,編譯器對兩者一樣嚴格" },
        { text: "Rust 只推薦 channel——官方口號是「不要透過共享記憶體來溝通」,Mutex 只是為了跟 C 互通才保留,新程式不該使用" },
        { text: "Arc<Mutex<T>> 永遠比 channel 快,channel 只是給初學者的簡化工具,效能敏感的程式都應該改寫成共享狀態" },
        { text: "channel 版本有 bug:三條執行緒沒有被 join,rx.iter().sum() 可能在訊息送完之前就算完,結果可能小於 14" },
      ],
      answer: 0,
      explanation: `The Rust Book 確實引用了 Go 的那句「不要透過共享記憶體來溝通,而要透過溝通來共享記憶體」,但緊接著就用整節篇幅教 Mutex 與 Arc,並說明兩種模型各有適用情境。Rust 的特別之處在於兩種模型都被同一套型別規則保護:channel 送出去的值必須是 Send,Arc<Mutex<T>> 裡的 T 必須是 Send——不管選哪一種,資料競爭都在編譯期被擋下,所以選擇的依據是「哪種寫法讓程式更清楚」,而不是「哪種比較安全」。
「只推薦 channel」把口號當成禁令,是這題最常見的誤解。「Mutex 永遠比較快」也不成立:效能取決於競爭程度與資料量,channel 讓每條執行緒各做各的、最後才匯總,鎖競爭反而可能更少。「channel 版本沒 join 所以會少算」則是誤解了 rx.iter():它會一直等,直到所有 Sender 都被 drop 才結束——每條執行緒的 tx 在送完後才 drop,主執行緒的 tx 也已經先 drop,所以 iter() 一定會收到全部三則,結果固定是 14。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::sync::{mpsc, Arc, Mutex};", note: "兩種模型的工具都引入。" },
          { code: "use std::thread;", note: "引入 thread 模組。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let (tx, rx) = mpsc::channel();", note: "訊息傳遞版:結果沿著 channel 流回主執行緒。" },
          { code: "    for id in 1..=3 {", note: "開三條生產者執行緒。" },
          { code: "        let tx = tx.clone();", note: "每條執行緒自己的 Sender。" },
          { code: "        thread::spawn(move || tx.send(id * id).unwrap());", note: "算出平方後送出,送完這條執行緒的 tx 就被 drop。沒有任何共享狀態,也沒有鎖。" },
          { code: "    }", note: "三條都開出去了。" },
          { code: "    drop(tx);", note: "主執行緒放掉自己那個 Sender,否則下一行永遠不會結束。" },
          { code: "    let by_message: i32 = rx.iter().sum();", note: "一直收到所有 Sender 都消失為止:1 + 4 + 9 = 14。這裡不需要 join,channel 本身就是同步點。" },
          { code: "", note: "" },
          { code: "    let total = Arc::new(Mutex::new(0));", note: "共享狀態版:大家往同一個格子裡累加。" },
          { code: "    let mut handles = Vec::new();", note: "這個版本需要 join 才知道大家做完了沒。" },
          { code: "    for id in 1..=3 {", note: "開三條執行緒。" },
          { code: "        let total = Arc::clone(&total);", note: "每條執行緒一個把手。" },
          { code: "        handles.push(thread::spawn(move || *total.lock().unwrap() += id * id));", note: "上鎖、累加、解鎖。" },
          { code: "    }", note: "三條都開出去了。" },
          { code: "    for h in handles {", note: "逐一等待。" },
          { code: "        h.join().unwrap();", note: "join 完才能確定三次累加都已完成。" },
          { code: "    }", note: "全部完成。" },
          { code: "    println!(\"{} {}\", by_message, *total.lock().unwrap());", note: "兩種方式結果相同,印出 14 14。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "兩個版本的同步點不同:channel 版的同步點是「所有 Sender 消失」,共享狀態版的同步點是 join。選擇時問自己:資料是在「流動」(用 channel),還是「待在原地被很多人讀寫」(用 Mutex)?",
      },
      csharp: `C# 裡兩種模型同樣並存,而且常常混用:Channel<T>、BlockingCollection<T>、TPL Dataflow 屬於訊息傳遞,lock、ConcurrentDictionary、Interlocked 屬於共享狀態。Rust 的慣例和 C# 其實很像——依情境挑選。真正的差別在安全網:C# 裡兩種模型都可能被誤用(把可變物件放進 channel 後,送出端繼續修改它;或在 lock 外面偷偷存取共享欄位),Rust 裡送出去的值所有權已經轉移、Mutex 外面碰不到資料,兩種誤用都寫不出來。`,
    },
    {
      id: "2-7-20",
      question: "本課 C# 對照總結:關於 C# 與 Rust 在多執行緒安全上的差異,哪一組描述最準確?",
      options: [
        { text: "C# 的 Thread/Task、lock、ConcurrentDictionary 都是執行期工具,忘了加鎖編譯器不會說話,資料競爭要等它發生才發現;Rust 用 Send/Sync 讓「這個值能不能交給別的執行緒、能不能被多條執行緒同時存取」變成編譯期的型別檢查,資料競爭在編譯期就被擋下——但死鎖不在這層保證裡,兩邊都得靠固定上鎖順序這類紀律來避免" },
        { text: "Rust 的所有權系統保證多執行緒程式不會有任何並行 bug,包括死鎖與所有的競態條件,這是 C# 做不到的" },
        { text: "兩者本質相同:Rust 的 Mutex 就是 C# 的 lock,Send/Sync 只是寫給人看的文件標記,不影響編譯結果" },
        { text: "差別主要在語法:C# 的 lock 可重入、Rust 的 Mutex 也可重入,Rust 只是多了離開作用域自動解鎖的功能" },
      ],
      answer: 0,
      explanation: `一句話收束本課:Rust 沒有發明新的並行工具——thread、channel、Mutex 在 C# 都有對應——它做的是把「這樣用安全嗎」的判斷,從執行期搬到編譯期。Send 決定一個值能不能被交給另一條執行緒(第 10、11 題),Sync 決定一個值能不能被多條執行緒同時存取(第 12、13 題),兩者都由編譯器依型別組成自動推導,一路穿透 struct、閉包、Arc,在 thread::spawn 這個關口統一檢查。
「沒有任何並行 bug」是最常見的過度推論。Rust 保證的是沒有資料競爭(data race);死鎖(第 15、16 題)在型別上完全合法,邏輯層面的競態條件(例如先檢查、再動作的兩個步驟之間被別人插隊)也一樣存在。「Send/Sync 只是文件標記」則剛好說反了——它們是編譯器強制的約束,本課一半的反面案例都是被它們擋下的。「Mutex 可重入」是第 16 題直接否定的差異:C# 的 lock 可重入,std::sync::Mutex 不行。`,
      walkthrough: [
        {
          label: "◇ C# equivalent: the race the compiler never mentions",
          lang: "csharp",
          lines: [
            { code: "int count = 0;", note: "一個會被多個 Task 共同修改的區域變數(被 lambda 捕獲後提升到堆積上的物件)。" },
            { code: "var tasks = new Task[4];", note: "準備 4 個 Task。" },
            { code: "for (int t = 0; t < 4; t++)", note: "開 4 個工作。" },
            { code: "    tasks[t] = Task.Run(() => { for (int i = 0; i < 250; i++) count++; });", note: "每個 Task 加 250 次。count++ 是「讀、加、寫」三步,沒有鎖,兩個 Task 可能讀到同一個舊值——編譯器完全不會提醒。" },
            { code: "Task.WaitAll(tasks);", note: "等全部完成,相當於 Rust 的逐一 join。" },
            { code: "Console.WriteLine(count);", note: "預期 1000,實際可能少一些,而且每次不一樣;迴圈次數小的時候甚至常常剛好是 1000,讓 bug 更難重現。" },
          ],
          outro: "C# 的修法是 lock 或 Interlocked.Increment(ref count)——但「要記得用」這件事,只有人知道,編譯器不知道。",
        },
        {
          label: "√ Rust: the same job, where the unlocked version cannot be written",
          lines: [
            { code: "use std::sync::{Arc, Mutex};", note: "共同擁有 + 互斥。" },
            { code: "use std::thread;", note: "引入 thread 模組。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let count = Arc::new(Mutex::new(0));", note: "想讓多條執行緒修改同一個整數,只能這樣包:少了 Arc 過不了 'static(第 3 題),少了 Mutex 拿不到 &mut(第 9 題)。" },
            { code: "    let mut handles = Vec::new();", note: "收集 JoinHandle。" },
            { code: "    for _ in 0..4 {", note: "開 4 條執行緒,對應 C# 的 4 個 Task。" },
            { code: "        let count = Arc::clone(&count);", note: "每條執行緒一個把手。" },
            { code: "        handles.push(thread::spawn(move || {", note: "spawn 在這裡檢查 Send:Arc<Mutex<i32>> 通過;換成 Rc 或 RefCell 就會被擋下(第 10、13 題)。" },
            { code: "            for _ in 0..250 {", note: "每條執行緒加 250 次。" },
            { code: "                *count.lock().unwrap() += 1;", note: "不經過 lock() 就碰不到那個整數,所以「忘了加鎖」在這裡是寫不出來的程式。" },
            { code: "            }", note: "迴圈結束。" },
            { code: "        }));", note: "閉包結束,這條執行緒的 Arc 被 drop。" },
            { code: "    }", note: "4 條都開出去了。" },
            { code: "    for h in handles {", note: "逐一等待。" },
            { code: "        h.join().unwrap();", note: "對應 Task.WaitAll。" },
            { code: "    }", note: "全部完成。" },
            { code: "    println!(\"{}\", *count.lock().unwrap());", note: "每次都印出 1000。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "帶著走的對照:C# 的並行安全靠紀律與執行期工具,Rust 的資料競爭防護靠型別與編譯器;但鎖的順序、鎖的範圍、會不會自己鎖自己,兩邊都一樣只能靠設計。",
        },
      ],
      csharp: `給 C# 開發者的三個帶著走的結論。一、看到 thread::spawn 的閉包,預設就寫 move;要借用外部資料就用 thread::scope。二、看到 Send / Sync 的錯誤訊息,別急著找語法錯:順著「within this closure」「required for Arc<...> to implement Send」往內找,一定有一個欄位是 Rc、RefCell 或 Cell,把它換成 Arc、Mutex 或 atomic 型別。三、Rust 替你擋下的是資料競爭,不是死鎖:C# 那套「固定上鎖順序、縮小鎖的範圍」的紀律照樣要帶過來,而且還要多記一條——std::sync::Mutex 不可重入。`,
    },
  ],
};
