/* 出題慣例見專案根目錄 AUTHORING.md:
 *   - answer 一律為 0(正確答案寫在第一個選項),顯示順序由 quiz.js 依題目 id 洗牌
 *   - 詳解禁止用「選項 A/B/C」字母指涉,必須直接描述選項內容
 *   - 有程式碼的題目一律附 walkthrough(逐行說明);反面案例必須同時附上 √ 正確寫法
 * 跨課邊界(見 ADVANCED_OUTLINE.md):
 *   - From / TryFrom 只談一般值轉換,不使用 ? 運算子或錯誤型別轉換(那是 lesson2-6)
 *   - Deref 只談「函式參數位置」的 coercion;方法呼叫穿透指標、DerefMut 屬於 lesson2-3
 *   - &String → &str 單層 coercion 與反方向失敗已在 lesson1-6 出過,本課改考多層與 slice */
window.RUST_LESSONS = window.RUST_LESSONS || {};
window.RUST_LESSONS["lesson2-9"] = {
  id: "lesson2-9",
  title: "型別轉換與轉換 Trait",
  goal: "分清楚 as、From / Into、TryFrom / TryInto、AsRef / Borrow、Deref coercion、FromStr / parse 各自解決哪一種轉換問題,遇到需求時知道該選哪一個,也看得懂選錯時編譯器在抱怨什麼。",
  primer: {
    intro: "Rust 幾乎不做隱式的型別轉換:i32 不會自己變成 i64,整數也不會自己變成浮點數,要轉就得明說。明說的方式不只一種——as 是語言內建的數值轉型,From 這類 trait 則是標準庫定義的轉換介面,任何型別都能實作。本課要釐清的,就是這些轉換工具各自承諾了什麼、什麼情境該用哪一個。",
    examples: [
      {
        code: "fn main() {\n    let small: i32 = 42;\n    let wide = i64::from(small);\n    let float = small as f64;\n    println!(\"{} {}\", wide, float);\n}",
        note: "i64::from 做一定不會出錯的放大轉換,as 把整數轉成浮點數;兩者都印出 42。",
      },
    ],
    csharp: "C# 的 long x = someInt; 會自動放大,Rust 連這種絕對安全的轉換都要你寫出來;好處是程式碼裡每一個轉換點都看得見,不會有「編譯器偷偷幫你轉了」的意外。",
  },
  questions: [
    {
      id: "2-9-01",
      question: "as 是數值之間的顯式轉型。以下程式碼的結果是?",
      questionCode: "fn main() {\n    let a: i32 = -1;\n    let b = 3.99_f64 as i32;\n    let c = a as i64;\n    let d = b as f64 / 2.0;\n    println!(\"{} {} {}\", b, c, d);\n}",
      options: [
        { text: "印出 3 -1 1.5——浮點數轉整數是直接捨去小數(往 0 取整),i32 放大成 i64 保留原值,轉回 f64 後做的是浮點除法" },
        { text: "印出 4 -1 2——浮點數轉整數會四捨五入,而 d 那一行是整數除法" },
        { text: "編譯錯誤:3.99 轉成 i32 會遺失資訊,as 要求編譯器先證明值在目標型別的範圍內才能轉" },
        { text: "印出 3 4294967295 1.5——負數經過 as 轉型一律被當成無號數重新解讀" },
      ],
      answer: 0,
      explanation: `as 是語言內建的數值轉型,規則是固定的、而且永遠能編譯:浮點數轉整數時直接丟掉小數部分(往 0 取整,3.99 變 3,-3.99 會變 -3);有號整數放大到更寬的有號型別時做符號延伸,-1 還是 -1;整數轉浮點數則得到最接近的浮點值。d 是 3.0 / 2.0,浮點除法得 1.5。
「as 要先證明值在範圍內才能編譯」是把 as 想成了 C# 的 checked 轉型——as 從不做任何檢查,也不會報錯,這正是下一題要看的風險。「四捨五入」則是從 Math.Round 或 Convert.ToInt32 帶來的直覺:Convert.ToInt32(3.99) 在 C# 得到 4,但 (int)3.99 得到 3,Rust 的 as 對應的是後者。至於 4294967295,那是 -1 轉成 u32 才會發生的事,轉成更寬的有號型別 i64 並不會。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let a: i32 = -1;", note: "a 是一個負的 i32。" },
          { code: "    let b = 3.99_f64 as i32;", note: "浮點數轉整數:直接截掉小數部分,往 0 的方向取整,b 是 3。不會四捨五入,也不會報錯。" },
          { code: "    let c = a as i64;", note: "有號整數放大成更寬的有號整數:做符號延伸,值不變,c 是 -1。" },
          { code: "    let d = b as f64 / 2.0;", note: "as 的優先順序比 / 高,先把 b 轉成 3.0,再做浮點除法,d 是 1.5。" },
          { code: "    println!(\"{} {} {}\", b, c, d);", note: "印出 3 -1 1.5。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "as 只能用在基本數值型別(以及指標、enum 判別值等少數情況)之間,而且它的規則是「一定轉得過去,代價自己承擔」。放大轉換沒有代價;縮小轉換的代價是什麼,下一題見分曉。",
      },
      csharp: `as 的寫法對應 C# 的 (int)3.99 這種強制轉型,行為也一樣是捨去小數。要注意 C# 的 as 關鍵字是完全不同的東西(參考型別的安全轉型,失敗給 null),千萬不要把兩個 as 聯想在一起。另外 C# 的 int 到 long 可以隱式轉換、不用寫任何東西,Rust 則連這種零風險的放大都要寫 as 或 i64::from。`,
    },
    {
      id: "2-9-02",
      question: "想把一個計數值存成一個位元組。以下程式碼在 debug 模式下執行,結果是?",
      questionCode: "fn main() {\n    let total: i32 = 300;\n    let byte = total as u8;\n    println!(\"{}\", byte);\n}",
      options: [
        { text: "印出 44,沒有任何錯誤或警告——as 從大整數轉成小整數時只保留低位元,300 對 256 取餘數就是 44" },
        { text: "執行期 panic:debug 模式會檢查溢位,300 超出 u8 的範圍" },
        { text: "印出 255——超出範圍時 as 會把值夾在目標型別的最大值" },
        { text: "編譯錯誤:total 是 i32,不能用 as 轉成範圍更小的 u8" },
      ],
      answer: 0,
      explanation: `整數之間的 as 縮小轉換就是「截斷」:只保留目標型別放得下的低位元。300 的二進位是 1_0010_1100,u8 只留下低 8 位 0010_1100,也就是 44。這個行為是語言明文定義的,不是未定義行為,也不會因為 debug 或 release 而改變,所以編譯器不警告、執行期也不 panic——這才是它危險的地方:錯的值會安安靜靜地流進後面的程式。
「debug 模式會 panic」是很合理的聯想,因為 debug 模式下 255_u8 + 1 這種算術溢位確實會 panic;但那個檢查只套用在 + - * 這類算術運算上,as 轉型刻意被排除在外。「夾在 255」是另一種規則:那是浮點數轉整數時 as 的行為(例如 300.0_f64 as u8 會得到 255),整數之間不是這樣。`,
      walkthrough: [
        {
          label: "X Question code (compiles, but produces the wrong value)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let total: i32 = 300;", note: "300 超出 u8 能表示的 0~255。" },
            { code: "    let byte = total as u8;", note: "⛔ 行為錯誤:as 只保留低 8 位元,byte 靜默變成 44。沒有編譯錯誤、沒有警告、沒有 panic,debug 模式的溢位檢查也不管 as。" },
            { code: "    println!(\"{}\", byte);", note: "印出 44——一個跟 300 毫無關係的數字。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "如果 total 來自使用者輸入或檔案,這個 44 就會被當成合法資料繼續往下傳,出事的地方往往離這一行很遠。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let total: i32 = 300;", note: "一樣是超出範圍的值。" },
            { code: "    match u8::try_from(total) {", note: "改動處:改用 u8::try_from,它會檢查範圍,回傳 Result 讓你自己決定超出範圍時怎麼辦(第 6 題會細看)。" },
            { code: "        Ok(byte) => println!(\"{}\", byte),", note: "值在 0~255 內才會走這裡,拿到的一定是原值。" },
            { code: "        Err(_) => println!(\"{} 超出 u8 範圍\", total),", note: "300 走這裡,印出 300 超出 u8 範圍,問題在轉換點就被攔下。" },
            { code: "    }", note: "match 結束。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "as 不是不能用,而是只該用在「你確定值放得下」或「截斷正是你要的結果」(例如取雜湊值的低位元)的地方。",
        },
      ],
      csharp: `C# 在這點上跟 Rust 一模一樣:預設是 unchecked 上下文,(byte)300 靜默得到 44;要包在 checked(...) 裡,或開啟專案的溢位檢查,才會丟 OverflowException。差別在 Rust 沒有「切換上下文」這個開關——as 永遠截斷,要檢查就換一個轉換工具(TryFrom),選擇寫在每一個轉換點上,而不是藏在專案設定裡。`,
    },
    {
      id: "2-9-03",
      question: "只實作了 From。以下程式碼的結果是?",
      questionCode: "struct Celsius(f64);\nstruct Fahrenheit(f64);\n\nimpl From<Celsius> for Fahrenheit {\n    fn from(c: Celsius) -> Self {\n        Fahrenheit(c.0 * 9.0 / 5.0 + 32.0)\n    }\n}\n\nfn main() {\n    let a = Fahrenheit::from(Celsius(100.0));\n    let b: Fahrenheit = Celsius(0.0).into();\n    println!(\"{} {}\", a.0, b.0);\n}",
      options: [
        { text: "印出 212 32——實作 From<Celsius> for Fahrenheit 之後,標準庫的 blanket impl 會自動讓 Celsius 獲得 into() 轉成 Fahrenheit 的能力" },
        { text: "編譯錯誤:into() 找不到——Into 是另一個 trait,沒有另外寫 impl Into<Fahrenheit> for Celsius 就不能用" },
        { text: "編譯錯誤:into() 必須寫成 into::<Fahrenheit>() 指定目標型別,光靠變數標註不夠" },
        { text: "印出 212 32,但同時也能寫 Fahrenheit(0.0).into() 轉回 Celsius——From 實作一次,兩個方向都通" },
      ],
      answer: 0,
      explanation: `標準庫裡有一條 impl<T, U> Into<U> for T where U: From<T>——只要「U 能從 T 產生」,T 就自動「能轉成 U」。所以你只寫 From,就同時拿到了 Fahrenheit::from(c) 與 c.into() 兩種寫法,兩者做的是同一件事。
「Into 要另外實作」是最常見的誤解,原因是它們看起來是兩個獨立的 trait;實際上 Into 幾乎從來不需要手寫。「要寫 into::<Fahrenheit>()」也不對:目標型別是 Into trait 的參數,不是 into 這個方法的泛型參數,所以 turbofish 放不上去,要靠變數標註或上下文推斷(第 5 題)。「兩個方向都通」則是想太多了:From<Celsius> for Fahrenheit 只描述了一個方向,反向要自己再寫一個 From<Fahrenheit> for Celsius。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "struct Celsius(f64);", note: "攝氏溫度,tuple struct 包一個 f64。" },
          { code: "struct Fahrenheit(f64);", note: "華氏溫度,同樣的形狀但是不同的型別,不能混用。" },
          { code: "", note: "" },
          { code: "impl From<Celsius> for Fahrenheit {", note: "宣告「Fahrenheit 可以從 Celsius 產生」。這是唯一一個手寫的轉換實作。" },
          { code: "    fn from(c: Celsius) -> Self {", note: "按值收下 Celsius,產生一個新的 Fahrenheit。From 的轉換不能失敗,所以回傳的就是 Self,不是 Result。" },
          { code: "        Fahrenheit(c.0 * 9.0 / 5.0 + 32.0)", note: "換算公式。" },
          { code: "    }", note: "from 結束,c 在這裡被消耗。" },
          { code: "}", note: "impl 結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let a = Fahrenheit::from(Celsius(100.0));", note: "直接呼叫剛寫的 from,a 是 212。" },
          { code: "    let b: Fahrenheit = Celsius(0.0).into();", note: "這個 into 不是你寫的,來自標準庫的 blanket impl:因為 Fahrenheit: From<Celsius> 成立,所以 Celsius: Into<Fahrenheit> 自動成立。目標型別由 b 的標註決定,b 是 32。" },
          { code: "    println!(\"{} {}\", a.0, b.0);", note: "印出 212 32(f64 的 212.0 以 {} 顯示時不帶小數點)。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "記住方向:From 寫在「目標型別」上(for Fahrenheit),into 叫在「來源值」上(Celsius(0.0).into())。兩者是同一條轉換的兩個入口。",
      },
      csharp: `最接近的 C# 寫法是在 Fahrenheit 上定義 public static implicit operator Fahrenheit(Celsius c)。差別有兩個:C# 的 implicit operator 會在賦值時自動生效,你看不到轉換點;Rust 的 From 永遠要你寫出 from 或 into 才會發生。另外 C# 的轉換運算子只能定義在來源或目標型別裡,Rust 的 From 則是一個一般的 trait,可以被泛型 bound 拿去用——這一點下一題會用到。`,
    },
    {
      id: "2-9-04",
      question: "要讓自訂型別 Meters 能從 f64 轉過來,並寫一個「接受任何能轉成 Meters 的值」的函式。哪一組做法最符合 Rust 慣例?",
      options: [
        { text: "實作 impl From<f64> for Meters;函式參數寫成 impl Into<Meters>(或等價的 T: Into<Meters> bound),函式裡呼叫 .into()——定義端寫 From,使用端要 Into" },
        { text: "實作 impl Into<Meters> for f64,因為呼叫端寫的是 value.into(),所以應該把能力實作在來源型別上" },
        { text: "From 與 Into 各實作一次,少寫任何一個,另一種寫法就不能用" },
        { text: "實作 impl From<f64> for Meters;函式參數寫成 impl From<f64>,這樣就能接受 f64" },
      ],
      answer: 0,
      explanation: `慣例是一條分工:實作時寫 From,因為寫 From 會透過 blanket impl 自動送你 Into;要求時寫 Into,因為 T: Into<Meters> 比 Meters: From<T> 讀起來更直觀,而且涵蓋的型別至少一樣多。標準庫的文件也明說「優先實作 From,不要直接實作 Into」。
「把 Into 實作在 f64 上」在今天的孤兒規則下其實能編譯,但你只得到 Into 那一半——Meters::from(2.5) 就不能用了,而其他以 From 為 bound 的泛型程式碼也看不到它。「兩個都實作」會直接撞上 blanket impl,報 conflicting implementations。最後那個 impl From<f64> 參數則是方向寫反了:它描述的是「能從 f64 產生出來的型別」,f64 本身並不滿足,Meters 也不是,呼叫端根本傳不進東西。`,
      walkthrough: {
        label: "⊕ Idiomatic version — walkthrough (runs successfully)",
        lines: [
          { code: "struct Meters(f64);", note: "要被轉換的目標型別。" },
          { code: "", note: "" },
          { code: "impl From<f64> for Meters {", note: "定義端:寫 From。這一個 impl 同時讓 f64 獲得 Into<Meters>。" },
          { code: "    fn from(v: f64) -> Self {", note: "按值收下 f64。" },
          { code: "        Meters(v)", note: "包起來就好。" },
          { code: "    }", note: "from 結束。" },
          { code: "}", note: "impl 結束。" },
          { code: "", note: "" },
          { code: "fn describe(len: impl Into<Meters>) {", note: "使用端:要 Into。任何「能轉成 Meters」的型別都收,呼叫端不必先自己轉。" },
          { code: "    let m: Meters = len.into();", note: "在函式裡統一轉成 Meters,之後只處理一種型別。" },
          { code: "    println!(\"{} m\", m.0);", note: "印出公尺數。" },
          { code: "}", note: "describe 結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    describe(2.5);", note: "傳 f64:透過 From<f64> 自動得到的 Into,印出 2.5 m。" },
          { code: "    describe(Meters(4.0));", note: "直接傳 Meters 也行:標準庫有 impl<T> From<T> for T,任何型別都能「轉成自己」。印出 4 m。" },
          { code: "    let m = Meters::from(1.0);", note: "寫 From 的另一個好處:目標型別寫在最前面的 Meters::from 讀起來最清楚。" },
          { code: "    println!(\"{} m\", m.0);", note: "印出 1 m。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "Into 很少出現在 impl 區塊,卻常出現在函式簽名的 bound 裡;From 則剛好相反。這是看別人的 API 時最快判斷「轉換在哪裡定義」的線索。",
      },
      csharp: `C# 沒有能拿來當泛型限制的「可轉換成 T」介面——你不能寫 void Describe<T>(T len) where T : ConvertibleTo<Meters>。常見的替代方案是寫多個多載(Describe(double)、Describe(Meters)),或在呼叫端先自己轉。Rust 把轉換做成 trait,「接受任何能轉成 Meters 的東西」就只是一個 bound,新型別只要補一個 From 實作就自動能傳進來,不用改函式。`,
    },
    {
      id: "2-9-05",
      question: "Meters 只有一個 From 實作。以下程式碼的結果是?",
      questionCode: "struct Meters(f64);\n\nimpl From<f64> for Meters {\n    fn from(v: f64) -> Self {\n        Meters(v)\n    }\n}\n\nfn main() {\n    let m = 3.5.into();\n    println!(\"{}\", m.0);\n}",
      options: [
        { text: "編譯錯誤:type annotations needed——into() 本身不指定目標型別,m 又沒有標註,編譯器沒有依據決定要轉成什麼" },
        { text: "印出 3.5——整個程式只有一個 From<f64> 的實作,編譯器會自動選它" },
        { text: "印出 3.5——m.0 這個欄位存取讓編譯器看出 m 是 tuple struct,足以推斷成 Meters" },
        { text: "編譯錯誤:3.5 是浮點數字面值,不能直接在字面值上呼叫方法" },
      ],
      answer: 0,
      explanation: `into() 的目標型別完全靠上下文決定:可能是變數標註、函式參數型別或回傳型別。這裡 m 沒標註,into 的結果也沒有被傳進任何有型別的地方,編譯器就停在 E0282 type annotations needed,並建議你幫 m 加上型別。
「只有一個實作所以會自動選」是最自然的誤解,但前提就錯了:f64 能 into 的目標不只 Meters——標準庫的 impl<T> From<T> for T 讓 f64 至少也能 into 成 f64 自己,而且 Rust 刻意不根據「目前碰巧只有幾個候選」來推斷,否則任何一個相依套件多加一個 impl,你的程式就會突然編譯不過。「靠 m.0 推斷」也行不通:型別推斷需要在存取欄位之前就知道 m 是什麼型別,.0 本身不能反推出是哪一個 struct。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "struct Meters(f64);", note: "目標型別。" },
            { code: "", note: "" },
            { code: "impl From<f64> for Meters {", note: "唯一一個手寫的轉換實作。" },
            { code: "    fn from(v: f64) -> Self {", note: "按值收下 f64。" },
            { code: "        Meters(v)", note: "包起來。" },
            { code: "    }", note: "from 結束。" },
            { code: "}", note: "impl 結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let m = 3.5.into();", note: "⛔ 編譯失敗:type annotations needed(E0282)。into 的目標型別要由呼叫端提供,這一行沒有任何線索,help 建議改成 let m: /* Type */ = 3.5.into();。" },
            { code: "    println!(\"{}\", m.0);", note: "編譯器在這裡標出 type must be known at this point——要存取欄位,必須先知道 m 是哪個型別。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "struct Meters(f64);", note: "不用改。" },
            { code: "", note: "" },
            { code: "impl From<f64> for Meters {", note: "不用改。" },
            { code: "    fn from(v: f64) -> Self {", note: "不用改。" },
            { code: "        Meters(v)", note: "不用改。" },
            { code: "    }", note: "from 結束。" },
            { code: "}", note: "impl 結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let m: Meters = 3.5.into();", note: "改動處:幫 m 標上 Meters,into 的目標型別就確定了。也可以改寫成 let m = Meters::from(3.5);,把目標型別寫在最前面。" },
            { code: "    println!(\"{}\", m.0);", note: "印出 3.5。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "經驗法則:目標型別在上下文裡本來就看得到(函式參數、回傳值、已標註的變數)時用 into;看不到時與其補標註,不如直接寫 Meters::from,讀的人也不用猜。",
        },
      ],
      csharp: `C# 的 implicit operator 不會有這個問題,因為 var m = 3.5; 直接就是 double,根本不會觸發轉換——轉換只在「賦值給另一個已知型別」時發生。Rust 的 into 是一個明確的方法呼叫,但它的回傳型別是開放的,所以反過來需要呼叫端提供目標型別。同樣的名字服務所有目標型別,代價就是推斷需要線索。`,
    },
    {
      id: "2-9-06",
      question: "TryFrom 是可能失敗的轉換。以下程式碼的輸出是?",
      questionCode: "fn main() {\n    let values = [100_i32, 300, -1];\n    for v in values {\n        match u8::try_from(v) {\n            Ok(b) => println!(\"{} -> ok {}\", v, b),\n            Err(e) => println!(\"{} -> err: {}\", v, e),\n        }\n    }\n}",
      options: [
        { text: "100 -> ok 100、300 -> err、-1 -> err 三行——值在 u8 範圍內給 Ok,超出上限或是負數都給 Err,不會產生任何替代值" },
        { text: "100 -> ok 100、300 -> ok 255、-1 -> ok 0 三行——TryFrom 超出範圍時會把值夾在邊界上" },
        { text: "100 -> ok 100、300 -> ok 44、-1 -> ok 255 三行——TryFrom 底層就是 as,只是包了一層 Ok" },
        { text: "編譯錯誤:要使用 try_from 必須先寫 use std::convert::TryFrom;" },
      ],
      answer: 0,
      explanation: `TryFrom 用在「有些值轉得過去、有些轉不過去」的情境,回傳 Result<目標型別, 錯誤型別>。整數之間的錯誤型別是 TryFromIntError,它的 Display 就是 out of range integral type conversion attempted。300 超過 255、-1 小於 0,兩個都給 Err;TryFrom 只負責告訴你失敗了,不會自作主張給一個「差不多的值」。
「夾在邊界上」是 saturating 系列方法(或浮點數的 as 轉型)的行為;「底層就是 as」則是把 TryFrom 當成 as 的包裝——那樣的話它就失去存在的意義了。至於 use std::convert::TryFrom:這是 2018 版以前的程式碼常見的一行,2021 版起 TryFrom / TryInto 已經在 prelude 裡,不用再 use。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let values = [100_i32, 300, -1];", note: "三個 i32:一個在 u8 範圍內,一個超過上限,一個低於下限。" },
          { code: "    for v in values {", note: "逐一嘗試轉換。" },
          { code: "        match u8::try_from(v) {", note: "try_from 回傳 Result<u8, TryFromIntError>,成功與失敗都要處理。2021 版不需要 use 任何東西。" },
          { code: "            Ok(b) => println!(\"{} -> ok {}\", v, b),", note: "100 走這裡,印出 100 -> ok 100。" },
          { code: "            Err(e) => println!(\"{} -> err: {}\", v, e),", note: "300 與 -1 走這裡,印出 300 -> err: out of range integral type conversion attempted,以及 -1 的同樣訊息。" },
          { code: "        }", note: "match 結束。" },
          { code: "    }", note: "迴圈結束。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "也可以從來源那一端呼叫:let b: Result<u8, _> = v.try_into();——跟 From / Into 一樣,實作 TryFrom 就會自動得到對應的 TryInto。",
      },
      csharp: `對應到 C# 的 checked((byte)v) 或 Convert.ToByte(v),兩者超出範圍都丟 OverflowException。Rust 把「失敗」放進回傳型別,而不是丟例外:u8::try_from 的簽名本身就告訴你這個轉換可能失敗,呼叫端不處理 Err 就拿不到 u8,不存在「忘了包 try/catch」這回事。`,
    },
    {
      id: "2-9-07",
      question: "感測器讀數要轉成 i32 存檔。以下程式碼的執行結果是?",
      questionCode: "fn main() {\n    let readings: Vec<i64> = vec![20, 35, 5_000_000_000];\n    for r in readings {\n        let v = i32::try_from(r).unwrap();\n        println!(\"{}\", v);\n    }\n}",
      options: [
        { text: "印出 20 與 35,接著在第三個值 panic:called Result::unwrap() on an Err value——try_from 有回報失敗,但 unwrap 把 Err 直接變成當機" },
        { text: "印出 20、35,第三個值被跳過——try_from 失敗時迴圈會自動略過這一筆" },
        { text: "印出 20、35 與一個截斷後的值——unwrap 在 Err 時會退回 as 的結果" },
        { text: "編譯錯誤:5_000_000_000 超出 i32 範圍,編譯器在編譯期就能看出 try_from 一定會失敗" },
      ],
      answer: 0,
      explanation: `TryFrom 把「可能失敗」誠實地放進了 Result,但處理 Result 仍然是你的責任。unwrap 的意思是「我保證這裡一定是 Ok,不是就讓程式當掉」——50 億超出 i32 上限,try_from 給 Err,unwrap 就 panic,程式在印出前兩筆之後中止。
「自動略過」與「退回 as 的結果」都是期待語言幫你做決定:但 Rust 不知道你想要略過、要夾住還是要中止,只有你寫出 match 或 unwrap_or 之類的處理時它才照做。「編譯期就看得出來」也不對:值存在 Vec 裡,編譯器不會去追蹤 Vec 的內容,try_from 的結果只能在執行期得知。`,
      walkthrough: [
        {
          label: "X Question code (compiles, but panics at runtime)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let readings: Vec<i64> = vec![20, 35, 5_000_000_000];", note: "第三筆讀數超出 i32 的上限(約 21 億)。" },
            { code: "    for r in readings {", note: "逐筆處理。" },
            { code: "        let v = i32::try_from(r).unwrap();", note: "⛔ 執行期 panic:called Result::unwrap() on an Err value: TryFromIntError(...)。try_from 正確地回報了失敗,是 unwrap 把失敗變成當機。" },
            { code: "        println!(\"{}\", v);", note: "前兩筆印出 20 與 35,第三筆走不到這裡。" },
            { code: "    }", note: "迴圈結束(實際上在第三圈中止)。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let readings: Vec<i64> = vec![20, 35, 5_000_000_000];", note: "資料不變。" },
            { code: "    for r in readings {", note: "逐筆處理。" },
            { code: "        match i32::try_from(r) {", note: "改動處:用 match 明確處理兩種結果,而不是假設一定成功。" },
            { code: "            Ok(v) => println!(\"{}\", v),", note: "20 與 35 走這裡,正常印出。" },
            { code: "            Err(_) => println!(\"skip {}\", r),", note: "改動處:超出範圍時由你決定策略——這裡選擇記錄並略過,印出 skip 5000000000。" },
            { code: "        }", note: "match 結束。" },
            { code: "    }", note: "迴圈正常跑完三圈。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "unwrap 適合「失敗代表程式有 bug」的地方;資料來自外部時,失敗是正常情況,要寫出處理方式。",
        },
      ],
      csharp: `跟 C# 的 Convert.ToInt32(long) 超出範圍丟 OverflowException、沒包 try/catch 就讓程式崩潰是同一件事。不同的是 C# 的方法簽名看不出會不會丟例外,Rust 的 try_from 回傳 Result,unwrap 這個字就是「我選擇在失敗時當機」的明文記錄,code review 時一眼就看得到。`,
    },
    {
      id: "2-9-08",
      question: "以下三個轉換:(1) u8 等級值放大成 u32;(2) Vec::len() 回傳的 usize 要寫進 u32 的檔頭欄位;(3) f64 座標轉成 i32 像素位置,小數捨去沒關係。哪一組選擇最恰當?",
      options: [
        { text: "(1) 用 u32::from(level)(用 as 也安全);(2) 用 u32::try_from(len) 並處理失敗;(3) 用 x as i32——標準庫沒有 f64 到 i32 的 TryFrom,而捨去小數正是這裡要的行為" },
        { text: "三個都用 try_from——as 是舊時代留下的寫法,正式的 Rust 程式碼不該再用" },
        { text: "三個都用 as——as 最快,而且 debug 模式下超出範圍會 panic,測試時就抓得到問題" },
        { text: "(1) 用 from、(2) 用 try_from、(3) 用 i32::try_from(x),讓浮點數超出範圍時也能拿到 Err" },
      ],
      answer: 0,
      explanation: `選擇的依據是「這個轉換可能出什麼事」。u8 到 u32 永遠不會失敗,標準庫提供了 From,寫 from 讓讀者一看就知道這是無損轉換;usize 在 64 位元平台上可能超過 u32,而檔頭欄位被截斷會產生壞檔,所以要 try_from 並處理失敗;浮點數轉像素本來就要丟掉小數,as 的行為正好符合需求(而且浮點數轉整數的 as 超出範圍時會夾在邊界、NaN 變 0,不會產生未定義行為)。
「as 已被淘汰」是矯枉過正:as 仍然是浮點數與整數之間唯一的內建轉換,也是明確想要截斷時最清楚的寫法。「debug 模式會 panic」則是上一題之前就破過的誤解——as 從不檢查。最後一組想用 i32::try_from(f64),方向是對的,但標準庫沒有這個實作,程式根本編譯不過;要檢查範圍得自己先比較數值,再 as。`,
      walkthrough: {
        label: "⊕ Idiomatic version — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let level: u8 = 200;", note: "情境 1 的來源值。" },
          { code: "    let names = vec![\"a\", \"b\", \"c\"];", note: "情境 2 的來源,長度由 len() 給出,型別是 usize。" },
          { code: "    let x: f64 = 12.7;", note: "情境 3 的來源值。" },
          { code: "", note: "" },
          { code: "    let wide = u32::from(level);", note: "無損放大:標準庫有 From<u8> for u32,寫 from 等於在程式碼裡宣告「這裡不可能出錯」。" },
          { code: "    let count = match u32::try_from(names.len()) {", note: "usize 可能比 u32 寬,轉換可能失敗,用 try_from 把失敗攤開。" },
          { code: "        Ok(n) => n,", note: "正常情況拿到長度 3。" },
          { code: "        Err(_) => u32::MAX,", note: "失敗時的策略由你決定;真實的檔案格式可能選擇回報錯誤,這裡為了簡短用上限值代替。" },
          { code: "    };", note: "count 是 3。" },
          { code: "    let pixel = x as i32;", note: "要的就是捨去小數,as 直接表達意圖,pixel 是 12。" },
          { code: "    println!(\"{} {} {}\", wide, count, pixel);", note: "印出 200 3 12。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "三種工具對應三種語氣:from 說「一定成功」,try_from 說「可能失敗,我會處理」,as 說「我知道會截斷,我接受」。挑工具就是挑語氣。",
      },
      csharp: `C# 的對照是隱式放大轉換(byte 到 uint 直接賦值)、checked 轉型與 unchecked 轉型三種。差別在 C# 的 checked 與 unchecked 常常是整個區塊或整個專案的設定,讀單一行程式碼看不出它屬於哪一種;Rust 的三種寫法字面上就不同,每一個轉換點都自帶語氣。`,
    },
    {
      id: "2-9-09",
      question: "Username 實作了 AsRef<str>。以下程式碼的結果是?",
      questionCode: "struct Username(String);\n\nimpl AsRef<str> for Username {\n    fn as_ref(&self) -> &str {\n        &self.0\n    }\n}\n\nfn greet(name: &str) {\n    println!(\"hi {}\", name);\n}\n\nfn main() {\n    let u = Username(String::from(\"ferris\"));\n    greet(&u);\n}",
      options: [
        { text: "編譯錯誤:mismatched types,expected &str, found &Username——AsRef 只是提供一個 as_ref 方法,編譯器不會在參數位置自動呼叫它" },
        { text: "印出 hi ferris——實作了 AsRef<str> 之後,&Username 在需要 &str 的地方會自動轉換" },
        { text: "編譯錯誤:AsRef 是標準庫 trait,不能為自訂型別實作" },
        { text: "印出 hi ferris,但要把呼叫改成 greet(u),按值傳入才會觸發 AsRef 轉換" },
      ],
      answer: 0,
      explanation: `AsRef<str> 的意思是「這個型別可以便宜地借出一個 &str」,它提供一個 as_ref 方法,僅此而已。編譯器在函式參數位置只會自動套用一種轉換(下一題與第 13 題會看到),AsRef 不在其中,所以 &Username 就是 &Username,跟 &str 對不上。
「實作了就會自動轉」是這題最主要的誤解:AsRef 只有在兩種情況下發揮作用——你親手呼叫 .as_ref(),或者函式本身寫成泛型、以 AsRef<str> 當 bound(第 11 題)。「按值傳入才觸發」同樣不成立,greet 要的是 &str,傳 Username 只會得到另一個 mismatched types。至於孤兒規則:AsRef 是外部 trait,但 Username 是你自己的型別,為它實作完全合法。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "struct Username(String);", note: "包一個 String 的自訂型別。" },
            { code: "", note: "" },
            { code: "impl AsRef<str> for Username {", note: "宣告 Username 能借出 &str。" },
            { code: "    fn as_ref(&self) -> &str {", note: "借用 self,回傳內部字串的 &str。" },
            { code: "        &self.0", note: "&String 在回傳值位置被轉成 &str。" },
            { code: "    }", note: "as_ref 結束。" },
            { code: "}", note: "impl 結束。" },
            { code: "", note: "" },
            { code: "fn greet(name: &str) {", note: "參數型別寫死成 &str。" },
            { code: "    println!(\"hi {}\", name);", note: "印出問候。" },
            { code: "}", note: "greet 結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let u = Username(String::from(\"ferris\"));", note: "建立 Username。" },
            { code: "    greet(&u);", note: "⛔ 編譯失敗:mismatched types(E0308),expected &str, found &Username。編譯器不會替你呼叫 as_ref。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "struct Username(String);", note: "不用改。" },
            { code: "", note: "" },
            { code: "impl AsRef<str> for Username {", note: "不用改。" },
            { code: "    fn as_ref(&self) -> &str {", note: "不用改。" },
            { code: "        &self.0", note: "不用改。" },
            { code: "    }", note: "as_ref 結束。" },
            { code: "}", note: "impl 結束。" },
            { code: "", note: "" },
            { code: "fn greet(name: &str) {", note: "不用改。" },
            { code: "    println!(\"hi {}\", name);", note: "印出問候。" },
            { code: "}", note: "greet 結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let u = Username(String::from(\"ferris\"));", note: "建立 Username。" },
            { code: "    greet(u.as_ref());", note: "改動處:親手呼叫 as_ref,得到 &str 再傳進去。印出 hi ferris。" },
            { code: "}", note: "main 結束,u 在這裡釋放。" },
          ],
          outro: "另一種修法是改 greet 本身,讓它接受任何 AsRef<str>——那是第 11 題的主題。",
        },
      ],
      csharp: `C# 沒有「可以借出某種檢視」這種介面;常見做法是替 Username 寫一個 implicit operator string,讓它在需要 string 的地方自動轉換。Rust 的 AsRef 刻意不做這件事:轉換一定要寫出來(.as_ref()),或者由函式簽名明確宣告「我接受任何能借出 &str 的東西」。`,
    },
    {
      id: "2-9-10",
      question: "函式參數寫成 &Vec<i32>。以下程式碼的結果是?",
      questionCode: "fn total(v: &Vec<i32>) -> i32 {\n    v.iter().sum()\n}\n\nfn main() {\n    let data = vec![1, 2, 3, 4];\n    println!(\"{}\", total(&data));\n    println!(\"{}\", total(&data[1..]));\n}",
      options: [
        { text: "編譯錯誤,錯在第二次呼叫:expected &Vec<i32>, found &[i32]——slice 不是 Vec,自動轉換只能從 &Vec 往 &[i32] 剝,不能反過來" },
        { text: "印出 10 與 9——&data[1..] 是從 data 借出來的,編譯器知道它背後就是一個 Vec" },
        { text: "編譯錯誤,錯在第一次呼叫:&data 是 &Vec<i32>,但 sum 只能用在 slice 上" },
        { text: "印出 10 與 9,但第二次呼叫會先複製出一個新的 Vec 再傳進去" },
      ],
      answer: 0,
      explanation: `&data[1..] 的型別是 &[i32]:一段連續的 i32,只有指標與長度,沒有容量,也不擁有任何東西。&Vec<i32> 則是指向一個完整 Vec 結構的參考。編譯器在參數位置能把 &Vec<i32> 自動轉成 &[i32](因為 Vec<T> 能解參考成 [T]),但沒有任何規則能把一段 slice 變回 Vec——它根本不知道那段記憶體屬於誰、容量多少。
「背後就是一個 Vec,編譯器知道」是從執行期角度想事情,但型別檢查只看型別,&[i32] 不帶任何「來自哪個 Vec」的資訊。「先複製一個新 Vec」則是 C# 的思路(Skip(1).ToList()),Rust 不會在你沒寫的地方偷偷配置記憶體。修法跟基礎課 &String 與 &str 的慣例一致:參數收 &[i32],呼叫端的 &Vec、slice、甚至陣列參考都能傳進來。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn total(v: &Vec<i32>) -> i32 {", note: "參數寫死成 &Vec<i32>,只接受「整個 Vec」的參考。" },
            { code: "    v.iter().sum()", note: "加總——這一行其實只需要一段連續的 i32,用不到 Vec 特有的能力。" },
            { code: "}", note: "total 結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let data = vec![1, 2, 3, 4];", note: "data 擁有四個元素。" },
            { code: "    println!(\"{}\", total(&data));", note: "&data 就是 &Vec<i32>,型別吻合,這一行沒問題。" },
            { code: "    println!(\"{}\", total(&data[1..]));", note: "⛔ 編譯失敗:mismatched types(E0308),expected &Vec<i32>, found &[i32]。slice 不是 Vec,沒有反方向的自動轉換。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn total(v: &[i32]) -> i32 {", note: "改動處:參數改收 &[i32],只要求「一段連續的 i32」。" },
            { code: "    v.iter().sum()", note: "本體完全不用改,slice 本來就有 iter。" },
            { code: "}", note: "total 結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let data = vec![1, 2, 3, 4];", note: "資料不變。" },
            { code: "    println!(\"{}\", total(&data));", note: "&Vec<i32> 在參數位置被自動轉成 &[i32](Vec<T> 能解參考成 [T]),印出 10。" },
            { code: "    println!(\"{}\", total(&data[1..]));", note: "本來就是 &[i32],直接吻合,印出 9。" },
            { code: "    println!(\"{}\", total(&[10, 20]));", note: "連陣列參考也能傳:&[i32; 2] 會轉成 &[i32](這是陣列到 slice 的 unsized coercion),印出 30。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "&String 與 &str、&Vec<T> 與 &[T] 是同一組關係:前者是「擁有者的參考」,後者是「內容的借用檢視」。參數收後者,呼叫端怎麼持有資料都傳得進來。Clippy 對 &Vec<T> 參數有專門的警告(ptr_arg)。",
        },
      ],
      csharp: `C# 的對照是參數收 List<int> 還是收 ReadOnlySpan<int> / IEnumerable<int>。收 List<int> 時,呼叫端手上只有陣列或某一段範圍,就得先 ToList() 複製一份。Rust 的 &[i32] 相當於零成本的 ReadOnlySpan<int>:Vec、陣列、Vec 的一段都能直接借出來,不用複製。`,
    },
    {
      id: "2-9-11",
      question: "把字串參數改成泛型 AsRef<str>。以下程式碼的結果是?",
      questionCode: "fn shout(s: impl AsRef<str>) {\n    let text = s.as_ref();\n    println!(\"{}!\", text.to_uppercase());\n}\n\nfn main() {\n    let owned = String::from(\"rust\");\n    shout(\"hi\");\n    shout(&owned);\n    shout(owned);\n}",
      options: [
        { text: "印出 HI!、RUST!、RUST! 三行——&str、&String、String 都實作了 AsRef<str>,函式裡統一呼叫 as_ref 拿到 &str" },
        { text: "編譯錯誤:impl AsRef<str> 只接受 &str,傳 String 或 &String 都不合法" },
        { text: "編譯錯誤,錯在 shout(owned):上一行已經把 owned 借給 shout,不能再按值傳入" },
        { text: "編譯錯誤:呼叫端必須自己寫 shout(owned.as_ref()),函式參數寫成 AsRef 並不會改變呼叫端的寫法" },
      ],
      answer: 0,
      explanation: `impl AsRef<str> 是泛型參數的簡寫:它接受「任何實作了 AsRef<str> 的型別」。str、String 都實作了 AsRef<str>,而標準庫又對「參考」加了一條 blanket impl(只要 T: AsRef<U>,&T 也是),所以 "hi"、&owned、owned 三種都收。函式內部呼叫一次 as_ref,之後就只處理 &str。
「只接受 &str」是把 AsRef<str> 讀成了型別本身,但它是一個 bound。「上一行借走了 owned」也不對:shout(&owned) 的借用在那次呼叫結束時就結束了,下一行按值傳入完全合法——只是傳入之後 owned 被 move 走,之後不能再用。「呼叫端要自己寫 as_ref」是第 9 題的情境:函式參數寫死 &str 時確實如此,改成泛型就是為了把這件事搬進函式裡。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn shout(s: impl AsRef<str>) {", note: "等價於 fn shout<S: AsRef<str>>(s: S):每一種呼叫端型別各自產生一份版本(單態化),沒有執行期成本。" },
          { code: "    let text = s.as_ref();", note: "統一在函式裡轉成 &str,之後的程式碼只面對一種型別。" },
          { code: "    println!(\"{}!\", text.to_uppercase());", note: "轉大寫後印出。" },
          { code: "}", note: "shout 結束。如果 s 是按值傳進來的 String,它在這裡被釋放。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let owned = String::from(\"rust\");", note: "一個 String。" },
          { code: "    shout(\"hi\");", note: "S 是 &str,印出 HI!。" },
          { code: "    shout(&owned);", note: "S 是 &String:透過「&T 也實作 AsRef」那條 blanket impl 成立,印出 RUST!。借用在呼叫結束時就結束了。" },
          { code: "    shout(owned);", note: "S 是 String,owned 被 move 進 shout,印出 RUST!。這一行之後 owned 不能再用。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "這種寫法常見於標準庫:File::open 的參數是 impl AsRef<Path>,所以 &str、String、PathBuf 都能直接傳。代價是函式變成泛型,會為每種呼叫端型別各產生一份機器碼;參數只是用來讀字串時,直接收 &str 往往就夠了。",
      },
      csharp: `C# 的思路通常是多載:同時提供 Shout(string) 與 Shout(ReadOnlySpan<char>),甚至 Shout(StringBuilder)。Rust 用一個泛型 bound 收下所有「能借出 &str」的型別,新型別只要實作 AsRef<str> 就自動能傳進來,不用再補多載。`,
    },
    {
      id: "2-9-12",
      question: "HashMap<String, i32> 用 &str 查詢。關於 map.get(\"apple\") 為什麼能編譯,以及 Borrow 與 AsRef 的差別,哪個描述正確?",
      questionCode: "use std::collections::HashMap;\n\nfn main() {\n    let mut stock: HashMap<String, i32> = HashMap::new();\n    stock.insert(String::from(\"apple\"), 3);\n    let key: &str = \"apple\";\n    println!(\"{:?}\", stock.get(key));\n    println!(\"{:?}\", stock.get(\"pear\"));\n}",
      options: [
        { text: "get 的參數是 &Q,要求 String: Borrow<Q> 且 Q: Hash + Eq;String 實作了 Borrow<str>,而 Borrow 額外承諾「借出的 str 與原本的 String 雜湊值、相等性完全一致」,這個保證 AsRef 沒有" },
        { text: "&str 在參數位置被自動轉成 &String,所以 get 收到的其實還是 &String" },
        { text: "Borrow 與 AsRef 是同一件事的兩個名字,HashMap 用哪一個只是歷史因素,可以互換" },
        { text: "get 在內部先用 &str 建立一個臨時的 String,再拿它去查詢" },
      ],
      answer: 0,
      explanation: `HashMap::get 的簽名大致是 fn get<Q>(&self, k: &Q) where K: Borrow<Q>, Q: Hash + Eq + ?Sized。K 是 String,String: Borrow<str> 成立,所以 Q 可以是 str,傳 &str 就能查。查詢時 HashMap 拿 &str 算雜湊值,跟存入時 String 算出的雜湊值比對——這一步能成立,完全是因為 Borrow 的契約要求「借出來的東西與本體的 Hash、Eq、Ord 行為一致」。
AsRef 沒有這個契約,它只說「可以便宜地借出一個 &T」,借出來的東西跟本體比較時可能完全不同,拿來當雜湊鍵查詢就可能查錯,所以兩者不能互換。「&str 自動轉成 &String」是方向搞反了:只有 &String 能轉成 &str,反方向不存在。「先建立臨時 String」則是 C# Dictionary 的經驗——Rust 這裡完全沒有配置記憶體,這正是 Borrow 存在的理由。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::collections::HashMap;", note: "引入 HashMap。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let mut stock: HashMap<String, i32> = HashMap::new();", note: "鍵的型別是擁有所有權的 String。" },
          { code: "    stock.insert(String::from(\"apple\"), 3);", note: "存入時需要一個真正的 String,因為 HashMap 要擁有鍵。" },
          { code: "    let key: &str = \"apple\";", note: "查詢用的是 &str,不是 String。" },
          { code: "    println!(\"{:?}\", stock.get(key));", note: "get 的 Q 被推斷成 str;因為 String: Borrow<str>,且 str 的雜湊值與同內容 String 的雜湊值保證相同,查得到。印出 Some(3)。" },
          { code: "    println!(\"{:?}\", stock.get(\"pear\"));", note: "字面值直接查,不存在的鍵給 None。全程沒有為了查詢配置任何 String。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "分工記法:AsRef 是「給我一個便宜的檢視,我只是要讀」;Borrow 是「給我一個檢視,而且它跟你本人在比較與雜湊上一模一樣」。寫 API 時,只有要把借出的值拿去做 Hash / Eq / Ord 比較(例如當查詢鍵)才需要 Borrow。",
      },
      csharp: `C# 的 Dictionary<string, int> 沒有這個問題,因為 string 只有一種表示法。但如果鍵是自訂型別,C# 查詢時就得先建出一個完整的鍵物件(或者自己寫 IEqualityComparer 並確保 GetHashCode 一致)。Rust 用 Borrow 把「兩種表示法的雜湊與相等性必須一致」寫成 trait 契約,讓 HashMap<String, _> 能直接用 &str 查詢,零配置。`,
    },
    {
      id: "2-9-13",
      question: "參數位置的 Deref coercion 可以連續發生幾次?以下程式碼的輸出是?",
      questionCode: "use std::rc::Rc;\n\nfn count_chars(s: &str) -> usize {\n    s.chars().count()\n}\n\nfn first(items: &[String]) -> &str {\n    &items[0]\n}\n\nfn main() {\n    let boxed: Box<String> = Box::new(String::from(\"hello\"));\n    let shared: Rc<String> = Rc::new(String::from(\"rust\"));\n    let list = vec![String::from(\"a\"), String::from(\"b\")];\n    println!(\"{} {} {}\", count_chars(&boxed), count_chars(&shared), first(&list));\n}",
      options: [
        { text: "印出 5 4 a——&Box<String> 與 &Rc<String> 在參數位置會連續解參考兩層到 &str,&Vec<String> 則轉成 &[String],全部由編譯器自動完成" },
        { text: "編譯錯誤:自動轉換只做一層,&Box<String> 只能變成 &String,要傳 &str 必須寫 &**boxed" },
        { text: "編譯錯誤,只錯在 count_chars(&shared):Rc 是共享所有權的指標,不能被當成 &str 借出" },
        { text: "印出 5 4 a,但 count_chars(&boxed) 會先把 String 從 Box 裡複製出來再轉成 &str" },
      ],
      answer: 0,
      explanation: `當參數型別是 &U、傳進來的是 &T,而 T 能透過 Deref 一路解參考到 U 時,編譯器會自動插入需要的解參考次數——不只一層。Box<String> 解參考成 String,String 再解參考成 str,於是 &boxed 變成 &str;Rc<String> 一樣走兩層;Vec<String> 解參考成 [String],&list 變成 &[String]。這些都是在編譯期決定的型別轉換,只是在指標上多走幾步,不複製任何資料。
「只做一層、要寫 &**boxed」是謹慎的猜測,&**boxed 也確實是這個轉換展開後的樣子,但你不需要自己寫——編譯器會自動插入任意層數。「Rc 不能借出」則混淆了所有權與借用:Rc 的內容雖然是共享的,但借出唯讀的 &str 正是 Rc 允許的事。「先複製再轉」同樣不成立,Deref coercion 從不複製資料。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::rc::Rc;", note: "引入 Rc。" },
          { code: "", note: "" },
          { code: "fn count_chars(s: &str) -> usize {", note: "參數收 &str。" },
          { code: "    s.chars().count()", note: "計算字元數。" },
          { code: "}", note: "count_chars 結束。" },
          { code: "", note: "" },
          { code: "fn first(items: &[String]) -> &str {", note: "參數收 String 的 slice。" },
          { code: "    &items[0]", note: "取第一個元素的參考,&String 在回傳值位置同樣被轉成 &str。" },
          { code: "}", note: "first 結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let boxed: Box<String> = Box::new(String::from(\"hello\"));", note: "String 被放進 Box,多了一層指標。" },
          { code: "    let shared: Rc<String> = Rc::new(String::from(\"rust\"));", note: "String 被放進 Rc,同樣多了一層。" },
          { code: "    let list = vec![String::from(\"a\"), String::from(\"b\")];", note: "一個 Vec<String>。" },
          { code: "    println!(\"{} {} {}\", count_chars(&boxed), count_chars(&shared), first(&list));", note: "&Box<String> 經過兩次解參考變成 &str(等於 &**boxed),得 5;&Rc<String> 同樣兩層,得 4;&Vec<String> 一層變成 &[String],first 回傳 a。印出 5 4 a。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "這就是為什麼參數收 &str、&[T] 的函式特別好用:呼叫端的資料不管包了幾層 Box、Rc、String、Vec,只要最後能解參考到 str 或 [T],都直接傳得進來。",
      },
      csharp: `C# 沒有這種解參考鏈:物件參考就是物件本身,存取欄位與方法時沒有「包了幾層」的問題,也就不需要自動剝殼。最接近的是 Lazy<T> 或 Nullable<T>,它們都要你手動寫 .Value 取出內容;Rust 的 Deref coercion 則讓智慧指標在參數位置自動「透明化」,前提是這條解參考鏈在編譯期就走得通。`,
    },
    {
      id: "2-9-14",
      question: "Buffer 實作了 Deref,Target 是 Vec<u8>,內容剛好是合法的 UTF-8。以下程式碼的結果是?",
      questionCode: "use std::ops::Deref;\n\nstruct Buffer {\n    bytes: Vec<u8>,\n}\n\nimpl Deref for Buffer {\n    type Target = Vec<u8>;\n    fn deref(&self) -> &Vec<u8> {\n        &self.bytes\n    }\n}\n\nfn print_text(s: &str) {\n    println!(\"{}\", s);\n}\n\nfn main() {\n    let buf = Buffer { bytes: b\"hello\".to_vec() };\n    println!(\"{}\", buf.len());\n    print_text(&buf);\n}",
      options: [
        { text: "編譯錯誤,錯在 print_text(&buf):expected &str, found &Buffer——Buffer 的解參考鏈是 Buffer → Vec<u8> → [u8],永遠到不了 str" },
        { text: "印出 5 與 hello——Buffer 實作了 Deref,就能在參數位置自動轉成需要的任何借用型別" },
        { text: "印出 5 與 hello——內容是合法的 UTF-8,編譯器會把 &[u8] 轉成 &str" },
        { text: "編譯錯誤,錯在 buf.len():Buffer 只實作了 Deref,要呼叫 Vec 的方法還必須實作 DerefMut" },
      ],
      answer: 0,
      explanation: `Deref coercion 只會沿著每個型別唯一的 Target 往下走:Buffer 的 Target 是 Vec<u8>,Vec<u8> 的 Target 是 [u8],[u8] 沒有 Deref,鏈就停了。str 不在這條鏈上,所以 &Buffer 無論如何都變不成 &str,編譯器報 mismatched types。
「實作了 Deref 就能變成任何借用型別」是這題的核心誤解——Deref 不是萬用轉換器,每個型別只能有一個 Target,轉換路線是固定的一條。「內容是合法 UTF-8 所以會轉」則是把執行期的資料內容帶進編譯期的型別檢查:bytes 轉 str 需要驗證,而驗證可能失敗,這種轉換絕不會自動發生。buf.len() 反而沒問題:len 只需要 &self,Deref 給的唯讀存取就夠了,DerefMut 是 push 這類需要 &mut self 的方法才要的(智慧指標那一課的主題)。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "use std::ops::Deref;", note: "引入 Deref。" },
            { code: "", note: "" },
            { code: "struct Buffer {", note: "包一個位元組緩衝區的自訂型別。" },
            { code: "    bytes: Vec<u8>,", note: "內容是位元組,不是字串。" },
            { code: "}", note: "struct 定義結束。" },
            { code: "", note: "" },
            { code: "impl Deref for Buffer {", note: "讓 Buffer 能被解參考。" },
            { code: "    type Target = Vec<u8>;", note: "解參考的目標是 Vec<u8>——這決定了整條轉換鏈:Buffer → Vec<u8> → [u8]。" },
            { code: "    fn deref(&self) -> &Vec<u8> {", note: "回傳內部 Vec 的參考。" },
            { code: "        &self.bytes", note: "直接借出欄位。" },
            { code: "    }", note: "deref 結束。" },
            { code: "}", note: "impl 結束。" },
            { code: "", note: "" },
            { code: "fn print_text(s: &str) {", note: "參數收 &str。" },
            { code: "    println!(\"{}\", s);", note: "印出文字。" },
            { code: "}", note: "print_text 結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let buf = Buffer { bytes: b\"hello\".to_vec() };", note: "b\"hello\" 是位元組字面值,to_vec 複製成 Vec<u8>。內容剛好是合法的 UTF-8,但型別上它只是一堆位元組。" },
            { code: "    println!(\"{}\", buf.len());", note: "方法呼叫穿透到 Vec 的 len,印出 5。這一行沒問題。" },
            { code: "    print_text(&buf);", note: "⛔ 編譯失敗:mismatched types(E0308),expected &str, found &Buffer。編譯器沿著 Buffer → Vec<u8> → [u8] 找過一輪,沒有一站是 str。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::ops::Deref;", note: "不用改。" },
            { code: "", note: "" },
            { code: "struct Buffer {", note: "不用改。" },
            { code: "    bytes: Vec<u8>,", note: "不用改。" },
            { code: "}", note: "struct 定義結束。" },
            { code: "", note: "" },
            { code: "impl Deref for Buffer {", note: "不用改。" },
            { code: "    type Target = Vec<u8>;", note: "不用改。" },
            { code: "    fn deref(&self) -> &Vec<u8> {", note: "不用改。" },
            { code: "        &self.bytes", note: "不用改。" },
            { code: "    }", note: "deref 結束。" },
            { code: "}", note: "impl 結束。" },
            { code: "", note: "" },
            { code: "fn print_text(s: &str) {", note: "不用改。" },
            { code: "    println!(\"{}\", s);", note: "印出文字。" },
            { code: "}", note: "print_text 結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let buf = Buffer { bytes: b\"hello\".to_vec() };", note: "資料不變。" },
            { code: "    println!(\"{}\", buf.len());", note: "印出 5。" },
            { code: "    match std::str::from_utf8(&buf) {", note: "改動處:明確做「位元組轉文字」的驗證。from_utf8 收 &[u8],而 &Buffer 正好能沿著解參考鏈轉成 &[u8]——鏈上有的站,自動轉換就到得了。" },
            { code: "        Ok(text) => print_text(text),", note: "驗證通過,拿到 &str,印出 hello。" },
            { code: "        Err(e) => println!(\"not UTF-8: {}\", e),", note: "不是合法 UTF-8 時的處理,這次不會走到。" },
            { code: "    }", note: "match 結束。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "判斷參數位置能不能自動轉,就把型別的 Target 一站一站列出來:目標型別在這條鏈上就行,不在就要自己寫轉換。",
        },
      ],
      csharp: `C# 沒有「沿著解參考鏈推斷目標型別」的機制,byte[] 要變成 string 一定是呼叫 Encoding.UTF8.GetString(bytes),而且無效位元組會被悄悄換成替代字元。Rust 的 from_utf8 同樣要你明確呼叫,但它回傳 Result,把「這些位元組可能不是合法文字」變成必須處理的情況。`,
    },
    {
      id: "2-9-15",
      question: "Point 已經實作了 Display,又想自訂 to_string 的輸出。以下程式碼的結果是?",
      questionCode: "use std::fmt;\n\nstruct Point {\n    x: i32,\n    y: i32,\n}\n\nimpl fmt::Display for Point {\n    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {\n        write!(f, \"({}, {})\", self.x, self.y)\n    }\n}\n\nimpl ToString for Point {\n    fn to_string(&self) -> String {\n        format!(\"Point{}\", self)\n    }\n}\n\nfn main() {\n    let p = Point { x: 1, y: 2 };\n    println!(\"{}\", p.to_string());\n}",
      options: [
        { text: "編譯錯誤:conflicting implementations of trait ToString for type Point——標準庫已經對所有實作 Display 的型別提供了 ToString,不能再實作第二份" },
        { text: "印出 Point(1, 2)——手寫的 ToString 會覆寫掉預設的版本,就像覆寫 virtual 方法" },
        { text: "印出 (1, 2)——標準庫的版本優先,手寫的 impl 被忽略" },
        { text: "編譯錯誤,錯在 p.to_string() 這一行:有兩個 to_string 可選,呼叫有歧義" },
      ],
      answer: 0,
      explanation: `標準庫有一條 impl<T: Display + ?Sized> ToString for T:任何實作了 Display 的型別都自動擁有 ToString,to_string 的結果就是 Display 的輸出。Point 實作了 Display,所以 ToString for Point 已經存在;再手寫一份,同一個 trait 對同一個型別就有兩個實作,編譯器在 impl 那一行就報 E0119,根本走不到呼叫端。
「像覆寫 virtual 方法」是 C# 的直覺:object.ToString() 是虛擬方法,子類別覆寫天經地義。但 Rust 的 trait 實作沒有「預設版本加覆寫」的機制,每個型別對每個 trait 只能有一份實作。「呼叫時才有歧義」也不對,衝突在定義實作時就被抓到了。想要 Point(1, 2) 這種輸出,就把它寫進 Display 本身;想要兩種格式,Display 之外可以另寫一個具名方法。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "use std::fmt;", note: "引入 fmt 模組。" },
            { code: "", note: "" },
            { code: "struct Point {", note: "一個二維座標。" },
            { code: "    x: i32,", note: "x 座標。" },
            { code: "    y: i32,", note: "y 座標。" },
            { code: "}", note: "struct 定義結束。" },
            { code: "", note: "" },
            { code: "impl fmt::Display for Point {", note: "實作 Display——從這一刻起,標準庫的 blanket impl 就已經替 Point 實作了 ToString。" },
            { code: "    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {", note: "Display 唯一要實作的方法。" },
            { code: "        write!(f, \"({}, {})\", self.x, self.y)", note: "輸出 (1, 2) 這種格式。" },
            { code: "    }", note: "fmt 結束。" },
            { code: "}", note: "impl 結束。" },
            { code: "", note: "" },
            { code: "impl ToString for Point {", note: "⛔ 編譯失敗:conflicting implementations of trait ToString for type Point(E0119)。note 指出衝突的另一份實作在 crate alloc:impl<T> ToString for T where T: Display。" },
            { code: "    fn to_string(&self) -> String {", note: "這份手寫的實作永遠不會被採用。" },
            { code: "        format!(\"Point{}\", self)", note: "想輸出 Point(1, 2)。" },
            { code: "    }", note: "to_string 結束。" },
            { code: "}", note: "impl 結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let p = Point { x: 1, y: 2 };", note: "建立 Point。" },
            { code: "    println!(\"{}\", p.to_string());", note: "錯誤不在這一行,編譯在上面的 impl 就停了。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "use std::fmt;", note: "不用改。" },
            { code: "", note: "" },
            { code: "struct Point {", note: "不用改。" },
            { code: "    x: i32,", note: "x 座標。" },
            { code: "    y: i32,", note: "y 座標。" },
            { code: "}", note: "struct 定義結束。" },
            { code: "", note: "" },
            { code: "impl fmt::Display for Point {", note: "只實作 Display。想要的輸出格式就寫在這裡。" },
            { code: "    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {", note: "不用改。" },
            { code: "        write!(f, \"({}, {})\", self.x, self.y)", note: "輸出 (1, 2)。" },
            { code: "    }", note: "fmt 結束。" },
            { code: "}", note: "impl 結束。改動處:整個手寫的 ToString 區塊刪掉。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let p = Point { x: 1, y: 2 };", note: "建立 Point。" },
            { code: "    let s: String = p.to_string();", note: "to_string 由標準庫的 blanket impl 提供,內部就是用 Display 格式化,s 是 (1, 2)。" },
            { code: "    println!(\"{}\", s);", note: "印出 (1, 2)。" },
            { code: "    println!(\"{}\", p);", note: "直接用 Display 印,結果相同。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "△ Desugared (what the compiler sees)",
          intro: "錯誤訊息說衝突的實作在 crate alloc,但你的程式碼裡只有一份 ToString。另一份來自標準庫:",
          lines: [
            { code: "impl<T: fmt::Display + ?Sized> ToString for T {", note: "標準庫裡的 blanket impl:對「所有實作 Display 的型別」一次實作 ToString。" },
            { code: "    fn to_string(&self) -> String {", note: "內容等同於 format!(\"{}\", self)。" },
            { code: "        format!(\"{}\", self)", note: "用 Display 格式化成新的 String。" },
            { code: "    }", note: "to_string 結束。" },
            { code: "}", note: "impl 結束。" },
            { code: "", note: "" },
            { code: "// 當 T = Point 時,上面那份就等於:", note: "你的 impl fmt::Display for Point 一寫完,這一份就自動成立。" },
            { code: "impl ToString for Point { /* 標準庫版本 */ }", note: "再加上你手寫的那一份,同一個 trait 對同一個型別出現兩個實作,就是 E0119。" },
          ],
        },
      ],
      csharp: `C# 每個物件都繼承 object.ToString(),覆寫它是基本功。Rust 反過來:格式化的「本體」是 Display,ToString 只是由 Display 推導出來的便利方法,而且 trait 實作沒有覆寫的概念。所以 C# 開發者在 Rust 要改的習慣是——想控制 to_string 的輸出,改的是 Display。`,
    },
    {
      id: "2-9-16",
      question: "parse 的目標型別與失敗條件。以下程式碼的輸出是?",
      questionCode: "fn main() {\n    let a = \"42\".parse::<u8>();\n    let b: Result<u8, _> = \"256\".parse();\n    let c = \" 7\".parse::<i32>();\n    let d: f64 = \"2.5\".parse().unwrap();\n    println!(\"{:?} {} {} {}\", a, b.is_err(), c.is_err(), d);\n}",
      options: [
        { text: "Ok(42) true true 2.5——256 超出 u8 範圍解析失敗,前面帶空白的字串也解析失敗;d 的目標型別由變數標註決定" },
        { text: "Ok(42) false false 2.5——256 會像 as 一樣截斷成 0,而 parse 會自動去掉前後空白" },
        { text: "Ok(42) true false 2.5——parse 會自動去掉前後空白,只有 256 失敗" },
        { text: "編譯錯誤:d 那一行的 parse 沒寫 turbofish,parse 只能用 ::<T> 指定目標型別" },
      ],
      answer: 0,
      explanation: `parse::<T>() 會呼叫 T 的 FromStr 實作,回傳 Result<T, T::Err>。目標型別可以用 turbofish 指定(a、c),也可以從變數標註推斷(b、d)。u8 的解析會檢查範圍,"256" 放不進 u8,給 Err;整數解析對格式非常嚴格,開頭多一個空白就是無效數字,也給 Err。
「像 as 一樣截斷」混淆了兩種轉換:as 對數值做位元截斷,parse 是對文字做解析,解析的結果放不下就是失敗,不會截斷。「自動去掉空白」是 C# int.Parse 的習慣(它預設允許前後空白),Rust 的 parse 不做這件事,要自己先呼叫 trim()。turbofish 也不是唯一的方式,任何能讓編譯器推斷出目標型別的線索都行。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let a = \"42\".parse::<u8>();", note: "turbofish 指定目標是 u8,42 在範圍內,a 是 Ok(42)。" },
          { code: "    let b: Result<u8, _> = \"256\".parse();", note: "目標型別從 b 的標註推斷(_ 讓編譯器自己填錯誤型別)。256 超出 u8 範圍,b 是 Err。" },
          { code: "    let c = \" 7\".parse::<i32>();", note: "開頭有空白,整數解析不接受,c 是 Err。實務上要先寫 \" 7\".trim().parse::<i32>()。" },
          { code: "    let d: f64 = \"2.5\".parse().unwrap();", note: "unwrap 之後的型別要是 f64,編譯器由此反推 parse 的目標是 f64,d 是 2.5。" },
          { code: "    println!(\"{:?} {} {} {}\", a, b.is_err(), c.is_err(), d);", note: "印出 Ok(42) true true 2.5。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "parse 只是一個入口,真正做事的是目標型別的 FromStr 實作——所以同一個 parse,解析 u8 會檢查範圍,解析 f64 則接受小數點與指數。",
      },
      csharp: `對應 C# 的 byte.Parse、int.Parse、double.Parse 一族。兩個差異值得記:C# 的 int.Parse(" 7") 預設接受前後空白,Rust 不接受;C# 用 Parse 與 TryParse 兩套 API 區分「失敗時丟例外」與「失敗時回傳 false」,Rust 只有一個 parse,一律回傳 Result,要不要當機由呼叫端決定。`,
    },
    {
      id: "2-9-17",
      question: "以下程式碼的結果是?",
      questionCode: "fn main() {\n    let input = \"42\";\n    let n = input.parse().unwrap();\n    println!(\"{}\", n + 1);\n}",
      options: [
        { text: "編譯錯誤:type annotations needed——parse 能產生的型別有很多種,n + 1 不足以決定是哪一種" },
        { text: "印出 43——沒有標註時,parse 跟整數字面值一樣預設為 i32" },
        { text: "印出 43——n + 1 裡的 1 是整數,編譯器從這裡推斷出 n 是整數" },
        { text: "執行期 panic:沒有指定目標型別,parse 解析失敗,unwrap 在 Err 上當機" },
      ],
      answer: 0,
      explanation: `parse 的簽名是 fn parse<F: FromStr>(&self) -> Result<F, F::Err>,F 必須從上下文決定。這裡 n 沒標註,unwrap 之後的值只被拿去做 n + 1,而 + 對很多型別都有定義(i32、u8、f64……甚至自訂型別),編譯器無法從中選出一個,報 E0284 type annotations needed,附註 cannot satisfy <_ as FromStr>::Err == _。
「跟整數字面值一樣預設 i32」是這題的主要陷阱:let x = 42; 確實會落到 i32,但那個預設只套用在「整數字面值本身的型別」,不會延伸到泛型函式的型別參數。「從 1 推斷」也行不通:1 的型別反而要跟著 n 走,兩邊都在等對方。「執行期失敗」則不可能,型別不確定的程式碼根本編譯不過,沒有執行的機會。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let input = \"42\";", note: "一個 &str。" },
            { code: "    let n = input.parse().unwrap();", note: "⛔ 編譯失敗:type annotations needed(E0284),note:cannot satisfy <_ as FromStr>::Err == _。parse 的目標型別沒有任何線索。" },
            { code: "    println!(\"{}\", n + 1);", note: "n + 1 對很多型別都成立,不構成足夠的線索;整數字面值預設 i32 的規則也不會套到 parse 的型別參數上。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let input = \"42\";", note: "不用改。" },
            { code: "    let n: i32 = input.parse().unwrap();", note: "改動處,寫法一:標註變數型別,parse 的目標由此推斷為 i32。" },
            { code: "    let m = input.parse::<u64>().unwrap();", note: "改動處,寫法二:用 turbofish 直接指定目標型別為 u64。" },
            { code: "    println!(\"{} {}\", n + 1, m * 2);", note: "印出 43 84。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "△ Desugared (what the compiler sees)",
          intro: "錯誤訊息提到 FromStr,但你只寫了 parse。因為 parse 本身是一個泛型方法:",
          lines: [
            { code: "let n = input.parse().unwrap();", note: "你寫的這一行。" },
            { code: "", note: "" },
            { code: "// parse 的簽名:", note: "標準庫 str 上的定義。" },
            { code: "// fn parse<F: FromStr>(&self) -> Result<F, F::Err>", note: "F 是泛型參數,呼叫端不給就要推斷;真正做解析的是 F 的 FromStr 實作。" },
            { code: "", note: "" },
            { code: "// 所以這一行其實是:", note: "編譯器眼中的樣子。" },
            { code: "let n = str::parse::<?>(input).unwrap();", note: "問號就是那個推不出來的 F。錯誤訊息裡的 <_ as FromStr>::Err 指的就是「F 的錯誤型別」,因為 F 未知,它也未知。" },
          ],
        },
      ],
      csharp: `C# 的 int.Parse("42") 用型別名稱當方法的接收者,目標型別寫在方法名稱裡,不存在推斷問題。Rust 把「所有型別的解析」統一成同一個 parse,換來一致的 API 與可以擴充到自訂型別(下一題),代價就是呼叫端要提供目標型別。`,
    },
    {
      id: "2-9-18",
      question: "為自訂型別 Rgb 實作 FromStr。以下程式碼的輸出是?",
      questionCode: "use std::str::FromStr;\n\n#[derive(Debug)]\nstruct Rgb(u8, u8, u8);\n\nimpl FromStr for Rgb {\n    type Err = String;\n\n    fn from_str(s: &str) -> Result<Self, Self::Err> {\n        let hex = match s.strip_prefix('#') {\n            Some(h) if h.len() == 6 && h.is_ascii() => h,\n            _ => return Err(format!(\"bad color: {}\", s)),\n        };\n        let part = |i: usize| u8::from_str_radix(&hex[i..i + 2], 16);\n        match (part(0), part(2), part(4)) {\n            (Ok(r), Ok(g), Ok(b)) => Ok(Rgb(r, g, b)),\n            _ => Err(format!(\"bad hex: {}\", s)),\n        }\n    }\n}\n\nfn main() {\n    let a = \"#ff8000\".parse::<Rgb>();\n    let b: Result<Rgb, _> = \"ff8000\".parse();\n    println!(\"{:?}\", a);\n    println!(\"{:?}\", b);\n}",
      options: [
        { text: "Ok(Rgb(255, 128, 0)) 與 Err(\"bad color: ff8000\")——實作 FromStr 之後,parse::<Rgb>() 就會呼叫你的 from_str,失敗時回傳你定義的 Err 型別" },
        { text: "編譯錯誤:parse 只能解析標準庫的型別,自訂型別要改用 Rgb::from_str(\"#ff8000\") 直接呼叫" },
        { text: "編譯錯誤:要讓 parse 可用必須實作 From<&str>,FromStr 只是提供了一個叫 from_str 的關聯函式" },
        { text: "印出 Ok(Rgb(255, 128, 0)),接著 panic——parse 遇到 from_str 回傳 Err 時會直接當機" },
      ],
      answer: 0,
      explanation: `parse 的定義就是「呼叫目標型別的 FromStr::from_str」,所以任何型別只要實作 FromStr,就能用 parse::<T>() 或靠推斷解析,錯誤型別由 type Err 決定——這裡選了 String,失敗時帶一段說明。"#ff8000" 通過檢查,三段十六進位解析成 255、128、0;"ff8000" 少了 #,在第一個 match 就回傳 Err。
「parse 只能解析標準庫型別」不對,標準庫的 i32、f64、bool 也不過是各自實作了 FromStr,自訂型別的地位完全一樣。「要實作 From<&str>」則是混淆了兩個 trait:From 代表「一定成功」的轉換,文字解析卻隨時可能失敗;FromStr 回傳 Result,才是解析該用的介面,而 parse 認的也只有它。若真的想要 TryFrom<&str> 的寫法,可以另外實作,但 parse 不會用到它。「失敗就當機」是把 parse 想成了 unwrap,parse 只負責把 from_str 的 Result 原樣交回。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "use std::str::FromStr;", note: "FromStr 不在 prelude 裡,實作它要先引入。" },
          { code: "", note: "" },
          { code: "#[derive(Debug)]", note: "讓 {:?} 能印出 Rgb。" },
          { code: "struct Rgb(u8, u8, u8);", note: "紅綠藍三個通道。" },
          { code: "", note: "" },
          { code: "impl FromStr for Rgb {", note: "宣告 Rgb 可以從文字解析出來。" },
          { code: "    type Err = String;", note: "失敗時的錯誤型別由你決定,這裡用一段文字說明。" },
          { code: "", note: "" },
          { code: "    fn from_str(s: &str) -> Result<Self, Self::Err> {", note: "唯一要實作的函式,回傳 Result:解析可能失敗。" },
          { code: "        let hex = match s.strip_prefix('#') {", note: "去掉開頭的 #;沒有 # 就是 None。" },
          { code: "            Some(h) if h.len() == 6 && h.is_ascii() => h,", note: "剩下的部分必須是 6 個 ASCII 字元,才能安全地按位元組位置切片。" },
          { code: "            _ => return Err(format!(\"bad color: {}\", s)),", note: "格式不對就提早回傳錯誤。\"ff8000\" 在這裡失敗。" },
          { code: "        };", note: "hex 是去掉 # 之後的 6 個字元。" },
          { code: "        let part = |i: usize| u8::from_str_radix(&hex[i..i + 2], 16);", note: "小閉包:把從 i 開始的兩個字元當成十六進位解析成 u8。" },
          { code: "        match (part(0), part(2), part(4)) {", note: "三段一起解析,同時比對三個結果。" },
          { code: "            (Ok(r), Ok(g), Ok(b)) => Ok(Rgb(r, g, b)),", note: "三段都成功才組成 Rgb。" },
          { code: "            _ => Err(format!(\"bad hex: {}\", s)),", note: "任何一段不是合法十六進位就失敗。" },
          { code: "        }", note: "match 結束,它的值就是函式的回傳值。" },
          { code: "    }", note: "from_str 結束。" },
          { code: "}", note: "impl 結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let a = \"#ff8000\".parse::<Rgb>();", note: "parse 呼叫 Rgb::from_str,a 是 Ok(Rgb(255, 128, 0))。" },
          { code: "    let b: Result<Rgb, _> = \"ff8000\".parse();", note: "目標型別從標註推斷;少了 #,b 是 Err(\"bad color: ff8000\")。" },
          { code: "    println!(\"{:?}\", a);", note: "印出 Ok(Rgb(255, 128, 0))。" },
          { code: "    println!(\"{:?}\", b);", note: "印出 Err(\"bad color: ff8000\")。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "三個「從 A 變成 B」的 trait 分工:From 用在一定成功的轉換;TryFrom 用在可能失敗的一般轉換;FromStr 專門負責「從文字解析」,並且是 parse 背後唯一認的那一個。",
      },
      csharp: `C# 的慣例是在型別上手寫 static Rgb Parse(string) 與 static bool TryParse(string, out Rgb),但這只是命名慣例,沒有一個共同介面能讓泛型程式碼呼叫(.NET 7 起才有 IParsable<T>)。Rust 從一開始就把解析做成 FromStr trait,自訂型別實作之後就跟 i32 一樣能用 parse,也能被 T: FromStr 的泛型程式碼使用。`,
    },
    {
      id: "2-9-19",
      question: "要把使用者輸入的金額字串(例如 \"0.29\")轉成內部以「分」為單位儲存的 Money { cents: i64 }。哪一種做法最恰當?",
      options: [
        { text: "為 Money 實作 FromStr(或 TryFrom<&str>),自己拆出整數與小數部分,回傳 Result——輸入可能不合法,轉換必須能表達失敗,也不應該經過浮點數" },
        { text: "先 parse::<f64>() 再乘以 100,最後 as i64——寫法最短,而且 as 會處理掉小數" },
        { text: "為 Money 實作 From<&str>,遇到不合法的輸入就在裡面 panic——反正能編譯,效果跟回傳 Result 差不多" },
        { text: "為 Money 實作 From<f64>,呼叫端先 parse 成 f64 再 into()——這樣 From 就能處理所有情況" },
      ],
      answer: 0,
      explanation: `這個需求有兩個特徵:來源是文字,所以一定會遇到不合法的輸入;目標是精確的金額,所以不能經過浮點數。實作 FromStr 並回傳 Result 同時照顧了這兩點——失敗寫在型別裡,整數與小數分別用整數解析,不經過任何浮點運算。
「parse 成 f64 再乘 100 再 as」看起來最省事,但 0.29 在二進位浮點數裡是 0.28999…,乘以 100 得到 28.999999999999996,as 往 0 取整就變成 28 分——少了一分錢,而且沒有任何錯誤訊息。「From<&str> 裡 panic」違反了 From 的契約:From 代表不會失敗,呼叫端看到 from 或 into 不會想到要防當機。「From<f64>」則把同樣的浮點數誤差搬進了轉換本身,而且把「解析文字」這個可能失敗的步驟丟給了呼叫端。`,
      walkthrough: {
        label: "⊕ Idiomatic version — walkthrough (runs successfully)",
        lines: [
          { code: "use std::str::FromStr;", note: "引入 FromStr。" },
          { code: "", note: "" },
          { code: "#[derive(Debug)]", note: "讓 {:?} 能印出 Money。" },
          { code: "struct Money {", note: "內部以整數的「分」儲存,避開浮點數誤差。" },
          { code: "    cents: i64,", note: "0.29 元存成 29。" },
          { code: "}", note: "struct 定義結束。" },
          { code: "", note: "" },
          { code: "impl FromStr for Money {", note: "文字到 Money 的解析,可能失敗。" },
          { code: "    type Err = String;", note: "失敗時帶一段說明。" },
          { code: "", note: "" },
          { code: "    fn from_str(s: &str) -> Result<Self, Self::Err> {", note: "回傳 Result,失敗是型別的一部分。" },
          { code: "        let (whole, frac) = s.split_once('.').unwrap_or((s, \"00\"));", note: "以小數點切成整數部分與小數部分;沒有小數點時視為 .00。" },
          { code: "        if frac.len() != 2 {", note: "這個簡化版本只接受剛好兩位小數(示範用,也沒有處理負數)。" },
          { code: "            return Err(format!(\"need 2 decimals: {}\", s));", note: "格式不符就回報錯誤。" },
          { code: "        }", note: "檢查結束。" },
          { code: "        match (whole.parse::<i64>(), frac.parse::<i64>()) {", note: "兩個部分都用整數解析,全程沒有浮點數。" },
          { code: "            (Ok(w), Ok(f)) => Ok(Money { cents: w * 100 + f }),", note: "組合成分:0 * 100 + 29 = 29,精確無誤。" },
          { code: "            _ => Err(format!(\"not a number: {}\", s)),", note: "任一部分不是數字就失敗。" },
          { code: "        }", note: "match 結束。" },
          { code: "    }", note: "from_str 結束。" },
          { code: "}", note: "impl 結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let naive = (\"0.29\".parse::<f64>().unwrap() * 100.0) as i64;", note: "對照組:經過浮點數。0.29 * 100.0 是 28.999999999999996,as 往 0 取整得 28。" },
          { code: "    println!(\"{}\", naive);", note: "印出 28——少了一分錢,而且沒有任何警告。" },
          { code: "    println!(\"{:?}\", \"0.29\".parse::<Money>());", note: "印出 Ok(Money { cents: 29 }),精確。" },
          { code: "    println!(\"{:?}\", \"abc\".parse::<Money>());", note: "不合法的輸入給 Err,印出 Err(\"not a number: abc\"),而不是當機或一個錯的金額。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "選轉換工具時先問兩個問題:會不會失敗?(會 → TryFrom / FromStr,不會 → From)會不會失真?(會 → 別用 as 或浮點數當中介)。",
      },
      csharp: `C# 開發者處理金額時會用 decimal,decimal.Parse("0.29") 得到精確的 0.29,這個坑在 C# 比較少踩到。Rust 標準庫沒有 decimal 型別,常見做法是用整數存最小單位(像這裡的分),或使用第三方的 decimal crate;不管哪一種,解析入口都應該是一個回傳 Result 的 FromStr 實作,而不是穿過 f64。`,
    },
    {
      id: "2-9-20",
      question: "本課 C# 對照總結:關於 C# 的型別轉換與 Rust 的轉換 trait,哪一組描述最準確?",
      options: [
        { text: "C# 靠轉型語法、implicit / explicit operator 與 Convert / Parse 系列方法處理轉換,失敗通常丟例外;Rust 把轉換拆成各自承諾不同的工具:as 做不檢查的數值轉型,From / Into 表示一定成功,TryFrom / TryInto 與 FromStr 以 Result 表示可能失敗,AsRef / Borrow 提供借用檢視,Deref coercion 只在參數位置沿著固定的解參考鏈自動發生" },
        { text: "兩者一一對應:implicit operator 對應 Into、explicit operator 對應 From、IConvertible 對應 AsRef,只是名字不同" },
        { text: "Rust 的 From / Into 跟 C# 的 implicit operator 一樣,實作之後賦值時就會自動轉換,不需要寫任何呼叫" },
        { text: "Rust 的 as 相當於 C# 的 checked 轉型,超出範圍一定會報錯,所以 TryFrom 只是一個比較囉嗦的替代寫法" },
      ],
      answer: 0,
      explanation: `整課收束成一句話:C# 讓轉換「可以不寫出來」(隱式轉換、預設 unchecked),失敗則靠例外;Rust 要求每一個轉換都寫出來,並且用你選的工具說明它的性質——from 說一定成功,try_from 與 parse 說可能失敗、請處理 Result,as 說我知道會截斷,as_ref 說我只是借來看。唯一的例外是 Deref coercion,而它的範圍被嚴格限制在「參數等位置、沿著唯一的 Target 鏈」。
「一一對應」是最容易記混的版本:From 與 Into 是同一條轉換的兩個入口,不是 implicit 與 explicit 的分別,IConvertible 也跟借用毫無關係。「From 會在賦值時自動轉換」忽略了 Rust 的核心設計:沒有 from 或 into 就沒有轉換。「as 相當於 checked」則剛好說反了——as 對應的是 unchecked,這是第 2 題的坑。`,
      walkthrough: [
        {
          label: "◇ C# equivalent: conversions are often invisible",
          lang: "csharp",
          lines: [
            { code: "long n = 300;", note: "一個 long。" },
            { code: "byte lossy = unchecked((byte)n);", note: "unchecked 轉型:靜默截斷成 44。預設上下文就是 unchecked,unchecked 關鍵字常常根本不會出現。" },
            { code: "long widened = lossy;", note: "隱式放大轉換,程式碼裡看不到任何轉換痕跡。" },
            { code: "int parsed = int.TryParse(\"7\", out var v) ? v : 0;", note: "解析用 Parse / TryParse 雙軌 API。" },
            { code: "byte strict = checked((byte)n);", note: "checked 轉型:超出範圍丟 OverflowException,失敗藏在例外裡,簽名看不出來。" },
          ],
        },
        {
          label: "√ Rust: every conversion states what it promises",
          lines: [
            { code: "use std::collections::HashMap;", note: "引入 HashMap,示範 Borrow。" },
            { code: "", note: "" },
            { code: "fn show(text: impl AsRef<str>) {", note: "AsRef:接受任何能借出 &str 的型別。" },
            { code: "    println!(\"{}\", text.as_ref());", note: "統一轉成 &str 後印出。" },
            { code: "}", note: "show 結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let n: i64 = 300;", note: "一個 i64。" },
            { code: "    let lossy = n as u8;", note: "as:明說「我接受截斷」,lossy 是 44。" },
            { code: "    let checked = u8::try_from(n);", note: "TryFrom:明說「可能失敗」,checked 是 Err。" },
            { code: "    let widened = i64::from(lossy);", note: "From:明說「一定成功」,widened 是 44。" },
            { code: "    let parsed: i32 = \"7\".parse().unwrap_or(0);", note: "FromStr / parse:文字解析,回傳 Result,這裡失敗時用 0 代替。" },
            { code: "    let mut map: HashMap<String, i32> = HashMap::new();", note: "鍵是 String。" },
            { code: "    map.insert(String::from(\"k\"), parsed);", note: "存入 7。" },
            { code: "    show(format!(\"{} {} {} {:?}\", lossy, checked.is_err(), widened, map.get(\"k\")));", note: "map.get(\"k\") 靠 Borrow 用 &str 查 String 鍵;format! 產生的 String 傳給 AsRef 參數。印出 44 true 44 Some(7)。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "同樣五種轉換,Rust 版本每一行的轉換方式都寫在字面上,讀程式碼就知道哪裡可能截斷、哪裡可能失敗。",
        },
      ],
      csharp: `給 C# 開發者帶著走的三個結論。一、Rust 沒有隱式轉換(Deref coercion 是唯一的、範圍很窄的例外),看到型別對不上,不要找「能讓它自動轉的設定」,而是選一個轉換工具寫出來。二、選工具就是在回答「會不會失敗」:不會就 From,會就 TryFrom 或 FromStr,並且處理 Result;as 只留給「確定放得下」或「截斷正是目的」的數值轉換。三、寫函式參數時優先收借用檢視(&str、&[T],或泛型的 AsRef),呼叫端不管怎麼持有資料都傳得進來,這是 C# 用多載與介面才做得到的事。`,
    },
  ],
};
