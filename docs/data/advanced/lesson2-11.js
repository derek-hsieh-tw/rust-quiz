/* 出題慣例見專案根目錄 AUTHORING.md:
 *   - answer 一律為 0(正確答案寫在第一個選項),顯示順序由 quiz.js 依題目 id 洗牌
 *   - 詳解禁止用「選項 A/B/C」字母指涉,必須直接描述選項內容
 *   - 有程式碼的題目一律附 walkthrough(逐行說明);反面案例必須同時附上 √ 正確寫法 */
window.RUST_LESSONS = window.RUST_LESSONS || {};
window.RUST_LESSONS["lesson2-11"] = {
  id: "lesson2-11",
  title: "巨集入門(Macros)",
  goal: "知道巨集跟函式的本質差異、看懂 macro_rules! 的模式比對與重複語法,並認識標準庫最常用的幾個巨集背後在做什麼。",
  primer: {
    intro: "名字後面帶驚嘆號的呼叫,例如 println! 和 vec!,都是巨集(macro)。巨集不是在執行期被呼叫的函式,而是在編譯期就被展開成一段普通的 Rust 程式碼,展開的結果再交給編譯器做型別檢查。除了使用標準庫提供的巨集,也可以用 macro_rules! 自己定義,寫法是一組「呼叫長這樣 => 展開成這樣」的規則。",
    examples: [
      {
        code: "fn main() {\n    let v = vec![10, 20, 30];\n    println!(\"{:?} has {} items\", v, v.len());\n}",
        note: "vec! 和 println! 都是標準庫的巨集,驚嘆號標示「這裡在編譯期展開」;印出 [10, 20, 30] has 3 items。",
      },
      {
        code: "macro_rules! hello {\n    () => { println!(\"hello from a macro\") };\n}\n\nfn main() {\n    hello!();\n}",
        note: "定義一個不收任何參數的巨集 hello!,呼叫處 hello!() 在編譯期被換成那一行 println!,執行時印出 hello from a macro。",
      },
    ],
    csharp: "C# 沒有對應的語言功能:#define 只能定義條件編譯用的符號,不能帶參數、不能產生程式碼。想在 C# 裡「編譯期產生程式碼」得另外寫一個 Source Generator,兩者的差別留到本課最後一題細看。",
  },
  questions: [
    {
      id: "2-11-01",
      question: "println! 為什麼是巨集而不是函式?以下程式碼的結果與原因是?",
      questionCode: "fn main() {\n    let lang = \"Rust\";\n    println!(\"{} + {} = {}\", 1, 2, 1 + 2);\n    println!(\"hello\");\n    println!(\"{lang} {}\", 2021);\n}",
      options: [
        { text: "印出三行 1 + 2 = 3、hello、Rust 2021——println! 在編譯期把格式字串拆開,依裡面有幾個 {} 決定要吃幾個引數,並檢查每個引數能不能被格式化;Rust 的函式參數個數是固定的,這件事只能交給巨集做" },
        { text: "印出同樣三行,但巨集和函式沒有本質差異,驚嘆號只是標準庫替「會輸出東西的函式」取名的慣例" },
        { text: "編譯錯誤:同一個函式不能這次收 4 個引數、下次收 1 個引數,要改成把引數包進一個陣列再傳" },
        { text: "前兩行正常,第三行照字面印出 {lang} 2021——格式字串只是一般的 &str,要到執行期才會被解析,而執行期已經看不到變數名稱 lang 了" },
      ],
      answer: 0,
      explanation: `這三行 println! 分別收了 4 個、1 個、2 個引數。Rust 的函式沒有可變數量參數(沒有 C# 的 params),也沒有多載,一個函式簽名就是一個固定的參數列——所以「同一個名字、每次收不同數量的引數」在函式世界裡根本寫不出來。println! 能這樣用,正是因為它不是函式:它在編譯期讀進整串 token,把格式字串拆開,數出有幾個 {}、每個 {} 對應哪個引數,再展開成一段參數固定的普通程式碼。
「編譯錯誤:同一個函式不能收不同數量的引數」這個選項的推理其實是對的——對函式來說確實不行——錯在前提:println! 根本不是函式。「驚嘆號只是命名慣例」則是這題最想破的誤解:驚嘆號是語法的一部分,它告訴編譯器「先把這一段展開,再來檢查」。至於 {lang} 會照字面印出:格式字串是在編譯期被解析的,那時候變數名稱還看得到,{lang} 就是直接取用作用域內的 lang。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let lang = \"Rust\";", note: "一個 &str 變數,等一下會被格式字串直接用名字取用。" },
          { code: "    println!(\"{} + {} = {}\", 1, 2, 1 + 2);", note: "格式字串裡有三個 {},後面就要剛好三個引數。巨集在編譯期數出這件事並逐一配對,印出 1 + 2 = 3。" },
          { code: "    println!(\"hello\");", note: "同一個 println!,這次只有格式字串、沒有任何 {}——對函式來說這是另一個簽名,對巨集來說只是另一串 token。印出 hello。" },
          { code: "    println!(\"{lang} {}\", 2021);", note: "{lang} 在編譯期被解析成「作用域內名叫 lang 的變數」,剩下那個 {} 依序對應 2021。印出 Rust 2021。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "把巨集想成「編譯器的外掛前處理」:它先拿到你寫下的 token,依規則產生一段普通的 Rust 程式碼,之後的型別檢查、借用檢查全部照常進行。本課後面的題目都是在看這個「先展開、再檢查」的流程。",
      },
      csharp: `C# 的 Console.WriteLine 靠兩樣語言功能才能收不同數量的引數:多載(WriteLine(string)、WriteLine(string, object)⋯)加上 params object[]。內插字串 $"{lang} {year}" 則是由編譯器降轉成 DefaultInterpolatedStringHandler 的一連串 Append 呼叫。Rust 這兩樣都沒有——沒有 params、沒有多載——所以同樣的需求由巨集承擔:在編譯期把呼叫處改寫成參數固定的程式碼。好處是沒有 object[] 的配置與裝箱,格式與引數的對應錯誤也在編譯期就被擋下。`,
    },
    {
      id: "2-11-02",
      question: "巨集呼叫可以用三種括號。以下程式碼的結果是?",
      questionCode: "fn main() {\n    let a = vec![1, 2, 3];\n    let b = vec!(1, 2, 3);\n    let c = vec! { 1, 2, 3 };\n    println![\"{}\", a == b && b == c];\n}",
      options: [
        { text: "印出 true——對巨集來說 ()、[]、{} 三種括號完全等價,巨集收到的只是括號裡的 token;習慣上 vec! 用方括號(像陣列)、println! 用小括號(像函式呼叫),但那只是慣例" },
        { text: "編譯錯誤:vec! 只接受方括號,因為它的展開規則是照著陣列語法寫的" },
        { text: "印出 false——大括號版本會被當成一個區塊,c 拿到的是區塊最後的值 3,不是 Vec" },
        { text: "編譯錯誤:println! 只能用小括號,方括號會被解析成對 println 做索引" },
      ],
      answer: 0,
      explanation: `巨集呼叫的語法是「名字 + 驚嘆號 + 一對括號」,括號可以是 ()、[]、{} 任一種,巨集定義那一側完全看不到你用的是哪一種——它收到的只有括號「裡面」的 token。所以 a、b、c 是三個一模一樣的 Vec<i32>,println! 用方括號呼叫也照樣能用,印出 true。
「vec! 只接受方括號」是最常見的誤解,因為大家從第一天起看到的都是 vec![...]。那是社群慣例:讓它看起來像陣列字面值。同理 println!(...) 像函式呼叫、而會定義一整塊東西的巨集(例如本課後面自己寫的 macro_rules! 本身)習慣用大括號。「大括號會被當成區塊」也不對:驚嘆號後面緊接的那組括號屬於巨集呼叫語法,不是區塊運算式。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let a = vec![1, 2, 3];", note: "慣用寫法:方括號,讀起來像陣列字面值。" },
          { code: "    let b = vec!(1, 2, 3);", note: "換成小括號。vec! 收到的 token 仍然是 1, 2, 3,展開結果完全相同。" },
          { code: "    let c = vec! { 1, 2, 3 };", note: "換成大括號,中間還多了空白——一樣合法,一樣展開成 Vec<i32>。" },
          { code: "    println![\"{}\", a == b && b == c];", note: "連 println! 也能用方括號呼叫。三個 Vec 內容相同,印出 true。" },
          { code: "}", note: "main 結束,三個 Vec 依序釋放。" },
        ],
        outro: "三種括號唯一的語法差異出現在「巨集呼叫本身當成一個敘述或項目」時:用 {} 呼叫的結尾不用加分號,用 () 或 [] 呼叫的要加。展開規則與功能則完全不受括號種類影響。",
      },
      csharp: `C# 沒有巨集呼叫語法,所以沒有直接對應。可以帶走的觀念是:在 Rust 裡,驚嘆號才是「這裡是巨集」的標記,括號種類不是。讀別人的程式碼看到 thing! { ... } 或 thing![ ... ] 不用去猜它有什麼特殊意義——那是作者依慣例挑的外觀,展開規則由巨集定義決定。`,
    },
    {
      id: "2-11-03",
      question: "macro_rules! 的比對對象是什麼?以下程式碼的結果是?",
      questionCode: "macro_rules! describe {\n    (zero) => {\n        \"the word zero\"\n    };\n    ($x:expr) => {\n        \"some expression\"\n    };\n}\n\nfn main() {\n    let zero = 0;\n    println!(\"{}\", describe!(zero));\n    println!(\"{}\", describe!(0));\n    println!(\"{}\", zero);\n}",
      options: [
        { text: "依序印出 the word zero、some expression、0——macro_rules! 比對的是呼叫時寫下的 token:第一條規則要求字面上出現 zero 這個識別字,和變數 zero 的值是多少毫無關係;規則由上往下試,第一條比對成功的就被採用" },
        { text: "依序印出 some expression、some expression、0——describe!(zero) 會先把 zero 求值成 0,結果跟 describe!(0) 一樣走第二條規則" },
        { text: "編譯錯誤:zero 同時符合第一條規則和第二條的 $x:expr,兩條規則都命中,巨集無法決定要用哪一條" },
        { text: "編譯錯誤:巨集規則裡的 zero 和 main 裡的變數 zero 同名,產生衝突" },
      ],
      answer: 0,
      explanation: `macro_rules! 長得很像 match:一組「模式 => 展開結果」,由上往下試。但它比對的東西完全不同——match 比對的是執行期的「值」,macro_rules! 比對的是編譯期的「語法片段」,也就是呼叫時字面上寫下的 token。第一條規則的模式是 (zero),意思是「括號裡剛好只有一個叫 zero 的識別字」。describe!(zero) 字面上就是這樣,所以命中第一條;describe!(0) 的 token 是字面值 0,不是識別字 zero,第一條不中,往下試第二條 $x:expr,成功。
「先把 zero 求值成 0」是把巨集當成函式的誤解——巨集展開時程式還沒開始執行,根本沒有「值」可以看。「兩條都命中所以有歧義」也不對:和 match 一樣,規則是依序嘗試,第一條成功就停,不會再往下看。這也代表規則的順序很重要,把 $x:expr 那條放在最前面,(zero) 那條就永遠輪不到。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "macro_rules! describe {", note: "定義一個叫 describe 的巨集。大括號裡是一條條規則,每條都是「模式 => 展開結果」。" },
          { code: "    (zero) => {", note: "第一條規則:括號裡必須剛好是 zero 這個識別字。沒有 $,所以它是字面比對,不是捕獲參數。" },
          { code: "        \"the word zero\"", note: "命中時展開成這個字串字面值。" },
          { code: "    };", note: "第一條規則結束,規則之間用分號分隔。" },
          { code: "    ($x:expr) => {", note: "第二條規則:接受任何一個運算式,捕獲成 $x(這裡沒有用到它)。" },
          { code: "        \"some expression\"", note: "命中時展開成另一個字串字面值。" },
          { code: "    };", note: "第二條規則結束。" },
          { code: "}", note: "巨集定義結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let zero = 0;", note: "一個變數剛好也叫 zero,值是 0。巨集展開時完全不會看它的值。" },
          { code: "    println!(\"{}\", describe!(zero));", note: "呼叫處的 token 是識別字 zero,第一條規則字面比對成功,展開成 \"the word zero\"。" },
          { code: "    println!(\"{}\", describe!(0));", note: "token 是字面值 0,不是識別字 zero,第一條失敗;第二條 $x:expr 能接住 0,展開成 \"some expression\"。" },
          { code: "    println!(\"{}\", zero);", note: "這裡才是真正讀取變數 zero 的值,印出 0。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "一句話分辨:match 在執行期看「值是什麼」,macro_rules! 在編譯期看「你寫了什麼」。變數叫什麼名字、值是多少,對巨集來說是兩件無關的事。",
      },
      csharp: `C# 沒有直接對應的機制。最接近的 Source Generator 也在編譯期處理語法樹,但它是另外寫一個分析器組件去「讀整個專案、生成新檔案」,不會在呼叫處對一小段 token 做模式比對。對 C# 開發者最重要的心智切換是:switch 運算式和 Rust 的 match 都在執行期看值,macro_rules! 雖然長得像,卻完全活在編譯期。`,
    },
    {
      id: "2-11-04",
      question: "$x:expr 片段指定子捕獲了什麼?以下程式碼的結果是?",
      questionCode: "macro_rules! double {\n    ($x:expr) => {\n        $x * 2\n    };\n}\n\nfn main() {\n    let n = double!(1 + 2);\n    println!(\"{}\", n);\n}",
      options: [
        { text: "印出 6——$x:expr 把 1 + 2 當成「一整個運算式」捕獲,展開時它是語法樹上的一個節點,效果等同 (1 + 2) * 2,不會被旁邊的 * 拆開" },
        { text: "印出 5——巨集是文字替換,展開後變成 1 + 2 * 2,乘法先算" },
        { text: "編譯錯誤:$x:expr 只能接單一個值(字面值或變數),1 + 2 有三個 token,要先存進變數再傳進巨集" },
        { text: "印出 6——呼叫巨集之前 1 + 2 會先被執行求值,巨集收到的是數值 3" },
      ],
      answer: 0,
      explanation: `片段指定子 expr 的意思是「這裡要一個完整的運算式」。巨集比對時會把 1 + 2 解析成一棵運算式語法樹,整棵捕獲成 $x;展開成 $x * 2 時,是把這棵樹整個放進乘法的左邊,所以結果等同 (1 + 2) * 2 = 6,而不是字串拼接成 1 + 2 * 2。
「印出 5」是 C/C++ #define 的經典陷阱——#define DOUBLE(x) x * 2 真的會得到 5,所以 C 程式設計師要養成在巨集裡到處加括號的習慣。Rust 的 macro_rules! 操作的是語法片段不是文字,這個陷阱不存在。「先執行求值、巨集收到 3」結果一樣是 6,但理由錯了:巨集收到的是「1 + 2 這段程式碼」,不是它的值。差別在 $x 出現兩次時就看得出來——如果本體寫成 $x + $x 而你傳入的是一個函式呼叫,那個函式會被呼叫兩次,因為程式碼被貼了兩份。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "macro_rules! double {", note: "定義 double 巨集。" },
          { code: "    ($x:expr) => {", note: "唯一一條規則:捕獲一個運算式,名字叫 $x。冒號後面的 expr 就是片段指定子,限定這個位置能接受哪一種語法。" },
          { code: "        $x * 2", note: "展開結果:把整個 $x 當成乘法的左運算元。$x 是一個完整的語法節點,不是一串文字。" },
          { code: "    };", note: "規則結束。" },
          { code: "}", note: "巨集定義結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let n = double!(1 + 2);", note: "1 + 2 被解析成一個加法運算式並整個捕獲。展開後相當於 let n = (1 + 2) * 2;,n 是 6。" },
          { code: "    println!(\"{}\", n);", note: "印出 6。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "常用的片段指定子還有 ident(識別字)、ty(型別)、literal(字面值)、tt(單一 token 樹)。每一種都規定「這個位置能放什麼語法」,下一題就來看傳錯種類會發生什麼事。",
      },
      csharp: `C# 的前處理器只有 #define SYMBOL 這種無參數符號,專門給 #if 用,不能寫成 C 那種帶參數的巨集——所以 C# 開發者沒踩過 DOUBLE(1 + 2) 得到 5 的坑,但很可能從 C/C++ 的經驗裡聽過。這題要帶走的是:Rust 的巨集不屬於那一脈。它的參數是型別化的語法片段(expr、ident、ty⋯),展開後保留原本的語法結構,不需要靠到處加括號自保。`,
    },
    {
      id: "2-11-05",
      question: "片段指定子不符合時會發生什麼事?以下程式碼能編譯嗎?",
      questionCode: "macro_rules! make_getter {\n    ($name:ident, $value:expr) => {\n        fn $name() -> i32 {\n            $value\n        }\n    };\n}\n\nmake_getter!(\"answer\", 42);\n\nfn main() {\n    println!(\"{}\", answer());\n}",
      options: [
        { text: "編譯錯誤:no rules expected `\"answer\"`——$name:ident 要求一個識別字,\"answer\" 是字串字面值;比對在展開之前就失敗了,巨集本體根本沒有被產生出來" },
        { text: "可以編譯,印出 42——巨集會把字串的內容 answer 取出來當成函式名稱" },
        { text: "編譯錯誤,但錯誤發生在展開後的 fn \"answer\"() 這一行,訊息是函式名稱不合法" },
        { text: "可以編譯,但產生出來的函式名稱是 \"answer\"(含引號),要用反射之類的方式才能呼叫" },
      ],
      answer: 0,
      explanation: `巨集的處理分兩步:先「比對」,再「展開」。比對階段會拿呼叫處的 token 去套每條規則的模式,片段指定子就是這一步的型別檢查——$name:ident 規定這個位置必須是識別字(像 answer、total 這種名字),字串字面值 "answer" 不符合,於是這條規則不中;沒有其他規則可試,編譯器回報 no rules expected,並用 note 指出它是在比對 $name:ident 時卡住的。
「錯誤發生在展開後那一行」是把巨集想成函式的誤解:函式是「先收參數、再在本體裡發現型別不對」,巨集則是連本體都還沒產生就被擋在門外。這其實是好事——錯誤訊息直接指向你呼叫巨集的那一行,而不是一段你看不到的展開結果。「把字串內容取出來當名字」也不會發生:macro_rules! 不會解析或改寫字串字面值的內容。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "macro_rules! make_getter {", note: "定義一個會「產生函式」的巨集——巨集的展開結果不一定是運算式,也可以是一整個項目。" },
            { code: "    ($name:ident, $value:expr) => {", note: "模式:第一個片段必須是識別字,接一個逗號,再接一個運算式。" },
            { code: "        fn $name() -> i32 {", note: "展開時用 $name 當函式名稱——所以它必須是合法的識別字。" },
            { code: "            $value", note: "函式本體直接回傳捕獲到的運算式。" },
            { code: "        }", note: "函式結束。" },
            { code: "    };", note: "規則結束。" },
            { code: "}", note: "巨集定義結束。" },
            { code: "", note: "" },
            { code: "make_getter!(\"answer\", 42);", note: "⛔ 編譯失敗:no rules expected `\"answer\"`(附註 while trying to match meta-variable `$name:ident`)。\"answer\" 是字串字面值,不是識別字,比對階段就被拒絕,巨集本體沒有展開。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    println!(\"{}\", answer());", note: "即使上一行過了,這裡也找不到 answer 函式——不過編譯器在上一行就停下來了。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "舊版編譯器的措辭是 no rules expected the token `\"answer\"`,意思相同。看到 no rules expected,就知道問題出在「比對」這一步:回頭檢查呼叫處的每個片段是否符合模式裡的指定子。",
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "macro_rules! make_getter {", note: "巨集定義完全不用改。" },
            { code: "    ($name:ident, $value:expr) => {", note: "同樣的模式。" },
            { code: "        fn $name() -> i32 {", note: "同樣的展開。" },
            { code: "            $value", note: "回傳捕獲到的運算式。" },
            { code: "        }", note: "函式結束。" },
            { code: "    };", note: "規則結束。" },
            { code: "}", note: "巨集定義結束。" },
            { code: "", note: "" },
            { code: "make_getter!(answer, 42);", note: "改動處:拿掉引號,傳入識別字 answer。比對成功,展開成 fn answer() -> i32 { 42 }。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    println!(\"{}\", answer());", note: "呼叫巨集產生出來的函式,印出 42。" },
            { code: "}", note: "main 結束。" },
          ],
        },
      ],
      csharp: `C# 呼叫方法時,引數是在語意分析階段才對照參數型別的,錯誤指向的是「這個引數的型別不對」。Rust 巨集多了一個 C# 沒有的中間步驟:比對。在比對階段,檢查的不是型別而是「語法種類」——這裡該放一個名字、一個運算式還是一個型別。C# 開發者可以把片段指定子想成「參數的語法型別」,它比一般型別檢查更早發生。`,
    },
    {
      id: "2-11-06",
      question: "重複模式的分隔符。以下程式碼能編譯嗎?",
      questionCode: "macro_rules! sum {\n    ($($x:expr),*) => {\n        0 $(+ $x)*\n    };\n}\n\nfn main() {\n    let total = sum!(1, 2 3);\n    println!(\"{}\", total);\n}",
      options: [
        { text: "編譯錯誤:no rules expected `3`——模式 $($x:expr),* 規定片段之間一定要用逗號隔開,2 和 3 之間少了逗號,比對失敗(編譯器還會提示 missing comma here)" },
        { text: "可以編譯,印出 6——逗號只是排版上的建議,巨集會自動用空白把片段切開" },
        { text: "可以編譯,印出 3——2 3 被當成同一個片段,取最後一個值" },
        { text: "編譯錯誤:巨集本體的 0 $(+ $x)* 不是合法的運算式,重複展開只能用在陳述式裡" },
      ],
      answer: 0,
      explanation: `$( ... ),* 由三個部分組成:括號裡是要重複的模式($x:expr),括號後面緊接的逗號是分隔符,最後的 * 表示「零次或多次」。分隔符是模式的一部分,不是裝飾:比對器讀完 2 這個運算式之後,下一個 token 只能是逗號(繼續重複)或結尾(重複結束),它看到的卻是 3,兩條路都走不通,於是回報 no rules expected。
「逗號只是排版建議」是這題的核心誤解。巨集比對是嚴格的語法比對,模式裡寫了什麼 token,呼叫處就要出現什麼 token。「2 3 被當成同一個片段」也不可能:2 3 不是一個合法的運算式,expr 片段在 2 就結束了。至於本體 0 $(+ $x)*——它完全合法,$( ... )* 在展開端一樣能用,而且不需要分隔符,每次重複就產生一段「+ 某個運算式」。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "macro_rules! sum {", note: "定義一個加總巨集。" },
            { code: "    ($($x:expr),*) => {", note: "模式:零個或多個運算式,彼此之間用逗號分隔。逗號寫在重複括號的外面、* 的前面,它就是分隔符。" },
            { code: "        0 $(+ $x)*", note: "展開:先寫一個 0,再對每個捕獲到的 $x 各產生一段 + $x。展開端的重複沒有分隔符。" },
            { code: "    };", note: "規則結束。" },
            { code: "}", note: "巨集定義結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let total = sum!(1, 2 3);", note: "⛔ 編譯失敗:no rules expected `3`(help: missing comma here)。比對到 2 之後,模式要求下一個是逗號或結尾,卻遇到 3。" },
            { code: "    println!(\"{}\", total);", note: "不會執行到這裡。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "macro_rules! sum {", note: "巨集定義不用改。" },
            { code: "    ($($x:expr),*) => {", note: "同樣的模式。" },
            { code: "        0 $(+ $x)*", note: "同樣的展開。" },
            { code: "    };", note: "規則結束。" },
            { code: "}", note: "巨集定義結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let total = sum!(1, 2, 3);", note: "改動處:補上 2 和 3 之間的逗號。三個片段都比對成功。" },
            { code: "    println!(\"{}\", total);", note: "展開後是 0 + 1 + 2 + 3,印出 6。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "△ Desugared (what the compiler sees)",
          intro: "重複模式的比對與展開是一對一對應的(AUTHORING §9 #55):比對端每抓到一個片段,展開端就把重複區塊產生一次。以修正後的呼叫為例:",
          lines: [
            { code: "let total = sum!(1, 2, 3);", note: "你寫的這一行。" },
            { code: "", note: "" },
            { code: "// 比對:$x 依序捕獲 1、2、3", note: "每抓到一個 $x,就必須看到一個逗號或結尾——少一個逗號就在這一步失敗。" },
            { code: "// 展開:0 $(+ $x)* 對每個 $x 各產生一次 + $x", note: "展開端的 $( ... )* 會重複「捕獲到幾次」就幾次。" },
            { code: "let total = 0 + 1 + 2 + 3;", note: "編譯器最後看到的程式碼。之後才進入一般的型別檢查。" },
          ],
        },
      ],
      csharp: `C# 的 params int[] 在呼叫端一樣要用逗號分隔引數,少了逗號是語法錯誤——這一點兩邊直覺一致。不同的是,在 Rust 裡「用逗號分隔」這條規則不是語言寫死的,而是巨集作者在模式裡自己選的:寫成 $( ... );* 就改用分號分隔,寫成 $( ... )* 就完全不要分隔符。所以讀一個陌生巨集時,要看它的模式才知道該怎麼呼叫。`,
    },
    {
      id: "2-11-07",
      question: "設計選擇:你要寫一個 max! 巨集,回傳所有引數中的最大值;「零個引數」沒有合理的答案。下面兩種模式寫法,哪一種較好?",
      questionCode: "// 寫法一:$(...),*\n($($x:expr),*) => {{\n    let mut m = i32::MIN;\n    $( if $x > m { m = $x; } )*\n    m\n}};\n\n// 寫法二:$(...),+\n($($x:expr),+) => {{\n    let mut m = i32::MIN;\n    $( if $x > m { m = $x; } )+\n    m\n}};",
      options: [
        { text: "寫法二。$(...),+ 要求至少一個片段,max!() 會在比對階段就被拒絕(unexpected end of macro invocation);寫法一的 * 允許零個,max!() 會安靜地展開成只剩 i32::MIN 的區塊,把「呼叫錯了」變成一個錯誤的答案" },
        { text: "兩者完全等價,* 和 + 只是不同作者的符號習慣" },
        { text: "寫法一。* 能接受的呼叫比較多,巨集越寬鬆越好用,零個引數時回傳 i32::MIN 也算合理" },
        { text: "寫法二,但原因是 + 會讓重複展開的片段之間自動用加號串起來,* 則是用乘號" },
      ],
      answer: 0,
      explanation: `* 是「零次或多次」,+ 是「一次或多次」,這是兩者唯一的差別,但它決定了 max!() 這種呼叫的命運。用寫法一,max!() 會比對成功(零個片段也算數),展開成 { let mut m = i32::MIN; m },編譯通過、執行時回傳 -2147483648——一個看起來像答案的錯誤值。用寫法二,max!() 直接在比對階段失敗,錯誤指向呼叫處。
「兩者等價」只在「至少傳一個引數」的情況下成立,而巨集的設計恰恰要考慮被誤用的情況。「巨集越寬鬆越好用」則是這題想破的直覺:當某種呼叫沒有合理語意時,讓它編譯失敗,比讓它安靜地產生垃圾值好得多。反過來說,sum! 這種巨集零個引數有合理答案(0),用 * 就很自然。至於「+ 代表用加號串起來」:重複運算子 * + ? 只表示次數,跟算術完全無關。`,
      walkthrough: {
        label: "⊕ Correct design — walkthrough (runs successfully)",
        lines: [
          { code: "macro_rules! sum {", note: "對照組:加總巨集。" },
          { code: "    ($($x:expr),*) => {", note: "用 *:零個引數時的總和是 0,這是有意義的答案,所以允許。" },
          { code: "        0 $(+ $x)*", note: "零個片段時展開成單純的 0。" },
          { code: "    };", note: "規則結束。" },
          { code: "}", note: "sum! 定義結束。" },
          { code: "", note: "" },
          { code: "macro_rules! max {", note: "本題的主角。" },
          { code: "    ($($x:expr),+) => {{", note: "用 +:至少要一個片段。外層大括號是巨集規則的語法,內層大括號讓展開結果成為一個區塊運算式。" },
          { code: "        let mut m = i32::MIN;", note: "從 i32 能表示的最小值開始比。" },
          { code: "        $(", note: "展開端的重複區塊開始:每個捕獲到的 $x 各產生一次下面的 if。" },
          { code: "            if $x > m {", note: "目前這個引數比較大就更新。" },
          { code: "                m = $x;", note: "記下新的最大值。" },
          { code: "            }", note: "if 結束。" },
          { code: "        )+", note: "重複區塊結束。展開端的重複運算子要跟比對端一致。" },
          { code: "        m", note: "區塊的最後一個運算式就是 max! 的結果。" },
          { code: "    }};", note: "規則結束。" },
          { code: "}", note: "max! 定義結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    println!(\"{}\", sum!());", note: "零個引數,展開成 0,印出 0。" },
          { code: "    println!(\"{}\", sum!(4, 9, 2));", note: "展開成 0 + 4 + 9 + 2,印出 15。" },
          { code: "    println!(\"{}\", max!(4, 9, 2));", note: "三個 if 依序比較,印出 9。如果在這裡加一行 max!(),編譯器會回報 unexpected end of macro invocation,指向那一行。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "另一種同樣常見的寫法是把「第一個」單獨拆出來:($first:expr $(, $rest:expr)*),用 $first 當初始值,就連 i32::MIN 都不需要了。兩種寫法的共同點是:讓「沒有意義的呼叫」在比對階段就失敗。",
      },
      csharp: `C# 的 params int[] 永遠允許零個引數,想強制「至少一個」要改寫簽名:int Max(int first, params int[] rest)。這正好對應 Rust 的兩種做法——$(...),+,或是把第一個片段拆出來寫成 $first:expr $(, $rest:expr)*。差別在於 C# 的 Enumerable.Max() 對空序列是執行期丟 InvalidOperationException,而 Rust 巨集可以把同樣的誤用擋在編譯期。`,
    },
    {
      id: "2-11-08",
      question: "vec! 展開後是什麼?以下程式碼的結果是?",
      questionCode: "fn main() {\n    let a = vec![1, 2, 3];\n    let mut b = Vec::new();\n    b.push(1);\n    b.push(2);\n    b.push(3);\n    let z = vec![0; 3];\n    println!(\"{:?} {:?} {}\", a, z, a == b);\n}",
      options: [
        { text: "印出 [1, 2, 3] [0, 0, 0] true——vec![1, 2, 3] 的效果等同先 Vec::new() 再依序 push 三次,產生的就是一般的 Vec<i32>;vec![0; 3] 是另一條規則,把 0 複製成 3 個元素" },
        { text: "編譯錯誤:vec![1, 2, 3] 產生的是固定長度陣列 [i32; 3],不能拿來跟 Vec<i32> 用 == 比較" },
        { text: "印出 [1, 2, 3] [0, 3] true——vec! 把分號也當成元素之間的分隔符" },
        { text: "印出 [1, 2, 3] [0, 0, 0] false——vec! 建立的 Vec 一次就配置好剛好 3 格容量,b 則是逐步擴充的,兩者內部狀態不同所以不相等" },
      ],
      answer: 0,
      explanation: `vec! 是一個有多條規則的巨集:vec![a, b, c] 走「逗號分隔的元素清單」那條,vec![elem; n] 走「重複 n 次」那條,就像陣列字面值 [0; 3] 的寫法。概念上 vec![1, 2, 3] 就等於「建一個空 Vec,依序 push 1、2、3」,展開出來的是一個普普通通的 Vec<i32>,跟手動 push 的 b 是同一個型別、同樣的內容,所以 == 是 true。
「兩者內部狀態不同所以不相等」這個選項混了一半事實:標準庫的實際實作確實不是逐一 push,而是先把元素放進一塊剛好大小的 heap 配置再轉成 Vec(AUTHORING §9 #23 的「實際是 Box<[T]> 轉換」),所以 a 的容量可能跟 b 不同。但 Vec 的 == 只比較元素,不看容量,結果仍然是 true。「產生固定長度陣列」則是把 vec![...] 跟 [...] 搞混——vec! 的全部意義就是給你一個 heap 上、可以成長的 Vec。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let a = vec![1, 2, 3];", note: "逗號清單形式。結果是 Vec<i32>,內容 1、2、3。" },
          { code: "    let mut b = Vec::new();", note: "手動建立一個空 Vec,元素型別由後面的 push 反推為 i32。" },
          { code: "    b.push(1);", note: "放入第一個元素。" },
          { code: "    b.push(2);", note: "第二個。" },
          { code: "    b.push(3);", note: "第三個。現在 b 的內容跟 a 相同——這就是 vec![1, 2, 3] 在概念上幫你寫掉的那幾行。" },
          { code: "    let z = vec![0; 3];", note: "重複形式:把 0 複製 3 次(元素型別要能 Clone),內容是 0、0、0。" },
          { code: "    println!(\"{:?} {:?} {}\", a, z, a == b);", note: "== 逐一比較元素,與容量無關。印出 [1, 2, 3] [0, 0, 0] true。" },
          { code: "}", note: "三個 Vec 依序釋放各自的 heap 空間。" },
        ],
        outro: "把 vec! 當成「Vec 的字面值寫法」就好:它產生的是一般的 Vec,之後要 push、要借用、要傳進函式都跟手動建立的沒有任何差別。第 19 題會自己寫一個簡化版,看看這種「逗號清單」規則是怎麼寫出來的。",
      },
      csharp: `C# 的集合初始設定式 new List<int> { 1, 2, 3 } 就是同一個概念,而且編譯器真的把它降轉成 new List<int>() 之後連續三次 Add 呼叫;C# 12 的集合運算式 [1, 2, 3] 則可能直接配置剛好大小的底層陣列。Rust 的 vec! 位置相同:它是「產生 Vec 的簡寫」,只是由標準庫用巨集實作,而不是寫死在語言裡的語法。`,
    },
    {
      id: "2-11-09",
      question: "format! 的格式字串什麼時候被解析?以下程式碼的結果是?",
      questionCode: "fn main() {\n    let w = 3;\n    let h = 4;\n    let s = format!(\"{}x{}\", w, h);\n    let t = format!(\"{w}x{h}={}\", w * h);\n    println!(\"{s} | {t}\");\n}",
      options: [
        { text: "印出 3x4 | 3x4=12——格式字串在編譯期就被拆解:{w}、{h} 直接對應作用域內的同名變數,剩下的 {} 依序對應後面的位置引數 w * h;對應關係與型別能否格式化都在編譯期確定" },
        { text: "印出 3x4 | {w}x{h}=12——在格式字串裡直接寫變數名稱只有 println! 支援,format! 會照字面輸出" },
        { text: "編譯錯誤:同一個格式字串不能混用具名的 {w} 和位置的 {}" },
        { text: "印出 3x4 | 3x4=12,但格式字串是在執行期才解析的,只是這次剛好每個 {} 都對上了" },
      ],
      answer: 0,
      explanation: `format! 和 println! 共用同一套格式化機制,差別只在結果是回傳一個 String 還是印到 stdout。格式字串必須是字面值,巨集在編譯期把它拆開:{w} 這種寫法會捕獲作用域內的同名變數,不帶名字的 {} 則依序對應後面的位置引數。混用完全合法,t 的 {} 對應的是 w * h,所以是 3x4=12。
「格式字串在執行期解析、這次剛好對上」是最需要破的誤解,因為它讓人以為對不上時會有執行期錯誤——實際上對不上的程式根本編譯不過(下一題)。這也是格式字串必須是字面值的原因:如果它是執行期才知道內容的變數,巨集就沒辦法在編譯期檢查它。另外注意 {w * h} 不合法——捕獲語法只接受單一識別字,運算式要寫成位置引數,這正是本題刻意把 w * h 放在後面的原因。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let w = 3;", note: "寬度。" },
          { code: "    let h = 4;", note: "高度。" },
          { code: "    let s = format!(\"{}x{}\", w, h);", note: "位置引數:兩個 {} 依序對應 w、h。format! 回傳一個新配置的 String,內容是 3x4。" },
          { code: "    let t = format!(\"{w}x{h}={}\", w * h);", note: "混用:{w}、{h} 在編譯期被解析成捕獲同名變數,唯一一個不帶名字的 {} 對應位置引數 w * h。內容是 3x4=12。" },
          { code: "    println!(\"{s} | {t}\");", note: "println! 也用同樣的捕獲語法,印出 3x4 | 3x4=12。" },
          { code: "}", note: "s 和 t 兩個 String 釋放。" },
        ],
        outro: "format!、println!、eprintln!、write!、panic! 全部共用這一套格式字串規則,學會一個就等於全部學會。",
      },
      csharp: `C# 的內插字串 $"{w}x{h}={w * h}" 同樣在編譯期處理,而且大括號裡可以放任意運算式,這一點比 Rust 的捕獲語法寬鬆。但另一條老路 string.Format("{0}x{1}={2}", w, h) 的格式字串就只是一般字串,索引與引數數量對不上要到執行期才丟 FormatException(較新的 .NET 分析器會對字面值格式字串發出警告,但那是額外的檢查)。Rust 只有一條路,而且這條路一律在編譯期檢查。`,
    },
    {
      id: "2-11-10",
      question: "格式字串的 {} 數量和引數數量對不上。以下程式碼的結果是?",
      questionCode: "fn main() {\n    let name = \"Ferris\";\n    let age = 7;\n    println!(\"{} is {} years old, {}\", name, age);\n}",
      options: [
        { text: "編譯錯誤:3 positional arguments in format string, but there are 2 arguments——{} 的數量和引數的數量在編譯期就被比對,少一個就不讓你編譯" },
        { text: "可以編譯,印出 Ferris is 7 years old, ——多出來的 {} 會印成空字串" },
        { text: "可以編譯,但執行到這一行時 panic,訊息是引數不足" },
        { text: "可以編譯,多出來的 {} 會重複使用最後一個引數,印出 Ferris is 7 years old, 7" },
      ],
      answer: 0,
      explanation: `格式字串裡有三個不帶名字的 {},後面卻只給了兩個引數。println! 在編譯期就把兩邊數清楚,對不上直接拒絕編譯,錯誤訊息把格式字串裡的三個 {} 和兩個引數都標出來給你看。反方向也一樣:引數比 {} 多,會得到 argument never used——多餘的引數不會被默默丟掉。
「多出來的 {} 印成空字串」和「重複使用最後一個引數」都是替執行期行為找合理化的猜測,但這裡根本沒有執行期:格式化引擎不會在程式跑起來之後才去數 {}。「執行到這一行時 panic」則是 C# string.Format 的行為(FormatException),正是 Rust 選擇用巨集做格式化想要消滅的那一類錯誤。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let name = \"Ferris\";", note: "第一個要印的值。" },
            { code: "    let age = 7;", note: "第二個要印的值。" },
            { code: "    println!(\"{} is {} years old, {}\", name, age);", note: "⛔ 編譯失敗:3 positional arguments in format string, but there are 2 arguments。三個 {} 只對到兩個引數,第三個 {} 沒有對象。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let name = \"Ferris\";", note: "不變。" },
            { code: "    let age = 7;", note: "不變。" },
            { code: "    let species = \"a crab\";", note: "改動處:補上第三個 {} 真正想印的東西。" },
            { code: "    println!(\"{} is {} years old, {}\", name, age, species);", note: "改動處:三個 {} 對三個引數,印出 Ferris is 7 years old, a crab。另一種修法是把多寫的那個 {} 刪掉。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "反方向的錯誤也會被抓:println!(\"{} is {}\", name, age, species) 會報 argument never used,指出 species 沒有對應的 {}。兩個方向都在編譯期擋下。",
        },
      ],
      csharp: `C# 的 string.Format("{0} is {1} years old, {2}", name, age) 可以正常編譯,執行到這一行才丟 FormatException: Index (zero based) must be greater than or equal to zero and less than the size of the argument list。改用內插字串 $"{name} is {age} years old" 可以避開,因為內插字串把值直接寫在洞裡,不存在「數量對不上」的問題。Rust 的 {name} 捕獲語法是同一個思路,而位置引數的寫法則靠巨集在編譯期數清楚。`,
    },
    {
      id: "2-11-11",
      question: "assert! 系列巨集。以下程式碼執行的結果是?",
      questionCode: "fn main() {\n    let v = vec![1, 2, 3];\n    assert!(v.len() == 3);\n    assert_ne!(v[0], v[1]);\n    println!(\"checks passed\");\n    assert_eq!(v[2], 4, \"third element should be 4\");\n    println!(\"done\");\n}",
      options: [
        { text: "先印出 checks passed,接著在 assert_eq! 那一行 panic,訊息包含自訂說明 third element should be 4,以及 left: 3、right: 4 兩個實際值;最後的 done 不會印出" },
        { text: "編譯錯誤:assert! 系列只能寫在 #[test] 函式裡,main 裡不能用" },
        { text: "先印出 checks passed,接著 panic,但訊息只有 assertion failed,看不到兩邊實際的值" },
        { text: "印出 checks passed 與 done——assert_eq! 只在 cargo test 時才檢查,一般執行時會被略過" },
      ],
      answer: 0,
      explanation: `assert!(條件) 在條件為 false 時 panic;assert_eq!(左, 右) 與 assert_ne!(左, 右) 則比較兩個值,不符合時 panic。前兩個檢查都成立(長度是 3、1 不等於 2),所以印出 checks passed;第三個檢查 v[2] 是 3 不是 4,於是 panic。assert_eq! 比單純的 assert!(v[2] == 4) 多做了一件事:它把左右兩邊的值都印出來(因此兩邊的型別要能用 {:?} 印),最後一個引數之後還能接格式字串當作自訂說明。
「只能寫在 #[test] 裡」是從 xUnit 帶過來的誤解——這些是一般的標準庫巨集,任何地方都能用,測試框架只是剛好大量使用它們。「一般執行時會被略過」則是把它跟 debug_assert! 搞混了:debug_assert! 系列才會在未開啟除錯斷言的建置(預設的 release)中被移除,assert! 系列永遠都會檢查。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (compiles; panics at the last assertion)",
        lines: [
          { code: "fn main() {", note: "程式進入點——注意這不是測試函式。" },
          { code: "    let v = vec![1, 2, 3];", note: "被檢查的資料。" },
          { code: "    assert!(v.len() == 3);", note: "條件成立,什麼都不做,繼續往下。" },
          { code: "    assert_ne!(v[0], v[1]);", note: "1 不等於 2,「不相等」的斷言成立,繼續往下。" },
          { code: "    println!(\"checks passed\");", note: "印出 checks passed。" },
          { code: "    assert_eq!(v[2], 4, \"third element should be 4\");", note: "3 不等於 4,panic。訊息是 assertion `left == right` failed: third element should be 4,下面兩行分別是 left: 3 和 right: 4。" },
          { code: "    println!(\"done\");", note: "panic 之後的程式碼不會執行,done 不會印出。" },
          { code: "}", note: "main 因為 panic 而沒有正常結束,程式以非零結束碼退出。" },
        ],
        outro: "終端機的實際輸出是:checks passed,接著 thread 'main' panicked at src/main.rs:6:5:,再接 assertion `left == right` failed: third element should be 4、left: 3、right: 4 三行。有了 left 與 right,不必另外加 println! 就知道差在哪。",
      },
      csharp: `xUnit 的 Assert.Equal(4, v[2]) 會印出 Expected: 4 / Actual: 3,這點 assert_eq! 很像;但 Assert 是測試框架的類別庫,離開測試專案就沒有了。C# 內建的 Debug.Assert 則只在 Debug 組態生效,Release 會整行消失——它對應的是 Rust 的 debug_assert!,不是 assert!。Rust 的 assert! 比較像「一行寫完的 if (!cond) throw」,在正式程式碼裡檢查不變式也很常見。`,
    },
    {
      id: "2-11-12",
      question: "matches! 能接受什麼?以下程式碼能編譯嗎?",
      questionCode: "fn main() {\n    let opt = Some(3);\n    let big = matches!(opt, Some(x) => x > 2);\n    println!(\"{}\", big);\n}",
      options: [
        { text: "編譯錯誤:no rules expected `=>`——matches! 只接受「運算式, 模式」,後面可以再接 if 條件,但不接受 match 臂的 => 本體;要加條件得寫成 matches!(opt, Some(x) if x > 2)" },
        { text: "可以編譯,印出 true——matches! 會把 => 右邊的運算式當成比對成功時的回傳值" },
        { text: "編譯錯誤:matches! 的模式裡不能綁定變數 x,只能寫 Some(_) 這種不綁定的模式" },
        { text: "編譯錯誤:matches! 的第一個引數必須是參考,要寫成 matches!(&opt, ...)" },
      ],
      answer: 0,
      explanation: `matches! 是一個把「match 只問對不對」這個常見需求縮成一行的巨集:matches!(值, 模式) 比對成功就是 true,否則 false,模式後面還可以加 if 條件(跟 match 臂的 guard 一樣)。它的規則裡沒有 => 這個 token 的位置,所以比對器讀到 => 就放棄,回報 no rules expected。
「把 => 右邊當回傳值」是把 matches! 想成縮寫版 match 的誤解——它的回傳值永遠是 bool,由巨集自己決定,不讓你寫。你想表達的「是 Some 而且裡面的值大於 2」,正確寫法是 guard:Some(x) if x > 2。「不能綁定變數」也不對:x 可以綁定,而且綁定之後能在 if 條件裡使用,只是綁定的名字不會流到 matches! 外面。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let opt = Some(3);", note: "要檢查的值,型別 Option<i32>。" },
            { code: "    let big = matches!(opt, Some(x) => x > 2);", note: "⛔ 編譯失敗:no rules expected `=>`。matches! 的規則在模式之後只接受結尾或 if 條件,沒有 => 的位置。" },
            { code: "    println!(\"{}\", big);", note: "不會執行到這裡。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let opt = Some(3);", note: "不變。" },
            { code: "    let big = matches!(opt, Some(x) if x > 2);", note: "改動處:把 => 換成 if,條件寫成 guard。x 在模式裡綁定,在 if 條件裡使用。" },
            { code: "    println!(\"{}\", big);", note: "3 大於 2,印出 true。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "△ Desugared (what the compiler sees)",
          intro: "錯誤訊息裡沒有陌生的識別字,但看一眼 matches! 的展開(AUTHORING §9 #24)就知道 => 為什麼不能由你來寫——它已經被巨集自己用掉了:",
          lines: [
            { code: "let big = matches!(opt, Some(x) if x > 2);", note: "你寫的這一行(修正後)。" },
            { code: "", note: "" },
            { code: "// 巨集把它展開成:", note: "以下是編譯器實際看到的程式碼。" },
            { code: "let big = match opt {", note: "matches! 本質上就是一個 match。" },
            { code: "    Some(x) if x > 2 => true,", note: "你的模式與 guard 被原封不動放進第一個臂,=> 右邊固定是 true。" },
            { code: "    _ => false,", note: "其他情況一律 false。" },
            { code: "};", note: "展開結束。兩個臂的 => 都由巨集寫好,留給你填的只有模式與 guard。" },
          ],
        },
      ],
      csharp: `C# 的 is 模式運算式就是 matches! 的同類:opt is > 2、shape is Circle { Radius: > 10 },結果同樣是 bool,也可以用 and、or 組合更多條件(例如 opt is > 2 and < 10)。C# 開發者看到 matches! 可以直接讀成「Rust 版的 is 模式」,記住兩點就好:模式寫在第二個引數,額外條件用 if 而不是 =>。`,
    },
    {
      id: "2-11-13",
      question: "todo!() 的型別是什麼?以下程式碼執行的結果是?",
      questionCode: "fn parse_port(s: &str) -> u16 {\n    if s.is_empty() {\n        return 8080;\n    }\n    todo!(\"parse {}\", s)\n}\n\nfn main() {\n    println!(\"{}\", parse_port(\"\"));\n    println!(\"{}\", parse_port(\"3000\"));\n}",
      options: [
        { text: "先印出 8080,第二次呼叫時 panic,訊息是 not yet implemented: parse 3000——todo!() 的型別是 never type !,能放在任何需要 u16 的位置,所以能編譯;但執行到它就 panic" },
        { text: "編譯錯誤:todo!() 的回傳型別是 (),和函式宣告的回傳型別 u16 不符" },
        { text: "先印出 8080,第二次呼叫印出 0——todo!() 會回傳該型別的預設值,讓程式能繼續跑" },
        { text: "編譯錯誤:含有 todo!() 的函式在實作完成前不能被呼叫" },
      ],
      answer: 0,
      explanation: `todo!() 展開後就是一個帶固定前綴訊息的 panic!(AUTHORING §9 #38),而 panic 永遠不會正常回傳,所以它的型別是 never type,寫作 !。! 可以被強制轉換成任何型別——它在型別檢查眼中「可以是 u16」,因為這個位置的值永遠不會真的被產生出來。於是 parse_port 能編譯,第一次呼叫走到 return 8080 正常回傳;第二次走到 todo!,以 not yet implemented: parse 3000 為訊息 panic。
「todo!() 的型別是 ()」是最常見的誤解,因為它看起來什麼都沒回傳;如果真是 (),這個函式就編譯不過了(下一題)。「回傳預設值讓程式繼續跑」則恰好相反:todo! 的設計目的就是大聲失敗,讓你不會忘記這裡還沒寫。同一族的還有 unimplemented!()(語氣是「刻意不實作」)與 panic!(),型別都是 !。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (compiles; panics on the second call)",
        lines: [
          { code: "fn parse_port(s: &str) -> u16 {", note: "宣告回傳 u16。" },
          { code: "    if s.is_empty() {", note: "已經寫好的部分:空字串用預設埠。" },
          { code: "        return 8080;", note: "這條路徑正常回傳 8080。" },
          { code: "    }", note: "if 結束。" },
          { code: "    todo!(\"parse {}\", s)", note: "還沒寫的部分。它是函式最後的運算式,型別是 !,能被當成 u16 接受,所以整個函式通過型別檢查。可以像 format! 一樣帶格式字串說明。" },
          { code: "}", note: "函式結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    println!(\"{}\", parse_port(\"\"));", note: "走已實作的分支,印出 8080。" },
          { code: "    println!(\"{}\", parse_port(\"3000\"));", note: "走到 todo!,panic,訊息是 not yet implemented: parse 3000。這一行的 println! 永遠不會印出東西。" },
          { code: "}", note: "因為 panic,main 沒有正常走到這裡。" },
        ],
      },
      csharp: `C# 的對應寫法是 throw new NotImplementedException();——它同樣能出現在任何回傳型別的方法裡,因為編譯器知道 throw 之後的程式碼到不了(C# 7 起 throw 也能當運算式用,例如 ?? throw ...)。Rust 把這個「到不了」正式寫進型別系統,取名 never type !,所以 todo!()、panic!()、無窮 loop、return 都能放在任何需要值的位置。概念相同,Rust 只是把它變成一個有名字的型別。`,
    },
    {
      id: "2-11-14",
      question: "寫到一半想先放個佔位符,讓其他部分能編譯。以下程式碼的結果與原因是?",
      questionCode: "fn perimeter(w: u32, h: u32) -> u32 {\n    2 * (w + h)\n}\n\nfn area(w: u32, h: u32) -> u32 {\n    ()\n}\n\nfn main() {\n    println!(\"{}\", perimeter(2, 3));\n}",
      options: [
        { text: "編譯錯誤:mismatched types,expected `u32`, found `()`——() 是 unit 型別的值,不是「什麼都不回傳」的萬用佔位符;要佔位應該寫 todo!(),它的型別 ! 能被當成 u32" },
        { text: "可以編譯,印出 10——area 從頭到尾沒有被呼叫,編譯器不檢查沒用到的函式" },
        { text: "可以編譯,印出 10——() 會被自動轉成 u32 的預設值 0" },
        { text: "編譯錯誤,而改成 Default::default() 或改成 todo!() 是完全等價的兩種修法" },
      ],
      answer: 0,
      explanation: `() 在 Rust 裡是一個真正的值,型別也叫 ()(unit)。area 宣告回傳 u32,本體卻產生一個 (),型別不符,編譯器回報 mismatched types。Rust 會型別檢查每一個函式,不管它有沒有被呼叫——「沒用到就不檢查」是 C++ 樣板或直譯語言的直覺,不適用於一般函式。
最值得細看的是「Default::default() 和 todo!() 等價」這個選項。Default::default() 確實能編譯——u32 有預設值 0——但那等於寫了一個「靜靜地回傳錯誤答案」的函式:哪天有人呼叫 area(2, 3),得到 0,不會有任何錯誤提示。todo!() 則是編譯通過、執行到就 panic,並且在訊息裡明說 not yet implemented。佔位符的目的是「提醒我這裡還沒寫」,所以該選會大聲失敗的那一個。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "fn perimeter(w: u32, h: u32) -> u32 {", note: "已經寫好的函式。" },
            { code: "    2 * (w + h)", note: "周長公式。" },
            { code: "}", note: "函式結束。" },
            { code: "", note: "" },
            { code: "fn area(w: u32, h: u32) -> u32 {", note: "還沒寫的函式,回傳型別宣告為 u32。" },
            { code: "    ()", note: "⛔ 編譯失敗:mismatched types,expected `u32`, found `()`。() 是 unit 型別的值,不能當成 u32。" },
            { code: "}", note: "函式結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    println!(\"{}\", perimeter(2, 3));", note: "只呼叫 perimeter,但 area 的型別錯誤一樣會讓整個程式編譯失敗。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "fn perimeter(w: u32, h: u32) -> u32 {", note: "不變。" },
            { code: "    2 * (w + h)", note: "不變。" },
            { code: "}", note: "函式結束。" },
            { code: "", note: "" },
            { code: "fn area(_w: u32, _h: u32) -> u32 {", note: "參數名加上底線前綴,告訴編譯器「暫時沒用到是故意的」,免得多出 unused variable 警告。" },
            { code: "    todo!()", note: "改動處:用 todo!() 佔位。型別是 !,能被當成 u32,編譯通過;真的被呼叫時會 panic 並印出 not yet implemented。" },
            { code: "}", note: "函式結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    println!(\"{}\", perimeter(2, 3));", note: "印出 10。編譯時會有一個 function `area` is never used 的警告——寫到一半的程式本來就會有,完成後自然消失。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "三種佔位方式的比較:() 編譯不過;Default::default() 編譯得過但安靜地回傳 0,是會被遺忘的地雷;todo!() 編譯得過,而且一旦被呼叫就明確失敗。",
        },
      ],
      csharp: `C# 對同樣的情況會報 CS0161: not all code paths return a value(本體完全空白時)。常見的兩種佔位寫法正好對應這題:return default; 能編譯,但靜靜地回傳 0,跟 Rust 的 Default::default() 一樣危險;throw new NotImplementedException(); 則對應 todo!()。Visual Studio 自動產生的方法骨架用的就是後者,理由完全相同——佔位符應該大聲失敗。`,
    },
    {
      id: "2-11-15",
      question: "dbg! 印了什麼、回傳了什麼?以下程式碼(位於 src/main.rs)執行的結果是?",
      questionCode: "fn main() {\n    let a = 2;\n    let b = dbg!(a * 3) + 1;\n    println!(\"{}\", b);\n}",
      options: [
        { text: "先在 stderr 印出 [src/main.rs:3:13] a * 3 = 6,再在 stdout 印出 7——dbg! 印出檔名、行號、欄位、原始運算式的文字與它的值,然後把值原封不動回傳,所以能嵌在運算式中間" },
        { text: "編譯錯誤:dbg! 跟 println! 一樣回傳 (),不能拿來做 + 1" },
        { text: "在 stdout 印出 6 與 7——dbg!(x) 就是 println!(\"{}\", x) 的簡寫" },
        { text: "印出同樣的兩行,但用 cargo build --release 建置時 dbg! 會被自動移除,只剩 7" },
      ],
      answer: 0,
      explanation: `dbg! 是專為「臨時插一行看看」設計的巨集:它把原始運算式的文字(a * 3)、所在的檔案與行欄位、以及 {:?} 格式的值印到 stderr,然後把那個值回傳出去。因為會回傳值,所以可以直接包住運算式中間的任何一段,不必另外拆出一個變數——b 仍然是 6 + 1 = 7。
「回傳 ()」是把 dbg! 當成 println! 的同類,這正是它跟 println! 最大的差別。「只是 println! 的簡寫」也漏了兩件事:它印到 stderr 而不是 stdout(所以把程式輸出導向檔案時,除錯訊息不會混進去),而且會印出運算式文字與位置。「release 建置會被移除」則不對——dbg! 在 release 也照樣輸出,它不是 debug_assert! 那種條件式巨集,除錯完要記得自己刪掉。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let a = 2;", note: "被觀察的變數。" },
          { code: "    let b = dbg!(a * 3) + 1;", note: "dbg! 包住 a * 3:先在 stderr 印出 [src/main.rs:3:13] a * 3 = 6,然後回傳 6,接著 + 1,b 是 7。3:13 是這個 dbg! 所在的行與欄。" },
          { code: "    println!(\"{}\", b);", note: "在 stdout 印出 7。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "注意 dbg! 會取得傳入值的所有權再還給你:dbg!(my_string) 會把 my_string 移進去再移出來,如果外面沒有接住,原變數就不能再用了。只想看一眼而不想移動,寫 dbg!(&my_string)。",
      },
      csharp: `C# 沒有對應的內建機制。最接近的是 Debug.WriteLine($"a * 3 = {a * 3}"),但它是陳述式、不能嵌在運算式中間,而且運算式文字要自己重打一次;想拿到檔名與行號還得靠 [CallerFilePath]、[CallerLineNumber] 參數。dbg! 身為巨集,能直接看到呼叫處的原始碼文字與位置,這是函式做不到的事——正好示範了本課第一題的主題:巨集能做什麼函式做不到的事。`,
    },
    {
      id: "2-11-16",
      question: "derive 巨集的使用面。以下程式碼能編譯嗎?",
      questionCode: "struct Point {\n    x: i32,\n    y: i32,\n}\n\nfn main() {\n    let p = Point { x: 1, y: 2 };\n    let q = p.clone();\n    println!(\"{} {}\", p.x, q.y);\n}",
      options: [
        { text: "編譯錯誤:no method named `clone` found for struct `Point`——Clone 不會自動被實作,即使所有欄位都是 i32 也一樣;要在 struct 上寫 #[derive(Clone)],讓 derive 巨集在編譯期產生 impl Clone for Point" },
        { text: "可以編譯,印出 1 2——所有欄位都是可以複製的 i32,編譯器會自動替 Point 提供 clone" },
        { text: "可以編譯,印出 1 2——每個型別都內建 clone 方法,就像 C# 每個物件都繼承 MemberwiseClone" },
        { text: "編譯錯誤,但原因是 p.clone() 把 p 移走了,最後一行不能再讀 p.x" },
      ],
      answer: 0,
      explanation: `在 Rust 裡,一個型別有哪些能力(能不能複製、能不能比較、能不能用 {:?} 印)全部要明確宣告,沒有「看起來簡單就自動給」這回事。#[derive(Clone)] 是一個掛在型別上的屬性形式巨集:它在編譯期讀取 struct 的定義,產生一段 impl Clone for Point { ... } 程式碼,效果跟你手寫的一模一樣。沒寫它,Point 就沒有 clone 方法,錯誤訊息還會提示你 Clone 這個 trait 定義了 clone。
「欄位都是 i32 就自動提供」是最容易犯的錯——Rust 刻意不這麼做,因為能不能複製是型別作者的承諾,一旦提供就不能輕易收回。「p.clone() 把 p 移走」則搞反了:clone 只需要 &self,本來就不會移動 p。至於 derive 巨集本身是怎麼寫出來的(程序式巨集),本課不展開,補充教材會另外介紹。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "struct Point {", note: "定義 Point,沒有任何 derive。" },
            { code: "    x: i32,", note: "欄位 x。" },
            { code: "    y: i32,", note: "欄位 y。兩個欄位都能複製,但這不會讓 Point 自動獲得任何能力。" },
            { code: "}", note: "struct 定義結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let p = Point { x: 1, y: 2 };", note: "建立一個 Point。" },
            { code: "    let q = p.clone();", note: "⛔ 編譯失敗:no method named `clone` found for struct `Point` in the current scope。Point 沒有實作 Clone,所以沒有 clone 方法。" },
            { code: "    println!(\"{} {}\", p.x, q.y);", note: "不會執行到這裡。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "#[derive(Clone)]", note: "改動處:加上 derive 屬性。derive 巨集在編譯期替 Point 產生 impl Clone,內容是逐欄呼叫每個欄位的 clone。" },
            { code: "struct Point {", note: "struct 定義本身不用改。" },
            { code: "    x: i32,", note: "欄位 x。" },
            { code: "    y: i32,", note: "欄位 y。" },
            { code: "}", note: "struct 定義結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let p = Point { x: 1, y: 2 };", note: "建立一個 Point。" },
            { code: "    let q = p.clone();", note: "現在有 clone 方法了。clone 只借用 p,產生一個獨立的新 Point。" },
            { code: "    println!(\"{} {}\", p.x, q.y);", note: "p 仍然有效,印出 1 2。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "像 Point 這種全部欄位都是 Copy 的小型別,實務上常寫成 #[derive(Clone, Copy)],之後連 .clone() 都不用寫,賦值時直接複製。",
        },
      ],
      csharp: `C# 的每個物件都繼承了 protected 的 MemberwiseClone,想對外公開複製能力得自己包一層或實作 ICloneable;record 則由編譯器自動提供 with 運算式與值相等比較。Rust 的立場是「什麼都不自動給」:Clone、Debug、PartialEq 都要用 derive 明確要求,而 derive 本身就是巨集——你寫一行屬性,它在編譯期替你寫出整個 impl 區塊。`,
    },
    {
      id: "2-11-17",
      question: "derive 會替欄位型別補實作嗎?以下程式碼能編譯嗎?",
      questionCode: "struct Handle {\n    id: u32,\n}\n\n#[derive(Clone)]\nstruct Session {\n    name: String,\n    handles: Vec<Handle>,\n}\n\nfn main() {\n    let s = Session { name: String::from(\"main\"), handles: vec![Handle { id: 1 }] };\n    let t = s.clone();\n    println!(\"{} {}\", t.name, t.handles[0].id);\n}",
      options: [
        { text: "編譯錯誤:the trait bound `Handle: Clone` is not satisfied,附註 required for `Vec<Handle>` to implement `Clone`——derive 產生的 clone 會逐欄呼叫欄位的 clone,Vec<Handle> 要能 clone 就需要 Handle 能 clone;derive 不會替欄位型別補上實作" },
        { text: "可以編譯——#[derive(Clone)] 會遞迴地替 Session 用到的所有型別(包括 Handle)一起產生 Clone" },
        { text: "可以編譯——Vec 永遠可以 clone,它只是複製指向 heap 的指標,不需要元素能 clone" },
        { text: "編譯錯誤,但錯誤出在 s.clone() 那一行:derive 產生的程式碼要等到真的被呼叫時才會被檢查" },
      ],
      answer: 0,
      explanation: `derive(Clone) 產生的程式碼很單純:對每個欄位呼叫它自己的 clone,再組成一個新的 Session。name 是 String,有 Clone;handles 是 Vec<Handle>,而 Vec<T> 只有在 T: Clone 時才實作 Clone。Handle 沒有 derive,於是這條鏈斷在 Handle,編譯器回報 Handle: Clone 不成立,並用 note 說明是 Vec<Handle> 需要它,還直接建議你替 Handle 加上 #[derive(Clone)]。
「derive 會遞迴補齊」是最直覺的誤解:derive 巨集只能看到它掛上的那個型別定義,看不到、也不會去改 Handle 的定義。「Vec 只複製指標」是把 clone 誤當成淺層複製——Vec 的 clone 會配置新的 heap 空間並逐一 clone 元素。「要等被呼叫才檢查」也不對:錯誤指向的是 handles 這個欄位宣告(in this derive macro expansion),因為產生出來的 impl 本身就型別不符,跟有沒有人呼叫無關。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "struct Handle {", note: "一個內層型別,沒有任何 derive。" },
            { code: "    id: u32,", note: "唯一的欄位。" },
            { code: "}", note: "Handle 定義結束——它沒有 Clone。" },
            { code: "", note: "" },
            { code: "#[derive(Clone)]", note: "要求 derive 巨集替 Session 產生 Clone 實作。" },
            { code: "struct Session {", note: "外層型別。" },
            { code: "    name: String,", note: "String 有 Clone,這個欄位沒問題。" },
            { code: "    handles: Vec<Handle>,", note: "⛔ 編譯失敗:the trait bound `Handle: Clone` is not satisfied(note: required for `Vec<Handle>` to implement `Clone`)。derive 產生的程式碼要 clone 這個欄位,而 Vec<Handle> 只有在 Handle 能 clone 時才能 clone。" },
            { code: "}", note: "Session 定義結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let s = Session { name: String::from(\"main\"), handles: vec![Handle { id: 1 }] };", note: "建立一個 Session,裡面有一個 Handle。" },
            { code: "    let t = s.clone();", note: "錯誤不在這一行——編譯器在產生 impl 時就已經失敗了。" },
            { code: "    println!(\"{} {}\", t.name, t.handles[0].id);", note: "不會執行到這裡。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (complete, runnable)",
          lines: [
            { code: "#[derive(Clone)]", note: "改動處:替內層型別 Handle 也加上 derive。derive 要一層一層自己寫,不會遞迴補齊。" },
            { code: "struct Handle {", note: "定義不變。" },
            { code: "    id: u32,", note: "u32 有 Clone,所以 Handle 的 derive 能成功。" },
            { code: "}", note: "Handle 定義結束——現在它有 Clone,Vec<Handle> 也跟著有了。" },
            { code: "", note: "" },
            { code: "#[derive(Clone)]", note: "外層的 derive 不用改。" },
            { code: "struct Session {", note: "外層型別。" },
            { code: "    name: String,", note: "clone 時配置一個新的 String。" },
            { code: "    handles: Vec<Handle>,", note: "clone 時配置一個新的 Vec,並對每個 Handle 呼叫 clone。" },
            { code: "}", note: "Session 定義結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let s = Session { name: String::from(\"main\"), handles: vec![Handle { id: 1 }] };", note: "建立一個 Session。" },
            { code: "    let t = s.clone();", note: "深層複製:t 擁有自己的 String 與自己的 Vec<Handle>,跟 s 完全獨立。" },
            { code: "    println!(\"{} {} {}\", s.name, t.name, t.handles[0].id);", note: "s 仍然有效。印出 main main 1。" },
            { code: "}", note: "t 與 s 各自釋放自己的資料。" },
          ],
        },
        {
          label: "△ Desugared (what the compiler sees)",
          intro: "錯誤指向的是欄位宣告而不是 s.clone(),因為問題出在 derive 產生的程式碼本身(AUTHORING §9 #30)。攤開來看:",
          lines: [
            { code: "#[derive(Clone)]", note: "你寫的這一行。" },
            { code: "", note: "" },
            { code: "// derive 巨集在編譯期產生(示意):", note: "以下是編譯器實際檢查的程式碼。" },
            { code: "impl Clone for Session {", note: "替 Session 實作 Clone。" },
            { code: "    fn clone(&self) -> Self {", note: "clone 只借用 self。" },
            { code: "        Session {", note: "組出一個新的 Session。" },
            { code: "            name: self.name.clone(),", note: "String::clone,沒問題。" },
            { code: "            handles: self.handles.clone(),", note: "Vec<Handle>::clone——只有在 Handle: Clone 時才存在。錯誤訊息的 required for `Vec<Handle>` to implement `Clone` 就是指這一行。" },
            { code: "        }", note: "struct 實字結束。" },
            { code: "    }", note: "方法結束。" },
            { code: "}", note: "impl 結束。每個欄位都必須自己能 clone,derive 只負責把它們串起來。" },
          ],
        },
      ],
      csharp: `C# 的 record 用 with 產生複本時做的是淺層複製:Session 的複本和原本共用同一個 List<Handle> 物件,改一邊另一邊也看得到,而且編譯器不會要求 Handle 具備任何能力。Rust 的 derive(Clone) 是逐欄 clone,Vec 會真的複製一份,因此每一層的元素型別都必須自己宣告能 clone——這條要求在編譯期就被檢查,不會出現「以為複製了其實共用」的意外。`,
    },
    {
      id: "2-11-18",
      question: "巨集衛生(hygiene)。以下程式碼的結果是?",
      questionCode: "macro_rules! add_ten {\n    ($e:expr) => {{\n        let tmp = 10;\n        $e + tmp\n    }};\n}\n\nfn main() {\n    let tmp = 1;\n    let r = add_ten!(tmp * 2);\n    println!(\"{} {}\", r, tmp);\n}",
      options: [
        { text: "印出 12 1——巨集衛生:巨集本體裡宣告的 tmp 和呼叫端寫的 tmp 是兩個不同的變數,即使名字一樣;$e 裡的 tmp 永遠指向呼叫端值為 1 的那個,所以是 1 * 2 + 10" },
        { text: "印出 30 1——巨集是文字替換,展開後 $e 裡的 tmp 被巨集內部的 let tmp = 10 遮蔽,變成 10 * 2 + 10" },
        { text: "編譯錯誤:巨集內部宣告的 tmp 和 main 裡已經存在的 tmp 重複定義" },
        { text: "印出 12 10——巨集內部的 let tmp = 10 蓋掉了呼叫端的 tmp,之後的 println! 看到的是 10" },
      ],
      answer: 0,
      explanation: `macro_rules! 是「衛生的」(hygienic):每個識別字都記得自己是在哪裡被寫下的。巨集本體裡的 let tmp = 10 是「巨集定義處」寫的 tmp,呼叫端傳進來的 tmp * 2 裡的 tmp 是「呼叫處」寫的 tmp,兩者雖然拼法相同,在編譯器眼中是兩個不同的名字,互不遮蔽。所以 $e 仍然是 1 * 2,加上巨集自己的 10,得到 12;呼叫端的 tmp 也完全沒被動到,仍然是 1。
「印出 30」正是單純文字替換(C 的 #define)會得到的結果——展開後的 { let tmp = 10; tmp * 2 + tmp } 裡,所有 tmp 都指向同一個變數。衛生機制就是為了消滅這種「巨集的內部變數意外捕獲呼叫端變數」的 bug。「重複定義」也不會發生:即使不談衛生,Rust 本來就允許 let 遮蔽,何況兩個 tmp 根本不是同一個名字。補充一點界線:衛生只保護區域變數這類名字,巨集展開出來的函式、型別等項目名稱仍然會出現在呼叫端的作用域裡(第 5 題的 answer 函式就是這樣被 main 呼叫到的)。`,
      walkthrough: {
        label: "⊕ Question code — walkthrough (runs successfully)",
        lines: [
          { code: "macro_rules! add_ten {", note: "定義一個會引入暫時變數的巨集。" },
          { code: "    ($e:expr) => {{", note: "捕獲一個運算式。內層大括號讓展開結果是一個區塊運算式。" },
          { code: "        let tmp = 10;", note: "巨集自己的暫時變數。這個 tmp 帶著「巨集定義處」的標記,呼叫端的程式碼看不到它。" },
          { code: "        $e + tmp", note: "$e 裡的識別字帶著「呼叫處」的標記,只會解析到呼叫端的變數;後面的 tmp 則是上一行巨集自己的那個。" },
          { code: "    }};", note: "規則結束。" },
          { code: "}", note: "巨集定義結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let tmp = 1;", note: "呼叫端的 tmp,值是 1。" },
          { code: "    let r = add_ten!(tmp * 2);", note: "展開後兩個 tmp 各歸各的:呼叫端的 tmp * 2 是 2,加上巨集內部的 10,r 是 12。" },
          { code: "    println!(\"{} {}\", r, tmp);", note: "呼叫端的 tmp 從未被改動,印出 12 1。" },
          { code: "}", note: "main 結束。" },
        ],
        outro: "衛生機制讓你寫巨集時不必擔心「我取的暫時變數名稱會不會剛好跟使用者的變數撞名」——這正是 C 巨集作者必須用 __tmp_42 這類怪名字自保的原因,在 Rust 裡完全不需要。",
      },
      csharp: `C# 沒有巨集,所以沒有這個問題,也沒有這個機制。可以拿來類比的是 C/C++ 的 #define:它在語法分析之前做純文字替換,巨集內部的名字會和呼叫端的名字攪在一起,所以有人說「能用函式就不要用 #define」。Rust 的 macro_rules! 操作的是帶有來源標記的 token,而不是文字,這是它能在語言裡被放心大量使用的前提之一。`,
    },
    {
      id: "2-11-19",
      question: "綜合:自己寫一個簡化版 vec!。以下程式碼的結果是?",
      questionCode: "macro_rules! my_vec {\n    ($($x:expr),* $(,)?) => {{\n        #[allow(unused_mut)]\n        let mut v = Vec::new();\n        $(\n            v.push($x);\n        )*\n        v\n    }};\n}\n\nfn main() {\n    let a = my_vec![1, 2, 3,];\n    let b: Vec<i32> = my_vec![];\n    println!(\"{:?} {} {}\", a, a == vec![1, 2, 3], b.is_empty());\n}",
      options: [
        { text: "印出 [1, 2, 3] true true——$(...),* 讓每個片段各展開一次 v.push($x);,結尾的 $(,)? 允許可有可無的尾逗號,零個片段時展開成空的 Vec;行為跟 vec! 一樣,但展開出來的程式碼不必跟標準庫一字不差" },
        { text: "編譯錯誤:my_vec![1, 2, 3,] 最後多了一個逗號,$(...),* 不允許尾逗號" },
        { text: "編譯錯誤:my_vec![] 沒有任何片段,$(...),* 至少要比對到一個才算成功" },
        { text: "印出 [1, 2, 3] true true,而且 my_vec! 展開後的程式碼跟標準庫 vec! 一模一樣,a == vec![1, 2, 3] 是 true 正是因為兩者展開結果相同" },
      ],
      answer: 0,
      explanation: `這個巨集把本課學過的東西組在一起:$($x:expr),* 比對零個以上、以逗號分隔的運算式;緊接著的 $(,)? 是另一個重複群組,內容只有一個逗號,? 表示零次或一次——這就是「允許尾逗號」的標準寫法。展開端的 $( v.push($x); )* 對每個捕獲到的 $x 產生一行 push。所以 a 是 [1, 2, 3];b 沒有任何片段,展開成只有 Vec::new() 的區塊,是空的 Vec(型別由 let 的標註決定)。
「多了尾逗號就編譯錯誤」在沒有 $(,)? 時是對的——單靠 $(...),* 確實會在最後那個逗號之後期待下一個運算式而失敗,這正是那一小段存在的原因。「跟標準庫 vec! 一字不差,所以才相等」是把「行為相同」和「程式碼相同」混為一談:標準庫的 vec! 實際上是一次配置好再轉成 Vec(第 8 題),跟這裡的逐一 push 完全不同,但 == 比較的是兩個 Vec 的元素,與它們怎麼被建出來無關。至於 #[allow(unused_mut)]:my_vec![] 展開後沒有任何 push,v 的 mut 用不到,這個屬性讓那種情況不產生警告。`,
      walkthrough: [
        {
          label: "⊕ Question code — walkthrough (runs successfully)",
          lines: [
            { code: "macro_rules! my_vec {", note: "自製的簡化版 vec!,只支援逗號清單形式。" },
            { code: "    ($($x:expr),* $(,)?) => {{", note: "兩個重複群組:前面是零個以上、逗號分隔的運算式;後面的 $(,)? 是「可有可無的一個逗號」,讓尾逗號合法。" },
            { code: "        #[allow(unused_mut)]", note: "零個元素時 v 不會被修改,mut 就是多餘的;這個屬性關掉那種情況下的 unused_mut 警告。" },
            { code: "        let mut v = Vec::new();", note: "先建一個空 Vec。這個 v 受巨集衛生保護,不會跟呼叫端的變數撞名。" },
            { code: "        $(", note: "展開端的重複區塊開始。" },
            { code: "            v.push($x);", note: "每個捕獲到的 $x 各產生一行 push。" },
            { code: "        )*", note: "重複區塊結束,次數等於比對端捕獲到的片段數。" },
            { code: "        v", note: "區塊的值就是這個 Vec。" },
            { code: "    }};", note: "規則結束。" },
            { code: "}", note: "巨集定義結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let a = my_vec![1, 2, 3,];", note: "三個片段加一個尾逗號,尾逗號被 $(,)? 吃掉。展開成三行 push,a 是 [1, 2, 3]。" },
            { code: "    let b: Vec<i32> = my_vec![];", note: "零個片段。展開成只有 Vec::new() 的區塊,元素型別靠左邊的標註決定。" },
            { code: "    println!(\"{:?} {} {}\", a, a == vec![1, 2, 3], b.is_empty());", note: "== 比較元素,與建構方式無關。印出 [1, 2, 3] true true。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "△ Desugared (what my_vec![1, 2, 3,] expands to)",
          intro: "依照重複展開的規則(AUTHORING §9 #55),把 a 那一行攤開:",
          lines: [
            { code: "let a = my_vec![1, 2, 3,];", note: "你寫的這一行。" },
            { code: "", note: "" },
            { code: "// 巨集把它展開成:", note: "以下是編譯器實際看到的程式碼。" },
            { code: "let a = {", note: "外層是區塊運算式,區塊的值就是最後的 v。" },
            { code: "    let mut v = Vec::new();", note: "巨集本體的第一行,原樣照抄。" },
            { code: "    v.push(1);", note: "第一個 $x。" },
            { code: "    v.push(2);", note: "第二個 $x。" },
            { code: "    v.push(3);", note: "第三個 $x。尾逗號不產生任何程式碼。" },
            { code: "    v", note: "區塊的值。" },
            { code: "};", note: "展開結束。" },
          ],
        },
      ],
      csharp: `C# 沒有能讓你自己定義這種語法的機制;集合初始設定式與 C# 12 的集合運算式都是語言寫死的,想擴充只能照編譯器規定的模式(提供 Add 方法或 CollectionBuilder 屬性)。Rust 的 vec! 不是語言內建語法,而是一個用 macro_rules! 寫成的普通巨集——你能用同樣的工具寫出自己的版本,這題的 my_vec! 就是證明。`,
    },
    {
      id: "2-11-20",
      question: "本課 C# 對照總結:關於 Rust 巨集與 C# 裡概念上最接近的 Source Generator,哪一組描述最準確?",
      options: [
        { text: "兩者都在編譯期產生程式碼,但形狀很不一樣:Source Generator 是另外寫在獨立組件裡的程式,通常由 attribute 或 partial 宣告觸發,讀取整個專案的語法樹,只能「新增」原始碼檔案;Rust 的 macro_rules! 就寫在程式碼裡,在呼叫處原地展開,輸入只有呼叫時寫下的 token,derive 這類屬性巨集也是掛在單一項目上就地產生程式碼" },
        { text: "兩者是同一種東西:Rust 巨集就是 Rust 版的 Source Generator,只是語法不同" },
        { text: "Rust 巨集就是 C 的 #define 文字替換,C# 刻意不支援帶參數的 #define,所以兩者沒有可比性" },
        { text: "Source Generator 在執行期透過反射產生程式碼,Rust 巨集在編譯期,所以只有 Rust 的做法沒有執行期成本" },
      ],
      answer: 0,
      explanation: `一句話收束本課:Rust 巨集是「寫在呼叫處、編譯期原地展開的程式碼樣板」。從這句話可以推出本課的每一條規則——它能接受可變數量的引數、能在編譯期檢查格式字串(第 1、9、10 題),因為它看到的是 token 而不是值;它有比對與展開兩個階段(第 3~7 題),因為規則是對語法片段做模式比對;它是衛生的(第 18 題),因為 token 帶著來源標記而不是單純的文字。
「Rust 巨集就是 C 的 #define」是最需要破的一個:第 4 題的 (1 + 2) * 2 與第 18 題的衛生機制,正是它跟文字替換的根本差異。「兩者是同一種東西」則忽略了觸發方式與作用範圍:Source Generator 由 attribute 驅動、看得到整個編譯單元、只能新增檔案(所以大量依賴 partial);macro_rules! 由呼叫語法驅動、只看得到自己的輸入、就地替換呼叫處。「Source Generator 在執行期透過反射」則把它跟舊的反射式做法搞混了——Source Generator 的重點正是把反射搬到編譯期。`,
      walkthrough: [
        {
          label: "◇ C# equivalent: a Source Generator driven by an attribute",
          lang: "csharp",
          lines: [
            { code: "using System.Text.RegularExpressions;", note: "GeneratedRegex 是 .NET 7 起內建的 Source Generator。" },
            { code: "", note: "" },
            { code: "public static partial class Validators", note: "類別必須是 partial:產生器不能改你的檔案,只能另外產生一個檔案補上另一半。" },
            { code: "{", note: "類別本體開始。" },
            { code: "    [GeneratedRegex(@\"^\\d{3}-\\d{4}$\")]", note: "觸發點是 attribute。編譯時,產生器讀到它,替下面那個方法寫出完整的比對程式碼。" },
            { code: "    public static partial Regex PhoneRegex();", note: "只有宣告沒有本體——本體在另一個自動產生的檔案裡,原始碼中看不到。" },
            { code: "}", note: "類別結束。" },
          ],
          outro: "特徵:由 attribute 驅動、產生器本身是另外一個組件、輸出是一個新的原始碼檔案,靠 partial 跟你的程式碼拼在一起。",
        },
        {
          label: "√ Rust: code generated in place, at the call site",
          lines: [
            { code: "macro_rules! point {", note: "巨集就定義在同一個檔案裡,不需要另外的專案或組件。" },
            { code: "    ($x:expr, $y:expr) => {", note: "輸入只有呼叫時寫下的兩個運算式。" },
            { code: "        Point { x: $x, y: $y }", note: "展開成一個 struct 實字,直接取代呼叫處。" },
            { code: "    };", note: "規則結束。" },
            { code: "}", note: "巨集定義結束。" },
            { code: "", note: "" },
            { code: "#[derive(Debug, Clone, PartialEq)]", note: "屬性形式的巨集:掛在單一個 struct 上,就地產生三個 impl 區塊,不需要 partial。" },
            { code: "struct Point {", note: "被 derive 的型別。" },
            { code: "    x: i32,", note: "欄位 x。" },
            { code: "    y: i32,", note: "欄位 y。" },
            { code: "}", note: "struct 定義結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let p = point!(1, 2);", note: "呼叫處在編譯期被換成 Point { x: 1, y: 2 }。" },
            { code: "    let q = p.clone();", note: "clone 來自 derive(Clone) 產生的 impl。" },
            { code: "    println!(\"{:?} {}\", q, p == q);", note: "{:?} 來自 derive(Debug),== 來自 derive(PartialEq)。印出 Point { x: 1, y: 2 } true。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "帶著走的對照:C# 的 Source Generator 是「另外寫一支程式,在編譯期替你新增檔案」;Rust 的巨集是「在程式碼裡直接寫樣板,編譯期原地展開」。後者輕量到連 println! 和 vec! 這種天天用的東西都是用它做的。",
        },
      ],
      csharp: `給 C# 開發者的三個帶著走的結論。一、看到驚嘆號就想「編譯期展開」:println!、vec!、assert_eq!、dbg! 能做函式做不到的事(可變引數、檢查格式字串、印出原始碼文字),都是因為它們在編譯期看得到你寫的 token。二、讀 macro_rules! 時分清兩個階段:比對階段看片段指定子與分隔符,錯誤訊息是 no rules expected;展開階段產生的程式碼才進入一般的型別檢查。三、#[derive(...)] 也是巨集,它替你寫 impl,但不替欄位型別補能力。C# 裡最接近的是 Source Generator,但那是重量級的擴充機制;在 Rust,巨集是日常工具,本課看到的已經涵蓋你會天天用到的部分。`,
    },
  ],
};
