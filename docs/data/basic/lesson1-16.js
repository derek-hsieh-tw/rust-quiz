/* 出題慣例見專案根目錄 AUTHORING.md:
 *   - answer 一律為 0(正確答案寫在第一個選項),顯示順序由 quiz.js 依題目 id 洗牌
 *   - 詳解禁止用「選項 A/B/C」字母指涉,必須直接描述選項內容
 *   - 有程式碼的題目一律附 walkthrough(逐行說明);反面案例必須同時附上 √ 正確寫法 */
window.RUST_LESSONS = window.RUST_LESSONS || {};
window.RUST_LESSONS["lesson1-16"] = {
  id: "lesson1-16",
  title: "測試、文件與基礎總整理",
  goal: "會寫單元測試與文件註解,並用四道綜合題驗收基礎階段:所有權、借用、enum、Result 的整合運用。",
  questions: [
    {
      id: "1-16-01",
      question: "在同一個檔案裡為 add 函式寫單元測試,正確的寫法是?",
      questionCode: "pub fn add(a: i32, b: i32) -> i32 {\n    a + b\n}",
      options: [
        { code: "#[cfg(test)]\nmod tests {\n    use super::*;\n\n    #[test]\n    fn it_works() {\n        assert_eq!(add(2, 2), 4);\n    }\n}" },
        { code: "[TestClass]\nmod tests {\n    [TestMethod]\n    fn it_works() {\n        Assert.AreEqual(add(2, 2), 4);\n    }\n}" },
        { code: "mod tests {\n    fn test_it_works() {\n        assert_eq!(add(2, 2), 4);\n    }\n}" },
        { code: "#[test]\nfn main() {\n    assert_eq!(add(2, 2), 4);\n}" },
      ],
      answer: 0,
      explanation: `標準三件套:#[cfg(test)] 標在測試模組上(只在 cargo test 時編譯)、use super::* 把被測項目從父模組引進來(tests 是子模組,lesson1-14 的路徑規則)、#[test] 標在每個測試函式上讓測試框架收錄。cargo test 一鍵執行。
[TestClass]/Assert.AreEqual 是 C#/MSTest 語法;只靠 test_ 開頭命名不會被執行(Rust 認屬性不認名字,函式還會被回報為 dead code);main 是程式進入點,不能兼任測試。`,
      walkthrough: [
        {
          label: "√ Correct version — walkthrough",
          lines: [
            { code: "pub fn add(a: i32, b: i32) -> i32 {", note: "被測的函式,和測試住在同一個檔案。" },
            { code: "    a + b", note: "函式本體。" },
            { code: "}", note: "函式結束。" },
            { code: "", note: "" },
            { code: "#[cfg(test)]", note: "件套一:條件編譯屬性——這個模組只在 cargo test 時才會被編譯,正式建置完全不含測試程式碼。" },
            { code: "mod tests {", note: "測試自成一個子模組。" },
            { code: "    use super::*;", note: "件套二:把父模組的項目全部引進來,才叫得到 add。因為 tests 是子模組,它連父模組的「私有」函式也看得到——所以 Rust 能直接測私有實作。" },
            { code: "", note: "" },
            { code: "    #[test]", note: "件套三:標記這是一個測試函式,測試框架靠這個屬性收錄,而不是靠函式名稱。" },
            { code: "    fn it_works() {", note: "測試函式沒有參數也沒有回傳值。" },
            { code: "        assert_eq!(add(2, 2), 4);", note: "斷言相等;不 panic 就代表通過。" },
            { code: "    }", note: "測試函式結束。" },
            { code: "}", note: "測試模組結束。用 cargo test 一鍵執行。" },
          ],
        },
        {
          label: "X What's wrong with the three versions",
          lines: [
            { code: "[TestClass]", note: "這是 C#/MSTest 的屬性語法;Rust 的屬性要寫成 #[...],而且沒有 TestClass 這種東西。" },
            { code: "fn test_it_works() {", note: "只靠 test_ 開頭的命名不會被執行——Rust 認屬性不認名字;沒有 #[test] 的話,這個函式還會被回報為 dead code。" },
            { code: "#[test]", note: "屬性本身沒問題。" },
            { code: "fn main() {", note: "但 main 是程式進入點,不能兼任測試函式。" },
          ],
          outro: "C# 測試住在獨立的測試專案 + xUnit/NUnit 套件;Rust 內建測試框架,單元測試慣例「與被測程式碼同檔案」,#[cfg(test)] 保證不進正式建置。",
        },
      ],
      csharp: `C# 測試住在獨立的測試專案 + xUnit/NUnit 套件;Rust 內建測試框架,單元測試慣例「與被測程式碼同檔案」(#[cfg(test)] 保證不進正式建置)——測試貼著實作,還能測私有函式(子模組看得到父模組私有項目)。`,
    },
    {
      id: "1-16-02",
      question: "assert_eq! 失敗時的行為。以下測試執行 cargo test 的結果是?",
      questionCode: "#[cfg(test)]\nmod tests {\n    #[test]\n    fn good() {\n        assert_eq!(1 + 1, 2);\n    }\n\n    #[test]\n    fn bad() {\n        assert_eq!(2 + 2, 5);\n    }\n}",
      options: [
        { text: "good 通過、bad 失敗:assert_eq! 失敗會 panic 並印出 left/right 兩值,但只影響該測試,其他測試照常執行" },
        { text: "第一個測試失敗後整個測試程序中止,good 的結果看不到" },
        { text: "兩個都通過:assert_eq! 只印警告不判失敗" },
        { text: "編譯錯誤:編譯器發現 2 + 2 不等於 5" },
      ],
      answer: 0,
      explanation: `測試框架的判定規則:測試函式沒 panic = 通過,panic = 失敗。assert_eq! 失敗時 panic 並印出 left = 4, right = 5 幫你對帳。每個測試跑在獨立執行緒,一個炸了不拖累別人(預設還是並行執行),最後總結 1 passed; 1 failed。
編譯器不會幫你算 assert 的對錯(那是執行期的事);assert 家族失敗從來不是「警告」,就是 panic。`,
      walkthrough: {
        label: "🔍 Question code — walkthrough (one passes, one fails)",
        lines: [
          { code: "#[cfg(test)]", note: "只在測試建置時編譯。" },
          { code: "mod tests {", note: "測試模組。" },
          { code: "    #[test]", note: "收錄第一個測試。" },
          { code: "    fn good() {", note: "測試函式。每個測試跑在「獨立執行緒」,而且預設並行執行。" },
          { code: "        assert_eq!(1 + 1, 2);", note: "斷言成立,函式安然返回 → 判定通過。判定規則就這麼簡單:沒 panic = 通過。" },
          { code: "    }", note: "測試結束。" },
          { code: "", note: "" },
          { code: "    #[test]", note: "收錄第二個測試。" },
          { code: "    fn bad() {", note: "第二個測試函式。" },
          { code: "        assert_eq!(2 + 2, 5);", note: "斷言不成立 → panic,並印出 left = 4, right = 5 幫你對帳 → 判定失敗。注意編譯器不會幫你算 assert 的對錯,那是執行期的事。" },
          { code: "    }", note: "測試結束。因為測試彼此隔離,這個失敗不會拖累 good。" },
          { code: "}", note: "測試模組結束。最後總結會顯示 test result: FAILED. 1 passed; 1 failed。" },
        ],
        outro: "assert 家族失敗從來不是「警告」,就是 panic。與 xUnit 的 Assert.Equal 行為幾乎一致,差別在機制:C# 靠測試框架捕捉例外,Rust 直接沿用語言的 panic——所以 assert! 在正式程式碼裡也能當防衛性檢查用。",
      },
      csharp: `與 xUnit 的 Assert.Equal 行為幾乎一致(失敗印 expected/actual、測試相互隔離)。差別在機制:C# 靠測試框架捕捉例外;Rust 直接沿用語言的 panic,assert! 家族在正式程式碼裡也能用(防衛性檢查),不是測試專屬。`,
    },
    {
      id: "1-16-03",
      question: "測試「應該 panic」的行為。#[should_panic] 的正確理解是?",
      questionCode: "pub fn set_age(age: u32) {\n    if age > 150 {\n        panic!(\"年齡不可能超過 150,收到 {}\", age);\n    }\n}\n\n#[cfg(test)]\nmod tests {\n    use super::*;\n\n    #[test]\n    #[should_panic(expected = \"年齡不可能超過 150\")]\n    fn rejects_impossible_age() {\n        set_age(200);\n    }\n}",
      options: [
        { text: "這個測試「panic 才算通過」:驗證防衛邏輯真的會擋下非法輸入;expected 進一步要求 panic 訊息包含指定文字,避免誤判成別處的 panic" },
        { text: "should_panic 表示這個測試還沒寫完,先跳過不執行" },
        { text: "測試通過的條件不變(不 panic 才通過),屬性只是文件註記" },
        { text: "expected 參數指定測試的預期回傳值" },
      ],
      answer: 0,
      explanation: `#[should_panic] 反轉判定:panic = 通過、安然返回 = 失敗——專測「錯誤路徑真的會炸」。不帶 expected 有個陷阱:測試中「任何」panic 都算過(可能是別的 bug 先炸了),加上 expected = "..." 要求 panic 訊息包含該子字串,把驗證釘在你設計的那個 panic 上。
跳過測試另有屬性(#[ignore]);它是行為改變不是註記;panic 的世界沒有「回傳值」可言。`,
      walkthrough: {
        label: "🔍 Question code — walkthrough (only passes if it panics)",
        lines: [
          { code: "pub fn set_age(age: u32) {", note: "被測函式。" },
          { code: "    if age > 150 {", note: "防衛性檢查:超出合理範圍代表呼叫端有 bug。" },
          { code: "        panic!(\"年齡不可能超過 150,收到 {}\", age);", note: "違反契約 → panic(lesson1-10 的劃分標準)。" },
          { code: "    }", note: "if 結束。" },
          { code: "}", note: "函式結束。" },
          { code: "", note: "" },
          { code: "#[cfg(test)]", note: "測試專用。" },
          { code: "mod tests {", note: "測試模組。" },
          { code: "    use super::*;", note: "引入被測函式。" },
          { code: "", note: "" },
          { code: "    #[test]", note: "收錄測試。" },
          { code: "    #[should_panic(expected = \"年齡不可能超過 150\")]", note: "反轉判定:panic = 通過、安然返回 = 失敗——專測「錯誤路徑真的會炸」。expected 要求 panic 訊息「包含」這段子字串,避免測試被別處意外的 panic 誤判成通過(不加 expected 時任何 panic 都算過,是個陷阱)。" },
          { code: "    fn rejects_impossible_age() {", note: "測試函式。" },
          { code: "        set_age(200);", note: "傳入非法值,預期它 panic → 測試通過。" },
          { code: "    }", note: "測試結束。" },
          { code: "}", note: "測試模組結束。" },
        ],
        outro: "跳過測試是另一個屬性 #[ignore];should_panic 是「行為改變」而不是文件註記;而 panic 的世界裡沒有回傳值可言。對照 xUnit 的 Assert.Throws<T>:C# 用例外型別鎖定,Rust 的 panic 沒有型別體系,改用訊息子字串鎖定。可恢復錯誤(Result)的測試則不用這招,直接斷言回傳值是 Err 即可。",
      },
      csharp: `對應 xUnit 的 Assert.Throws<ArgumentException>(() => SetAge(200))——C# 用型別鎖定預期的例外,Rust 的 panic 沒有型別體系,改用訊息子字串鎖定。可恢復錯誤(Result)的測試則不用這招:直接 assert 回傳值是 Err 即可。`,
    },
    {
      id: "1-16-04",
      question: "文件註解與一般註解。/// 與 // 的差別,正確的是?",
      questionCode: "/// 將兩數相加。\n///\n/// # Examples\n///\n/// ```\n/// let result = my_crate::add(2, 3);\n/// assert_eq!(result, 5);\n/// ```\npub fn add(a: i32, b: i32) -> i32 {\n    a + b\n}",
      options: [
        { text: "/// 是文件註解:支援 Markdown、cargo doc 生成 HTML 文件,而且 ``` 圍起來的範例會被 cargo test 當測試執行" },
        { text: "/// 和 // 完全相同,多一條斜線只是視覺強調" },
        { text: "/// 的內容會在執行期印到主控台,類似 log" },
        { text: "/// 是編譯器指令,# Examples 會改變函式的編譯行為" },
      ],
      answer: 0,
      explanation: `/// 附著在下一個項目上成為它的文件:cargo doc --open 生成與標準庫同款的 HTML。殺手級特性是最後半句——文件裡的程式碼範例是「doc test」,cargo test 會真的編譯執行它:範例過時(API 改了)測試就紅,文件永遠不會爛掉。
# Examples 只是 Markdown 標題慣例,不影響編譯;文件註解是給 rustdoc 的,不是執行期輸出;另有 //! 寫在模組/crate 開頭,記述「這整個模組是幹嘛的」。`,
      walkthrough: {
        label: "🔍 Doc comments — walkthrough",
        lines: [
          { code: "/// 將兩數相加。", note: "三斜線是「文件註解」,附著在「下一個項目」上成為它的說明;內容支援 Markdown。一般的 // 只是給人看的註解,rustdoc 不會理它。" },
          { code: "///", note: "空行分隔摘要與後續段落,和 Markdown 一樣。" },
          { code: "/// # Examples", note: "只是 Markdown 標題,不影響編譯——但這是社群慣例的區段名,rustdoc 產生的頁面會照樣呈現。" },
          { code: "///", note: "空行。" },
          { code: "/// ```", note: "圍籬起始:接下來是程式碼範例。" },
          { code: "/// let result = my_crate::add(2, 3);", note: "殺手級特性:這段範例是「doc test」,cargo test 會真的把它編譯並執行。" },
          { code: "/// assert_eq!(result, 5);", note: "範例裡的斷言也會被驗證——API 改了而範例沒跟上,測試就紅,文件永遠不會爛掉。" },
          { code: "/// ```", note: "圍籬結束。" },
          { code: "pub fn add(a: i32, b: i32) -> i32 {", note: "文件註解所描述的項目。cargo doc --open 會生成與標準庫同款的 HTML。" },
          { code: "    a + b", note: "函式本體。" },
          { code: "}", note: "函式結束。" },
        ],
        outro: "另有 //!(內部文件註解),寫在模組或 crate 檔案的最開頭,記述「這整個模組是幹嘛的」。對照 C#:XML 文件註解對應前半,但 <example> 裡的程式碼沒人幫你編譯執行——「文件範例即測試」是 Rust 生態文件品質出名的結構性原因。",
      },
      csharp: `C# 的 XML 文件註解(/// <summary>)對應前半;但 <example> 裡的程式碼沒人幫你編譯執行,過時了也沒人知道。「文件範例即測試」是 Rust 生態文件品質出名的結構性原因——crates.io 上的套件文件幾乎都能直接抄範例跑。`,
    },
    {
      id: "1-16-05",
      question: "cargo test 實際會執行哪些東西?",
      options: [
        { text: "三類全跑:#[test] 單元測試、tests/ 目錄的整合測試、文件註解裡的範例(doc tests)" },
        { text: "只跑 #[test] 標記的函式" },
        { text: "只跑 tests/ 目錄下的檔案" },
        { text: "跑所有測試,然後自動執行 main 確認程式能啟動" },
      ],
      answer: 0,
      explanation: `cargo test 的完整清單:(1)單元測試——散在 src/ 各檔案 #[cfg(test)] 模組裡的 #[test];(2)整合測試——專案根目錄 tests/ 資料夾,每個檔案是獨立 crate,只能呼叫函式庫的「公開」API,模擬真實使用者視角;(3)doc tests——上一題的文件範例。三類各有分工:白箱、黑箱、文件保鮮。
main 不會被執行(它不是測試)。輸出報告也會分三段呈現。`,
      walkthrough: [
        {
          label: "🔍 Where each of the three test kinds lives",
          lang: "bash",
          lines: [
            { code: "my_crate/", note: "專案根目錄。" },
            { code: "├── src/", note: "原始碼。" },
            { code: "│   └── lib.rs", note: "類型一(單元測試):散在各檔案的 #[cfg(test)] 模組裡,白箱測試,連私有函式都能測。順帶一提,這裡的 /// 文件範例就是類型三(doc tests)。" },
            { code: "└── tests/", note: "類型二(整合測試):專案根目錄下的固定資料夾,cargo 自動辨識。" },
            { code: "    └── api_test.rs", note: "每個檔案是「獨立的 crate」,只能呼叫函式庫的公開 API——黑箱測試,模擬真實使用者視角。" },
          ],
        },
        {
          label: "√ What an integration test file looks like",
          lines: [
            { code: "// tests/api_test.rs", note: "獨立 crate,不需要也不能寫 #[cfg(test)](這整個檔案本來就只在測試時編譯)。" },
            { code: "use my_crate::add;", note: "像外部使用者一樣,用 crate 名稱引入「公開」項目;私有項目在這裡碰不到。" },
            { code: "", note: "" },
            { code: "#[test]", note: "一樣用 #[test] 標記。" },
            { code: "fn add_works_from_outside() {", note: "測試函式。" },
            { code: "    assert_eq!(add(2, 3), 5);", note: "驗證公開 API 的行為。" },
            { code: "}", note: "測試結束。" },
          ],
          outro: "cargo test 一次跑完三類,輸出報告也分三段呈現。三類各有分工:白箱、黑箱、文件保鮮。至於 main,它不是測試,不會被執行。",
        },
      ],
      csharp: `C# 的單元/整合測試靠專案結構與命名慣例區分,工具鏈(dotnet test)不強制;doc test 則沒有對應物。Rust 把三類測試的位置、可見性、執行方式都定成語言級慣例——換專案零適應成本。`,
    },
    {
      id: "1-16-06",
      question: "【綜合】enum + match + 借用。以下程式碼的輸出是?",
      questionCode: "#[derive(Debug)]\nenum Shape {\n    Circle(f64),\n    Rect(f64, f64),\n}\n\nfn area(s: &Shape) -> f64 {\n    match s {\n        Shape::Circle(r) => 3.14 * r * r,\n        Shape::Rect(w, h) => w * h,\n    }\n}\n\nfn main() {\n    let shapes = vec![Shape::Circle(1.0), Shape::Rect(2.0, 3.0)];\n    let mut total = 0.0;\n    for s in &shapes {\n        total += area(s);\n    }\n    println!(\"{:.2}\", total);\n}",
      options: [
        { text: "9.14" },
        { text: "編譯錯誤:area 收 &Shape,但 for 迴圈給的 s 是 Shape" },
        { text: "3.14" },
        { text: "編譯錯誤:match 缺少萬用分支 _" },
      ],
      answer: 0,
      explanation: `Circle(1.0) 面積 3.14、Rect(2.0, 3.0) 面積 6.0,合計 9.14({:.2} 控制兩位小數)。
借用鏈完全正確:for s in &shapes 迭代借用,s 就是 &Shape,原樣傳給 area——shapes 全程沒被 move,迴圈後還能用。match 對 &Shape 匹配時自動把 r、w、h 綁定為欄位的參考(match ergonomics),浮點運算對參考直接可用。Shape 只有兩個變體且都列出,窮盡性滿足,不需要 _。`,
      walkthrough: {
        label: "🔍 Question code — walkthrough (runs successfully)",
        lines: [
          { code: "#[derive(Debug)]", note: "讓 {:?} 可以印(這題沒用到,但慣例會加)。" },
          { code: "enum Shape {", note: "帶資料的 enum(lesson1-8)。" },
          { code: "    Circle(f64),", note: "圓形,攜帶半徑。" },
          { code: "    Rect(f64, f64),", note: "矩形,攜帶寬與高。兩個變體攜帶的資料形狀不同。" },
          { code: "}", note: "enum 定義結束。" },
          { code: "", note: "" },
          { code: "fn area(s: &Shape) -> f64 {", note: "參數是「借用」——函式只讀不拿,呼叫端的資料不受影響。" },
          { code: "    match s {", note: "對 &Shape 做匹配。編譯器的 match ergonomics 會自動把綁定變成參考。" },
          { code: "        Shape::Circle(r) => 3.14 * r * r,", note: "r 的型別是 &f64,浮點運算對參考直接可用(自動解參考),算出 3.14。" },
          { code: "        Shape::Rect(w, h) => w * h,", note: "同理算出 6.0。兩個變體都列出來了,窮盡性滿足,不需要萬用分支 _。" },
          { code: "    }", note: "match 是運算式,值成為函式回傳值。" },
          { code: "}", note: "函式結束,借用在此結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let shapes = vec![Shape::Circle(1.0), Shape::Rect(2.0, 3.0)];", note: "集合擁有這兩個 enum 值。" },
          { code: "    let mut total = 0.0;", note: "累加器,要改所以是 mut。" },
          { code: "    for s in &shapes {", note: "迭代「借用」:s 的型別正是 &Shape,可以原樣傳給 area。shapes 全程沒被 move,迴圈後還能用。" },
          { code: "        total += area(s);", note: "累加面積。" },
          { code: "    }", note: "迴圈結束,借用結束。" },
          { code: "    println!(\"{:.2}\", total);", note: "{:.2} 控制兩位小數,印出 9.14。" },
          { code: "}", note: "shapes 離開作用域並釋放。" },
        ],
        outro: "「area 收 &Shape 但迴圈給的是 Shape」是誤解:for s in &shapes(注意那個 &)迭代出來的就是參考。若寫成 for s in shapes 才會 move,那時要傳給 area 得寫 area(&s)。",
      },
      csharp: `等價 C#:abstract record Shape + switch 運算式 + foreach 累加——形狀幾乎一樣。差別藏在細節:C# 迭代的是物件參考(隨手可改);Rust 的 &shapes 是唯讀借用,迴圈裡想偷改 shapes 會被編譯器抓(lesson1-9 考過)。`,
    },
    {
      id: "1-16-07",
      question: "【綜合】Result + match + 所有權。以下程式碼的輸出是?",
      questionCode: "fn find_user(id: u32) -> Result<String, String> {\n    if id == 1 {\n        Ok(String::from(\"Derek\"))\n    } else {\n        Err(String::from(\"id not found\"))\n    }\n}\n\nfn main() {\n    let name = match find_user(2) {\n        Ok(n) => n,\n        Err(e) => {\n            println!(\"warn: {}\", e);\n            String::from(\"guest\")\n        }\n    };\n    println!(\"hello, {}\", name);\n}",
      options: [
        { text: "warn: id not found\nhello, guest" },
        { text: "hello, Derek" },
        { text: "程式 panic:Err 沒有被 unwrap 處理" },
        { text: "編譯錯誤:match 的兩個分支回傳型別不一致" },
      ],
      answer: 0,
      explanation: `find_user(2) 走 Err 路徑。match 當運算式用:Err 分支先印警告,區塊尾端的 String::from("guest") 是分支值——兩個分支都給出 String,型別一致,name 得到 "guest"。輸出兩行。
這題是基礎篇錯誤處理哲學的縮影:錯誤是值,用 match 接住、印個警告、給預設值,程式優雅降級繼續走——沒有 unwrap、沒有 panic、沒有 try/catch。所有權細節:n 和 e 都是從 Result 裡 move 出來的 String,拿到就是你的。`,
      walkthrough: {
        label: "🔍 Question code — walkthrough (runs successfully)",
        lines: [
          { code: "fn find_user(id: u32) -> Result<String, String> {", note: "簽名誠實地說:這件事會失敗,而且成功與失敗都帶一段訊息。" },
          { code: "    if id == 1 {", note: "判斷。" },
          { code: "        Ok(String::from(\"Derek\"))", note: "成功路徑:把值包進 Ok,所有權交給呼叫端。" },
          { code: "    } else {", note: "另一條分支。" },
          { code: "        Err(String::from(\"id not found\"))", note: "失敗路徑:錯誤也是一個普通的值,不是丟出去的例外。" },
          { code: "    }", note: "if 運算式的值就是函式回傳值。" },
          { code: "}", note: "函式結束。" },
          { code: "", note: "" },
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let name = match find_user(2) {", note: "id 是 2,會走 Err 路徑。match 當運算式用,整個結果賦給 name。" },
          { code: "        Ok(n) => n,", note: "成功分支:n 是從 Result 裡 move 出來的 String,拿到就是你的。" },
          { code: "        Err(e) => {", note: "失敗分支用區塊,可以做多件事。e 同樣是 move 出來的 String。" },
          { code: "            println!(\"warn: {}\", e);", note: "先印警告:warn: id not found。" },
          { code: "            String::from(\"guest\")", note: "區塊尾端沒有分號 = 這是分支的值。兩個分支都給出 String,型別一致,match 才成立。" },
          { code: "        }", note: "分支區塊結束。" },
          { code: "    };", note: "match 結束,name 得到 \"guest\"。" },
          { code: "    println!(\"hello, {}\", name);", note: "印出 hello, guest。" },
          { code: "}", note: "main 正常結束——沒有 unwrap、沒有 panic、沒有 try/catch,程式優雅降級繼續走。" },
        ],
        outro: "這題是基礎篇錯誤處理哲學的縮影:錯誤是值,用 match 接住、印個警告、給預設值。C# 要達到同樣效果得 try/catch 包住再賦預設值,錯誤路徑與正常路徑分家;Rust 的 match 把兩條路寫在同一個運算式裡。",
      },
      csharp: `C# 要達到同樣效果得 try/catch 包住再賦預設值,或設計 TryFindUser(out ...)——錯誤路徑與正常路徑分家。Rust 的 match 把兩條路寫在同一個運算式裡,「這個值怎麼來的」一目瞭然。`,
    },
    {
      id: "1-16-08",
      question: "【綜合】方法 + 借用規則。以下程式碼的結果是?",
      questionCode: "struct Inventory {\n    items: Vec<String>,\n}\n\nimpl Inventory {\n    fn first(&self) -> Option<&String> {\n        self.items.get(0)\n    }\n\n    fn add(&mut self, item: String) {\n        self.items.push(item);\n    }\n}\n\nfn main() {\n    let mut inv = Inventory {\n        items: vec![String::from(\"sword\")],\n    };\n    let f = inv.first();\n    inv.add(String::from(\"shield\"));\n    println!(\"{:?}\", f);\n}",
      options: [
        { text: "編譯錯誤:f 保存著來自 first(&self) 的借用,add(&mut self) 需要可變借用,兩者衝突" },
        { text: "印出 Some(\"sword\"):讀第一項和加新項目互不干擾" },
        { text: "印出 Some(\"shield\")" },
        { text: "執行期 panic:f 指向的資料已因擴容失效" },
      ],
      answer: 0,
      explanation: `追蹤借用鏈:first(&self) 回傳 Option<&String>——省略規則第三條(lesson1-13),回傳參考綁定 &self,所以「f 活著 = inv 被不可變借用中」。下一行 add(&mut self) 要可變借用,而 f 在 println 還要用——讀寫衝突,編譯錯誤。
這正是 lesson1-9「持有元素參考時 push」的方法版:push 可能擴容搬家,f 會懸空,借用檢查器在編譯期擋下(所以「執行期失效」的選項永遠沒機會發生)。修法:先 println!("{:?}", inv.first()) 用完再 add,或 add 之後再取。`,
      walkthrough: [
        {
          label: "X Question code (won't compile)",
          lines: [
            { code: "struct Inventory {", note: "定義型別。" },
            { code: "    items: Vec<String>,", note: "擁有一個集合。" },
            { code: "}", note: "struct 定義結束。" },
            { code: "", note: "" },
            { code: "impl Inventory {", note: "行為區塊。" },
            { code: "    fn first(&self) -> Option<&String> {", note: "關鍵:回傳的是「參考」。依省略規則第三條(lesson1-13),它綁定 &self 的生命週期——換句話說,只要回傳值還活著,inv 就處於「被不可變借用」的狀態。" },
            { code: "        self.items.get(0)", note: "get 回傳 Option<&String>,安全取值。" },
            { code: "    }", note: "方法結束。" },
            { code: "", note: "" },
            { code: "    fn add(&mut self, item: String) {", note: "接收者是 &mut self:呼叫它需要對 inv 建立「可變借用」。" },
            { code: "        self.items.push(item);", note: "push 可能觸發擴容搬家。" },
            { code: "    }", note: "方法結束。" },
            { code: "}", note: "impl 結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let mut inv = Inventory {", note: "建立實例,宣告 mut 以便呼叫 add。" },
            { code: "        items: vec![String::from(\"sword\")],", note: "初始內容。" },
            { code: "    };", note: "實例建立完成。" },
            { code: "    let f = inv.first();", note: "不可變借用開始,而且會延續到 f 的最後一次使用。" },
            { code: "    inv.add(String::from(\"shield\"));", note: "⛔ 編譯失敗:cannot borrow `inv` as mutable because it is also borrowed as immutable。這正是 lesson1-9「持有元素參考時 push」的方法版。" },
            { code: "    println!(\"{:?}\", f);", note: "正是這一行讓不可變借用活過 add,衝突因此成立。" },
            { code: "}", note: "main 結束。" },
          ],
          outro: "借用檢查器在編譯期就擋下了——push 擴容會讓 f 指向的舊位置失效,所以「執行期才失效」的情況永遠沒機會發生。",
        },
        {
          label: "√ Correct version (stagger the two borrows)",
          lines: [
            { code: "fn main() {", note: "struct 與 impl 完全不用改。" },
            { code: "    let mut inv = Inventory {", note: "建立實例。" },
            { code: "        items: vec![String::from(\"sword\")],", note: "初始內容。" },
            { code: "    };", note: "實例建立完成。" },
            { code: "    println!(\"{:?}\", inv.first());", note: "改動處:借用取得後立刻用完,不可變借用在這一行結束(NLL)。印出 Some(\"sword\")。" },
            { code: "    inv.add(String::from(\"shield\"));", note: "此刻沒有任何存活的借用,可變借用完全合法。" },
            { code: "    println!(\"{:?}\", inv.first());", note: "add 之後重新取一次參考,再印一次,同樣是 Some(\"sword\")。" },
            { code: "}", note: "inv 離開作用域並釋放。" },
          ],
        },
      ],
      csharp: `C# 版(List<string> + First() + Add())毫無阻力地編譯執行——大多數時候也真的沒事,直到某天在迭代中 Add 或多執行緒下共享,才以執行期例外或資料錯亂的形式討債。Rust 把債務在編譯期結清。`,
    },
    {
      id: "1-16-09",
      question: "【綜合】所有權 + 泛型 + trait bound。以下程式碼的結果是?",
      questionCode: "fn largest<T: PartialOrd + Copy>(list: &[T]) -> T {\n    let mut largest = list[0];\n    for &item in list {\n        if item > largest {\n            largest = item;\n        }\n    }\n    largest\n}\n\nfn main() {\n    let numbers = vec![34, 50, 25, 100, 65];\n    let words = vec![String::from(\"hi\"), String::from(\"yo\")];\n    println!(\"{}\", largest(&numbers));\n    println!(\"{}\", largest(&words));\n}",
      options: [
        { text: "編譯錯誤:String 不滿足 Copy bound,largest(&words) 那次呼叫不合法;數字那次沒問題" },
        { text: "印出 100 和 yo:兩次呼叫都正常" },
        { text: "編譯錯誤:Vec 不能傳給 &[T] 參數" },
        { text: "印出 100 後,words 因為被 move 而執行期 panic" },
      ],
      answer: 0,
      explanation: `簽名要求 T: PartialOrd + Copy(能比較「且」能複製——函式體的 largest = list[0] 要把元素複製出來)。i32 兩者都滿足;String 能比較但不是 Copy(lesson1-4 的老朋友),largest(&words) 編譯錯誤,訊息會明說 String: Copy 不成立。
&numbers(&Vec<i32>)傳給 &[i32] 靠 deref coercion 自動轉(lesson1-6 &String→&str 的同族機制)。順帶:把簽名改成回傳 &T 就能去掉 Copy bound,String 版也能用——bound 的鬆緊跟著實作需求走。`,
      walkthrough: [
        {
          label: "X Question code (second call won't compile)",
          lines: [
            { code: "fn largest<T: PartialOrd + Copy>(list: &[T]) -> T {", note: "簽名要求兩個能力:PartialOrd(能比較)「且」Copy(能按位元複製)。為什麼要 Copy?因為函式體要把元素「複製出來」。" },
            { code: "    let mut largest = list[0];", note: "這一行就是 Copy 的來源:把元素從 slice 裡複製一份出來當初始值。若 T 不是 Copy,這裡會是 cannot move out of index。" },
            { code: "    for &item in list {", note: "&item 這個模式把迭代出來的 &T 解構成 T 本身(同樣需要 Copy)。" },
            { code: "        if item > largest {", note: "這裡用到 PartialOrd。" },
            { code: "            largest = item;", note: "更新最大值。" },
            { code: "        }", note: "if 結束。" },
            { code: "    }", note: "迴圈結束。" },
            { code: "    largest", note: "回傳值(不是參考)。" },
            { code: "}", note: "函式結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let numbers = vec![34, 50, 25, 100, 65];", note: "元素是 i32。" },
            { code: "    let words = vec![String::from(\"hi\"), String::from(\"yo\")];", note: "元素是 String。" },
            { code: "    println!(\"{}\", largest(&numbers));", note: "合法:i32 同時滿足 PartialOrd 與 Copy。&Vec<i32> 靠 deref coercion 自動轉成 &[i32](和 lesson1-6 的 &String→&str 同族機制)。印出 100。" },
            { code: "    println!(\"{}\", largest(&words));", note: "⛔ 編譯失敗:the trait bound `String: Copy` is not satisfied。String 能比較,但它擁有 heap 資源,不可能是 Copy(lesson1-4 的老朋友)。" },
            { code: "}", note: "main 結束。" },
          ],
        },
        {
          label: "√ Correct version (return a reference instead, drop the Copy requirement)",
          lines: [
            { code: "fn largest<T: PartialOrd>(list: &[T]) -> &T {", note: "改動處:回傳 &T,bound 只留 PartialOrd——實作不再需要複製元素,約束自然可以放寬。bound 的鬆緊永遠跟著實作需求走。" },
            { code: "    let mut largest = &list[0];", note: "改動處:改成取「參考」當初始值,不搬動任何資料。" },
            { code: "    for item in list {", note: "改動處:不再解構,item 直接是 &T。" },
            { code: "        if item > largest {", note: "參考之間的比較同樣走 PartialOrd。" },
            { code: "            largest = item;", note: "更新指向。" },
            { code: "        }", note: "if 結束。" },
            { code: "    }", note: "迴圈結束。" },
            { code: "    largest", note: "回傳最大元素的參考。" },
            { code: "}", note: "函式結束。" },
            { code: "", note: "" },
            { code: "fn main() {", note: "程式進入點。" },
            { code: "    let numbers = vec![34, 50, 25, 100, 65];", note: "整數版。" },
            { code: "    let words = vec![String::from(\"hi\"), String::from(\"yo\")];", note: "字串版。" },
            { code: "    println!(\"{}\", largest(&numbers));", note: "印出 100。" },
            { code: "    println!(\"{}\", largest(&words));", note: "現在合法了,印出 yo。" },
            { code: "}", note: "兩個 Vec 離開作用域並釋放。" },
          ],
        },
      ],
      csharp: `C# 的 T Largest<T>(IList<T>) where T : IComparable<T> 對 string 直接可用——複製參考無需授權。Rust 的 bound 把「這個函式需要元素會什麼」寫到一根毛都不差:要比較(PartialOrd)、要複製(Copy),缺一個就編譯錯誤,而不是執行期才發現。`,
    },
    {
      id: "1-16-10",
      question: "基礎篇畢業題:以下對 Rust 核心心智模型的總結,正確的是?",
      options: [
        { text: "每個值有唯一擁有者,離開作用域即釋放;借用分共享唯讀與獨占可寫,編譯期檢查;「可能沒有」是 Option、「可能失敗」是 Result,都必須顯式處理——三套機制共同取代了 GC 與例外" },
        { text: "Rust 用背景執行緒做輕量 GC,所有權只是給編譯器的最佳化提示,違反了頂多變慢" },
        { text: "借用規則只在多執行緒程式中生效,單執行緒可以隨意同時讀寫" },
        { text: "unwrap 是處理 Option/Result 的標準方式,match 是給不熟悉函數式風格的人的替代語法" },
      ],
      answer: 0,
      explanation: `基礎篇的三根柱子:(1)所有權——唯一擁有者、move 語意、作用域結束即 drop(lesson1-4);(2)借用——多個唯讀「或」一個可寫、參考不活過資料(lesson1-5、1-13);(3)錯誤即值——Option/Result + match/? 強制處理(lesson1-8、1-10)。全部在編譯期執行,這就是無 GC、無例外、卻記憶體安全的完整拼圖。
Rust 沒有任何形式的 GC,所有權是硬規則不是提示;借用規則單執行緒同樣全額生效(1-5、1-9 一路都在單執行緒裡吃編譯錯誤);unwrap 是「明知可能炸」的快捷方式,match/? 才是正規軍。`,
      walkthrough: {
        label: "🔍 One program that revisits all three pillars",
        lines: [
          { code: "fn main() {", note: "程式進入點。" },
          { code: "    let a = String::from(\"own\");", note: "柱子一(所有權):這份 heap 資料有唯一的擁有者 a。" },
          { code: "    let b = a;", note: "柱子一:move 語意——所有權交給 b,a 立即失效,同一時間絕不會有兩個擁有者。" },
          { code: "    println!(\"{}\", b);", note: "只有 b 能用;碰 a 會編譯失敗。" },
          { code: "", note: "" },
          { code: "    let mut v = vec![1, 2, 3];", note: "柱子二(借用)登場。" },
          { code: "    let r1 = &v;", note: "柱子二:任意多個唯讀借用。" },
          { code: "    let r2 = &v;", note: "兩個讀者並存沒問題。" },
          { code: "    println!(\"{:?} {:?}\", r1, r2);", note: "讀者用完,借用在此結束(NLL)。" },
          { code: "    v.push(4);", note: "柱子二:或者「恰好一個」可寫借用——兩者互斥,而且參考絕不活過資料。這條規則在單執行緒同樣全額生效。" },
          { code: "", note: "" },
          { code: "    let maybe: Option<i32> = v.iter().copied().max();", note: "柱子三(錯誤即值):「可能沒有」寫進型別,而不是回傳 null。" },
          { code: "    match maybe {", note: "柱子三:必須顯式處理兩種情況,編譯器檢查窮盡性。" },
          { code: "        Some(m) => println!(\"max = {}\", m),", note: "有值的路徑,印出 max = 4。" },
          { code: "        None => println!(\"empty\"),", note: "沒有值的路徑,不能省略。" },
          { code: "    }", note: "match 結束。「可能失敗」則是 Result,用 match 或 ? 處理,同一套思路。" },
          { code: "}", note: "柱子一:b 與 v 離開作用域,在這一刻釋放。全部檢查都在編譯期完成——這就是無 GC、無例外、卻記憶體安全的完整拼圖。" },
        ],
        outro: "三個錯誤說法一起澄清:Rust 沒有任何形式的 GC,所有權是硬規則不是最佳化提示;借用規則在單執行緒同樣生效(它防的不只是資料競爭,還有迭代器失效與別名 bug);unwrap 是「明知可能炸」的快捷方式,match 與 ? 才是正規軍。",
      },
      csharp: `畢業寄語:C# 的直覺裡「物件隨便傳、null 檢查靠自律、錯誤靠 try/catch」,Rust 把這三件事全部搬進型別系統。接下來的進階篇(閉包、迭代器、智慧指標、並行、async)會不斷回扣這三根柱子——基礎越熟,進階越像自然推論而不是新知識。`,
    },
  ],
};
